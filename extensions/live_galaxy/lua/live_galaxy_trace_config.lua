return {
    enabled = false,
    attempt_id = "unset",
    max_frame_events = 64,
    version_diagnostic_enabled = false,
    capability_probe_enabled = false,
    capability_probe_attempt_id = "unset",
}
