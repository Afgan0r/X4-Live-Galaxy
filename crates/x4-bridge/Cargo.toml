[package]
name = "x4-bridge"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
observation-application = { path = "../observation-application" }
observation-domain = { path = "../observation-domain" }
observation-ingest = { path = "../observation-ingest" }
observation-persistence = { path = "../observation-persistence" }
x4-carrier-native = { path = "../x4-carrier-native" }

[target.'cfg(windows)'.dependencies]
interprocess = "=2.4.3"
recvmsg = "=1.0.0"
