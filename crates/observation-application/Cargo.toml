[package]
name = "observation-application"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
observation-domain = { path = "../observation-domain" }
observation-ingest = { path = "../observation-ingest" }
observation-persistence = { path = "../observation-persistence" }

[dev-dependencies]
rusqlite = { version = "=0.40.2", default-features = false, features = ["bundled"] }

[lints]
workspace = true
