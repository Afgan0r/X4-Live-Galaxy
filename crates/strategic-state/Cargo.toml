[package]
name = "strategic-state"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
observation-domain = { path = "../observation-domain" }
observation-ingest = { path = "../observation-ingest" }
serde = { version = "=1.0.229", features = ["derive"] }
