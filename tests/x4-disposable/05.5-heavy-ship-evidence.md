# Phase 05.5 heavy-ship evidence

## Gate A

Gate A is accepted for source revision
`f4465ef48f71b74e79f93dd9bafb7954ad13935a`. Normal active average FPS was
184.671 and SETA active average FPS was 129.260. Both windows reported a 4 ms
maximum callback, no rejected or failed outcome, and independent previous/current
readback for every core, cargo, crew, and loadout section. Exact private logical
artifact IDs and locator digests remain in `05.5-EXECUTION-REVISION.md`.

## Full-set local evidence

The `heavy-ship-full-set` local scenario uses the real Lua 5.1 host, owned native
DLL, named pipe, production receiver, and SQLite publication path with a fake X4
source. It proves:

- dynamically ordered census entries with included `argon`, `scaleplate`,
  `xenon`, and `khaak`, excluded `player` and four service factions, and
  blocking unknown `custom_mod`;
- eleven streamed census entries crossing the 9-to-10 string-ID boundary,
  with exact ordered IDs in independent earlier/current SQLite readback;
- ordinary, small pirate, XEN, and KHK observation on the same scheduler path;
- three core rotations per included faction and at least two detail revisions per
  included nonempty faction;
- a partial zero-record XEN core without a known-empty claim or detail sections;
- one blocked scaleplate detail branch skipped without starving later factions;
- per-record core churn, exact faction-scoped dependencies, producer restart,
  varied census order, and independent earlier/current SQLite readback;
- no scheduling of excluded or unknown IDs.

This is local fixture-source evidence. It does not establish X4 completeness,
native getter latency, runtime performance, or Gate B.

## Gate B

The first owner-operated normal-speed attempt used package source `69de7da`
and logical run ID `055-gate-b-normal-20260924-120017`. It failed: zero
committed sections, eight rejected/failed outcomes, and four census attempts
that each stopped after nine received batches. The active-window average FPS
was 162.416, but a failed data window is not performance acceptance. Neither
the bridge-free control nor SETA window was performed. The 25 raw run/frame
files are retained in an owner-only local locator named by that run ID, with
each SHA-256 re-read and verified; no raw capture is in Git.

A native producer test failed at the tenth census ordinal before the fix with
`InvalidInput`. The certificate compares record IDs lexically; unpadded
`...:10` sorts before `...:9`. Census IDs now use the established 20-digit
ship-ordinal width. The producer test crosses both 9-to-10 and 99-to-100;
the local Lua/DLL/pipe/receiver/SQLite full-set path crosses 9-to-10 and
independently reads back two committed census revisions. These are local
regression results, not a replacement for a new owner-operated Gate B run.

The census correction passed independent scoped review, final Rust/Lua/full-set
regression, package self-test, and evidence self-test. A subsequent native
initial-connect reconnect race failed Windows CI run `35960863699`; a native
pipe test reproduced the generation-1 timeout before correction and passed
afterward. CI run `35962088554` passed on source
`b9a16c15b6f211bc65e9964944d9dc4f2dd36ce7`. Its package passed local
full-set and recovery chains and is installed: all 27 extension files match
manifest SHA-256
`76bb4bd5b396ed926afb69c33ead137b7fef30720c184104262d893e04157882`;
the previous installed set was backed up. Status: failed first attempt;
affected in-game reconnect recheck and corrected-package owner normal/SETA
Gate B rerun remain pending. This package has no X4 runtime acceptance yet.
