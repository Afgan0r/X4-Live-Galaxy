# Phase 05.4 Carrier B Evidence Ledger

## Status

Status: READY FOR USER X4 CHECKPOINT.

Automated Plan 02, Plan 03, and Plan 04 evidence is complete. Plan 05 X4 and
sustained SETA evidence is pending. Runtime acceptance is not claimed.

## Candidate Identity

<!-- markdownlint-disable MD013 -->

| Item | Frozen value |
| --- | --- |
| Package path | `dist/live-galaxy-carrier-b` |
| Logical ID | `live-galaxy-carrier-b:d12d78f030be071505290503fe990d6ab720b7c4bf21a7ba755a5aeb39e31bd5` |
| Source revision | `d628962cd8c90b29724f947fc52d67346402ab8f` |
| Manifest SHA-256 | `d12d78f030be071505290503fe990d6ab720b7c4bf21a7ba755a5aeb39e31bd5` |
| Limits SHA-256 | `440a55d8a0aed233f29fca14a6e06482c5880a0655f5beb98675f075303f4748` |
| Native SHA-256 | `7aea7def83d9066db3c29504cec441e571d888760e8e8f1ce68c671c1602a487` |
| Bridge SHA-256 | `ee31978d37b07f2cfd8fc70977b261417b7f1d7a06fad96096ed94a0b0aec5b8` |
| Contract tuple | ABI 2 / control 3 / envelope 2 / semantics 1-2-3-1 |

<!-- markdownlint-enable MD013 -->

## Local Measurement

Workload: one actual Lua 5.1 getter, one typed fact, the actual release DLL
producer, one start/batch/completion sequence, the same-user named pipe, the
release bridge, SQLite publication, and independent bridge readback. Values are
single bounded qualification observations, not performance guarantees.

<!-- markdownlint-disable MD013 -->

| Scenario | Elapsed | Host peak | Bridge peak | SQLite | History | Result |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Bridge first | 5,314 ms | 6,152,192 B | 5,914,624 B | 90,112 B | 2,097 B | committed/read back |
| Native first | 402 ms | 6,000,640 B | 5,914,624 B | 90,112 B | 1,877 B | committed/read back |
| Five-cycle sustained | 20,822 ms | 6,135,808 B | 5,939,200 B | 90,112 B | 5,492 B | revisions 1 through 5/read back |
| Two-process restart | 5,732 ms | 6,111,232 B | 5,939,200 B | 90,112 B | 4,677 B | revisions 1, 2, 3/read back |
| Pending I/O close/reload | 747 ms | 5,935,104 B | 5,734,400 B | 90,112 B | 977 B | terminal/unloaded/fenced |

<!-- markdownlint-enable MD013 -->

The first three predecessor-candidate measurements used config digest
`440a55d8a0aed233f29fca14a6e06482c5880a0655f5beb98675f075303f4748`,
native digest
`79bc1c0102e4bedb25673607868666987d8e07da50459a6beab036dfa43052b8`,
and exactly one getter call. Bridge-first and native-first independently
reopened revision 1 from SQLite. The pending-I/O scenario killed only the
runner-owned bridge, closed the Lua state, observed DLL unload, rejected the old
token with `-16`, accepted a different fresh token in a new Lua state, and
observed the second unload.

The attempt-11 control-v3 candidate passed five same-process collection cycles
on one producer session and connection with zero `peer-inactive` diagnostics.
It then passed the two-process restart scenario with native digest
`7aea7def83d9066db3c29504cec441e571d888760e8e8f1ce68c671c1602a487`,
three getter calls, six clock calls, exact ordered commits 1, 2, and 3 across
two producer sessions, zero permanent rejections, and durable revision 3
readback. The bridge digest was
`ee31978d37b07f2cfd8fc70977b261417b7f1d7a06fad96096ed94a0b0aec5b8`.

The effective measured limits were 2,048-byte complete messages, 512-byte
control messages, one native slot/record/batch, 4,096 pending bytes, 8,192 total
bytes/lifecycle work, two delivery and reconnect attempts, 25 ms reconnect
delay, and a 5,000 ms conditional availability gate. Boundary and one-over
rejections are covered by the contract suites; metadata is not substituted for
the native projection.

## Automated Qualification

<!-- markdownlint-disable MD013 -->

| Gate | Result |
| --- | --- |
| TDD package RED | `0a5f23a`: retired Carrier A dependency and missing owned bundle were rejected |
| Package GREEN | `d518d18`: AMD64 PE32+, sole export, exact layout/XML, strict versions/limits, hashes, tamper rejection, atomic preservation |
| Actual-path RED | `6dc912e`: missing real module/bridge/SQLite path failed intentionally |
| Exact receipt RED | `8d862b3`: real control history reproduced `4` then `-20` for the mismatched batch ID |
| Pending unload RED | `f460003`: real `lua_close` reported `native image remained loaded` |
| Actual-path GREEN | `c9c7dba`: real DLL/Lua/IPC/bridge/SQLite chain, both startup orders, exact receipt, pending close/unload, stale-generation fence |
| Final limits | `df0a806`: exact strict config and retained time/memory/storage measurements |
| Review repair | `6181cff` through `df0a806`: callback, transport recovery, bounded diagnostics, exact recovery and actual-path assertions |
| Rust focused | ABI 7/7, lifecycle/transport 7/7, producer 6/6, recovery 3/3, startup 7/7 |
| Lua focused | Aggregate Carrier B contracts 51/51 and syntax 9/9 |
| Real bridge | Wire 5/5, contract 5/5, publication 3/3, recovery 3/3, rejection/restart matrix 1/1 |
| Package self-test | PASS against the frozen config and package |
| X4 loader hotfix | `f8a1682`: five local imports use the observed extension-qualified form; restricted loader RED/GREEN and revert/reconfirm passed |
| Hotfix regression | Aggregate contracts 50/50 Lua and 9/9 syntax; actual local bridge-first/native-first/pending-unload; full Rust workspace, Clippy, format, and source-size passed |
| Revision restart repair | `f0e56d5`: control v3 carries the durable next revision; repeated collection and a distinct producer process commit exact revisions 1, 2, and 3 |
| Recurring demand repair | `c1f9e09`: the bridge issues post-cadence demand on one connection; game-load boundary remains latched until admission |
| Install guard repair | `d628962`: preflight requires one recurring 50 ms observation cue and permits the one-shot game-load cue |
| Final rereview | CLEAN after exact SQLite exhaustion and independent v1/v2 bootstrap rejection tests |

<!-- markdownlint-enable MD013 -->

## Mutation Gate

The runner was explicitly prepared as reviewed `cargo-mutants 27.1.0`, pinned
and installed with its lockfile after crates.io metadata identified the MIT
package and `sourcefrog/cargo-mutants` upstream.

The first targeted run selected eight `batch_identity` and `claim_values`
mutants. It reported two caught and six unviable because the replacement made
imports or parameters unused under repository `deny(warnings)`. Inspection of
all six logs showed warning-only compile rejection, not survivors. Repeating
the same eight operators with `--cap-lints true` produced eight caught, zero
missed, zero timeout, and zero unviable mutants in 27 seconds. No score is
generalized beyond this targeted set.

A bounded Lua operator kept the close guardian armed after successful explicit
close. The existing focused test failed with two native close calls instead of
one, so the useful operator was caught. No replacement Lua mutation engine or
native/MD mutation score was invented.

## Focused Security Review

<!-- markdownlint-disable MD013 -->

| Surface | Disposition |
| --- | --- |
| Native lifetime and C ABI | Panic boundary, owned buffers, terminal close, no Lua callback from worker, and real pending unload pass. No unresolved high finding. |
| Reload generation | Process/DLL-load-scoped nonzero OS seed advances through a checked environment value; malformed, zero, and exhaustion reject; old token rejects after real unload. No wrap or probabilistic same-process collision claim. |
| Peer and control authority | Same-logon pipe and remote-client refusal remain tested; ABI/control/envelope mismatch, stale epoch, extra fields, mutation kinds, malformed and oversized traffic fail closed. Durable floors cannot lower the producer revision and values outside SQLite's signed range reject before collection. |
| Exact receipt and retry | Expected batch identity is decoded from the actual canonical batch; retained bytes drive bounded retry; duplicate publication and ambiguous response paths remain tested. |
| Loader and package | Exact owned path, AMD64 PE32+, sole initializer export, dependency-free XML, complete hash manifest, tamper rejection, and no hot replacement. |
| Persistence and diagnostics | Independent SQLite reopen succeeds; public status remains bounded and excludes prompts, secrets, raw private payloads, and machine-local paths. |

<!-- markdownlint-enable MD013 -->

No new network endpoint, authentication path, schema, mutation authority, or
game-state action surface was introduced. No unresolved high security finding
enters the user checkpoint.

## Runtime Attempt Log

- A first launch loaded a pre-existing disposable test save before B1 began.
  It is retained as an invalid attempt and supplies no acceptance evidence.
- B1 attempt 1 installed the old candidate under the profile-local extension
  directory. X4 loaded `ui.xml` but rejected the slash-qualified Carrier
  import; the bridge remained `peer-absent`. The attempt failed and is retained.
- B1 attempt 2 moved the unchanged old candidate to the game-root extension
  directory. X4 emitted the identical module-not-found error before native
  initialization, eliminating installation root as the sole cause. The attempt
  failed and is retained.
- The loader defect was fixed in source revision `f8a1682`. Attempt 10 used the
  control-v3 candidate from `f0e56d5`: revisions 1 through 3 persisted, but
  later collection depended on peer-inactivity reconnects and ended in a
  lifecycle rejection. The attempt failed and is retained.
- Attempt 11 is frozen at source revision `d628962`. The bridge now issues a
  fresh demand after each five-second cadence on the same connection while the
  MD seam keeps one bounded 50 ms pump and a one-shot game-load boundary. It
  has not yet been installed or retried in X4. No save file was read or
  modified by an agent.

## Pending User Matrix

| Evidence | Status |
| --- | --- |
| B1 bridge-first X4 startup | pending |
| B2 X4/native-first startup | pending |
| B3 absent bridge and compatible restart | pending |
| B4 lost response and exact reconciliation | pending |
| B5 incompatible candidate rejection | pending |
| B6 60-second bridge absence and recovery | pending |
| B7 disposable game load and fresh baseline | pending |
| B8 single UI reload and incarnation fence | pending |
| B9 repeated UI reload with pending teardown | pending |
| 10-real-minute ordinary window | pending |
| 20-real-minute sustained SETA window | pending |

The exact operations, deadlines, stop conditions, retention fields, and
readback command are in `05.4-carrier-b-procedure.md`. Installation and two
failed loader probes occurred with the user's authorization; no agent operated
game controls, used SETA, or accessed saves.
