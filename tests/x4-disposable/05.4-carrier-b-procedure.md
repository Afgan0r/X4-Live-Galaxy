# Phase 05.4 Carrier B X4 Checkpoint Procedure

## Stop Boundary

This procedure is prepared for the user. Automated Plan 02, Plan 03, and Plan
04 qualification is complete. X4 runtime acceptance is not claimed. The
executor must stop before installation, X4 launch, game controls, or SETA.

Do not use an existing save. Use a disposable Creative Custom campaign with
vanilla, owned DLC, and Live Galaxy only. Do not read, copy, inspect, or modify
save files during evidence collection.

## Candidate Identity

- Package path: `dist/live-galaxy-carrier-b`
- Logical ID: `live-galaxy-carrier-b:6865832c1dffbca204c5ebc105a958cef62ba73296ac9751314394547ee78f4a`
- Source revision: `f0e56d5d192e2d58c4bfbbc38e9b3686e3d20eac`
- Manifest SHA-256: `6865832c1dffbca204c5ebc105a958cef62ba73296ac9751314394547ee78f4a`
- Limits SHA-256: `440a55d8a0aed233f29fca14a6e06482c5880a0655f5beb98675f075303f4748`
- Native SHA-256: `7aea7def83d9066db3c29504cec441e571d888760e8e8f1ce68c671c1602a487`
- Bridge SHA-256: `ee8b6e0eee026dbc59b997843349292473e63ed40895102b9bc9d65968354fd9`
- Contract tuple: native ABI 2, control 3, envelope 2, semantic
  `1/2/3/1`

Do not use the candidate if any digest differs. Do not hot-replace its DLL or
Lua files while X4 is running.

## Effective Limits

| Boundary | Candidate value |
| --- | ---: |
| Complete/control message | 2,048 / 512 bytes |
| Candidate raw bytes/records/batches/work | 2,048 / 1 / 1 / 2,048 |
| Message age/inactivity | 5,000 / 5,000 ms |
| Aggregate candidates/bytes/records/batches/work | 2 / 4,096 / 2 / 2 / 4,096 |
| Publication records/content | 1 / 2,048 bytes |
| Pending/total retained bytes | 4,096 / 8,192 bytes |
| Lifecycle work | 8,192 |
| Delivery attempts/blockers | 2 / 4 |
| Reconnect attempts/delay | 2 / 25 ms |
| Availability gate | 5,000 ms |
| Native callback slots/records/batches/work | 1 / 1 / 1 / 2,048 |

The five-second availability value is a conditional response bound after a
callback, not the X4 callback cadence. The packaged MD seam requests a
best-effort 50 ms game-time pump. Native admission exposes capacity to Lua;
without bridge-issued intent and demand, pump callbacks do not invoke the
observation clock or getter. Runtime scenario deadlines remain conservative
end-to-end recovery bounds rather than callback-period guarantees.

## One-Time Preparation

1. Verify all candidate digests while X4 is closed.
2. Copy only `dist/live-galaxy-carrier-b/extensions/live_galaxy` to
   `<X4-game-root>/extensions/live_galaxy`. Do not use the profile-local user
   extensions directory: Carrier B's native image and X4 module names are
   game-root-relative. Keep the package itself unchanged as the source copy.
3. In X4 extension settings, enable Live Galaxy and disable Protected UI.
   Disabling Protected UI is required for the native UI module and may disable
   protected/online features; this loss is accepted by ADR-LG-009.
4. Create a new private evidence directory outside the repository and outside
   the X4 installation. Restrict it to the current user. Use it as
   `<private-data-dir>` below.
5. Retain a copy of `manifest.json`, `carrier-b-limits.json`, and all bridge
   output in that directory. Never retain a private artifact only in a
   temporary directory.

The external bridge invocation is the same in PowerShell, Command Prompt, and
other shells when each placeholder is replaced by one quoted absolute path:

<!-- markdownlint-disable MD013 -->

```text
live-galaxy-bridge.exe --data-dir <private-data-dir> --limits-file carrier-b-limits.json
```

Offline readback must run only after the bridge is stopped:

```text
live-galaxy-bridge.exe --readback --data-dir <private-data-dir> --section-key carrier_b_realtime_sample --section-revision <revision>
```

<!-- markdownlint-enable MD013 -->

Expected readback is one typed record with getter `GetCurRealTime`, semantics
`opaque_runtime_number`, the requested revision, and a receipt. It must contain
no action or mutation command.

## Scenario Matrix

Run every row in a new disposable campaign or after the restart instruction in
the row. Record wall-clock timestamps, bridge output, the tested revision, and
the exact offline readback.

<!-- markdownlint-disable MD013 -->

| ID | User operation | Deadline and expected result | Stop condition |
| --- | --- | --- | --- |
| B1 | Start the bridge, then start X4 and the disposable campaign. | First committed revision within 35 real seconds after the campaign becomes controllable; exactly one typed fact in readback. | Stop immediately on crash, freeze, mutation-shaped traffic, digest mismatch, or no commit by 35 seconds. |
| B2 | Fully exit X4. Start X4 and the campaign first; after it is controllable, start the bridge. | Connection and first commit within 35 real seconds of bridge start; no repeated getter for one revision. | Stop on the B1 conditions or more than one record in a revision. |
| B3 | With a compatible session, stop the bridge for 60 real seconds, then restart the same bridge against the same private directory. | X4 remains responsive while absent; a new commit appears within 35 seconds of restart; existing committed revision remains independently readable and no revision is duplicated. | Stop if X4 blocks, the bridge mutates an existing committed revision, or recovery exceeds 35 seconds. |
| B4 | Stop the bridge immediately after a callback is observed, wait 10 real seconds, then restart it. | The exact pending identity is reconciled or retried within the two-attempt bound; at most one committed copy exists. | Stop on duplicate publication, a third delivery attempt, or recovery beyond 35 seconds. |
| B5 | With X4 closed, make a disposable copy of the package and change one ABI/control/envelope version or one limits field. Run package self-test against that copy; do not install it. | The altered copy is rejected before X4 launch. Restore the untouched candidate for later rows. | Stop if the altered package is accepted. |
| B6 | Start X4 without the bridge and leave it absent for 60 real seconds. | X4 remains responsive for two callback intervals; no game-state action occurs. Start the bridge and require recovery within 35 seconds. | Stop on freeze, crash, unbounded log growth, action traffic, or missed recovery deadline. |
| B7 | In a fresh disposable campaign, allow one revision to commit, create a disposable in-game save, then load that same disposable save through X4. Do not inspect or manipulate the save file outside X4. | The pre-load revision remains readable as history, unpublished work is discarded, and one fresh baseline commits within 35 real seconds without reusing the old producer incarnation. | Stop on stale-current eligibility, duplicate publication, crash, freeze, or failure to publish the fresh baseline by 35 seconds. |
| B8 | After a committed revision, invoke one X4 UI reload without changing any installed file or running bridge binary. | The old Lua handle/result is fenced, the compatible bridge remains bounded, and one fresh baseline commits within 35 real seconds; the prior committed revision remains readable as history. | Stop on stale-handle acceptance, duplicate publication, crash, freeze, hot-replacement prompt, or missed deadline. |
| B9 | Repeat the UI reload twice. On the second reload, trigger it only after the bridge reports a pending start, batch, or completion. | Each Lua incarnation is distinct; incomplete work is discarded; every accepted fresh baseline publishes at most once; prior committed revisions remain readable and the final baseline commits within 35 real seconds. | Stop on a stale callback changing current state, duplicate publication, third delivery attempt, crash, freeze, or missed deadline. |

<!-- markdownlint-enable MD013 -->

## Ordinary Window

After B1 through B9 pass, run at normal speed for 10 real minutes. Sample once
per real minute and retain the current bridge output and an offline readback at
minutes 1, 5, and 10. Pass only if:

- X4 remains responsive and the bridge remains bounded;
- at least 18 of the expected 20 callback opportunities commit a revision;
- every committed revision has exactly one record and one receipt;
- no duplicate revision, third delivery attempt, action, or mutation appears;
- process working sets do not show monotonic unbounded growth across all ten
  samples.

Stop immediately on any failed condition. Fully exit X4 before changing the
installed candidate or any DLL/Lua/ABI/contract file.

## Sustained SETA Window

Only after the ordinary window passes, run SETA for 20 real minutes. Sample
once per real minute and retain offline readbacks at real minutes 1, 5, 10, 15,
and 20. Pass only if:

- the game remains controllable and exits SETA normally on user input;
- every sample shows finite bridge/storage growth and no stalled callback for
  more than 35 real seconds;
- each readable revision has exactly one typed fact and one receipt;
- bridge restart once at real minute 10 recovers within 35 seconds without a
  duplicate publication;
- no mutation operation, third delivery attempt, raw prompt, secret, or
  machine-local path appears in public diagnostics.

Stop SETA and exit X4 immediately on a crash, freeze, repeated missed deadline,
duplicate publication, or any mutation-shaped traffic.

## Restart Rules

- The compatible external bridge may restart independently.
- A bridge binary, limits, or protocol change requires stopping the old bridge
  before replacement.
- Any loaded DLL, Lua, ABI, or game-facing contract change requires a full X4
  exit and restart. Never hot-replace the native image.
- Do not use UI reload, save reload, or a save file as a substitute for a full
  process restart in this checkpoint.

## Evidence Handoff

Record each row in `05.4-carrier-b-evidence.md` as observed or failed. Include
the package logical ID, all four digests, exact wall-clock durations, startup
order, revision identities, readback text, working-set samples, and stop reason.
Do not mark runtime acceptance complete until every B row and both timed
windows pass.
