---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 02
type: tdd
wave: 1
depends_on: [05.1-01]
files_modified:
  - extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
  - extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/tests/component_discovery_contract.lua
  - extensions/live_galaxy/tests/component_discovery_binding_contract.ps1
  - extensions/live_galaxy/tests/x4_discovery_contract.lua
  - crates/observation-ingest/tests/runtime_facts_contract.rs
autonomous: true
requirements: [OBS-06]
requirement_note: "No new phase requirement ID exists. This is bounded D-09 evidence closure strengthening inherited OBS-06 behavior."
spec_less_probe_fallback: "skipped — no newly assigned requirement IDs; manual X4 evidence is tracked under D-09"
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One owner-scoped station class yields a deterministic real-component observation only when its entire validated count fits the named hard maximum and count, fill, identity, metadata, sector, ownership, and capacity facts all validate, per D-09."
    - "An unavailable, overflowed, partial, malformed, or unsupported component source emits no observation or completion marker and leaves Rust's previous accepted projection intact."
    - "The existing framing, protocol negotiation, reconnect behavior, strict admission, and telemetry-only authority boundary remain unchanged."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      provides: <=200-line injected native station enumeration adapter with bound-first validation and canonical facts.
    - path: extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
      provides: Existing discovery orchestration connected to the real-component adapter without transport changes.
  key_links:
    - from: live_galaxy_component_discovery.lua
      to: live_galaxy_x4_discovery.lua
      via: Existing read_observation/list_scope seam forwards one complete component observation or an explicit failure.
      pattern: read_observation
    - from: live_galaxy_x4_discovery.lua
      to: observation-ingest runtime facts
      via: Existing telemetry serializer emits the established compact s/x/c/o envelope.
      pattern: runtime_facts
  prohibitions:
    - category: mutation
      statement: "No report, acknowledgement, command, effect, persistence, model, public UI, or game-state mutation capability belongs in this plan."
    - category: evidence
      statement: "Passing local contracts cannot close the human-only native X4 availability or cost assumption."
---

<objective>
Implement and prove one production-quality station-only tracer through the current
Lua telemetry and Rust admission path.

Purpose: replace the failed sector-as-capacity target with real component facts without
expanding the protocol, authority, or declared runtime scope.
Output: bounded injected component adapter and end-to-end local contract evidence.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-RESEARCH.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-PATTERNS.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED-to-GREEN end-to-end bounded real-station discovery</name>
  <files>extensions/live_galaxy/lua/live_galaxy_component_discovery.lua, extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua, extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/tests/component_discovery_binding_contract.ps1</files>
  <read_first>Read the component-adapter and orchestration assignments in 05.1-PATTERNS.md, then read the completed Wave 0 contract file. Read live_galaxy_x4_discovery.lua and live_galaxy_runtime.lua once for injected APIs, runtime-health fallback, serializer use, protocol generation, and reconnect boundaries.</read_first>
  <behavior>
    - Test 1: one predeclared faction-station scope validates a raw count before any allocation, conversion, metadata, ownership, sector, or capacity operation.
    - Test 2: valid filled handles become sorted, opaque, real station assets and every member of the declared owner-scoped result receives required component reads only when the validated count fits the hard maximum.
    - Test 3: every count/fill/conversion/component failure is classed, produces no observation or marker, and retains telemetry health-only behavior.
    - Test 4: a static binding contract proves the production module binds C.GetNumAllFactionStations, C.GetAllFactionStations, stable-ID conversion through ConvertIDTo64Bit as needed, GetComponentData(component, "owner", "sector"), and C.GetPeopleCapacity behind protected calls without claiming native behavior.
    - Test 5: a complete declared bounded scope emits the existing canonical telemetry envelope without changing hello, generation, sequence, or reconnect behavior.
  </behavior>
  <action>First add the focused fake-native and static-binding cases, run the component-discovery suite, and record the expected failure caused by the absent component adapter. Then implement original code in a new cohesive Lua module of at most 200 physical lines, following the injected API, protected-call, named-limit, sorted-candidate, and fact-shape analogs in 05.1-PATTERNS.md. The runtime API binding must expose count_stations through pcall(C.GetNumAllFactionStations, faction_id), fill_stations through pcall(C.GetAllFactionStations, buffer, count, faction_id), stable identity through the required ConvertIDTo64Bit conversion path, owner and sector through GetComponentData(component, "owner", "sector"), and capacity through pcall(C.GetPeopleCapacity, component64, empty context, false). Validate count and fill return shapes as finite integral values; allocate the FFI buffer only after the raw count is valid and no greater than one named hard maximum. A count over that maximum returns enumeration_overflow before allocation, conversion, or component reads. A successful count declares the entire owner-scoped station result: validate, canonically sort, and read every returned member; do not create a prefix, cursor, or multi-cycle contract for less than that complete result. Classify each protected-call failure without raw native text. Build the observation only after every member validates, wire it into the existing discovery seam and serializer, and preserve hello, generation, sequence, reconnect, and runtime-health behavior. A failed or unsupported component read emits health-only frames with no completion marker; this minimal runtime correction is required for the plan's no-partial-update invariant. The static contract proves source binding only; local passing status remains pending-X4 for native availability, cost, and owner normalization. A failed human probe retains facts_unsupported and requires a separately authorized revision rather than automatic scope widening. Use D-09 in the public module contract; do not add a second schema, Lua-side reconciliation, a global component scan, synthetic sector assets, or an authority-expanding route.</action>
  <verify><automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery</automated></verify>
  <done>The executor has observed the new focused case fail, implemented the adapter, and rerun the same focused suite green. The local tracer produces one complete owner-scoped real-station observation only within the hard bound, rejects every invalid source stage before a completion marker, and remains pending-X4 for native behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm strict Rust acceptance for the tracer's exact envelope</name>
  <files>crates/observation-ingest/tests/runtime_facts_contract.rs, extensions/live_galaxy/tests/component_discovery_contract.lua</files>
  <read_first>Read the completed tracer test and the current runtime_facts_contract.rs cases. Use the batch_reconciliation.rs and compact envelope analogs identified in 05.1-PATTERNS.md; do not add a second wire format.</read_first>
  <behavior>
    - Test 1: the tracer's canonical station envelope is accepted by the existing strict Rust boundary and exposes the expected real asset relationships.
    - Test 2: reordering, duplicate identities, malformed relationships, missing completion, and unsupported Lua outcomes remain rejected without replacing the prior projection.
  </behavior>
  <action>Align the Wave 0 Lua and Rust fixtures on the existing compact runtime-facts envelope and add only evidence needed to demonstrate the produced tracer envelope crosses the current strict admission seam. Keep order and relationship assertions independent on both sides. Verify the rejected-outcome projection remains byte-for-byte or semantically equivalent to the prior accepted projection according to the current test helpers. Preserve all completion-marker behavior in Rust; this task must not alter production Rust admission, bridge protocol, persistence, report, acknowledgement, command, effect, model, or UI paths. Record the native-availability and native-cost assumption delta in test or plan-facing evidence metadata, not as a local passing claim.</action>
  <verify><automated>cargo test -p observation-ingest --test runtime_facts_contract --locked; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery</automated></verify>
  <done>Lua and Rust agree on one strict canonical station envelope, while every non-complete variant preserves the previous projection and no test claims that a native X4 call was observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| X4 UI-Lua native API → component adapter | External native count/fill and component reads can fail, over-report, or be costly. |
| component adapter → telemetry serializer | Only fully validated, bounded facts can enter existing framing. |
| serialized telemetry → observation ingest | Rust independently validates and atomically accepts or rejects the candidate projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-01 | Denial of service | count/fill adapter | high | mitigate | Named raw and per-cycle limits, with raw-count rejection before any costly work. |
| T-05.1-02 | Tampering | component identities and links | high | mitigate | Lua canonicalization plus independent strict Rust identity, order, and relationship rejection. |
| T-05.1-03 | Tampering | complete-scope reconciliation | high | mitigate | No marker on partial or unsupported discovery; rejected batches preserve the accepted projection. |
| T-05.1-04 | Elevation of privilege | telemetry seam | high | mitigate | Preserve the existing one-way protocol and phase-local package guard. |
| T-05.1-05 | Information disclosure | diagnostics | medium | mitigate | Class-only bounded errors; no raw IDs, native errors, payloads, prompts, or hidden reasoning. |
| T-05.1-SC | Tampering | package installs | high | accept | No package installation is planned. |
</threat_model>

<verification>
Run both focused contract commands. Inspect the resulting envelope and rejection cases for a real station asset identity, deterministic ordering, no marker on incomplete scope, preserved prior projection, and no added authority path. The X4 disposable probe remains pending human execution.
</verification>

<success_criteria>
One bounded station class crosses every existing local layer safely; native X4 availability and cost remain explicitly pending rather than inferred from fakes.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-02-SUMMARY.md` when done.
</output>
