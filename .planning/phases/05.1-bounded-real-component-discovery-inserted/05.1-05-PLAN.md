---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 05
type: tdd
wave: 3
depends_on: [05.1-03]
files_modified:
  - extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
  - extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_telemetry.lua
  - extensions/live_galaxy/tests/component_discovery_contract.lua
  - extensions/live_galaxy/tests/x4_discovery_contract.lua
  - extensions/live_galaxy/tests/component_discovery_binding_contract.ps1
  - crates/x4-bridge/src/server.rs
  - crates/x4-bridge/src/server/session_gate.rs
  - crates/x4-bridge/tests/named_pipe_contract.rs
  - tests/x4-disposable/05.1-component-discovery-probe.md
  - .planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
autonomous: false
gap_closure: true
requirements: [OBS-06]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A faction with hundreds of stations is not rejected merely because its total station count exceeds an arbitrary product cap."
    - "Native count/fill work and the Lua-to-bridge transport are bounded independently by measured time/bytes/queue budgets."
    - "A producer stops native work while its bounded FIFO is backlogged, and the bridge drains queued frames over later ticks without blocking the game thread."
    - "Only a verified complete owner-scoped generation can reconcile the faction; queue failure, reconnect, retry exhaustion, overflow, or incomplete native discovery leaves the prior projection intact."
    - "The selected transport envelope and cadence are justified by measured Live Galaxy evidence against the X4 MCP baseline, rather than copied from the MCP 256 KiB limit or existing 2 KiB constants."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      provides: "Measured native enumeration admission and incremental candidate production"
    - path: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      provides: "Bounded FIFO, cadence, producer pause, marker and reconnect state machine"
    - path: extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
      provides: "Station-only production orchestrator with no sector-as-asset fallback"
    - path: crates/x4-bridge/src/server/session_gate.rs
      provides: "Generation/sequence-aware staged reconciliation and no-partial failure handling"
    - path: crates/x4-bridge/tests/named_pipe_contract.rs
      provides: "Bridge drain, reconnect, idempotency, and atomic reconciliation contracts"
    - path: tests/x4-disposable/05.1-component-discovery-probe.md
      provides: "Disposable X4 measurement and streaming transport UAT procedure"
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      to: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      via: "A measured complete native owner generation becomes a bounded local candidate source; it never implies a completed bridge snapshot by itself."
      pattern: "generation"
    - from: extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
      to: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      via: "The production orchestrator selects only the injected real-station adapter; missing station enumeration yields health-only failure rather than a sector substitute."
      pattern: "new_runtime_adapter"
    - from: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      to: crates/x4-bridge/src/server/session_gate.rs
      via: "The Lua FIFO emits sequenced observation envelopes and one terminal marker only after the same generation drains."
      pattern: "complete_marker"
    - from: crates/x4-bridge/src/server/session_gate.rs
      to: crates/observation-ingest
      via: "The bridge invokes atomic observation admission only for a confirmed complete owner scope; every degraded path preserves the accepted projection."
      pattern: "complete_snapshot"
---

<objective>
Replace the superseded 64-station/single-aggregate design with a measured,
bounded incremental telemetry transport for complete owner-scoped station
discovery.

Purpose: close UAT gap G-05.1-1 without truncating an actual faction scope or
letting transport pressure turn an incomplete discovery into reconciliation.
Output: production-quality Lua and bridge streaming contracts, measured envelope
decision evidence, and a disposable X4 UAT procedure.
</objective>

<scope_fence>
This plan owns one existing station-discovery-to-bridge protocol seam and the
named focused contracts only. It must not introduce a second transport, a new
schema family, general X4 paging, global MCP completion semantics, return
messages, or scope outside the frontmatter file list.
</scope_fence>

<decision_record>
- D-05.1-TRANSPORT-01 (owner decision, 2026-08-30): Use the installed X4 MCP
  as a clean-room reference baseline for bounded FIFO, cadence,
  drain/retry/backpressure, and transport-envelope measurement. Do not copy its
  implementation, 256 KiB envelope, player-empire model, or global
  `assets.complete` semantics. Any divergence from the measured MCP baseline
  requires recorded Live Galaxy evidence.
- D-05.1-TRANSPORT-02 (owner decision, 2026-08-30): Existing 2 KiB Lua/bridge
  limits are prototype constants, not proven native or pipe requirements. The
  executor must measure and explicitly select, retain, or revise each envelope
  dimension before treating it as a production budget.
- D-05.1-TRANSPORT-03 (supersession): 05.1-04-PLAN.md's 64-station cap and
  aggregate-frame approach are not executable. This plan replaces them; a
  faction's total station count is not a product limit.
</decision_record>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-04-RESEARCH.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-RESEARCH.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-VERIFICATION.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-REVIEW.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one owner-scoped generation drains end-to-end</name>
  <files>extensions/live_galaxy/lua/live_galaxy_component_discovery.lua, extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/lua/live_galaxy_telemetry.lua, extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, crates/x4-bridge/src/server.rs, crates/x4-bridge/src/server/session_gate.rs, crates/x4-bridge/tests/named_pipe_contract.rs</files>
  <behavior>
    - A complete measured owner generation containing more than 64 stations is represented as sequenced bounded observation envelopes, drains over later ticks, and admits only after its terminal marker is confirmed.
    - Each tick performs a bounded amount of Lua queue/drain work; when the FIFO reaches its byte/message ceiling, no new native enumeration or component reads begin until backlog is below the documented resume threshold.
    - A write defer preserves the head envelope for retry. A disconnect, generation change, malformed envelope, sequence conflict, exhausted retry budget, or native-incomplete result discards the staged generation and preserves the previous accepted projection.
    - Duplicate delivery of a previously accepted envelope is idempotent; a completion marker identifies exactly one generation, owner scope, snapshot version, and terminal sequence.
    - A deterministic oracle keeps `fifo_enqueued_messages`, `fifo_enqueued_bytes`, `successful_local_write_messages`, `successful_local_write_bytes`, queue byte/message depths, and bridge-admitted generation count separate; a local write alone never increments bridge admission.
  </behavior>
  <action>Per D-05.1-TRANSPORT-01 through D-05.1-TRANSPORT-03, replace the owner-station count cap and single aggregate frame with a clean-room bounded FIFO protocol. Follow the component adapter pattern in `extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua`, the runtime/reconnect contract in `extensions/live_galaxy/tests/x4_discovery_contract.lua`, and the staged Rust admission pattern in `crates/observation-ingest/tests/batch_reconciliation.rs`. First write failing Lua and Rust contract cases for one complete generation that exceeds 64 stations but remains within the measured native allocation/time ceiling and FIFO envelope budgets. In `live_galaxy_x4_discovery.lua`, physically delete the legacy sector enumeration, sector capacity call, synthetic-sector asset construction, `legacy_new()` implementation, and every production/test selection path to it. Extend `component_discovery_binding_contract.ps1` with an explicit legacy-path-absence contract that fails when any removed construct remains. If station enumeration is absent, unavailable, or exceeds the measured native ceiling, production returns the existing explicit unsupported/health-only outcome before creating any observation. Separate three state machines: native enumeration admission (count/fill only when measured allocation and game-thread time budgets permit), producer queueing (fixed byte/message capacity, deterministic canonical station order, explicit head retry and resume hysteresis), and bridge staging/admission (generation plus monotonic sequence and terminal marker). Preserve the existing telemetry-only schema/authority boundary and atomic reconciliation. Native count/fill remains an all-scope operation until actual paging evidence exists: above its measured resource ceiling emit a distinct overflow health outcome before allocation/component work; never invent an offset, truncate an owner scope, or treat emission chunks as native pages. Maintain deterministic message and byte counters independently for FIFO enqueue and successful local writes, plus a separate Rust `admitted_generation_count` observable. A successful nonblocking local pipe write is only a transport handoff, never an acknowledgement and never a new protocol. Emit the marker only after every envelope in that generation has a successful local pipe write. In `named_pipe_contract`, assert that the local-write counters advance while `admitted_generation_count` stays unchanged until a contiguous same-generation terminal marker passes the Rust session gate and atomically reconciles. Make reconnect/new-generation, bridge rejection after a successful local write, and retry outcomes discard staged producer and bridge state deterministically, keeping the last accepted projection. Instrument only bounded class/count/bytes/timing metadata, no raw IDs/payloads/native error text.</action>
  <verify>
    <automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; cargo test -p x4-bridge --test named_pipe_contract --locked; cargo test -p observation-ingest --test runtime_facts_contract --locked; cargo fmt --all -- --check; cargo clippy -p x4-bridge --test named_pipe_contract --locked -- -D warnings</automated>
  </verify>
  <done>A deterministic legacy-path-absence contract proves removed sector fallback constructs do not remain; station count is not the transport scope cap; queue pressure pauses producer work; a single complete owner generation drains and atomically replaces the projection; every incomplete, retry, bridge-rejection, and reconnect path omits valid completion and preserves the prior projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish the measured native and transport budget decision</name>
  <files>extensions/live_galaxy/tests/component_discovery_binding_contract.ps1, extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, crates/x4-bridge/tests/named_pipe_contract.rs, tests/x4-disposable/05.1-component-discovery-probe.md, .planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md</files>
  <behavior>
    - Tests expose envelope bytes, queued-message count, native count/fill consistency, elapsed bounds, per-tick work, drain rate, and retry/backpressure classes without asserting the unverified prototype 2 KiB value or the MCP 256 KiB value as an invariant.
    - The disposable probe can compare normal speed and SETA observations. At the start and end of one fixed 30-tick nonempty-FIFO window, it captures FIFO enqueue and successful-local-write message/byte counters plus both FIFO message/byte depths. The same-window deltas satisfy `write_messages_delta > enqueue_messages_delta` and `write_bytes_delta > enqueue_bytes_delta`; after the finite candidate source is exhausted both ending depths are zero without a reconnect or retry-exhaustion class.
    - A separate Rust contract observes `admitted_generation_count`: successful local writes do not advance it, while one contiguous same-generation terminal marker advances it once and atomically replaces the accepted projection.
    - The probe reports a native resource ceiling separately from queue and wire envelope budgets; a failure to obtain a measured ceiling remains pending-X4 and cannot be recast as complete discovery.
  </behavior>
  <action>Implement the tests and UAT evidence contract for the budget decision before declaring fixed numeric values. Follow `extensions/live_galaxy/tests/x4_discovery_contract.lua` for deterministic runtime fixtures, `crates/x4-bridge/tests/named_pipe_contract.rs` for pipe/reconnect and admission fixtures, and `tests/x4-disposable/01-probe-procedure.md` for the disposable evidence structure. Record each selected dimension and its evidence source: native allocation ceiling, count/fill consistency, native elapsed budget, per-tick producer work, envelope byte ceiling, FIFO message/byte ceiling, low-water resume threshold, bridge drain quota, retry limit, cadence, and diagnostic retention cap. Select drain quota strictly greater than producer enqueue quota for both messages and bytes; this is the configured capacity relation that permits drain to outpace accumulation. Use X4 MCP measurements and behavior as the initial reference baseline under D-05.1-TRANSPORT-01, but select Live Galaxy values only from this extension's focused fake/bridge checks plus the disposable X4 result. Define the pass invariant in fixtures and the probe using same-window deltas, never cumulative totals: at tick `t0` and `t1 = t0 + 30`, capture `fifo_enqueued_messages`, `fifo_enqueued_bytes`, `successful_local_write_messages`, `successful_local_write_bytes`, `queue_message_depth`, and `queue_byte_depth`; require nonzero FIFO depth at `t0`, then calculate each counter's `t1 - t0` delta. Pass only if write-message delta exceeds enqueue-message delta, write-byte delta exceeds enqueue-byte delta, and both queue depths are zero after the finite candidate source ends; retry/reconnect counts remain zero. Retain byte/message high-water values as diagnostics only. Treat any failed delta comparison, nonzero final depth, or nonzero retry/reconnect count as a non-complete health outcome with no marker admission. Keep the independent bridge oracle out of this rate test: `named_pipe_contract` must show that local-write counters alone do not change `admitted_generation_count`, and that only one contiguous same-generation marker changes it and reconciles the projection. Extend the default-off, attempt-scoped probe with those sanitized measurements for a faction scope above 64 stations; test normal speed and SETA separately, correlate Lua and bridge attempt/generation/sequence metadata, and classify drain lag, queue saturation, retry, and reconnect. The procedure must stop without completion/reconciliation on an unmeasured or exceeded native ceiling, incomplete fill, unstable ordering, bridge backlog that does not drain, or any mutation signal. Update G-05.1-1 so its missing evidence is the measured incremental-flow criterion, replacing the obsolete selected-64 criterion.</action>
  <verify>
    <automated>powershell -NoProfile -File extensions/live_galaxy/tests/component_discovery_binding_contract.ps1; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; cargo test -p x4-bridge --test named_pipe_contract --locked; git diff --check</automated>
    <human-check>Run the revised one-attempt Creative Custom procedure only after local checks pass; append one sanitized result row with X4 build, extension identifiers, native count/fill class, selected budget evidence, normal/SETA state, queue high-water/drain evidence, bridge health, and completion outcome.</human-check>
  </verify>
  <done>Budget values are explicit decisions backed by recorded local and disposable-X4 evidence, rather than inherited constants. A qualifying window proves both byte and message local writes outpace their respective enqueues and returns both queues to zero; a separate Rust observable proves reconciliation only after its contiguous marker; the UAT can distinguish native resource rejection, producer backpressure, bridge drain lag, reconnect/retry rejection, and a complete observed generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock staged-scope completion and recovery invariants</name>
  <files>extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, crates/x4-bridge/src/server/session_gate.rs, crates/x4-bridge/tests/named_pipe_contract.rs</files>
  <behavior>
    - A terminal marker cannot reconcile a partial prefix, a replaced queue, a different owner scope, an earlier generation, or a sequence gap.
    - Repeated post-reconnect frames cannot duplicate accepted station facts or advance the completed generation twice.
    - Backpressure is observable and nonblocking: it changes cadence/producer state without blocking an X4 event handler or widening native work.
    - A successful local write followed by bridge rejection or disconnect leaves `admitted_generation_count` and the accepted projection unchanged.
  </behavior>
  <action>Expand the end-to-end tracer into exhaustive adverse-path contracts, following `extensions/live_galaxy/tests/x4_discovery_contract.lua` for Lua reconnect state and `crates/observation-ingest/tests/batch_reconciliation.rs` for atomic prior-projection preservation. Make the scope completion marker an explicit bridge contract, distinct from global MCP completion semantics: it certifies only the declared owner-scoped station generation after the bridge has a contiguous, canonical, same-generation stream. A local successful pipe write remains a one-way handoff only; add no acknowledgement frame or acknowledgement protocol. In `crates/x4-bridge/tests/named_pipe_contract.rs`, observe `admitted_generation_count` or its equivalent public test-facing state and assert: local write success leaves it unchanged; an incomplete/duplicate/stale/rejected marker leaves it unchanged; exactly one contiguous same-generation marker advances it once and atomically replaces the prior projection. Assert re-enumeration replacement, queue discard, successful local write followed by bridge disconnect or rejection, pipe write failure, deferred writes past the retry budget, stale/later generation frames, duplicate observations, duplicate markers, and bridge restart. Keep native discovery, Lua staging, pipe transport, and Rust pending admission accountable to the same generation identity; ensure diagnostics expose only bounded aggregate state. Preserve existing higher-generation fail-closed behavior and do not add reports, acknowledgements, commands, persistence, models, public UI, or mutation vocabulary.</action>
  <verify>
    <automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; cargo test -p x4-bridge --test named_pipe_contract --locked; cargo test -p observation-ingest --test runtime_facts_contract --locked; cargo fmt --all -- --check; cargo clippy -p x4-bridge --test named_pipe_contract --locked -- -D warnings</automated>
  </verify>
  <done>All recognized incomplete/reconnect/idempotency paths preserve the prior accepted faction projection, while one fully drained, contiguous owner generation can reconcile exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| X4 native enumeration to Lua | Native count/fill and component calls can be unavailable, costly, malformed, or inconsistent. |
| Lua FIFO to named pipe | Bounded telemetry crosses a nonblocking transport with defer, disconnect, replay, and backlog conditions. |
| Named pipe to bridge admission | The bridge must not convert an incomplete or adversarial generation into a new accepted projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-05-01 | D | Native owner enumeration | high | mitigate | Measure allocation/time ceiling, refuse work above it before allocation/reads, and retain overflow as health-only. |
| T-05.1-05-02 | T | Lua FIFO frames | high | mitigate | Bind owner scope, generation, snapshot version, sequence, canonical order, byte/message budgets, and terminal marker. |
| T-05.1-05-03 | R | Retry/reconnect recovery | high | mitigate | Discard staged stream on generation change or retry exhaustion; verify duplicate and reconnect contracts preserve the accepted projection. |
| T-05.1-05-04 | I | Runtime diagnostics | medium | mitigate | Emit bounded classes/counts/bytes/timing only; forbid raw native IDs, payloads, error text, prompts, and saves. |
| T-05.1-05-05 | S | MCP reference evidence | medium | mitigate | Treat MCP as a clean-room behavioral baseline; document measured divergence and do not import its implementation or domain completion semantics. |
| T-05.1-05-SC | Tampering | Package/runtime dependencies | high | mitigate | No package installation is authorized; retain existing locked toolchain and package guard. |
</threat_model>

<verification>
Run the focused Lua/binding/package contracts, the named-pipe and strict-admission
Rust tests, format and focused Clippy checks. Then stop autonomous execution at
the default-off disposable Creative Custom handoff. A qualifying normal-speed
and SETA result records `observed in X4`; a missing run records
`pending-game-smoke`; any failed native, queue, drain, retry, reconnect, or
marker condition records the explicit fail-closed UAT gap and does not satisfy
this plan. Resume only from the updated `05.1-UAT.md` result and its sanitized
ledger row. Report local, pending-game-smoke, and observed-in-X4 evidence
separately.
</verification>

<success_criteria>
- The previous 64-station product limit is absent from executable behavior and
  does not constrain complete faction discovery.
- Measured native resource safety and incremental transport flow-control have
  independent explicit limits and fail-closed outcomes.
- The producer never outpaces transport capacity indefinitely: same-window
  30-tick deltas show successful local writes exceed enqueues in both bytes and
  messages; separately captured initial and final queue-depth dimensions return
  to zero after the finite candidate source ends.
- Local transport handoff is never counted as bridge admission; a Rust contract
  proves that only the contiguous same-generation marker reconciles once.
- A complete owner-scoped generation reconciles atomically once; every other
  outcome leaves the previously accepted projection unchanged.
- The X4 MCP reference is used as documented clean-room baseline, and no
  unmeasured prototype or MCP numeric constant is promoted to a Live Galaxy
  invariant.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-SUMMARY.md` when done.
</output>
