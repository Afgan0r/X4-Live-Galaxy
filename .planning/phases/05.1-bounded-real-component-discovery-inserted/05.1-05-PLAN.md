---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 05
type: execute
wave: 3
depends_on: [05.1-03]
files_modified:
  - extensions/live_galaxy/lua/live_galaxy_telemetry.lua
  - extensions/live_galaxy/tests/component_discovery_contract.lua
  - extensions/live_galaxy/tests/component_discovery_binding_contract.ps1
  - extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/tests/x4_discovery_contract.lua
autonomous: true
requirements: [OBS-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05.1-TRANSPORT-01 through D-05.1-TRANSPORT-03: telemetry produces at least 129 station frames plus one terminal marker without a total-frame or aggregate-output ceiling."
    - "The retired 64-observation check is absent; the retired 1800-byte aggregate interpretation is re-scoped to one canonical encoded frame only, with no replacement aggregate cap."
    - "The FIFO counts and writes the identical immutable canonical JSON string and treats a local pipe write only as handoff, never Rust acknowledgement."
    - "Reconnect, deferred write, overflow, and incomplete discovery cannot produce a false marker."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_telemetry.lua
      provides: Uncapped generation iterator with per-frame safety
    - path: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      provides: Bounded FIFO production and drain
    - path: extensions/live_galaxy/tests/component_discovery_binding_contract.ps1
      provides: Static ownership proof for both retired aggregate checks
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_telemetry.lua
      to: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      via: immutable canonical encoded frame strings
      pattern: canonical_encoded_frame_utf8_bytes
    - from: extensions/live_galaxy/tests/component_discovery_contract.lua
      to: extensions/live_galaxy/lua/live_galaxy_telemetry.lua
      via: 129-plus observations and terminal-marker regression
      pattern: 129
---

<objective>
Produce and drain one complete Lua owner generation without the superseded aggregate ceilings.

Purpose: establish the Lua tracer that later Rust plans consume while preserving per-frame safety and bounded concurrent work.
Output: uncapped telemetry aggregation, bounded canonical-byte FIFO, legacy fallback removal, and executable 129-plus contracts.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-RESEARCH.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-PATTERNS.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-VALIDATION.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-03-SUMMARY.md
</context>

## Scope Calibration

The six existing files contain 68,612 characters (17,153 read tokens at chars/4). Twelve thousand implementation tokens plus 4,000 verification/output tokens yield 33,153 raw tokens, rounded to 34,000. `estimate-calibration` returned factor 1, sample count 0, and required low confidence. The former 12-file plan is split into two three-file tasks; Rust ingest and bridge ownership moved to Plans 08 and 09.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remove both telemetry aggregate ceilings</name>
  <files>extensions/live_galaxy/lua/live_galaxy_telemetry.lua, extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/tests/component_discovery_binding_contract.ps1</files>
  <behavior>
    - RED: 129 valid station observations serialize in order and are followed by exactly one terminal marker.
    - The former 64-observation generation guard cannot reject the stream.
    - The former 1800-byte value is not an aggregate-output budget; one canonical frame at the selected per-frame ceiling passes and one byte over fails before enqueue.
    - No total frame/member/encoded-generation byte replacement cap is introduced.
  </behavior>
  <action>Per D-05.1-TRANSPORT-02 and D-05.1-TRANSPORT-03, add the failing 129-plus observation and terminal-marker contracts first. Remove the production 64-observation ceiling from `produce_observations`. Re-scope the existing 1800-byte check and its binding assertion so it applies only to each immutable canonical encoded frame, aligned with the shared per-frame contract; it must never sum or reject the generation's aggregate output. Update the binding contract to fail if either retired aggregate responsibility returns, while retaining per-frame one-over rejection and all schema/identity safety. Do not add a total station, frame, generation-byte, or MCP-envelope substitute.</action>
  <verify>
    <automated>powershell -NoProfile -File extensions/live_galaxy/tests/component_discovery_binding_contract.ps1; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery</automated>
  </verify>
  <done>A telemetry-produced 129-plus frame stream plus terminal marker passes; neither the 64-observation check nor the 1800-byte aggregate interpretation can reject it, while exact per-frame safety remains enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drain immutable canonical frames through a bounded FIFO</name>
  <files>extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua</files>
  <behavior>
    - A finite 129-plus source drains to zero message and byte depth under bounded per-tick enqueue and write quotas.
    - Enqueue and successful-write counters add the exact same immutable canonical JSON string bytes, including unequal-size fixtures.
    - Deferred write retains the head without double-accounting; reconnect discards the generation and restarts at higher generation/sequence one.
    - Only all successfully written observations permit the terminal marker; incomplete or health-only discovery emits no marker.
  </behavior>
  <action>Per D-05.1-TRANSPORT-01 through D-05.1-TRANSPORT-03, make `live_galaxy_runtime.lua` own bounded concurrent message/byte capacity, independent high/low water, enqueue/drain quotas, and head retry over the immutable strings from Task 1. Count `canonical_encoded_frame_utf8_bytes` once at enqueue and again only after successful local write; exclude pipe metadata and never treat write success as Rust acknowledgement. Pause discovery above high water, resume below low water, and emit the marker only after the finite source ends and every observation was written. Remove every production selection path to the legacy synthetic-sector adapter in `live_galaxy_x4_discovery.lua`. Keep health-only behavior and sanitized aggregate diagnostics on any incomplete native result.</action>
  <verify>
    <automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery</automated>
  </verify>
  <done>The Lua tracer drains 129-plus frames with identical enqueue/write byte units, bounded concurrent resources, correct retry/reconnect behavior, no synthetic fallback, and no false terminal marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Native observation objects to telemetry | Untrusted/incomplete native values become canonical frames only after validation. |
| Lua FIFO to local pipe | Local write is a bounded handoff, not delivery acknowledgement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-05-01 | Tampering | telemetry aggregation | high | mitigate | Remove aggregate rejection while retaining exact per-frame validation and binding tests. |
| T-05.1-05-02 | Denial of Service | Lua FIFO | high | mitigate | Checked message/byte capacity, watermarks, quotas, and finite-source pause/resume. |
| T-05.1-05-03 | Repudiation | byte counters | medium | mitigate | Count the identical immutable canonical string at enqueue and successful write. |
| T-05.1-05-04 | Information Disclosure | diagnostics | medium | mitigate | Emit only bounded aggregate classes/counts/bytes/timing without raw identities or payloads. |
</threat_model>

<verification>
Both focused Lua suites pass and explicitly cover 129-plus frames plus a terminal marker without an aggregate cap.
</verification>

<success_criteria>
- Both existing telemetry aggregate checks are removed or correctly re-scoped, with binding proof.
- Per-frame one-over rejection and bounded FIFO resource behavior remain executable.
- Every frontmatter file is owned by one of the two tasks.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-SUMMARY.md` when done.
</output>
