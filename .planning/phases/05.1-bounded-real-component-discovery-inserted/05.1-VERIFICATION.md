---
phase: 05.1-bounded-real-component-discovery-inserted
verified: 2026-08-29T16:48:31Z
status: gaps_found
score: 4/7 must-haves verified
behavior_unverified: 2
overrides_applied: 0
gaps:
  - truth: "One owner-scoped station class yields a deterministic real-component observation only when every validated member belongs to the declared owner scope."
    status: failed
    reason: "The adapter passes the requested faction only to enumeration, then accepts and serializes every syntactically valid owner value without comparing it to the declared scope. Its green fake fixture deliberately returns faction:antigone for one member while runtime_api declares faction:argon."
    artifacts:
      - path: "extensions/live_galaxy/lua/live_galaxy_component_discovery.lua"
        issue: "Lines 103-112 validate owner_id syntax but never require owner_id == api.faction_id before creating an observation."
      - path: "extensions/live_galaxy/tests/component_discovery_contract.lua"
        issue: "The passing fake supplies faction:antigone for station:20 and asserts successful serialization, so the contract encodes the scope violation."
    missing:
      - "Require each component owner to equal the predeclared owner scope before observation construction."
      - "Add an owner-mismatch regression test proving no observation or completion marker and prior-projection preservation."
behavior_unverified_items:
  - truth: "Current X4 9.00 station enumeration, component-local capacity, and owner normalization are available and bounded for the selected native seam."
    test: "Run the one-attempt Creative Custom procedure in tests/x4-disposable/05.1-component-discovery-probe.md after the owner-scope blocker is fixed."
    expected: "One sanitized ledger entry classifies the complete scope or an explicit unsupported/partial result, records bounded timing and SETA state, and contains no raw identifiers or payloads."
    why_human: "Lua fakes and static bindings cannot establish X4 ABI behavior, game-thread cost, or native owner semantics."
  - truth: "The native component path remains telemetry-only and suppresses completion after a real X4 discovery failure."
    test: "In the same disposable attempt, induce or observe one bounded unsupported/partial outcome and inspect the class-only ledger plus health output."
    expected: "The result remains facts_unsupported or the documented explicit failure class; no completion marker, mutation signal, retry, cursor, or scope widening occurs."
    why_human: "The local state-machine test proves fake-adapter handling, not actual X4 runtime interaction."
human_verification:
  - test: "After closing the owner-scope blocker, follow the one-attempt procedure at tests/x4-disposable/05.1-component-discovery-probe.md in a fresh X4 9.00 Creative Custom campaign."
    expected: "Record exactly one sanitized ledger row for obs-x4-component-discovery-051-01; classify it as observed in X4 only on a full bounded result, otherwise retain pending game smoke test/facts_unsupported."
    why_human: "Native API availability, ABI shape, cost, and owner normalization cannot be inferred from static or fake tests."
---

# Phase 05.1: Bounded Real Component Discovery Verification Report

**Phase Goal:** Replace the unsupported sector-as-capacity-target probe with deterministic, bounded discovery of real X4 components so runtime asset, capacity, and ownership facts can be admitted without synthetic fallback or unbounded game-thread work.
**Verified:** 2026-08-29T16:48:31Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | An original, read-only station-enumeration seam is identified while native availability, cost, and owner semantics remain pending-X4. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | The adapter binds the researched count/fill/conversion/component calls, but only the Creative Custom probe can prove their X4 behavior. |
| 2 | The adapter bounds the whole owner-scoped result and fails before allocation or component reads when the raw count exceeds the hard maximum. | ✓ VERIFIED | `component_discovery_contract.lua` exercises count `17`, asserts `enumeration_overflow`, zero allocations, conversions, metadata, and capacity calls; the focused suite passed. |
| 3 | Every admitted real station belongs to the declared owner scope and supplies real asset, capacity, and ownership facts without a synthetic sector fallback. | ✗ FAILED | `live_galaxy_component_discovery.lua` accepts any valid `owner_id`; its passing fake serializes `faction:antigone` in an adapter fixed to `faction:argon`. |
| 4 | Telemetry framing, strict Rust admission, atomic reconciliation, reconnect behavior, and no-mutation boundary remain intact locally. | ✓ VERIFIED | Focused Lua guard/discovery/X4 suites, 9 Rust admission tests, format, and Clippy all passed; runtime emits health-only frames instead of a marker after fake discovery failure. |
| 5 | A bounded disposable X4 procedure separately classifies observed, partial, and unsupported behavior without treating local evidence as observed-in-X4. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | The procedure is substantive and explicitly pending, but no fresh Creative Custom ledger entry exists. |
| 6 | Fake Lua and strict Rust contracts reject unavailable, overflowed, malformed, partial, unsupported, duplicate, unordered, and relationship-invalid facts without replacing the prior projection. | ✓ VERIFIED | Focused Lua and `runtime_facts_contract` tests passed; Rust tests cover prior-projection preservation and canonical complete-scope admission. |
| 7 | The phase contains no report, acknowledgement, command, effect, persistence, model, public UI, or game-state mutation path. | ✓ VERIFIED | The fixed-path package guard self-test and production-path checks passed; targeted source scan found no prohibited authority vocabulary in the two guarded discovery modules. |

**Score:** 4/7 truths verified (2 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `extensions/live_galaxy/lua/live_galaxy_component_discovery.lua` | Bound-first real station adapter | ⚠️ HOLLOW | Exists, is 127 lines, and is wired into `live_galaxy_x4_discovery.lua`; owner-scope validation is absent, so its accepted facts can be out of scope. |
| `extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua` | Existing discovery seam wired to component adapter | ✓ VERIFIED | Requires the component module and dispatches `count_stations` adapters to `component_discovery.new`; otherwise preserves the legacy adapter. |
| `extensions/live_galaxy/tests/component_discovery_package_guard.ps1` | Fixed path and authority guard | ✓ VERIFIED | The scripted allowlist and three expected-failure fixtures pass deterministically. |
| `crates/observation-ingest/tests/runtime_facts_contract.rs` | Strict station-fact admission and prior-projection preservation | ✓ VERIFIED | Substantive 9-test executable contract; malformed, duplicate, unknown, incomplete, and noncanonical input is rejected. |
| `tests/x4-disposable/05.1-component-discovery-probe.md` | One-attempt Creative Custom probe | ✓ VERIFIED (procedure) | Concrete preconditions, stop conditions, sanitized ledger, and pending-X4 classification exist; execution remains human-only. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `live_galaxy_component_discovery.lua` | `live_galaxy_x4_discovery.lua` | `discovery.new` selects the component adapter when `count_stations` is present | ✓ WIRED | Direct source trace confirms the required module import and adapter dispatch. |
| `live_galaxy_x4_discovery.lua` | Existing telemetry serializer and Rust runtime facts | `live_galaxy_runtime.lua` calls `telemetry.produce_observation`; Rust admits compact `s/x/c/o` frames | ✓ WIRED | Component contract asserts the compact envelope; `runtime_facts_contract` accepts complete station facts. |
| Failed component discovery | Runtime frame sequencing | `discovery_incomplete` yields health-only rather than a completion marker | ✓ WIRED | `x4_discovery` focused suite passed the failed-discovery sequence assertion. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Component adapter | `candidates` / `runtime_facts` | Native count/fill plus component metadata and capacity calls | Local fakes produce data; native source remains unobserved | ⚠️ PENDING-X4 |
| Telemetry-to-Rust admission | compact `s/x/c/o` envelope | Fully built Lua observation → existing telemetry serializer → strict Rust decoder | Yes for local deterministic fixtures | ✓ FLOWING (local) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Guard expected-failure fixtures | `powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery_guard` | Exit 0 | ✓ PASS |
| Bound-first component adapter and static binding | `powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery` | Exit 0 | ✓ PASS — but does not test owner equality |
| Runtime no-marker path | `powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery` | Exit 0 | ✓ PASS |
| Strict station admission | `cargo test -p observation-ingest --test runtime_facts_contract --locked` | 9 passed | ✓ PASS |
| Rust format and focused lint | `cargo fmt --check`; `cargo clippy -p observation-ingest --test runtime_facts_contract --locked -- -D warnings` | Exit 0 | ✓ PASS |
| Workspace Rust regression | `cargo test --workspace --locked` | Passed in this run; known intermittent packet-determinism residual did not reproduce | ✓ PASS |
| Full Lua regression | `powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1` | Failed only at known unrelated `telemetry_contract.lua:32` | ? BASELINE RESIDUAL |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| OBS-06 | 05.1-01, 05.1-02 | Runtime assets, capacity, and ownership originate from observed rather than fixed state | ✗ BLOCKED | The path can serialize a syntactically valid station whose owner is outside the declared owner scope, violating the bounded owner-scoped discovery contract. |
| D-09 (local evidence contract) | 05.1-01, 05.1-02 | Complete-only component fact admission with honest pending native evidence | ⚠️ PARTIAL | Fail-closed bounds and atomic local admission pass, but owner-scope validation is missing and native proof is still pending. |

No plan-declared requirement is orphaned: both plans declare inherited `OBS-06`; `D-09` is correctly documented as a local evidence contract rather than a newly assigned milestone requirement.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `live_galaxy_component_discovery.lua` | 103-112 | Missing owner-to-scope comparison | 🛑 Blocker | Allows an out-of-scope station to enter a claimed owner-scoped observation. |
| Phase-owned files | — | `TBD` / `FIXME` / `XXX` / placeholder / empty rendering or return stubs | — | None found. |

### Human Verification Required

After the owner-scope blocker is corrected, execute exactly one fresh Creative Custom attempt using [05.1-component-discovery-probe.md](../../../tests/x4-disposable/05.1-component-discovery-probe.md). The expected result is one sanitized ledger row that either proves one complete bounded scope in X4 or honestly retains an explicit unsupported/partial outcome. Do not auto-approve, retry, widen the scope, or infer native behavior from the local fakes.

## Gaps Summary

The phase does not achieve its owner-scoped discovery goal: it validates only that an owner string is well formed, not that it matches the requested owner scope. This is observable in the green local fixture, which admits and serializes `faction:antigone` while the production adapter declares `faction:argon`.

Fix the owner equality gate and its no-observation/no-marker regression coverage before running the human-only Creative Custom probe. The native probe remains a separate required escalation gate; it cannot close this code defect.

## Skill Learning

Skill learning: none — considered the owner-mismatch implementation and green fake fixture, focused Lua/Rust/package checks, full-suite baseline result, pending X4 probe, and existing fail-closed/native-fake separation rules. The failure is an implementation violation of existing owner-scoped and fake-versus-X4 rules, not evidence of a missing reusable skill rule.

---

_Verified: 2026-08-29T16:48:31Z_
_Verifier: the agent (gsd-verifier)_
