---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 04
type: tdd
status: superseded
superseded_by: 05.1-05
depends_on: [05.1-03]
gap_closure: true
requirements: [OBS-06]
---

# Raise the bounded faction-station scope

## Objective

Allow one complete, deterministic faction-station observation when the real
Argon scope is larger than the obsolete 16-station development bound, while
retaining fail-closed behavior above the new bound.

## Task 1: Red-to-green atomic bounded frame sequence

**Files:**

- `extensions/live_galaxy/lua/live_galaxy_component_discovery.lua`
- `extensions/live_galaxy/tests/component_discovery_contract.lua`
- `extensions/live_galaxy/tests/component_discovery_binding_contract.ps1`

**Action:** Replace the single aggregate observation with at most 64 sorted,
per-station observation frames and one completion marker. Keep every frame at
or below the existing 2 KiB pipe budget. Use the existing bridge pending batch
to admit all frames and the marker atomically; a failed candidate, overflow,
or oversized frame emits no marker and retains the prior projection.

**Verification:**

```text
powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery
powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery
powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery_guard
powershell -NoProfile -File tests/x4-disposable/01-install-guard.ps1 -VerifyPackageOnly
cargo test -p observation-ingest --test runtime_facts_contract --locked
```

## Task 2: Disposable X4 verification

**Files:**

- `tests/x4-disposable/05.1-component-discovery-probe.md`
- `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md`

**Action:** Deploy only after X4 is closed. Use one named, default-off trace
attempt with the approved disposable save. Record bridge frame classes and the
sanitized result only. Restore trace default-off and stop the bridge afterward.

**Acceptance:** A valid full per-station frame set plus completion marker is
observed in X4, or a distinct fail-closed class is recorded. No partial data,
raw identity, or game mutation is accepted.
