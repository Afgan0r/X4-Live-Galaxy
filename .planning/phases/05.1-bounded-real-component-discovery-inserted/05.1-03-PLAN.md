---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 03
type: tdd
wave: 2
depends_on: [05.1-02]
files_modified:
  - extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
  - extensions/live_galaxy/tests/component_discovery_contract.lua
  - extensions/live_galaxy/tests/x4_discovery_contract.lua
autonomous: true
gap_closure: true
requirements: [OBS-06]
requirement_note: "Inherited OBS-06 closure for the verifier's owner-scope blocker; D-09 remains the local evidence contract."
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every station fact in the fixed faction:argon discovery scope has an owner exactly equal to api.faction_id before an observation is constructed."
    - "A syntactically valid faction:antigone owner within the faction:argon fixture produces neither an observation nor a completion marker."
    - "The existing no-marker admission path retains the prior Rust projection; no owner-mismatch input can trigger reconciliation."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      provides: Exact owner-scope admission predicate on discovered stations.
    - path: extensions/live_galaxy/tests/component_discovery_contract.lua
      provides: RED-to-GREEN owner-mismatch regression fixture.
    - path: extensions/live_galaxy/tests/x4_discovery_contract.lua
      provides: Runtime-frame regression proving the mismatch produces health-only frames rather than a marker.
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      to: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      via: A rejected read_observation result causes the established discovery-incomplete health sequence.
      pattern: read_observation
    - from: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      to: crates/observation-ingest/src/batch.rs
      via: No observation or complete_marker frame reaches the existing atomic admission path, which retains the prior projection.
      pattern: complete_marker
---

<objective>
Close the verified owner-scope admission gap in the bounded station discovery tracer.

Purpose: prevent an out-of-scope but well-formed component owner from becoming an
accepted telemetry observation for the fixed faction:argon enumeration scope.
Output: one exact owner-equality gate and focused RED-to-GREEN regressions; no new
files, wire schema, authority path, or native-X4 claim.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-RESEARCH.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-02-SUMMARY.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-VERIFICATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED owner-mismatch fixture across discovery and runtime framing</name>
  <files>extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua</files>
  <read_first>Read 05.1-VERIFICATION.md gaps and the completed 05.1-02-SUMMARY.md. Then read extensions/live_galaxy/lua/live_galaxy_component_discovery.lua, extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, and extensions/live_galaxy/tests/x4_discovery_contract.lua once. Per 05.1-PATTERNS.md, use extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua as the component adapter's protected-call and deterministic-ordering analog, and extensions/live_galaxy/tests/x4_discovery_contract.lua as the component-contract harness and runtime-health framing analog. Reuse runtime_facts_contract.rs only as the existing proof that an incomplete no-marker batch retains the accepted projection.</read_first>
  <behavior>
    - Test 1: the fixed faction:argon fake returns one faction:antigone component owner; telemetry production returns no observation and the owner-scope failure result.
    - Test 2: the same mismatch adapter drives the runtime discovery slot to health-only frames; the next frame is not a complete marker.
    - Test 3: the focused Rust admission contract continues to prove that the existing no-marker path preserves the prior accepted projection.
  </behavior>
  <action>First change only the existing fake contract fixtures so the mismatched owner is asserted as a rejected owner-scope result rather than as serialized output. Add the runtime regression using the component adapter with the same owner-mismatch fixture, advance the established sequence through the discovery slot, and assert that it emits runtime-health frames instead of an observation and marker. Run the Lua component suite and confirm RED because the production adapter currently accepts the fixture. Run the focused Rust admission contract unchanged to retain evidence that the no-marker path does not replace the prior projection. Per D-09 and OBS-06, keep the fixture's predeclared api.faction_id fixed to faction:argon and treat the other owner as a failure of that declared scope.</action>
  <verify>
    <automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; cargo test -p observation-ingest --test runtime_facts_contract --locked</automated>
  </verify>
  <acceptance_criteria>The new owner-mismatch assertions fail before production code changes; the current Rust focused test remains green and demonstrates prior-projection preservation for the established incomplete no-marker path.</acceptance_criteria>
  <done>A behavior-focused regression exposes the verifier's exact cross-owner admission defect and covers its runtime no-marker consequence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN exact owner-scope validation before observation construction</name>
  <files>extensions/live_galaxy/lua/live_galaxy_component_discovery.lua, extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua</files>
  <read_first>Read the failing regression from Task 1 and lines 83-117 of extensions/live_galaxy/lua/live_galaxy_component_discovery.lua. Read the owner-normalization acceptance decision in 05.1-RESEARCH.md and the failed truth plus missing items in 05.1-VERIFICATION.md.</read_first>
  <behavior>
    - Test 1: every valid owner equal to api.faction_id continues to produce the canonical complete station observation.
    - Test 2: any component owner not exactly equal to api.faction_id rejects the complete candidate set before section_from_candidates runs.
    - Test 3: the mismatch has no telemetry observation, no completion marker, and no Rust projection replacement through the existing no-marker path.
  </behavior>
  <action>Add the smallest equality predicate to the existing per-candidate validation condition: after the owner value passes the current bounded identifier checks, require owner_id to exactly equal api.faction_id before storing candidate facts or calling section_from_candidates. Follow the 05.1-PATTERNS.md assignments: preserve extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua's protected-call and deterministic-ordering patterns in live_galaxy_component_discovery.lua; preserve extensions/live_galaxy/tests/x4_discovery_contract.lua's fake-harness assertions in component_discovery_contract.lua; and preserve extensions/live_galaxy/lua/live_galaxy_runtime.lua plus x4_discovery_contract.lua's telemetry-only runtime-health and no-marker sequencing. Return the established class-only failure result used for unsupported component facts; preserve capacity conversion and transport framing. Re-run the RED commands until all focused suites are green. This closes the verifier gap without adding owner normalization, multi-owner behavior, cursors, schema fields, new authority, or unrelated production edits.</action>
  <verify>
    <automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; cargo test -p observation-ingest --test runtime_facts_contract --locked; cargo fmt --check; cargo clippy -p observation-ingest --test runtime_facts_contract --locked -- -D warnings</automated>
  </verify>
  <acceptance_criteria>The exact owner-mismatch fixture is rejected before observation construction; the matching-owner fixtures remain canonical; runtime emits no observation or completion marker; and the focused Rust contract confirms the accepted projection is preserved on the no-marker path.</acceptance_criteria>
  <done>Only stations owned by the predeclared faction:argon scope can enter the existing telemetry-to-Rust admission path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| X4 component metadata → Lua candidate | The native owner value is untrusted even when syntactically valid and must match the predeclared enumeration authority. |
| Lua discovery → runtime framing | A rejected candidate must produce health-only sequencing, never a telemetry observation or completion marker. |
| Runtime framing → Rust admission | Only complete markers reconcile an accepted projection; no marker retains the earlier projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-06 | Tampering | owner metadata in component discovery | high | mitigate | Require exact owner_id == api.faction_id before candidate facts can form an observation; RED-to-GREEN mismatch fixtures enforce it. |
| T-05.1-07 | Tampering | runtime completion sequencing | high | mitigate | Mismatch adapter contract asserts health-only output and no complete marker, preserving current reconciliation semantics. |
| T-05.1-08 | Elevation of privilege | owner-scope enumeration | high | mitigate | Keep one fixed faction owner scope and reject foreign component ownership rather than widening the source contract. |
| T-05.1-SC | Tampering | package installs | high | accept | No package installation is planned. |

ASVS Level 1 applies. `workflow.security_block_on` is `high`; no high-severity threat may remain unresolved after the focused automated checks.
</threat_model>

<verification>
Execute RED before implementation, then the focused Lua runtime/discovery and Rust admission contracts, format, and focused Clippy check. The native X4 Creative Custom smoke remains a later blocking-human verification item after this code closure; do not run it or claim native owner semantics here.
</verification>

<success_criteria>
The fixed faction:argon discovery scope cannot serialize a faction:antigone owner; all local failure evidence remains telemetry-only and preserves the prior accepted projection.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-03-SUMMARY.md` when done.
</output>
