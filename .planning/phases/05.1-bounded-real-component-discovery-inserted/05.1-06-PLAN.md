---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 06
type: execute
wave: 4
depends_on: [05.1-05]
files_modified:
  - extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
  - extensions/live_galaxy/tests/component_discovery_contract.lua
  - extensions/live_galaxy/tests/component_discovery_binding_contract.ps1
  - scripts/component_discovery_package_guard.ps1
  - extensions/live_galaxy/tests/component_discovery_package_guard.ps1
  - extensions/live_galaxy/tests/run_contracts.ps1
autonomous: true
requirements: [OBS-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05.1-TRANSPORT-03: native count/fill uses checked allocation bytes and an enforced exact component-read work budget, not a total station product limit."
    - "Every work-unit check occurs before its native/conversion call; exhaustion discards all candidates before any observation or marker is exposed."
    - "Production initializes a conservative finite native policy before the owner run from the 129-member proof obligation and the actual `UniverseID` allocation size; absent or invalid policy fails closed before the first native call."
    - "The package guard scans exactly the four intentionally changed production Lua files and rejects every other production path."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      provides: Checked allocation and component-read work admission
    - path: extensions/live_galaxy/tests/component_discovery_contract.lua
      provides: Deterministic budget exhaustion and atomic no-output tests
    - path: scripts/component_discovery_package_guard.ps1
      provides: Authoritative fixed four-file Lua guard
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      to: extensions/live_galaxy/tests/component_discovery_contract.lua
      via: injected allocation/work budgets and call ledger
      pattern: work_budget
    - from: extensions/live_galaxy/tests/run_contracts.ps1
      to: scripts/component_discovery_package_guard.ps1
      via: component_discovery and component_discovery_guard suites
      pattern: component_discovery_guard
---

<objective>
Enforce bounded native discovery work and make the revised Lua package boundary authoritative.

Purpose: replace the superseded member count with measurable resource admission and preserve the exact four-file authority guard.
Output: checked allocation/work budgets, deterministic fail-closed tests, legacy fallback absence, and the fixed guard runner.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-RESEARCH.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-PATTERNS.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VERIFICATION.md
</context>

## Scope Calibration

The five existing files contain 51,940 characters (12,985 read tokens at chars/4); the root guard is new. Twelve thousand implementation tokens plus 5,000 verification/output tokens yield 29,985 raw tokens, rounded to 30,000. `estimate-calibration` returned factor 1, sample count 0, and required low confidence. Rust receiver work moved to Plans 08 and 09, leaving two three-file tasks.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce native allocation and component-read work budgets</name>
  <files>extensions/live_galaxy/lua/live_galaxy_component_discovery.lua, extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/tests/component_discovery_binding_contract.ps1</files>
  <read_first>Read the component contract mapping in `05.1-PATTERNS.md`, `extensions/live_galaxy/lua/live_galaxy_component_discovery.lua`, and `extensions/live_galaxy/tests/component_discovery_contract.lua`. Follow the existing injected `fake_api`, call-counter, count/fill, and exact-boundary test seam; do not create a second policy owner.</read_first>
  <behavior>
    - Count multiplication by the actual `ffi.sizeof("UniverseID")` is checked before allocation; an over-budget or overflow count performs no allocation, fill, conversion, metadata, or capacity read.
    - The exact work ledger is one count call, one fill call, and four calls per returned member: `to_component`, `to_component64`, `get_component_data`, and `get_people_capacity`.
    - One work unit is checked and consumed immediately before every listed call. Zero remaining units fails closed before that call, with no observation prefix or marker.
    - Count/fill mismatch, work exhaustion, invalid identity/owner/sector/capacity, or unstable ordering discards the whole candidate.
  </behavior>
  <action>Per D-05.1-TRANSPORT-03, make `extensions/live_galaxy/lua/live_galaxy_component_discovery.lua` the single production policy owner. Add module-local `PRE_RUN_OWNER_MEMBER_EVIDENCE = 129` and a deterministic initializer such as `derive_pre_run_native_policy(universe_id_bytes)`: validate `ffi.sizeof("UniverseID")` as a positive finite integer, then use checked safe-integer arithmetic to produce immutable `max_allocation_bytes = PRE_RUN_OWNER_MEMBER_EVIDENCE * universe_id_bytes` and `max_work_units = 2 + 4 * PRE_RUN_OWNER_MEMBER_EVIDENCE`. The 129 source is the phase's required first proof above the superseded 128-frame boundary; the allocation multiplier and exact work formula come from the existing count/fill allocation and call ledger, so neither value is arbitrary. Inject this initialized policy through the existing runtime API into `discovery.new(api)` before any native call. Missing policy, unknown fields, non-integer/non-positive values, arithmetic overflow, or initialization failure returns the bounded unsupported/overflow class with zero native calls, observations, and marker; there is no unbounded fallback.

Permit the count only after its pre-call unit check. After count, check `count * universe_id_bytes` before allocation and check that the remaining finite ledger covers exactly one fill plus four calls per returned member; reject before allocation/fill when either candidate is insufficient. Check/decrement again immediately before fill and before `to_component`, `to_component64`, `get_component_data`, and `get_people_capacity`. Do not emit while reading; retain private candidates until count/fill equality, all work units, canonical sorting/uniqueness, owner equality, and facts validate. Follow the exact `fake_api` and call-counter analog mapped in `05.1-PATTERNS.md`. Add configured exact-bound success, one-byte/unit-over pre-call failure, and absent/invalid initializer tests against the actual production policy path. The owner X4 run starts with this source-owned finite candidate. Plan 07 may record only `retain`; if evidence shows it insufficient or unsafe, it records `replan-required`, leaves production unchanged, and the phase remains incomplete for a new implementation plan. Extend binding tests to prove the legacy sector selection/capacity/synthetic path and its selection fallback are physically absent.</action>
  <verify>
    <automated>powershell -NoProfile -File extensions/live_galaxy/tests/component_discovery_binding_contract.ps1; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery</automated>
  </verify>
  <done>Production deterministically initializes the 129-derived finite policy before a run; configured exact-bound behavior passes, absent/invalid policy and one-unit/one-byte-over paths fail before the next native call, and every exhausted path emits zero partial observations or marker.</done>
</task>

<task type="auto">
  <name>Task 2: Make the four-file Lua package guard authoritative</name>
  <files>scripts/component_discovery_package_guard.ps1, extensions/live_galaxy/tests/component_discovery_package_guard.ps1, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <action>Create the repository-root authoritative guard and retain the extension-local stable runner entrypoint. The fixed production allowlist contains exactly `live_galaxy_component_discovery.lua`, `live_galaxy_x4_discovery.lua`, `live_galaxy_runtime.lua`, and `live_galaxy_telemetry.lua`. Self-tests accept all four neutral fixtures and reject forbidden Lua authority vocabulary, an unrelated Live Galaxy production path, and a foreign-extension path. Update both `component_discovery_guard` and `component_discovery` suites to run the self-test and all four real files. Do not scan Rust, tests, or planning prose.</action>
  <verify>
    <automated>powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -SelfTest; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -ProductionPath extensions/live_galaxy/lua/live_galaxy_component_discovery.lua; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -ProductionPath extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -ProductionPath extensions/live_galaxy/lua/live_galaxy_runtime.lua; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -ProductionPath extensions/live_galaxy/lua/live_galaxy_telemetry.lua; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery_guard; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery</automated>
  </verify>
  <done>The exact four-file production allowlist passes through both entrypoints; authority vocabulary and every non-allowlisted production path fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Native count/fill to Lua allocation | Untrusted count controls memory only after checked byte admission. |
| Native component calls to observations | Calls consume exact work units before private candidates may become frames. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-06-01 | Denial of Service | native discovery | high | mitigate | Checked allocation multiplication and exact pre-call work ledger. |
| T-05.1-06-02 | Tampering | partial candidates | high | mitigate | No output until the complete private candidate validates atomically. |
| T-05.1-06-03 | Elevation of Privilege | Lua package boundary | high | mitigate | Exact four-file guard plus fixed-path negative fixtures. |
</threat_model>

<verification>
Focused component contracts and both package-guard entrypoints pass with exact-bound and one-over cases.
</verification>

<success_criteria>
- Native component work is checked and consumed before every call, never merely instrumented.
- Missing/unmeasured/exhausted budgets fail closed before partial output or a terminal marker.
- Every frontmatter file is owned by exactly one task.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-06-SUMMARY.md` when done.
</output>
