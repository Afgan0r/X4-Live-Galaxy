---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 09
type: execute
wave: 5
depends_on: [05.1-08]
files_modified:
  - crates/observation-ingest/src/wire.rs
  - crates/x4-bridge/src/server.rs
  - crates/x4-bridge/src/server/session_gate.rs
  - crates/x4-bridge/tests/named_pipe_contract.rs
  - crates/x4-bridge/src/ingress.rs
  - crates/x4-bridge/src/listener.rs
  - crates/x4-bridge/tests/backpressure_contract.rs
autonomous: true
requirements: [OBS-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05.1-TRANSPORT-02 and D-05.1-TRANSPORT-03: the bridge sends every accepted sequence directly to incremental staging and the marker itself closes admission."
    - "No pending Vec, 64-observation server cap, deferred marker, later-heartbeat confirmation, or aggregate byte cap remains on the production stream."
    - "Listener and ingress retain exact per-frame overflow and bounded concurrent backpressure while permitting 129-plus sequential frames."
  artifacts:
    - path: crates/x4-bridge/src/server/session_gate.rs
      provides: Direct marker-owned incremental session admission
    - path: crates/x4-bridge/src/listener.rs
      provides: Exact per-frame receive overflow handling
    - path: crates/x4-bridge/tests/named_pipe_contract.rs
      provides: End-to-end 129-plus bridge regression
    - path: crates/x4-bridge/src/ingress.rs
      provides: Concurrent queued-message and queued-byte admission accounting
  key_links:
    - from: crates/x4-bridge/src/server/session_gate.rs
      to: crates/observation-ingest/src/generation.rs
      via: typed decoded frame submission
      pattern: generation
    - from: crates/x4-bridge/src/ingress.rs
      to: crates/x4-bridge/tests/backpressure_contract.rs
      via: accepted byte charge and consumption-time release
      pattern: queued_bytes
---

<objective>
Wire incremental generation admission through the bridge and listener.

Purpose: remove server/listener aggregate bottlenecks while retaining per-frame safety, concurrent backpressure, and reconnect isolation.
Output: direct session staging, marker-owned commit, exact listener overflow, and bridge regressions.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-PATTERNS.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-08-SUMMARY.md
</context>

## Scope Calibration

The seven existing files keep the incremental state and listener/backpressure layers in two cohesive cross-file tasks. The 30,000-token estimate retains the prior low-confidence calibration while adding the existing `ingress.rs` production owner to Task 2.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Admit one contiguous stream at its terminal marker</name>
  <files>crates/observation-ingest/src/wire.rs, crates/x4-bridge/src/server.rs, crates/x4-bridge/src/server/session_gate.rs, crates/x4-bridge/tests/named_pipe_contract.rs</files>
  <read_first>Read the named-pipe/session-gate mapping in `05.1-PATTERNS.md`, `crates/x4-bridge/src/server/session_gate.rs`, and `crates/x4-bridge/tests/named_pipe_contract.rs`, plus Plan 08's summary. Preserve the existing session-generation rejection seam while replacing aggregate pending ownership.</read_first>
  <behavior>
    - Hello plus 129-plus ordered observations preserves the snapshot until the same-generation terminal marker, which commits once without a later heartbeat.
    - Gap, duplicate/stale marker, mixed generation, disconnect, malformed frame, or rejection discards only pending state.
    - Local write state cannot advance the Rust admission observable.
  </behavior>
  <action>Expose the minimum typed decoded-frame seam from `wire.rs`, preserving deny-unknown-fields and the canonical per-frame bound. Follow the named-pipe/session-gate analog mapped in `05.1-PATTERNS.md`. Replace `PendingSnapshot.observations`, the 64 pending limit, marker deferral, aggregate pending bytes, and later-heartbeat confirmation with Plan 08's incremental state. Submit each accepted session sequence directly; only the contiguous same-generation marker commits. Reconnect and all invalid paths discard staging while retaining the accepted projection. Add small and 129-plus end-to-end named-pipe contracts and a narrow admitted-generation observable containing no payload data.</action>
  <verify>
    <automated>cargo test -p x4-bridge --test named_pipe_contract --locked; cargo test -p observation-ingest --test batch_reconciliation --locked; cargo fmt --all -- --check; cargo clippy -p observation-ingest -p x4-bridge --tests --locked -- -D warnings</automated>
  </verify>
  <done>The bridge admits both small and 129-plus generations exactly once at the marker, with no heartbeat confirmation, local-write acknowledgement, or aggregate pending cap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce per-frame listener overflow and concurrent backpressure</name>
  <files>crates/x4-bridge/src/ingress.rs, crates/x4-bridge/src/listener.rs, crates/x4-bridge/src/server.rs, crates/x4-bridge/src/server/session_gate.rs, crates/x4-bridge/tests/backpressure_contract.rs</files>
  <read_first>Read the backpressure mapping in `05.1-PATTERNS.md`, then `crates/x4-bridge/src/ingress.rs`, `crates/x4-bridge/src/listener.rs`, `crates/x4-bridge/src/server.rs`, `crates/x4-bridge/src/server/session_gate.rs`, and `crates/x4-bridge/tests/backpressure_contract.rs`. `FrameLimits` and `BoundedIngress` in `ingress.rs` are the production concurrent-cap owner; use their real accepted/released state rather than a test-only counter.</read_first>
  <behavior>
    - A canonical frame at the shared per-frame ceiling is received completely; one byte over is rejected without truncation or parsing.
    - More than 128 sequential accepted/released frames pass, while exact concurrent message/byte saturation rejects one over.
    - Debug-event caps affect diagnostics only and never admission.
  </behavior>
  <action>Follow the listener/backpressure analog mapped in `05.1-PATTERNS.md`. Use the shared canonical per-frame maximum in the listener and receive with explicit overflow detection before UTF-8/JSON admission. In `ingress.rs`, extend `FrameLimits` with `max_queued_bytes` and `BoundedIngress` with actual `queued_bytes` state. For each submission, compute the accepted charge in the canonical `canonical_encoded_frame_utf8_bytes` unit, use checked addition, and reject exact byte-cap one-over before advancing sequence, queued-message count, or queued-byte count. Carry that exact accepted byte charge with the accepted submission (or require it as the release argument), and release both one message and its bytes only after `session_gate.rs` has actually consumed the frame; underflow or mismatched release is an invariant failure, not saturating arithmetic. Update the `server.rs` production initializer to pass explicit finite message and byte capacities. The Plan 09 shared per-frame ceiling and these cross-layer production owners are immutable to Plan 07: later evidence may only retain them or require a new cross-layer plan.

Extend `backpressure_contract.rs` through the actual `BoundedIngress` state with unequal-size frames, exact concurrent message saturation, exact concurrent byte saturation, one-message/one-byte-over rejection without state or sequence advancement, and consumption release that restores exactly the charged message and byte capacity. Prove more than 128 sequential accepted/consumed/released frames independently of concurrent caps. Preserve bounded debug evidence separately from admission. Do not use MCP's envelope or add a total generation/member cap.</action>
  <verify>
    <automated>cargo test -p x4-bridge --test backpressure_contract --test named_pipe_contract --locked; cargo fmt --all -- --check; cargo clippy -p x4-bridge --tests --locked -- -D warnings</automated>
  </verify>
  <done>Listener one-over behavior and concurrent backpressure are exact, while a released 129-plus sequential stream reaches marker-owned admission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Named-pipe bytes to frame parser | Untrusted bytes require complete per-frame receipt before parsing. |
| Session gate to accepted projection | Only contiguous marker-owned staging may cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-09-01 | Tampering | session gate | high | mitigate | Typed frames, generation/sequence checks, atomic terminal commit. |
| T-05.1-09-02 | Denial of Service | listener/ingress | high | mitigate | Exact per-frame overflow plus concurrent message/byte backpressure. |
| T-05.1-09-03 | Repudiation | marker admission | medium | mitigate | Test-observable once-only admitted-generation count. |
</threat_model>

<verification>
Focused bridge/ingest tests, formatting, and clippy pass with 129-plus sequential and exact concurrent-limit cases.
</verification>

<success_criteria>
- The production bridge has no aggregate owner-generation cap or delayed marker confirmation.
- Per-frame, concurrent queue, reconnect, and diagnostic resource bounds remain explicit.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-09-SUMMARY.md` when done.
</output>
