---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 06
subsystem: bounded native component discovery
tags: [lua, x4, native-allocation, work-budget, powershell, tdd]
requires:
  - phase: 05.1-05
    provides: uncapped canonical-frame source and bounded Lua FIFO
provides:
  - source-derived finite native allocation and component-read work policy
  - exact pre-call work admission with atomic no-output failure
  - authoritative four-file Lua package authority guard
affects: [05.1-08, 05.1-09, 05.1-07]
actuals:
  tokens: 7950
  tasks: 2
  commits: 3
tech-stack:
  added: []
  patterns:
    - checked native allocation before caller-owned buffer creation
    - exact pre-call work-unit consumption
    - fixed production-path authority allowlist
key-files:
  created:
    - scripts/component_discovery_package_guard.ps1
  modified:
    - extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
    - extensions/live_galaxy/tests/component_discovery_contract.lua
    - extensions/live_galaxy/tests/component_discovery_binding_contract.ps1
    - extensions/live_galaxy/tests/component_discovery_package_guard.ps1
    - extensions/live_galaxy/tests/run_contracts.ps1
key-decisions:
  - "Production native discovery derives allocation and work limits from the 129-member proof obligation and the actual UniverseID size."
  - "The Lua authority guard owns exactly four production paths; every other production path fails closed."
patterns-established:
  - "Native policy snapshot: validate and copy the exact policy before any native call, with no unbounded fallback."
  - "Atomic candidate read: reserve the complete count/fill/member ledger before allocation and consume again immediately before every native or conversion call."
requirements-completed: [OBS-06]
coverage:
  - id: D1
    description: "Native station discovery uses checked allocation bytes and an exact finite work ledger derived from 129 members."
    requirement: OBS-06
    verification:
      - kind: integration
        ref: "extensions/live_galaxy/tests/component_discovery_contract.lua#accepts_the_exact_allocation_and_work_bound"
        status: pass
      - kind: other
        ref: "powershell -NoProfile -File extensions/live_galaxy/tests/component_discovery_binding_contract.ps1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Absent, invalid, one-byte-over, and one-work-unit-over policies fail before the next native call and expose no candidate prefix."
    requirement: OBS-06
    verification:
      - kind: integration
        ref: "powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery"
        status: pass
    human_judgment: false
  - id: D3
    description: "The authoritative package guard scans exactly four allowed production Lua files and rejects authority vocabulary or every other production path."
    requirement: OBS-06
    verification:
      - kind: integration
        ref: "powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery_guard"
        status: pass
      - kind: other
        ref: "powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -SelfTest"
        status: pass
    human_judgment: false
duration: 29m
completed: 2026-09-01
status: complete
---

# Phase 05.1 Plan 06: Native Discovery Budgets and Lua Guard Summary

Source-derived native allocation and exact component-read work budgets now fail closed before partial discovery, while one root guard owns the four-file production Lua authority boundary.

## Performance

- **Duration:** 29m
- **Started:** 2026-09-01T03:02:00+07:00
- **Completed:** 2026-09-01T03:31:00+07:00
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Replaced the retired 64-member product limit with a finite policy derived from 129 members and `ffi.sizeof("UniverseID")`.
- Added checked safe-integer allocation arithmetic and exact count/fill/four-calls-per-member work admission with no unbounded fallback.
- Kept candidates private until fill equality, stable unique identity, owner, sector, capacity, and canonical ordering all validate.
- Centralized the exact four-file Lua package authority guard while retaining the extension-local runner entrypoint.

## Task Commits

1. **Task 1 RED: native budget contracts** - `ae43090` (`test`)
2. **Task 1 GREEN: native allocation and work budgets** - `d06697f` (`feat`)
3. **Task 2: authoritative four-file Lua guard** - `d53a596` (`test`)

## Files Created/Modified

- `extensions/live_galaxy/lua/live_galaxy_component_discovery.lua` - Source-owned native policy, checked allocation, and exact work ledger.
- `extensions/live_galaxy/tests/component_discovery_contract.lua` - Exact-bound, absent/invalid, and one-over behavioral contracts.
- `extensions/live_galaxy/tests/component_discovery_binding_contract.ps1` - Policy binding and retired-path absence assertions.
- `scripts/component_discovery_package_guard.ps1` - Authoritative fixed four-file production guard and negative self-tests.
- `extensions/live_galaxy/tests/component_discovery_package_guard.ps1` - Stable delegating runner entrypoint.
- `extensions/live_galaxy/tests/run_contracts.ps1` - Both component suites now exercise all four production Lua paths.

## Verification

- RED failed as intended on the missing `ffi.sizeof("UniverseID")` policy binding and missing derivation API.
- Passed `component_discovery_binding_contract.ps1` in 324 ms.
- Passed the 23-case `component_discovery` suite in 2115 ms.
- Passed the root guard self-test in 162 ms and four real-file checks in 1268 ms total.
- Passed the `component_discovery_guard` suite in 1708 ms.
- Passed the extension-local guard entrypoint in 348 ms.
- Confirmed the production Lua module is 185 physical lines and `git diff --check` is clean.
- X4 was not launched; native behavior remains pending the later owner-controlled disposable X4 gate.

## Decisions Made

- The initialized production policy has only `max_allocation_bytes` and `max_work_units`; the adapter validates and snapshots it before any native call.
- Count consumes one unit first; successful non-empty candidates must then reserve one fill plus four calls per returned member before allocation.
- The package authority vocabulary applies only to the four named Lua production files and does not scan Rust, tests, or planning prose.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None.

## Threat Model Coverage

- **T-05.1-06-01:** Checked safe-integer allocation multiplication and exact pre-call work admission prevent unbounded native discovery work.
- **T-05.1-06-02:** Candidate observations remain private and are discarded atomically on every mismatch, exhaustion, or invalid fact.
- **T-05.1-06-03:** The exact four-file allowlist and negative path/vocabulary fixtures reject Lua authority expansion.

## Skill Learning

Skill learning: none — considered the RED failure, exact/one-over work and byte tests, absent/invalid policy tests, 185-line production result, four-file guard self-tests, extension entrypoint, and all focused verification. The evidence applies existing bounded-native-call, fail-closed, atomic-candidate, fixed-authority, and local-versus-X4 rules without establishing a new reusable project rule.

## Next Phase Readiness

Plans 05.1-08 and 05.1-09 can consume a finite source-owned native candidate and the exact four-file Lua authority boundary. Native X4 cost and sufficiency remain deliberately unclaimed until Plan 07's owner-controlled run.

## Self-Check: PASSED

All six declared files exist. Commits `ae43090`, `d06697f`, and `d53a596` exist in Git history. Every focused verification is green, the TDD RED/GREEN order is intact, and no goal-blocking stub remains.
