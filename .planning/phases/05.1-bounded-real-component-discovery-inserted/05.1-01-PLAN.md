---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - extensions/live_galaxy/tests/component_discovery_package_guard.ps1
  - extensions/live_galaxy/tests/run_contracts.ps1
  - crates/observation-ingest/tests/runtime_facts_contract.rs
  - tests/x4-disposable/05.1-component-discovery-probe.md
autonomous: true
requirements: [OBS-06]
requirement_note: "No new phase requirement ID exists. OBS-06 is inherited evidence closure; D-09 is a current local evidence contract."
spec_less_probe_fallback: "skipped — Phase 05.1 has no newly assigned requirement IDs"
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A raw native station count over the declared hard limit stops before allocation, ID conversion, metadata, ownership, sector, or capacity reads; a count within the limit declares the entire owner-scoped result, per D-09."
    - "Fake Lua and Rust contracts distinguish unavailable, overflow, malformed, partial, unsupported, and complete component facts without accepting a partial snapshot."
    - "The disposable X4 probe is a bounded human-only procedure whose outcome remains pending until a fresh Creative Custom run records it."
  artifacts:
    - path: extensions/live_galaxy/tests/component_discovery_package_guard.ps1
      provides: Deterministic fixed-path allowlist and authority-boundary guard self-test.
    - path: crates/observation-ingest/tests/runtime_facts_contract.rs
      provides: Strict station-fact admission and prior-projection preservation cases.
    - path: tests/x4-disposable/05.1-component-discovery-probe.md
      provides: One-attempt X4 9.00 Creative Custom probe procedure and evidence-classification ledger fields.
  key_links:
    - from: crates/observation-ingest/tests/runtime_facts_contract.rs
      to: crates/observation-ingest/tests/runtime_facts_contract.rs
      via: Existing compact runtime-facts envelope and atomic admission contract.
      pattern: runtime_facts
  prohibitions:
    - category: trust-boundary
      statement: "Phase 05.1 must remain telemetry-only; it cannot create report, acknowledgement, command, effect, persistence, model, public UI, or game-state mutation paths."
    - category: evidence
      statement: "Local and fake-adapter results cannot be represented as observed X4 native behavior."
---

<objective>
Create passing safety and evidence infrastructure for a bounded real-station discovery
slice before any game-facing implementation changes.

Purpose: prevent an untrusted native enumeration, incomplete scope, or scope-expanding
change from entering the existing telemetry and Rust admission path.
Output: executable Lua/Rust contracts, a phase-local package guard, and a human-only
disposable X4 procedure.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-RESEARCH.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-PATTERNS.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Passing phase-local package guard infrastructure</name>
  <files>extensions/live_galaxy/tests/component_discovery_package_guard.ps1, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <read_first>Read the runner assignment in 05.1-PATTERNS.md and run_contracts.ps1 once. Read 05.1-CONTEXT.md Locked scope before selecting production paths and authority categories.</read_first>
  <behavior>
    - Test 1: the guard accepts the fixed Phase 05.1 production allowlist consisting only of the component-discovery module and its existing discovery orchestrator seam.
    - Test 2: boundary-aware deny categories reject known expected-failure fixtures for authority-expanding token use, an unallowlisted production path, and a source file outside the fixed allowlist.
    - Test 3: the runner returns nonzero deterministically for each expected-failure fixture and green for the allowlisted fixture without scanning tests, documentation, or unrelated packages.
  </behavior>
  <action>Create a deterministic PowerShell guard with an explicit fixed production-path allowlist: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua and extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua. Define boundary-aware deny categories for report, acknowledgement, command, effect, persistence, model, public-UI, and mutation authority, without scanning test files, documents, or other packages. Embed sanitized expected-failure fixture strings for each guard branch and assert that each produces a nonzero guard result while the allowlisted fixture passes. Extend the locked runner with a phase-guard suite that runs this fixture self-test. The guard is passing infrastructure in Wave 0; it does not assert that the planned module already exists.</action>
  <verify><automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery_guard</automated></verify>
  <done>The guard self-test passes, proves its expected-failure branches fail deterministically, and constrains future Phase 05.1 production files to the fixed telemetry-only allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Passing strict station-fact admission and atomic-reconciliation contracts</name>
  <files>crates/observation-ingest/tests/runtime_facts_contract.rs</files>
  <read_first>Read the runtime_facts_contract.rs and batch_reconciliation.rs patterns assigned in 05.1-PATTERNS.md, then read crates/observation-ingest/src/batch.rs and runtime_facts.rs once for current strict decoding and completion-marker semantics.</read_first>
  <behavior>
    - Test 1: station assets have canonical asset, capacity, ownership, and sector links; duplicate, unordered, malformed, or relationship-invalid facts are rejected.
    - Test 2: an incomplete or unsupported component batch has no completion marker and retains the prior accepted projection.
    - Test 3: a complete, valid bounded station scope reconciles only after strict admission accepts the full envelope.
  </behavior>
  <action>Extend the existing Rust contract fixture with compact station-specific envelopes that follow the current s/x/c/o wire shape from 05.1-PATTERNS.md. Assert independent Rust rejection for malformed IDs, duplicate or noncanonical members, invalid ownership/capacity relationships, unknown fields, and incomplete membership. Start from an accepted projection and assert every rejection preserves it; then add a valid complete-scope reconciliation case. These cases must pass against existing strict admission behavior in Wave 0; do not change production admission. Mark OBS-06 as inherited evidence coverage and D-09 as the local no-partial evidence closure.</action>
  <verify><automated>cargo test -p observation-ingest --test runtime_facts_contract --locked</automated></verify>
  <done>The focused Rust test passes and proves station facts cross the existing strict boundary only as a complete, canonical scope; all invalid or incomplete inputs retain the earlier projection.</done>
</task>

<task type="auto">
  <name>Task 3: Write the one-attempt human-only X4 evidence procedure</name>
  <files>tests/x4-disposable/05.1-component-discovery-probe.md</files>
  <read_first>Read the disposable-procedure assignment in 05.1-PATTERNS.md and tests/x4-disposable/01-probe-procedure.md once. Read 05.1-CONTEXT.md Evidence Classification and RESEARCH.md Assumptions Log before choosing the procedure's fields.</read_first>
  <action>Write an English, bounded Creative Custom procedure for exactly one predeclared faction-station scope whose entire returned result must fit one hard raw-count bound. Include installation and disposable-campaign preconditions, one sanitized attempt identifier, one bridge attempt, class-only diagnostics, explicit stop conditions, and a result ledger template recording X4 build, mod list, scenario, elapsed real and game time, SETA state, health, expected versus observed outcome, and evidence class. State the implementation/evidence checkpoint: local implementation may pass local safety contracts but remains pending-X4; native availability, native cost, and owner normalization cannot close until this probe. If a probe fails, retain facts_unsupported and route a future separately authorized revision; do not widen scope, add a cursor, or choose another native seam automatically. Preserve no-save access, disabled-by-default opt-in telemetry, and no raw IDs, payloads, native error text, prompts, or hidden reasoning.</action>
  <verify><automated>powershell -NoProfile -Command "$p='tests/x4-disposable/05.1-component-discovery-probe.md'; $t=Get-Content -Raw $p; if (-not (Test-Path $p) -or $t -notmatch 'Creative Custom' -or $t -notmatch 'pending game smoke test' -or $t -notmatch 'Stop') { exit 1 }"</automated><human-check>Run only after Wave 1 local contracts pass. Use a fresh disposable Creative Custom campaign and append a sanitized ledger entry; do not assert a successful native result before that run.</human-check></verify>
  <done>The repository contains a runnable, one-attempt procedure that classifies runtime evidence honestly and stops on overflow, unsupported calls, unbounded cost, or any mutation signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| X4 native count/fill → Lua adapter | Native values are untrusted until type and hard-bound validation succeeds. |
| Lua observation envelope → Rust admission | Lua facts remain untrusted until strict Rust decoding and atomic reconciliation accept them. |
| disposable probe → evidence ledger | A human-operated game run supplies runtime evidence but must retain sanitized, bounded metadata only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-01 | Denial of service | native station enumeration | high | mitigate | RED counter assertions require the hard count check before allocation, conversion, or component reads. |
| T-05.1-02 | Tampering | Lua-to-Rust runtime facts | high | mitigate | Canonical-order and strict Rust fixtures reject malformed, duplicate, partial, and relationship-invalid facts atomically. |
| T-05.1-03 | Tampering | reconciliation marker | high | mitigate | No-completion-marker cases preserve the prior accepted projection. |
| T-05.1-04 | Elevation of privilege | phase package surface | high | mitigate | Focused package guard confines Phase 05.1 to one-way telemetry discovery. |
| T-05.1-05 | Information disclosure | disposable diagnostics | medium | mitigate | Procedure records class-only, bounded, sanitized attempt evidence and excludes raw runtime values. |
| T-05.1-SC | Tampering | package installs | high | accept | No package installation is planned. |
</threat_model>

<verification>
Run both focused automated contracts. The X4 procedure is intentionally human-only and remains pending until a disposable run produces a ledger entry.
</verification>

<success_criteria>
Wave 0 makes unsafe enumeration, partial scope completion, authority expansion, and fabricated X4 proof impossible to pass unnoticed.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-01-SUMMARY.md` when done.
</output>
