---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 07
type: execute
wave: 6
depends_on: [05.1-06, 05.1-09]
files_modified:
  - tests/x4-disposable/05.1-component-discovery-probe.md
  - .planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
autonomous: false
gap_closure: true
requirements: [OBS-06]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-05.1-TRANSPORT-01 through D-05.1-TRANSPORT-03: production native, FIFO, drain, retry, cadence, and diagnostic budgets are explicitly selected, retained, or revised from recorded Live Galaxy evidence."
    - "Separate normal-speed and SETA 30-tick windows use t0/t1 deltas in identical canonical frame-byte units and finish with zero message and byte queue depth."
    - "The owner-controlled X4 run remains read-only and default-off; absent, failed, or ambiguous evidence remains pending-game-smoke and cannot close G-05.1-1."
    - "Every Lua case except the exact known `telemetry_contract.lua::normalizes_identity_source_time_version_and_quality@32` baseline passes in isolation, and the complete failure set equals only that baseline."
  artifacts:
    - path: tests/x4-disposable/05.1-component-discovery-probe.md
      provides: "Bounded owner-run procedure and sanitized evidence schema"
    - path: .planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
      provides: "Observed-in-X4 or fail-closed gap disposition"
  key_links:
    - from: tests/x4-disposable/05.1-component-discovery-probe.md
      to: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      via: "normal/SETA counter and queue-depth measurements"
    - from: .planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
      to: extensions/live_galaxy/lua/live_galaxy_component_discovery.lua
      via: "measured native count/allocation/time budget decision"
---

<objective>
Run the single owner-controlled X4 checkpoint, retain supported production budgets or require re-planning, and execute complete local closure gates.

Purpose: convert the last native/scheduling unknowns into observed evidence or an explicit fail-closed gap without reopening Phase 05.2.
Output: sanitized normal/SETA evidence, immutable retain/replan decisions, complete local verification, and final G-05.1-1 disposition.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-RESEARCH.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-06-SUMMARY.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-09-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the bounded owner evidence and closure gate</name>
  <files>tests/x4-disposable/05.1-component-discovery-probe.md, .planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md</files>
  <read_first>Read both owned files, Plans 05–06 summaries, the Phase 05.2 passed verification boundary, and `tests/x4-disposable/01-probe-procedure.md`. Do not read saves or private retained evidence.</read_first>
  <action>Update the existing default-off Creative Custom procedure for one authorized attempt with no save access and no mutation. Add a dedicated `Current Run Qualification` table with exact columns `Attempt ID | Mode | Evidence class | Qualification`; it must contain one current-attempt `normal-speed` row and one current-attempt `SETA` row, plus one explicit `Final classification: G-05.1-1 closed` line only when both qualify. Historical tables and the vocabulary `pending game smoke test` remain valid outside this current-run block. Record only bounded sanitized fields: X4 build and extension IDs, native station count class, allocation candidate/result, exact native work-unit total/exhaustion state, fill equality, ordering stability, native elapsed class, selected per-frame byte ceiling, FIFO capacities/watermarks/quotas, retry/cadence, diagnostic cap, and separate `t0`/`t1=t0+30` snapshots. For each window compute message and byte deltas in the exact `canonical_encoded_frame_utf8_bytes` unit, record initial/final depths, and keep `admitted_generation_count` separate. Stop on native inconsistency, work/allocation exhaustion, backlog without drain, byte-unit mismatch, reconnect/retry rejection, mutation, or missing terminal admission. Update UAT instructions/statuses only; do not claim observed evidence before the checkpoint.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='tests/x4-disposable/05.1-component-discovery-probe.md'; $u='.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md'; $t=Get-Content -Raw -LiteralPath $p; $v=Get-Content -Raw -LiteralPath $u; if ($t -notmatch 'canonical_encoded_frame_utf8_bytes' -or $t -notmatch 't0' -or $t -notmatch 't1' -or $t -notmatch 'SETA' -or $t -notmatch 'Stop' -or $v -notmatch 'pending-game-smoke') { exit 1 }"; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -SelfTest</automated>
  </verify>
  <done>The procedure is runnable, bounded, privacy-safe, and cannot promote local/fake evidence or a missing X4 run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Owner runs the disposable X4 measurement</name>
  <files>tests/x4-disposable/05.1-component-discovery-probe.md, .planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md</files>
  <precondition>Plans 05 and 06 focused automated gates pass and the candidate extension is installed in the disposable Creative Custom environment described by the procedure.</precondition>
  <what-built>A default-off read-only candidate with locally passing Lua, Rust, package, and recovery contracts plus a sanitized one-attempt procedure.</what-built>
  <how-to-verify>Run the procedure once at normal speed and SETA, append only its bounded sanitized rows, and confirm every listed stop condition stayed false.</how-to-verify>
  <resume-signal>Reply `observed in X4` after both qualifying rows are recorded, or reply with the exact failed/pending classification so execution stops fail closed.</resume-signal>
  <action>Run exactly the prepared attempt at normal speed and SETA, then append the sanitized rows and explicit outcome to the owned evidence files. Do not inspect a save, retain raw IDs/payloads/native errors, run Docs MCP, modify installed files, or treat a local write as delivery acknowledgement.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='tests/x4-disposable/05.1-component-discovery-probe.md'; $t=Get-Content -Raw -LiteralPath $p; $section=[regex]::Match($t,'(?ms)^## Current Run Qualification\r?\n.*?(?=^## |\z)'); if (-not $section.Success) { exit 1 }; $b=$section.Value; $normal=[regex]::Matches($b,'(?m)^\| [^|]+ \| normal-speed \| observed in X4 \| qualifying \|\r?$').Count; $seta=[regex]::Matches($b,'(?m)^\| [^|]+ \| SETA \| observed in X4 \| qualifying \|\r?$').Count; $final=[regex]::Matches($b,'(?m)^Final classification: `G-05\.1-1 closed`\r?$').Count; if ($normal -ne 1 -or $seta -ne 1 -or $final -ne 1 -or $b -notmatch 'admitted_generation_count') { exit 1 }"</automated>
    <human-check>Confirm the two sanitized rows came from the single authorized disposable run, all stop conditions stayed false, and the final classification is `observed in X4`. If any criterion is absent or failed, retain `pending-game-smoke` or record the explicit gap and stop.</human-check>
  </verify>
  <done>Only one qualifying `observed in X4` current-run row for normal speed and one for SETA, plus the explicit current-run closed classification, close G-05.1-1. A failed, ambiguous, absent, or pending row in either mode leaves Task 2, Plan 07, and Phase 05.1 incomplete.</done>
</task>

<task type="auto">
  <name>Task 3: Record immutable budget decisions and run complete closure</name>
  <files>.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md</files>
  <read_first>Read the Task 2 sanitized rows, `tools/lua-runner.lock.json`, and `extensions/live_galaxy/tests/run_contracts.ps1`. Use only qualifying Live Galaxy evidence. Production owners remain in Plans 05, 06, and 09; this task must not edit their Lua, Rust, or test files.</read_first>
  <behavior>
    - Every production value is either retained unchanged from its owning plan or marked `replan-required`; this task never mutates a value owned outside Plan 07.
    - The Plan 09 cross-layer per-frame ceiling is immutable here and can only be retained on qualifying evidence.
    - Every non-baseline Lua case passes in an isolated locked-runner process; the complete failure set is exactly the one named telemetry baseline.
  </behavior>
  <action>Own only the durable decision record in `05.1-UAT.md`. Add `Current Run Budget Decision` with exact columns `Dimension | Production owner | Pre-run value | Observed value/range | Final value | Decision | Rationale | Evidence row`. Include separate rows for native allocation bytes, native component-read work units, native elapsed evidence, per-frame bytes, FIFO message capacity, FIFO byte capacity, high-water messages, high-water bytes, low-water messages, low-water bytes, enqueue message quota, enqueue byte quota, drain message quota, drain byte quota, retry limit/backoff, normal-speed cadence, SETA cadence, and diagnostic cap. `Decision` is the closed enum `retain | replan-required`. A `retain` row must preserve the exact pre-run production value as `Final value` and cite qualifying current-run evidence. A `replan-required` row must record `Final value = unchanged (replan required)`, make no production/test edit, leave Task 3, Plan 07, and Phase 05.1 incomplete, and route back to a cross-layer implementation plan owning every affected production/test file. The Plan 09 per-frame byte ceiling is specifically immutable in Plan 07: record `retain` only if both modes qualify, otherwise `replan-required`. Apply the same rule to Plan 06 native policy and every Plan 05 FIFO/cadence value; never make a partial Lua-only change, introduce a total station/frame product cap, or disable allocation/work guards.

Then run focused suites, the four-file guard, Rust workspace gates, and the complete Lua runner. Because `run_contracts.ps1` executes a file's cases through unordered `pairs` and stops on the first assertion, classify the known baseline with deterministic per-case isolation using the actual locked command shape from that runner: invoke the pinned Lua executable from `tools/lua-runner.lock.json` with `-e`, extend `package.path` exactly as `run_contracts.ps1` does, load each `extensions/live_galaxy/tests/*_contract.lua` file plus `x4_candidate_runner_adversarial.lua`, sort exported case names, and run each named case in its own process. Require exit zero for every case except the exact key `telemetry_contract.lua::normalizes_identity_source_time_version_and_quality`; require that one process to exit nonzero and its complete diagnostic to identify the normalized `telemetry_contract.lua:32` assertion. Build a sorted failure-key set and require exact equality with the single key `telemetry_contract.lua::normalizes_identity_source_time_version_and_quality@32`. Any additional, missing, renamed, moved, or differently diagnosed failure blocks completion. Run `run_contracts.ps1` without `-Suite` afterward for aggregate coverage, but never use substring presence or an unrelated `git diff` as classification proof.</action>
  <verify>
<automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -SelfTest; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -ProductionPath extensions/live_galaxy/lua/live_galaxy_component_discovery.lua; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -ProductionPath extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -ProductionPath extensions/live_galaxy/lua/live_galaxy_runtime.lua; powershell -NoProfile -File scripts/component_discovery_package_guard.ps1 -ProductionPath extensions/live_galaxy/lua/live_galaxy_telemetry.lua; cargo fmt --all -- --check; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo test --workspace --locked; powershell -NoProfile -Command '& { $root=(Resolve-Path ".").Path; $lock=Get-Content -Raw -LiteralPath (Join-Path $root "tools/lua-runner.lock.json") | ConvertFrom-Json; $lua=Join-Path $root $lock.materialization.executableRelativePath; if (-not (Test-Path -LiteralPath $lua -PathType Leaf)) { throw "locked Lua executable missing" }; $reported=(& $lua -v 2>&1) -join "`n"; if ($reported -notmatch ("(?m)^"+[regex]::Escape([string]$lock.executableVersion)+"(?:\s|$)")) { throw "locked Lua version mismatch" }; $modulePath=Join-Path $root "extensions\?.lua"; $moduleInitPath=Join-Path $root "extensions\?\init.lua"; $extensionLuaPath=Join-Path $root "extensions\live_galaxy\lua\?.lua"; $prefix="package.path = [[$modulePath;$moduleInitPath;$extensionLuaPath;]] .. package.path "; $tests=@(Get-ChildItem -LiteralPath (Join-Path $root "extensions/live_galaxy/tests") -Filter "*_contract.lua" | ForEach-Object FullName); $tests += Join-Path $root "extensions/live_galaxy/tests/x4_candidate_runner_adversarial.lua"; $tests=@($tests | Sort-Object -Unique); $expected="telemetry_contract.lua::normalizes_identity_source_time_version_and_quality"; $failures=[System.Collections.Generic.List[string]]::new(); foreach ($path in $tests) { $listCode=$prefix+"local cases=dofile([[$path]]) local names={} for name,_ in pairs(cases) do names[#names+1]=name end table.sort(names) for _,name in ipairs(names) do print(name) end"; $names=@(& $lua -e $listCode 2>&1); if ($LASTEXITCODE -ne 0) { throw "cannot enumerate Lua cases: $path" }; foreach ($name in $names) { $escaped=([string]$name).Replace("\","\\").Replace("'","\'"); $caseCode=$prefix+"local cases=dofile([[$path]]) cases['$escaped']()"; $caseOut=@(& $lua -e $caseCode 2>&1); $caseCodeExit=$LASTEXITCODE; $base=([IO.Path]::GetFileName($path))+"::"+$name; $diag=(($caseOut -join "`n") -replace "\\","/"); if ($base -eq $expected) { if ($caseCodeExit -eq 0 -or $diag -notmatch "(?:^|/)telemetry_contract\.lua:32:" -or $diag -notmatch "section\.observed_at_unix_millis == 1725000000000") { $failures.Add($base+"@diagnostic-mismatch") } else { $failures.Add($base+"@32") } } elseif ($caseCodeExit -ne 0) { $failures.Add($base+"@unexpected"); $caseOut | ForEach-Object { Write-Output $_ } } } }; $actual=@($failures | Sort-Object); $wanted=@("telemetry_contract.lua::normalizes_identity_source_time_version_and_quality@32"); if ($actual.Count -ne 1 -or $actual[0] -ne $wanted[0]) { Write-Error ("Lua failure set mismatch: "+($actual -join ", ")); exit 1 }; $aggregate=@(& powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 2>&1); $aggregateExit=$LASTEXITCODE; $aggregateText=(($aggregate -join "`n") -replace "\\","/"); $aggregate | ForEach-Object { Write-Output $_ }; if ($aggregateExit -eq 0 -or $aggregateText -notmatch "(?:^|/)telemetry_contract\.lua:32:" -or $aggregateText -notmatch "section\.observed_at_unix_millis == 1725000000000") { throw "aggregate runner did not reproduce the exact isolated baseline" }; Write-Output "EXACT LUA FAILURE SET: $($wanted[0])" }'; git diff --check</automated>
  </verify>
  <done>The durable UAT table records every dimension as exact `retain` or `replan-required` without production mutation; every non-baseline Lua case passes in isolation and the complete failure set equals only `telemetry_contract.lua::normalizes_identity_source_time_version_and_quality@32`.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | Item | Coverage | Plan/task |
| --- | --- | --- | --- |
| GOAL | Real component discovery without synthetic fallback or unbounded work | COVERED | 05-T1, 06-T1/T2, 07-T2/T3 |
| REQ | OBS-06 runtime sectors/assets/capacity/ownership from observed state | COVERED | 05-T1/T2/T3, 06-T2, 07-T2/T3 |
| RESEARCH | Count/fill has no observed paging and needs a measured allocation ceiling | COVERED | 06-T2, 07-T2/T3 |
| RESEARCH | Incremental FIFO, exact bytes, backpressure, and marker-owned completion | COVERED | 05-T1/T2/T3, 06-T1, 07-T2/T3 |
| CONTEXT | D-05.1-TRANSPORT-01 clean-room MCP reference only | COVERED | 05–07 decision/action bodies |
| CONTEXT | D-05.1-TRANSPORT-02 measure each envelope dimension | COVERED | 06-T1, 07-T1/T2/T3 |
| CONTEXT | D-05.1-TRANSPORT-03 no 64-station product cap | COVERED | 05-T1/T2/T3, 06-T1/T2 |
| CONTEXT | Deferred reports, acknowledgements, broader soak, compatibility, Docs MCP | EXCLUDED | Scope fences and actions preserve deferral |
| PHASE 05.2 | Passed verification/conformance/candidate authority remains closed | COVERED | Read-only context; no 05.2 files or dependencies modified |

</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Owner-observed X4 evidence to configuration | Sanitized measurements influence production resource values. |
| Aggregate test result to closure claim | A known unrelated baseline must not mask changed behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-07-01 | Spoofing / repudiation | X4 evidence | high | mitigate | Single owner-controlled attempt, exact fields, normal/SETA rows, explicit classification. |
| T-05.1-07-02 | Tampering | budget selection | high | mitigate | Every value is immutable here and traces to an exact `retain` or `replan-required` decision. |
| T-05.1-07-03 | Repudiation | known Lua baseline | high | mitigate | Per-case locked-runner isolation must yield exactly one named failure key; every other case passes. |
| T-05.1-07-SC | Tampering | dependencies | low | accept | No package installation, network acquisition, or Docs MCP use is authorized. |
</threat_model>

<verification>
The final closure is the conjunction of qualifying owner evidence, all focused changed suites, the fixed four-file guard, `cargo test --workspace --locked`, and the complete Lua runner classification. Local writes remain handoffs; only the Rust terminal marker is admission.
</verification>

<success_criteria>
- Owner evidence qualifies at both normal speed and SETA without mutation or save access.
- Every native/transport budget has an explicit `retain` or `replan-required` decision; required changes leave the phase incomplete.
- Focused suites, guard, format, Clippy, and workspace tests pass.
- The sorted isolated Lua failure set equals only `telemetry_contract.lua::normalizes_identity_source_time_version_and_quality@32`; aggregate substring matching is not evidence.
- Phase 05.2 remains closed and unchanged.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-07-SUMMARY.md` when done.
</output>
