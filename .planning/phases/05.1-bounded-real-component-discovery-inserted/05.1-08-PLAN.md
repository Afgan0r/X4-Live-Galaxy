---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 08
type: execute
wave: 4
depends_on: [05.1-05]
files_modified:
  - crates/observation-ingest/src/batch.rs
  - crates/observation-ingest/src/generation.rs
  - crates/observation-ingest/src/lib.rs
  - crates/observation-ingest/tests/batch_reconciliation.rs
  - crates/observation-ingest/src/batch_budget.rs
  - crates/observation-ingest/tests/batch_bounds.rs
autonomous: true
requirements: [OBS-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05.1-TRANSPORT-03: incremental staging admits 129-plus frames only on a contiguous same-scope/version/generation terminal marker."
    - "Legacy slice batch limits remain enforced for slice callers but are unreachable from the streamed owner-generation API."
    - "Any gap, invalid fact, stale generation, resource exhaustion, or missing marker discards only the candidate and preserves accepted state."
  artifacts:
    - path: crates/observation-ingest/src/generation.rs
      provides: Incremental resource-bounded generation staging
    - path: crates/observation-ingest/tests/batch_reconciliation.rs
      provides: Atomic 129-plus marker-owned regressions
  key_links:
    - from: crates/observation-ingest/src/generation.rs
      to: crates/observation-ingest/src/batch.rs
      via: shared strict frame/fact validation and reconciliation
      pattern: validate
---

<objective>
Create the incremental Rust generation state machine and re-scope legacy slice limits.

Purpose: carry the Lua stream across former Rust aggregate ceilings without weakening bounded legacy callers.
Output: typed incremental staging, atomic marker admission, and exact legacy-bound isolation tests.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-PATTERNS.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-SUMMARY.md
</context>

## Scope Calibration

The five existing files contain 22,100 characters (5,525 read tokens at chars/4); `generation.rs` is new. Fifteen thousand implementation tokens plus 5,000 verification/output tokens yield 25,525 raw tokens, rounded to 26,000. Calibration factor is 1 with sample count 0 and required low confidence. Each task owns at most four files.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Stage one incremental owner generation atomically</name>
  <files>crates/observation-ingest/src/batch.rs, crates/observation-ingest/src/generation.rs, crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/batch_reconciliation.rs</files>
  <read_first>Read the batch reconciliation mapping in `05.1-PATTERNS.md`, `crates/observation-ingest/src/batch.rs`, and `crates/observation-ingest/tests/batch_reconciliation.rs`. Preserve the existing strict frame/runtime-fact validation and atomic reconciliation seam.</read_first>
  <behavior>
    - 129-plus valid frames leave accepted state unchanged until their contiguous terminal marker, then commit once.
    - Missing marker, sequence gap, mixed scope/version/generation, invalid fact, or exhausted staged byte/work budget preserves accepted state.
    - The path stores only bounded staged data/canonical keys and has no total member/frame count cap.
  </behavior>
  <action>Per D-05.1-TRANSPORT-03, extract `generation.rs` as the cohesive incremental state machine. Follow the exact batch reconciliation analog mapped in `05.1-PATTERNS.md`: reuse `batch.rs` strict frame/runtime-fact validation and the existing atomic accepted-state assertion style in `batch_reconciliation.rs` rather than duplicating schema logic. Stage against a private candidate with checked bytes/work and canonical membership keys. Only an exact contiguous marker matching scope, version, generation, and terminal sequence atomically replaces the accepted projection and increments a test-observable admission count once. Export the minimum typed API through `lib.rs`; every error drops the candidate and retains accepted state.</action>
  <verify>
    <automated>cargo test -p observation-ingest --test batch_reconciliation --test runtime_facts_contract --locked; cargo fmt --all -- --check; cargo clippy -p observation-ingest --tests --locked -- -D warnings</automated>
  </verify>
  <done>The incremental API atomically admits a complete 129-plus generation exactly once and rejects every incomplete, invalid, stale, or exhausted candidate without altering accepted state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep aggregate batch ceilings local to slice callers</name>
  <files>crates/observation-ingest/src/batch_budget.rs, crates/observation-ingest/tests/batch_bounds.rs</files>
  <read_first>Read the bounded-slice mapping in `05.1-PATTERNS.md`, `crates/observation-ingest/src/batch_budget.rs`, and `crates/observation-ingest/tests/batch_bounds.rs`. Preserve their exact/one-over counter semantics for legacy callers.</read_first>
  <behavior>
    - Existing exact slice frame/byte/marker/scope limits and one-over rejection remain green.
    - The streamed generation API never constructs `BatchBudget` from total generation length and crosses 128 frames.
  </behavior>
  <action>Keep `BatchBudget` as the bounded in-memory slice API only. Follow the exact-bound and one-over analog in `batch_bounds.rs`; add executable tests that preserve those semantics while proving the Task 1 streamed path cannot reach its frame, observation, or aggregate-byte counters. Do not delete bounded slice safety or turn any legacy value into a streamed owner-generation cap.</action>
  <verify>
    <automated>cargo test -p observation-ingest --test batch_bounds --test batch_reconciliation --locked; cargo fmt --all -- --check; cargo clippy -p observation-ingest --tests --locked -- -D warnings</automated>
  </verify>
  <done>Legacy batch callers retain exact bounded behavior and the streamed 129-plus path bypasses those aggregate ceilings by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Decoded frames to private candidate | Untrusted frames cannot affect accepted state before complete validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.1-08-01 | Tampering | generation staging | high | mitigate | Strict validation and atomic marker-owned replacement. |
| T-05.1-08-02 | Denial of Service | staged resources | high | mitigate | Checked staged bytes/work and immediate candidate discard. |
| T-05.1-08-03 | Repudiation | generation identity | medium | mitigate | Exact scope/version/generation/sequence agreement and once-only counter. |
</threat_model>

<verification>
Focused ingest tests, formatting, and clippy pass for both incremental and legacy APIs.
</verification>

<success_criteria>
- Incremental staging crosses 129 frames without using any legacy aggregate limit.
- Legacy bounds remain executable and local.
</success_criteria>

<output>
Create `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-08-SUMMARY.md` when done.
</output>
