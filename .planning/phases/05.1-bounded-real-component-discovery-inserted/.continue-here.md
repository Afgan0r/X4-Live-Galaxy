---
context: phase
phase: 05.1-bounded-real-component-discovery-inserted
task: 3
total_tasks: 7
status: paused
last_updated: 2026-09-01T18:57:31.728Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Do not resume the old Phase 05.1 execution route. Its fixed 129-member, 64-frame, 1800-byte, FIFO, marker, and transport policies are implementation history, not the accepted target architecture.
- [ ] CONSTRAINT: Do not plan Phase 05.3 before the repository relocation, shared X4 Modding instructions, and three-repository wiring are complete.
- [ ] CONSTRAINT: Do not let the station or heavy-ship proof define generic architecture. Both are conformance examples for source-agnostic collection, scheduling, transport, assembly, and publication contracts.
- [ ] CONSTRAINT: Do not query installed X4 broadly from Live Galaxy. Route game-API research through the separate Docs MCP repository and retain immutable snapshot provenance.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
| --- | --- | --- | --- |
| Autonomous implementation before architecture closure | Phase 05.1 encoded arbitrary limits and a station-shaped design before the complete data-flow contract was understood. | blocking | Reconcile GSD from the accepted architecture and ADR authorities before creating or executing 05.3 plans. |
| Example-driven generic design | Questions and implementation repeatedly treated stations as the architecture rather than one source. | blocking | Require every 05.3 mechanism to be source-agnostic and prove it with at least two materially different conformance examples. |
| Queue capacity used as message or source-scope policy | FIFO, pipe capacity, one-message size, native enumeration size, staged candidates, and durable state were conflated. | blocking | Bound each layer independently and derive numerical policy from measured bottlenecks and safety evidence. |
| Local pipe write treated as semantic success | A successful write does not prove Rust validation, staging, durable commit, or publication. | blocking | Preserve distinct local-handoff, `received`, and `committed` semantics with exact retry identity. |
| Broad mechanical Rust formatting | A Plan 08 command changed 106 unrelated Rust files before they were restored from HEAD. | blocking | Format only explicitly owned files and inspect the path-limited diff before staging. |
| Mixed architecture authority | The old architecture document combined target behavior, decisions, gaps, verification, and phase planning. | advisory | Keep target architecture, ADRs, verification evidence, and GSD delivery state in their separate committed owners. |

<current_state>

Phase 05.2 is complete. Phase 05.1 Plans 05, 06, and 08 are implemented and summarized. Plan 09 has committed RED bridge contracts but is intentionally not implemented. Plan 07 human X4 UAT was not run. The owner stopped further Phase 05.1 execution after a deep architecture review exposed that the legacy numeric limits and station-specific shape were not an acceptable general data-flow foundation.

The architecture has now been normalized into three authorities: `docs/ARCHITECTURE.md`, `docs/architecture-decisions.md`, and `docs/architecture-verification.md`. ADR-LG-023 accepts the successor slicing, but `.planning/ROADMAP.md`, `.planning/STATE.md`, and phase context do not yet represent Phases 05.3-05.5.

The pushed repository checkpoint is `07bb535` on `codex/0.1-shadow-director`. The only untracked state is `.gsd/dispatch-isolation-sentinel.json`; it is machine-local runtime state that must move with the repository but must not be committed.
</current_state>

<completed_work>

- Completed and summarized Phase 05.1 Plans 05, 06, and 08.
- Added the direct marker bridge RED contracts for Plan 09 without implementing the superseded production policy.
- Restored 106 accidental unrelated Rust formatting changes from HEAD while preserving the four Plan 08 owned files and `.gsd/`.
- Replaced the mixed architecture draft with target-only architecture, a normalized ADR ledger, and a verification/probe registry.
- Captured the accepted Phase 05.3-05.5 boundaries in ADR-LG-023.
- Audited, committed, and pushed the repository through `07bb535`.
</completed_work>

<remaining_work>

1. Hard-relocate the complete repository into the owner-approved `live-galaxy` project root. Run from outside both roots; recheck that the destination is still empty; do not overwrite it. Preserve `.git` and `.gsd`, then verify HEAD, branch, remote, tracked/untracked manifests, and the sentinel digest.
1. Let the separately owned task create the shared X4 Modding agent instructions.
1. Wire docs-mcp, live-mcp, and live-galaxy to those instructions.
1. Regenerate project-local typed GSD agent routing from `.planning/config.json` with the shared sync helper, then restart Codex before any typed GSD dispatch.
1. Reconcile unfinished Phase 01/05.1 obligations into the accepted 05.3-05.5 phase boundaries. Do not silently mark real gaps complete and do not carry superseded implementation policy forward.
1. Update `ROADMAP.md` and `STATE.md`, create the owner-native Phase 05.3 directory and `05.3-CONTEXT.md`, then route to `$gsd-plan-phase 05.3`.
</remaining_work>

<decisions_made>

- Phase 05.3 owns the generic observation data-flow foundation and receiver-feedback baseline.
- Phase 05.4 owns the heavy faction-ship end-to-end proof across both X4 and Rust.
- Phase 05.5 owns only station-specific source or compatibility remainder and may disappear if 05.4 closes the shared seam.
- Generic mechanisms are source-agnostic; ships and stations are conformance examples.
- Carrier A starts with `sn_mod_support_apis` behind a carrier-neutral application facade. Carrier B is an owned minimal native module only after executable or measured evidence proves A insufficient.
- Inbound traffic is a small bounded control plane. Observation traffic starts with stop-and-wait and explicit `received`/`committed` dispositions.
- Numerical limits are independent per layer and derived from evidence; legacy `129`, `64`, `1800`, and `256 KiB` values are not production policy merely because prior code or X4 Live MCP used them.
- X4 API research belongs to Docs MCP, not a second Live Galaxy corpus.
- Agent instructions are owned by a separate task and must be wired before 05.3 begins.
</decisions_made>

<blockers>

- Phase 05.3 exists only as an accepted ADR boundary; GSD roadmap, state, and context still require reconciliation.
- Repository relocation and shared instruction wiring are explicit prerequisites before 05.3 planning.
- `gsd-tools query init.resume` reports project-local typed-agent routing stale relative to `.planning/config.json`; regenerate it and restart Codex before planning dispatch.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — after shared-instruction wiring, establishes the final workflow and memory contract.
1. `.planning/HANDOFF.json` — machine-readable pause state and exact next action.
1. This file — failure-derived constraints and transition context.
1. `docs/ARCHITECTURE.md` — target architecture; do not mistake it for current implementation status.
1. `docs/architecture-decisions.md` — accepted ADRs, especially ADR-LG-023 and ADR-LG-024.
1. `docs/architecture-verification.md` — open evidence, disposable probes, and proof boundaries.
1. `.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-DISCUSS-CHECKPOINT.json` — detailed answers, unresolved P0s, and the older phase conflict that led to the successor split.
1. `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` — current GSD state to reconcile, not authority to resurrect superseded 05.1 policy.
1. Personal MemPalace wing `wing_x4_live_galaxy` — recall relevant decisions and operations after reading current repository authorities; repository evidence wins on staleness.

## Critical Anti-Patterns (do NOT repeat these)

- Do not execute `$gsd-execute-phase 05.1` from the old handoff.
- Do not reuse the old native-count or wire-size numbers as architecture defaults.
- Do not design a generic scheduler, queue, candidate map, or publication protocol around only stations.
- Do not treat named-pipe capacity, Lua FIFO capacity, message size, source scope, Rust staging, and durable state as one limit.
- Do not run broad formatting or stage unrelated user files.
- Do not put decisions, verification evidence, current gaps, or phase plans back into `docs/ARCHITECTURE.md`.

## Infrastructure State

- Git branch: `codex/0.1-shadow-director`.
- Pushed checkpoint: `07bb5354105a1eaaae930936ac828c33f07f7206`.
- Remote: local HEAD and upstream matched when the checkpoint was created.
- Untracked machine-local state: `.gsd/dispatch-isolation-sentinel.json`; preserve physically, never commit as project authority.
- X4, bridge, watchers, servers, and async external jobs: none running.
- Saves: never read or modified.
- Relocation destination: an empty placeholder was previously observed, but occupancy must be rechecked immediately before the move.
- Typed GSD routing: stale relative to `.planning/config.json`; regeneration and a Codex restart remain pending after relocation and instruction wiring.

<context>

The immediate task is a repository and instruction transition, not continued Phase 05.1 implementation. Once the repository is in its new project root and all three repositories inherit the common instructions, use the architecture authorities to rebuild the GSD delivery boundary. Phase 05.3 should then be discussed only for genuinely X4-specific unresolved choices; the generic data-flow contract is already documented and must not be reinvented from the station example.
</context>

<next_action>

Start with the hard repository relocation and verification. Do not execute Phase 05.1. After shared instructions and three-repository wiring complete, regenerate project-local GSD routing and restart Codex. Then run `$gsd-resume-work` in the relocated `live-galaxy` repository, reconcile `ROADMAP.md` and `STATE.md` to Phases 05.3-05.5, create `05.3-CONTEXT.md` from the three architecture authorities, and route to `$gsd-plan-phase 05.3`.
</next_action>
