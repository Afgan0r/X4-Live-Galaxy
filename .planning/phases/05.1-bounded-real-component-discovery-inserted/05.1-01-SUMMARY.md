---
phase: 05.1-bounded-real-component-discovery-inserted
plan: 01
subsystem: bounded component discovery safety contracts
tags: [powershell, rust, x4, telemetry, admission]
requires: [phase-05-runtime-admission]
provides: [fixed-path-package-guard, station-fact-admission-contract, disposable-probe-procedure]
affects: [05.1-02, x4-component-discovery]
tech-stack:
  added: []
  patterns: [fixed allowlist, strict admission, atomic reconciliation, human-only evidence]
key-files:
  created:
    - extensions/live_galaxy/tests/component_discovery_package_guard.ps1
    - tests/x4-disposable/05.1-component-discovery-probe.md
  modified:
    - extensions/live_galaxy/tests/run_contracts.ps1
    - crates/observation-ingest/tests/runtime_facts_contract.rs
decisions:
  - "Phase 05.1 production authority is confined to two fixed telemetry-only Lua paths."
  - "Station facts require a complete canonical envelope before replacing the accepted projection."
metrics:
  duration: 28m
  completed: 2026-08-29
status: complete
actuals:
  tokens: 4258
  tasks: 3
  commits: 3
---

# Phase 05.1 Plan 01: Bounded Component Discovery Safety Contracts Summary

Phase-local guardrails now reject authority expansion and invalid station facts,
while a one-attempt Creative Custom procedure preserves the pending-X4 boundary.

## Completed Tasks

1. Added a deterministic fixed-path PowerShell package guard and runner suite.
2. Added canonical station fact rejection and successful reconciliation contracts.
3. Added a sanitized, one-attempt human-only Creative Custom probe procedure.

## Verification

- Passed: `powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery_guard`
- Passed: `cargo test -p observation-ingest --test runtime_facts_contract --locked`
- Passed: `cargo clippy -p observation-ingest --tests --locked -- -D warnings`
- Passed: `cargo test --workspace --locked`
- Passed: structural procedure assertion and `markdownlint-cli2`.
- Deferred, unrelated: the full Lua runner fails in unchanged
  `extensions/live_galaxy/tests/telemetry_contract.lua:32`.

## Decisions Made

- Use a fixed two-path allowlist and boundary-aware authority vocabulary guard
  before Phase 05.1 production discovery exists.
- Treat malformed, duplicate, relationship-invalid, unknown, and incomplete
  station envelopes as atomic rejections that retain the prior projection.

## Deviations from Plan

### Auto-fixed Issues

1. [Rule 1 - Bug] Corrected the package guard token boundary.
   - **Found during:** Task 1 verification.
   - **Issue:** The initial word boundary allowed `send_report`.
   - **Fix:** Treat underscores as separators so authority-bearing identifiers
     are rejected.
   - **Files modified:** `component_discovery_package_guard.ps1`.
   - **Commit:** `8c55329`.

## Deferred Issues

- The complete Lua contract runner fails in pre-existing
  `telemetry_contract.lua:32`; this plan did not modify that file or its
  production dependencies.

## Evidence Status

The X4 component discovery procedure is `pending game smoke test`. No local or
fake result is represented as observed native X4 behavior.

## Self-Check: PASSED

All declared artifacts exist and task commits `8c55329`, `dc2e944`, and
`b334d95` are present in Git history.
