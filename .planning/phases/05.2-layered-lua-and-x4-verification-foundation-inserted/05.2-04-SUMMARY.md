---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 04
subsystem: testing
tags: [powershell, x4, candidate-matrix, build-partition, package-conformance]

requires:
  - phase: 05.2-03
    provides: Deterministic three-axis candidate runner and runtime-evidence v1 contract
provides:
  - Complete seven-row Phase 05.1 runtime-pending candidate matrix
  - Deterministic conflict-safe partition into one shared and one exclusive lifecycle build
  - Developer-only package-faithful build roots with validated digest-linked manifests
affects: [05.2-05, 05.1-runtime-handback]

actuals:
  tokens: 13668
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns: [canonical profile digest, symmetric conflict graph, exact-root generation, per-group package conformance]

key-files:
  created:
    - tests/x4-candidates/phase-05.1-candidates.v1.json
    - tools/x4-verification/contracts/candidate-build-manifest.v1.json
    - tools/x4-verification/build-candidate-extension.ps1
    - tools/x4-verification/tests/candidate_build_contract.ps1
    - tools/x4-verification/templates/candidate-content.xml
    - tools/x4-verification/templates/candidate-ui.xml
  modified: []

key-decisions:
  - "The six candidates that retain the same loader, registration, entrypoint, import root, and ffi.C binding share one build; lifecycle/reload instrumentation is exclusive and conflicts symmetrically with every shared candidate."
  - "Generated roots remain developer-only and prepared-not-executed; each independently passes the existing production-faithful package conformance command before its manifest is written."
  - "Source-resolvable design defects remain explicit exclusions rather than being promoted to X4 runtime candidates."

patterns-established:
  - "Canonical partition: stable candidate order, five-field profile digest, symmetric conflict closure, exclusive singleton groups, and minimal sharing for compatible peers."
  - "Build identity chain: dossier, registry, coverage, matrix, build profile, package conformance, runtime-evidence schema, and generated-file digests are bound in every machine-local manifest."

requirements-completed: [R6]

coverage:
  - id: D1
    description: Exactly seven Phase 05.1 runtime-only questions are represented once and partitioned deterministically by canonical profile, exclusivity, and symmetric conflicts
    requirement: R6
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File tools/x4-verification/tests/candidate_build_contract.ps1 -Case partition"
        status: pass
    human_judgment: false
  - id: D2
    description: Every candidate group generates one developer-only package-faithful root with a validated digest-linked build manifest
    requirement: R6
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File tools/x4-verification/tests/candidate_build_contract.ps1 -Case generated-builds"
        status: pass
    human_judgment: false

duration: 14min
completed: 2026-08-30
status: complete
---

# Phase 05.2 Plan 04: Conflict-Safe Candidate Builds Summary

**Seven Phase 05.1 runtime questions now partition into one shared read-only build and one exclusive lifecycle build, each package-conformant and bound by a validated machine-local manifest.**

## Performance

- **Duration:** 14 min
- **Started:** 2026-08-30T00:11:38Z
- **Completed:** 2026-08-30T00:25:58Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Completed the exact seven-candidate Phase 05.1 matrix with evidence links, expected results, failure classifications, bounded steps, stop conditions, three verdict axes, and explicit source-resolvable exclusions.
- Partitioned six compatible candidates into `p051-build-read-only-shared` and isolated registration-sensitive lifecycle work in `p051-build-lifecycle` with symmetric conflicts.
- Generated and independently conformed both durable machine-local developer roots; their manifests bind seven source/conformance digests plus every generated-file digest and remain `prepared-not-executed`.
- Rejected empty, duplicate, missing, source-action-only, undeclared/asymmetric-conflict, stale-profile, conflicting-group, tampered-manifest, repository, public-package, filesystem-root, and X4-installation inputs before unsafe output.

## Task Commits

Each TDD gate was committed atomically:

1. **Task 1 RED: candidate partition contract** - `305c578` (`test`)
2. **Task 1 GREEN: complete matrix and manifest contract** - `8b4fe1a` (`feat`)
3. **Task 2 RED: isolated build and destination contract** - `9d97134` (`test`)
4. **Task 2 GREEN: package-faithful builder and templates** - `062f27d` (`feat`)

## Files Created/Modified

- `tests/x4-candidates/phase-05.1-candidates.v1.json` - Exact seven-row pending matrix, source exclusions, canonical profiles, conflicts, and build groups.
- `tools/x4-verification/contracts/candidate-build-manifest.v1.json` - Required identity chain, generated-file contract, bounds, and prohibited destinations/controls.
- `tools/x4-verification/build-candidate-extension.ps1` - Fail-closed exact-root validator, group generator, per-group conformance runner, and manifest writer.
- `tools/x4-verification/tests/candidate_build_contract.ps1` - Independent partition, negative, destination, generation, conformance, and tamper contracts.
- `tools/x4-verification/templates/candidate-content.xml` - Developer-only extension identity and Mod Support dependency template.
- `tools/x4-verification/templates/candidate-ui.xml` - Menus environment, dependency, and entrypoint registration template.

## Decisions Made

- Lifecycle/reload receives its own root because it changes the registration profile; the other six candidates retain an identical package and binding profile and therefore share one build.
- Generated entrypoints register the developer harness and expose the generic Plan 03 runner without executing candidates automatically. Generation never launches X4, installs an extension, accesses saves, or changes the Phase 05.1 production seam.
- One runtime run may measure a candidate envelope but cannot select a production ceiling, invent native paging, truncate the owner scope, or equate transport framing with native allocation.

## Verification

- `candidate_build_contract.ps1 -Case partition`: PASS, including all malformed partition negatives.
- `candidate_build_contract.ps1 -Case generated-builds`: PASS, including exact-root generation, Plan 02 conformance per group, tamper detection, and destination rejection.
- `candidate_build_contract.ps1 -Case all`: PASS.
- Durable machine-local `p051-build-lifecycle`: PASS; one candidate, `prepared-not-executed`, package conformance digest `59051413a9d2636a88757a535e31ce8d48a20957a05a2de3aa83dd5d56c020ef` independently reproduced.
- Durable machine-local `p051-build-read-only-shared`: PASS; six candidates, `prepared-not-executed`, package conformance digest `b67012afbe766307bbe0e91a2285060cd4c21f60c32fb9a6b58c40214f2c7dc1` independently reproduced.
- `run_contracts.ps1 -Suite x4-verification`: PASS.
- `run_contracts.ps1 -Suite all`: unchanged pre-existing failure at `telemetry_contract.lua:32`; all Phase 05.2 admission and package-conformance gates passed first.
- `cargo fmt --all -- --check`: PASS.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: PASS.
- `cargo test --workspace --locked`: unchanged known flaky `strategic-state/tests/packet_determinism.rs` failure in this run; no Rust file changed.
- `cargo run -p source-size-lint --locked -- crates tools`: unchanged pre-existing violations at `batch.rs` (231), `runtime_facts_contract.rs` (222), and `named_pipe_contract.rs` (226).
- Git diff from the Plan 03 baseline contains only the six Plan 04 artifacts; the public package and Phase 05.1 production seam are unchanged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved one-item PowerShell validation collections**

- **Found during:** Task 1 GREEN.
- **Issue:** A one-item sorted pipeline became a scalar and had no `.Count`, obscuring the intended partition assertion.
- **Fix:** Normalized unique values and profile properties to explicit arrays before count comparisons.
- **Files modified:** `tools/x4-verification/tests/candidate_build_contract.ps1`.
- **Verification:** `candidate_build_contract.ps1 -Case partition`.
- **Committed in:** `8b4fe1a`.

**2. [Rule 1 - Bug] Summed generated file bytes explicitly**

- **Found during:** Task 2 GREEN.
- **Issue:** `Measure-Object -Property bytes` could not read ordered-dictionary entries and returned an internal build error.
- **Fix:** Accumulated each validated generated-file byte count explicitly before enforcing the total bound.
- **Files modified:** `tools/x4-verification/build-candidate-extension.ps1`.
- **Verification:** `candidate_build_contract.ps1 -Case generated-builds` and both durable machine-local builds.
- **Committed in:** `062f27d`.

---

**Total deviations:** 2 auto-fixed Rule 1 bugs.
**Impact on plan:** Both fixes preserve strict deterministic validation and introduce no runtime, game, or production scope.

## Issues Encountered

- The full Lua suite retains the authoritative pre-existing `telemetry_contract.lua:32` failure.
- Workspace tests hit the known unrelated `packet_determinism` flake in this run.
- The repository-wide source-size gate retains its three authoritative pre-existing Rust violations. No Rust file was modified.
- The first sandboxed Git index write was denied at `.git/index.lock`; the identical scoped commit succeeded with approved Git access, with no reset, stash, or staging fallback.

## Known Stubs

None. `runtime-pending` and `prepared-not-executed` are deliberate evidence classifications at the locked no-X4-execution boundary, not unfinished implementations.

## Skill Learning

Skill learning: none — evidence considered included partition negatives, canonical profile hashing, symmetric/exclusive conflict enforcement, exact-root cleanup, package conformance in both groups, digest tampering, destination rejection, PowerShell collection mechanics, and all focused/full baselines. The fixes instantiate existing deterministic, bounded, fail-closed, developer-only, production-faithful, and no-game-mutation rules; they do not establish a missing reusable project rule.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 05.2-05 can validate either machine-local build manifest against `candidate-build-manifest.v1.json` before retaining runtime evidence.
- Phase 05.1 can later execute the two prepared roots through its human-only disposable X4 gate; no candidate has been executed or admitted here.
- Phase-level closure still requires the existing Lua telemetry, flaky Rust determinism, and source-size baselines to be resolved by their owners; Plan 05.2-04 neither weakens nor modifies them.

## Self-Check

PASSED — all seven repository artifacts exist, all four TDD commits resolve in Git, both focused cases pass through the aggregate contract, both durable machine-local manifests reproduce their generated-file and package-conformance digests, and the final diff passes `git diff --check`.

---

*Phase: 05.2-layered-lua-and-x4-verification-foundation-inserted*
*Completed: 2026-08-30*
