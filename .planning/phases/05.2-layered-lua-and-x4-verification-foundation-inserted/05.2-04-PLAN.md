---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 04
type: execute
wave: 4
depends_on: [05.2-03]
files_modified:
  - tests/x4-candidates/phase-05.1-candidates.v1.json
  - tools/x4-verification/contracts/candidate-build-manifest.v1.json
  - tools/x4-verification/build-candidate-extension.ps1
  - tools/x4-verification/tests/candidate_build_contract.ps1
  - tools/x4-verification/templates/candidate-content.xml
  - tools/x4-verification/templates/candidate-ui.xml
autonomous: true
requirements: [R6]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "R6/D-06: all seven Phase 05.1 runtime-only questions are pending candidates; source-resolvable defects are excluded and returned to their owner."
    - "D-04: candidates share a generated build only when loader/registration/binding profiles are identical and conflict-free; conflicting candidates use separate roots."
    - "Every generated root is package-conformant, developer-only, and described by a validated machine-local build manifest."
  artifacts:
    - tests/x4-candidates/phase-05.1-candidates.v1.json
    - tools/x4-verification/contracts/candidate-build-manifest.v1.json
    - tools/x4-verification/build-candidate-extension.ps1
    - tools/x4-verification/tests/candidate_build_contract.ps1
  key_links:
    - "The final candidate matrix deterministically partitions stable candidate IDs by `build_profile_digest`, `exclusive_build`, and symmetric `conflicts_with` metadata."
    - "Each generated root contains machine-local `manifest/build-manifest.v1.json` linking dossier, registry, coverage, matrix, build profile, generated-file, and package-conformance digests for Plan 05."
---

<objective>
Produce the complete Phase 05.1 candidate matrix and deterministically partition it into conflict-safe isolated test-extension builds (R6; D-04, D-06).

Purpose: Prepare every runtime-only question for later human execution without putting loader/registration conflicts in one build or changing the production seam.
Output: Final versioned matrix, build-manifest contract, isolated builder, templates, and partition/conformance tests.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-PATTERNS.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-03-SUMMARY.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-VERIFICATION.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-04-X4-REFERENCE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-RESEARCH.md
@tests/x4-disposable/01-install-guard.ps1
@tests/x4-candidates/lua/live_galaxy_candidate_runner.lua
@tools/x4-verification/contracts/runtime-evidence.v1.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Partition the seven Phase 05.1 candidates into safe build groups</name>
  <files>tests/x4-candidates/phase-05.1-candidates.v1.json, tools/x4-verification/contracts/candidate-build-manifest.v1.json, tools/x4-verification/tests/candidate_build_contract.ps1</files>
  <behavior>
    - R6/D-06: the matrix contains exactly `p051-native-count-fill-runtime`, `p051-native-fill-completeness`, `p051-native-identity-closure`, `p051-native-volume-envelope`, `p051-lifecycle-reload`, `p051-cadence-seta`, and `p051-mod-stack-compatibility`, all `runtime-pending`.
    - Each candidate declares `build_group`, canonical loader/registration/entrypoint/import-root/binding profile fields, `build_profile_digest`, `exclusive_build`, and symmetric `conflicts_with` IDs.
    - Candidates share a group only when profile digests match, neither is exclusive, and no conflict edge exists; loader/registration changes are exclusive and require distinct group IDs/generated roots.
    - Empty, duplicate, missing, source-action-only, asymmetric-conflict, undeclared-conflict, profile-mismatch, or conflicting-same-group matrices fail closed.
  </behavior>
  <action>Write matrix and partition negatives first. Create the final seven-row matrix from the saturated Phase 05.1 research per D-06, including evidence IDs, expected results, failure classifications, bounded steps, stop conditions, three verdict axes, and explicit source-resolvable exclusions. Define a deterministic partition: sort candidate IDs; canonicalize the loader/registration/entrypoint/import-root/binding profile; verify its declared digest; then sort groups by ID. Allow sharing only for identical profiles with no conflicts. Any loader or registration change must set `exclusive_build=true` and occupy a unique group/root per D-04. Define the committed manifest schema for later machine-local build outputs. Do not convert Phase 05.1 source defects into X4 candidates.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/candidate_build_contract.ps1 -Case partition</automated>
  </verify>
  <done>The exact seven candidates partition deterministically; safe peers share a build, every loader/registration conflict separates, and all malformed or incomplete matrices fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate and conform every isolated build root</name>
  <files>tools/x4-verification/build-candidate-extension.ps1, tools/x4-verification/tests/candidate_build_contract.ps1, tools/x4-verification/templates/candidate-content.xml, tools/x4-verification/templates/candidate-ui.xml</files>
  <behavior>
    - One deterministic machine-local extension root is generated per validated build group; no candidate appears in two roots.
    - Each root passes Plan 02 package conformance and contains no launcher, save access, mutation, acknowledgement, report, or public-package control.
    - Each root writes `manifest/build-manifest.v1.json` with schema version, group/profile IDs, dossier/registry/coverage/matrix/package-conformance digests, generated-file digests, and runtime-evidence schema identity.
    - Repository public-package roots, game installation roots, broad cleanup targets, or manifest/digest mismatch are rejected.
  </behavior>
  <action>Write grouped-build and conflict negatives first. Follow `tests/x4-disposable/01-install-guard.ps1` for an isolated verification-only package root, but deliberately generate outside the repository public package and game installation. Generate content/UI registration, entrypoint, candidate runner, matrix subset, and the exact machine-local `manifest/build-manifest.v1.json` under a caller-supplied durable build root. Validate paths before creating or cleaning an exact group directory. Run Plan 02 conformance separately for every group and bind its digest into that group's manifest. Reject same-root loader/registration conflicts before writing. This phase prepares files only: it must not launch X4, access saves, install into the game, mutate Phase 05.1 production, or claim admission.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/candidate_build_contract.ps1 -Case generated-builds</automated>
  </verify>
  <done>Every safe build group has one deterministic package-conformant root and validated manifest, every conflicting group has a separate root, and no X4/public-package/production mutation occurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Candidate matrix to partitioner | Build metadata and conflicts are untrusted until canonicalized and validated. |
| Builder output to later human X4 run | Generated extension structure approaches a human-only boundary but is not installed or executed here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-13 | Tampering | build partition | high | mitigate | Canonical profile digests, symmetric conflicts, exclusive loader/registration groups, and deterministic ordering. |
| T-05.2-14 | Elevation of privilege | generated destination | high | mitigate | Reject public package/game roots and require an explicit validated machine-local root. |
| T-05.2-15 | Repudiation | build manifest | medium | mitigate | Versioned manifest links every source and generated artifact digest. |
| T-05.2-16 | Denial of service | grouping/build | medium | mitigate | Bound candidates, groups, generated files, bytes, path depth, and conformance diagnostics. |
| T-05.2-SC | Tampering | package supply chain | low | accept | No install occurs; existing pinned tools and committed templates are used. |
</threat_model>

<verification>
Focused: run both `candidate_build_contract.ps1` cases and Plan 02 package conformance against every generated fixture root.

Full: run `run_contracts.ps1 -Suite all`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, and `cargo run -p source-size-lint --locked -- crates tools`. Verify no public package, Phase 05.1 production seam, X4 installation, or save changed.

Before completing the summary, apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` and record the required `Skill learning: none` evidence statement or a complete candidate. Missing closure blocks completion.
</verification>

<success_criteria>
R6 preparation is complete: every runtime-only candidate is represented once, conflicts partition deterministically into separate builds, safe candidates share only compatible builds, and every generated root has a package-conformance-linked manifest.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-04-SUMMARY.md` when done.
</output>
