---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/x4-verification/x4-admission.ps1
  - tools/x4-verification/contracts/dossier.v1.json
  - tools/x4-verification/contracts/known-failures.v1.json
  - tools/x4-verification/contracts/coverage.v1.json
  - tools/x4-verification/contracts/owner-override.v1.json
  - tools/x4-verification/fixtures/negative-fixtures.v1.json
  - tools/x4-verification/tests/admission_contract.ps1
  - extensions/live_galaxy/tests/run_contracts.ps1
autonomous: true
requirements: [R1, R2]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "R1/D-01: no artifact is admissible until the independent policy accepts it."
    - "R2/D-02: all seven known X4 failure classes have versioned coverage and executable negative evidence."
    - "A small known exception remains blocked unless an explicit owner override identifies the exact finding."
  artifacts:
    - tools/x4-verification/contracts/dossier.v1.json
    - tools/x4-verification/contracts/known-failures.v1.json
    - tools/x4-verification/fixtures/negative-fixtures.v1.json
    - tools/x4-verification/x4-admission.ps1
    - tools/x4-verification/tests/admission_contract.ps1
  key_links:
    - "The admission command loads the dossier, registry, coverage, and optional owner override by declared schema version."
    - "Every coverage claim resolves to an executable fixture and every registry entry resolves to primary and precedent evidence identifiers."
---

<objective>
Establish the first executable Phase 05.2 slice: a versioned validation dossier, a seven-class known-failure registry, executable negative fixtures, and an independent fail-closed admission gate (R1, R2; D-01, D-02).

Purpose: Make later package conformance, candidate execution, and retained runtime evidence consumers of one explicit policy rather than independent sources of truth.
Output: Versioned JSON contracts, fixtures, a bounded admission CLI, and focused PowerShell contracts.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-PATTERNS.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/debug/resolved/phase-051-component-require.md
@.planning/debug/phase1-lua-runner-gap.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Admit one complete versioned dossier end to end</name>
  <files>tools/x4-verification/contracts/dossier.v1.json, tools/x4-verification/contracts/known-failures.v1.json, tools/x4-verification/fixtures/negative-fixtures.v1.json, tools/x4-verification/x4-admission.ps1, tools/x4-verification/tests/admission_contract.ps1</files>
  <behavior>
    - R1/D-01: a complete v1 dossier plus known-failure registry reaches an explicit admissible/non-admissible verdict with stable reason codes.
    - The dossier requires loader/registration, extension-relative module resolution, Lua/native binding, exact arguments/returns, native/canonical identity, lifecycle/game-thread ownership, cadence/reconnect, failure/partial/completeness, and source-supported or observed volume behavior, each marked `EVIDENCED`, `CONFLICTING`, or `UNKNOWN` with both primary-source and independent verified working-production-precedent provenance.
    - R2/D-02: the registry names `loader-mismatch`, `native-binding-assumptions`, `native-canonical-identity-mismatch`, `invented-unmeasured-bounds`, `partial-results-represented-complete`, `permissive-local-harness`, and `isolated-call-shape-research`, each with primary and precedent evidence identifiers.
    - Unknown versions, every individually missing required field, either missing provenance class on any dimension, unverified or non-production precedent, `UNKNOWN`/`CONFLICTING` evidence without an exact owner decision, duplicate IDs, excess rows/bytes, and unresolved evidence IDs fail closed without partial output.
  </behavior>
  <action>Write the failing `-Case dossier` contract first, then implement a dependency-free PowerShell admission entry point. Follow `tools/verify_xen_khk_evidence.ps1` for `Set-StrictMode`, exact required-field handling, stable IDs, provenance links, and nonzero failure exits; follow `extensions/live_galaxy/checkpoint_schema.json` for committed schema identity while keeping validation independent. Require every dossier dimension—not only registry entries—to carry a primary-source record and a separately identified, verified working-production-precedent record; reject either missing class, a self-reference, or precedent without verification scope. Keep the policy owner independent of any runner per D-01. Treat the registry as known-failure evidence, not proof that the Phase 05.1 seam is admitted, per D-02. Reject unknown schema versions, ambiguous booleans, duplicate identifiers, unbounded arrays, and malformed evidence references before evaluating admissibility. Do not invoke X4, read saves, inspect private evidence, or modify the Phase 05.1 production seam.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case dossier</automated>
  </verify>
  <done>The complete fixture traverses the real parser and policy to a stable verdict; every malformed or over-bound dossier is rejected, and all seven R2/D-02 failure classes are machine-enumerable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind coverage claims to seven executable negative fixtures</name>
  <files>tools/x4-verification/contracts/coverage.v1.json, tools/x4-verification/fixtures/negative-fixtures.v1.json, tools/x4-verification/tests/admission_contract.ps1, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <behavior>
    - R2/D-02: each registry class resolves to at least one named fixture that produces the expected stable failure code.
    - A claimed-covered class with a missing, skipped, mismatched, duplicated, or passing negative fixture is rejected; newly added registry rows fail until covered by evidence plus an executable check or a justified not-applicable row.
    - Fixture inputs and output are byte-, row-, and time-bounded and execute in deterministic ID order.
  </behavior>
  <action>Extend the test first with the seven negative cases, then add versioned coverage and a single fixture bundle containing complete and one-dimension-mutated cases. Follow `extensions/live_galaxy/tests/persistence_schema_contract.ps1`: independently decode fixtures, mutate one dimension per case, and assert process exit/result codes rather than source text. Execute every fixture through the real admission command. Fix fixture clocks and ordering; cap bytes, entries, reasons, and output. Register focused `x4-admission` and aggregate `x4-verification` suites without changing existing behavior. Preserve the seven-class D-02 taxonomy; do not collapse or waive a class.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case negative-fixtures</automated>
  </verify>
  <done>All seven negative fixtures fail for their intended reason and the coverage contract rejects every false-coverage edge listed in AC-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce known-failure ownership and exact owner overrides</name>
  <files>tools/x4-verification/contracts/owner-override.v1.json, tools/x4-verification/x4-admission.ps1, tools/x4-verification/tests/admission_contract.ps1</files>
  <behavior>
    - R1/D-01: a known failure, including a small exception, remains non-admissible by default.
    - An owner override applies only to the exact finding ID, dossier digest, expiry, rationale, remaining-risk statement, and owner decision it names.
    - Missing, stale, broad, expired, or digest-mismatched overrides fail closed and cannot mutate the source dossier.
  </behavior>
  <action>Add failing known-failure and override tests before implementation. Introduce an optional versioned override document that is explicit, exact-scope, non-mutating, and separately reported. Require a stable dossier SHA-256 digest, finding ID, owner decision ID, rationale, remaining-risk statement, and bounded expiry per D-01. Do not auto-admit based on severity, count, or perceived smallness. Emit safe diagnostics only: logical IDs and digests, never local paths or private values.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case admission</automated>
  </verify>
  <done>Known failures are blocked by default, only an exact valid owner override changes that one disposition, and the original dossier remains byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSON dossier/fixture to admission CLI | Repository and generated JSON are untrusted until schema, semantic, and bound checks pass. |
| Owner override to policy result | A high-authority exception crosses into an independently owned admission decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-01 | Tampering | versioned policy JSON | high | mitigate | Exact schema versions, canonical digest binding, duplicate rejection, and immutable inputs. |
| T-05.2-02 | Elevation of privilege | owner override | high | mitigate | Exact finding/digest/decision scope, expiry, and no severity-based auto-admission. |
| T-05.2-03 | Denial of service | JSON parsing and fixture execution | medium | mitigate | Hard byte, row, reason, and execution bounds with deterministic order. |
| T-05.2-04 | Information disclosure | diagnostics | medium | mitigate | Emit only logical IDs, stable codes, and digests; test against private path leakage. |
| T-05.2-SC | Tampering | package supply chain | low | accept | No npm, pip, cargo, or other package installation occurs in this plan. |
</threat_model>

<verification>
Focused: run all three `admission_contract.ps1` cases and `run_contracts.ps1 -Suite x4-admission`.

Full: run `run_contracts.ps1 -Suite all`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, and `cargo run -p source-size-lint --locked -- crates tools`; record the authoritative pre-existing baseline and introduce no new failure. Preserve the repository ban on production `unwrap`, `expect`, `panic!`, and `unsafe`; do not weaken a gate to obtain green status.

Before completing the summary, apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` and record the required `Skill learning: none` evidence statement or a complete candidate. Missing closure blocks completion.
</verification>

<success_criteria>
R1 and R2 are executable: one versioned independent policy owns admission, all seven known failure classes have negative fixtures, and no known failure is silently admitted. The Phase 05.1 seam remains explicitly runtime-pending.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-01-SUMMARY.md` when done.
</output>
