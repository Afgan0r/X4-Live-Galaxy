---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 08
type: execute
wave: 7
depends_on: [05.2-06, 05.2-07]
files_modified:
  - tools/x4-verification/candidate-adapters.psm1
  - tools/x4-verification/run-candidate-package.ps1
  - tools/x4-verification/isolation/candidate-worker.ps1
  - tools/x4-verification/build-candidate-extension.ps1
  - tools/x4-verification/contracts/candidate-build-manifest.v1.json
  - tools/x4-verification/templates/candidate-entry.lua
  - tools/x4-verification/tests/candidate_package_runtime_contract.ps1
  - tools/x4-verification/tests/candidate_package_adversarial.ps1
  - tools/x4-verification/tests/candidate_build_contract.ps1
  - tests/x4-candidates/05.1-candidate-run-procedure.md
autonomous: true
gap_closure: true
requirements: [R6]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "R6: both developer-only candidate roots are runnable through a verified local process boundary without launching X4 or changing the public runtime."
    - "All seven candidate IDs have concrete declarative adapters, stable serial scheduling, fixed digest/verdict services, bounded JSONL output, and a human-triggered dispatcher."
    - "Blocking/native-class work can only use Plan 07's terminable external worker protocol; generated Lua contains no direct `ffi.C` execution path."
    - "Local-process readiness is verified by executable self-tests; retainable producer identity additionally requires a repository-rooted `candidate-producer` delegated certificate and payload signature, never manifest status strings alone."
  artifacts:
    - tools/x4-verification/run-candidate-package.ps1
    - tools/x4-verification/candidate-adapters.psm1
    - tools/x4-verification/templates/candidate-entry.lua
    - tools/x4-verification/tests/candidate_package_adversarial.ps1
  key_links:
    - "The builder copies the exact dispatcher/adapters/worker protocol digests into each manifest only after held-out local-process readiness passes."
    - "The human-triggered dispatcher schedules the seven matrix adapters through the fixed external worker, canonicalizes JSONL, hashes with .NET SHA-256, and signs only when an already owner-provisioned CNG key matches a root-signed `candidate-producer` purpose/epoch/scope certificate."
---

<objective>
Turn the seven-candidate scaffold into a runnable developer-only local-process package while keeping actual X4 execution human-owned and pending (R6).

Purpose: Make preparation concrete and execution-ready at a verifiable local boundary without adding public controls, direct native calls, saves, mutation, or automatic game launch.
Output: Seven adapters, serial dispatcher/scheduler, terminable worker integration, fixed digest/JSONL sink, repository-rooted producer-attestation mechanism, execution-ready local manifests, and held-out package tests. Production producer authority may remain explicitly unconfigured.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/live-galaxy-code-review/SKILL.md
@.agents/skills/live-galaxy-skill-evolution/SKILL.md
@C:/Users/pavlo/.agents/skills/lua/SKILL.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VERIFICATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY-FIX.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW-FIX.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-04-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-06-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-07-SUMMARY.md
@tests/x4-candidates/phase-05.1-candidates.v1.json
@tools/x4-verification/build-candidate-extension.ps1
@tools/x4-verification/contracts/candidate-build-manifest.v1.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run all seven adapters through the local isolated dispatcher</name>
  <files>tools/x4-verification/candidate-adapters.psm1, tools/x4-verification/run-candidate-package.ps1, tools/x4-verification/isolation/candidate-worker.ps1, tools/x4-verification/tests/candidate_package_runtime_contract.ps1</files>
  <behavior>
    - RED/GREEN: exact adapters exist for count/fill call-shape, fill completeness, identity closure, measured envelope, lifecycle/reload, cadence/SETA, and mod-stack coexistence; each validates only its declared bounded input/output and stop conditions.
    - The dispatcher loads the committed group subset, schedules stable candidate IDs serially, invokes every adapter through the fixed Plan 07 external worker, continues after closed failures, and never accepts callback/module/command paths from candidate data.
    - .NET SHA-256 over canonical payload and internal verdict evaluation are fixed dispatcher services; a bounded owner-only JSONL sink writes atomically and closes before signing.
    - A retainable `candidate-producer` envelope carries the root-signed delegated certificate and binds protocol/purpose/epoch/scope, dispatcher/adapter/worker/schema/build/package/matrix digests, run/candidate IDs, evidence digest, start/end, classification, nonce, and expiry.
    - Production dispatchers accept no root/certificate/key/test-mode selector: the anchor is repository-pinned, the purpose certificate comes from the fixed owner-provisioned location, and its SPKI must match the already provisioned CNG key. Missing authority yields `PRODUCER_ATTESTATION_UNCONFIGURED` after a valid local run and before any retainable-success claim.
  </behavior>
  <action>RED: add a local process happy path for both build groups plus adapter-specific malformed/partial/identity/bound/timeout failures; make it fail because the current package has no dispatcher/adapters. Add production black-box cases for absent delegated key/certificate, fresh root, root swap, test-root certificate, wrong purpose/epoch/scope, and delegated-key substitution. GREEN: implement declarative adapters and a human-triggered PowerShell dispatcher. All possibly blocking adapter execution goes through `invoke-candidate-worker.ps1`; `candidate-worker.ps1` imports only the fixed adapter module and exact allowlisted IDs. Compute canonical SHA-256 internally with .NET and write JSONL to an exact owner-only destination using atomic create/close. Invoke Plan 06's fixed repository-root verifier, require the root-signed `candidate-producer` certificate, and sign only with its matching already provisioned CNG key. Never generate/enroll/import/replace a key or accept alternate root/key/certificate paths. A valid local run remains execution-ready but non-retainable with `PRODUCER_ATTESTATION_UNCONFIGURED` when owner provisioning is absent. The fixture provider proves process readiness only and must emit `authenticated-local-contract`, never `observed-in-X4`; isolated test helpers may exercise a TEST-ONLY positive signer, but production CLI must reject it. On non-Windows, local pure checks may run, while producer signing returns `PRODUCER_ATTESTATION_PLATFORM_UNSUPPORTED` and cannot cross retention.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/candidate_package_runtime_contract.ps1 -Case adapters</automated>
  </verify>
  <done>All seven adapters execute deterministically through a terminable worker; retainable evidence is produced only when the separately provisioned repository-rooted delegated key is present, while unconfigured/test-root/non-Windows authority stays explicitly non-retainable without launching X4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate execution-ready local-process roots and procedure</name>
  <files>tools/x4-verification/build-candidate-extension.ps1, tools/x4-verification/contracts/candidate-build-manifest.v1.json, tools/x4-verification/templates/candidate-entry.lua, tools/x4-verification/tests/candidate_build_contract.ps1, tests/x4-candidates/05.1-candidate-run-procedure.md</files>
  <behavior>
    - RED/GREEN: each generated root contains package XML, inert developer metadata entrypoint, runner, exact group subset, adapter/dispatcher/worker assets, protocol/contracts, and digest manifest; no required runtime component is listed as a future implementation gate.
    - Builder reports `execution-ready-local-process` and `terminable-external-isolation` only after running the real local readiness contract and independently binding every generated file plus package conformance result.
    - Status text alone, copied readiness fields, missing component, changed tool digest, direct `ffi.C`, public-package/game-install/save destination, or an existing/reparse group root fails.
    - Procedure gives concrete local preflight/dispatcher/retention commands and separately states that X4 launch and observation remain a later human gate.
  </behavior>
  <action>RED: replace scaffold-only success expectations with executable local-boundary expectations while retaining every partition/path/tamper/security negative. GREEN: move generated entrypoint content to `candidate-entry.lua`; it may register developer metadata but cannot execute candidates, call `ffi.C`, expose a public control, or launch anything. Extend the builder to copy the exact runtime assets, run the real local dispatcher self-test for every group, and write component/protocol/dispatcher/worker digests plus scoped readiness into the manifest. Readiness means the local process contract is invokable and terminable, not that X4 was observed. Rewrite the procedure's blocking-future list into concrete local readiness steps and a distinct later human X4 boundary; never instruct save access or automatic installation/launch.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/candidate_build_contract.ps1 -Case all</automated>
  </verify>
  <done>Both roots are locally runnable with every required component and verified digest, while public/production X4 surfaces remain unchanged and actual game execution remains pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hold out readiness forgery and dispatcher escape attacks</name>
  <files>tools/x4-verification/tests/candidate_package_adversarial.ps1, tools/x4-verification/run-candidate-package.ps1, tools/x4-verification/build-candidate-extension.ps1</files>
  <behavior>
    - Held-out cases forge execution-ready strings, fresh/swapped/test roots, purpose/epoch/scope certificates, delegated keys, producer signatures, component digests, package conformance, worker responses, adapter IDs, output paths, and same-length JSONL content.
    - Endless worker/descendant, delayed post-timeout response, direct-native token, arbitrary command/module, path/junction swap, and public destination remain blocked.
    - Missing candidate, skipped failed candidate, nondeterministic order, collapsed verdicts, or valid-unexpected pass cannot produce a signed complete run.
  </behavior>
  <action>RED: create a separate black-box adversarial suite using only generated roots and public commands; ensure a forged manifest/response reaches a current insufficient check before GREEN. GREEN: tighten the smallest dispatcher/builder/attestation binding. Do not share fixture-building helpers with the focused runtime contract beyond immutable input generation, and do not delete any positive build/runner test. Keep output privacy-safe and bounded.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/candidate_package_adversarial.ps1</automated>
  </verify>
  <done>Held-out readiness, signature, worker, path, ordering, completeness, and direct-native attacks fail while both local-process roots remain runnable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Matrix/build root to local dispatcher | Generated package metadata is untrusted until all static and runtime-component digests are independently verified. |
| Dispatcher to isolated worker | Candidate work crosses a terminable exact protocol; no arbitrary command/module/native callback is accepted. |
| Dispatcher output to producer attestation | JSONL becomes retainable only after fixed digest/verdict evaluation, repository-rooted purpose/epoch/scope certificate verification, delegated SPKI-to-CNG match, and payload signature. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-10 | Denial of service | adapter execution | critical | mitigate | All blocking work through Plan 07 process deadline/tree kill; serial bounded scheduler. |
| T-05.2-14 | Elevation of privilege | generated roots | high | mitigate | Existing containment/reparse/public/game/save rejection plus immutable component allowlist. |
| T-05.2-15 | Tampering | readiness/build identity | high | mitigate | Executable self-test, recomputed component/package digests, producer signature; status is never sufficient. |
| T-05.2-G08-01 | Spoofing | producer evidence | critical | mitigate | Repository-pinned root, root-signed `candidate-producer` delegation, SPKI-to-CNG match, full run/evidence/build signature, and explicit unconfigured/test-root rejection. |
| T-05.2-G08-02 | Information disclosure | JSONL sink | high | mitigate | Owner-only exact destination, atomic close, bounded safe fields/reasons, no paths in public output. |
| T-05.2-SC | Tampering | package supply chain | low | accept | Built-in committed PowerShell/Lua/.NET only; no install or network acquisition. |
</threat_model>

<verification>
Focused: run adapter runtime, full candidate-build, Plan 07 isolation/runner, and held-out package suites. Full: run `x4-verification`, `Suite all`, Rust format/strict Clippy/locked tests/source-size, canonical package conformance, public runtime diff, private-path/credential scan, and `git diff --check`. Preserve every closed threat and known unrelated baseline.

Apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` in the summary and record the required learning outcome.
</verification>

<success_criteria>
R6 is no longer a scaffold: all seven candidates are locally runnable through concrete bounded components and a terminable process boundary; authenticated retention is available only with separately owner-provisioned repository-rooted delegation and otherwise reports an honest pending status, while X4 remains unlaunched, unmodified, and human-owned.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-08-SUMMARY.md` when done. Close the required skill-learning gate in that summary with `Skill learning: none` plus evidence considered, or one evidence-backed candidate naming its sole owning project skill, minimal proposed rule, and owner-approval status.
</output>
