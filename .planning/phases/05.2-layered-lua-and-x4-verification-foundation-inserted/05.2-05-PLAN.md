---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 05
type: execute
wave: 5
depends_on: [05.2-04]
files_modified:
  - tools/x4-verification/retain-evidence.ps1
  - tools/x4-verification/contracts/sanitized-ledger.v1.json
  - tools/x4-verification/tests/evidence_retention_contract.ps1
  - tools/x4-verification/x4-admission.ps1
  - tools/x4-verification/tests/admission_contract.ps1
  - tests/x4-candidates/phase-05.1-candidate-ledger.v1.json
  - tests/x4-candidates/05.1-candidate-run-procedure.md
autonomous: true
requirements: [R5, R6]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "R5/D-05: runtime JSONL and its locator are retained machine-locally with owner-only access and verified digests."
    - "Only sanitized logical IDs, schema versions, three verdicts, dispositions, and digests can enter repository artifacts or admission input."
    - "R6/D-06: Phase 05.1 receives a complete pending ledger and human-only run procedure without production seam change or admission claim."
  artifacts:
    - tools/x4-verification/retain-evidence.ps1
    - tools/x4-verification/contracts/sanitized-ledger.v1.json
    - tests/x4-candidates/phase-05.1-candidate-ledger.v1.json
    - tests/x4-candidates/05.1-candidate-run-procedure.md
  key_links:
    - "Retention validates Plan 03 runtime-evidence JSONL and Plan 04 machine-local build manifest before storing or sanitizing a run."
    - "Admission consumes only the sanitized ledger identity chain, never raw JSONL, private locator paths, or producer-owned verdict shortcuts."
---

<objective>
Complete private evidence retention and hand a fail-closed candidate ledger back to Phase 05.1 (R5, R6; D-05, D-06).

Purpose: Preserve later human-run evidence across sessions without leaking private data, while keeping runtime admission independently gated.
Output: Sanitized-ledger contract, retention helper and tests, pending ledger, and human-only run procedure.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-PATTERNS.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-03-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-04-SUMMARY.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-UAT.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-VERIFICATION.md
@tests/x4-disposable/01-probe-evidence.md
@tools/provision-lua.ps1
@tools/x4-verification/contracts/runtime-evidence.v1.json
@tools/x4-verification/contracts/candidate-build-manifest.v1.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate and retain one runner JSONL stream plus build manifest</name>
  <files>tools/x4-verification/retain-evidence.ps1, tools/x4-verification/contracts/sanitized-ledger.v1.json, tools/x4-verification/tests/evidence_retention_contract.ps1</files>
  <behavior>
    - R5/D-05: input validates against Plan 03 `runtime-evidence.v1.json` and the exact Plan 04 `manifest/build-manifest.v1.json` validates against `candidate-build-manifest.v1.json` before retention.
    - Evidence records exact game version, mod list, scenario, candidate source, expected/actual result, completeness, elapsed real/game time, SETA when relevant, exact failure point, run identity, and digest.
    - The helper creates a durable owner-only run directory and owner-only JSONL/locator files, rereads all retained files, recalculates SHA-256, and reports success only after permission and digest verification.
    - Partial lines, unknown schemas, excess rows/bytes, interrupted writes, missing files, permission mismatch, build/evidence identity mismatch, or digest mismatch block sanitized output.
  </behavior>
  <action>Write failing retention tests first using disposable exact roots. Follow `tools/provision-lua.ps1` for download/artifact digest verification and reread, and follow the private-artifact retention contract in `AGENTS.md` for durable directory `0700` and file/locator `0600` semantics; unlike the tool analogue, retain runtime JSONL rather than a provisioned binary. Independently validate every Plan 03 JSONL row and the Plan 04 machine-local build manifest, including their dossier/matrix/build/run identity chain. Write atomically, create the locator beside the retained artifact, reread every file, verify SHA-256 and owner-only ACL/mode, then emit a separately validated sanitized object. Resolve cleanup targets exactly and never delete a broad/unresolved path. Do not read environment secret files, saves, or unspecified runtime artifacts.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/evidence_retention_contract.ps1 -Case retention</automated>
  </verify>
  <done>A bounded Plan 03 stream plus Plan 04 manifest survives validation, retention, locator, reread, digest, and permission checks; all malformed, interrupted, identity, or leakage cases fail before sanitization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand Phase 05.1 a sanitized pending ledger and independent admission chain</name>
  <files>tools/x4-verification/x4-admission.ps1, tools/x4-verification/tests/admission_contract.ps1, tests/x4-candidates/phase-05.1-candidate-ledger.v1.json, tests/x4-candidates/05.1-candidate-run-procedure.md</files>
  <behavior>
    - R6/D-06: all seven Plan 04 candidates appear once as `runtime-pending` with evidence requirements, stop conditions, and separate execution/contract/effect fields.
    - Admission accepts only sanitized rows with matching dossier, registry, coverage, matrix, build-profile, package-conformance, evidence, run, and locator digests.
    - Missing evidence, incomplete retention, source-action defects, identity gaps, any failed verdict, or a valid unexpected result with `effect_verdict = pass` remain non-admissible.
    - The procedure assigns X4 launch, disposable campaign setup, execution, and observation to a later human Phase 05.1 gate and performs none here.
  </behavior>
  <action>Write handback and identity-chain negatives first. Follow `tests/x4-disposable/01-probe-evidence.md` for scope-limited evidence language and logical run identity, while deliberately replacing its embedded run details with a sanitized versioned ledger. Extend independent admission only to consume the sanitized chain; the runner and retention producer cannot decide admission. Require all three verdicts and explicitly reject a valid-but-unexpected record whose effect verdict is pass. Create the seven-row pending ledger and concise procedure covering preparation, human-only execution, stop conditions, safe collection, retention, and handback without private paths or claims of successful X4 execution. Assert Phase 05.1 production and public package files remain unchanged from the executor baseline.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/evidence_retention_contract.ps1 -Case handback</automated>
  </verify>
  <done>Phase 05.1 has a complete pending ledger and safe human-only procedure; no row is admissible without a retained digest-linked three-verdict chain, unexpected effects never pass, and no X4 or production mutation occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runner JSONL/build manifest to private store | Potentially private, malformed, partial, oversized, or identity-confused runtime data enters durable storage. |
| Private store to repository ledger | Sanitization must prevent raw values and machine-local paths from crossing into Git. |
| Sanitized ledger to admission | Evidence identity crosses into independent policy, not directly into game mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-17 | Information disclosure | JSONL and locator | high | mitigate | Owner-only ACL/mode, no Git placement, explicit inputs, and leakage tests. |
| T-05.2-18 | Tampering | evidence identity chain | high | mitigate | Atomic writes, reread, SHA-256, and schema/dossier/build/run digest linkage. |
| T-05.2-19 | Repudiation | sanitized ledger | medium | mitigate | Stable logical IDs, three verdicts, retention status, and immutable digest references. |
| T-05.2-20 | Denial of service | JSONL retention | medium | mitigate | Bound bytes, line bytes, rows, diagnostics, and exact output depth. |
| T-05.2-21 | Elevation of privilege | cleanup/permissions | high | mitigate | Resolve exact roots, reject broad targets, assert native owner-only ACL/mode. |
| T-05.2-SC | Tampering | package supply chain | low | accept | No package installation occurs. |
</threat_model>

<verification>
Focused: run both retention cases, `admission_contract.ps1 -Case evidence-chain`, and `run_contracts.ps1 -Suite x4-verification`.

Full: run `run_contracts.ps1 -Suite all`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, and `cargo run -p source-size-lint --locked -- crates tools`. Verify the diff contains no raw evidence, locator, private path, production-seam change, or public test control. Pre-existing failures are not waived.

Before completing the summary, apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` and record the required `Skill learning: none` evidence statement or a complete candidate. Missing closure blocks completion.
</verification>

<success_criteria>
R5 and R6 are complete at the preparation boundary: runner output and build identity can be durably retained and safely referenced, Phase 05.1 has a pending ledger, and neither X4 execution nor seam admission is claimed.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-05-SUMMARY.md` when done.
</output>
