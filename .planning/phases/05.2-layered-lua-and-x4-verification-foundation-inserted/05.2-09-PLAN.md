---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 09
type: execute
wave: 8
depends_on: [05.2-06, 05.2-08]
files_modified:
  - tools/x4-verification/retain-evidence.ps1
  - tools/x4-verification/contracts/sanitized-ledger.v1.json
  - tools/x4-verification/x4-admission.ps1
  - tools/x4-verification/tests/evidence_retention_contract.ps1
  - tools/x4-verification/tests/admission_contract.ps1
  - tools/x4-verification/tests/evidence_chain_adversarial.ps1
  - extensions/live_galaxy/tests/run_contracts.ps1
autonomous: true
gap_closure: true
requirements: [R5]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "R5: a repository-rooted trusted-producer run reaches positive durable retention, root-delegated locator verification, retention-generated sanitized ledger, and independent admission-chain evaluation when the separately owner-provisioned delegated keys are present; unconfigured production remains fail-closed."
    - "Retention independently recomputes evidence, manifest, generated files, canonical package conformance, producer, and locator identities; embedded/self-attested values are insufficient."
    - "Admission accepts only a locator that retention verifies now; hand-authored completed ledgers remain rejected."
    - "Authenticated local-contract evidence proves the chain but remains non-admissible for the Phase 05.1 seam until separately attested `observed-in-X4` evidence exists."
  artifacts:
    - tools/x4-verification/retain-evidence.ps1
    - tools/x4-verification/x4-admission.ps1
    - tools/x4-verification/tests/evidence_chain_adversarial.ps1
  key_links:
    - "Retention verifies Plan 08's repository-rooted `candidate-producer` delegation/signature, reruns canonical package conformance, retains exact bytes owner-only, then signs locator/sanitized identity only with an already provisioned key matching a root-signed `retention-locator` purpose/epoch/scope certificate."
    - "Admission invokes the fixed retention verifier on `-VerifiedLocatorPath`, consumes only its generated sanitized object, then reaches the existing `Test-EvidenceChain`; `-SanitizedLedgerPath` remains rejected."
---

<objective>
Restore positive evidence retention and verified-chain admission without trusting producer JSON, manifest status, locator paths, or hand-authored ledgers (R5).

Purpose: Complete the repository-rooted producer → private artifact → delegated signed locator → sanitized ledger → independent admission flow while preserving all security closures and X4-pending truth.
Output: Attested retention/admission mechanism, isolated positive cryptographic core tests, production unconfigured/platform negatives, held-out adversarial chain tests, aggregate registration, and phase re-verification evidence. No plan claims that real production root or delegated keys were provisioned.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/live-galaxy-code-review/SKILL.md
@.agents/skills/live-galaxy-skill-evolution/SKILL.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VERIFICATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY-FIX.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW-FIX.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-05-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-06-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-08-SUMMARY.md
@tools/x4-verification/retain-evidence.ps1
@tools/x4-verification/x4-admission.ps1
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Retain one signed producer stream and verify its signed locator</name>
  <files>tools/x4-verification/retain-evidence.ps1, tools/x4-verification/contracts/sanitized-ledger.v1.json, tools/x4-verification/tests/evidence_retention_contract.ps1</files>
  <behavior>
    - RED/GREEN: remove no existing positive test; make the currently dead retain → ACL/mode → locator → reread core path execute using Plan 08's real dispatcher output and isolated TEST-ONLY root-signed producer/locator delegations confined to the cryptographic test harness. The production CLI rejects the TEST-ONLY root/certificates and exposes no verifier/root/key injection; no production signature is fabricated when the owner root remains unconfigured.
    - Before writing, production retention verifies the pinned root, producer certificate purpose/epoch/scope/SPKI/signature/expiry, and exact build/run/candidate/evidence/component digests, generated-file bytes, runtime schema, and fixed package identity.
    - Retention reruns `x4-package-conformance.ps1` against the retained build root with the canonical contract and recomputes its canonical digest; embedded manifest conformance cannot substitute.
    - After atomic owner-only placement and digest reread, retention generates the sanitized ledger itself, creates an exact locator, verifies the root-signed `retention-locator` delegation, matches delegated SPKI to the already provisioned CNG key, and signs the locator/sanitized digest chain. Missing authority returns `RETENTION_ATTESTATION_UNCONFIGURED` without retained/sanitized success.
    - An explicit non-Windows black-box contract asserts `RETENTION_ATTESTATION_PLATFORM_UNSUPPORTED`, nonzero exit, no artifact/locator/sanitized-ledger creation, and no success marker; Unix mode bits, TEST-ONLY fixtures, or caller-supplied authority material cannot authenticate producer or locator.
  </behavior>
  <action>RED: remove the early success exit from the retention contract so the existing positive/tamper/ACL/locator cases fail at `TRUSTED_RUNTIME_ATTESTATION_MISSING`; replace hand-built runtime rows with actual Plan 08 dispatcher output. Add separate production black-box cases for unconfigured delegated authorities and a simulated non-Windows capability result; assert `RETENTION_ATTESTATION_PLATFORM_UNSUPPORTED` produces no retained artifact, locator, sanitized ledger, or success marker. GREEN: replace the unconditional manifest failure with full pinned-root plus producer-delegation verification through Plan 06's fixed module. Independently reread all source/generated files and invoke canonical package conformance rather than trusting embedded result/status. Preserve exact destination containment, read-time no-reparse checks, atomic staging, owner-only ACL/modes, digest reread, and non-recursive failed cleanup. Generate sanitized data only after successful retention; include no raw/private fields. Verify the root-signed `retention-locator` certificate and delegated CNG key match before signing the canonical locator. Keep TEST-ONLY signing behind a test-internal core harness that production CLIs cannot select; never create, enroll, import, replace, or trust a root/key/ACL file.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/evidence_retention_contract.ps1 -Case retention; pwsh -NoProfile -File tools/x4-verification/tests/evidence_retention_contract.ps1 -Case retention-platform</automated>
  </verify>
  <done>The positive cryptographic retention core is executable under isolated test authority, production accepts only repository-rooted delegated authority, and both unconfigured Windows and unsupported non-Windows paths produce no retained or sanitized success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume only the verified locator chain in admission</name>
  <files>tools/x4-verification/x4-admission.ps1, tools/x4-verification/tests/admission_contract.ps1, tools/x4-verification/tests/evidence_retention_contract.ps1</files>
  <behavior>
    - RED/GREEN: admission accepts `-VerifiedLocatorPath`, invokes the fixed retention verifier, parses its bounded sanitized output, and reaches `Test-EvidenceChain`; a caller-supplied completed ledger remains `UNVERIFIED_LEDGER_INPUT`.
    - The isolated TEST-ONLY cryptographic core exercises a positive local-contract chain decision, while every production CLI rejects that root and reports root/delegated-key unconfigured when real owner provisioning is absent; neither path can mark the Phase 05.1 seam admissible or `observed-in-X4`.
    - Actual `admissible` remains reachable only for a future signed producer classification bound to separately retained `observed-in-X4` evidence, exact expected effects, complete three-pass verdicts, and canonical Phase 05.1 identities.
    - Missing/expired/wrong-purpose producer or locator signature, stale conformance, altered retained bytes, hand-authored sanitized fields, partial candidates, failed verdict, unexpected effect, and replayed cross-run envelope remain non-admissible.
  </behavior>
  <action>RED: remove the early exits in `admission_contract.ps1 -Case evidence-chain` and retention-admission tests without deleting any forged-ledger regression. Feed the positive evaluator only the verified object emitted by Task 1's isolated TEST-ONLY cryptographic core, while proving the public production CLI rejects that root and accepts a live locator only when real owner-provisioned delegated keys are available. GREEN: add `-VerifiedLocatorPath`; retain `-SanitizedLedgerPath` solely as an explicit rejection path. The production CLI invokes the repository-fixed retention verifier, validates bounded structured output, and passes only that in-memory object into `Test-EvidenceChain`; the test harness calls the same core after independent test-root verification without exposing a test-mode parameter publicly. Return a distinct `chain-verified-x4-pending` result for authenticated local/validation evidence. Reject TEST-ONLY roots and replay under any changed identity in production. Keep actual seam admission conditional on root-delegated observed-X4 classification that no Phase 05.2 test fabricates or claims. Preserve exact owner override independence: an R2 override changes one known finding but cannot waive producer/locator/effect evidence.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/evidence_retention_contract.ps1 -Case retention-admission</automated>
  </verify>
  <done>The shared admission core proves the positive chain under isolated test authority; production accepts only a genuinely provisioned repository-rooted live locator, while unconfigured/test-root/hand-authored/replayed inputs remain rejected and the Phase 05.1 seam remains X4-pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hold out chain forgeries, register full gates, and prepare re-verification</name>
  <files>tools/x4-verification/tests/evidence_chain_adversarial.ps1, tools/x4-verification/tests/evidence_retention_contract.ps1, tools/x4-verification/tests/admission_contract.ps1, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <behavior>
    - Held-out attacks change/reorder rows, recompute row digests, transplant valid producer/locator signatures, alter package conformance, swap generated/retained files through junctions, widen ACLs, append fields, or replay another run/candidate/build identity.
    - Aggregate `x4-verification` executes R2 root/delegation authority, all eight R4 behaviors, isolation, R6 runnable package, isolated positive R5 retention/admission core, production unconfigured/platform negatives, and every held-out suite with nonempty markers.
    - No positive test is deleted, filtered, early-exited, or replaced by hard-disable-only assertions.
  </behavior>
  <action>RED: write an independent black-box chain-forgery suite using copies of Task 1's isolated positive output; ensure at least one transplanted/recomputed artifact reaches an incomplete check before GREEN. Include root swap, TEST-ONLY root injection, purpose confusion, epoch rollback, and producer/locator delegated-key substitution. GREEN: tighten the smallest root/delegation/producer/retention/admission binding and register every focused plus held-out gate in `run_contracts.ps1`. If and only if real production delegated authorities are already provisioned out of band, generate and immediately reverify a fresh durable authenticated local-contract artifact and owner-only locator. Otherwise record stable `*_ATTESTATION_UNCONFIGURED` results and retain no test artifact as production evidence. Preserve the old historical artifact untouched, record no private path/key identifier, and run the full gate list for phase re-verification.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/evidence_chain_adversarial.ps1; pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-verification</automated>
  </verify>
  <done>Held-out chain attacks fail, every restored positive behavior runs in aggregate, a current-schema durable local artifact re-verifies, and re-verification inputs are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Signed producer output to retention | Runtime claims enter private storage only after signature, identity, schema, bound, component, and independent conformance checks. |
| Retained artifact to locator/sanitized output | Retention alone generates and signs the safe identity projection after atomic placement/reread. |
| Verified locator to admission | Admission consumes only current verifier output; no completed ledger file is authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-15 | Tampering | package conformance | high | mitigate | Rerun canonical command and recompute digest; reject embedded-only claims. |
| T-05.2-18 | Spoofing | producer/locator chain | critical | mitigate | Repository-pinned root, root-signed purpose/epoch/scope certificates, delegated SPKI-to-CNG matches, full manifest/evidence/run/locator binding, and TEST-ONLY root rejection. |
| T-05.2-19 | Elevation of privilege | sanitized/admission status | critical | mitigate | Retention-generated object only; verified locator input; local evidence remains X4-pending. |
| T-05.2-21 | Tampering | staging/cleanup | high | mitigate | Preserve no-reparse rereads, exact atomic placement, owner-only ACL/modes, no recursive cleanup. |
| T-05.2-G09-01 | Repudiation | retained run identity | high | mitigate | Producer and locator signed envelopes plus recomputed evidence/run/component digests. |
| T-05.2-SC | Tampering | package supply chain | low | accept | No install/network; built-in committed PowerShell/.NET only. |
</threat_model>

<verification>
Focused: retention, handback, retention-admission, admission evidence-chain, authority, runner, isolation, package runtime/build, and all four held-out suites. Full: `run_contracts.ps1 -Suite x4-verification`; `run_contracts.ps1 -Suite all`; `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked`; `cargo run -p source-size-lint --locked -- crates tools`; canonical shipped/generated package conformance; public runtime diff; private-path/credential scan; `git diff --check`. Known unrelated failures must match the recorded baseline exactly and cannot be waived.

Apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` in the summary and record the required learning outcome.
</verification>

<reverification>
After `05.2-09-SUMMARY.md`, dispatch the Phase 05.2 verifier against the new HEAD and current SECURITY/SECURITY-FIX/REVIEW/VALIDATION artifacts. Require `status: passed`, R1-R6 score 6/6, all eight acceptance criteria and edges active, no disabled/erased positive tests, and no reopened T-05.2-01 through T-05.2-21/T-05.2-SC disposition. This is agent-only re-verification; it must not launch X4, inspect saves, mutate game state, or convert local-contract evidence into an observed-X4 claim.
</reverification>

<success_criteria>
R5's secure mechanism and positive core path are restored: production retention/admission accepts only the repository-rooted delegated chain when separately owner-provisioned keys exist; unconfigured Windows, unsupported non-Windows, TEST-ONLY, self-attested, and hand-authored inputs produce no authoritative success.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-09-SUMMARY.md` when done, then run the re-verification gate above. Close the required skill-learning gate in that summary with `Skill learning: none` plus evidence considered, or one evidence-backed candidate naming its sole owning project skill, minimal proposed rule, and owner-approval status.
</output>
