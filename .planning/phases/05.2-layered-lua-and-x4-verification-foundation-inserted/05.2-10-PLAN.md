---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 10
type: execute
wave: 9
depends_on: [05.2-09]
files_modified:
  - tools/x4-verification/isolation/candidate-worker.ps1
  - tools/x4-verification/isolation/invoke-candidate-worker.ps1
  - tools/x4-verification/tests/candidate_isolation_contract.ps1
  - tools/x4-verification/x4-package-conformance.ps1
  - tools/x4-verification/build-candidate-extension.ps1
  - tools/x4-verification/retain-evidence.ps1
  - tools/x4-verification/x4-admission.ps1
  - tools/x4-verification/tests/evidence_retention_contract.ps1
autonomous: true
gap_closure: true
requirements: [R3, R4, R5, R6]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "R4: descendant PID readiness is observed deterministically before the isolation deadline is armed, and three consecutive focused timeout runs prove process-tree termination."
    - "R3/R5: retention reruns the repository canonical full package graph verifier against the exact retained build root; a self-consistent embedded conformance forgery cannot pass."
    - "Admission, package conformance, candidate build, and retention reject oversized inputs from file metadata before full-file allocation and revalidate file identity and byte counts after reading."
    - "The focused and aggregate suites pass without X4, save, installed-mod, private-evidence, production-authority, or external-service access."
    - "Post-fix review is CLEAN, Nyquist is COMPLIANT, security reports threats_open: 0, and the Phase 05.2 verifier passes R1-R6 before phase metadata can close."
  artifacts:
    - path: "tools/x4-verification/tests/candidate_isolation_contract.ps1"
      provides: "Repeated deterministic descendant-readiness and process-tree timeout regression"
    - path: "tools/x4-verification/x4-package-conformance.ps1"
      provides: "Single canonical bounded full-graph verifier for production and inert candidate contracts"
    - path: "tools/x4-verification/retain-evidence.ps1"
      provides: "Independent live conformance rerun and bounded race-safe retained-input reads"
    - path: "tools/x4-verification/tests/evidence_retention_contract.ps1"
      provides: "Embedded-only forgery and four-reader oversized sparse-file regressions"
  key_links:
    - from: "tools/x4-verification/isolation/invoke-candidate-worker.ps1"
      to: "tools/x4-verification/tests/candidate_isolation_contract.ps1"
      via: "parent-owned readiness signal is validated before the timeout deadline and descendant-death assertions"
      pattern: "readiness.*timeout|child.*pid"
    - from: "tools/x4-verification/retain-evidence.ps1"
      to: "tools/x4-verification/x4-package-conformance.ps1"
      via: "retention executes the fixed verifier against the exact manifest-derived build root and compares canonical output"
      pattern: "x4-package-conformance"
    - from: "tools/x4-verification/tests/evidence_retention_contract.ps1"
      to: "tools/x4-verification/{x4-admission.ps1,x4-package-conformance.ps1,build-candidate-extension.ps1,retain-evidence.ps1}"
      via: "black-box sparse-file cases require pre-allocation rejection and post-read race detection at all four readers"
      pattern: "preallocation-bounds|sparse"
---

<objective>
Close the four post-review Phase 05.2 gaps without reopening package trust, adapter design, or production authority.

Purpose: Make isolation evidence deterministic, make retained package conformance independently executable, and enforce resource bounds before allocation at every named reader while preserving D-03 through D-06.
Output: Repeatable isolation evidence, canonical retained-root graph verification, race-safe bounded readers, adversarial regressions, and complete post-fix audit inputs.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-code-review/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/live-galaxy-skill-evolution/SKILL.md
@.planning/HANDOFF.json
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/.continue-here.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-PATTERNS.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-07-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-08-SUMMARY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-09-SUMMARY.md

<interfaces>
`candidate_isolation_contract.ps1` invokes the fixed launcher and currently checks `$ResponsePath.child.pid` only after the launcher has already returned `worker-timeout`. The worker publishes that PID after starting its descendant. The gap is ordering, not the existing process-tree-kill mechanism.

`x4-package-conformance.ps1` accepts `PackageRoot`, `ContractPath`, `DossierPath`, and `CoveragePath`, then walks registration and every contained transitive import under explicit bounds. Candidate roots already carry `manifest/package-conformance.v1.json`; retention must execute this verifier rather than accept the manifest's embedded result.

The four bounded-reader seams are `Read-BoundedJson` in `x4-admission.ps1`, `Read-BoundedBytes` in `x4-package-conformance.ps1`, `Read-Json` plus digest reads in `build-candidate-extension.ps1`, and `Read-BoundedBytes` plus digest reads in `retain-evidence.ps1`.
</interfaces>
</context>

## Multi-Source Coverage Audit

| Source | ID | Gap / constraint | Task | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Verification foundation must fail closed before later X4 work | 1-3 + closure gates | COVERED | No X4 execution or authority ceremony is introduced. |
| REQ | R3 | Canonical production-faithful package graph verification | 2 | COVERED | One fixed verifier serves the public and inert-candidate contract modes without weakening the public rule. |
| REQ | R4 | Deterministic isolated candidate timeout evidence | 1 | COVERED | Readiness precedes the deadline and is repeated three times. |
| REQ | R5 | Independent retained evidence and conformance chain | 2-3 | COVERED | Live graph rerun plus bounded retention reads. |
| REQ | R6 | Generated candidate build inputs remain bounded and locally runnable | 2-3 | COVERED | Builder emits/verifies the canonical candidate contract and rejects oversized inputs. |
| CONTEXT | D-03 | Packaged conformance remains production-faithful | 2 | COVERED | Public direct-binding requirements stay unchanged; inert candidates use an explicit forbidden-binding policy. |
| CONTEXT | D-04 | Candidate failure isolation and bounded execution | 1 | COVERED | Existing fixed worker/process-tree design remains; only deterministic readiness is added. |
| CONTEXT | D-05 | Private evidence is digest-linked and independently verified | 2-3 | COVERED | No private value or retained corpus enters Git. |
| CONTEXT | D-06 | Phase 05.2 prepares but does not execute X4 | 1-3 | COVERED | Local contract tests only. |
| HANDOFF | G-01 | Descendant PID readiness race | 1 | COVERED | Three consecutive focused runs are mandatory. |
| HANDOFF | G-02 | Embedded-only retention conformance | 2 | COVERED | Live canonical full-graph rerun and forgery regression. |
| HANDOFF | G-03 | Post-allocation admission/conformance bounds | 3 | COVERED | Sparse-file regressions prove early rejection. |
| HANDOFF | G-04 | Post-allocation build/retention bounds | 3 | COVERED | Same defect class and race-safe rereads. |

R1, R2, D-01, and D-02 are already closed and are intentionally unchanged. Deferred mutation experiments, hot reload, reporting, automatic launch, and save management remain excluded.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Arm the isolation timeout only after descendant readiness</name>
  <reversibility rating="reversible">The handshake is confined to the fixed local-contract worker protocol and tests; removing it restores the prior timing behavior without data migration.</reversibility>
  <files>tools/x4-verification/isolation/candidate-worker.ps1, tools/x4-verification/isolation/invoke-candidate-worker.ps1, tools/x4-verification/tests/candidate_isolation_contract.ps1</files>
  <read_first>
    - `.planning/HANDOFF.json` blockers and the focused-failure rationale.
    - `05.2-07-PLAN.md` Task 2 and `05.2-07-SUMMARY.md` isolation decisions.
    - The complete current worker, launcher, and isolation contract listed in `<files>`.
  </read_first>
  <behavior>
    - RED: run the descendant timeout case three consecutive times and preserve every failure; the current race must be reproducible or the test must still be rewritten to assert explicit ordering rather than timing luck.
    - GREEN: for the fixed `local-contract-child-endless` adapter, the worker atomically publishes a bounded PID sidecar followed by a distinct readiness signal only after the descendant exists and is alive.
    - The launcher validates the derived sidecars under the already validated response directory, waits only within a separate protocol-bounded readiness deadline, and starts the worker timeout interval only after readiness is confirmed.
    - Missing, malformed, stale, forged, early, or mismatched readiness fails closed and cannot become a timeout PASS; normal adapters retain their existing deadline behavior.
    - A timeout still kills the whole process tree, accepts no response, and leaves enough bounded test evidence to prove the descendant PID is gone.
  </behavior>
  <action>Per D-04, add a parent-owned readiness handshake for the fixed descendant fixture without accepting a caller-selected path, process, module, or command. Derive readiness/PID paths from the validated response path, write them atomically, validate PID and liveness before arming the short execution deadline, and clean only validated sidecars. Keep `Process.Kill(true)`, the bounded kill wait, no-shell worker launch, hidden Windows process, canonical response validation, and the platform-unsupported result unchanged. Make the test assert the readiness-before-timeout ordering explicitly, then require three consecutive focused timeout passes; a single aggregate pass is not evidence.</action>
  <rollback>Revert the readiness-sidecar fields and ordering changes together; the existing worker protocol and termination path remain otherwise intact.</rollback>
  <acceptance_criteria>
    - Three consecutive `-Case timeout` runs emit `PASS candidate isolation process-tree timeout` with exit 0.
    - Each run proves readiness was observed before the timeout result, the child PID is parseable, the descendant no longer exists, and no response is accepted.
    - Missing/forged readiness produces a stable non-success result and never a process-tree PASS marker.
    - `-Case success` and `-Case responses` remain green.
  </acceptance_criteria>
  <verify>
    <automated>1..3 | ForEach-Object { pwsh -NoProfile -File tools/x4-verification/tests/candidate_isolation_contract.ps1 -Case timeout; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; pwsh -NoProfile -File tools/x4-verification/tests/candidate_isolation_contract.ps1 -Case success; pwsh -NoProfile -File tools/x4-verification/tests/candidate_isolation_contract.ps1 -Case responses</automated>
  </verify>
  <done>Descendant readiness deterministically precedes timeout measurement and three repeated focused runs prove tree termination without timing luck.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rerun the canonical full package graph during retention</name>
  <reversibility rating="reversible">The change replaces duplicate embedded conformance with a shared verifier call and explicit contract policy; no persisted public format or production authority changes.</reversibility>
  <files>tools/x4-verification/x4-package-conformance.ps1, tools/x4-verification/build-candidate-extension.ps1, tools/x4-verification/retain-evidence.ps1, tools/x4-verification/tests/evidence_retention_contract.ps1</files>
  <read_first>
    - `05.2-09-PLAN.md` Task 1 threat T-05.2-15 and `05.2-09-SUMMARY.md` retained-chain claims.
    - `x4-package-conformance.ps1` parameters, `Visit-Module`, native-binding evaluation, and canonical output.
    - `build-candidate-extension.ps1` functions `New-GeneratedConformanceContract` and `Invoke-Conformance`.
    - `retain-evidence.ps1` function `Test-BuildManifest` and the retention contract's package-digest cases.
  </read_first>
  <behavior>
    - RED: forge a self-consistent manifest `package_conformance` object and digest while the retained build graph has a missing/changed transitive module; the current embedded-only check must accept or reach the wrong failure before GREEN.
    - GREEN: the repository canonical verifier walks content/UI registration, the case-exact entrypoint, and every contained transitive import under its existing file/import/depth/total-byte bounds for the exact build root bound to retention.
    - The public package contract still requires exactly one direct `ffi.C` acquisition site. The generated inert candidate contract explicitly forbids direct native acquisition while retaining the same full registration/import-graph walk; it cannot weaken or reclassify the public contract.
    - Builder and retention both invoke the same fixed verifier and consume canonical bounded JSON output. Retention recomputes the live result and graph digest immediately before durable placement, rather than trusting the manifest's embedded object or digest.
    - Any live/embedded contract, graph, dossier, coverage, classification, or digest mismatch rejects before evidence placement or sanitized output.
  </behavior>
  <action>Per D-03 and D-05, remove the builder-local three-file conformance shortcut as an authority source and route generated packages through the fixed full-graph verifier using an explicit inert native-binding policy in the generated contract. In retention, resolve the exact no-reparse build root from the validated manifest path, execute the fixed verifier with the retained contract/dossier/coverage inputs, require exit 0 plus one bounded canonical JSON result, and compare the recomputed result and graph digest to the manifest. Preserve public-package direct-binding semantics, production authority `UNCONFIGURED`, owner-only retention, exact digest rereads, and `chain-verified-x4-pending` classification. Do not redesign adapters or access X4, saves, installed mods, private retained evidence, or external services.</action>
  <rollback>Restore the prior builder/retention conformance call sites together while retaining the unchanged public conformance contract and all prior security checks.</rollback>
  <acceptance_criteria>
    - Real public package and both generated inert candidate groups pass the same canonical full-graph verifier under their explicit binding policies.
    - A build with a missing/changed transitive import fails retention even when every embedded conformance field and digest is recomputed consistently by the attacker.
    - Retention creates no evidence, locator, sanitized ledger, or success marker on live-versus-embedded mismatch.
    - Existing package escape, reparse, cycle, local-only, registration, and public direct-binding negatives remain green.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/evidence_retention_contract.ps1 -Case retention; pwsh -NoProfile -File tools/x4-verification/tests/candidate_build_contract.ps1 -Case all; pwsh -NoProfile -File tools/x4-verification/tests/package_conformance_contract.ps1 -Case all</automated>
  </verify>
  <done>Retention independently reruns the fixed full graph and rejects embedded-only conformance forgeries without changing completed package trust or adapter behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce all four reader bounds before allocation and after reread</name>
  <reversibility rating="reversible">The changes are local reader hardening and black-box regressions; schemas, accepted small inputs, and external contracts stay unchanged.</reversibility>
  <files>tools/x4-verification/x4-admission.ps1, tools/x4-verification/x4-package-conformance.ps1, tools/x4-verification/build-candidate-extension.ps1, tools/x4-verification/retain-evidence.ps1, tools/x4-verification/tests/evidence_retention_contract.ps1</files>
  <read_first>
    - Handoff threats T-05.2-03, T-05.2-07, T-05.2-16, and T-05.2-20.
    - Every production `ReadAllBytes` call in the four named reader scripts and its caller-supplied or contract-supplied maximum.
    - Existing no-reparse, path-identity, digest, generated-file, and retention tamper tests before adding the shared regression case.
  </read_first>
  <behavior>
    - RED: create oversized sparse files at each reader's exact maximum plus one byte, invoke the public admission, conformance, build, and retention entry points, and require rejection before JSON decoding, hashing, graph walking, or durable output.
    - GREEN: resolve and stat a regular no-reparse file, compare metadata length to the applicable bound, then and only then allocate/read or stream-hash it.
    - After reading, restat the exact resolved path and reject if full path, length, last-write time, reparse state, or read byte count differs from the pre-read snapshot; retain the post-read maximum check as defense in depth.
    - Digest helpers must either consume already bounded bytes or use bounded streaming after the same pre/post identity checks; no unbounded full-file digest read remains in the four scripts.
    - Valid files at exactly the maximum and normal small fixtures retain their current results; oversized sparse files never reach parsers, graph logic, signing, or retention placement.
  </behavior>
  <action>Close the whole defect class, not only the four originally cited lines. In each named script, trace every production full-file read and digest helper to an explicit schema/contract bound before allocation. Reuse the strongest existing `x4-package-conformance.ps1` no-reparse and pre/post metadata pattern, adding its missing pre-read length gate, and apply equivalent race-safe checks to admission, builder, and retention. Extend `evidence_retention_contract.ps1` with a `preallocation-bounds` case that creates sparse files without allocating their logical size, attacks all four public entry points, verifies stable rejection codes/no side effects, and covers exact-bound success. Preserve every existing valid digest, canonical JSON, graph, authority, ACL, and classification behavior.</action>
  <rollback>Revert reader and regression changes as one unit; no schema migration or retained artifact rewrite is involved.</rollback>
  <acceptance_criteria>
    - Admission rejects max-input-plus-one with `BOUND_EXCEEDED` before JSON allocation.
    - Package conformance rejects max-file-plus-one with `FILE_BYTES_EXCEEDED` before graph allocation.
    - Candidate build rejects max-input/generated-file-plus-one with the existing bound code before parsing or hashing.
    - Retention rejects max-input/generated-file-plus-one before parsing, hashing, placement, locator creation, or sanitized output.
    - A race that changes length/metadata between stat and reread is rejected; exact-bound and normal inputs still pass.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/evidence_retention_contract.ps1 -Case preallocation-bounds; pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case dossier; pwsh -NoProfile -File tools/x4-verification/tests/package_conformance_contract.ps1 -Case all; pwsh -NoProfile -File tools/x4-verification/tests/candidate_build_contract.ps1 -Case all; pwsh -NoProfile -File tools/x4-verification/tests/evidence_retention_contract.ps1 -Case retention</automated>
  </verify>
  <done>All admission, conformance, build, and retention file readers reject oversized sparse inputs before allocation and detect post-read races without changing valid behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Worker descendant to parent timeout verdict | A child PID must be proven ready before timeout evidence can assert process-tree termination. |
| Generated build root to retention | Caller-owned manifests and embedded conformance cannot attest the live package graph. |
| Filesystem metadata to bounded readers | Untrusted file size/path identity may change before or during a read and must not drive unbounded allocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-10 | Denial of service | descendant isolation | high | mitigate | Atomic readiness handshake, separate bounded readiness deadline, existing tree kill, three repeated focused runs. |
| T-05.2-15 | Tampering | retained package conformance | high | mitigate | Execute the fixed canonical full-graph verifier against the exact retained root and reject live/embedded mismatch. |
| T-05.2-03 | Denial of service | admission reader | medium | mitigate | Metadata byte gate before allocation plus post-read identity/length verification and sparse-file regression. |
| T-05.2-07 | Denial of service | conformance reader | medium | mitigate | Pre-read per-file/aggregate limits plus contained race-safe graph reads. |
| T-05.2-16 | Denial of service | build reader | medium | mitigate | Bound every JSON/generated/digest input before allocation or streaming hash. |
| T-05.2-20 | Denial of service | retention reader | medium | mitigate | Bound manifest/evidence/generated/digest inputs before allocation and before durable placement. |
| T-05.2-SC | Tampering | package supply chain | low | accept | No dependency installation, network acquisition, X4 access, or external service is used. |
</threat_model>

<verification>
Run the three task-focused commands first, including three consecutive isolation timeout passes. Then run:

- `pwsh -NoProfile -File tools/x4-verification/tests/candidate_isolation_contract.ps1 -Case all`
- `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-verification`
- `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite all` and record only the already documented unrelated baseline if it remains exact
- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --locked`
- `cargo run -p source-size-lint --locked -- crates tools` and record only the already documented unrelated baseline if it remains exact
- PowerShell parser validation for every changed `.ps1`
- `git diff --check`

No aggregate PASS may waive a failed focused case.
</verification>

<closure_gates>
After implementation and aggregate verification, run these gates in order and do not mark Phase 05.2 complete or close STATE/ROADMAP until all pass:

1. Dispatch the deep Phase 05.2 code reviewer over the complete changed behavior and adjacent call chain. Require `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW.md` with `status: clean` and zero P1/P2/P3 findings.
2. Dispatch the Nyquist auditor. Require `05.2-VALIDATION.md` to report `COMPLIANT`, explicitly cite three consecutive focused isolation passes, the embedded-only forgery rejection, and all four sparse-file boundaries.
3. Dispatch the security auditor. Require `05.2-SECURITY.md` to report `SECURED`, frontmatter `threats_open: 0`, T-05.2-15 closed, and T-05.2-03/T-05.2-07/T-05.2-16/T-05.2-20 closed with executable evidence.
4. Dispatch the Phase 05.2 verifier against the reviewed/audited HEAD. Require `05.2-VERIFICATION.md` with `status: passed`, score `6/6`, every AC/edge active, and no promotion of local evidence to `observed-in-X4`.
5. Apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` to `05.2-10-SUMMARY.md`. Record `Skill learning: none` with the review/Nyquist/security/verifier evidence considered, or a complete single-owner candidate with approval status.
</closure_gates>

<success_criteria>
- The four handoff gap classes are closed by executable focused regressions.
- Three consecutive descendant timeout runs pass before the aggregate is trusted.
- Retention reruns the canonical full package graph and rejects a self-consistent embedded-only forgery.
- All four readers prove pre-allocation byte bounds, post-read race checks, and oversized sparse-file rejection.
- Code review is CLEAN; Nyquist is COMPLIANT; security is SECURED with `threats_open: 0`; phase verification passes R1-R6 at 6/6.
- No X4, save, installed mod, private retained evidence, production authority, public package behavior, external service, or deferred idea enters the plan's execution scope.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-10-SUMMARY.md` after implementation. Keep Phase 05.2 open until every `<closure_gates>` artifact passes; then update STATE/ROADMAP through the owning GSD closure workflow.
</output>
