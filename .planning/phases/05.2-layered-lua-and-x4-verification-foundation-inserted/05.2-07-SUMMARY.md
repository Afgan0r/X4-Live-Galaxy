---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 07
subsystem: x4-verification
tags: [lua, powershell, process-isolation, watchdog, jsonl, fail-closed]
requires:
  - phase: 05.2-03
    provides: deterministic candidate JSONL contract and three-axis verdict model
  - phase: 05.2-05
    provides: private retention boundary and sanitized admission handback
provides:
  - inert exact-schema candidates resolved only through a trusted adapter registry
  - fixed external worker protocol with deadline and process-tree termination
  - active eight-behavior R4 suite plus held-out authority and false-green attacks
affects: [05.2-08, 05.1-runtime-candidates, x4-verification]
actuals:
  tokens: 13358
  tasks: 3
  commits: 9
tech-stack:
  added: []
  patterns:
    - exact inert candidate declarations with runner-owned verdict and digest authority
    - fixed no-shell worker with atomic bounded request and response files
    - named nonempty behavior markers in focused and aggregate verification
key-files:
  created:
    - extensions/live_galaxy/tests/x4_candidate_runner_adversarial.lua
    - tools/x4-verification/contracts/candidate-worker-protocol.v1.json
    - tools/x4-verification/isolation/invoke-candidate-worker.ps1
    - tools/x4-verification/isolation/candidate-worker.ps1
    - tools/x4-verification/tests/candidate_isolation_contract.ps1
  modified:
    - tests/x4-candidates/lua/live_galaxy_candidate_runner.lua
    - tests/x4-candidates/lua/phase_051_candidates.lua
    - extensions/live_galaxy/tests/x4_candidate_runner_contract.lua
    - extensions/live_galaxy/tests/run_contracts.ps1
key-decisions:
  - "Candidate declarations contain only id, adapter_id, source, expected_result, and one bounded work limit; callbacks, metatables, registries, digests, watchdogs, and verdict fields are rejected."
  - "The pure runner evaluates completeness and expected-effect equality internally, while only the trusted harness registry may supply local-contract adapter functions."
  - "Potentially blocking work uses one fixed repository worker, no shell, canonical bounded files, parent deadlines, and process-tree termination; native and arbitrary command modes remain forbidden."
patterns-established:
  - "Authority separation: inert candidate selection -> trusted registry -> fixed internal verdicts -> canonical digest-bound JSONL."
  - "Isolation closure: validate request -> fixed process -> atomic response -> deadline/tree kill -> exact identity and canonical-byte validation."
requirements-completed: [R4]
coverage:
  - id: D1
    description: All eight R4 behaviors execute with deterministic ordering, failure isolation, continuation, bounds, three verdict axes, and unexpected-effect mismatch
    requirement: R4
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-candidate-runner"
        status: pass
    human_judgment: false
  - id: D2
    description: Blocking local-contract adapters cross a fixed terminable process boundary and malformed, stale, forged, oversized, partial, duplicate, late, or noncanonical output is rejected
    requirement: R4
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File tools/x4-verification/tests/candidate_isolation_contract.ps1 -Case all"
        status: pass
    human_judgment: false
  - id: D3
    description: Candidate authority injection, metatable callbacks, direct native tokens, dynamic commands, unregistered adapters, and false-green suites fail closed
    requirement: R4
    verification:
      - kind: integration
        ref: "extensions/live_galaxy/tests/x4_candidate_runner_adversarial.lua via x4-candidate-runner"
        status: pass
    human_judgment: false
duration: 49min
completed: 2026-08-30
status: complete
---

# Phase 05.2 Plan 07: Secure Candidate Runner and Isolation Summary

**All eight R4 candidate behaviors now execute through inert declarations and runner-owned verdict authority, while blocking adapter work crosses a fixed terminable external process boundary.**

## Performance

- **Duration:** 49 min
- **Started:** 2026-08-30T04:27:00Z
- **Completed:** 2026-08-30T05:16:00Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Restored the eight erased runner behaviors with exact plain-data candidates, stable ordering, continuation after closed failures, fixed completeness/effect evaluation, canonical digest verification, and all original hard bounds.
- Added a fixed PowerShell worker protocol that disables shell execution, bounds request/response bytes and identities, atomically publishes responses, kills timed-out descendant trees, and never admits direct native or caller-selected commands/modules/executables.
- Added held-out authority, metatable, direct-native, forged-response, timeout-race, noncanonical-output, and false-green attacks; focused and aggregate gates now require every named marker and reject empty output.

## Task Commits

1. **Task 1 RED: restore candidate runner behaviors** - `809fb0b`
2. **Task 1 GREEN: trusted inert candidate runner** - `72ec1c0`
3. **Task 2 RED: candidate isolation contract** - `775eaea`
4. **Task 2 GREEN: fixed terminable worker boundary** - `70ac053`
5. **Task 3 RED: held-out authority attacks** - `571b10d`
6. **Task 3 GREEN: authority-injection closure and markers** - `450e194`
7. **Security RED: noncanonical worker output** - `f49cad5`
8. **Security GREEN: canonical response and safe cleanup** - `0c71741`
9. **Integration fix: register Plan 06 authority gates** - `414b3be`

## Files Created/Modified

- `tests/x4-candidates/lua/live_galaxy_candidate_runner.lua` - Exact inert schema, trusted adapter lookup, fixed contract/effect evaluation, bounds, continuation, and unattested-native rejection.
- `tests/x4-candidates/lua/phase_051_candidates.lua` - Callback-free local-contract candidate declaration.
- `extensions/live_galaxy/tests/x4_candidate_runner_contract.lua` - Eight active positive/negative R4 behavior cases plus candidate-owned authority rejection.
- `extensions/live_galaxy/tests/x4_candidate_runner_adversarial.lua` - Independently loaded metatable, callback, native, dynamic-authority, and unregistered-adapter attacks.
- `extensions/live_galaxy/tests/run_contracts.ps1` - Isolation/authority aggregate registration and mandatory nonempty named marker accounting.
- `tools/x4-verification/contracts/candidate-worker-protocol.v1.json` - Exact request/response schemas, fixed adapter allowlist, native-mode prohibition, and byte/work/deadline bounds.
- `tools/x4-verification/isolation/invoke-candidate-worker.ps1` - Fixed no-shell launcher, parent deadline, process-tree kill, and strict response admission.
- `tools/x4-verification/isolation/candidate-worker.ps1` - Deterministic local-contract worker and held-out failure fixtures only.
- `tools/x4-verification/tests/candidate_isolation_contract.ps1` - Black-box success, timeout, descendant, malformed, identity, bound, stale, partial, duplicate, late, dynamic-authority, and canonicality coverage.

## Decisions Made

- Candidate data never supplies executable behavior or trust services. A stable `adapter_id` is the only selection seam, and unknown or metatable-backed declarations fail before adapter invocation.
- Completeness is the fixed contract criterion and exact expected-result equality is the fixed effect criterion. A complete valid unexpected result always produces `effect_verdict=mismatch`.
- The checked-in worker is local-contract-only. It does not import X4, read saves, call native APIs, mutate game state, or claim observed-in-X4 evidence.

## Verification

- `run_contracts.ps1 -Suite x4-candidate-runner`: PASS with three isolation markers, eight required R4 markers, one candidate-authority marker, and four independently loaded held-out markers.
- `candidate_isolation_contract.ps1 -Case all`: PASS, including Windows descendant-process termination and no accepted partial/late response.
- `run_contracts.ps1 -Suite x4-verification`: PASS, including Plan 06 owner-authority gates, candidate build/retention, isolation, package conformance, all eight R4 behaviors, and held-out attacks.
- `run_contracts.ps1 -Suite all`: all Phase 05.2 PowerShell gates pass first, then the unchanged pre-existing `telemetry_contract.lua:32` baseline fails before later Lua files.
- `cargo fmt --all -- --check`: PASS.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: PASS.
- `cargo test --workspace --locked`: PASS.
- `cargo run -p source-size-lint --locked -- crates tools`: unchanged pre-existing 231/222/226-line Rust violations only.
- `git diff --check`: PASS; the Plan 07 diff contains no public production Lua/XML/MD, save, game-installation, or mutation surface.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Required canonical response bytes and validation-scoped cleanup**

- **Found during:** Task 3 security review.
- **Issue:** Exact parsed fields alone did not reject alternate response encodings, and a generic exception path could reference the caller spelling of a response path.
- **Fix:** Added a RED noncanonical response, preserved timestamp strings during parsing, compared exact compact canonical bytes, and limited cleanup to a previously validated full response path.
- **Files modified:** `candidate-worker-protocol.v1.json`, `candidate-worker.ps1`, `invoke-candidate-worker.ps1`, `candidate_isolation_contract.ps1`.
- **Verification:** Focused isolation and candidate-runner suites.
- **Committed in:** `f49cad5`, `0c71741`.

**2. [Rule 3 - Blocking] Registered disjoint Plan 06 authority gates in the shared aggregate**

- **Found during:** Final aggregate integration with concurrent Plan 06.
- **Issue:** Plan 06 could not edit the Plan 07-owned shared runner, so its root-delegation and held-out authority suites were absent from `x4-verification` and `all`.
- **Fix:** Registered both fixed scripts in the shared aggregate without changing their implementation or ownership.
- **Files modified:** `extensions/live_galaxy/tests/run_contracts.ps1`.
- **Verification:** Direct Plan 06 suites and final `x4-verification` aggregate pass.
- **Committed in:** `414b3be`.

---

**Total deviations:** 2 auto-fixed (1 Rule 2, 1 Rule 3).
**Impact on plan:** Both fixes close required security/integration gates without adding X4, save, mutation, public runtime, or dependency scope.

## Issues Encountered

- The mandatory full Lua suite retains the documented unrelated `telemetry_contract.lua:32` failure.
- The source-size gate retains the documented unrelated 231/222/226-line Rust baseline. No Rust file changed.
- Plan 06 commits ran concurrently on disjoint files; its two authority gates were integrated only through the Plan 07-owned aggregate runner.

## Known Stubs

None. The worker adapter IDs are deterministic negative/local-contract fixtures, not an X4 runtime implementation or readiness claim.

## Skill Learning

Skill learning: none — evidence considered included all eight restored R4 behaviors, exact candidate-schema attacks, metatable callbacks, fixed internal verdict authority, cooperative watchdog exhaustion, external process-tree termination, malformed/partial/duplicate/stale/forged/late/noncanonical worker output, dynamic command/module/executable injection, direct-native token scans, focused/full/Rust gates, and the security cleanup review. The implementation applies existing inert-data, deterministic-order, bounded-work, fail-closed trust-boundary, terminable-isolation, no-native-in-pure-Lua, and aggregate-marker rules; it does not establish a missing reusable project rule.

## User Setup Required

None. X4 execution remains outside Phase 05.2 and no external service, dependency, save, or installed extension was touched.

## Next Phase Readiness

- Plan 05.2-08 can wire the declarative dispatcher to this fixed worker protocol without granting candidate code authority.
- The human-owned Phase 05.1 disposable X4 run remains pending; this plan proves local contracts and isolation only.
- Phase closure still owns the documented full-Lua and source-size baselines; Plan 07 neither weakens nor modifies them.

## Self-Check

PASSED — all nine plan-owned files and all nine task commits exist, and
`git diff --check` reports no errors.

---

*Phase: 05.2-layered-lua-and-x4-verification-foundation-inserted*
*Completed: 2026-08-30*
