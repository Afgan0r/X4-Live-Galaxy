---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 02
type: execute
wave: 2
depends_on: [05.2-01]
files_modified:
  - tools/x4-verification/x4-package-conformance.ps1
  - tools/x4-verification/contracts/package-conformance.v1.json
  - tools/x4-verification/fixtures/package-negative-fixtures.v1.json
  - tools/x4-verification/tests/package_conformance_contract.ps1
  - extensions/live_galaxy/tests/run_contracts.ps1
autonomous: true
requirements: [R3]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "R3/D-03: conformance follows the actual packaged entrypoint, import graph, manifest/UI registration, and native-binding acquisition path."
    - "A bare import, test-only loader, or alternate binding path cannot establish production-faithful conformance."
    - "Package checks remain static/local and never launch or mutate X4."
  artifacts:
    - tools/x4-verification/x4-package-conformance.ps1
    - tools/x4-verification/contracts/package-conformance.v1.json
    - tools/x4-verification/fixtures/package-negative-fixtures.v1.json
    - tools/x4-verification/tests/package_conformance_contract.ps1
  key_links:
    - "The conformance command resolves the package entrypoint from content/UI registration, then walks its declared imports to the native acquisition site."
    - "The resulting R3/D-03 verdict is presented to the Plan 01 admission gate with the v1 dossier and coverage digest."
---

<objective>
Prove production-faithful Lua package conformance over the real packaged import and binding path, and reject local-only success (R3; D-03).

Purpose: Prevent a green standalone Lua import or test loader from being mistaken for evidence about the shipped X4 package.
Output: A bounded package-conformance CLI, versioned contract, negative package fixtures, and focused tests.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-PATTERNS.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-01-SUMMARY.md
@extensions/live_galaxy/content.xml
@extensions/live_galaxy/ui.xml
@extensions/live_galaxy/lua/live_galaxy_runtime.lua
@extensions/live_galaxy/tests/run_contracts.ps1
@.planning/debug/resolved/phase-051-component-require.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the packaged entrypoint to native binding acquisition</name>
  <files>tools/x4-verification/x4-package-conformance.ps1, tools/x4-verification/contracts/package-conformance.v1.json, tools/x4-verification/fixtures/package-negative-fixtures.v1.json, tools/x4-verification/tests/package_conformance_contract.ps1</files>
  <behavior>
    - R3/D-03: the real package root resolves registration to the shipped entrypoint, its import graph, and its production native-binding acquisition site.
    - Missing registration, wrong entrypoint, unresolved import, cycle, root escape, alternate binding source, and excess graph depth/size produce stable non-conformant reason codes.
    - A bare import that succeeds outside the packaged graph is classified `local-only`, never `production-faithful`.
  </behavior>
  <action>Write failing real-package and negative-fixture tests first. Follow `tests/x4-disposable/01-install-guard.ps1` for verification-only isolated package-root checks and `extensions/live_galaxy/tests/component_discovery_binding_contract.ps1` for binding-contract expectations, while deliberately replacing their source-text/local-only limits with a parsed registered import graph. Implement a dependency-free PowerShell conformance command that accepts an explicit package root, loads the Plan 01 dossier, resolves content/UI registration and the production Lua graph under that root, and identifies the actual binding acquisition path per D-03. Normalize separators and case without following paths outside the package; cap bytes, files, imports, graph depth, cycles, and diagnostics. Feed a structured result to independent admission, not a second authority. Do not emulate X4 runtime semantics or claim runtime admission.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/package_conformance_contract.ps1 -Case packaged-path</automated>
  </verify>
  <done>The actual package reaches a stable conformance verdict through registration, entrypoint, import graph, and native binding acquisition; every R3/D-03 local-only or malformed path fixture fails for its intended reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register package conformance and enforce production-faithful classification</name>
  <files>tools/x4-verification/tests/package_conformance_contract.ps1, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <behavior>
    - The package suite invokes the real conformance CLI and Plan 01 admission policy against the repository package.
    - Test-only registration, fixture-local loaders, and alternate native-binding paths remain visible as `local-only` and cannot satisfy AC-04.
    - Existing Lua contract suite names and behavior are preserved.
  </behavior>
  <action>Add the failing aggregate test, then register `x4-package-conformance` under `x4-verification` in the existing runner. Test both the production package root and isolated negative fixtures; compare structured codes rather than error prose. Assert that conformance output contains no absolute machine path. Preserve the existing full-suite command and do not edit current production Lua, manifest, or UI registration to make the test pass; any source defect is reported back to its owning phase.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/package_conformance_contract.ps1 -Case all</automated>
  </verify>
  <done>The focused suite distinguishes production-faithful from local-only evidence and feeds the independent admission result without changing the Phase 05.1 seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Package root to conformance walker | Package manifests, registration, and Lua imports are untrusted graph input. |
| Conformance verdict to admission policy | Structural evidence crosses into the independent Plan 01 policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-05 | Spoofing | test-only package path | high | mitigate | Resolve from explicit package root and registered shipped entrypoint; label alternate roots local-only. |
| T-05.2-06 | Tampering | import graph traversal | high | mitigate | Normalize and contain paths, reject root escapes/cycles/duplicates, bind verdict to content digests. |
| T-05.2-07 | Denial of service | graph walker | medium | mitigate | Bound file bytes, node count, import count, depth, and diagnostic output. |
| T-05.2-08 | Information disclosure | path diagnostics | medium | mitigate | Emit repository-relative logical paths only and negative-test machine path leakage. |
| T-05.2-SC | Tampering | package supply chain | low | accept | No package installation occurs; only repository files and pinned existing tools are used. |
</threat_model>

<verification>
Focused: run `package_conformance_contract.ps1 -Case all` and `run_contracts.ps1 -Suite x4-package-conformance`.

Full: run `run_contracts.ps1 -Suite all`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, and `cargo run -p source-size-lint --locked -- crates tools`. Compare against the recorded baseline, preserve the production ban on `unwrap`, `expect`, `panic!`, and `unsafe`, and introduce no new failures; final phase closure still requires all mandatory gates green.

Before completing the summary, apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` and record the required `Skill learning: none` evidence statement or a complete candidate. Missing closure blocks completion.
</verification>

<success_criteria>
R3 is observable: only the actual packaged path can produce a production-faithful conformance result, and standalone/test-only success remains explicitly non-admissible.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-02-SUMMARY.md` when done.
</output>
