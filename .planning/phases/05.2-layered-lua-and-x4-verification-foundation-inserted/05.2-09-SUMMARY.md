---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 09
subsystem: x4-verification
tags: [powershell, evidence-retention, cryptography, admission, security]
status: complete
requires:
  - 05.2-06 owner-root delegation verification
  - 05.2-08 repository-rooted candidate producer
provides:
  - authenticated producer-to-retention chain
  - signed locator verification and sanitized admission object
  - held-out evidence-chain forgery gates
affects:
  - Phase 05.2 verification and security review
tech-stack:
  added: []
  patterns:
    - fixed-root delegated-purpose verification
    - owner-only atomic retention with reread verification
    - verified-locator-only admission
key-files:
  created:
    - tools/x4-verification/tests/evidence_chain_adversarial.ps1
  modified:
    - tools/x4-verification/retain-evidence.ps1
    - tools/x4-verification/contracts/sanitized-ledger.v1.json
    - tools/x4-verification/x4-admission.ps1
    - tools/x4-verification/tests/evidence_retention_contract.ps1
    - tools/x4-verification/tests/admission_contract.ps1
    - extensions/live_galaxy/tests/run_contracts.ps1
key-decisions:
  - Production owner root and delegated signing keys remain unconfigured; no ceremony or production artifact is claimed.
  - Authenticated local-contract evidence reaches chain-verified-x4-pending only; observed-in-X4 authority remains absent.
  - Admission consumes only the object returned by live locator verification and continues to reject hand-authored ledgers.
actuals:
  tokens: 19130
  tasks: 3
  commits: 7
metrics:
  completed: 2026-08-30
  duration: 1h
---

# Phase 05.2 Plan 09: Authenticated Evidence Retention Summary

Repository-rooted producer evidence now reaches owner-only retention, delegated signed-locator verification, and sanitized admission evaluation without granting local evidence X4 authority.

## What Was Built

- Retention verifies the fixed owner root and exact `candidate-producer` delegation, producer signature, schema, bounds, generated-component digests, and inert candidate package graph before any durable write.
- Retained evidence, manifest, and producer attestation use atomic staging, destination containment, no-reparse checks, owner-only permissions, and post-write digest and ACL rereads.
- Locator signing requires an already provisioned root-delegated `retention-locator` key with the exact purpose, epoch, scope, and SPKI identity.
- Sanitized ledger data is derived only after locator verification and includes the producer-attestation digest without private paths or key material.
- Admission accepts only `-VerifiedLocatorPath`, invokes the repository-fixed retention verifier, and evaluates its bounded in-memory result. Direct sanitized-ledger input remains rejected.
- Held-out tests cover replay, root swap, TEST-only root injection, purpose confusion, epoch rollback, producer and locator delegated-key substitution, signature transplant, tampering, reparse defense, and unsupported-platform behavior.

## TDD Gate Compliance

- RED `fb212ed` -> GREEN `9161281`: authenticated retention producer and signed locator.
- RED `bd0b07d` -> GREEN `ad616b7`: verified-locator-only admission.
- RED `e67e6ff` -> GREEN `eb23bc1`: held-out chain-forgery and aggregate gates.

## Commits

| Commit | Purpose |
| --- | --- |
| `fb212ed` | Restore the failing retention tracer contract. |
| `9161281` | Authenticate and retain candidate evidence. |
| `bd0b07d` | Add the failing verified-locator admission contract. |
| `ad616b7` | Admit only live verified locator chains. |
| `e67e6ff` | Add the failing held-out evidence-chain gate. |
| `eb23bc1` | Close replay, substitution, purpose, epoch, and signature forgery gates. |
| `8399491` | Preserve the established retained-admission aggregate marker. |

## Verification

- Focused retention, retention-platform, retention-admission, admission evidence-chain, and held-out adversarial contracts: PASS.
- `run_contracts.ps1 -Suite x4-verification`: PASS with all Plan 09 markers and existing authority, build, isolation, package, and candidate-runner gates.
- `package_conformance_contract.ps1 -Case all`: PASS after preserving its established aggregate marker.
- `run_contracts.ps1 -Suite all`: all Phase 05.2 PowerShell and candidate-runner gates pass before the unchanged unrelated `extensions/live_galaxy/tests/telemetry_contract.lua:32` baseline failure.
- `cargo fmt --all -- --check`: PASS.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: PASS.
- `cargo test --workspace --locked`: PASS.
- `cargo run -p source-size-lint --locked -- crates tools`: unchanged unrelated 231/222/226-line Rust baselines only.
- Public runtime diff under `extensions/live_galaxy` excluding tests: empty.
- Credential, private-key, and machine-private-path scan over Plan 09 files: no matches.
- Temporary transformed retention/admission harness scan: empty.
- `git diff --check` over the Plan 09 range: PASS.

## Retained Artifact and Authority Result

Production root and delegated producer/locator authority are not provisioned. Production therefore returns the stable unconfigured result before retention and creates no production artifact, locator, or sanitized ledger. The isolated TEST-only chain generated fresh current-schema evidence, reread and verified every retained byte, digest, signature, and ACL, then removed the test corpus; it is not represented as durable or production evidence.

No X4 process was launched, no save was accessed, and no installed game or mod file was modified.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved the established retained-admission marker**

- **Found during:** Final canonical package conformance gate.
- **Issue:** Plan 09's more specific cryptographic-core marker replaced a marker consumed by the independent aggregate contract.
- **Fix:** Emit both the established fail-closed marker and the new cryptographic-core marker from the same verified positive path.
- **Files modified:** `tools/x4-verification/tests/evidence_retention_contract.ps1`
- **Commit:** `8399491`

## Known Baselines

- `extensions/live_galaxy/tests/telemetry_contract.lua:32` remains the pre-existing full-Lua-suite failure.
- Source-size lint remains red only for `batch.rs` at 231 lines, `runtime_facts_contract.rs` at 222 lines, and `named_pipe_contract.rs` at 226 lines.

## Skill Learning

Skill learning: none. Evidence considered: fixed-root/delegated-purpose implementation, independent digest and ACL rereads, the replay/substitution/tamper suite, unsupported-platform behavior, the aggregate-marker regression, and all gate results. Existing X4 integration, X4 test, Rust, Lua, and repository verification rules already require these boundaries and full aggregate registration; no new durable behavioral rule is justified.

## Self-Check: PASSED

All seven Plan 09 implementation/test paths exist, all seven task/fix commits resolve, no Plan 09 temporary harness remains, and the summary records the exact green gates and unchanged baselines without claiming production authority or observed-in-X4 evidence.
