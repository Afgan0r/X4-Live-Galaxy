---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 01
subsystem: testing
tags: [powershell, json, x4, admission, fail-closed, provenance]

requires:
  - phase: 05.1-bounded-real-component-discovery-inserted
    provides: Runtime loader, binding, identity, bounds, completeness, and harness failure evidence
provides:
  - Versioned nine-dimension X4 integration dossier contract
  - Seven-class Known X4 Failure Registry with exhaustive executable coverage
  - Independent fail-closed admission CLI with exact owner overrides
affects: [05.2-02, 05.2-03, 05.2-04, 05.2-05, 05.1-runtime-handback]

actuals:
  tokens: 13984
  tasks: 3
  commits: 6

tech-stack:
  added: []
  patterns: [bounded PowerShell JSON validation, dual-provenance admission, exact digest-bound override]

key-files:
  created:
    - tools/x4-verification/x4-admission.ps1
    - tools/x4-verification/contracts/dossier.v1.json
    - tools/x4-verification/contracts/known-failures.v1.json
    - tools/x4-verification/contracts/coverage.v1.json
    - tools/x4-verification/contracts/owner-override.v1.json
    - tools/x4-verification/fixtures/negative-fixtures.v1.json
    - tools/x4-verification/tests/admission_contract.ps1
  modified:
    - extensions/live_galaxy/tests/run_contracts.ps1

key-decisions:
  - "The committed complete dossier is an explicitly named validation fixture; it proves the policy path without admitting the runtime-pending Phase 05.1 seam."
  - "Registry growth is allowed, but every current row must have evidence and executable coverage before any dossier is evaluated."
  - "An owner override changes one known-failure disposition only when its finding, dossier ID and SHA-256 digest, decision, rationale, remaining risk, and expiry match exactly."

patterns-established:
  - "Dual provenance: every dossier dimension and registry row binds distinct primary-source and verified working-production-precedent records."
  - "Fail-closed result: every malformed, ambiguous, over-bound, uncovered, unresolved, or stale input returns a stable logical reason code and no local path."

requirements-completed: [R1, R2]

coverage:
  - id: D1
    description: Versioned nine-dimension dossier is independently parsed and admitted only with complete dual provenance
    requirement: R1
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case dossier"
        status: pass
    human_judgment: false
  - id: D2
    description: All seven Known X4 Failure classes have deterministic executable negative fixtures and exhaustive coverage
    requirement: R2
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case negative-fixtures"
        status: pass
    human_judgment: false
  - id: D3
    description: Known failures stay blocked unless an exact immutable digest-bound owner override applies
    requirement: R2
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case admission"
        status: pass
    human_judgment: false

duration: 19min
completed: 2026-08-30
status: complete
---

# Phase 05.2 Plan 01: X4 Admission Policy Summary

**Bounded PowerShell admission now enforces nine-dimension dual provenance, seven executable X4 failure classes, and exact digest-bound owner overrides.**

## Performance

- **Duration:** 19 min
- **Started:** 2026-08-29T23:02:23Z
- **Completed:** 2026-08-29T23:20:52Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Added a dependency-free admission CLI that validates strict schema versions, exact dossier dimensions, independent provenance, stable IDs, bounds, and immutable SHA-256 identity.
- Bound the seven locked Known X4 Failure classes to deterministic negative fixtures and rejected missing, skipped, mismatched, duplicated, passing, or newly uncovered coverage.
- Kept known failures non-admissible by default and allowed only one exact, separately reported, 90-day-bounded owner override without modifying the source dossier.

## Task Commits

Each TDD gate was committed atomically:

1. **Task 1 RED: complete dossier contract** - `d46573a` (`test`)
2. **Task 1 GREEN: dossier admission policy** - `f1bc825` (`feat`)
3. **Task 2 RED: seven negative fixture coverage** - `66d5535` (`test`)
4. **Task 2 GREEN: exhaustive fixture binding** - `a3c9b1c` (`feat`)
5. **Task 3 RED: exact owner override contract** - `27463ab` (`test`)
6. **Task 3 GREEN: digest-bound owner override** - `cacfd60` (`feat`)

## Files Created/Modified

- `tools/x4-verification/x4-admission.ps1` - Independent bounded parser, coverage gate, dossier policy, and override evaluator.
- `tools/x4-verification/contracts/dossier.v1.json` - Complete validation-only nine-dimension dossier.
- `tools/x4-verification/contracts/known-failures.v1.json` - Seven-class registry with primary and precedent evidence identifiers.
- `tools/x4-verification/contracts/coverage.v1.json` - Exhaustive registry-to-fixture coverage matrix.
- `tools/x4-verification/contracts/owner-override.v1.json` - Versioned fail-closed owner override document shape.
- `tools/x4-verification/fixtures/negative-fixtures.v1.json` - Seven bounded deterministic known-failure fixtures.
- `tools/x4-verification/tests/admission_contract.ps1` - Executable dossier, coverage, fixture, and override contracts.
- `extensions/live_galaxy/tests/run_contracts.ps1` - Focused `x4-admission` and aggregate `x4-verification` dispatch.

## Decisions Made

- The complete dossier is a contract fixture, not evidence that the Phase 05.1 runtime seam is admissible; the real seam remains `runtime-pending`.
- The registry preserves all seven locked classes and permits future growth only when every new row receives complete coverage.
- Override diagnostics expose logical IDs, reason codes, and digests only; paths, private values, and source mutations remain prohibited.

## Verification

- `admission_contract.ps1 -Case dossier`: PASS.
- `admission_contract.ps1 -Case negative-fixtures`: PASS.
- `admission_contract.ps1 -Case admission`: PASS.
- `run_contracts.ps1 -Suite x4-admission`: PASS.
- `run_contracts.ps1 -Suite x4-verification`: PASS.
- `run_contracts.ps1 -Suite all`: expected pre-existing failure only at `telemetry_contract.lua:32`; all three new admission cases passed first.
- `cargo fmt --all -- --check`: PASS.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: PASS.
- `cargo test --workspace --locked`: unrelated flaky failure in `strategic-state/tests/packet_determinism.rs`; three focused reruns produced PASS, FAIL, PASS.
- `cargo run -p source-size-lint --locked -- crates tools`: expected pre-existing failures at `batch.rs` (231), `runtime_facts_contract.rs` (222), and `named_pipe_contract.rs` (226).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved a valid empty findings array**

- **Found during:** Task 1 GREEN.
- **Issue:** PowerShell deserialized an empty JSON array as null, so the complete no-finding dossier was rejected.
- **Fix:** Required the property explicitly, then normalized its value to a bounded array.
- **Files modified:** `tools/x4-verification/x4-admission.ps1`.
- **Verification:** `admission_contract.ps1 -Case dossier`.
- **Committed in:** `f1bc825`.

**2. [Rule 1 - Bug] Preserved single fixture arrays during coverage validation**

- **Found during:** Task 2 GREEN.
- **Issue:** PowerShell pipeline unrolling converted a one-item `fixture_ids` array into a scalar and rejected valid coverage.
- **Fix:** Validated the original property type before explicitly normalizing it to an array.
- **Files modified:** `tools/x4-verification/x4-admission.ps1`.
- **Verification:** `admission_contract.ps1 -Case negative-fixtures`.
- **Committed in:** `a3c9b1c`.

**3. [Rule 1 - Bug] Accepted PowerShell's typed ISO expiry representation**

- **Found during:** Task 3 GREEN.
- **Issue:** `ConvertFrom-Json` materialized ISO timestamps as `DateTime`, while the override parser expected only text.
- **Fix:** Normalized `DateTime`, `DateTimeOffset`, and exact UTC text before enforcing expiry bounds.
- **Files modified:** `tools/x4-verification/x4-admission.ps1`.
- **Verification:** `admission_contract.ps1 -Case admission`.
- **Committed in:** `cacfd60`.

---

**Total deviations:** 3 auto-fixed Rule 1 bugs.
**Impact on plan:** The fixes preserve the planned strict fail-closed contract without adding scope.

## Issues Encountered

- Full Lua verification retains the authoritative pre-existing `telemetry_contract.lua:32` failure.
- The repository-wide source-size gate retains the three authoritative pre-existing violations listed above.
- Workspace tests exposed an unrelated nondeterministic `packet_determinism` failure. Its focused PASS/FAIL/PASS sequence proves it is not caused by the PowerShell/JSON-only plan diff; it remains outside Plan 05.2-01 ownership.

## Known Stubs

None. The expired owner-override document is an intentionally unusable fail-closed contract example, not runtime configuration.

## Skill Learning

Skill learning: none — evidence considered included the three PowerShell representation bugs, all focused admission runs, the full Lua baseline, Rust gates, and the unrelated flaky Rust test. The fixes are language/runtime mechanics already expressed by the existing strict parsing, bounds, deterministic-test, and fail-closed skill rules; they do not establish a missing reusable Live Galaxy rule.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 05.2-02 can consume the dossier, registry, coverage, fixture, and admission result contracts.
- Phase-level closure still requires the external Lua, flaky Rust determinism, and source-size gates to be resolved by their owners; Plan 05.2-01 did not weaken or modify them.

## Self-Check

PASSED — all nine created or modified files exist and all six TDD commits resolve in Git.

---

*Phase: 05.2-layered-lua-and-x4-verification-foundation-inserted*
*Completed: 2026-08-30*
