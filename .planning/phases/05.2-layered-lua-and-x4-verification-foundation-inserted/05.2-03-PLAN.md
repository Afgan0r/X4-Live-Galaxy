---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 03
type: execute
wave: 3
depends_on: [05.2-02]
files_modified:
  - tests/x4-candidates/lua/live_galaxy_candidate_runner.lua
  - tests/x4-candidates/lua/phase_051_candidates.lua
  - tools/x4-verification/contracts/runtime-evidence.v1.json
  - extensions/live_galaxy/tests/x4_candidate_runner_contract.lua
  - extensions/live_galaxy/tests/run_contracts.ps1
autonomous: true
requirements: [R4, R5]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "R4/D-04: candidates run in stable order, isolate failures, continue after failure, and report execution, contract, and effect separately."
    - "A valid but unexpected result always has `effect_verdict != pass`."
    - "Every candidate produces bounded versioned JSONL that validates against the runtime-evidence contract consumed by retention."
  artifacts:
    - tests/x4-candidates/lua/live_galaxy_candidate_runner.lua
    - tests/x4-candidates/lua/phase_051_candidates.lua
    - tools/x4-verification/contracts/runtime-evidence.v1.json
    - extensions/live_galaxy/tests/x4_candidate_runner_contract.lua
  key_links:
    - "The runner writes runtime-evidence v1 JSONL records with run/candidate identity, expected/actual result, completeness, failure point, and digest."
    - "Plan 05 consumes the same committed runtime-evidence contract before retaining JSONL or emitting a sanitized ledger row."
---

<objective>
Create a deterministic read-only candidate runner whose bounded JSONL output is compatible with the later private evidence boundary (R4, R5; D-04, D-05).

Purpose: Prove candidate isolation, three-axis semantics, and the producer side of the evidence contract before building an X4-facing test package.
Output: Pure Lua runner and candidates, versioned runtime-evidence schema, focused negative contracts, and suite registration.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-PATTERNS.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-02-SUMMARY.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-VERIFICATION.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-04-X4-REFERENCE.md
@.planning/phases/05.1-bounded-real-component-discovery-inserted/05.1-05-RESEARCH.md
@extensions/live_galaxy/lua/live_galaxy_runtime.lua
@extensions/live_galaxy/tests/x4_discovery_contract.lua
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Emit one candidate as validated runtime-evidence JSONL</name>
  <files>tests/x4-candidates/lua/live_galaxy_candidate_runner.lua, tests/x4-candidates/lua/phase_051_candidates.lua, tools/x4-verification/contracts/runtime-evidence.v1.json, extensions/live_galaxy/tests/x4_candidate_runner_contract.lua</files>
  <behavior>
    - R4/D-04: one candidate executes through `execution_verdict`, `contract_verdict`, and `effect_verdict` in that order and emits distinct stable values.
    - Each JSONL row carries schema version, exact game version, mod list, scenario, run/candidate/stage IDs, candidate source, expected and actual result, completeness, elapsed real/game time, optional SETA state, exact failure point, prior dossier/build identity, digest algorithm, canonical digest payload, and record digest.
    - Candidate count, stages, per-step work units, observations, strings, row bytes, total bytes, and output rows are deterministic and bounded.
  </behavior>
  <action>Write the failing runner/JSONL contract first using the pinned Lua path. Follow `extensions/live_galaxy/lua/live_galaxy_runtime.lua` for protected stage boundaries and lifecycle-safe callback shape, and `extensions/live_galaxy/tests/x4_discovery_contract.lua` for deterministic injected outcomes; deliberately keep this harness developer-only rather than registering production behavior. Define `runtime-evidence.v1.json` as the shared producer/retention interface. Implement a declarative runner that sorts stable candidate IDs, uses protected calls per stage, applies a deterministic per-step work-unit budget and explicit externally supplied timeout marker, continues after failure, and emits canonical bounded JSONL. Require an injected digest adapter rather than hand-rolled cryptography; it hashes the exact canonical payload and emits algorithm plus digest, while missing/error/mismatched digest results fail the contract. It must not launch X4, inspect saves, or claim runtime observation.</action>
  <verify>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-candidate-runner</automated>
  </verify>
  <done>One candidate traverses all three verdict stages and its bounded JSONL validates independently against `runtime-evidence.v1.json` with stable identity and digest fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate failures, budgets, timeout markers, and unexpected effects</name>
  <files>tests/x4-candidates/lua/live_galaxy_candidate_runner.lua, extensions/live_galaxy/tests/x4_candidate_runner_contract.lua, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <behavior>
    - An early candidate exception, malformed result, exhausted work-unit budget, or deterministic timeout marker records the exact failure and later candidates still run in stable order.
    - A structurally valid observation that differs from the declared expected result produces `effect_verdict != pass`; it cannot be collapsed into execution or contract success.
    - Missing identity, excess steps/bytes/rows, digest mismatch, collapsed verdict fields, and noncanonical order fail the independent JSONL contract.
  </behavior>
  <action>Add negative fixtures before implementation for every behavior above, including a valid-but-unexpected observation. Extend the runner with explicit `max_steps`, `max_work_units_per_step`, `max_candidate_rows`, and total serialized byte enforcement; use deterministic counters and injected timeout markers rather than an unmeasured wall-clock promise. Register focused `x4-candidate-runner` under aggregate `x4-verification` without weakening existing suite behavior. Assert exact reason codes and later-candidate rows rather than source text.</action>
  <verify>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-candidate-runner</automated>
  </verify>
  <done>All failure modes remain isolated and bounded, later candidates run, JSONL stays contract-valid, and the valid-but-unexpected fixture proves `effect_verdict != pass`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Candidate definitions to Lua runner | Declarative candidates and injected results are untrusted executable configuration. |
| Runner JSONL to later retention | Structured output may be malformed, oversized, identity-confused, or privacy-sensitive until independently validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-09 | Tampering | verdict axes and JSONL | high | mitigate | Canonical schema, non-collapsible fields, digest input, deterministic IDs/order, and negative fixtures. |
| T-05.2-10 | Denial of service | candidate stages | high | mitigate | Per-step work units, stages, rows, bytes, observations, and deterministic timeout-marker bounds. |
| T-05.2-11 | Repudiation | run/candidate identity | medium | mitigate | Stable run/candidate/stage IDs, source identity, prior-artifact digests, and canonical row digests. |
| T-05.2-12 | Information disclosure | serialized diagnostics | medium | mitigate | Contract excludes raw private paths, secrets, prompts, hidden reasoning, and unbounded error prose. |
| T-05.2-SC | Tampering | package supply chain | low | accept | No install occurs; the existing pinned Lua runner and repository PowerShell are used. |
</threat_model>

<verification>
Focused: run both candidate-runner cases and `run_contracts.ps1 -Suite x4-candidate-runner`.

Full: run `run_contracts.ps1 -Suite all`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, and `cargo run -p source-size-lint --locked -- crates tools`. No Rust source is modified; all Rust source remains at most 200 physical lines and production `unwrap`, `expect`, `panic!`, and `unsafe` remain forbidden.

Before completing the summary, apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` and record the required `Skill learning: none` evidence statement or a complete candidate. Missing closure blocks completion.
</verification>

<success_criteria>
R4 and the producer side of R5 are executable: failures cannot suppress later work, three verdicts remain distinct, unexpected effects never pass, and every output row is bounded versioned JSONL ready for retention validation.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-03-SUMMARY.md` when done.
</output>
