---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
verified: 2026-08-30T04:04:02Z
status: gaps_found
score: 2/6 requirements verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "R2: an exact, scoped owner override is the only supported exception to the Known X4 Failure gate."
    status: failed
    reason: "The override parser still exists, but the public admission path rejects every supplied override with OWNER_OVERRIDE_DISABLED before it can evaluate exact scope, digest, expiry, rationale, owner decision, or remaining risk."
    artifacts:
      - path: "tools/x4-verification/x4-admission.ps1"
        issue: "The unconditional OWNER_OVERRIDE_DISABLED guard makes the required exact-override success path unreachable."
      - path: "tools/x4-verification/tests/admission_contract.ps1"
        issue: "The current test proves only that all overrides are disabled; it no longer proves that one exact valid override changes one disposition."
    missing:
      - "Add an authenticated owner-decision input or another independently trusted authority boundary."
      - "Restore one exact valid-override behavioral test while retaining malformed, broad, stale, expired, and digest-mismatched rejection tests."
  - truth: "R4: the read-only candidate harness runs candidates deterministically, isolates failures, continues later candidates, and emits three distinct verdict axes."
    status: failed
    reason: "The complete runner implementation is overwritten by an unconditional trusted_runtime_attestation_missing function. The contract deletes all eight positive/negative behavioral cases before execution and keeps only the hard-disable case."
    artifacts:
      - path: "tests/x4-candidates/lua/live_galaxy_candidate_runner.lua"
        issue: "runner.run is replaced unconditionally at line 331, so no candidate, digest adapter, or watchdog can execute."
      - path: "extensions/live_galaxy/tests/x4_candidate_runner_contract.lua"
        issue: "All earlier R4 cases are removed from the cases table at line 439; Suite x4-candidate-runner exits 0 with no behavioral output."
    missing:
      - "Separate the pure local-contract runner from the unattested X4/native dispatcher instead of disabling both."
      - "Re-enable named tests for deterministic order, failure isolation, continuation, bounded timeout/work, three-axis progression, and valid-unexpected effect mismatch."
      - "Keep native execution behind a terminable trusted adapter while allowing the pure deterministic contract required by Phase 05.2."
  - truth: "R5: runtime-evidence JSONL and its locator can be retained durably with owner-only access, digest reread, and sanitized handback."
    status: failed
    reason: "Test-BuildManifest unconditionally fails TRUSTED_RUNTIME_ATTESTATION_MISSING before generated-file, evidence-stream, atomic-placement, ACL, locator, reread, or sanitized-output logic. The retention test exits after proving this rejection, leaving the former positive retention path unreachable."
    artifacts:
      - path: "tools/x4-verification/retain-evidence.ps1"
        issue: "The unconditional fail at line 307 prevents every retain and locator-verification success path."
      - path: "tools/x4-verification/tests/evidence_retention_contract.ps1"
        issue: "The retention cases exit at line 245 after checking hard-disable behavior; the positive retention and tamper/ACL checks below it are dead code."
      - path: "tools/x4-verification/x4-admission.ps1"
        issue: "Runtime admission always fails RUNTIME_ADMISSION_PENDING and rejects every supplied sanitized ledger before Test-EvidenceChain can run."
    missing:
      - "Define and implement the trusted runtime/build attestation consumed by retention."
      - "Restore executable positive retention, locator re-verification, sanitized handback, and locator-to-admission tests."
      - "Keep hand-authored ledger rejection, but add a verified locator/attestation input path that can reach Test-EvidenceChain and a non-admissible/admissible decision from real retained evidence."
      - "Regenerate or migrate the retained local-contract artifact to the current manifest schema; its current locator verification fails because native_execution_status is absent."
  - truth: "R6 and Roadmap SC5: the complete Phase 05.1 matrix is prepared for one later human X4 run, while Phase 05.2 performs no X4 execution or mutation."
    status: failed
    reason: "The seven-row matrix and pending ledger exist, but the generated roots are explicitly scaffold-only/runtime-pending. The run procedure says not to install or invoke them and lists seven adapters, a scheduler, terminable isolation, digest adapter, private JSONL sink, and dispatcher as missing future implementation. This is a handoff specification, not a run-ready prepared candidate set."
    artifacts:
      - path: "tools/x4-verification/build-candidate-extension.ps1"
        issue: "Generated manifests are scaffold-only and runtime-pending; generated entrypoints do not provide executable candidate adapters or evidence transport."
      - path: "tests/x4-candidates/05.1-candidate-run-procedure.md"
        issue: "The Blocking implementation gate explicitly forbids using the current scaffold and enumerates the missing runtime pieces."
      - path: "tests/x4-candidates/phase-05.1-candidates.v1.json"
        issue: "The matrix is complete as planning data but is not wired to executable adapters."
    missing:
      - "Package all seven declarative adapters without changing the Phase 05.1 production seam."
      - "Add a bounded cooperative dispatcher, terminable native isolation boundary, verified digest adapter, and private JSONL sink."
      - "Generate current-schema execution-ready manifests only after those controls pass contract tests; keep the actual X4 run human-owned and pending."
unverified_prohibition_items:
  - statement: "MUST NOT commit full JSONL, secrets, private paths, saves, prompts, or hidden reasoning."
    verdict: "non-authoritative LLM judgment: no leak found in the changed runtime/test surface; human review recommended"
  - statement: "MUST NOT execute X4, inspect saves, mutate game state, or change Phase 05.1 production behavior in this phase."
    verdict: "non-authoritative LLM judgment: public runtime diff is empty and artifacts claim no execution; human review recommended"
---

# Phase 05.2: Layered Lua and X4 Verification Foundation Verification Report

**Phase Goal:** Require source-saturated dossiers, an executable known-failure gate, production-faithful conformance, and bounded read-only candidate experiments before any future X4 seam is planned, implemented, packaged, or tested in game.
**Verified:** 2026-08-30T04:04:02Z
**Status:** gaps_found
**Re-verification:** No — initial verification
**Revision verified:** `df7130824a830bf291e8b8a392a8019ce47443ab` (branch 51 ahead, 0 behind refreshed upstream)

## Goal Achievement

The phase is secure in the narrow fail-closed sense, but the goal is not achieved. Security fixes closed unsafe authority, callback, retention, and admission paths by making their success paths unreachable. That is valid threat mitigation, but it removed required R2/R4/R5 behavior and left R6 as a non-runnable scaffold. A passing rejection test is not evidence that the positive foundation contract exists.

### Specification Requirements

| Requirement | Status | Evidence |
| --- | --- | --- |
| R1 Integration dossier contract | ✓ VERIFIED | `admission_contract.ps1 -Case dossier` passes complete dual-provenance validation and rejects missing dimensions, provenance, unknown/conflicting states, duplicate IDs, and bounds. The canonical Phase 05.1 dossier remains runtime-pending rather than falsely admitted. |
| R2 Known X4 Failure Registry | ✗ FAILED | All seven failure fixtures and exhaustive coverage pass, but the required exact owner-override path is unconditionally disabled at `x4-admission.ps1:647`. Plan 01's exception truth is false. |
| R3 Production-faithful conformance | ✓ VERIFIED | `package_conformance_contract.ps1 -Case all` passes. The shipped package alone can be `production-faithful`; alternate roots remain `local-only`; registration, complete import graph, binding acquisition, path containment, alias/helper, and malformed/bounded negatives are exercised. |
| R4 Read-only candidate experiment harness | ✗ FAILED | `runner.run` is unconditionally replaced and eight behavioral cases are deleted at test load. The focused suite exits 0 but exercises only hard-disable behavior. |
| R5 Evidence retention and decision ledger | ✗ FAILED | Retention unconditionally fails before placement/ACL/digest/locator logic; current tests exit before the former positive path. Admission rejects every sanitized ledger and never reaches the evidence-chain implementation. |
| R6 Phase 05.1 candidate matrix | ✗ FAILED | The seven-row matrix is structurally complete, but the procedure explicitly says it cannot be installed or invoked until a later implementation supplies all adapters, isolation, scheduler, digest, sink, and dispatcher. It is not ready for the promised later human run. |

**Score:** 2/6 requirements verified

### Roadmap Success Criteria

| # | Success criterion | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Versioned dual-provenance dossier rejects incomplete seams | ✓ VERIFIED | Dossier contract and negative mutations pass. |
| 2 | Seven-class registry and exhaustive coverage gate every seam with no automatic exception | ✓ VERIFIED | Seven fixtures execute; uncovered, skipped, duplicated, mismatched, passing, and new-row gaps reject. All overrides currently fail closed. |
| 3 | Packaged production import/registration/binding graph is proven without permissive local paths | ✓ VERIFIED | Full package conformance case passes. |
| 4 | Bounded developer-only harness isolates failures and retains private digest-linked three-axis JSONL | ✗ FAILED | Runner and retention success paths are unreachable; their aggregate pass markers prove only the hard-disable guards. |
| 5 | Phase 05.1 unknowns are prepared for one later human run without Phase 05.2 execution/mutation | ✗ FAILED | Matrix exists, but current generated roots are non-invokable scaffolds and the handoff lists a later implementation gate. |

### Acceptance Criteria

| Criterion | Status | Evidence |
| --- | --- | --- |
| AC-01 complete dossier and missing-field/provenance rejection | ✓ VERIFIED | Focused dossier contract passes. |
| AC-02 seven registered defects have failing negative fixtures | ✓ VERIFIED | Focused negative-fixtures contract passes with all seven stable result codes. |
| AC-03 incomplete coverage is rejected and only complete evidence/check mappings proceed | ✓ VERIFIED (validation boundary) | Coverage validation works for the current all-applicable registry. Runtime admission and owner override remain separate failed truths. |
| AC-04 packaged conformance rejects bare/test-only/missing/wrong binding paths | ✓ VERIFIED | `package_conformance_contract.ps1 -Case all` exits 0. |
| AC-05 candidate continuation, isolation, and three verdicts | ✗ FAILED | The named behavioral tests are removed from the returned case table; only hard-disable runs. |
| AC-06 bounded digest-linked private JSONL and sanitized committed metadata | ✗ FAILED | Schemas and validation code exist, but no current retention success path can execute. |
| AC-07 complete Phase 05.1 matrix has no source-resolvable or mutating candidate | ✓ VERIFIED (structure only) | Exactly seven runtime-pending rows, five source exclusions, two deterministic conflict-safe groups, and no mutation vocabulary. This does not make the builds run-ready. |
| AC-08 no unsupported observed-in-X4 claim | ✓ VERIFIED | Ledger remains scaffold-only/runtime-pending; summaries and current procedure explicitly deny X4 execution/admission. |

### Edge Coverage

| Edge | Status | Evidence |
| --- | --- | --- |
| Missing dossier fields | ✓ VERIFIED | One-field and one-dimension negatives reject. |
| `UNKNOWN` / `CONFLICTING` evidence | ✓ VERIFIED | Non-admissible without a trusted resolution. |
| Registry growth | ✓ VERIFIED | Newly uncovered rows reject. |
| Override boundary | ✗ FAILED | Exact valid override cannot succeed because all overrides are disabled. |
| Package resolution | ✓ VERIFIED | Bare/test-only/escape/cycle/alternate-binding/bound fixtures reject. |
| Partial candidate failure | ✗ FAILED | Continuation implementation is unreachable and its test is deleted from the executed table. |
| Evidence size and identity | ✗ FAILED | Negative prechecks exist, but retention cannot reach a positive evidence/locator identity flow. |
| Empty candidate set | ✓ VERIFIED | Empty, duplicate, incomplete, asymmetric, conflict, and profile mismatch matrices reject. |

### Prohibitions

| Prohibition | Status | Evidence |
| --- | --- | --- |
| No admission from isolated call signature/design-derived fake | ✓ VERIFIED | Dossier dimensions and dual provenance are required; validation fixtures cannot become runtime admission. |
| No skipped failure class or automatic small-change exception | ✓ VERIFIED | Coverage is exhaustive and every current override is fail-closed. |
| No production path more permissive than X4 resolution | ✓ VERIFIED | Alternate roots and permissive imports are local-only/non-conformant. |
| No candidate failure suppresses later candidates or collapses verdict axes | ⚠ UNVERIFIED | The original behavioral implementation is unreachable and its tests are erased; hard-disable prevents the forbidden behavior only vacuously. |
| No full/private evidence or secret-bearing content enters Git | ✓ VERIFIED (non-authoritative judgment flag) | No leak found in phase runtime/test artifacts; retained data is outside Git. Human review remains recommended for the judgment tier. |
| No X4/save/mutation/Phase 05.1 production change | ✓ VERIFIED (non-authoritative judgment flag) | Diff from executor baseline contains no public package/production Lua/XML/MD change; no X4 execution evidence exists. Human review remains recommended. |

## Required Artifacts

| Artifact group | Exists | Substantive | Wired / flowing | Status |
| --- | --- | --- | --- | --- |
| Dossier, registry, coverage, fixtures, admission validator | Yes | Yes | Validation flows; exact override and runtime admission do not | ⚠ PARTIAL |
| Package contract, walker, fixtures, tests | Yes | Yes | Registered package → full import graph → binding classification | ✓ VERIFIED |
| Candidate runner, fixture, runtime-evidence schema | Yes | Yes internally | Public `runner.run` is overwritten; behavioral tests removed | ✗ STUB FOR R4 |
| Candidate matrix, builder, templates, build-manifest contract | Yes | Yes | Matrix → two deterministic package-conformant scaffold roots | ⚠ HOLLOW FOR EXECUTION |
| Retention helper, sanitized schema, retention tests | Yes | Large/dead implementation remains | Unconditional attestation failure disconnects JSONL → private store → locator | ✗ STUB FOR R5 |
| Pending ledger and human procedure | Yes | Yes | Procedure points to missing future implementation and disconnected retention/admission | ⚠ PARTIAL |

The generic `verify.artifacts` and `verify.key-links` queries returned zero parsed artifacts/links for all five plans because these plans use scalar lists rather than the object form understood by the query. The table above is the required manual Level 1–4 verification.

## Key Links and Data Flow

| From | To | Status | Details |
| --- | --- | --- | --- |
| Dossier/registry/coverage | Independent validation verdict | ✓ FLOWING | Complete fixture validates and malformed/uncovered inputs fail with stable codes. |
| Shipped package root | Registration/import graph/native acquisition verdict | ✓ FLOWING | Full conformance suite passes on the real package. |
| Candidate definitions | Three-stage JSONL runner | ✗ DISCONNECTED | `runner.run` always returns `trusted_runtime_attestation_missing`. |
| Runtime JSONL + build manifest | Owner-only retained files + locator | ✗ DISCONNECTED | `Test-BuildManifest` always fails before evidence validation or writes. |
| Retained locator/sanitized ledger | Independent evidence-chain admission | ✗ DISCONNECTED | Locator input is not accepted by admission; sanitized ledger input is rejected and runtime admission always remains pending. |
| Seven-row matrix | Generated candidate roots | ⚠ STATIC/SCAFFOLD | Two roots are deterministic and package-conformant, but include no executable candidate adapters or evidence sink. |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Phase aggregate | `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-verification` | Exit 0 | ⚠ PASS proves current hard-disable contracts, not R4/R5 positive behavior |
| Candidate runner | `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-candidate-runner` | Exit 0, no output | ✗ FALSE-GREEN for R4; only one hard-disable case executes |
| Package conformance | `pwsh -NoProfile -File tools/x4-verification/tests/package_conformance_contract.ps1 -Case all` | Exit 0 | ✓ PASS |
| Evidence-chain contract | `pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case evidence-chain` | Exit 0, only hand-authored-ledger rejection marker | ✗ INSUFFICIENT for positive admission |
| Full Lua | `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite all` | Exit 1 at known `telemetry_contract.lua:32` after Phase 05.2 markers | ✗ mandatory repository gate remains red (known baseline) |
| Rust format | `cargo fmt --all -- --check` | Exit 0 | ✓ PASS |
| Rust lint | `cargo clippy --workspace --all-targets --locked -- -D warnings` | Exit 0 | ✓ PASS |
| Rust workspace tests | `cargo test --workspace --locked` | Exit 0 in this verification run | ✓ PASS |
| Source-size | `cargo run -p source-size-lint --locked -- crates tools` | Exit 1: known 231/222/226-line Rust baseline | ✗ mandatory repository gate remains red (known baseline) |

## Private Artifact and Public Runtime Checks

- The three retained local-contract files still exist outside the repository and their SHA-256 values match the Plan 05 summary claims.
- Current locator verification rejects that retained artifact because its build manifest predates the required `native_execution_status` field. The historical digest claim is true; current re-verifiability is false.
- Both durable candidate build manifests still exist and match the committed pending-ledger manifest digests, but they also predate `native_execution_status` and remain `prepared-not-executed`.
- The public runtime diff from executor baseline `c4bc2ace2036eb2e27d2ef6a37671dfcb8b8d77e` through current HEAD is empty for `extensions/live_galaxy/content.xml`, `ui.xml`, production Lua, and Mission Director files.
- `git diff --check` passes for the phase range. No tracked deletion or public candidate control was found.

## Test Quality Audit

| Test | Linked requirement | Active proof | Problem | Verdict |
| --- | --- | --- | --- | --- |
| `admission_contract.ps1` | R1/R2 | Dossier, coverage, hard-disable negatives | No valid exact owner override or trusted runtime admission success case | ⚠ INSUFFICIENT for full R2 |
| `package_conformance_contract.ps1` | R3 | Full real-package and negative graph cases | No material gap found | ✓ PASS |
| `x4_candidate_runner_contract.lua` | R4 | One hard-disable case | Eight behavioral cases are defined then removed from `cases` before return | 🛑 BLOCKER |
| `evidence_retention_contract.ps1` | R5/R6 | Hard-disable and stale-digest rejection | The script exits before the former successful retention/admission/ACL/tamper path; test fixtures are hand-constructed rather than runner output | 🛑 BLOCKER |
| `candidate_build_contract.ps1` | R6 | Matrix/partition/package scaffold | Proves structure only; no runnable adapters, dispatcher, or sink | ⚠ INSUFFICIENT for run readiness |

**Disabled/erased requirement tests:** 8 R4 behavioral cases. **Circular/synthetic evidence:** retention fixtures are produced by the contract helper, not the production runner. **Insufficient assertions:** R2 override success, R4 behavior, R5 positive retention/admission, and R6 run readiness.

## Threat Gates

| Threat set | Safety status | Goal impact |
| --- | --- | --- |
| T-05.2-01–04 admission/override/DoS/disclosure | Mitigated fail-closed | T-02 mitigation removed the required exact owner-override capability. |
| T-05.2-05–08 package spoofing/traversal/DoS/disclosure | Mitigated and behaviorally covered | No goal regression found; R3 remains functional. |
| T-05.2-09–12 candidate tampering/DoS/identity/disclosure | Mitigated by making callbacks unreachable | R4 is no longer implemented or behaviorally tested. |
| T-05.2-13–16 build partition/destination/manifest/DoS | Mitigated for scaffold generation | Builds remain safe but non-runnable. |
| T-05.2-17–21 retention privacy/tampering/identity/DoS/cleanup | Mitigated by blocking retention/admission and removing recursive cleanup | R5 is no longer implemented; R6 cannot complete its future handback chain. |
| T-05.2-SC supply chain | Accepted | No installation or network acquisition occurs. |

`05.2-SECURITY.md` is therefore a valid security disposition but not proof of phase-goal achievement. Security and functionality must both pass.

## Requirements and Decision Coverage

Phase 05.2 owns phase-local R1–R6; `.planning/REQUIREMENTS.md` maps no additional global IDs to this phase, so there are no orphaned milestone requirements. The automated decision-coverage query returned `no trackable decisions`; manual D-01–D-06 coverage is reflected in the requirement and key-link tables above.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | ---: | --- | --- | --- |
| `live_galaxy_candidate_runner.lua` | 331 | Complete implementation overwritten by unconditional failure stub | 🛑 BLOCKER | R4 cannot execute. |
| `x4_candidate_runner_contract.lua` | 439 | Requirement tests removed from the exported case table | 🛑 BLOCKER | Focused and aggregate suites are false-green for R4. |
| `retain-evidence.ps1` | 307 | Unconditional failure before all positive retention logic | 🛑 BLOCKER | R5 cannot retain or verify any artifact. |
| `evidence_retention_contract.ps1` | 245 | Early exit leaves the positive contract as dead code | 🛑 BLOCKER | Aggregate pass proves only rejection. |
| `x4-admission.ps1` | 647, 704 | Exact overrides and every runtime admission are unconditionally disabled | 🛑 BLOCKER | R2 exception and R5 admission link cannot work. |
| `05.1-candidate-run-procedure.md` | 8–18 | Explicit blocking implementation gate | 🛑 BLOCKER | R6 is a future implementation brief, not a prepared runnable handoff. |

No unreferenced `TBD`, `FIXME`, or `XXX` markers were found in the phase-owned implementation/test files.

## Human Verification

N/A for phase-goal closure — this is an infrastructure/foundation phase, and the blocking failures are directly observable in code and executable contracts. The two judgment-tier prohibitions remain prominently flagged for human review, but they cannot convert the failed requirements into a pass.

## Deferred Item Filter

No gap is deferred. Phase 7 owns observed X4 operational proof, but it does not explicitly own restoring the Phase 05.2 local candidate runner, trusted retention/locator chain, exact override authority, or run-ready Phase 05.1 candidate package. Deferring these foundation gaps would let later phases depend on capabilities that do not exist.

## Exact Gap Closure Plans

### Gap Plan A — Trusted authority without deleting R2 behavior

1. Define one authenticated owner-decision envelope and stable verifier-owned trust anchor.
2. Keep default fail-closed behavior and restore one exact valid override path scoped to finding, canonical dossier digest, decision, rationale, remaining risk, and expiry.
3. Add RED/GREEN tests for valid one-finding override plus all existing forged/broad/stale/expired/digest mismatch cases.

### Gap Plan B — Split local contract execution from native runtime authority

1. Export the existing pure deterministic runner as a local-contract-only API.
2. Put X4/native callbacks behind a separate trusted, terminable dispatcher interface; no synchronous `ffi.C` callback may claim isolation.
3. Re-enable all eight erased R4 tests as named active cases and add a test that the native dispatcher remains pending without attestation.
4. Package the seven adapters, cooperative scheduler, verified SHA-256 adapter, bounded private sink, and human trigger into the developer-only roots; keep production package files untouched.

### Gap Plan C — Restore retention and admission as a verified chain

1. Extend the build manifest with verifiable producer/isolation attestation rather than caller-set readiness strings.
2. Allow only attested manifests through build validation, then execute evidence schema, identity, ACL, atomic placement, locator, reread, and sanitization checks.
3. Make admission accept a verified locator/attestation result, never a hand-authored completed ledger; reach `Test-EvidenceChain` through that trusted input.
4. Restore positive tests for retain → verify locator → sanitized handback → independent admission, plus every current negative.
5. Regenerate both durable candidate roots and migrate/regenerate the retained local-contract artifact under the current manifest schema; verify all digests immediately.

## Skill Learning

Skill learning: none — evidence considered included the security hard-disable diff, erased/dead behavioral tests, current focused/full/Rust/source-size gates, public-runtime diff, private retained-artifact digests and failed current locator verification, all six requirements, eight acceptance criteria, eight edges, six prohibitions, five roadmap criteria, plan must-haves, and T-05.2-01 through T-05.2-21 plus T-05.2-SC. The defect is a phase implementation/review failure already governed by existing goal-backward verification, positive-behavior testing, X4 trust-boundary, bounded isolation, private-retention, and skill rules; it does not establish a missing reusable project rule.

## Gaps Summary

Four connected blockers remain. The security repair correctly rejected unauthenticated authority and unbounded native callbacks, but it closed the gaps by removing required positive behavior and then changed tests to assert only the removal. R1 and R3 are real, functional foundations. R2 is incomplete, R4 and R5 are disabled, and R6 is explicitly not runnable. Phase 05.2 must not proceed as passed.

---

_Verified: 2026-08-30T04:04:02Z_
_Verifier: the agent (gsd-verifier)_
