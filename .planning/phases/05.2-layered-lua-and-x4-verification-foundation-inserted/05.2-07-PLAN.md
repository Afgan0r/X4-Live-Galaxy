---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 07
type: execute
wave: 6
depends_on: [05.2-05]
files_modified:
  - tests/x4-candidates/lua/live_galaxy_candidate_runner.lua
  - tests/x4-candidates/lua/phase_051_candidates.lua
  - extensions/live_galaxy/tests/x4_candidate_runner_contract.lua
  - extensions/live_galaxy/tests/x4_candidate_runner_adversarial.lua
  - extensions/live_galaxy/tests/run_contracts.ps1
  - tools/x4-verification/isolation/invoke-candidate-worker.ps1
  - tools/x4-verification/isolation/candidate-worker.ps1
  - tools/x4-verification/contracts/candidate-worker-protocol.v1.json
  - tools/x4-verification/tests/candidate_isolation_contract.ps1
autonomous: true
gap_closure: true
requirements: [R4]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "R4: all eight named runner behaviors execute again with deterministic order, isolation, continuation, bounds, three verdict axes, and valid-unexpected effect mismatch."
    - "Candidate declarations are inert exact-schema data; digesting, verdict evaluation, watchdog, and adapter authority are fixed trusted services outside candidate control."
    - "Potentially blocking work crosses a terminable external process protocol; candidate Lua and the in-process runner never call `ffi.C`."
  artifacts:
    - tests/x4-candidates/lua/live_galaxy_candidate_runner.lua
    - extensions/live_galaxy/tests/x4_candidate_runner_contract.lua
    - tools/x4-verification/isolation/invoke-candidate-worker.ps1
    - tools/x4-verification/contracts/candidate-worker-protocol.v1.json
    - tools/x4-verification/tests/candidate_isolation_contract.ps1
  key_links:
    - "The pure runner resolves declarative `adapter_id` values only through a trusted registry created outside candidate data and evaluates contract/effect verdicts internally."
    - "The local dispatcher in Plan 08 invokes the fixed worker through this parent-owned timeout/kill protocol and accepts only bounded canonical responses."
---

<objective>
Restore the complete R4 runner behavior securely and establish a terminable external isolation boundary for any blocking adapter work.

Purpose: Replace the security hard-disable with positive bounded behavior without returning authority to candidate callbacks or in-process native calls.
Output: Secure pure runner, eight active behavior tests, external worker protocol/launcher, and held-out adversarial tests.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/live-galaxy-code-review/SKILL.md
@.agents/skills/live-galaxy-skill-evolution/SKILL.md
@C:/Users/pavlo/.agents/skills/lua/SKILL.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VERIFICATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY-FIX.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW-FIX.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-03-SUMMARY.md
@tests/x4-candidates/lua/live_galaxy_candidate_runner.lua
@extensions/live_galaxy/tests/x4_candidate_runner_contract.lua
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore all eight pure runner behaviors without callback authority</name>
  <files>tests/x4-candidates/lua/live_galaxy_candidate_runner.lua, tests/x4-candidates/lua/phase_051_candidates.lua, extensions/live_galaxy/tests/x4_candidate_runner_contract.lua</files>
  <behavior>
    - RED/GREEN: preserve and reactivate the eight existing named cases: digest tamper vectors, three-stage JSONL success, exception/malformed/work exhaustion isolation, watchdog continuation, unexpected-effect mismatch, contract/effect failure continuation, identity/bounds/digest rejection, and collapsed/noncanonical verdict rejection.
    - Candidate declarations contain only exact inert fields including `id`, `adapter_id`, expected result, source, and bounds; function/callback/digest/watchdog/verdict/authority fields are rejected.
    - Trusted services are created once outside candidate data; fixed internal contract/effect evaluation derives verdicts from adapter result plus expected result, and a valid unexpected value always yields `effect_verdict=mismatch`.
    - Sorted later candidates continue after a closed failure; a failed stage cannot become pass or invoke later stages for the same candidate.
  </behavior>
  <action>RED: remove neither existing test function nor assertion; first stop deleting the eight cases from the exported table so the current hard-disable fails them. Add exact-schema candidate injection negatives. GREEN: remove the unconditional `runner.run` overwrite and refactor candidate execution so declarations select an `adapter_id` from a trusted registry supplied by the harness/dispatcher, never executable candidate fields. Make digest verification and verdict evaluation fixed runner-owned services; test fakes can implement the trusted interface only at runner construction, not inside candidate data. Preserve canonical JSONL, closed privacy-safe reasons, all hard bounds, deterministic stage/order rules, and the existing hard-disable regression as a renamed rejection of unattested native dispatcher use rather than a blanket pure-runner disable.</action>
  <verify>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-candidate-runner</automated>
  </verify>
  <done>All eight positive/negative behaviors execute and pass; malicious candidate callbacks cannot run, and no test is deleted or filtered from the case table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Terminate blocking adapters at an external process boundary</name>
  <files>tools/x4-verification/isolation/invoke-candidate-worker.ps1, tools/x4-verification/isolation/candidate-worker.ps1, tools/x4-verification/contracts/candidate-worker-protocol.v1.json, tools/x4-verification/tests/candidate_isolation_contract.ps1</files>
  <behavior>
    - RED/GREEN: the parent launches only the fixed repository worker with shell execution disabled, bounded request/response files, hidden window on Windows, fixed protocol/version/adapter allowlist, and no caller executable/path interpolation.
    - A parent deadline terminates the worker and its descendant process tree, returns `worker-timeout`, and leaves no accepted partial response or orphan process.
    - Oversized, partial, malformed, wrong-run/candidate/adapter, duplicate, stale, extra-field, or post-timeout worker output is rejected before runner consumption.
    - The protocol forbids direct/in-process native modes. Windows proves process-tree termination; non-Windows either proves equivalent `Kill(entireProcessTree)` capability or returns `ISOLATION_PLATFORM_UNSUPPORTED` without readiness claims.
  </behavior>
  <action>RED: add black-box success, endless-worker, child-process, malformed-response, identity-swap, and output-bound tests. GREEN: implement a fixed PowerShell worker protocol with exact JSON fields and hard byte/time/process/diagnostic bounds. Use `System.Diagnostics.ProcessStartInfo.ArgumentList`, no shell, and `Process.Kill(true)` after a bounded wait; hide the worker window on Windows. The checked-in worker exposes only deterministic local-contract adapter IDs in this plan. It must not import X4, load saves, use `ffi.C`, accept arbitrary commands/modules, or claim X4 observation. On unsupported platforms return a stable fail-closed capability result rather than emulating isolation.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/candidate_isolation_contract.ps1 -Case all</automated>
  </verify>
  <done>Blocking and descendant work is externally terminable with no accepted partial evidence, and only bounded identity-matched worker responses reach the trusted adapter registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hold out authority injection, direct native binding, and false-green attacks</name>
  <files>extensions/live_galaxy/tests/x4_candidate_runner_adversarial.lua, tools/x4-verification/tests/candidate_isolation_contract.ps1, tests/x4-candidates/lua/live_galaxy_candidate_runner.lua, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <behavior>
    - Held-out cases inject candidate-owned `execute`, `validate`, `assess`, `digest`, `watchdog`, registry, and verdict functions and prove none execute.
    - Direct `ffi`, `ffi.C`, dynamic module/command, unregistered adapter, forged worker-success, and timeout-late-success attempts fail closed.
    - The focused suite must report all eight named behavior markers plus held-out security markers; empty/no-output success is a failure.
  </behavior>
  <action>RED: create an independently loaded adversarial Lua suite and extend the isolation black-box test; assert public runner/worker interfaces only. GREEN: tighten exact candidate/service/protocol allowlists and suite marker accounting. Literal direct-native/import checks are permitted here because the prohibited boundary is an exact package token, but behavior tests remain primary. Never remove positive cases or replace their case table. Register the held-out suite in the final aggregate only after it catches the current authority injection attempt.</action>
  <verify>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-candidate-runner</automated>
  </verify>
  <done>Held-out authority, direct-native, forged-response, timeout-race, and false-green attempts fail while all eight required behaviors remain active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Candidate data to trusted runner | Untrusted declarations may select bounded adapter IDs but cannot supply code, digest, verdict, or watchdog authority. |
| Runner/dispatcher to worker process | Potentially blocking work crosses a bounded, terminable, identity-matched external protocol. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-09 | Tampering | candidate verdict/digest | critical | mitigate | Exact inert schema, fixed trusted registry/evaluator/digest service, held-out callback injection tests. |
| T-05.2-10 | Denial of service | blocking/native work | critical | mitigate | External child boundary, bounded deadline, process-tree kill, reject partial/late output. |
| T-05.2-G07-01 | Elevation of privilege | worker protocol | high | mitigate | Fixed worker and adapter allowlist, no shell/arbitrary path/module/command. |
| T-05.2-G07-02 | Spoofing | worker response | high | mitigate | Run/candidate/adapter/protocol identity and later producer attestation chain. |
| T-05.2-SC | Tampering | package supply chain | low | accept | Built-in PowerShell/Lua/.NET only; no install or network acquisition. |
</threat_model>

<verification>
Focused: run the candidate runner, isolation, and held-out suites and assert nonempty named markers. Full: run `run_contracts.ps1 -Suite x4-verification`, `run_contracts.ps1 -Suite all`, Rust format/strict Clippy/locked workspace tests/source-size. Preserve canonical package conformance, retained-admission rejection, public runtime immutability, and known unrelated baselines.

Apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` in the summary and record the required learning outcome.
</verification>

<success_criteria>
R4 is restored non-vacuously: eight behaviors execute, no candidate controls authority, blocking work is externally terminable, and direct in-process native work remains prohibited.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-07-SUMMARY.md` when done. Close the required skill-learning gate in that summary with `Skill learning: none` plus evidence considered, or one evidence-backed candidate naming its sole owning project skill, minimal proposed rule, and owner-approval status.
</output>
