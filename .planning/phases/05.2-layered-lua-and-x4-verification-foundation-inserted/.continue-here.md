---
context: phase
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
task: 5
total_tasks: 8
status: in_progress
last_updated: 2026-08-30T12:44:22.234Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] Deterministic isolation readiness — the child PID sidecar currently races the 500 ms timeout; a green aggregate cannot replace the three focused failures.
- [ ] Canonical retention conformance — retention must rerun the fixed package-conformance graph verifier rather than trust an embedded claim plus three hashes.
- [ ] Pre-allocation bounds — admission, conformance, build, and retention must reject oversized files before `ReadAllBytes` allocation.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
| --- | --- | --- | --- |
| Aggregate timing luck | The full gate passed once while the focused isolation case failed three times because PID evidence was not published before timeout. | blocking | Add an explicit readiness handshake and require repeated focused timeout coverage before trusting the aggregate. |
| Embedded conformance self-attestation | Retention hashes an embedded conformance claim without rerunning the canonical graph walk required by Plan 09. | blocking | Execute the repository-fixed conformance verifier against the retained build root and attack embedded-only forgeries. |
| Post-allocation size checks | Four readers call `ReadAllBytes` before enforcing their maximum size. | advisory | Inspect bounded file length before allocation and keep post-read equality checks for race defense. |

<current_state>
All Phase 05.2 plans are implemented. The final independent deep code review is CLEAN at commit `8e23f19`. The post-review Nyquist audit is PARTIAL and security is OPEN_THREATS, so phase verification and closure have not started.
</current_state>

<completed_work>

- Closed the original three P1 findings and their held-out gaps.
- Ran three additional adversarial review/fix cycles until the 22-file deep review became CLEAN.
- Exact generated-byte trust covers all 13 paths and canonical JSON variants.
- All seven adapters enforce caller work budgets; producer chronology and repository dispatcher/retention handoff are independently verified.
- Nyquist and security auditors completed on the CLEAN revision.
</completed_work>

<remaining_work>

1. Make child-process readiness deterministic before the isolation timeout test begins.
2. Rerun canonical package conformance during retention.
3. Move four file-size checks before allocation and add bounded regressions.
4. Obtain another CLEAN review and rerun Nyquist/security.
5. Run the phase verifier, then close STATE/ROADMAP and remove one-shot handoff files.
</remaining_work>

<decisions_made>

- Production authority stays `UNCONFIGURED`; no owner ceremony is in scope.
- Local evidence remains `authenticated-local-contract` or `chain-verified-x4-pending`, never `observed-in-X4`.
- Focused repeated behavioral failure outranks a single green aggregate run.
</decisions_made>

<blockers>

- Nyquist: 5/6 covered; R4 isolation descendant termination is escalated after three `Child PID evidence is missing` failures.
- Security: 25/30 closed/documented; T-05.2-15 is the single high blocking threat, with four medium pre-allocation gaps.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — project workflow and trust boundaries.
2. This handoff and `.planning/HANDOFF.json` — exact resume state.
3. `05.2-REVIEW.md` and `05.2-REVIEW-FIX.md` — CLEAN review and last fix evidence.
4. `05.2-VALIDATION.md` — update from the Nyquist verdict recorded here before claiming compliance.
5. `05.2-SECURITY.md` and `05.2-SECURITY-FIX.md` — update from the security verdict recorded here.
6. `05.2-SPEC.md`, `05.2-CONTEXT.md`, plans/summaries `05.2-07` through `05.2-09` — isolation, retention, and closure contracts.
7. `.agents/skills/live-galaxy-code-review/SKILL.md`, `live-galaxy-x4-integration/SKILL.md`, `live-galaxy-x4-tests/SKILL.md`, and `live-galaxy-skill-evolution/SKILL.md` plus every Markdown reference/changelog they contain.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat aggregate PASS as evidence that a repeatedly failing focused isolation test is sound.
- Do not accept embedded conformance metadata as an independent retained-build verification.
- Do not satisfy an input-byte bound only after allocating the complete file.
- Do not access X4, saves, installed mods, private retained evidence, or configure production authority during these local fixes.

## Infrastructure State

- Branch: `codex/0.1-shadow-director`.
- HEAD: `8e23f19d7e4bd2d772b9a018e75c73c787f35b7e`.
- Upstream: `54e0e34dbf8a3fa219806ebf7306653d61a97354`; ahead 101, behind 0.
- Worktree was clean before this handoff.
- No background process, server, watcher, X4 process, or async external job is active.
- Production owner, producer, and locator authority remain unconfigured.
- Known unrelated full-suite baselines remain `telemetry_contract.lua:32` and Rust source-size 231/222/226.

<context>
Resume narrowly from the two blocking gates and four same-class medium bounds. Do not reopen completed package trust or adapter design. After fixes, require independent adversarial review, repeated focused isolation evidence, Nyquist compliance, threats_open: 0, and phase-verifier PASS before closing Phase 05.2.
</context>

<next_action>
Start with: reproduce `candidate_isolation_contract.ps1 -Case all` repeatedly, trace PID sidecar publication, and create a GSD fix plan that also reruns canonical conformance inside retention and moves the four size checks before allocation.
</next_action>
