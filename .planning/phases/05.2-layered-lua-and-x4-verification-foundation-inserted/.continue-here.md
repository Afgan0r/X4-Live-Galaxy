---
context: phase
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
task: 6
total_tasks: 9
status: in_progress
last_updated: 2026-08-30T14:57:00.938Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] Deterministic isolation readiness — prove the descendant is identified and alive before arming the execution timeout; never infer correctness from one aggregate pass.
- [ ] Canonical retained-graph verification — retention must rerun the repository canonical full import-graph verifier against the retained build root; embedded metadata is not independent evidence.
- [ ] Pre-allocation bounds — admission, conformance, build, and retention must reject oversized files before allocation and recheck identity and byte count after reading.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
| --- | --- | --- | --- |
| Aggregate timing luck | One aggregate run passed while three focused timeout runs failed because descendant PID evidence raced the timeout. | blocking | Keep the separate readiness deadline, validate fresh parent-owned sidecars and descendant liveness, then run repeated focused timeout coverage before the aggregate. |
| Embedded conformance self-attestation | Retention previously trusted an embedded conformance object and partial hashes instead of inspecting the live retained graph. | blocking | Rerun the canonical verifier against the exact retained build root and retain the embedded-only forgery regression. |
| Post-allocation size checks | Four readers previously allocated the complete file before enforcing the declared maximum. | blocking | Check metadata length before allocation, then recheck full path, length, timestamp, reparse state, and byte count after reading. |

<current_state>
All ten Phase 05.2 implementation plans are complete. Plan 10 closed the isolation readiness race, retained-graph self-attestation gap, and four pre-allocation byte-bound gaps. Focused and aggregate implementation verification passed. The phase remains open only because the user requested a pause before the required fresh code review, Nyquist audit, security audit, and phase verifier.
</current_state>

<completed_work>

- Planned and independently checked `05.2-10-PLAN.md`.
- Added deterministic descendant readiness before timeout measurement.
- Replaced retained package self-attestation with canonical live graph verification.
- Added pre-allocation and post-read race checks to admission, conformance, build, and retention.
- Updated the single authorized direct dispatcher consumer to the same canonical conformance contract.
- Passed repeated focused isolation runs, all focused package and retention contracts, `x4-verification`, final Rust formatting/lint/tests, parser checks, and diff checks.
- Committed the Plan 10 summary and paused state at `4760a75`.
</completed_work>

<remaining_work>

1. Run a fresh complete Phase 05.2 code review. The interrupted partial reviewer output is invalid and must not be reused.
2. Rerun Nyquist and require full compliance.
3. Rerun security and require `threats_open: 0`.
4. Run the phase verifier and close STATE/ROADMAP only after all previous gates pass.
</remaining_work>

<decisions_made>

- Production authority remains `UNCONFIGURED`; no owner ceremony is in scope.
- Local evidence remains local-only and never claims `observed-in-X4`.
- A focused repeated behavioral failure outranks a single green aggregate result.
- The direct dispatcher update was the only authorized Rule 3 scope deviation because it consumed the changed canonical conformance contract.
</decisions_made>

<blockers>

- None. The remaining work consists of ordered independent closure gates.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — workflow, trust boundaries, Git rules, and required skill chain.
2. This file and `.planning/HANDOFF.json` — exact resume position and invalidated partial-review boundary.
3. `05.2-10-PLAN.md` and `05.2-10-SUMMARY.md` — implemented gaps, commits, tests, baselines, and approved scope deviation.
4. `05.2-REVIEW.md` and `05.2-REVIEW-FIX.md` — prior CLEAN review history; replace with a fresh post-Plan-10 review.
5. `05.2-VALIDATION.md` and `05.2-VERIFICATION.md` — update only from fresh Nyquist and verifier evidence.
6. `05.2-SECURITY.md` and `05.2-SECURITY-FIX.md` — update only from the fresh post-Plan-10 security audit.
7. `.agents/skills/live-galaxy-code-review/SKILL.md`, `live-galaxy-x4-integration/SKILL.md`, `live-galaxy-x4-tests/SKILL.md`, `live-galaxy-rust-conventions/SKILL.md`, `live-galaxy-rust-tests/SKILL.md`, and `live-galaxy-skill-evolution/SKILL.md`, plus every Markdown changelog/reference in those skill directories.

## Critical Anti-Patterns (do NOT repeat these)

- Do not accept one aggregate PASS in place of repeated focused isolation evidence.
- Do not accept embedded conformance metadata as independent retained-build verification.
- Do not enforce an input-byte bound only after allocating the complete file.
- Do not reuse or cite the interrupted partial reviewer output.
- Do not access X4, saves, installed mods, private retained evidence, or production authority during the closure audits.

## Infrastructure State

- Branch: `codex/0.1-shadow-director`.
- HEAD: `4760a756ba471dec30924c1c3b918fce682016da`.
- Upstream: `54e0e34dbf8a3fa219806ebf7306653d61a97354`; ahead 111, behind 0 after refreshed refs.
- No background process, server, watcher, X4 process, or async external job is active.
- Production owner, producer, and locator authority remain unconfigured.
- Known unrelated baselines: `telemetry_contract.lua:32` and Rust source-size 231/222/226.

<context>
Resume narrowly at the closure gates. Implementation is complete and verified; do not reopen package trust, adapter design, Plan 10 code, or production authority unless a fresh gate produces concrete evidence requiring a minimal fix.
</context>

<next_action>
Start with a fresh complete Phase 05.2 code review from `4760a75`; discard the interrupted partial reviewer output. If CLEAN, run Nyquist, security, and the phase verifier in order.
</next_action>
