---
context: phase
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-08-30T15:55:08.9080572+07:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] Coordinated component forgery — a caller can rewrite an adapter and its manifest digest together, execute the substituted bytes, and still publish `PASS`. Executable component identity must be anchored outside the caller-rewritable manifest.
- [ ] Independent adapter oracle — identity and volume adapters still emit hard-coded observations for valid alternate fixtures. Tests must derive expected outcomes independently from bounded inputs.
- [ ] Signed chronology — `completed_at` in the future is accepted when `started_at` is current. Every signed timestamp must be bounded against verification time.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
| --- | --- | --- | --- |
| Self-attested executable identity | Snapshotting closes TOCTOU but does not stop coordinated component plus manifest forgery. | blocking | Bind exact executable component digests into authenticated producer authority and run a black-box substitution probe. |
| Expected-result adapter | Valid alternate fixtures still receive canned observations. | blocking | Compute observations from input and use independent metamorphic fixtures. |
| Relative-only chronology | Internal ordering passed while a signed future completion was accepted. | blocking | Compare all signed timestamps with bounded verifier time and test an unchanged valid signature after time shift. |
| Green aggregate without adversarial coverage | All Phase 05.2 gates pass despite the three reproduced P1 defects. | advisory | Treat `05.2-REVIEW.md` reproductions as mandatory new held-out gates before trusting aggregate status. |

<current_state>
Plans 01-09 are implemented and committed. The first final-review fix set is committed as `aaca3e1`. The independent re-review is `NOT CLEAN`: three P1 blockers and one P2 coverage warning remain. No further fixes were started after the owner's pause instruction.
</current_state>

<completed_work>

- Universal X4 integration dossier, known-failure registry, negative fixtures, and admission contract.
- Canonical package-conformance gate and exact seven-candidate matrix.
- Executable local Lua runner, external terminable worker, developer-only package builder, dispatcher, and JSONL sink.
- Fixed-root delegated authority, exact owner override, producer attestation, durable retention, signed locator, and verified-locator-only admission.
- Plans 01-09, initial review/fix cycles, Nyquist audit, security audit, verifier pass attempt, and final re-review.
- First final-review fix set: shared producer envelope, destination exclusions, immutable snapshots, CNG user-presence enforcement, and dead admission removal.
</completed_work>

<remaining_work>

1. Reproduce and fix the three P1 findings recorded in `05.2-REVIEW.md`.
2. Replace missing held-out coverage with executable black-box tests.
3. Obtain a CLEAN independent code review.
4. Rerun Nyquist, security, and phase verification against the final commit.
5. Update `STATE.md` and `ROADMAP.md` only after verification proves Phase 05.2 complete.
</remaining_work>

<decisions_made>

- Phase 05.2 remains before further Phase 05.1 work because the human X4 probe needs trustworthy local foundations first.
- Production authority stays `UNCONFIGURED`; no owner root or delegated-key ceremony was fabricated.
- Local results may be `authenticated-local-contract` or `chain-verified-x4-pending`, never `observed-in-X4`.
- No X4 launch, installed-game modification, save access, or game mutation belongs to Phase 05.2.
</decisions_made>

<blockers>

- `CR-P1`: coordinated adapter plus manifest digest forgery executes substituted code and returns ready/pass.
- `CR-P1`: identity and volume adapters return hard-coded observations on alternate valid fixtures.
- `CR-P1`: producer validation accepts future `completed_at`.
- `CR-P2`: current held-out suites do not cover those three attacks.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — repository workflow, trust boundaries, and required skills.
2. `05.2-REVIEW.md` — authoritative reproductions and open findings.
3. `05.2-REVIEW-FIX.md` — fixes already attempted and evidence collected.
4. `05.2-SPEC.md` and `05.2-CONTEXT.md` — phase contract and non-goals.
5. `05.2-VALIDATION.md`, `05.2-SECURITY.md`, and `05.2-VERIFICATION.md` — prior gates; some are stale after `aaca3e1`.
6. Plans and summaries `05.2-06` through `05.2-09` — authority, runner, package, retention, and admission design.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat a digest stored beside mutable code as independent authentication.
- Do not let tests provide the same expected value that the adapter returns.
- Do not prove chronology only by comparing signed timestamps to each other.
- Do not infer safety from the currently green aggregate suite; it is known false-green for the open findings.

## Infrastructure State

- Branch: `codex/0.1-shadow-director`.
- Current implementation head before this pause commit: `aaca3e1`.
- Production authority: `UNCONFIGURED`.
- No background X4, server, watcher, or async external job is required for resume.
- Known unrelated baselines: Lua `telemetry_contract.lua:32`; Rust size `231/222/226`.

<context>
The phase has functional breadth but is not complete. Resume narrowly from the three reproduced P1 defects. Do not reopen the whole architecture or continue Phase 05.1 until Phase 05.2 obtains CLEAN review plus security and verifier approval.
</context>

<next_action>
Start with: read `05.2-REVIEW.md`, reproduce the three P1 cases on current HEAD, then create a narrowly scoped GSD fix plan with executable held-out tests.
</next_action>
