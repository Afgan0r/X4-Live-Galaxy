---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 02
subsystem: testing
tags: [powershell, lua, x4, package-conformance, import-graph, fail-closed]

requires:
  - phase: 05.2-01
    provides: Versioned dossier, Known X4 Failure Registry, exhaustive coverage, and independent admission policy
provides:
  - Bounded production-package conformance over registration and the complete internal Lua import graph
  - Stable rejection of local-only, malformed, escaping, cyclic, and over-bound package roots
  - Aggregate package-conformance suite backed by the independent Plan 01 admission command
affects: [05.2-03, 05.2-04, 05.2-05, 05.1-runtime-handback]

actuals:
  tokens: 8001
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns: [case-exact contained package walk, structured local-only classification, digest-bound packaged-static evidence]

key-files:
  created:
    - tools/x4-verification/x4-package-conformance.ps1
    - tools/x4-verification/contracts/package-conformance.v1.json
    - tools/x4-verification/fixtures/package-negative-fixtures.v1.json
    - tools/x4-verification/tests/package_conformance_contract.ps1
  modified:
    - extensions/live_galaxy/tests/run_contracts.ps1

key-decisions:
  - "Production-faithful means the exact content/UI registration, case-exact entrypoint, complete contained internal import graph, and one direct ffi.C acquisition site; standalone module success is never sufficient."
  - "Package conformance emits packaged-static evidence plus dossier, coverage, and graph digests, while Plan 01 remains the independent admission authority."
  - "Intrinsic and registered support modules are explicit external leaves; every other bare or test-only import is local-only evidence and fails AC-04."

patterns-established:
  - "Contained graph walk: normalize logical paths, resolve every segment case-exactly under the explicit package root, and reject escape, cycle, duplicate, depth, file, import, and byte overflow."
  - "Evidence classification: conformant package structure is packaged-static, while permissive bare/test-only or alternate-binding success is local-only and non-admissible."

requirements-completed: [R3]

coverage:
  - id: D1
    description: The real package resolves from UI registration through its complete internal import graph to the production native-binding acquisition site
    requirement: R3
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File tools/x4-verification/tests/package_conformance_contract.ps1 -Case packaged-path"
        status: pass
    human_judgment: false
  - id: D2
    description: Malformed, local-only, escaping, cyclic, and over-bound package roots fail with stable structured classifications and no absolute path leakage
    requirement: R3
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File tools/x4-verification/tests/package_conformance_contract.ps1 -Case all"
        status: pass
    human_judgment: false
  - id: D3
    description: Existing aggregate verification invokes package conformance and the independent Plan 01 admission policy without changing production Lua
    requirement: R3
    verification:
      - kind: integration
        ref: "pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-verification"
        status: pass
    human_judgment: false

duration: 14min
completed: 2026-08-30
status: complete
---

# Phase 05.2 Plan 02: Production-Faithful Package Conformance Summary

**Case-exact package registration now drives a bounded Lua import graph to the production `ffi.C` acquisition site, while bare and test-only success remains explicitly non-admissible local evidence.**

## Performance

- **Duration:** 14 min
- **Started:** 2026-08-29T23:27:03Z
- **Completed:** 2026-08-29T23:41:33Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added a dependency-free PowerShell conformance command that parses `content.xml` and `ui.xml`, follows the registered entrypoint, and walks every contained production Lua import under explicit file, byte, import, depth, cycle, and diagnostic bounds.
- Added ten isolated negative package fixtures covering missing registration, wrong entrypoint, unresolved import, cycle, root escape, alternate binding, graph depth/size, bare import, and test-only loading with stable reason codes and classifications.
- Registered `x4-package-conformance` in the existing contract runner and extended `x4-verification` without changing the Phase 05.1 production Lua, manifest, or UI registration.

## Task Commits

Each TDD gate was committed atomically:

1. **Task 1 RED: packaged path and negative package contract** - `17867ec` (`test`)
2. **Task 1 GREEN: bounded package conformance** - `e52d3ae` (`feat`)
3. **Task 2 RED: aggregate suite registration contract** - `ace45d5` (`test`)
4. **Task 2 GREEN: package-conformance runner registration** - `2388be9` (`test`)

## Files Created/Modified

- `tools/x4-verification/x4-package-conformance.ps1` - Bounded, case-exact package parser, import walker, binding classifier, and digest-producing result command.
- `tools/x4-verification/contracts/package-conformance.v1.json` - Versioned production package identity, external leaves, native binding, admission links, and traversal bounds.
- `tools/x4-verification/fixtures/package-negative-fixtures.v1.json` - Ten virtual package roots with stable expected reason codes and classifications.
- `tools/x4-verification/tests/package_conformance_contract.ps1` - Real-package, negative-fixture, admission, path-leakage, and aggregate-runner contracts.
- `extensions/live_galaxy/tests/run_contracts.ps1` - Focused `x4-package-conformance` dispatch and expanded `x4-verification` aggregate.

## Decisions Made

- The conformance walker treats `ffi` and the registered Mod Support named-pipe module as explicit external leaves. All other imports must resolve through the `live_galaxy/lua/` production prefix.
- A package can claim only `packaged-static`; this phase neither emulates X4 nor upgrades structural evidence to `observed-in-X4`.
- The command validates and binds the relevant dossier and coverage inputs by SHA-256, but the existing Plan 01 command remains the only admission authority.

## Verification

- `package_conformance_contract.ps1 -Case packaged-path`: PASS, including all ten negative package roots.
- `package_conformance_contract.ps1 -Case all`: PASS, including aggregate runner dispatch.
- `run_contracts.ps1 -Suite x4-package-conformance`: PASS.
- `run_contracts.ps1 -Suite x4-verification`: PASS for dossier, known-failure coverage, owner override, and package conformance.
- `run_contracts.ps1 -Suite all`: expected pre-existing failure only at `telemetry_contract.lua:32`; all admission and package-conformance cases passed before the failure.
- `cargo fmt --all -- --check`: PASS.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: PASS.
- `cargo test --workspace --locked`: unrelated known flaky failure in `strategic-state/tests/packet_determinism.rs`; three focused reruns passed.
- `cargo run -p source-size-lint --locked -- crates tools`: expected pre-existing violations at `batch.rs` (231), `runtime_facts_contract.rs` (222), and `named_pipe_contract.rs` (226).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved one-item PowerShell import sets**

- **Found during:** Task 1 GREEN.
- **Issue:** A one-item sorted import pipeline became a scalar under PowerShell and raised an internal `.Count` error.
- **Fix:** Normalized the unique import result to an array before comparing counts.
- **Files modified:** `tools/x4-verification/x4-package-conformance.ps1`.
- **Verification:** `package_conformance_contract.ps1 -Case packaged-path`.
- **Committed in:** `e52d3ae`.

**2. [Rule 1 - Bug] Classified absent XML registration fail-closed**

- **Found during:** Task 1 GREEN.
- **Issue:** Strict property access on an absent `<file>` node raised an internal error instead of `MISSING_REGISTRATION`.
- **Fix:** Switched registration and dependency parsing to explicit bounded DOM node sets and attributes.
- **Files modified:** `tools/x4-verification/x4-package-conformance.ps1`.
- **Verification:** `missing-registration` negative package fixture.
- **Committed in:** `e52d3ae`.

**3. [Rule 1 - Bug] Distinguished the selected binding site from legacy graph references**

- **Found during:** Task 1 GREEN.
- **Issue:** The actual graph contains legacy `_G.C` references alongside the selected direct `ffi.C` component-discovery path, causing an over-broad alternate-source rejection.
- **Fix:** Required one unique direct `require("ffi")` plus `local C = ffi.C` site and classified an alternate source as local-only only when no valid unique production acquisition exists.
- **Files modified:** `tools/x4-verification/x4-package-conformance.ps1`.
- **Verification:** Real package PASS plus `alternate-binding-source` negative package fixture.
- **Committed in:** `e52d3ae`.

**4. [Rule 1 - Bug] Kept the roadmap plan count authoritative**

- **Found during:** Plan metadata update.
- **Issue:** `roadmap.update-plan-progress` counted `05.2-PLAN-CHECK.md` as a sixth executable plan and inserted it into the checklist.
- **Fix:** Restored the locked five-plan count and removed the non-plan review artifact from the execution checklist; also advanced the state activity description to Plan 02.
- **Files modified:** `.planning/ROADMAP.md`, `.planning/STATE.md`.
- **Verification:** Roadmap lists exactly Plans 01 through 05 with 2/5 executed, and State points to completed Plan 02.
- **Committed in:** Plan metadata commit.

---

**Total deviations:** 4 auto-fixed Rule 1 bugs.
**Impact on plan:** All fixes preserve the planned fail-closed package contract and add no product or runtime scope.

## Issues Encountered

- Full Lua verification retains the authoritative pre-existing `telemetry_contract.lua:32` failure.
- Workspace tests retain the known unrelated `packet_determinism` flake; three immediate focused reruns passed.
- The repository-wide source-size gate retains its three authoritative pre-existing Rust violations. No Rust file was modified.

## Known Stubs

None.

## Skill Learning

Skill learning: none — evidence considered included the three PowerShell parser defects, the ten negative package roots, the real packaged graph, all focused and aggregate runs, and the unchanged full-suite baselines. The fixes instantiate existing bounded, fail-closed, production-relative, local-only-evidence, and deterministic-test rules; they do not establish a missing reusable project rule.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 05.2-03 can consume digest-bound packaged-static conformance without treating it as X4 runtime evidence.
- Phase-level closure still requires the existing Lua telemetry, flaky determinism, and source-size gates to be resolved by their owners; Plan 05.2-02 neither weakens nor modifies them.

## Self-Check

PASSED — all six created or modified artifacts exist, all four TDD commits resolve in Git, and the final diff passes `git diff --check`.

---

*Phase: 05.2-layered-lua-and-x4-verification-foundation-inserted*
*Completed: 2026-08-30*
