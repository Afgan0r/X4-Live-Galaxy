---
phase: 05.2-layered-lua-and-x4-verification-foundation-inserted
plan: 06
type: execute
wave: 6
depends_on: [05.2-05]
files_modified:
  - tools/x4-verification/local-attestation.psm1
  - tools/x4-verification/new-owner-override.ps1
  - tools/x4-verification/contracts/owner-root-anchor.v1.json
  - tools/x4-verification/contracts/delegated-purpose-certificate.v1.json
  - tools/x4-verification/contracts/owner-override.v1.json
  - tools/x4-verification/x4-admission.ps1
  - tools/x4-verification/tests/owner_authority_contract.ps1
  - tools/x4-verification/tests/owner_authority_adversarial.ps1
  - tools/x4-verification/tests/fixtures/test-owner-root-fixture.v1.json
  - tools/x4-verification/tests/admission_contract.ps1
autonomous: true
gap_closure: true
requirements: [R2]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "R2: one cryptographically authenticated, exact, scoped owner override can bypass exactly one known-failure disposition."
    - "Caller-authored IDs, public keys, signatures, authority roots, readiness fields, or ACL-only files cannot create owner authority."
    - "Production tooling trusts only the immutable repository-pinned owner-root SPKI fingerprint and root-signed delegated certificates; it cannot generate, enroll, replace, or select any root."
    - "Windows may use an out-of-band-provisioned non-exportable CNG delegated key only after its root-signed purpose/epoch/scope certificate verifies; non-Windows fails with an explicit unsupported-platform verdict."
  artifacts:
    - tools/x4-verification/local-attestation.psm1
    - tools/x4-verification/contracts/owner-root-anchor.v1.json
    - tools/x4-verification/contracts/delegated-purpose-certificate.v1.json
    - tools/x4-verification/new-owner-override.ps1
    - tools/x4-verification/x4-admission.ps1
    - tools/x4-verification/tests/owner_authority_adversarial.ps1
  key_links:
    - "The signing command verifies a root-signed `owner-override` delegated certificate against the repository pin before it asks Windows CNG to sign the canonical payload with the already provisioned delegated key."
    - "Admission loads the immutable repository anchor, validates root signature plus purpose/epoch/scope, verifies the detached payload signature, then runs the existing exact scope/digest/expiry/risk checks."
---

<objective>
Restore the required exact owner-override exception through a genuinely verifiable local authority boundary without reopening fabricated-ID admission (R2).

Purpose: Preserve the specification's explicit owner bypass while keeping T-05.2-01/T-05.2-02 fail-closed against circular bootstrap and caller-authored authority.
Output: Repository-pinned root-anchor contract, root-signed delegated-purpose contract, Windows delegated-key signing/verification, signed override integration, and held-out adversarial tests. Production root provisioning remains a separate owner-controlled process and may be unconfigured during execution.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/live-galaxy-code-review/SKILL.md
@.agents/skills/live-galaxy-skill-evolution/SKILL.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SPEC.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VERIFICATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-SECURITY-FIX.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW-FIX.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-VALIDATION.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-01-SUMMARY.md
@tools/x4-verification/x4-admission.ps1
@tools/x4-verification/tests/admission_contract.ps1
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Pin the out-of-band root and verify delegated purpose authority</name>
  <files>tools/x4-verification/local-attestation.psm1, tools/x4-verification/contracts/owner-root-anchor.v1.json, tools/x4-verification/contracts/delegated-purpose-certificate.v1.json, tools/x4-verification/tests/owner_authority_contract.ps1, tools/x4-verification/tests/fixtures/test-owner-root-fixture.v1.json</files>
  <behavior>
    - RED/GREEN: verification starts from one committed runtime-immutable production owner-root contract at a fixed repository path plus matching compiled pin constants. The contract is either explicitly `unconfigured` with no usable key material, or contains the exact public SPKI/fingerprint supplied through a separate owner-controlled reviewed repository process; no production function or CLI creates, enrolls, replaces, imports, selects, or trusts a root, root key, ACL file, certificate-store entry, or alternate anchor.
    - Each delegated certificate is signed by that pinned root and binds schema, root ID/fingerprint, delegated SPKI digest and Windows key identifier, exact purpose, monotonically accepted epoch, exact scope, algorithm, validity, and policy digest. The repository anchor records the accepted epoch floor/current epoch and scopes for `owner-override`, `candidate-producer`, and `retention-locator`.
    - A missing production root pin, missing real delegated certificate/key, failed Windows user-presence/key access, wrong purpose/scope, epoch rollback, substituted delegated key, or invalid root signature returns a stable fail-closed status; it never creates local authority.
    - Tests use a distinct conspicuously `TEST-ONLY` root/private fixture confined under `tools/x4-verification/tests/fixtures`; only test helpers may consume it. Every production entry point rejects its fingerprint, marker, scope, and any alternate/fresh root even when a caller supplies a path or matching-shape metadata.
    - On non-Windows, production delegated signing returns `OWNER_AUTHORITY_PLATFORM_UNSUPPORTED`; pure verification of fixed signed bytes may run, but Unix mode bits or caller-supplied material never upgrade authority.
  </behavior>
  <action>RED: add production black-box tests proving a first-run fresh root, replaced pin file, caller root/certificate/key path, test-root injection, valid certificate with wrong purpose/scope, older epoch, and substituted delegated key all fail before payload evaluation. Add isolated test-only positive verification using the explicit fixture and fixed pre-signed certificates, while asserting production CLIs reject the same fixture. GREEN: implement `local-attestation.psm1` as a verifier/signer consumer only. Parse the repository-pinned root contract from its fixed repository-relative location, validate its expected schema/ID/fingerprint constants, verify root-signed delegated certificates, enforce exact purpose/epoch/scope/validity, and compare the certificate SPKI digest to the already provisioned Windows CNG delegated key before signing. Expose no root enrollment/import/replacement API and no generic trust-root, test-root, public-key, key-name, or purpose parameter. The owner-controlled root and delegated-key provisioning ceremony stays out of repository production tooling; document an unconfigured status rather than fabricating it. Built-in .NET only.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/owner_authority_contract.ps1 -Case root-delegation</automated>
  </verify>
  <done>The mechanism verifies only repository-rooted delegated authority; production root/key provisioning is neither performed nor claimed, unconfigured machines fail closed, and test authority cannot cross into production entry points.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore one exact authenticated override through admission</name>
  <files>tools/x4-verification/new-owner-override.ps1, tools/x4-verification/contracts/owner-override.v1.json, tools/x4-verification/x4-admission.ps1, tools/x4-verification/tests/admission_contract.ps1</files>
  <behavior>
    - RED/GREEN: a valid signed envelope binds authority purpose, dossier ID/digest, finding ID, decision ID, `accept-risk`, rationale, remaining risk, issued/expiry timestamps, nonce, payload digest, and signature; it overrides only that finding.
    - The existing malformed, broad, stale, expired, digest-mismatched, wrong-decision, missing-risk, and input-mutation negatives remain active.
    - Wrong signer/purpose/nonce, altered rationale/risk/expiry, unverified trust anchor, self-authored key metadata, and a signature copied to another dossier/finding fail before `Test-OwnerOverride` can change disposition.
    - No override can authorize runtime evidence, package readiness, X4 execution, mutation, save access, or more than one registered finding.
  </behavior>
  <action>RED: restore the exact valid-override evaluator test in `admission_contract.ps1` without deleting any security regression. Exercise the positive cryptographic/evaluator core only through the isolated TEST-ONLY helper, and separately prove every production CLI rejects that root and returns `OWNER_ROOT_UNCONFIGURED` or `OWNER_DELEGATED_KEY_UNCONFIGURED` when the real owner-controlled pin/key is absent. GREEN: extend the override schema and create `new-owner-override.ps1` to canonicalize the complete exact payload, require the repository-rooted `owner-override` delegated certificate, confirm its SPKI matches an already provisioned non-exportable CNG key, and sign only after user-presence/key access succeeds. In admission, replace the unconditional disable with root/delegation/payload verification, then call the existing scope/digest/expiry/rationale/risk evaluator. Keep unsigned, unconfigured, TEST-ONLY, and unverifiable inputs non-admissible. Accept no authority/root/public-key/key-name/SID/test-mode/verifier parameter from CLI or JSON. Repeated verification of the same unchanged bounded decision is idempotent; any altered field invalidates its signature. If the production root remains unconfigured, claim only mechanism completion and the exact pending status, never a production override success.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/admission_contract.ps1 -Case admission</automated>
  </verify>
  <done>Exactly one signed owner decision bypasses exactly one matching known-failure finding, while every fabricated or widened authority path remains rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hold out authority-confusion and forgery attacks</name>
  <files>tools/x4-verification/tests/owner_authority_adversarial.ps1, tools/x4-verification/x4-admission.ps1</files>
  <behavior>
    - Held-out black-box cases attempt a fresh self-created root, repository root swap, test-root injection, caller anchor redirection, purpose confusion, epoch rollback, delegated-key substitution, signature/payload transplant, same-shape digest recomputation, stale policy, and junction swaps.
    - A valid signature cannot authorize a second finding, another dossier digest, a later expiry, changed rationale/risk, or any runtime-admission field.
    - No test deletes, filters, or replaces existing positive/negative admission cases.
  </behavior>
  <action>RED: write a separate adversarial script after the focused implementation, invoking only public CLIs and immutable fixture copies; require fresh-root, root-swap, test-root, purpose-confusion, epoch-rollback, and delegated-key-substitution attacks to reach the current incomplete check before GREEN. GREEN: tighten the smallest root/delegation/payload binding. Keep the held-out suite separately registered for the final aggregate so implementation tests cannot silently remove it. Assert stable rejection codes and unchanged signed inputs; never print private key material, machine-local key identifiers, or paths.</action>
  <verify>
    <automated>pwsh -NoProfile -File tools/x4-verification/tests/owner_authority_adversarial.ps1</automated>
  </verify>
  <done>Held-out forgery, confused-deputy, path-swap, and scope-widening attacks all fail without disabling the exact valid override.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Owner decision to admission | Human-authorized risk acceptance crosses from an OS-protected signing key into one finding disposition. |
| Machine-local trust policy to verifier | Public-key and owner identity metadata are trusted only after fixed-root, owner-only, no-reparse verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.2-02 | Spoofing | owner override | critical | mitigate | Repository-pinned root, root-signed purpose/epoch/scope delegation, delegated SPKI-to-CNG match, full canonical payload signature, exact scope. |
| T-05.2-G06-01 | Tampering | bootstrap/root policy | critical | mitigate | No production enrollment/import/root parameter; immutable repository pin plus constant identity/fingerprint checks and held-out fresh-root/root-swap/test-root attacks. |
| T-05.2-G06-02 | Elevation of privilege | generic signer reuse | high | mitigate | Purpose-separated keys/envelopes; owner override cannot authorize producer/runtime fields. |
| T-05.2-G06-03 | Information disclosure | key/diagnostics | high | mitigate | Non-exportable private key and stable codes only; no key bytes or local roots in output. |
| T-05.2-SC | Tampering | package supply chain | low | accept | Built-in PowerShell/.NET CNG only; no install or network acquisition. |
</threat_model>

<verification>
Focused: run root-delegation, admission, and held-out authority suites, including production-unconfigured and production-rejects-test-root cases. Full: run `run_contracts.ps1 -Suite x4-verification`, `run_contracts.ps1 -Suite all`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, and `cargo run -p source-size-lint --locked -- crates tools`. Preserve the known unrelated Lua/source-size baselines exactly; do not weaken closed package, retained-ledger, path, or authority regressions.

Apply `.agents/skills/live-galaxy-skill-evolution/SKILL.md` in the summary and record the required learning outcome.
</verification>

<success_criteria>
R2's exception mechanism is complete and accepts only a repository-rooted, purpose/epoch/scope-certified delegated signature when the separately owner-provisioned Windows key is present; unconfigured production, fabricated IDs, fresh roots, test roots, and self-attested authority remain rejected without claiming enrollment occurred.
</success_criteria>

<output>
Create `.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-06-SUMMARY.md` when done. Close the required skill-learning gate in that summary with `Skill learning: none` plus evidence considered, or one evidence-backed candidate naming its sole owning project skill, minimal proposed rule, and owner-approval status.
</output>
