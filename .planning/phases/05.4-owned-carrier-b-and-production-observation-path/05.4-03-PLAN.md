---
phase: 05.4-owned-carrier-b-and-production-observation-path
plan: "03"
type: execute
wave: 3
depends_on: [05.4-01, 05.4-02]
files_modified:
  - extensions/live_galaxy/lua/live_galaxy_sha256.lua
  - extensions/live_galaxy/lua/live_galaxy_completion_certificate.lua
  - extensions/live_galaxy/tests/completion_certificate_spec.lua
  - tests/fixtures/completion-certificate-v1.json
  - crates/observation-ingest/tests/completion_golden_vectors.rs
  - extensions/live_galaxy/lua/live_galaxy_carrier.lua
  - extensions/live_galaxy/ui.xml
  - extensions/live_galaxy/content.xml
  - extensions/live_galaxy/tests/run_contracts.ps1
  - extensions/live_galaxy/tests/carrier_b_contract.lua
  - extensions/live_galaxy/lua/live_galaxy_observation.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_scheduler.lua
  - extensions/live_galaxy/md/live_galaxy_observation.xml
autonomous: true
requirements: [OBS-01, OBS-08, VAL-06, VAL-01, VAL-02, VER-LG-001, VER-LG-002, VER-LG-003, VER-LG-011, VER-LG-012]
estimate:
  tokens: 94000
  raw_tokens: 94000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The UI-registered Lua wrapper loads the exact owned module and preserves distinct loader/operation failures."
    - "Lua builds complete observation certificates byte-compatible with the independently exercised Rust producer."
    - "One capacity reservation permits one real GetCurRealTime sample; pending retry never repeats the getter or changes bytes."
    - "Retry exhaustion pauses collection; compatible recovery creates fresh collection and rejects stale generations."
    - "Real-time admission uses native monotonic metadata and remains independent of SETA/game urgency and opaque sample values."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_carrier.lua
      provides: "Exact package.loadlib wrapper and typed native results"
    - path: extensions/live_galaxy/lua/live_galaxy_completion_certificate.lua
      provides: "Bounded byte-compatible producer completion"
    - path: extensions/live_galaxy/lua/live_galaxy_observation.lua
      provides: "One-call opaque real-source acceptance producer"
    - path: extensions/live_galaxy/lua/live_galaxy_scheduler.lua
      provides: "Pump/reserve/retry/reconnect admission"
  key_links:
    - from: extensions/live_galaxy/ui.xml
      to: extensions/live_galaxy/lua/live_galaxy_carrier.lua
      via: "Normal extension-relative loader, exact .txt path and initializer"
    - from: extensions/live_galaxy/lua/live_galaxy_observation.lua
      to: extensions/live_galaxy/lua/live_galaxy_completion_certificate.lua
      via: "Exact retained one-record batch produces canonical counts and digests"
    - from: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      to: extensions/live_galaxy/lua/live_galaxy_carrier.lua
      via: "Pump control, reserve, collect and send exact immutable message"
---
<objective>
Implement the production Lua observation/recovery path per D-04/D-05/D-06/D-07/D-08. Use the source-resolved owned loader and one opaque real getter sample; preserve the DLL as transport-only while matching Phase 05.3's exact producer wire contract.
</objective>
<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agent-instructions/x4/MEMORY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-CONTEXT.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RESEARCH.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-PATTERNS.md
@.agents/skills/live-galaxy-code-conventions/SKILL.md
@.agents/skills/live-galaxy-code-conventions/references/logging.md
@.agents/skills/live-galaxy-code-conventions/references/tooling.md
@.agents/skills/live-galaxy-tests/SKILL.md
@.agents/skills/live-galaxy-tests/references/test-standards.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/lua/SKILL.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-01-SUMMARY.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-02-SUMMARY.md
@crates/observation-ingest/src/producer_completion.rs
@crates/observation-ingest/src/batch_canonical.rs
@crates/observation-ingest/src/completion_digest.rs
@extensions/live_galaxy/tests/test_helper.lua
@tools/lua-runner.lock.json
@tools/live-galaxy-tests-1.0-1.rockspec
</context>
<planning_contract>
Invocation: Phase 05.4 --granularity coarse --no-tracer. The owner explicitly approved --no-tracer after the complete-stack tracer conflicted with the five-files-per-task limit. Docs MCP establishes the native loading architecture and Phase 05.3 verifies publication; these plans integrate those proven boundaries horizontally. No global configuration change is required. Research and checked plans still require owner approval before execution.

Every behavior-producing task starts with a failing behavioral assertion, then GREEN, then bounded REFACTOR; missing imports, compile errors and zero discovery are not valid RED. Record command, intended failed assertion and result using the normal GSD TDD evidence gate. All new verification paths below are created by their owning task before use. Focused warmed checks target less than 60 seconds; cold builds are preparation and must be reported separately.

Never weaken workspace lint policy. All Rust files, including tests, remain at most 200 physical lines. The approved exception permits narrowly named helper modules within the current plan-owned crate when required for this physical limit; report that execution deviation, preserve behavior/dependencies, and do not invent generic scaffolding. Read mapped analogs as compatibility evidence, not authority over current conventions.
</planning_contract>

<interface_contract>
Use exact research identities: scope:x4:carrier_b_acceptance; section_key carrier_b_realtime_sample; record_id record:carrier_b_realtime:REVISION; entity_id x4:runtime:realtime_clock; batch_id batch:carrier_b_realtime:REVISION. One record has observation_version 1 and content fields getter=GetCurRealTime, raw_value=exact retained text, semantics=opaque_runtime_number. Protocol contract version 1; schema/policy/canonicalization/digest versions 1/2/3/1. Source incarnation, epoch and revision come from current typed state.
GetCurRealTime has no arguments and returns an opaque numeric sample; absolute epoch/precision and untested settings remain unknown. Scheduling uses Plan 01 native monotonic real milliseconds, not that sample.
</interface_contract>
<bring_up_contract>
Use explicit bring-up defaults: max_data_message_bytes=2048, max_control_message_bytes=512, one occupied data slot, one bounded control/result slot, at most two total send attempts for identical retained bytes, one non-waiting progress step per callback, and availability_check_interval_ms=5000 while callbacks continue. The second attempt is allowed only after proven non-consumption; ambiguous completion never retries. The second failed/non-consumed attempt exhausts the transfer, and availability checks never renew it. Test independent numeric bounds at minus one, exact and plus one, absent/slow/restarting peers, and no getter repetition or extra slot. These are finite testable defaults, not claims of measured production safety; Plans 04-05 own measured freeze.
</bring_up_contract>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Match the Rust completion certificate with bounded Lua producer code</name>
  <files>extensions/live_galaxy/lua/live_galaxy_sha256.lua, extensions/live_galaxy/lua/live_galaxy_completion_certificate.lua, extensions/live_galaxy/tests/completion_certificate_spec.lua, tests/fixtures/completion-certificate-v1.json, crates/observation-ingest/tests/completion_golden_vectors.rs</files>
  <read_first>RESEARCH.md Lua Completion Certificate Compatibility; crates/observation-ingest/src/batch_canonical.rs; crates/observation-ingest/src/completion_digest.rs; crates/observation-ingest/src/producer_completion.rs; extensions/live_galaxy/tests/test_helper.lua. No tracked Lua hashing analog exists; use exact Rust protocol behavior and independent vectors.</read_first>
  <behavior>SHA-256 known empty/short/multiblock vectors pass; Lua and Rust independently match shared expected canonical counts/digests for empty, one-record, optional detail, reversed batch input and non-ASCII content; ordinal gaps, integer overflow and excess work reject before admission.</behavior>
  <action>Implement the research-selected owned binary-safe Lua 5.1 SHA-256 with explicit 32-bit modular arithmetic and bounded input/chunk accounting; return raw 32-byte and lowercase hex forms. Do not invent an unevidenced X4 bit/hash API or move producer policy into the native DLL. The completion module alone owns exact u64 big-endian byte framing, canonical batch domain separator/field order and optional-detail tags, ordinal-contiguous batch sorting, manifest and content hashing, record byte ordering and checked counts. Reproduce decoded_bytes as canonical bytes plus the Rust AMD64 EnvelopeRecord layout charge; obtain that exact charge from the independently generated fixture and carry it explicitly in the versioned producer contract, never infer it from Lua tables. Reject integers outside the exact supported representation instead of rounding. Freeze shared JSON fixtures with independently obtained expected digests; Rust test deserializes inputs and calls real bind_completion_certificate, while Lua tests load the same fixture using existing pinned test-only dkjson 2.11-1 and call only Lua production modules. No new package is needed. Add one-byte mutation and over-limit rejection cases for every framed field. Use direct installed Busted invocation for this task; Task 2 registers it into the normal suite. Bound SHA/canonicalization work and expose measured cost to admission so large bytes cannot monopolize a game callback.</action>
  <verify>
    <automated>cargo test --locked -p observation-ingest --test completion_golden_vectors</automated>
    <fails_when>The real Rust producer disagrees with fixture counts, digests or the AMD64 layout charge.</fails_when>
    <automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/completion_certificate_spec.lua</automated>
    <fails_when>Either language disagrees with independent expected material, a framed-field mutation is invisible, unsupported numbers silently round, or hashing exceeds admitted input/work bounds.</fails_when>
  </verify>
  <done>The Lua producer certificate exactly matches the existing Rust contract without adding schema/hash functions to the transport DLL.</done>
  <fixture_metadata>The shared fixture records rust_target=x86_64-pc-windows-msvc, pointer_width_bits=64, the pinned compiler version, and the measured size_of and align_of EnvelopeRecord values. The Rust test compares its actual target/layout with that metadata before checking decoded_bytes; the Lua test consumes the same explicit record-size charge. Unsupported targets, layout/compiler drift or a missing charge fail the contract instead of silently regenerating expectations.</fixture_metadata>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Register the exact owned native wrapper and strict product loader tests</name>
  <files>extensions/live_galaxy/lua/live_galaxy_carrier.lua, extensions/live_galaxy/ui.xml, extensions/live_galaxy/content.xml, extensions/live_galaxy/tests/run_contracts.ps1, extensions/live_galaxy/tests/carrier_b_contract.lua</files>
  <read_first>extensions/live_galaxy/tests/module_loading_spec.lua and test_helper.lua for normal-loader isolation; extensions/live_galaxy/lua/live_galaxy_component_discovery.lua for protected external-call and typed-failure analog; Plan 01 module contract.</read_first>
  <behavior>Normal require reaches package.loadlib with exact path/symbol and invokes the returned initializer once per Lua generation; open, symbol/init and initializer failure remain distinct; unexpected native calls/results reject; no Carrier A fallback exists.</behavior>
  <action>Load .\extensions\live_galaxy\ui_c_library_live_galaxy_carrier_64.txt through package.loadlib with luaopen_live_galaxy_carrier, then invoke the returned initializer and retain its module API for that Lua generation. Use protected calls at the external boundary; validate result shape, exact handle representation, lengths and typed operation state. Carrier loading uses neither ffi.load nor process-global ffi.C registration; existing X4 getter FFI is a separate seam. Correct ui.xml registration and remove sn_mod_support_apis dependency from content/ui; preserve exact current package identity. Add carrier_b and completion_certificate suites to run_contracts.ps1 and all; missing tooling is an error. Strict fakes replace only loader/native/X4 environment seams, assert exact calls and distinguish each failure, restore package/global state, and never make an unknown API succeed. Native close/reset owns resource teardown; Lua recreates its wrapper on start/load/reload and cannot preserve old generation handles. Record local fake evidence honestly pending actual package tests/X4 conformance.</action>
  <verify>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite carrier_b</automated>
    <fails_when>Exact loader path/symbol or typed failure cases fail, or the carrier suite is not discovered.</fails_when>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite completion_certificate</automated>
    <fails_when>Wrong paths/symbols load, initializer failure becomes success, the strict fake permits unexpected native calls, or the new specs are not discovered.</fails_when>
  </verify>
  <done>The registered owned wrapper and both new test suites run through normal paths with distinct loader/operation failures and no runtime Carrier A dependency.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Collect the real one-call sample through bounded recovery and exact retry</name>
  <files>extensions/live_galaxy/lua/live_galaxy_observation.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/lua/live_galaxy_scheduler.lua, extensions/live_galaxy/md/live_galaxy_observation.xml, extensions/live_galaxy/tests/carrier_b_contract.lua</files>
  <read_first>RESEARCH.md Small Getter Acceptance Seam and exact messages; crates/observation-ingest/src/scheduler.rs and feedback/slot.rs for pump/reserve/exact-retry analog; current runtime/MD entrypoints as historical compatibility only.</read_first>
  <behavior>Full capacity prevents getter/serialization work; one admitted sample calls GetCurRealTime exactly once and encodes exact retained text; retry never changes identity/bytes or repeats the getter. Exhaustion terminates that transfer; 4999/5000/5001 ms availability boundaries do not renew retries; reconnect/load/reload demands fresh samples and rejects old generations.</behavior>
  <action>Implement the research-specified one-record producer: reserve capacity first, call GetCurRealTime with no arguments once, reject non-number/NaN/infinity, retain tostring(value), and reject empty or over-96-byte text before encoding. Preserve the exact opaque content tuple and versioned start/batch/completion messages from research; use Task 1's certificate builder, never fabricated counts/digests. Emit the safe acceptance tuple once for independent readback. This sample performs no enumeration, station/faction discovery or people-capacity query and claims no world completeness. Replace historical runtime pipe/FIFO/save-cooldown flow with the owned wrapper and explicit disconnected/compatible/pending/paused-after-failure/checking/incompatible/degraded states. Pump bounded control before collection, reserve before getter/allocation, retain one immutable pending message and bounded continuation, and retry exact bytes only under the current finite attempt/time budget (D-08). On exhaustion abandon transfer, pause collection and rate-gate availability at five seconds of native monotonic real time while callbacks continue (D-04/D-05); callback gaps and SETA never create catch-up budgets. After compatible reconnect reconcile exact receipts, discard incomplete work, fence old epochs/source authority, then reserve and sample afresh (D-06). Keep incompatible game-facing identity restart-required. Use the researched instantiated 50 ms best-effort MD callback shape with explicit Lua event registration and real-time admission before work; no hard deadline or save-resume guarantee follows. D-07 requires safe bounded transition diagnostics, not per-pulse unbounded logs. Extend strict tests for complete three-message flow, canonical bytes, stale/ambiguous/permanent dispositions, slow/absent peer, both startup orders, callback silence and every source-boundary transition.</action>
  <verify><automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite carrier_b</automated><fails_when>A getter runs without admission, certificate differs from Rust, retry repeats collection, availability renews failed transfers, stale generations advance state, or accelerated game time increases real-time allowance.</fails_when></verify>
  <done>The actual Lua producer/runtime passes deterministic admission, immutable-message, recovery and source-fence cases; real getter and SETA acceptance remain Plan 05.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Contract |
| --- | --- |
| Lua to owned native module | Checked arguments, synchronous copy, no retained Lua state and no unwind |
| Native local IPC to bridge | Local peer access, explicit compatibility/epoch, complete bounded messages |
| Bridge to SQLite/current authority | Existing lifecycle, durable receipts and reconciliation |
| Product to diagnostic/package output | Safe bounded metadata and exact artifact identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.4-MEM | Tampering / DoS | Native buffers and lifetime | high | mitigate | Plan 01 bounds validation/copy, generation fences, panic containment and final completion ownership; Plan 05 verifies actual reload/load |
| T-05.4-PEER | Spoofing / Elevation | Loader and pipe | high | mitigate | Exact owned path/export; same-user local OS access and remote-client refusal; no mutation operation; Plans 01-04 negative tests |
| T-05.4-REPLAY | Tampering / Repudiation | Publication/retry | high | mitigate | Plans 02-03 preserve immutable identity/bytes, finite attempts, fresh baseline and existing exact receipt reconciliation |
| T-05.4-LOG | Information disclosure | Console/journal/evidence | medium | mitigate | Plans 02/04/05 test startup writes, bounded degraded reporting, sanitized correlation and private retained artifacts |
| T-05.4-SC | Tampering | Dependency preparation | high | mitigate | Research audits windows-sys 0.61.2 OK; no recvmsg added to production; any new package/tool preparation requires source pin and legitimacy gate |
</threat_model>
<risk_gates>
Affected layers: rust-domain and x4-runtime; no llm changes.
Selected: mandatory research, plan-check, code review and verification; security review for native memory, local peer/control authority, persistence and diagnostics; X4 probe and sustained SETA for actual package/getter/lifecycle/callback behavior; targeted mutation where changed pure safety invariants have useful operators.
Not selected: AI specification/eval (no model contract changes); custom UI design (console only); broad compatibility or AFK/model/report soak (later scope). Nyquist remains disabled unless the requirement-to-test audit exposes a concrete gap; missing scenarios must still be repaired. Native OS effects and MD are outside mutation scoring; never invent a mutation engine.
</risk_gates>
<verification>
Run the carrier_b, completion_certificate and syntax suites; cargo test --locked -p observation-ingest --test completion_golden_vectors; cargo fmt --all --check; cargo run --locked -p source-size-lint -- crates; cargo clippy --locked --workspace --all-targets --all-features -- -D warnings. The current XML guard still asserts the historical Carrier A dependency, so Plan 04 explicitly owns that guard's migration, negative package fixtures and final full suite. Record that pending integration check honestly; do not edit files outside this plan or report the old XML expectation as passing.
</verification>
<success_criteria>
Lua loads only the exact owned carrier, generates byte-compatible complete sections from the one safe real getter seam, and implements finite pump/reserve/retry/reconnect behavior without mutation, discovery expansion, automatic save recovery or clock-semantic invention.
</success_criteria>
<output>
Create .planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-03-SUMMARY.md with exact loader/wire/certificate contracts, golden-vector results, callback/clock ownership, getter tuple and pending runtime limits.
</output>
