---
phase: 05.4-owned-carrier-b-and-production-observation-path
plan: "03"
type: execute
wave: 3
depends_on: [05.4-01, 05.4-02]
files_modified:
  - crates/x4-carrier-native/Cargo.toml
  - Cargo.lock
  - crates/x4-carrier-native/src/lib.rs
  - crates/x4-carrier-native/src/producer.rs
  - crates/x4-carrier-native/tests/producer_contract.rs
  - crates/x4-carrier-native/src/lua_operations.rs
  - crates/x4-carrier-native/src/abi.rs
  - crates/x4-carrier-native/src/abi_windows.rs
  - crates/x4-carrier-native/tests/abi_contract.rs
  - extensions/live_galaxy/lua/live_galaxy_carrier.lua
  - extensions/live_galaxy/lua/live_galaxy_observation.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_scheduler.lua
  - extensions/live_galaxy/tests/carrier_b_contract.lua
autonomous: true
requirements: [OBS-01, OBS-08, VAL-06, VAL-01, VAL-02, VER-LG-001, VER-LG-002, VER-LG-003, VER-LG-011, VER-LG-012]
estimate:
  tokens: 94000
  raw_tokens: 94000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The normal-loader-tested thin Lua wrapper loads the exact owned module and preserves distinct loader/operation failures; Plan 04 wires packaged registration."
    - "The DLL owns typed producer assembly, canonical hashing/certificates and immutable retry; the bridge independently recomputes admission."
    - "One capacity reservation permits one real GetCurRealTime sample; pending retry never repeats the getter or changes bytes."
    - "Retry exhaustion pauses collection; compatible recovery creates fresh collection and rejects stale generations."
    - "Real-time admission uses native monotonic metadata and remains independent of SETA/game urgency and opaque sample values."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_carrier.lua
      provides: "Exact package.loadlib wrapper and typed native results"
    - path: crates/x4-carrier-native/src/producer.rs
      provides: "Bounded DLL-owned typed production and immutable retry"
    - path: extensions/live_galaxy/lua/live_galaxy_observation.lua
      provides: "One-call opaque real-source acceptance producer"
    - path: extensions/live_galaxy/lua/live_galaxy_scheduler.lua
      provides: "Pump/reserve/retry/reconnect admission"
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_carrier.lua
      to: crates/x4-carrier-native/src/lua_operations.rs
      via: "Normal loader, exact .txt path/initializer and typed ABI 2"
    - from: extensions/live_galaxy/lua/live_galaxy_observation.lua
      to: crates/x4-carrier-native/src/producer.rs
      via: "Typed source facts enter DLL-owned canonical batching and certificate construction"
    - from: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      to: extensions/live_galaxy/lua/live_galaxy_carrier.lua
      via: "Progress/control, reserve and submit typed source facts without Lua wire ownership"
---
<objective>
Implement the DLL-owned Rust producer and thin game-callback Lua adapter per D-04/D-05/D-06/D-08/D-12/D-13. Replace the superseded Lua SHA/certificate plan without creating its proposed files. Preserve the already implemented native transport worker and lifecycle isolation; expose only bounded typed producer operations.
</objective>
<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agent-instructions/x4/MEMORY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-CONTEXT.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RESEARCH.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-PATTERNS.md
@.agents/skills/live-galaxy-code-conventions/SKILL.md
@.agents/skills/live-galaxy-code-conventions/references/logging.md
@.agents/skills/live-galaxy-code-conventions/references/tooling.md
@.agents/skills/live-galaxy-tests/SKILL.md
@.agents/skills/live-galaxy-tests/references/test-standards.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/lua/SKILL.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-01-SUMMARY.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-02-SUMMARY.md
@crates/observation-ingest/src/producer_completion.rs
@crates/observation-ingest/src/batch_canonical.rs
@crates/observation-ingest/src/completion_digest.rs
@extensions/live_galaxy/tests/test_helper.lua
@tools/lua-runner.lock.json
@tools/live-galaxy-tests-1.0-1.rockspec
</context>
<planning_contract>
Remaining work only, at d604c97bfe745a8bd65c207eb3ae03f9cac29664 plus the owner-approved D-12/D-13 research and reconciled architecture. Preserve completed 05.4-01 and partial 05.4-02. The inherited owner-approved --no-tracer decomposition remains in force.

The owner approved exactly one exception to the five-files-per-task cap: 02 Task 1 may perform the cohesive atomic D-13 contract migration across direct consumers so GREEN remains compile-preserving. This is not permission to broaden other tasks. All other tasks name at most five files. The separate existing repository permission for narrowly named same-crate helper modules needed solely for Rust's 200-line limit remains applicable; record any such mechanical execution deviation and do not use it to add responsibilities.

Every behavior-producing task records an intended failing assertion before GREEN, using the GSD RED evidence gate; compile/link errors and empty discovery are not RED. Create a new test target with its owning task before invoking it. Keep focused warmed checks under 60 seconds where practical; report cold builds and deliberate integration windows separately. No new registry dependency/install is planned: reuse locked workspace libraries and explicitly prepared local tools.

Plans 02, 03 and 04 are automated implementation/qualification. Plan 05 starts with the user-only X4 checkpoint. After 04 is reviewed and verified, present exact ready procedure/package/config identity and STOP before installation, X4 launch, game controls or SETA; do not mark 05 or phase acceptance complete.
</planning_contract>

<interface_contract>
Native PE export remains solely luaopen_live_galaxy_carrier. Freeze the returned table to exactly: abi_version, open, begin_section, push_record, finish_section, fail_section, progress, poll_control, reset, close. ABI is 2. No Lua-facing raw byte-send or wire/hash helper is exposed.

Lua call layouts:
- abi_version() returns integer 2.
- open(expected_abi, limits, source) returns code plus opaque token. limits is a bounded plain table using the exact Plan 02 config projection for data/control bytes, record/content/canonical bytes, batches/work/age, one pending slot, attempts/retry age and availability interval; source contains source_scope, source_epoch_status, source_boundary. No metatables, functions, pointers or extra keys.
- begin_section(token, evidence) reserves one section and its next fact before getter work; evidence names the bridge-issued section intent, expected_records, capture start with explicit clock meaning, stated quality/availability/coverage, consistency and stable_identity. The DLL assigns protocol incarnation/epoch/revision/message/batch identities. Caller cannot assign them.
- push_record(token, fact) accepts a plain typed fact table with entity_id, observation_version, getter, raw_value and semantics. The admitted point example uses exactly getter=GetCurRealTime and semantics=opaque_runtime_number; raw_value is the retained finite number's text, at most 96 UTF-8 bytes. No wire bytes, digests, ordinals or certificates.
- finish_section(token, evidence) supplies capture end and explicit collection success/final evidence. Any evidence strengthening, count mismatch or failed source aborts without completion.
- fail_section(token, reason) accepts a closed safe source-failure reason.
- progress(token, work_units) advances at most the admitted finite work, performs bounded transport progress and returns code plus bounded metadata: producer_state, connection_state, monotonic_millis (or explicit clock_unavailable), capacity and safe correlation.
- poll_control(token) returns at most one validated typed control result; Rust decodes/applies protocol disposition and Lua consumes only bounded game-callback control/status. Neither code path executes game commands.
- reset(token, reason) fences/abandons incomplete work and requests bounded transport cancellation; close(token) releases logical ownership idempotently. Neither waits/drains in Lua.

Return codes: 0 accepted, 1 progress/local_handoff, 2 capacity_unavailable, 3 no_control, 4 received, 5 committed, 6 permanently_rejected, 7 ambiguous, 8 paused_after_failure, 9 disconnected, 10 restart_required; errors retain -10 wrong_abi, -12 data_limit, -13 control_limit, -16 stale_token, -17 generation_exhausted, -18 unavailable and -20 invalid_input. Add -21 invalid_transition and -22 clock_unavailable. Code 1 never means receipt/durability. Freeze these exact values, arities and metadata fields in executable ABI tests; wrong versions/extra fields reject before state changes. Tokens and wider counters use bounded decimal strings, never lossy Lua-number identities. Resource integers must be finite integral exact values within the supported conversion range; validate before copying.

The acceptance section is scope:x4:carrier_b_acceptance / carrier_b_realtime_sample, entity x4:runtime:realtime_clock, observation_version 1. Native construction owns record and batch ID namespaces and canonical content tuple order getter, raw_value, semantics. Coverage is PointMeasurement with unknown source-epoch/consistency; it confers no membership or deletion authority. Capture evidence must retain its actual clock meaning and cannot relabel the opaque sample as an epoch or scheduler clock. Native monotonic metadata controls real-time admission. The semantic version vector remains 1/2/3/1 and envelope/control contracts are 2.
</interface_contract>
<bring_up_contract>
Use initial local-only limits 2048 complete-message bytes, 512 control bytes, one pending data slot, one ordinary control/result slot plus the separate terminal-state visibility retained by 01, two total attempts only on proven non-consumption, one bounded callback progress step and 5000 ms availability checks while callbacks continue. They are bring-up inputs, not earned production limits; 04 measures all actual retained copies, work/callback costs and timeouts, then 05 qualifies ordinary/SETA runtime behavior. No decoded layout estimate is introduced. No callback waits or background Lua/X4 calls.
</bring_up_contract>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement bounded native producer assembly and immutable retry</name>
  <files>crates/x4-carrier-native/Cargo.toml, Cargo.lock, crates/x4-carrier-native/src/lib.rs, crates/x4-carrier-native/src/producer.rs, crates/x4-carrier-native/tests/producer_contract.rs</files>
  <read_first>02 SUMMARY and strict wire/control exports; CanonicalBatch, bind_completion_certificate, StopAndWaitSlot; native TransportConfig, NativeTransport and 01 SUMMARY worker/module-lease ownership.</read_first>
  <behavior>Typed source facts produce exact start/batch/completion bytes and independently expected canonical/digest material. Limits reject before retained growth; partial/failure never emits completion. Exact retry preserves native-owned identity/bytes after caller mutation, while terminal/reconnect/load boundaries discard incomplete work and create fresh identity.</behavior>
  <action>Per D-12/D-13 add a cohesive safe Rust Producer state machine inside the existing native crate, consuming only owned typed values, explicit source evidence, injected monotonic time and validated finite limits. Add only workspace path dependencies on observation-domain/observation-ingest and update their existing lockfile dependency edges; no registry install or new crate. Reuse shared pure canonicalization, encoder/control codec and completion binding from 02; receiver remains independent and the DLL acquires no application/persistence/SQLite/strategy dependency.

Implement states awaiting compatibility, ready, section reserved, collecting, pending start/batch/completion, paused-after-failure, incompatible and closed, with precise typed outcomes. Producer owns all protocol IDs, sequence/ordinals, canonical batching, certificate and one immutable unacknowledged message; section source scope/coverage/capture/stability always originate in validated Lua evidence. Preserve at most one retained section and finite total records, canonical bytes, batches, work and age for this acceptance path, with complete semantic records and separate message/aggregate limits. Capacity reservation includes the next fact and terminal/control headroom before collection; caller mutation after admission cannot alter retained records.

Use exactly the completed transport worker and its ownership contracts, not another worker. Producer assembly/progress is cooperatively bounded on the calling thread; no returned work captures Lua/X4 pointers or invokes Lua/X4 APIs. Work-unit charging covers copy, encoding and hashing by bounded record/byte increments; finish_section only signals closure and cannot run unbounded hashing. Bound any indivisible helper by the admitted finite input, and measure its maximum in 04. No internal unbounded loop drains a section in one callback.

Apply exact received/terminal feedback to release pending bytes; a local native handoff never releases them. Two total attempts are bring-up input only, the second requires proven non-consumption, and aggregate retry age is finite. Missing response or unknown completion is ambiguous; do not resend automatically. Exhaustion pauses collection and uses a 5000 ms availability gate while callbacks continue per D-04/D-05/D-08. Compatibility/reconciliation must precede fresh collection after reconnect; start/load/reload cannot inherit incomplete state. Test 4999/5000/5001 ms, frozen/jumping clocks, stale token/epoch, duplicate/conflicting disposition, capacity, count and byte edges, all failure cuts, and no source-evidence strengthening. Golden expected bytes/digests are fixed independently; tampered claims must also fail through the actual receiver functions.</action>
  <verify><automated>cargo test --locked -p x4-carrier-native --test producer_contract</automated><fails_when>Cargo exits nonzero or discovers no tests, independent byte/digest expectations disagree, admitted growth or work exceeds bounds, retries alter identity/bytes or repeat without proven non-consumption, or failed/source-strengthened input emits completion.</fails_when></verify>
  <done>The safe DLL producer owns the complete bounded protocol lifecycle and exact retry material, using the retained transport worker and independent receiver validation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expose the exact typed ABI and preserve native lifetime isolation</name>
  <files>crates/x4-carrier-native/src/lib.rs, crates/x4-carrier-native/src/lua_operations.rs, crates/x4-carrier-native/src/abi.rs, crates/x4-carrier-native/src/abi_windows.rs, crates/x4-carrier-native/tests/abi_contract.rs</files>
  <read_first>Current seven-operation registrations, Lua symbol resolution, argument copying, native finalizer and 01 SUMMARY; interface_contract above.</read_first>
  <behavior>The real initializer registers exactly the ten ABI-2 members; ABI-1 callers and old raw-send input reject before producer changes. Malformed tables/types, NaN/fractional/out-of-range values, extra fields, oversize content, stale tokens and injected panics preserve state. Accepted values survive caller changes; close/reset never wait or leak into replacement generation.</behavior>
  <action>Wire the safe producer into the existing Lua C module per D-01/D-12. Bump ABI_VERSION and actual registrations together, preserving sole PE export and the approved worker/module lease. Use the exact interface_contract and add only required Lua 5.1 host symbols to the existing resolver. Decode plain tables through raw field operations with exact keys/counts and type checks, never invoke metatable behavior or coerce arbitrary values. Reject bounded lengths and counts before allocation/copy; the current bytes helper copies before a size check and must not be used that way for the expanded boundary. Validate token/producer state/capacity before retaining an input; copy accepted strings synchronously. Check integral/finiteness/range before native conversions.

Keep unsafe only in this reviewed ABI/Win32 boundary, every C operation panic-contained, and the unwinding profile intact. Pure producer/domain code stays safe. Retain no Lua state, callback, table, foreign buffer or X4 handle in global/worker-owned state. Bind producer state to the exact logical handle generation; cancellation owns pending I/O storage until terminal OS completion while producer work becomes inaccessible immediately. Polling applies only typed collection/control results; no game mutation authority is added. Test real operation entrypoints using the existing ABI harness, not only ModuleContract metadata. A terminal transport state remains visible independently of the ordinary result slot per 01 SUMMARY. Add focused producer-aware pending-I/O and stale-generation cases to the ABI test target; actual Lua-state teardown/image unload remains mandatory in 04.</action>
  <verify><automated>cargo test --locked -p x4-carrier-native --test abi_contract --test producer_contract --test lifecycle_contract</automated><fails_when>Cargo exits nonzero, a named target discovers no tests, actual ABI registrations or return shapes differ, invalid/oversize/stale arguments change producer state, panic/copy isolation fails, or pending-I/O completion ownership and generation teardown assertions fail.</fails_when></verify>
  <done>ABI 2 has one exact typed producer surface, synchronous bounded copying, contained failures and generation-local teardown without changing the approved completion worker.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Supply one real X4 fact through a thin callback-owned Lua adapter</name>
  <files>extensions/live_galaxy/lua/live_galaxy_carrier.lua, extensions/live_galaxy/lua/live_galaxy_observation.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/lua/live_galaxy_scheduler.lua, extensions/live_galaxy/tests/carrier_b_contract.lua</files>
  <read_first>Current module_loading_spec.lua/test_helper.lua; runtime/observation/scheduler modules as historical integration evidence; exact ABI contract above. 04 owns XML/MD registration and suite runner migration.</read_first>
  <behavior>Normal extension-relative require invokes exact package.loadlib path/initializer and validates ABI 2. Pump/control/reservation precede a single GetCurRealTime call; pending retry causes zero extra getters. Callback gaps and SETA never multiply real-time allowance. Loader, initializer, invalid fact, source failure and stale/reconnect paths are distinct and fail closed.</behavior>
  <action>Replace the historical Carrier A runtime and Lua FIFO with the typed producer wrapper per D-02/D-04/D-05/D-06/D-07/D-08/D-12. Load .\\extensions\\live_galaxy\\ui_c_library_live_galaxy_carrier_64.txt with luaopen_live_galaxy_carrier, invoke the returned initializer once per Lua generation, and require the exact ABI-2 table. Preserve separate open/symbol/initializer/operation failures. This carrier loader is package.loadlib; existing getter ffi.C is a separate X4 seam.

Lua retains only X4-specific continuation and callback admission. First progress/poll existing producer/control state, require receiver intent and capacity, begin/reserve the section, then invoke GetCurRealTime() exactly once. Reject non-number/NaN/infinity and empty/over-96-byte tostring output; pass its exact text as typed raw_value with the fixed getter/semantics tuple. Obtain capture metadata from source-supported callback context with honest clock labels; do not treat this opaque sample as Unix/game/scheduler time. Supply explicit failure or finish evidence, leaving all IDs, canonical bytes, batching, hashes/certificates and retry material in the DLL. No observation enumeration or membership claim is added.

Keep callback registration idempotent and guard against reentry with cleanup on error. Lua gives at most one admitted work step per callback; native monotonic real milliseconds govern reservation and the conditional 5000 ms availability check. Clock-unavailable pauses work with a reason. No catch-up after callback silence, no polling spin, no save flag/cooldown/resume rule. Load/reload constructs a new wrapper and explicit source boundary; compatible external restart reconciles then recollects, while loaded DLL/Lua or incompatible contract changes require X4 restart. Detailed bounded transition diagnostics follow LOG-01 through LOG-05; no per-frame unbounded trace.

Run real Lua modules through normal require with strict fakes only for X4/native external seams; assert exact ABI field shapes and getter call count. Exercise startup order, unavailable/slow peer, failed transfer, ambiguous/permanent response, reset/load/reload, old token and source evidence preservation. These tests establish local adapter behavior; 04 migrates actual registration and uses the real DLL, then 05 earns X4 evidence.</action>
  <verify><automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/carrier_b_contract.lua</automated><fails_when>Busted exits nonzero or reports zero tests, normal loading uses the wrong path/ABI shape, a getter runs without reservation or repeats for retry, callback gaps/SETA multiply work allowance, or failure/reconnect/source-evidence assertions fail.</fails_when></verify>
  <done>The tested Lua adapter supplies bounded typed facts and source evidence only; DLL producer owns the protocol/retry path and all game API calls remain in admitted callbacks.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Contract |
| --- | --- |
| Lua to owned native module | Checked arguments, synchronous copy, no retained Lua state and no unwind |
| Native local IPC to bridge | Local peer access, explicit compatibility/epoch, complete bounded messages |
| Bridge to SQLite/current authority | Existing lifecycle, durable receipts and reconciliation |
| Product to diagnostic/package output | Safe bounded metadata and exact artifact identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.4-MEM | Tampering / DoS | Native buffers and lifetime | high | mitigate | Plan 01 bounds validation/copy, generation fences, panic containment and final completion ownership; Plan 05 verifies actual reload/load |
| T-05.4-PEER | Spoofing / Elevation | Loader and pipe | high | mitigate | Exact owned path/export; same-user local OS access and remote-client refusal; no mutation operation; Plans 01-04 negative tests |
| T-05.4-REPLAY | Tampering / Repudiation | Publication/retry | high | mitigate | Plans 02-03 preserve immutable identity/bytes, finite attempts, fresh baseline and existing exact receipt reconciliation |
| T-05.4-LOG | Information disclosure | Console/journal/evidence | medium | mitigate | Plans 02/04/05 test startup writes, bounded degraded reporting, sanitized correlation and private retained artifacts |
| T-05.4-SC | Tampering | Dependency preparation | high | mitigate | Research audits windows-sys 0.61.2 OK; no recvmsg added to production; any new package/tool preparation requires source pin and legitimacy gate |
</threat_model>
<risk_gates>
Layers: rust-domain and x4-runtime. Research, plan-check, code review and verification are mandatory. Select focused security review for ABI ownership, local peer/control authority, version/source fences, persistence and diagnostics; targeted mutation for changed deterministic bounds/version/digest/epoch/retry predicates; actual local module integration followed by user-operated X4 and sustained SETA for the unresolved runtime behavior. No AI specification/eval or custom design gate: no llm or custom UI change. No broad mod compatibility, heavy ship/station, report or milestone-wide AFK soak. Nyquist is disabled; missing scenarios are repaired in their owning tests, not by introducing a separate validation platform.
</risk_gates>
<verification>
Run each task's focused Rust or strict normal-loader Lua behavioral target. Independently fixed wire/digest expectations and actual receiver rejection prove the producer contract; real ABI entrypoint tests prove copying, argument validation and lifetime fences. Plan 04 must then prove actual Lua/DLL/bridge/SQLite and registered packaging; fake native seams are not product integration evidence.
</verification>
<success_criteria>
DLL-owned producer bytes/certificates/retries are bounded and independently verified. The exact ABI-2 adapter passes typed facts/source evidence only, invokes the getter once after capacity reservation and pauses safely across failure/restart. All Lua/X4 calls stay in admitted callbacks.
</success_criteria>
<output>
Create .planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-03-SUMMARY.md recording exact ABI/wire versions, producer ownership, RED/GREEN evidence, measured-versus-unmeasured bounds and remaining package/local/X4 gates.
</output>
