# Phase 05.4 Post-Closure CI Correction

## Failure

GitHub Actions run `34563673530` failed the mandatory command:

```text
cargo run -p source-size-lint --locked -- crates tools
```

It reported:

- `crates/observation-persistence/src/sqlite_read.rs`: 201 lines;
- `crates/x4-bridge/src/production.rs`: 205 lines.

Both files changed during Phase 05.4. The prior verification disposition that
treated these violations as acceptable pre-WR-03 debt was incorrect.

## Correction

Commit `d2e67c3` removed repeated result-type spelling in `sqlite_read.rs` and
imported already-used domain/application types in `production.rs`. The change
does not alter control flow, protocol behavior, persistence semantics, or the
game-facing contract.

The resulting files are 200 and 199 lines respectively.

## Verification

The following passed locally:

- Rust formatting;
- workspace Clippy with warnings denied;
- the full locked workspace test suite;
- the complete isolated X4 contract suite;
- the package self-test;
- the exact source-size command used by CI.

GitHub Actions run `34564923033` then passed every deterministic gate, including
the source-size check.

Focused review of `82182de..d2e67c3` is CLEAN. No new targeted behavior test is
needed beyond the direct source-size red/green reproduction and the full
regression because the correction is mechanical and changes no branch or data
contract.

## Runtime Evidence Applicability

The current bridge executable does not have the same whole-file digest as the
accepted Attempt 13 bridge because compiler/PDB read-only metadata changed. A
controlled accepted-source rebuild and corrected-source rebuild produced
byte-identical `.text`, `.data`, `.pdata`, and `.reloc` PE sections; only
`.rdata` differed. The shipped native DLL hash remains
`7aea7def83d9066db3c29504cec441e571d888760e8e8f1ce68c671c1602a487`, and
Lua, XML, limits, and all game-facing contracts are unchanged.

Therefore the accepted disposable-X4 runtime evidence remains applicable and
no X4 test cell requires repetition.
