---
phase: 05.4-owned-carrier-b-and-production-observation-path
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/x4-carrier-native/Cargo.toml
  - crates/x4-carrier-native/src/lib.rs
  - crates/x4-carrier-native/src/abi.rs
  - crates/x4-carrier-native/src/types.rs
  - crates/x4-carrier-native/src/handle.rs
  - crates/x4-carrier-native/src/transport.rs
  - crates/x4-carrier-native/src/abi_windows.rs
  - crates/x4-carrier-native/tests/abi_contract.rs
  - crates/x4-carrier-native/tests/lifecycle_contract.rs
autonomous: true
requirements: [OBS-01, OBS-08, VAL-06]
estimate:
  tokens: 86000
  raw_tokens: 86000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The owned AMD64 native module has one Lua initializer export and no game-state mutation operation."
    - "Accepted bytes are copied synchronously; Lua state, borrowed buffers and callbacks never escape a call."
    - "Unavailable peers and occupied capacity produce bounded typed outcomes without waiting for peer progress."
    - "Reset, stale handles and cancellation cannot free pending I/O storage or affect a replacement generation."
  artifacts:
    - path: crates/x4-carrier-native/src/abi.rs
      provides: "Audited Lua C ABI and owned-copy boundary"
    - path: crates/x4-carrier-native/src/transport.rs
      provides: "Bounded local Windows transport and lifetime state"
    - path: crates/x4-carrier-native/tests/lifecycle_contract.rs
      provides: "Actual local IPC and cancellation evidence"
  key_links:
    - from: crates/x4-carrier-native/src/lib.rs
      to: crates/x4-carrier-native/src/abi.rs
      via: "luaopen_live_galaxy_carrier initializes the narrow Lua module API"
    - from: crates/x4-carrier-native/src/abi.rs
      to: crates/x4-carrier-native/src/transport.rs
      via: "Validated caller bytes become DLL-owned complete messages"
---
<objective>
Implement the Live Galaxy-owned Lua C module and bounded local carrier per D-01/D-08. Deliver the real native transport with a safe Rust peer API for the bridge; preserve observation-only authority and exact resource ownership.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agent-instructions/x4/MEMORY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-CONTEXT.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RESEARCH.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-PATTERNS.md
@.agents/skills/live-galaxy-code-conventions/SKILL.md
@.agents/skills/live-galaxy-code-conventions/references/logging.md
@.agents/skills/live-galaxy-code-conventions/references/tooling.md
@.agents/skills/live-galaxy-tests/SKILL.md
@.agents/skills/live-galaxy-tests/references/test-standards.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/lua/SKILL.md
@Cargo.toml
@crates/x4-bridge/src/facade.rs
@crates/x4-bridge/src/session.rs
@crates/observation-ingest/src/feedback.rs
</context>

<planning_contract>
Invocation: Phase 05.4 --granularity coarse --no-tracer. The owner explicitly approved --no-tracer after the complete-stack tracer conflicted with the five-files-per-task limit. Docs MCP establishes the native loading architecture and Phase 05.3 verifies publication; these plans integrate those proven boundaries horizontally. No global configuration change is required. Research and checked plans still require owner approval before execution.

Every behavior-producing task starts with a failing behavioral assertion, then GREEN, then bounded REFACTOR; missing imports, compile errors and zero discovery are not valid RED. Record command, intended failed assertion and result using the normal GSD TDD evidence gate. All new verification paths below are created by their owning task before use. Focused warmed checks target less than 60 seconds; cold builds are preparation and must be reported separately.

Never weaken workspace lint policy. All Rust files, including tests, remain at most 200 physical lines. The approved exception permits narrowly named helper modules within the current plan-owned crate when required for this physical limit; report that execution deviation, preserve behavior/dependencies, and do not invent generic scaffolding. Read mapped analogs as compatibility evidence, not authority over current conventions.
</planning_contract>

<interface_contract>
The only PE export is luaopen_live_galaxy_carrier. The returned module provides abi_version, open, connection, try_send, poll, reset and close through Lua C functions registered by the initializer. Lua owns getters. The native module owns transport only.
Freeze version/record-size and fixed-width result contracts in module documentation and tests; use opaque exact generation tokens, never imprecise Lua-number conversion. Preserve LocalHandoff, CapacityUnavailable, stale epoch, size rejection, unavailable transport, no control, bounded control and terminal outcomes. Received and Committed are receiver policy, never local write results.
The production loader will use package.loadlib with .\extensions\live_galaxy\ui_c_library_live_galaxy_carrier_64.txt and luaopen_live_galaxy_carrier; its wrapper is owned by Plan 03.
Connection/poll metadata includes checked monotonic real milliseconds derived from the Windows performance counter. Task 3 owns its frequency/conversion and unavailable/overflow outcomes; add only the required published System_Performance feature in Task 1. Lua scheduling consumes this clock, never the opaque GetCurRealTime acceptance value. No sample, thread observation or clock reading establishes a hard callback deadline.
</interface_contract>
<bring_up_contract>
Use max_data_message_bytes=2048 and max_control_message_bytes=512, one occupied data slot and one bounded control/result slot, and one non-waiting progress poll per Lua callback. Preserve terminal visibility inside that bounded control/result capacity by refusing ordinary results when terminal capacity is needed; do not create an extra queue. These finite bring-up defaults are test inputs, not production-safety claims. Plan 04 measures candidate limits and Plan 05 freezes production limits after ordinary/SETA evidence.
</bring_up_contract>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the owned Lua initializer under the isolated native safety policy</name>
  <files>Cargo.toml, Cargo.lock, crates/x4-carrier-native/Cargo.toml, crates/x4-carrier-native/src/lib.rs, crates/x4-carrier-native/tests/abi_contract.rs</files>
  <read_first>Cargo.toml; crates/x4-bridge/Cargo.toml; crates/x4-bridge/src/lib.rs; RESEARCH.md Standard Stack and native-boundary evidence. These are manifest/export analogs; no existing source implements the native loader.</read_first>
  <behavior>The native crate builds as cdylib and rlib; its initializer returns the expected versioned API; loader failures are distinct from operation errors; panics injected in tests cannot unwind across C.</behavior>
  <action>Register x4-carrier-native and exact audited windows-sys 0.61.2 Windows dependency. Verify published feature names before pinning only Foundation, Security, FileSystem, Pipes, IO, Threading and LibraryLoader features actually used. Refresh only required Cargo.lock entries. Preserve inherited forbid-unsafe for every existing crate; the approved native crate alone uses equivalent blocking lint policy with explicit unsafe allowance and deny unsafe_op_in_unsafe_fn. Implement luaopen_live_galaxy_carrier and the Lua 5.1 C API registration edge in Rust; no separate C product layer. Acquire Lua API symbols through the source-confirmed host contract in research, and fail loading explicitly if required symbols are unavailable. Use an unwinding release panic profile and catch_unwind at C entrypoints; no production panic/unwrap/expect. Start with valid test harness declarations so RED is an intended initializer/API assertion, not a link/import failure. Keep lib.rs under 200 lines; Task 2 extracts operation responsibilities as the surface expands.</action>
  <verify><automated>cargo test --locked -p x4-carrier-native --test abi_contract</automated><fails_when>The intended ABI assertions fail, panic escapes, no contract test executes, or existing safe-crate lint policy is relaxed.</fails_when></verify>
  <done>The actual compiled native initializer and version API are tested under isolated unsafe policy; no production carrier compatibility is claimed yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact owned bytes, handles and typed outcomes at the native API</name>
  <files>crates/x4-carrier-native/src/lib.rs, crates/x4-carrier-native/src/abi.rs, crates/x4-carrier-native/src/types.rs, crates/x4-carrier-native/src/handle.rs, crates/x4-carrier-native/tests/abi_contract.rs</files>
  <read_first>crates/observation-ingest/src/wire.rs for validate-before-admission; crates/x4-bridge/src/session.rs for generation transitions; crates/x4-bridge/src/facade.rs for exact outcome distinctions.</read_first>
  <behavior>Null/nonzero, oversize, conversion overflow, wrong version, stale token and occupied slot reject before state consumption; accepted bytes survive caller mutation/release; short output capacity does not consume control; double close is safe.</behavior>
  <action>Extract ABI operations, typed configuration/results and checked generation registry into their named owners. Register logical operations into the initializer-created Lua module rather than additional PE exports. Validate actual Lua argument types and fixed-width conversions before construction; where foreign pointer buffers are used, specify caller readability/writability and aliasing obligations, validate null/length/range before copying, and never claim arbitrary pointer validity can be proven by a null check. Own one complete-message slot and bounded control/terminal visibility. Copy accepted input before returning; retain no Lua state, borrowed buffer or callback. Generation exhaustion rejects instead of wrapping. Isolate documented unsafe operations in abi.rs. Keep pure handle/capacity/result logic free of effects and executable with injected finite limits. Exercise limit-minus-one, exact and one-over, invalid output capacity, intentional test panic, stale/double-close and exact-copy cases. Per D-08, the slot reports local capacity only and implements no schema/publication policy.</action>
  <verify><automated>cargo test --locked -p x4-carrier-native --test abi_contract</automated><fails_when>Invalid arguments consume state, caller changes alter retained data, tokens alias across generations, short output consumes a result, or local handoff is reported as durable.</fails_when></verify>
  <done>The native public boundary has executable ownership, rejection, panic and result-mapping evidence with at most one retained complete message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Exchange complete messages and close pending local I/O safely</name>
  <files>crates/x4-carrier-native/src/lib.rs, crates/x4-carrier-native/src/transport.rs, crates/x4-carrier-native/src/abi_windows.rs, crates/x4-carrier-native/src/abi.rs, crates/x4-carrier-native/tests/lifecycle_contract.rs</files>
  <read_first>crates/x4-bridge/src/listener.rs as local-listener/diagnostic analog; RESEARCH.md synchronous-copy/asynchronous-progress and generation-lifecycle sections; Microsoft completion/cancellation references recorded there.</read_first>
  <behavior>A real local peer receives exact bytes and returns bounded control; absent/busy/slow peers never force callback waits; incomplete reads never become complete messages; cancelled operations retain storage through final completion; stale completion cannot affect new state. Fake-OS cases cover immediate completion, ERROR_IO_PENDING, incomplete poll, completion, cancellation request, cancellation completion, close and stale generation with exactly one retained operation owner. Current-logon access succeeds; a token outside the permitted logon SID is denied; the actual pipe reports remote-client rejection; null/default security descriptors never reach production construction.</behavior>
  <implementation_disposition>Use the resolved workerless design: no carrier-owned worker, wait, drain/retry loop or cancellation spin in a Lua callback. Create each server pipe with PIPE_REJECT_REMOTE_CLIENTS and a non-null security descriptor whose DACL grants only required directional pipe rights to the current logon SID. Establish both controls before accepting traffic; failure is fail-closed, with no default-descriptor or Carrier A fallback. The existing capability/version/session/epoch handshake follows OS admission and does not replace it. Exercise the actual descriptor/flag and denied-token path in lifecycle_contract, not a mocked boolean. If workerless completion/unload or later ordinary/SETA boundedness fails, stop acceptance and replan explicit worker ownership/lifetime/resource bounds; do not add a worker or weaken a threshold silently.</implementation_disposition>
  <action>Use pulse-driven overlapped message-mode local pipes with one data write and one bounded control read, bounded handles and reserved terminal visibility. Keep Win32 unsafe calls in abi_windows.rs; safe bridge APIs expose listener/peer operations without leaking handles or requiring unsafe in x4-bridge. Implement same-user OS access restriction and remote-client refusal before accepting traffic; compatibility/session identity is not authentication. Enforce complete-length checks, message and aggregate bounds, explicit ERROR_MORE_DATA/short/closed/pending outcomes and no busy waits. Per D-04/D-06, native progress never renews application retry budgets. Reset/close progress owns OVERLAPPED storage until terminal OS completion; CancelIoEx return alone is insufficient. The Lua userdata finalizer and module unload must not leave callbacks, worker code or kernel writes targeting released memory. Prove finalization at every pending-operation cut with actual local Windows tests; do not claim a bounded safe unload if completion ownership remains unresolved. Prefer the researched no-worker design; if evidence requires a worker or different lifecycle architecture, return that concrete conflict before expanding scope. Measure copy/allocation/progress/cancel cost using injected finite fixtures for Plan 04; numeric production limits remain unselected until measurement. Native getters and game mutation are absent by public API construction.</action>
  <verify><automated>cargo test --locked -p x4-carrier-native --test lifecycle_contract</automated><fails_when>Partial bytes pass as complete, peer progress is required for a callback return, unauthorized peers are accepted, finalization frees live I/O storage, or Windows cases are skipped.</fails_when></verify>
  <done>The real local native transport, control visibility and pending-operation lifetime pass bounded Windows tests; actual X4 conformance remains Plan 05.</done>
  <unload_evidence_boundary>These Rust lifecycle tests prove operation completion ownership, not package.loadlib finalization. Plan 04 Task 2 must additionally run its named pending-io-unload scenario with the real native image in an actual compatible Lua state, close that state, verify image unload in the still-running host, then load a new state and reject the retained old token. Plan 04 cannot qualify the candidate without that failure-capable check; native reset/close tests or host process exit alone do not satisfy it.</unload_evidence_boundary>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Contract |
| --- | --- |
| Lua to owned native module | Checked arguments, synchronous copy, no retained Lua state and no unwind |
| Native local IPC to bridge | Local peer access, explicit compatibility/epoch, complete bounded messages |
| Bridge to SQLite/current authority | Existing lifecycle, durable receipts and reconciliation |
| Product to diagnostic/package output | Safe bounded metadata and exact artifact identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.4-MEM | Tampering / DoS | Native buffers and lifetime | high | mitigate | Plan 01 bounds validation/copy, generation fences, panic containment and final completion ownership; Plan 05 verifies actual reload/load |
| T-05.4-PEER | Spoofing / Elevation | Loader and pipe | high | mitigate | Exact owned path/export; same-user local OS access and remote-client refusal; no mutation operation; Plans 01-04 negative tests |
| T-05.4-REPLAY | Tampering / Repudiation | Publication/retry | high | mitigate | Plans 02-03 preserve immutable identity/bytes, finite attempts, fresh baseline and existing exact receipt reconciliation |
| T-05.4-LOG | Information disclosure | Console/journal/evidence | medium | mitigate | Plans 02/04/05 test startup writes, bounded degraded reporting, sanitized correlation and private retained artifacts |
| T-05.4-SC | Tampering | Dependency preparation | high | mitigate | Research audits windows-sys 0.61.2 OK; no recvmsg added to production; any new package/tool preparation requires source pin and legitimacy gate |
</threat_model>
<risk_gates>
Affected layers: rust-domain and x4-runtime; no llm changes.
Selected: mandatory research, plan-check, code review and verification; security review for native memory, local peer/control authority, persistence and diagnostics; X4 probe and sustained SETA for actual package/getter/lifecycle/callback behavior; targeted mutation where changed pure safety invariants have useful operators.
Not selected: AI specification/eval (no model contract changes); custom UI design (console only); broad compatibility or AFK/model/report soak (later scope). Nyquist remains disabled unless the requirement-to-test audit exposes a concrete gap; missing scenarios must still be repaired. Native OS effects and MD are outside mutation scoring; never invent a mutation engine.
</risk_gates>

<verification>
Run focused native tests, cargo fmt --all --check, cargo run --locked -p source-size-lint -- crates, and cargo clippy --locked --workspace --all-targets --all-features -- -D warnings. Plan 04 owns final mutation/security convergence and one final full regression.
</verification>
<success_criteria>
The owned module exposes the documented narrow API, preserves exact bytes and typed outcomes, confines unsafe to reviewed native edges, and proves local peer/lifetime bounds. No Carrier A runtime dependency or game mutation is added. Native loader mechanism is source-resolved; real packaged X4 behavior still needs Plan 05.
</success_criteria>
<output>
Create .planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-01-SUMMARY.md with ABI/result table, safety/lifetime contract, dependency features, focused commands, measured local costs and evidence limitations.
</output>
