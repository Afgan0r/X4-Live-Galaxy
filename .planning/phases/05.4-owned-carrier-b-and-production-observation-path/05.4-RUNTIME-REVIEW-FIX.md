---
phase: 05.4-owned-carrier-b-and-production-observation-path
fixed_at: 2026-09-10T18:23:54.7569094Z
review_path: .planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RUNTIME-REVIEW.md
iteration: 1
findings_in_scope: 6
fixed: 5
skipped: 1
status: partial
---

# Phase 05.4 Runtime Review Fix Report

**Fixed at:** 2026-09-10T18:23:54.7569094Z  
**Source review:** `.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RUNTIME-REVIEW.md`  
**Iteration:** 1

## Summary

- Findings in scope: 6
- Fixed: 5
- Skipped after evidence-based rejection: 1
- Package artifact changed during this run: no
- Next rebuilt candidate identity: must change

## Fixed Issues

### CR-02: The process-restart scenario replaces the producer instead of testing bridge-only recovery

**Adjudication:** Downgraded from P1 implementation risk to a P2 deterministic regression gap. Attempt 12 already exercised the accepted live behavior twice on the same producer. B3 kept X4 responsive across 60 seconds without the bridge and recovered with the producer session retained. The SETA restart retained producer session `x4-session-1947430045`, advanced transport epoch to `1947430048`, and committed revision 402 15.339 seconds after stop and 152 milliseconds after compatibility.

**Files modified:** `extensions/live_galaxy/tests/carrier_b_local.lua`, `extensions/live_galaxy/tests/run_contracts.ps1`, `tests/carrier-b-local.ps1`  
**Commits:** `6207fab`, `2df2a4a`  
**Applied fix:** Added an actual-native `bridge-restart` scenario that keeps one Lua/DLL producer host alive while only the bridge restarts. It asserts revisions 1 and 2, one producer session, two transport epochs, and successful recovery. The existing distinct-producer process-restart scenario remains independently asserted with two sessions and two epochs.

### CR-03: Package verification is not closed over the shipped file set or manifest identity

**Files modified:** `tools/carrier-b-package-contract.ps1`, `tools/package-live-galaxy.ps1`, `tests/x4-disposable/01-install-guard.ps1`  
**Commit:** `e924951`  
**Applied fix:** Added one shared package-contract validator used by both the packager and install guard. It validates the full manifest identity, canonical contained relative paths, exact equality between the recursive package file set and manifest entries, and every declared hash. Negative package tests now cover an unmanifested file, traversal path, and altered candidate identity.

### CR-04: The ready-package path accepts limits the production bridge rejects

**Files modified:** `crates/x4-bridge/src/bin/validate_limits.rs`, `tools/package-live-galaxy.ps1`  
**Commits:** `81845bf`, `d468e32`  
**Applied fix:** The ready-package path now invokes a small unshipped validator binary that reads limits through the production `ProductionLimits::read` parser. Package self-tests cover all six cross-field relationships and duplicate keys. The follow-up commit replaced a forbidden stderr macro with the repository's bounded locked-stderr pattern.

### WR-01: The default Lua regression hides a broken obsolete loading suite

**Files modified:** `extensions/live_galaxy/tests/module_loading_spec.lua`  
**Commit:** `d352515`  
**Applied fix:** Removed the obsolete Carrier A and `sn_mod_support_apis` behavioral block. The file now owns shipped-Lua syntax checks only; current Carrier B loader behavior remains covered by `x4_loader_contract.lua` in the active suite.

### WR-02: Three reviewed Rust test files violate the mandatory 200-line limit

**Files modified:** `crates/x4-bridge/tests/carrier_b_contract.rs`, `crates/x4-bridge/tests/carrier_b_contract/control_contract.rs`, `crates/x4-bridge/tests/production_startup/actual_pipe.rs`, `crates/x4-bridge/tests/production_startup/actual_pipe/sample.rs`, `crates/x4-carrier-native/tests/producer_contract.rs`, `crates/x4-carrier-native/tests/producer_contract/failure_paths.rs`  
**Commit:** `17d7e61`  
**Applied fix:** Split the three oversized test owners into narrowly named helper modules. The resulting files are 124, 105, 157, 64, 158, and 129 lines respectively, with behavior unchanged.

## Skipped Issues

### CR-01: Game-loaded handling does not terminate pre-load in-flight work

**Reason:** Rejected because its same-Lua-producer premise conflicts with the accepted lifecycle and the actual attempt-12 observation. B7 observed pre-load producer session `x4-session-2773850038`, retained committed revision 68, discarded incomplete revision 69, then observed a new DLL-owned producer incarnation `x4-session-2773850039` and committed fresh revision 70 about 1.073 seconds after compatibility. Independent readback retained revisions 68 and 70. B8 and B9 separately observed new producer sessions on UI reload and rejected late old callbacks as `peer-disconnected`. The accepted X4 load path therefore already terminates the old producer incarnation outside the Lua table, so adding a speculative `game_loaded` recovery branch would duplicate an unobserved lifecycle rather than repair a demonstrated gap.

## Verification

All commands ran in the isolated review-fix worktree. The Lua suite used the already prepared Busted/Lua binaries from the main checkout cache while executing the isolated worktree's sources; no dependencies were installed.

- `cargo fmt --all -- --check`: passed.
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`: passed.
- `cargo test --workspace --all-targets --all-features --locked`: passed; five pre-existing named-pipe tests remained intentionally ignored.
- Focused bridge contract and production-startup tests: 5/5 and 7/7 passed.
- Focused native producer contract tests: 6/6 passed.
- Actual-native `bridge-restart`, distinct-producer `process-restart`, both startup orders, multi-collection, sustained, and pending-unload scenarios: passed.
- Active Busted product suite: 52 successes, 0 failures, 0 errors.
- Shipped Lua syntax suite: 9/9 passed.
- XML/package registration suite: four files well-formed and all seven rejection checks passed.
- Persistence schema check: passed.
- Package self-test, including new negative package and limits cases: passed.
- `git diff --check 0188080 HEAD`: passed.
- `cargo run --locked -p source-size-lint`: still fails only on untouched pre-existing files `crates/observation-persistence/src/sqlite_read.rs` at 201 lines and `crates/x4-bridge/src/production.rs` at 205 lines. Neither file differs from the review base. All six WR-02 split files pass the 200-line limit.

## Exact Changed Files

- `crates/x4-bridge/src/bin/validate_limits.rs`
- `crates/x4-bridge/tests/carrier_b_contract.rs`
- `crates/x4-bridge/tests/carrier_b_contract/control_contract.rs`
- `crates/x4-bridge/tests/production_startup/actual_pipe.rs`
- `crates/x4-bridge/tests/production_startup/actual_pipe/sample.rs`
- `crates/x4-carrier-native/tests/producer_contract.rs`
- `crates/x4-carrier-native/tests/producer_contract/failure_paths.rs`
- `extensions/live_galaxy/tests/carrier_b_local.lua`
- `extensions/live_galaxy/tests/module_loading_spec.lua`
- `extensions/live_galaxy/tests/run_contracts.ps1`
- `tests/carrier-b-local.ps1`
- `tests/x4-disposable/01-install-guard.ps1`
- `tools/carrier-b-package-contract.ps1`
- `tools/package-live-galaxy.ps1`

The report itself is intentionally excluded from the fix commits.

## Package Identity and X4 Retest Blast Radius

No `dist/live-galaxy-carrier-b` artifact existed in the isolated worktree, and this run did not regenerate, install, or mutate the frozen attempt-12 package. Consequently, no existing package identity or packaged byte changed during the run.

A package rebuilt from these commits must receive a new manifest source revision and logical identity. The accepted fixes do not change shipped Rust bridge production code, native DLL production code, Lua/MD/XML production code, or limits bytes; `validate_limits.exe` is a packaging-time validator and is not shipped. Nevertheless, attempt-12 evidence must not be claimed for the new logical identity until the rebuilt package passes the install guard and its shipped runtime payload hashes are compared with the attempt-12 candidate.

- If every shipped runtime payload hash is identical and only manifest/package-verification metadata changes, repeat Plan 05 B5 for the new exact package. No user-operated X4 cell (B1-B4 or B6-B9, ordinary window, or SETA window) needs repetition because their executable/game-facing inputs are byte-identical.
- If any shipped bridge, native, Lua, MD, XML, or limits payload hash changes, repeat B1-B9 plus the ordinary and sustained SETA windows against the new exact candidate. Prior attempt-12 runtime observations do not qualify that changed payload.

---

_Fixed: 2026-09-10T18:23:54.7569094Z_  
_Fixer: Codex (gsd-code-fixer)_  
_Iteration: 1_
