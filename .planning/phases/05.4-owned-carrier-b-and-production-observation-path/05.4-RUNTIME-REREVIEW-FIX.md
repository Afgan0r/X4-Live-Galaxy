---
phase: 05.4-owned-carrier-b-and-production-observation-path
fixed_at: 2026-09-10T18:52:00.9324232Z
review_path: .planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RUNTIME-REREVIEW.md
iteration: 2
findings_in_scope: 1
fixed: 1
skipped: 0
status: all_fixed
---

# Phase 05.4 Runtime Re-review Fix Report

**Fixed at:** 2026-09-10T18:52:00.9324232Z
**Source review:** `.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RUNTIME-REREVIEW.md`
**Iteration:** 2

## Summary

- Findings in scope: 1
- Fixed: 1
- Skipped: 0
- Shipped runtime payload impact: none

## Fixed Issue

### WR-03: The PowerShell pre-parser remains narrower than the authoritative Rust parser

**File modified:** `tools/package-live-galaxy.ps1`
**Commit:** `a2111a1`
**Applied fix:** Removed PowerShell's duplicate JSON shape, integer-type, and positivity admission from `Read-Limits`. PowerShell retains file existence, the 4,096-byte input bound, validator availability, validator exit-status handling, and byte-for-byte propagation of the accepted input. `ProductionLimits::read` is now the single owner of JSON fields, duplicates, integer parsing, positivity, and cross-field relationships.

The package self-test now constructs an otherwise valid fixture with `max_blockers=9223372036854775808`, verifies that `Read-Limits` accepts it, and verifies that the exact raw bytes are returned. The six existing relationship-negative fixtures and duplicate-key fixture remain active and are rejected by the authoritative Rust validator.

## Verification

All verification ran in the isolated review-fix worktree.

- RED: `pwsh -NoProfile -File tools/package-live-galaxy.ps1 -SelfTest` failed before the implementation change with `LIMIT_NOT_INTEGER:max_blockers` on the new BigInteger fixture.
- GREEN: the same focused package self-test passed after removing the duplicate semantic parser.
- `pwsh -NoProfile -File tools/package-live-galaxy.ps1 -SelfTest -LimitsFile config/carrier-b-limits.json`: passed, including all existing relationship and duplicate-key negative cases.
- `cargo fmt --all -- --check`: passed.
- `cargo test --locked -p x4-bridge --test production_startup production_limits_require_exact_bounded_json_shape`: 1 passed.
- `cargo clippy --locked -p x4-bridge --all-targets --all-features -- -D warnings`: passed.
- `git diff --check HEAD^ HEAD`: passed.

## Package Payload Impact and Remaining Risk

This change affects only the repository package tool. It does not modify the Rust bridge, native DLL, extension Lua/MD/XML, limits configuration, or any other shipped runtime payload. No package was installed and no X4 cell needs repetition.

The remaining platform boundary is explicit: admission follows the production target's `usize`. The qualified package is AMD64, so the concrete above-`Int64::MAX` value is valid on the supported target. Future production parser changes automatically remain authoritative because PowerShell no longer shadows their semantics.

No known WR-03 risk remains. The package path still fails closed when the authoritative validator is missing or rejects the input.

---

_Fixed: 2026-09-10T18:52:00.9324232Z_
_Fixer: Codex (gsd-code-fixer)_
_Iteration: 2_
