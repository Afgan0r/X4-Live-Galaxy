---
phase: 05.4-owned-carrier-b-and-production-observation-path
plan: "02"
type: execute
wave: 2
depends_on: [05.4-01]
files_modified:
  - crates/observation-domain/src/identity.rs
  - crates/observation-domain/src/observation.rs
  - crates/observation-ingest/src/lib.rs
  - crates/observation-ingest/src/wire.rs
  - crates/observation-ingest/src/wire_decode.rs
  - crates/observation-ingest/src/wire_encode.rs
  - crates/observation-ingest/src/sender_evidence.rs
  - crates/observation-ingest/src/carrier_control.rs
  - crates/observation-ingest/src/batch_canonical.rs
  - crates/observation-ingest/src/completion_digest.rs
  - crates/observation-ingest/src/completion_types.rs
  - crates/observation-ingest/src/producer_completion.rs
  - crates/observation-ingest/src/completion.rs
  - crates/observation-ingest/src/generation.rs
  - crates/observation-ingest/src/legacy_generation.rs
  - crates/observation-ingest/src/batch_budget.rs
  - crates/observation-ingest/src/candidate_limits.rs
  - crates/observation-ingest/src/accepted_versions_tests.rs
  - crates/observation-ingest/tests/wire_v2_contract.rs
  - crates/observation-ingest/tests/completion_contract.rs
  - crates/observation-ingest/tests/completion_contract/rejections.rs
  - crates/observation-ingest/tests/contract_lifecycle.rs
  - crates/observation-ingest/tests/keyed_candidates.rs
  - crates/observation-ingest/tests/session_fencing.rs
  - crates/observation-application/tests/support.rs
  - crates/observation-application/tests/support/versioned.rs
  - crates/observation-application/tests/support/hydration.rs
  - crates/observation-application/tests/certificate_contract.rs
  - crates/observation-persistence/src/lib.rs
  - crates/observation-persistence/tests/support/revisions.rs
  - crates/x4-bridge/src/carrier_b_codec.rs
  - crates/x4-bridge/src/carrier_b.rs
  - crates/x4-bridge/src/production_startup.rs
  - crates/x4-bridge/tests/carrier_b_contract.rs
  - crates/x4-bridge/tests/carrier_b_support/mod.rs
  - crates/x4-bridge/src/production.rs
  - crates/x4-bridge/src/receiver_context.rs
  - crates/observation-application/src/lifecycle.rs
  - crates/x4-bridge/src/lib.rs
  - crates/x4-bridge/tests/receiver_context_contract.rs
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/production_limits.rs
  - crates/x4-bridge/tests/production_startup.rs
autonomous: true
requirements: [OBS-01, OBS-08, VAL-06]
estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The bridge starts independently and accepts only compatible local observation sessions."
    - "Real complete messages traverse ObservationLifecycle to independently readable SQLite publication."
    - "Lost responses and reconnect cannot duplicate publication or revive uncertain source authority."
    - "The console and structured history expose connection, collection, waiting and degradation with safe reasons."
  artifacts:
    - path: crates/x4-bridge/src/carrier_b.rs
      provides: "Production carrier-neutral facade implementation"
    - path: crates/x4-bridge/src/production.rs
      provides: "ObservationLifecycle and durable repository composition"
    - path: crates/x4-bridge/tests/carrier_b_recovery.rs
      provides: "Restart and exact receipt reconciliation proof"
  key_links:
    - from: crates/x4-bridge/src/main.rs
      to: crates/x4-bridge/src/production.rs
      via: "Independent production bridge startup"
    - from: crates/x4-bridge/src/production.rs
      to: crates/observation-application/src/lifecycle.rs
      via: "submit, reconcile_ambiguous and retry_proven_not_committed"
    - from: crates/x4-bridge/src/production.rs
      to: crates/observation-persistence/src/sqlite.rs
      via: "Existing durable publication and clean restart readback"
---
<objective>
Finish only the real remaining production work per D-02/D-03/D-06/D-07/D-12/D-13: atomic exact-contract migration, receiver-owned context assembly, and the independent live receive loop. Preserve the committed facade, lifecycle/publication/recovery, diagnostics, startup and readback work (52e40cc, 6f00741, 0f7f3fe, de71ae3); do not re-execute their completed tasks. Run their tests as regressions. Plan 01 remains complete and unchanged.
</objective>
<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agent-instructions/x4/MEMORY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-CONTEXT.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RESEARCH.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-PATTERNS.md
@.agents/skills/live-galaxy-code-conventions/SKILL.md
@.agents/skills/live-galaxy-code-conventions/references/logging.md
@.agents/skills/live-galaxy-code-conventions/references/tooling.md
@.agents/skills/live-galaxy-tests/SKILL.md
@.agents/skills/live-galaxy-tests/references/test-standards.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/lua/SKILL.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-01-SUMMARY.md
@crates/x4-bridge/src/facade.rs
@crates/observation-application/src/lifecycle.rs
@crates/observation-application/src/lifecycle_publication.rs
@crates/observation-persistence/src/port.rs
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-04-SUMMARY.md
</context>
<planning_contract>
Remaining work only, at d604c97bfe745a8bd65c207eb3ae03f9cac29664 plus the owner-approved D-12/D-13 research and reconciled architecture. Preserve completed 05.4-01 and partial 05.4-02. The inherited owner-approved --no-tracer decomposition remains in force.

The owner approved exactly one exception to the five-files-per-task cap: 02 Task 1 may perform the cohesive atomic D-13 contract migration across direct consumers so GREEN remains compile-preserving. This is not permission to broaden other tasks. All other tasks name at most five files. The separate existing repository permission for narrowly named same-crate helper modules needed solely for Rust's 200-line limit remains applicable; record any such mechanical execution deviation and do not use it to add responsibilities.

Every behavior-producing task records an intended failing assertion before GREEN, using the GSD RED evidence gate; compile/link errors and empty discovery are not RED. Create a new test target with its owning task before invoking it. Keep focused warmed checks under 60 seconds where practical; report cold builds and deliberate integration windows separately. No new registry dependency/install is planned: reuse locked workspace libraries and explicitly prepared local tools.

Plans 02, 03 and 04 are automated implementation/qualification. Plan 05 starts with the user-only X4 checkpoint. After 04 is reviewed and verified, present exact ready procedure/package/config identity and STOP before installation, X4 launch, game controls or SETA; do not mark 05 or phase acceptance complete.
</planning_contract>
<interface_contract>
Keep the existing carrier-neutral facade and lifecycle authority. Migrate only the actual missing production codec/context/loop, not the already implemented facade/publication/recovery. Envelope contract, control contract and native ABI are exactly 2; semantic schema/policy/canonicalization/digest stay 1/2/3/1. No compatibility reader for 1 or unknown versions. Reject extra decoded_bytes instead of ignoring it. Sender evidence is capture/source truth; receiver owns policy/dependencies/current pointers/clocks/limits. No wire CandidateContext.

Control negotiation binds a session, native incarnation and transport epoch to the exact version tuple. Each data attempt/disposition is bound to one immutable message identity/digest and section/revision; maintain existing identity namespaces and strictly checked sequence/batch ordinals. The DLL owns production IDs and exact retry bytes; receiver computes canonical/count/digest evidence from received records independently before publication. Reuse pure contract helpers without accepting caller-computed claims as verification. Controls grant only bounded observation intent/credit and receipt/status/reset, never game mutation.
</interface_contract>
<resolved_transport_contract>
The completed native DLL is the local pipe server; BridgePeer in the independent external process is the client. Preserve the single bounded native worker, overlapped completion ownership and module lease through terminal completion from 01 SUMMARY. No new worker, listener, supervisor or synchronous Lua wait. Preserve isolated error classes and whole-message rejection; a partial/oversize read is not a parseable prefix. Volatile candidates are fenced across uncertain source boundaries while SQLite history and exact receipts remain.
</resolved_transport_contract>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Atomically migrate the observation contract and direct consumers</name>
  <reversibility rating="costly">Owner-approved D-13 changes coordinated private wire/type contracts; retained semantic SQLite records need no migration.</reversibility>
  <files>crates/observation-domain/src/identity.rs, crates/observation-domain/src/observation.rs, crates/observation-ingest/src/lib.rs, crates/observation-ingest/src/wire.rs, crates/observation-ingest/src/wire_decode.rs, crates/observation-ingest/src/wire_encode.rs, crates/observation-ingest/src/sender_evidence.rs, crates/observation-ingest/src/carrier_control.rs, crates/observation-ingest/src/batch_canonical.rs, crates/observation-ingest/src/completion_digest.rs, crates/observation-ingest/src/completion_types.rs, crates/observation-ingest/src/producer_completion.rs, crates/observation-ingest/src/completion.rs, crates/observation-ingest/src/generation.rs, crates/observation-ingest/src/legacy_generation.rs, crates/observation-ingest/src/batch_budget.rs, crates/observation-ingest/src/candidate_limits.rs, crates/observation-ingest/src/accepted_versions_tests.rs, crates/observation-ingest/tests/wire_v2_contract.rs, crates/observation-ingest/tests/completion_contract.rs, crates/observation-ingest/tests/completion_contract/rejections.rs, crates/observation-ingest/tests/contract_lifecycle.rs, crates/observation-ingest/tests/keyed_candidates.rs, crates/observation-ingest/tests/session_fencing.rs, crates/observation-application/tests/support.rs, crates/observation-application/tests/support/versioned.rs, crates/observation-application/tests/support/hydration.rs, crates/observation-application/tests/certificate_contract.rs, crates/x4-bridge/src/carrier_b_codec.rs, crates/x4-bridge/src/carrier_b.rs, crates/x4-bridge/src/production_startup.rs, crates/x4-bridge/tests/carrier_b_contract.rs, crates/x4-bridge/tests/carrier_b_support/mod.rs, crates/observation-persistence/src/lib.rs, crates/observation-persistence/tests/support/revisions.rs</files>
  <read_first>Amended research Exact Versioning, D-13 Source Impact and sender-context sections; identity.rs require_contract; wire.rs/wire_decode.rs; batch_canonical.rs; completion_digest.rs; producer_completion.rs; completion.rs; generation.rs; batch_budget.rs; candidate_limits.rs; direct constructor/fixture consumers listed above; carrier_b_codec.rs.</read_first>
  <behavior>Envelope contract 2 accepts exact valid start/batch/completion; 1 and unknown versions, missing required fields, extra decoded_bytes, unknown fields and forged receiver-context fields reject before candidate admission. All remaining record/count/canonical-byte/digest/coverage/version and independent incremental limits still reject mismatches while durable accepted state survives.</behavior>
  <action>Per D-12/D-13, execute the sole owner-approved atomic migration. Remove decoded_bytes from domain/wire certificates, CanonicalBatch, ProducerMaterial, CompletionCertificate, usage/limit structures, checked charges, default construction and direct fixtures; do not replace it with another allocation-layout claim or retain the local formula absent a demonstrated uncovered resource risk. Preserve checked canonical-byte, record, batch, work, candidate-count, age and aggregate limits, including legacy consumers that still compile; adapt only their affected shared constructors, not their unrelated protocol.

Require exact complete-message envelope contract 2; retain semantic schema/policy/canonicalization/digest vector 1/2/3/1. No dual reader, aliases, accepted-state deletion or SQLite data rewrite. Add bounded pure encode_complete_message and decode_sender_evidence functions in observation-ingest. The start envelope must carry strict sender evidence (capture window with its clock meaning, stated section-state axes, source-epoch status/boundary and source consistency/stability evidence, semantic versions), scoped to its exact identity. Keep this separate from internal CandidateContext and out of canonical batch framing. Start/completion evidence must agree; stronger completion evidence rejects. Reject fields purporting to supply expected_current, receiver dependencies/clocks/limits or SQLite pointers. Existing generic lifecycle callers remain supported through their existing typed context API, while production requires decoded sender evidence.

Migrate the current kind-only Carrier B codec to strict control contract 3 with preserved typed bodies. Put the reusable pure control codec/types in observation-ingest and keep bridge public re-exports, because the DLL must encode/decode the same controls without depending on x4-bridge and creating a dependency cycle. Handshake binds control_version=3, native_abi=2, envelope_contract=2, semantic versions, bounded session/incarnation and transport epoch; any mismatch is restart-required before data. Define closed bodies for handshake, demand, disposition, collection_intent, health and reset only. Disposition includes exact message identity, epoch, section/revision, message digest and precise ReceiverDisposition; received and committed are never collapsed to a kind. Demand is bounded implicit credit 1; collection intent names only the admitted acceptance section, the bridge-owned durable `current + 1` revision floor, and finite budgets, never arbitrary getters or game actions. The DLL uses the greater of its next local revision and that floor so a fresh X4/DLL process cannot reuse durable section or observation versions. Preserve OS admission and facade outcome classes. Old v=1/v=2, extra/missing/duplicate fields, unknown kind, mutation kind, wrong identities/digests and stale epoch reject.

First add executable old-compatible RED assertions for new decoding/rejection behavior, then migrate all named direct consumers in one GREEN commit. Add independent fixed expected canonical/digest cases and adversarial changes to every retained evidence dimension, including extra decoded_bytes as a rejection fixture only. Golden expectations must not be produced by the same helper under test. Local allocation review uses the concrete retained copies and configured bounds; only an evidenced uncovered multiplier can justify a separate receiver-only mechanism with its own tests.</action>
  <verify>
    <automated>cargo test --locked -p observation-ingest --test wire_v2_contract --test completion_contract --test contract_lifecycle --test keyed_candidates --test session_fencing</automated>
    <fails_when>Cargo exits nonzero, a named target discovers no tests, or strict version/field rejection, independent certificate recomputation, candidate limits or session fencing assertions fail.</fails_when>
    <automated>cargo test --locked -p observation-application --test certificate_contract --test lifecycle_contract</automated>
    <fails_when>Cargo exits nonzero, either named target discovers no tests, or certificate tampering is admitted, lifecycle rejection changes accepted state, or exact publication/recovery assertions fail.</fails_when>
    <automated>cargo test --locked -p observation-persistence</automated>
    <fails_when>Cargo exits nonzero, required persistence tests are missing or skipped, or migrated fixtures no longer prove durable revision/current-pointer/receipt preservation and restart readback.</fails_when>
    <automated>cargo test --locked -p x4-bridge --test carrier_b_contract --test carrier_b_publication --test carrier_b_recovery</automated>
    <fails_when>Cargo exits nonzero, a named target discovers no tests, or strict typed control negotiation, exact durable publication or ambiguous receipt/reconnect recovery assertions fail.</fails_when>
  </verify>
  <done>The coordinated contract migration compiles and all named behavioral targets pass; exact version 2 and independent receiver recomputation are enforced without layout accounting or durable-data loss. The exception applies only to this task.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build receiver admission context from sender evidence and durable current state</name>
  <files>crates/x4-bridge/src/production.rs, crates/x4-bridge/src/receiver_context.rs, crates/observation-application/src/lifecycle.rs, crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/receiver_context_contract.rs</files>
  <read_first>ProductionObservationSession; ObservationLifecycle; ObservationRepository::{current,current_snapshot}; CandidateContext and CompletionCurrent; existing carrier_b_publication/recovery tests.</read_first>
  <behavior>A real start/batch/completion with a point measurement reaches an isolated SQLite revision using decoded sender evidence and independently read receiver context. Forged dependencies/current pointers fail; a dependency or pointer changed during collection rejects at completion; exact retry preserves its original admission context. Source/reconnect boundaries retain durable history and deny old current authority.</behavior>
  <action>Complete only the missing production seam per D-06/D-08/D-12. Add a receiver-owned context provider and a production submit_received entry that accepts negotiated identity plus complete bounded bytes and receiver time/work inputs, not caller-authored CandidateContext. Read current/dependency revisions from the lifecycle's actual repository through narrow read operations; do not expose mutable internals or duplicate repository state. For the acceptance section the policy dependency set is explicitly empty, while expected_current comes from the actual store; exercise a dependency-bearing receiver-policy fixture as the adversarial context case without adding a game collector.

Validate sender evidence, freeze CandidateContext at section start, and retain that exact context across replay. Re-read CompletionCurrent at completion and preserve finish-then-validate rejection. Receiver clock and work charges come from actual receive/decode accounting and validated receiver limits; sender cannot set them. Use existing lifecycle for slots, staging, independent certificate recomputation, publication and ambiguous receipt reconciliation. Lifecycle boundary invalidation must discard volatile candidates/slot state and retire uncertain source eligibility while preserving SQLite revisions/receipts. Hydrate durable current pointers before the next replacement so restart cannot accidentally use expected_current=None over an existing row. Reconcile an exact retained ambiguous attempt before fresh collection; still-ambiguous blocks within existing limits.

Tests submit actual encoded bytes through the new public production entry and independently reopen SQLite to check exact content, current pointer and receipt. Preserve lower-layer helper-driven tests as local contract evidence, but do not cite them as proof of production context assembly.</action>
  <verify><automated>cargo test --locked -p x4-bridge --test receiver_context_contract --test carrier_b_publication --test carrier_b_recovery</automated><fails_when>Cargo exits nonzero, a named target discovers no tests, sender-forged receiver authority is accepted, changed current/dependency state is missed at completion, or independent SQLite content/pointer/receipt and exact recovery assertions fail.</fails_when></verify>
  <done>The production byte path derives context from both proper owners, revalidates current state and preserves exact durable recovery without trusting injected sender authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Keep the independent bridge running on the actual peer receive loop</name>
  <files>crates/x4-bridge/src/production_startup.rs, crates/x4-bridge/src/production_runtime.rs, crates/x4-bridge/src/production_limits.rs, crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/production_startup.rs</files>
  <read_first>Existing run_production and OperationalHistory; BridgePeer::{connect,receive,send_control}; native pipe direction; existing offline --readback behavior.</read_first>
  <behavior>The actual bridge process remains alive waiting for the owned DLL pipe; both startup orders negotiate exact contracts and publish a three-message sample. Wrong/missing/duplicate/extra limits reject and changing a limit changes admission. Disconnect, malformed/oversize messages, response loss and incompatible identity preserve state and expose their distinct reason.</behavior>
  <action>Finish run_production instead of recreating completed startup/readback/diagnostics. The DLL is the pipe server; the external bridge connects as BridgePeer and services complete messages, never creates a competing listener. Preserve --data-dir, --limits-file and offline --readback mode; offline mode opens no pipe. Replace the current syntactic key=value check and unused hardcoded defaults with strict bounded JSON config consumed by actual transport, receiver, publication, retry/availability and diagnostics settings. Freeze the exact config keys in production_limits with nonzero/overflow/relationship checks, reject unknown/duplicate/missing fields, and share the documented keys with Plan 04.

The live loop connects/reconnects within finite real-time attempts, completes the explicit version/identity handshake, sends bounded collection intent/demand, dispatches actual bytes through Task 2, and returns exact typed dispositions. All waiting belongs to the independent external process, never a Lua callback. Idle/disconnected periods create no work or queue growth; after resumed input check expiry before admission. Handle oversize/partial/error peer reads as rejected whole messages and abandon the affected session, never parse a prefix. No background process supervisor or game launch is added per D-02/D-03. Preserve startup journal write validation and bounded independent runtime degradation; record meaningful connection/collection/waiting/rejection/commit states per D-07.

Rewrite the old test that expects successful startup to return immediately. Use runner-owned child bridge processes, bounded readiness/result watchdogs and isolated data directories; terminate only those children. Exercise both startup orders with actual native transport, exact readback in a fresh process, incompatible tuple before data and changes to effective limits. Preserve completed readback and diagnostic behavior as regressions. Configuration documentation and runtime package wiring belong to Plan 04.</action>
  <verify><automated>cargo test --locked -p x4-bridge --test production_startup</automated><fails_when>Cargo exits nonzero or discovers no tests, a child-process watchdog expires, the live bridge exits instead of waiting, either startup order fails, invalid or ineffective limits are accepted, or fresh-process readback/compatibility rejection assertions fail.</fails_when></verify>
  <done>The production executable stays available, accepts only compatible complete messages through receiver-derived admission, and independently exposes durable readback with effective finite configuration.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Contract |
| --- | --- |
| Lua to owned native module | Checked arguments, synchronous copy, no retained Lua state and no unwind |
| Native local IPC to bridge | Local peer access, explicit compatibility/epoch, complete bounded messages |
| Bridge to SQLite/current authority | Existing lifecycle, durable receipts and reconciliation |
| Product to diagnostic/package output | Safe bounded metadata and exact artifact identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.4-MEM | Tampering / DoS | Native buffers and lifetime | high | mitigate | Plan 01 bounds validation/copy, generation fences, panic containment and final completion ownership; Plan 05 verifies actual reload/load |
| T-05.4-PEER | Spoofing / Elevation | Loader and pipe | high | mitigate | Exact owned path/export; same-user local OS access and remote-client refusal; no mutation operation; Plans 01-04 negative tests |
| T-05.4-REPLAY | Tampering / Repudiation | Publication/retry | high | mitigate | Plans 02-03 preserve immutable identity/bytes, finite attempts, fresh baseline and existing exact receipt reconciliation |
| T-05.4-LOG | Information disclosure | Console/journal/evidence | medium | mitigate | Plans 02/04/05 test startup writes, bounded degraded reporting, sanitized correlation and private retained artifacts |
| T-05.4-SC | Tampering | Dependency preparation | high | mitigate | Research audits windows-sys 0.61.2 OK; no recvmsg added to production; any new package/tool preparation requires source pin and legitimacy gate |
</threat_model>
<risk_gates>
Layers: rust-domain and x4-runtime. Research, plan-check, code review and verification are mandatory. Select focused security review for ABI ownership, local peer/control authority, version/source fences, persistence and diagnostics; targeted mutation for changed deterministic bounds/version/digest/epoch/retry predicates; actual local module integration followed by user-operated X4 and sustained SETA for the unresolved runtime behavior. No AI specification/eval or custom design gate: no llm or custom UI change. No broad mod compatibility, heavy ship/station, report or milestone-wide AFK soak. Nyquist is disabled; missing scenarios are repaired in their owning tests, not by introducing a separate validation platform.
</risk_gates>
<verification>
Run the new task-owned behavioral targets and existing facade/publication/recovery/readback regressions. Confirm all workspace direct consumers compile after the atomic migration, strict version/extra-field tests exercise real decoders, and SQLite readback is independent of producer expectations. Record RED/GREEN commands and outcomes. Plan 04 owns final full regression, packaging and actual Lua/DLL integration.
</verification>
<success_criteria>
Compatible complete messages reach durable publication, incompatible/invalid input preserves state, restart uses exact durable evidence, and independent startup/diagnostics implement D-02/D-03/D-06/D-07 without game mutation.
</success_criteria>
<output>
Create .planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-02-SUMMARY.md with control codec/session contract, exact durable readback, interruption matrix, diagnostics behavior and commands.
</output>
