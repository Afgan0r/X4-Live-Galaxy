---
phase: 05.4-owned-carrier-b-and-production-observation-path
plan: "02"
type: execute
wave: 2
depends_on: [05.4-01]
files_modified:
  - crates/x4-bridge/Cargo.toml
  - Cargo.lock
  - crates/x4-bridge/src/carrier_b.rs
  - crates/x4-bridge/src/lib.rs
  - crates/x4-bridge/src/production.rs
  - crates/x4-bridge/src/diagnostics.rs
  - crates/x4-bridge/src/main.rs
  - crates/x4-bridge/tests/carrier_b_contract.rs
  - crates/x4-bridge/tests/carrier_b_publication.rs
  - crates/x4-bridge/tests/carrier_b_recovery.rs
  - crates/x4-bridge/tests/carrier_b_support/mod.rs
  - crates/x4-bridge/tests/production_startup.rs
autonomous: true
requirements: [OBS-01, OBS-08, VAL-06]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The bridge starts independently and accepts only compatible local observation sessions."
    - "Real complete messages traverse ObservationLifecycle to independently readable SQLite publication."
    - "Lost responses and reconnect cannot duplicate publication or revive uncertain source authority."
    - "The console and structured history expose connection, collection, waiting and degradation with safe reasons."
  artifacts:
    - path: crates/x4-bridge/src/carrier_b.rs
      provides: "Production carrier-neutral facade implementation"
    - path: crates/x4-bridge/src/production.rs
      provides: "ObservationLifecycle and durable repository composition"
    - path: crates/x4-bridge/tests/carrier_b_recovery.rs
      provides: "Restart and exact receipt reconciliation proof"
  key_links:
    - from: crates/x4-bridge/src/main.rs
      to: crates/x4-bridge/src/production.rs
      via: "Independent production bridge startup"
    - from: crates/x4-bridge/src/production.rs
      to: crates/observation-application/src/lifecycle.rs
      via: "submit, reconcile_ambiguous and retry_proven_not_committed"
    - from: crates/x4-bridge/src/production.rs
      to: crates/observation-persistence/src/sqlite.rs
      via: "Existing durable publication and clean restart readback"
---
<objective>
Connect the owned local transport to the existing carrier-neutral lifecycle and durable repository, with independent startup and honest diagnostics per D-02/D-03/D-06/D-07.
</objective>
<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agent-instructions/x4/MEMORY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-CONTEXT.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RESEARCH.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-PATTERNS.md
@.agents/skills/live-galaxy-code-conventions/SKILL.md
@.agents/skills/live-galaxy-code-conventions/references/logging.md
@.agents/skills/live-galaxy-code-conventions/references/tooling.md
@.agents/skills/live-galaxy-tests/SKILL.md
@.agents/skills/live-galaxy-tests/references/test-standards.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/lua/SKILL.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-01-SUMMARY.md
@crates/x4-bridge/src/facade.rs
@crates/observation-application/src/lifecycle.rs
@crates/observation-application/src/lifecycle_publication.rs
@crates/observation-persistence/src/port.rs
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-04-SUMMARY.md
</context>
<planning_contract>
Invocation: Phase 05.4 --granularity coarse --no-tracer. The owner explicitly approved --no-tracer after the complete-stack tracer conflicted with the five-files-per-task limit. Docs MCP establishes the native loading architecture and Phase 05.3 verifies publication; these plans integrate those proven boundaries horizontally. No global configuration change is required. Research and checked plans still require owner approval before execution.

Every behavior-producing task starts with a failing behavioral assertion, then GREEN, then bounded REFACTOR; missing imports, compile errors and zero discovery are not valid RED. Record command, intended failed assertion and result using the normal GSD TDD evidence gate. All new verification paths below are created by their owning task before use. Focused warmed checks target less than 60 seconds; cold builds are preparation and must be reported separately.

Never weaken workspace lint policy. All Rust files, including tests, remain at most 200 physical lines. The approved exception permits narrowly named helper modules within the current plan-owned crate when required for this physical limit; report that execution deviation, preserve behavior/dependencies, and do not invent generic scaffolding. Read mapped analogs as compatibility evidence, not authority over current conventions.
</planning_contract>
<interface_contract>
Reuse ObservationCarrierFacade::{connection_state,try_send_complete,poll_control} and the existing ConnectionState, CompleteMessageSendOutcome, FacadeError and ControlPollOutcome variants exactly. ControlEnvelope remains limited to Handshake, Demand, Disposition, CollectionIntent, Health and Reset.
The wire's existing RawControl carries only contract_version; it is not a complete codec for production control payloads. Implement the bounded concrete carrier control codec in carrier_b.rs, translating typed existing control fields without broadening authority or changing generic domain semantics. Validate before allocation and reject unknown kinds/fields.
ObservationLifecycle owns slot/stager/eligibility/repository and publish/reconcile classification. CarrierBFacade owns transport; ProductionObservationSession owns sequencing only.
Task 3 also provides an offline --readback mode with --data-dir, --section-key and --section-revision: open the existing SQLite store in a fresh process without starting a listener and return the selected revision, exact acceptance-record content and durable receipt as bounded machine-readable output. Reject unknown/missing identity. This is the independent reader used by Plans 04/05, not a new network or game-control surface.
</interface_contract>
<resolved_transport_contract>
Consume Plan 01's workerless safe peer API and bring-up ceilings of 2048 data bytes and 512 control bytes, one data slot and one bounded control/result slot; do not inflate independent limits implicitly. Require current-logon-SID DACL admission with least directional rights and PIPE_REJECT_REMOTE_CLIENTS before the capability/version/session/epoch handshake. The bridge fails closed if either OS restriction cannot be established. Test each byte boundary at minus one, exact and plus one, and test incompatible/stale handshakes before data admission. Final limits remain measured outputs owned by Plans 04-05.
</resolved_transport_contract>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Negotiate and exchange bounded complete messages through the real facade</name>
  <precondition>Phase 05.3 lifecycle/publication artifacts exist and its verification reports passed.</precondition>
  <files>crates/x4-bridge/Cargo.toml, Cargo.lock, crates/x4-bridge/src/carrier_b.rs, crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/carrier_b_contract.rs</files>
  <read_first>crates/x4-bridge/src/facade.rs; crates/x4-bridge/src/protocol.rs; crates/x4-bridge/src/session.rs; crates/x4-bridge/src/server/session_gate.rs; crates/observation-ingest/src/feedback.rs; native Plan 01 public interfaces. Preserve mapped compatibility-before-generation-before-payload admission.</read_first>
  <behavior>Actual local transport negotiates a compatible session before traffic; incompatible identity requires restart; stale epochs and oversized/unknown controls reject without publication; forbidden mutation kinds are explicitly rejected.</behavior>
  <action>Add only required workspace path dependencies on native safe peer APIs, observation-application, observation-persistence and their public domain types. Implement CarrierBFacade and bounded control encode/decode with the existing exact vocabulary, session/incarnation/epoch binding, payload/capacity checks and explicit local/received/committed distinction. Use same-user OS peer access from Plan 01; a hello is not authentication. Reuse existing protocol/session contracts rather than a second compatibility policy. Preserve complete-message ordering, exact-byte copies and limits-first allocation. Exercise the actual native local pipe path with an independently received complete message and control response. Negative tests attempt forbidden operations through the public parser and assert the intended rejection, not a helper boolean. Do not add recvmsg to this path or upgrade historical dependencies; research labels it SUS. If the new path unexpectedly requires it, stop for its blocking human package-legitimacy gate before use.</action>
  <verify><automated>cargo test --locked -p x4-bridge --test carrier_b_contract</automated><fails_when>Traffic precedes compatibility, wrong peers/epochs are accepted, control data is erased by version-only decoding, or mutation input reaches application admission.</fails_when></verify>
  <done>The production facade exchanges exact bounded messages with typed control and rejection outcomes through the real local carrier.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Publish real sections and reconcile exact durable identity after interruption</name>
  <files>crates/x4-bridge/src/production.rs, crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/carrier_b_publication.rs, crates/x4-bridge/tests/carrier_b_recovery.rs, crates/x4-bridge/tests/carrier_b_support/mod.rs</files>
  <read_first>crates/observation-application/src/lifecycle.rs; crates/observation-application/src/lifecycle_publication.rs; crates/observation-application/tests/lifecycle_contract.rs; crates/observation-persistence/tests/crash_recovery.rs. These are exact lifecycle and independent-storage analogs.</read_first>
  <behavior>Start/batch/completion yields exact durable revision and receipt; local write is never committed; response loss, stale session, duplicate conflict and still-ambiguous receipt preserve last accepted state; clean restart cannot duplicate publication.</behavior>
  <action>Compose ProductionObservationSession from CarrierBFacade, ObservationLifecycle with SqliteObservationRepository, validated injected limits, and current typed source context. Map complete bytes to existing submit operations; no schema, stager, publication or reconciliation algorithm is duplicated. Emit Received only at volatile receipt and Committed only after durable result. Reconcile the exact retained attempt before admitting new collection; proven-not-committed permits only exact retained retry, still-ambiguous remains blocked within age/count limits. Per D-06, reconnect/load uncertainty discards incomplete volatile work, retires uncertain current authority to history, and requests a fresh qualifying source baseline. Restore repository-owned state with clean in-memory construction. Test actual isolated SQLite and independent reopen/query of exact revision, receipt and current pointer, not only row counts or same-serializer round trips. Include capacity, malformed/oversized bytes, out-of-order/context mismatch, changed bytes under duplicate identity, permanent rejection and ambiguous response cuts.</action>
  <verify><automated>cargo test --locked -p x4-bridge --test carrier_b_publication --test carrier_b_recovery</automated><fails_when>Publication bypasses the lifecycle, eligibility precedes durable evidence, receipt loss duplicates revision, or uncertainty is upgraded to current source authority.</fails_when></verify>
  <done>A complete observation section reaches durable publication and clean restart/reconciliation preserves exact identity without duplicate effects.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Run the independent production bridge with bounded operational history</name>
  <files>crates/x4-bridge/src/main.rs, crates/x4-bridge/src/production.rs, crates/x4-bridge/src/diagnostics.rs, crates/x4-bridge/tests/production_startup.rs</files>
  <read_first>crates/x4-bridge/src/listener.rs; crates/x4-bridge/src/server.rs; common logging/tooling references. Preserve stable operation/reason semantics, not the legacy dispatch path.</read_first>
  <behavior>Bridge-first and client-first work; missing/unwritable required journal blocks bridge startup; runtime journal failure remains visibly degraded while valid durable work continues; incompatible hello and absent peer have clear console reasons.</behavior>
  <action>Replace main's legacy listener entry with production composition. Expose explicit --data-dir and --limits-file inputs, validate finite configuration and reject unknown/invalid owned parameters. Per D-02/D-03 the bridge never starts X4 or supervises/restarts another process. Open durable storage and prove journal write acceptance before listening. Per D-07 render concise connection/collection/waiting/degradation state and reason; structured history records meaningful transitions with epoch/incarnation/batch/revision, bytes, attempts and clock-labelled durations. Bound formatting, buffering, retention and repeated events. Runtime sink failure reports bounded independent degradation and an honest history gap without waiving durable writes. Existing historical listener modules may remain compiled but cannot be selected or fallback from this entrypoint; no historical dependency is newly installed or upgraded. Tests own only their child bridge processes and isolated directories with checked deadlines/cleanup; they never operate X4.</action>
  <verify><automated>cargo test --locked -p x4-bridge --test production_startup</automated><fails_when>The production entrypoint uses the historical path, invalid config starts, a failed journal is hidden, or startup creates/supervises X4.</fails_when></verify>
  <done>The independent bridge executable operates the durable path and exposes tested bounded console/history outcomes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Contract |
| --- | --- |
| Lua to owned native module | Checked arguments, synchronous copy, no retained Lua state and no unwind |
| Native local IPC to bridge | Local peer access, explicit compatibility/epoch, complete bounded messages |
| Bridge to SQLite/current authority | Existing lifecycle, durable receipts and reconciliation |
| Product to diagnostic/package output | Safe bounded metadata and exact artifact identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.4-MEM | Tampering / DoS | Native buffers and lifetime | high | mitigate | Plan 01 bounds validation/copy, generation fences, panic containment and final completion ownership; Plan 05 verifies actual reload/load |
| T-05.4-PEER | Spoofing / Elevation | Loader and pipe | high | mitigate | Exact owned path/export; same-user local OS access and remote-client refusal; no mutation operation; Plans 01-04 negative tests |
| T-05.4-REPLAY | Tampering / Repudiation | Publication/retry | high | mitigate | Plans 02-03 preserve immutable identity/bytes, finite attempts, fresh baseline and existing exact receipt reconciliation |
| T-05.4-LOG | Information disclosure | Console/journal/evidence | medium | mitigate | Plans 02/04/05 test startup writes, bounded degraded reporting, sanitized correlation and private retained artifacts |
| T-05.4-SC | Tampering | Dependency preparation | high | mitigate | Research audits windows-sys 0.61.2 OK; no recvmsg added to production; any new package/tool preparation requires source pin and legitimacy gate |
</threat_model>
<risk_gates>
Affected layers: rust-domain and x4-runtime; no llm changes.
Selected: mandatory research, plan-check, code review and verification; security review for native memory, local peer/control authority, persistence and diagnostics; X4 probe and sustained SETA for actual package/getter/lifecycle/callback behavior; targeted mutation where changed pure safety invariants have useful operators.
Not selected: AI specification/eval (no model contract changes); custom UI design (console only); broad compatibility or AFK/model/report soak (later scope). Nyquist remains disabled unless the requirement-to-test audit exposes a concrete gap; missing scenarios must still be repaired. Native OS effects and MD are outside mutation scoring; never invent a mutation engine.
</risk_gates>
<verification>
Run all three focused bridge targets, cargo fmt --all --check, cargo run --locked -p source-size-lint -- crates, and cargo clippy --locked --workspace --all-targets --all-features -- -D warnings. Plan 04 owns final full regression.
</verification>
<success_criteria>
Compatible complete messages reach durable publication, incompatible/invalid input preserves state, restart uses exact durable evidence, and independent startup/diagnostics implement D-02/D-03/D-06/D-07 without game mutation.
</success_criteria>
<output>
Create .planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-02-SUMMARY.md with control codec/session contract, exact durable readback, interruption matrix, diagnostics behavior and commands.
</output>
