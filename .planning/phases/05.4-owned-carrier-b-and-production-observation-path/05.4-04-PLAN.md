---
phase: 05.4-owned-carrier-b-and-production-observation-path
plan: "04"
type: execute
wave: 4
depends_on: [05.4-02, 05.4-03]
files_modified:
  - tools/package-live-galaxy.ps1
  - extensions/live_galaxy/tests/x4-package-conformance.ps1
  - tests/carrier-b-local.ps1
  - extensions/live_galaxy/tests/carrier_b_local.lua
  - config/carrier-b-limits.json
  - tests/x4-disposable/05.4-carrier-b-procedure.md
  - tests/x4-disposable/05.4-carrier-b-evidence.md
autonomous: true
requirements: [OBS-01, OBS-08, VAL-06, VAL-01, VAL-02, VER-LG-001, VER-LG-002, VER-LG-011]
estimate:
  tokens: 69000
  raw_tokens: 69000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A portable bundle contains the owned native artifact, Lua/MD mod, external bridge and exact measured candidate configuration."
    - "Package tests verify actual relative layout, PE architecture/export and reject wrong or missing artifacts."
    - "The actual Lua module, DLL, local IPC, bridge and SQLite path runs with only X4 getter seams faked."
    - "Candidate numeric limits have recorded measurements and final tested artifact identity before user game acceptance."
  artifacts:
    - path: tools/package-live-galaxy.ps1
      provides: "Atomic portable bundle and integrity/identity manifest"
    - path: tests/carrier-b-local.ps1
      provides: "Actual cross-language local integration and measurement"
    - path: config/carrier-b-limits.json
      provides: "Finite measured candidate limits"
    - path: tests/x4-disposable/05.4-carrier-b-procedure.md
      provides: "Prepared bounded user-operated acceptance matrix"
  key_links:
    - from: tools/package-live-galaxy.ps1
      to: extensions/live_galaxy/lua/live_galaxy_carrier.lua
      via: "Exact extension-root native path and initializer symbol"
    - from: tests/carrier-b-local.ps1
      to: crates/x4-bridge/src/production.rs
      via: "Actual Lua/native producer to bridge and independent durable readback"
    - from: config/carrier-b-limits.json
      to: tests/x4-disposable/05.4-carrier-b-procedure.md
      via: "Measured candidate values and frozen package/config digests"
---
<objective>
Build and qualify the portable observation package per D-01/D-03/D-08/D-09/D-10/D-11. Establish measured local limits, actual producer/consumer integration and a written disposable X4 procedure before human runtime acceptance.
</objective>
<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agent-instructions/x4/MEMORY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-CONTEXT.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-RESEARCH.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-PATTERNS.md
@.agents/skills/live-galaxy-code-conventions/SKILL.md
@.agents/skills/live-galaxy-code-conventions/references/logging.md
@.agents/skills/live-galaxy-code-conventions/references/tooling.md
@.agents/skills/live-galaxy-tests/SKILL.md
@.agents/skills/live-galaxy-tests/references/test-standards.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/lua/SKILL.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-01-SUMMARY.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-02-SUMMARY.md
@.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-03-SUMMARY.md
@extensions/live_galaxy/tests/x4-package-conformance.ps1
@tools/provision-lua.ps1
@tests/x4-disposable/01-probe-procedure.md
</context>
<planning_contract>
Invocation: Phase 05.4 --granularity coarse --no-tracer. The owner explicitly approved --no-tracer after the complete-stack tracer conflicted with the five-files-per-task limit. Docs MCP establishes the native loading architecture and Phase 05.3 verifies publication; these plans integrate those proven boundaries horizontally. No global configuration change is required. Research and checked plans still require owner approval before execution.

Every behavior-producing task starts with a failing behavioral assertion, then GREEN, then bounded REFACTOR; missing imports, compile errors and zero discovery are not valid RED. Record command, intended failed assertion and result using the normal GSD TDD evidence gate. All new verification paths below are created by their owning task before use. Focused warmed checks target less than 60 seconds; cold builds are preparation and must be reported separately.

Never weaken workspace lint policy. All Rust files, including tests, remain at most 200 physical lines. The approved exception permits narrowly named helper modules within the current plan-owned crate when required for this physical limit; report that execution deviation, preserve behavior/dependencies, and do not invent generic scaffolding. Read mapped analogs as compatibility evidence, not authority over current conventions.
</planning_contract>
<tasks>
<calibration_contract>
Start local-only calibration fixtures at the resolved bring-up defaults: 2048 complete-message bytes, 512 control bytes, one occupied data slot, one bounded control/result slot, two total immutable send attempts, one non-waiting callback progress step and 5000 ms availability checks conditional on continuing callbacks. These specified defaults replace guesses from historical maximum buffers. Measure operation timeout/cancellation budget and every additional candidate work/retention bound before candidate finalization. Task 3 freezes locally measured candidate values; Plan 05 ordinary/SETA acceptance freezes production values. Select the minimum independently safe bound across Lua production, pipe behavior, Rust staging and durable commit. Any increase above a bring-up default requires boundary and stability evidence; an unsuccessful gate reduces the bound or blocks acceptance. Workerless ordinary/SETA acceptance requires no synchronous wait or unbounded callback work, Rust staged rate at least Lua produced rate, and the independent bounded-backlog invariant. Failure stops acceptance and requires replanning worker ownership/cancellation/unload/resource bounds, never an implicit worker or weaker threshold.
</calibration_contract>
<task type="auto" tdd="true">
  <name>Task 1: Produce and negatively validate the portable Windows bundle</name>
  <files>tools/package-live-galaxy.ps1, extensions/live_galaxy/tests/x4-package-conformance.ps1, tests/x4-disposable/05.4-carrier-b-procedure.md</files>
  <read_first>extensions/live_galaxy/tests/x4-package-conformance.ps1; runbooks/x4-extension-presets/x4-extension-preset.ps1 as containment/hash/atomic-output analog; RESEARCH.md source/runtime packaging ledger.</read_first>
  <behavior>Correct owned package validates; missing/wrong PE, unexpected export, wrong path/case, malformed XML, missing entrypoint, wrong protocol or Carrier A dependency fails for its intended reason; failed build preserves previous ready output.</behavior>
  <action>Create package-live-galaxy.ps1 with -SelfTest, -OutputDirectory and -LimitsFile. Build locked Windows x64 release artifacts; stage the mod, native image at .\extensions\live_galaxy\ui_c_library_live_galaxy_carrier_64.txt, external bridge executable, limits and English independent-startup instructions. Verify AMD64 PE32+ and sole export luaopen_live_galaxy_carrier with bounded PE inspection in the package tool; do not assume absent dumpbin/cl tools. Manifest records product/protocol/ABI/source revision, architecture and owned-file SHA-256. Verify actual ui.xml wrapper registration and module API path; preserve documented package.loadlib open/init/initializer distinctions. Installed support-API 1.95 .txt is precedent only; the public .dll path is version-scoped conflict and no support-API runtime dependency is shipped. Resolve/contain all targets, publish ready only after complete checked writes, preserve unrelated files and write only ignored generated output. D-03 prohibits a launcher/supervisor; D-11 leaves installation and game operation to the user. SelfTest initially uses explicit finite calibration fixtures derived from boundary sizes/Plan 01 costs; those bundles are local-only and ineligible for Plan 05. Ordinary packaging requires a finalized limits file and fails if absent. Write preparation/stop instructions including source-required protected-UI configuration; do not mutate game configuration automatically.</action>
  <verify><automated>pwsh -NoProfile -File tools/package-live-galaxy.ps1 -SelfTest</automated><fails_when>Negative package fixtures pass, production inputs are missing but silently skipped, output escapes containment, or a partial bundle is published as ready.</fails_when></verify>
  <done>Reproducible package generation and actual PE/XML/path/identity checks pass; calibration bundles remain explicitly local-only.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Run and measure the actual Lua-to-native-to-durable path</name>
  <files>tests/carrier-b-local.ps1, extensions/live_galaxy/tests/carrier_b_local.lua, tests/x4-disposable/05.4-carrier-b-procedure.md</files>
  <read_first>extensions/live_galaxy/tests/test_helper.lua and module_loading_spec.lua for strict normal-loader fixtures; crates/x4-bridge/tests/carrier_b_publication.rs and carrier_b_recovery.rs for independent readback; tools/provision-lua.ps1 for explicit preparation.</read_first>
  <behavior>Normal product modules call the actual owned native module and bridge; both startup orders, absent/slow/restarted/incompatible peers and lost responses have expected bounded outcomes; an independent reader proves exact durable publication.</behavior>
  <action>Create a bounded -SelfTest runner with explicit -LuaJitPath and -LimitsFile plus configured local tool discovery. Use a compatible explicitly prepared runtime capable of loading the actual built Lua C module; validate its ABI before the run. Existing pure Lua/Busted still owns pure/fake tests, but cannot substitute a fake native library here. If compatible tooling is missing, perform explicit pinned official-source preparation under the normal legitimacy gate; do not silently fetch on ordinary checks. Start/stop only runner-owned bridge/local peer processes; fake only X4 getter/environment seams and load the real producer/wrapper with normal extension-relative paths and package.loadlib. Exercise complete start/batch/completion, exact retry, control saturation, malformed/oversized messages, permanent/ambiguous dispositions, reconnect and independent SQLite reopen in a clean process using Plan 02's reader. Use Plan 01 finite calibration fixtures until Task 3 finalizes limits. Measure native copy/allocation/progress, serialization/GC, IPC/staging/commit, receipt roundtrip, occupancy/backlog and retry amplification; retain exact values, workload, code/config identity and durations in runner output. Avoid a permanent performance framework. Once final config exists SelfTest must consume that exact config and packaged digest, never substitute fixtures. Prepare ordinary/SETA sample windows and stop rules from local measurements; runtime costs are still pending X4.</action>
  <unload_scenario>Extend the same runner-owned files with -Scenario pending-io-unload and an explicit -LuaLibraryPath for the compatible source-pinned Lua shared runtime prepared by this task. A small local host inside the existing PowerShell runner creates a real Lua state, loads the built product through its actual package.loadlib wrapper, and holds its local peer so an operation is demonstrably pending. Capture the operation/token identity and completion-ownership diagnostics; then destroy that Lua state through the real lua_close boundary while the host process stays alive. Require terminal OS completion before any operation buffer/storage release, exactly-once close accounting, no callback into the dead state, and independent OS module inspection showing that the owned native image unloads. Create a fresh Lua state in that same host, load the image again, and assert explicit stale-generation rejection of the retained old token before accepting a fresh token. The host uses the actual Lua C API and DLL; its .NET interop is test infrastructure within the existing runner, not a new C product layer. Unexpected host exit, crash, timeout, missing pending-operation observation, absent unload, incomplete completion accounting or accepted stale token makes the scenario return nonzero. Keep finite watchdogs and terminate only runner-owned processes. Fake reset/close, clearing package.loaded without registry teardown, and whole-process exit cannot substitute for this scenario. If the available compatible harness cannot expose real Lua-state close/module-unload, report the mandatory scenario blocked and retain the candidate as unqualified until the harness is prepared; do not convert a fallback into passing evidence.</unload_scenario>
  <verify>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest</automated>
    <fails_when>The chain substitutes fake native transport, getters run while capacity is unavailable, readback lacks exact durable identity, owned subprocesses hang, or final config is silently replaced by fixtures.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario pending-io-unload</automated>
    <fails_when>Actual Lua-state teardown does not establish pending-operation terminal ownership and module unload, the host crashes/exits/times out, any required observation is missing, or an old token is accepted by the reloaded generation.</fails_when>
  </verify>
  <done>The actual cross-language local path passes with independent durable readback and retained finite-limit measurements.</done>
</task>
<task type="auto">
  <name>Task 3: Freeze the measured candidate after safety review and final local regression</name>
  <files>config/carrier-b-limits.json, tests/x4-disposable/05.4-carrier-b-procedure.md, tests/x4-disposable/05.4-carrier-b-evidence.md</files>
  <read_first>Tasks 1-2 measured outputs; docs/architecture-verification.md; project Rust and X4 mutation/test contracts.</read_first>
  <action>Only after Task 2 measurement, write finite candidate values for complete/control bytes, retained bytes/candidate work, callback work, queue/slot count, retries/elapsed budget, availability gate and diagnostics bounds. Record each measurement/source basis, exact config digest and pending runtime qualification in the existing evidence ledger. D-05's five-second target is conditional responsiveness, not evidence for copy/retry/save limits. Missing measurement blocks candidate finalization. Perform the selected focused security review of native memory/lifetime, peer controls, loader/package, control codec, exact receipt/retry and diagnostics; route substantiated fixes to their owning implementation tasks and rerun affected checks. No unresolved high security finding enters game acceptance. Run targeted mutation of changed pure result/capacity/epoch/retry transitions with a currently available pinned legitimate runner; if unavailable, explicitly prepare it or report the gate blocked. Inspect every survivor/equivalent/invalid result and derive no invented score. Lua mutation needs a bounded useful-operator feasibility result; do not build a replacement engine or score native/MD effects. Map every required scenario before acceptance. After review fixes converge rebuild the final bundle, rerun actual local integration against exact final config/package and run one final full Rust/Lua regression including strict format/lint/size checks. If candidate values change, repeat affected measurements/checks and freeze a new digest; no document edit can bless untested binaries.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <fails_when>Any workspace Rust source differs from required formatting.</fails_when>
    <automated>cargo clippy --locked --workspace --all-targets --all-features -- -D warnings</automated>
    <fails_when>Any required workspace lint or warning is emitted.</fails_when>
    <automated>cargo run --locked -p source-size-lint -- crates</automated>
    <fails_when>Any Rust source exceeds the repository physical-line limit.</fails_when>
    <automated>cargo test --locked --workspace</automated>
    <fails_when>Any required workspace behavior test fails or is unintentionally skipped.</fails_when>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite all</automated>
    <fails_when>A required Lua, XML, loader, certificate or persistence-schema test fails or is missing.</fails_when>
    <automated>pwsh -NoProfile -File tools/package-live-galaxy.ps1 -SelfTest -LimitsFile config/carrier-b-limits.json</automated>
    <fails_when>The actual final package/config fails layout, export, identity or negative-fixture validation.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -LimitsFile config/carrier-b-limits.json</automated>
    <fails_when>Final checks fail or skip required cases, config lacks measured basis, exact tested digests differ, or a mandatory safety/mutation finding remains undispositioned.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario pending-io-unload -LimitsFile config/carrier-b-limits.json</automated>
    <fails_when>The real pending-I/O teardown/unload scenario fails or skips any ownership, unload or stale-generation assertion on the exact final package/config.</fails_when>
  </verify>
  <done>A measured, reviewed and locally verified exact candidate bundle plus written user procedure is ready for Plan 05; runtime acceptance remains unclaimed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Contract |
| --- | --- |
| Lua to owned native module | Checked arguments, synchronous copy, no retained Lua state and no unwind |
| Native local IPC to bridge | Local peer access, explicit compatibility/epoch, complete bounded messages |
| Bridge to SQLite/current authority | Existing lifecycle, durable receipts and reconciliation |
| Product to diagnostic/package output | Safe bounded metadata and exact artifact identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.4-MEM | Tampering / DoS | Native buffers and lifetime | high | mitigate | Plan 01 bounds validation/copy, generation fences, panic containment and final completion ownership; Plan 05 verifies actual reload/load |
| T-05.4-PEER | Spoofing / Elevation | Loader and pipe | high | mitigate | Exact owned path/export; same-user local OS access and remote-client refusal; no mutation operation; Plans 01-04 negative tests |
| T-05.4-REPLAY | Tampering / Repudiation | Publication/retry | high | mitigate | Plans 02-03 preserve immutable identity/bytes, finite attempts, fresh baseline and existing exact receipt reconciliation |
| T-05.4-LOG | Information disclosure | Console/journal/evidence | medium | mitigate | Plans 02/04/05 test startup writes, bounded degraded reporting, sanitized correlation and private retained artifacts |
| T-05.4-SC | Tampering | Dependency preparation | high | mitigate | Research audits windows-sys 0.61.2 OK; no recvmsg added to production; any new package/tool preparation requires source pin and legitimacy gate |
</threat_model>
<risk_gates>
Affected layers: rust-domain and x4-runtime; no llm changes.
Selected: mandatory research, plan-check, code review and verification; security review for native memory, local peer/control authority, persistence and diagnostics; X4 probe and sustained SETA for actual package/getter/lifecycle/callback behavior; targeted mutation where changed pure safety invariants have useful operators.
Not selected: AI specification/eval (no model contract changes); custom UI design (console only); broad compatibility or AFK/model/report soak (later scope). Nyquist remains disabled unless the requirement-to-test audit exposes a concrete gap; missing scenarios must still be repaired. Native OS effects and MD are outside mutation scoring; never invent a mutation engine.
</risk_gates>
<verification>
Task 3 owns the phase's one final full local regression after review fixes. Keep focused tests during iteration. Mandatory X4 and SETA evidence cannot be replaced by this plan's local success.
</verification>
<success_criteria>
Portable output is validated, the real local producer/consumer chain reaches durable storage, all candidate limits have explicit evidence, security and mutation findings are dispositioned, and exact candidate identity is frozen for user-operated acceptance.
</success_criteria>
<output>
Create .planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-04-SUMMARY.md recording final package/config digests, local measurements, commands/results, safety/mutation disposition and pending X4 matrix.
</output>
