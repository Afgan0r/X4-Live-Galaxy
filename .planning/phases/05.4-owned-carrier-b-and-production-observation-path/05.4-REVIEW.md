---
phase: 05.4-owned-carrier-b-and-production-observation-path
reviewed: 2026-09-07T23:34:41Z
depth: deep
reviewed_revision: a99ef58c62c0b261acf9a17646e2f0c227eb0e5b
diff_base: ea76b0895c9fb840c3532d416ec516308554b8a7
files_reviewed: 115
files_reviewed_list:
  - Cargo.lock
  - Cargo.toml
  - config/carrier-b-limits.json
  - crates/observation-application/src/lifecycle.rs
  - crates/observation-application/tests/certificate_contract.rs
  - crates/observation-application/tests/support.rs
  - crates/observation-application/tests/support/hydration.rs
  - crates/observation-application/tests/support/versioned.rs
  - crates/observation-domain/src/identity.rs
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/src/observation.rs
  - crates/observation-domain/src/section_state.rs
  - crates/observation-domain/src/sender_evidence.rs
  - crates/observation-ingest/src/accepted_versions_tests.rs
  - crates/observation-ingest/src/batch_budget.rs
  - crates/observation-ingest/src/batch_canonical.rs
  - crates/observation-ingest/src/candidate_limits.rs
  - crates/observation-ingest/src/carrier_control.rs
  - crates/observation-ingest/src/carrier_control_types.rs
  - crates/observation-ingest/src/completion.rs
  - crates/observation-ingest/src/completion_digest.rs
  - crates/observation-ingest/src/completion_types.rs
  - crates/observation-ingest/src/generation.rs
  - crates/observation-ingest/src/hydration_validation.rs
  - crates/observation-ingest/src/legacy_candidate.rs
  - crates/observation-ingest/src/legacy_generation.rs
  - crates/observation-ingest/src/lib.rs
  - crates/observation-ingest/src/producer_completion.rs
  - crates/observation-ingest/src/sender_evidence.rs
  - crates/observation-ingest/src/wire.rs
  - crates/observation-ingest/src/wire_decode.rs
  - crates/observation-ingest/src/wire_encode.rs
  - crates/observation-ingest/tests/completion_contract.rs
  - crates/observation-ingest/tests/completion_contract/coverage.rs
  - crates/observation-ingest/tests/completion_contract/rejections.rs
  - crates/observation-ingest/tests/contract_lifecycle.rs
  - crates/observation-ingest/tests/keyed_candidates.rs
  - crates/observation-ingest/tests/session_fencing.rs
  - crates/observation-ingest/tests/wire_v2_contract.rs
  - crates/observation-persistence/src/context.rs
  - crates/observation-persistence/src/lib.rs
  - crates/observation-persistence/src/record.rs
  - crates/observation-persistence/src/schema.rs
  - crates/observation-persistence/tests/support/revisions.rs
  - crates/x4-bridge/Cargo.toml
  - crates/x4-bridge/src/carrier_b.rs
  - crates/x4-bridge/src/carrier_b_codec.rs
  - crates/x4-bridge/src/diagnostics.rs
  - crates/x4-bridge/src/lib.rs
  - crates/x4-bridge/src/main.rs
  - crates/x4-bridge/src/production.rs
  - crates/x4-bridge/src/production_limits.rs
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/production_runtime_message.rs
  - crates/x4-bridge/src/production_startup.rs
  - crates/x4-bridge/src/receiver_context.rs
  - crates/x4-bridge/tests/carrier_b_contract.rs
  - crates/x4-bridge/tests/carrier_b_publication.rs
  - crates/x4-bridge/tests/carrier_b_recovery.rs
  - crates/x4-bridge/tests/carrier_b_support/mod.rs
  - crates/x4-bridge/tests/carrier_b_support/repository.rs
  - crates/x4-bridge/tests/production_startup.rs
  - crates/x4-bridge/tests/production_startup/actual_pipe.rs
  - crates/x4-bridge/tests/production_startup/support.rs
  - crates/x4-bridge/tests/receiver_context_contract.rs
  - crates/x4-carrier-native/Cargo.toml
  - crates/x4-carrier-native/src/abi.rs
  - crates/x4-carrier-native/src/abi_windows.rs
  - crates/x4-carrier-native/src/abi_windows_access.rs
  - crates/x4-carrier-native/src/abi_windows_io.rs
  - crates/x4-carrier-native/src/abi_windows_peer.rs
  - crates/x4-carrier-native/src/abi_windows_security.rs
  - crates/x4-carrier-native/src/abi_windows_sid.rs
  - crates/x4-carrier-native/src/handle.rs
  - crates/x4-carrier-native/src/lib.rs
  - crates/x4-carrier-native/src/lua_generation.rs
  - crates/x4-carrier-native/src/lua_input.rs
  - crates/x4-carrier-native/src/lua_open.rs
  - crates/x4-carrier-native/src/lua_operations.rs
  - crates/x4-carrier-native/src/lua_producer_operations.rs
  - crates/x4-carrier-native/src/lua_progress.rs
  - crates/x4-carrier-native/src/lua_table.rs
  - crates/x4-carrier-native/src/lua_transport.rs
  - crates/x4-carrier-native/src/producer.rs
  - crates/x4-carrier-native/src/producer_collection.rs
  - crates/x4-carrier-native/src/producer_feedback.rs
  - crates/x4-carrier-native/src/producer_message.rs
  - crates/x4-carrier-native/src/producer_types.rs
  - crates/x4-carrier-native/src/transport.rs
  - crates/x4-carrier-native/src/transport_peer.rs
  - crates/x4-carrier-native/src/transport_types.rs
  - crates/x4-carrier-native/src/transport_worker.rs
  - crates/x4-carrier-native/src/types.rs
  - crates/x4-carrier-native/tests/abi_contract.rs
  - crates/x4-carrier-native/tests/lifecycle_contract.rs
  - crates/x4-carrier-native/tests/producer_contract.rs
  - crates/x4-carrier-native/tests/producer_contract/support.rs
  - crates/x4-carrier-native/tests/producer_receipt_contract.rs
  - docs/ARCHITECTURE.md
  - docs/architecture-decisions.md
  - extensions/live_galaxy/content.xml
  - extensions/live_galaxy/lua/live_galaxy_carrier.lua
  - extensions/live_galaxy/lua/live_galaxy_observation.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_scheduler.lua
  - extensions/live_galaxy/md/live_galaxy_observation.xml
  - extensions/live_galaxy/tests/carrier_b_contract.lua
  - extensions/live_galaxy/tests/carrier_b_local.lua
  - extensions/live_galaxy/tests/run_contracts.ps1
  - extensions/live_galaxy/tests/x4-package-conformance.ps1
  - extensions/live_galaxy/ui.xml
  - tests/carrier-b-local.ps1
  - tests/x4-disposable/05.4-carrier-b-evidence.md
  - tests/x4-disposable/05.4-carrier-b-procedure.md
  - tools/package-live-galaxy.ps1
findings:
  critical: 10
  warning: 10
  info: 0
  total: 20
status: issues_found
---

# Phase 05.4: Code Review Report

**Reviewed:** 2026-09-07T23:34:41Z
**Depth:** deep
**Files Reviewed:** 115
**Status:** issues_found

## Summary

The exact reviewed revision builds and its existing automated suites pass, but
the submitted production path is not ready for the human X4 checkpoint. Ten
release-blocking defects break the real callback, burst control delivery,
bridge restart, session lifetime, retry-age accounting, unload proof, or the
mandatory diagnostic bound. Ten additional test and diagnostic defects create
false-green evidence or make failures unactionable.

No X4 runtime, SETA, installation, save, or hot-reload claim is made. The human
Plan 05 evidence is correctly pending, but it cannot safely start until the P1
findings below are fixed and locally requalified.

## Narrative Findings (AI reviewer)

## Critical Issues

### CR-01: The real MD event can never supply the context required by the scheduler

**Classification:** BLOCKER (P1)

**File:** `extensions/live_galaxy/md/live_galaxy_observation.xml:18`; `extensions/live_galaxy/lua/live_galaxy_runtime.lua:20`; `extensions/live_galaxy/lua/live_galaxy_scheduler.lua:13`; `extensions/live_galaxy/lua/live_galaxy_observation.lua:27-28,60-61`

**Trigger:** The MD cue raises the Lua event with the literal string
`telemetry_tick`, while the registered callback accepts one argument and passes
it as a table containing decimal `capture_start_millis` and
`capture_end_millis` fields.

**Rule/evidence:** D-12 permits a thin X4 callback/getter adapter, and Phase 05.4
requires one real `GetCurRealTime` observation. The actual MD payload is not the
synthetic table injected by `carrier_b_contract.lua` and
`carrier_b_local.lua`.

**Consequence:** Every real tick returns `clock_unavailable` before reserving or
capturing a fact, so the phase's core X4-to-SQLite observation cannot occur.

**Fix:** Define and test the exact X4 `RegisterEvent` callback signature. Supply
bounded capture evidence from the actual callback/game clock seam (or collect it
through the allowed getter seam), and drive the registered callback with the
same event arguments in a contract test.

### CR-02: A burst of compatibility controls is silently discarded

**Classification:** BLOCKER (P1)

**File:** `crates/x4-carrier-native/src/transport_worker.rs:101-115`; `crates/x4-bridge/src/production_runtime.rs:59-78`; `crates/x4-bridge/tests/production_startup/actual_pipe.rs:88-97`

**Trigger:** The bridge sends handshake, collection intent, and demand back to
back. The worker continuously reads them into a capacity-one channel, ignores a
`TrySendError::Full`, drops the completed read, and immediately begins another.
The test drains each control between sends and therefore removes the production
race.

**Rule/evidence:** D-12's typed state machine requires the exact ordered
compatibility/intent/demand sequence; bounded capacity may backpressure but may
not lose admitted control state.

**Consequence:** Intent or demand can disappear, leaving the producer permanently
stuck before `Ready` even though both peers report a compatible session.

**Fix:** Retain a completed control message until Lua consumes the channel slot,
or stop issuing reads while the slot is occupied. Add an actual-pipe test that
sends all three controls before any Lua-side poll and proves ordered delivery.

### CR-03: A compatible bridge restart cannot reconnect to the native server

**Classification:** BLOCKER (P1)

**File:** `crates/x4-carrier-native/src/transport_worker.rs:41-69,81-115`; `crates/x4-carrier-native/src/abi_windows_io.rs:69-83,145-149`

**Trigger:** When the bridge client closes, read/write polling becomes failed.
The worker clears only the individual operation; it never clears `connected`,
calls `DisconnectNamedPipe`, or enters `begin_connect` again. `begin_connect`
is executed only once at worker startup.

**Rule/evidence:** ADR-LG-008 and Phase D-05 explicitly require a compatible
external bridge to restart independently without restarting X4.

**Consequence:** The native pipe remains in a broken connected instance and a
replacement bridge cannot establish a new session. Recovery requires a
game-facing Lua/DLL restart, contrary to the accepted ownership boundary.

**Fix:** Add an explicit connected/disconnected/reconnecting worker state. On
terminal peer I/O, cancel and drain all operations, disconnect the server pipe,
advance/reset the session fence as designed, and issue a fresh overlapped
connect. Prove kill-and-restart with the actual DLL and bridge process.

### CR-04: Message age is measured from connection start and kills valid long-lived sessions

**Classification:** BLOCKER (P1)

**File:** `crates/x4-bridge/src/production_runtime.rs:81-86`; `config/carrier-b-limits.json:8`; `extensions/live_galaxy/md/live_galaxy_observation.xml:16`

**Trigger:** `connected_at` is captured once, and every later message is rejected
after five seconds of total connection lifetime. The game callback interval is
30 seconds.

**Rule/evidence:** The finite age limit belongs to an in-flight candidate/message,
not the lifetime of a healthy transport session. Availability and reconnect are
separate limits.

**Consequence:** Unless a sample happens within the first five seconds, the first
real observation is rejected. Every healthy session is forcibly terminated no
later than its next post-five-second message.

**Fix:** Remove transport-session age from `serve`. Let the receiver lifecycle
enforce candidate age from its own admitted start time, and implement the
separate inactivity deadline against the last transport progress if required.
Add a >5-second connected-session test with a later valid message.

### CR-05: Retry age starts at zero for production messages

**Classification:** BLOCKER (P1)

**File:** `crates/x4-carrier-native/src/lua_operations.rs:96`; `crates/x4-carrier-native/src/producer_feedback.rs:129`; `crates/x4-carrier-native/src/producer.rs:72,99-107`

**Trigger:** Production constructs the bootstrap with `now_millis = 0`, and each
successor batch/completion pending item is also constructed at zero.
`mark_local_handoff` retains the minimum of zero and the real monotonic clock.
Any `capacity_unavailable` received after host uptime exceeds 5 seconds is
therefore treated as older than the retry budget.

**Rule/evidence:** D-04 permits the exact retained retry up to two attempts and
five seconds when non-consumption is proven. Existing tests use a synthetic
clock starting at zero and miss the production epoch mismatch.

**Consequence:** Bootstrap, batch, and completion lose their permitted retry in
normal process uptime and pause immediately after the first capacity response.

**Fix:** Initialize each `Pending.first_attempt_at` from the transport monotonic
clock at actual creation/handoff; pass `now` while advancing messages. Add a test
whose first clock value is well above 5,000 ms and exercises all three message
stages.

### CR-06: The mandatory pending-I/O unload proof does not create or assert pending I/O

**Classification:** BLOCKER (P1)

**File:** `extensions/live_galaxy/tests/carrier_b_local.lua:49-56`; `tests/carrier-b-local.ps1:93-117,175-203`

**Trigger:** The harness kills the bridge before calling `carrier:progress(1)`,
ignores that call's result, then writes `actual_native=true` and `sampled=true`
unconditionally. The host checks eventual module unload and token fencing, but
never observes a pending OS operation, owner count, retained pending message, or
terminal completion before storage release.

**Rule/evidence:** Plan 04 makes a real pending operation at `lua_close` a
mandatory lifetime gate. A broken pipe commonly makes the post-kill operation
fail synchronously, which is not the required scenario.

**Consequence:** The suite can report
`PENDING_IO_TERMINAL_AND_MODULE_UNLOADED` without testing the use-after-unload
boundary it claims to qualify.

**Fix:** Establish and inspect a genuinely pending connect/read/write before
closing the Lua state, assert nonzero operation ownership and exact pending
producer identity, then prove cancellation terminality, zero owners, unload,
and stale-token rejection. Do not derive pass fields from constants.

### CR-07: Operational history is unbounded and can exhaust durable storage

**Classification:** BLOCKER (P1)

**File:** `crates/x4-bridge/src/diagnostics.rs:43-78`; `crates/x4-bridge/src/production_runtime.rs:27,51,55,76,80,85,89,98,100`

**Trigger:** Every wait/reject/disposition transition is appended and flushed to
one JSONL file with no byte/count/age bound, rotation, retention, aggregation,
or repeated-event rate limit.

**Rule/evidence:** LOG-04 and ADR-LG-006 require diagnostic streams to have
independent finite resource bounds.

**Consequence:** Normal long-running operation or a reconnect/rejection loop can
grow the file until disk exhaustion, compromising the adjacent SQLite store and
recovery path.

**Fix:** Add an explicit bounded retention/rotation policy plus bounded repeated
event aggregation and lost/suppressed counters. Test the exact boundary and
retained-history behavior.

### CR-08: Both reset paths leave a session that cannot bootstrap again

**Classification:** BLOCKER (P1)

**File:** `crates/x4-carrier-native/src/lua_operations.rs:114-117`; `crates/x4-carrier-native/src/lua_transport.rs:22-43`; `crates/x4-carrier-native/src/producer_feedback.rs:55-60`; `extensions/live_galaxy/lua/live_galaxy_carrier.lua:61`

**Trigger:** Lua `reset` requests transport shutdown, resets only registry queue
fields, and clears the producer while leaving the wrapper open. Subsequent calls
return transport/producer unavailable. A received typed `Reset` likewise clears
the current pending bootstrap and enters `AwaitingCompatibility` without
creating a replacement bootstrap message.

**Rule/evidence:** Reset/reconnect is an explicit ABI-2 operation and D-05
requires bounded recovery with fresh generation fencing.

**Consequence:** Neither local nor peer-requested reset can reach a new handshake;
the only recovery is an undocumented full wrapper/state recreation.

**Fix:** Make reset atomically construct a fresh generation, transport, producer,
and bootstrap, or define it as terminal close and require `carrier.new()`.
Whichever contract is selected, test the complete reset-to-compatible sequence
and stale-token rejection.

### CR-09: A stalled peer bypasses candidate age and inactivity limits

**Classification:** BLOCKER (P1)

**File:** `crates/x4-bridge/src/production_runtime.rs:81-100`; `crates/x4-carrier-native/src/transport_peer.rs:30-40`; `crates/x4-carrier-native/src/abi_windows_peer.rs:57-76`; `crates/observation-ingest/src/candidate_limits.rs:126-140`

**Trigger:** Bridge `receive` is a blocking `ReadFile`. After a `SectionStart`, a
peer can remain connected and send nothing; `serve` never regains control to
check a timer, and production never calls `expire_candidates`.
`max_message_inactivity_millis` is parsed but not used by the runtime.

**Rule/evidence:** D-06 and ADR-LG-006 require finite candidate age, inactivity,
retained capacity, and work under every failure path.

**Consequence:** One stalled local peer retains the candidate and aggregate slot
indefinitely and prevents recovery or fresh collection.

**Fix:** Use bounded overlapped/nonblocking peer reads or a timed wait loop,
expire the active candidate during idle periods, close/reset the session at the
inactivity boundary, and test a start-then-stall case with exact capacity
release.

### CR-10: A failed native write drops handed-off bytes without feedback

**Classification:** BLOCKER (P1)

**File:** `crates/x4-carrier-native/src/transport_worker.rs:119-128`; `crates/x4-carrier-native/src/lua_producer_operations.rs:92-102`

**Trigger:** Lua marks the message locally handed off when it enters the worker
channel. If `begin_write` then fails, the worker drops the owned `Vec<u8>` and
only clears `data_busy`; it sends no failure to the producer and does not requeue
the immutable bytes.

**Rule/evidence:** Local handoff is explicitly not receiver `received`; exact
bytes must remain retryable until an unambiguous disposition.

**Consequence:** The producer retains a handed-off pending message that
`pending_bytes()` will no longer expose, so no retry or terminal transition is
possible. Collection deadlocks with the only transport copy lost.

**Fix:** Retain/requeue the exact buffer until write completion or surface a
typed terminal/non-consumption result back to the producer that makes the same
bytes retryable under the finite retry budget. Add synchronous and asynchronous
write-failure tests.

## Warnings

### WR-01: The default actual suite silently substitutes calibration limits

**Classification:** WARNING (P2)

**File:** `extensions/live_galaxy/tests/run_contracts.ps1:31-35`; `tests/carrier-b-local.ps1:37-40,159-160,210-217`

**Issue:** `-Suite all` invokes the local runner without `-LimitsFile`, so it
writes a duplicate compact JSON fixture. The observed hash is
`51f50da5...`, not the frozen production hash `440a55d8...`.

**Fix:** Always pass `config/carrier-b-limits.json` after finalization and assert
the expected frozen/package digest; reserve generated calibration input for an
explicit pre-finalization mode.

### WR-02: Actual-path readback assertions do not prove the typed publication

**Classification:** WARNING (P2)

**File:** `crates/x4-bridge/tests/production_startup/actual_pipe.rs:98-112`; `tests/carrier-b-local.ps1:205-207`

**Issue:** The checks assert three disposition names, revision `1`, and getter
count only. Wrong/empty record content, getter, semantics, message identity,
digest, or receipt can pass.

**Fix:** Independently parse readback and assert the exact section, record ID,
entity, observation version, getter/raw value/semantics content, receipt, and no
extra current/history row.

### WR-03: Required actual-path rejection and recovery scenarios are absent

**Classification:** WARNING (P2)

**File:** `tests/carrier-b-local.ps1:4-10`; `extensions/live_galaxy/tests/run_contracts.ps1:31-35`; `crates/x4-bridge/tests/production_startup/actual_pipe.rs:83-107`

**Issue:** The real DLL/pipe suite covers only a happy chain and nominal unload.
It does not exercise version 1/unknown/mismatch, forbidden `decoded_bytes`, a
control burst/full slot, exact retry after caller mutation, permanent/ambiguous
feedback, malformed/oversized traffic, or a killed-and-restarted bridge.

**Fix:** Add table-driven actual-process modes for these boundaries and assert
the precise disposition, immutable bytes, getter count, generation fence,
diagnostic outcome, and independent durable state.

### WR-04: Journal write failure is invisible after startup

**Classification:** WARNING (P2)

**File:** `crates/x4-bridge/src/diagnostics.rs:70-84`; `crates/x4-bridge/src/production_runtime.rs:27,51,55,76,80,85,89,98,100`

**Issue:** `record` sets `sink = None` and `gap_reported = true`, but all runtime
callers discard the return and nothing exposes `has_history_gap` through an
independent bounded emergency/status channel.

**Fix:** Surface one bounded operator-visible degradation event, retain an honest
gap counter/status, and test failure plus recovery without recursive logging.

### WR-05: Operational records lack the identity and cause needed for diagnosis

**Classification:** WARNING (P2)

**File:** `crates/x4-bridge/src/diagnostics.rs:64-68`; `crates/x4-bridge/src/production_runtime.rs:47-100`

**Issue:** Events contain only `state` and `reason`; `admit` erases every cause
into `Err(())` and records `whole-message`. There is no clock, component,
session/epoch, message/section/revision, attempt, or specific rejection cause.

**Fix:** Use a typed bounded event schema with stable safe identity, clock
meaning, outcome, and closed reason codes; preserve typed error causes to the
final event owner.

### WR-06: Terminal producer states and operator status are collapsed

**Classification:** WARNING (P2)

**File:** `crates/x4-carrier-native/src/lua_progress.rs:14-37`; `extensions/live_galaxy/lua/live_galaxy_carrier.lua:45-50`; `extensions/live_galaxy/lua/live_galaxy_scheduler.lua:3,21-26`; `extensions/live_galaxy/lua/live_galaxy_runtime.lua:30-33`; `crates/x4-bridge/src/main.rs:3-7`

**Issue:** Lua keeps only one native detail, maps codes 6-10 to one
`producer_terminal` value, and the bridge emits no authoritative current
waiting/collection/disconnected/degraded status.

**Fix:** Preserve the bounded native fields, map every terminal outcome to a
stable reason, and expose one concise current-status channel separate from the
detailed journal.

### WR-07: Raw callback exceptions are copied into X4 diagnostics

**Classification:** WARNING (P2)

**File:** `extensions/live_galaxy/lua/live_galaxy_runtime.lua:11-17,24-28`

**Issue:** `tostring(detail):sub(1,64)` is not redaction. Lua errors can include
local paths, source locations, control characters, or runtime content.

**Fix:** Log only a closed allowlisted error class and sanitize bounded safe
fields. Add path/control-character redaction tests.

### WR-08: Package conformance does not enforce the no-mutation/no-Carrier-A contract

**Classification:** WARNING (P2)

**File:** `extensions/live_galaxy/tests/x4-package-conformance.ps1:45-63`

**Issue:** The check validates XML shape, dependencies, and entrypoint but never
rejects mutation MD actions or support-API/Carrier-A references inside product
Lua/MD files.

**Fix:** Add structural forbidden-node/reference checks and negative fixtures
whose precise rejection proves the intended contract.

### WR-09: Ambiguous recovery test asserts an enum, not exact durable recovery

**Classification:** WARNING (P2)

**File:** `crates/x4-bridge/tests/carrier_b_recovery.rs:34-54`

**Issue:** `response_loss_reconciles_the_exact_committed_attempt` checks only
`ReconcileResult::Committed`; wrong content/identity/receipt or duplicate
publication can remain undetected.

**Fix:** Reopen the independent repository and assert exact current/history
identity, content digest, receipt, and row count after reconciliation.

### WR-10: The Lua fake normalizes malformed native results into success

**Classification:** WARNING (P3)

**File:** `extensions/live_galaxy/tests/carrier_b_contract.lua:13-24`

**Issue:** `return result or 0` converts `false` into successful numeric zero,
and no case injects false/string/fractional operation codes.

**Fix:** Preserve explicitly supplied fake results unchanged and add malformed
result cases proving the wrapper fails closed with `result_shape`.

## Rejected Candidates and Dispositions

- The removed `decoded_bytes` field and v2 wire/certificate canonicalization
  were traced through encode, decode, receipt, persistence, and recovery. No
  bypass was found; direct negative tests are effective at the codec boundary.
- Same-logon DACL construction plus `PIPE_REJECT_REMOTE_CLIENTS` is present. The
  anonymous access probe is narrower than a full adversarial token matrix, but
  no concrete ACL bypass was proven from this revision.
- `readback_revision` returns record content intentionally through an explicit
  offline developer command; it was not treated as a public diagnostic leak.
- `unsafe` usage is confined to the native ABI/Win32 boundary and the worker
  retains a module lease. The defect is the unproven pending-I/O test, not a
  source-proven use-after-free on the reviewed happy path.
- Human X4, SETA, game reload, and installed-package observations are missing by
  design and remain pending Plan 05 evidence, not code-review findings.
- The stale heavy-ship/station phase wording in ADR-LG-023 predates the reviewed
  changed lines; it is a documentation reconciliation candidate, not attributed
  to this implementation review.
- Performance-only observations were excluded unless they also created an
  explicit finite-resource correctness or data-loss risk.

## Specialist Outcomes

- `phase054_bugs` (GPT-5.6 Luna/high): six correctness candidates; all were
  independently re-traced and accepted directly or consolidated into
  CR-02/03/05/08/09/10.
- `phase054_tests` (GPT-5.6 Luna/high): eight false-green candidates; six were
  accepted directly or consolidated into WR-01/02/03/08/09/10.
- `phase054_logging` (GPT-5.6 Luna/high): six diagnostic candidates; the
  unbounded journal was promoted to CR-07, and the remaining grounded issues
  were accepted or consolidated into WR-04/05/06/07.

## Verification Evidence

- Git freshness: `origin` fetched with prune; local and remote reviewed branch
  both resolve to `a99ef58c62c0b261acf9a17646e2f0c227eb0e5b`, ahead/behind `0/0`.
- Preserved unrelated orchestrator dirt only:
  `.planning/STATE.md`, `.planning/config.json`, `.planning/state.json`.
- `ast-index stats` then `ast-index update` completed before structural queries.
- `cargo fmt --all --check` — passed.
- `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings`
  — passed.
- `cargo run --locked -p source-size-lint` — passed.
- `cargo test --locked --workspace` — passed; five pre-existing deferred tests
  remain ignored in `named_pipe_contract`.
- `extensions/live_galaxy/tests/run_contracts.ps1 -Suite all` — passed, while
  reproducing WR-01's non-production config hash and the false-green boundaries
  described above.
- `tools/package-live-galaxy.ps1 -SelfTest -LimitsFile
  config/carrier-b-limits.json` — passed.

## Mandatory Context Read

The lead and specialists read the complete applicable instruction chain:
`AGENTS.md`, `.agent-instructions/x4/AGENTS.md`,
`.agent-instructions/x4/MEMORY.md`, `game-repo-standard/SKILL.md` and its
`profiles-and-registry.md` and `memory-lifecycle.md` references; all named Live
Galaxy review, conventions, logging, tooling, common test, Rust, Rust-test,
X4-integration, Lua/MD, X4-test, and Lua skill files; Phase 05.4 `CONTEXT`,
amended `RESEARCH`, `PATTERNS`, Plans 01-05, Summaries 01-04;
`docs/ARCHITECTURE.md`, `docs/architecture-decisions.md`, and
`docs/architecture-verification.md`; every file in `files_reviewed_list`; and
the relevant lifecycle, feedback, session, pipe, package, and test callers.

The shared X4 memory contract was followed. Relevant `wing_x4_modding`
decision drawers for the Phase 05.4 owner discussion and D-12/D-13 producer
boundary were recalled and checked against repository evidence.

---

_Reviewed: 2026-09-07T23:34:41Z_
_Reviewer: gsd-code-reviewer lead_
_Depth: deep_
