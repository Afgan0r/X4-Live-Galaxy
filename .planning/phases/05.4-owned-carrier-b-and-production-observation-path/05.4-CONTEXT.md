# Phase 05.4: Owned Carrier B and Production Observation Path - Context

**Created:** 2026-09-04
**Gathered:** 2026-09-06
**Status:** Discussion decisions approved; ready for mandatory research and checked planning. Execution is not approved.
**Authority:** The Phase 05.3 accepted context, ADR-LG-001 through ADR-LG-024, the 2026-09-04 Docs MCP publication, the approved carrier split and Rust/FFI clarification, and the owner's decisions in this discussion.

<domain>

## Phase Boundary

Connect X4 to the completed Phase 05.3 generic observation contracts through
a minimal Live Galaxy-owned carrier. Deliver the Windows package, independent
startup, bounded observation/control transport, recovery and console diagnostics,
then prove a small real observation flow through durable publication in X4.

X4 remains authoritative. This phase adds no game-state mutation, new model
behavior, public-readiness claim, web interface, or joint game/bridge launcher.
Heavy ship and station observation coverage retain their separate phase owners.

</domain>

<decisions>

## Locked decisions from the split

- Phase 05.3 remains source-agnostic and owns generic Rust contracts, validation and durable publication.
- This phase owns the minimal Live Galaxy-controlled production carrier and the X4-to-Rust observation path behind the Phase 05.3 carrier-neutral facade.
- Carrier B is a minimal Rust/C native library with a Lua-facing boundary. It is not a general plugin platform and does not own observation policy, publication policy or strategic behavior.
- Carrier B replaces `sn_mod_support_apis` in the production runtime path. Carrier A remains research and comparison evidence, not a required Live Galaxy dependency or an automatic runtime fallback.
- The owned DLL is implemented in Rust and exposes a minimal C ABI that X4's LuaJIT runtime calls through FFI. "C ABI" defines the binary calling contract; it does not require a separate C implementation layer unless later research proves one necessary.
- The intended boundary is `X4 UI Lua -> LuaJIT FFI -> exported C ABI -> Rust carrier implementation -> local IPC/Rust ingestion`. Exact exported functions and result codes remain research and discussion decisions.
- Lua owns X4 getters, cooperative scheduling and final admission of each native step. The DLL receives only bounded caller-owned buffers and returns bounded typed transport outcomes; it does not move X4 getter ownership into Rust.
- The DLL copies accepted input synchronously into memory it owns before retaining or processing it. Background Rust work may use only DLL-owned memory and must not retain LuaJIT/X4 pointers or call Lua/X4 APIs.
- The FFI boundary catches panics and failures and must never unwind into X4. Any required `unsafe` Rust is confined to the smallest reviewed ABI boundary for pointer validation and copying.
- The transport exposes no fleet, economy, diplomacy, institution or other game-state mutation command.
- Complete-message, connection, capacity, send, receive, control-poll, duplicate, permanent rejection and ambiguous outcomes remain distinct and bounded.
- Retry preserves the exact immutable batch identity and bytes. A local native write is not a volatile `received` acknowledgement or durable `committed` receipt.
- Carrier A (`sn_mod_support_apis`) is retained as research evidence and may not be presented as a safe fallback unless new executable evidence closes its bounded nonblocking and typed-result gaps.
- Automatic save pause/resume is unsupported under current evidence. A numeric cooldown must not be invented from the historical implementation.
- Heavy faction-ship conformance belongs to Phase 05.5 and the conditional station remainder to Phase 05.6.

## Decisions from the Dedicated Discussion

### Distribution and startup

- **D-01:** Deliver a coherent portable Windows package containing the mod with
  its owned DLL and the external Rust bridge. Preserve the agreed Rust C ABI
  boundary; no additional C implementation layer is selected.
- **D-02:** X4 and the bridge start independently, in either order, and connect
  automatically. The user starts X4. A compatible external bridge may restart
  without restarting X4; changes to game-facing code or incompatible game-side
  protocol revisions retain the existing restart boundary.
- **D-03:** Joint startup/shutdown automation is deferred to public installation
  design. No personal shortcut, launcher, process watcher, or automatic bridge
  process supervisor is a required Phase 05.4 deliverable.

### Failure recovery

- **D-04:** After a temporary transport failure exhausts the current send
  retries, stop the failed transfer and pause collection. Enter bounded
  availability checking rather than an immediate resend/rescan loop. After
  reconnection and compatibility confirmation, start fresh collection.
- **D-05:** Target one availability check every **five seconds of real time**.
  With continuing game callbacks, reconnection should normally be detected
  within that interval. Pause/load have no five-second guarantee. This is an
  owner-approved responsiveness target, not a measured safe production limit:
  research must establish check cost, scheduling feasibility and final values.
- **D-06:** Automatic connection recovery does not restart a crashed bridge
  process, waive version incompatibility, or prove a source boundary. Preserve
  exact immutable retry within its valid scope, discard incomplete work at
  reconnect, reconcile durable receipts and never duplicate publication.
  Start/load/reload uncertainty requires a fresh qualifying baseline; previous
  accepted revisions remain history-only across an uncertain source boundary.

### Overload and diagnostics

- **D-07:** Expose a concise console status for connection, collection, waiting
  and degradation with its reason. Retain detailed structured developer history
  under the existing logging contract. A bridge web interface is deferred in
  `SEED-001`, not admitted as an HTTP/API or frontend task here.
- **D-08:** Preserve Phase 05.3 backpressure and freshness rules: pump before
  collection, reserve capacity before producing work, pause at a bounded
  continuation and retain immutable pending bytes. Never repeat getters for a
  transport retry. Independent resource limits remain finite; stale required
  observations block dependent decisions rather than being reported as fresh.
  This is an inherited contract, not a newly selected overload algorithm.

### Disposable X4 acceptance

- **D-09:** Prove a small real observation flow from X4 through the owned DLL
  and Rust ingestion to independently verified durable publication. Include
  reconnection, load/UI reload and sustained SETA as well as ordinary time.
  Local fake success and synthetic transport traffic alone cannot close this
  real observation acceptance boundary.
- **D-10:** The minimum acceptance setup is vanilla X4 plus DLC and Live Galaxy
  with necessary dependencies. Broader coexistence testing with other supported
  mods belongs to later stages; public compatibility obligations are preserved.
  Necessary dependencies do not reintroduce Carrier A as a runtime fallback.
- **D-11:** The user performs every in-game action under a written disposable
  verification plan. Agents may prepare instructions and interpret retained
  evidence; they never start/control X4 or read/modify saves.

### Research-owned implementation choices

Exact ABI exports/results, IPC implementation, package layout and native loading,
resource/handle lifecycle, reconnect/reset handshake, numeric safety limits,
console rendering and the concrete small observation example belong to research
and checked planning within these decisions. The same applies to measured
normal-time/SETA workload, duration and acceptance thresholds. No ABI function
set, crate dependency or arbitrary queue/payload default is selected here.

Conflicts that change accepted architecture, behavior or scope return to the
owner. In particular, research must reconcile older architecture passages about
Carrier A and save cooldown with the newer accepted split and unsupported-save
boundary; it must not restore obsolete runtime policy from old code or prose.

</decisions>

## Historical Evidence that Triggered Carrier B

The registered Docs MCP publication combined the base X4 and support-API snapshots with:

- `carrier-a-message-mode-source-20260903-v2`;
- `save-boundary-source-20260903-v1`;
- `x4-probe-md-pulse-a70af158-v1`.

It established the following planning constraints:

1. Carrier A is conditionally message-oriented only when the server is `PIPE_TYPE_MESSAGE` and `PIPE_NOWAIT` is installed successfully. Its wrapper does not expose the `SetNamedPipeHandleState` result, collapses zero and hard failures, and does not length-check positive counts. The required bounded nonblocking behavior and retry classification therefore cannot be proven.
2. The selected instantiated 50 ms MD cue is a best-effort game-time pump. It is not a hard deadline or real-time service guarantee. Load and UI reload recreate Lua and invalidate old pipe state.
3. First-party save completion and availability shapes exist, but no proven save-begin or durability boundary supports automatic resume.

This evidence is carried from the accepted split and Phase 05.3 readiness
record. This discussion performed no new X4 probe or Docs MCP verification.
Research must repeat the relevant registered requests and inspect complete
pagination before relying on current runtime/API claims.

## Required Research and Owner Gate

Resolve the exact exported ABI functions and result codes within the locked
ownership boundary, Windows packaging and loading, process/handle lifecycle,
message and aggregate queue bounds, reconnect/reset handshake, failure mapping,
and the smallest disposable X4 proof. Any required unsafe Rust belongs only in
the dedicated reviewed ABI boundary with its safety contract and focused tests;
the safe domain and persistence crates do not acquire unsafe code.

Research must show how the five-second availability-check target coexists with
finite send-retry budgets and game callback admission. Discovery checks are not
permission to renew failed-transfer budgets indefinitely or resume ambiguous
publication as if it were acknowledged. Automatic save pause/resume remains
unsupported; no save cooldown is selected by the reconnection target.

The owner reviews research and checked plans before execution. The phase does
not broaden installed-game, filesystem or runtime mutation authority. Keep the
approved Phase 05.4 planner-model trial from `.planning/config.json` and
`.planning/STATE.md`; do not silently extend it to another phase.

## Required gates

Affected layers: `rust-domain` and `x4-runtime`; no `llm` change is selected.

- Research, plan-check, code review and verification.
- Security review for native input, control authority, memory ownership and persistence boundaries.
- Rust and X4/Lua test contracts, including static, pure-Lua, fake-adapter and disposable runtime evidence.
- X4 probe and SETA evidence for delivery, lifecycle, reconnect and measured callback work.
- Mutation testing where deterministic validation or safety invariants can be weakened without ordinary tests failing.
- Nyquist validation only if planning exposes a requirement-to-test coverage gap.

AI specification/eval and UI design gates are not selected because this phase changes neither model-visible behavior nor a custom interface.

<canonical_refs>

## Canonical References

Downstream agents must read these before research, planning or implementation:

- `.planning/PROJECT.md` — public direction, prototype scope and restart boundary.
- `.planning/REQUIREMENTS.md` — OBS-01/08, VAL-01/02/06 and successor ownership.
- `.planning/ROADMAP.md` — 05.4 outcome and deferred source/compatibility stages.
- `.planning/STATE.md` and `.planning/config.json` — current workflow and the
  Phase 05.4-only planner-model trial.
- `.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-CONTEXT.md`
  — inherited generic scheduling, feedback, publication and recovery contracts.
- `.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-READINESS.md`
  — exact historical Docs MCP requests, snapshots and residual unknowns.
- `docs/ARCHITECTURE.md`, `docs/architecture-decisions.md` and
  `docs/architecture-verification.md` — target contracts, decisions and evidence
  boundaries, reconciled against newer accepted phase decisions.
- `.agent-instructions/x4/AGENTS.md` and `.agent-instructions/x4/MEMORY.md`
  — Docs MCP lookup/handoff, human game operation, memory and Git boundaries.
- `.planning/seeds/SEED-001-bridge-web-interface.md` — future milestone idea only.

Applicable project skills and every required rule reference remain mandatory
through the root `AGENTS.md`; this list does not replace their loading contract.

</canonical_refs>

<code_context>

## Existing Code Insights

### Reusable assets and integration points

- `crates/x4-bridge/src/facade.rs` defines `ObservationCarrierFacade`, complete
  send/control-poll outcomes and connection epochs. Adapt Carrier B behind this
  seam; local handoff is explicitly distinct from receiver publication.
- `crates/observation-ingest/src/lib.rs` exports `StopAndWaitSlot`, immutable
  application batches, `GenerationStager`, `ObservationScheduler`, completion
  validation and decision eligibility. Reuse their generic authority instead
  of moving policy into the DLL.
- `crates/observation-persistence/src/lib.rs` exports `ObservationRepository`,
  `SqliteObservationRepository`, publication outcomes and recovery contracts.
  The real observation proof must reach this durable boundary.
- `extensions/live_galaxy/lua/live_galaxy_scheduler.lua` is a historical adapter
  candidate, not proof of current admission policy: it samples before enqueue,
  uses a fixed single-slice bound and consumes `save_in_progress`. Research must
  reconcile it with pump-first/reservation and unsupported-save requirements.

### Evidence limits

The scout inspected current source on the milestone branch after remote
freshness was established. Existing exports and local patterns establish reuse
candidates, not production DLL loading, nonblocking operation or X4 safety.
No source implementation changed during this discussion.

</code_context>

<specifics>

## Specific Ideas

The owner rejected a personal joint-launch shortcut as the current phase's
distribution solution because the application must eventually support a public
installation. The selected baseline keeps startup independent. A web interface
was explicitly reserved for a separate future milestone, while console status
was accepted for this phase.

</specifics>

<deferred>

## Deferred Ideas

- Joint game/bridge startup and shutdown automation belongs to later public
  installation design; no launcher or watcher commitment is made now.
- Bridge web interface: canonical capture is
  `.planning/seeds/SEED-001-bridge-web-interface.md` (`SEED-001`). Its features,
  stack, API, milestone number and delivery date remain unselected.
- Broader supported-mod compatibility proof remains a later-stage obligation.

Multiple in-flight batches, dynamic credit, durable partial staging, generalized native extension infrastructure, mutation commands, report delivery, heavy observation coverage and full unattended soak remain outside this phase unless the owner admits them through their owning later phase.

</deferred>
