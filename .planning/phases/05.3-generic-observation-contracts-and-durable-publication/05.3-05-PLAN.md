---
phase: 05.3-generic-observation-contracts-and-durable-publication
plan: 05
type: execute
wave: 4
depends_on:
  - 05.3-03
files_modified:
  - crates/observation-ingest/src/scheduler.rs
  - crates/observation-ingest/src/scheduler_queue.rs
  - crates/observation-ingest/src/scheduler_budget.rs
  - crates/observation-ingest/src/lib.rs
  - crates/observation-ingest/tests/scheduler_policy.rs
autonomous: true
gap_closure: true
requirements:
  - OBS-02
  - OBS-03
  - OBS-07
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Core collection intents rank before deferrable detail; within a class, greater game-time urgency ranks first and exact ties preserve stable FIFO order."
    - "Changing only game-time urgency can change which queued intent is selected but cannot add real-time permits, step work, heavy permits, terminal capacity, or debt repayment."
    - "Every pulse pumps pending immutable bytes first, leaves the configured terminal reserve unused by bulk traffic, and begins no collection unless downstream capacity remains after that reserve."
    - "Declared work above the per-step allowance is rejected; admitted work consumes the exact finite step budget and one heavy permit when applicable."
    - "Heavy permits remain finite until the matching admitted intent completes; duplicate or unknown completion cannot release a second permit."
    - "Indivisible actual-work overrun creates bounded debt; real-time refill repays debt before creating permits, by at most one step-work allowance per pulse, so frozen and coarse clocks cannot trigger catch-up."
    - "Debt at the exact limit remains observable and blocks new collection until repaid; one-over overrun rejects the collector fail closed without wrapping or granting work."
  prohibitions:
    - statement: "Deterministic pulse and urgency tests must not be represented as X4 cadence, SETA, save/load, carrier-pressure, or game-thread performance proof."
      status: resolved
      verification: judgment
    - statement: "The scheduler must not acquire a carrier, invoke X4 getters, or define production numeric budgets that belong to measured Phase 05.4/05.5 evidence."
      status: resolved
      verification: judgment
  artifacts:
    - path: "crates/observation-ingest/src/scheduler_queue.rs"
      provides: "Bounded stable core/detail and game-time-urgency ordering"
    - path: "crates/observation-ingest/src/scheduler_budget.rs"
      provides: "Finite real-time permit, step-work, heavy-permit, terminal-reserve, and overrun-debt accounting"
    - path: "crates/observation-ingest/tests/scheduler_policy.rs"
      provides: "Exact/one-over boundary evidence under frozen and coarse injected clocks"
  key_links:
    - from: "crates/observation-ingest/src/scheduler_queue.rs"
      to: "crates/observation-ingest/src/scheduler.rs"
      via: "The scheduler selects the stable highest-priority eligible intent without using urgency as budget"
      pattern: "CollectionIntent|CollectionClass|game_time_urgency"
    - from: "crates/observation-ingest/src/scheduler_budget.rs"
      to: "crates/observation-ingest/src/scheduler.rs"
      via: "Every admission and completion updates checked permits, work, heavy in-flight state, and bounded debt"
      pattern: "SchedulerBudget|OverrunDebt|HeavyPermit"
    - from: "crates/observation-ingest/src/scheduler.rs"
      to: "crates/observation-ingest/tests/scheduler_policy.rs"
      via: "Observable outcomes expose selected intent, pumped bytes, reserved terminal bytes, work consumed, heavy permits, and debt"
      pattern: "SchedulerOutcome|SchedulerAdmission"
---

<objective>
Complete the accepted scheduler policy so every exposed limit constrains observable behavior and game-time urgency remains ordering-only.

Purpose: Close the second 05.3 verification gap with deterministic, bounded policy that Phase 05.4 can adapt without deriving capacity from SETA or a runtime cue.
Output: Stable urgency queueing, finite step/heavy admission, terminal-reserved pump behavior, bounded overrun debt, and exact boundary tests under injected clocks.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-CONTEXT.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-VERIFICATION.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-01-SUMMARY.md

<interfaces>
Existing surface to correct, not duplicate:
- `CollectionPolicyLimits::new(refill_millis, burst, step_work, heavy_permits)` already guarantees finite non-zero values.
- `TransportPolicyLimits::new(max_pump_bytes, terminal_reserve)` already guarantees finite non-zero values.
- `DeliveredPulse`, `SchedulerOutcome`, and `ObservationScheduler<C: MonotonicClock>` are public but currently expose only pumped bytes and a boolean collection start.
- `ObservationScheduler::stage_pending` owns one immutable pending byte vector and `deliver_pulse` already pumps before collection.

New exact policy surface:
- `CollectionIntentId`, `CollectionClass::{Core, Detail}`, `WorkKind::{Light, Heavy}`, and `CollectionIntent` with declared work and game-time urgency.
- `SchedulerSafetyLimits` with injected non-zero queue capacity and maximum overrun debt; no `Default`.
- `SchedulerAdmission` and expanded `SchedulerOutcome` exposing selected intent, admitted work, remaining heavy permits, terminal reserve, and current debt.
- `ObservationScheduler::{enqueue, complete}` for checked admission completion and idempotent heavy-permit release.
</interfaces>
</context>

<gap_root_cause>
Plan deficiency plus execution miss: the original scheduler task named urgency, step work, heavy permits, terminal reserve, and debt, but its test contract asserted only refill and pump-first booleans. The implementation therefore left urgency unused and treated three limits as constructor tautologies. This plan makes every dimension observable and boundary-tested.
</gap_root_cause>

<file_ownership>
This plan exclusively owns the five listed `observation-ingest` scheduler files in Wave 4. Plan 05.3-04 waits for these finalized public exports because both plans touch `observation-ingest/src/lib.rs`, then consumes the scheduler with the other lower-level contracts in the complete application lifecycle. This plan does not edit the root workspace or the new application crate.
</file_ownership>

## Artifacts this phase produces

- New paths: `crates/observation-ingest/src/scheduler_queue.rs`, `crates/observation-ingest/src/scheduler_budget.rs`, `crates/observation-ingest/tests/scheduler_policy.rs`.
- New symbols: `CollectionIntentId`, `CollectionClass`, `WorkKind`, `CollectionIntent`, `SchedulerSafetyLimits`, `SchedulerAdmission`, `CompletionDisposition`, `SchedulerBudget`, and `OverrunDebt`.
- No production policy value, carrier, X4 runtime adapter, Lua/XML path, database artifact, UI, or model-facing symbol is produced.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace urgency-ranked collection through pump-first reserved admission</name>
  <reversibility rating="costly">The scheduler outcome and intent types are the compile contract that the Phase 05.4 runtime adapter will consume.</reversibility>
  <read_first>
    - crates/observation-ingest/src/feedback.rs
    - crates/observation-ingest/src/scheduler.rs
    - crates/observation-ingest/src/lib.rs
    - crates/observation-ingest/tests/contract_lifecycle.rs
    - .planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-VERIFICATION.md
  </read_first>
  <files>crates/observation-ingest/src/scheduler.rs, crates/observation-ingest/src/scheduler_queue.rs, crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/scheduler_policy.rs</files>
  <behavior>
    - RED: Two otherwise equal queued intents with different urgency currently cannot produce an observable stable selection.
    - GREEN: Core precedes detail, then urgency descending, then insertion ordinal ascending; changing urgency changes selection only.
    - GREEN: Pending bulk bytes pump before any intent, bulk pump leaves `terminal_reserve` bytes unused, and insufficient post-reserve capacity preserves the exact pending bytes and admits no work.
    - GREEN: With identical monotonic time and capacity, low and maximum urgency produce identical permit/work/heavy/reserve totals.
  </behavior>
  <tdd_cycle>
    - RED: Add `urgency_orders_without_increasing_allowance` first; run `cargo test --locked -p observation-ingest --test scheduler_policy urgency_orders_without_increasing_allowance -- --exact` and record its failure for the absent urgency ordering/reserve behavior before production edits.
    - GREEN: Implement the single pump-first urgency-ranked admission path, then rerun `cargo test --locked -p observation-ingest --test scheduler_policy urgency_orders_without_increasing_allowance -- --exact` and require it to pass.
    - REFACTOR: Extract stable queue ownership and narrow public exports without changing the traced behavior, then rerun `cargo test --locked -p observation-ingest --test scheduler_policy urgency_orders_without_increasing_allowance -- --exact` and require it to remain green.
  </tdd_cycle>
  <action>Start with the named failing integration test and record RED, then split stable queue ordering into `scheduler_queue.rs` before `scheduler.rs` exceeds 200 physical lines. Follow the mapped `05.3-PATTERNS.md` analog `crates/x4-bridge/src/session.rs:28-114`: preserve its finite typed state transitions across a session boundary through `admit_hello`, `reconnect`, and `accept_sequence`, including typed-generation increment, sequence reset, and terminal incompatible state, while intentionally superseding that session-only admission policy with the independent urgency/work/heavy/reserve/debt policy. Replace the pulse's boolean capacity with an exact finite available-byte value and expose game-time urgency only on `CollectionIntent`; use `CollectionClass` to put core identity/ownership/location work before details, then descending urgency and stable FIFO for ties. Bound queue length through injected `SchedulerSafetyLimits` and reject excess without evicting an accepted intent. In `deliver_pulse`, refill from monotonic real time, pump the existing immutable pending bytes first, and calculate bulk capacity only after subtracting `TransportPolicyLimits::terminal_reserve`; if the pending complete message cannot fit within the per-pulse pump cap and remaining non-terminal capacity, retain it unchanged and admit no collection. Select at most one eligible intent per pulse and return an observable outcome; do not call a getter, split an immutable message, invent X4 cadence, or assign production numeric defaults.</action>
  <acceptance_criteria>
    - The named RED test fails because urgency ordering/reserve behavior is absent, then passes after implementation.
    - Priority order is total, deterministic, and stable for exact ties.
    - Game-time urgency is absent from every permit/refill/work/debt calculation.
    - Exact reserve and one-byte-short capacity cases retain pending bytes and start no collection.
    - All new public values are re-exported deliberately from `lib.rs` and every source file remains within 200 physical lines.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --locked -p observation-ingest --test scheduler_policy urgency_orders_without_increasing_allowance -- --exact</automated>
  </verify>
  <done>One deterministic pulse proves urgency-only ordering, pump-first immutable transport, terminal reserve, and unchanged real-time allowance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact step, heavy-permit, and bounded debt repayment</name>
  <reversibility rating="costly">Debt and completion semantics affect future adapter admission but remain replaceable behind the scheduler's typed interface.</reversibility>
  <read_first>
    - crates/observation-ingest/src/scheduler.rs
    - crates/observation-ingest/src/scheduler_queue.rs
    - crates/observation-ingest/src/feedback.rs
    - crates/observation-ingest/tests/scheduler_policy.rs
    - .agents/skills/live-galaxy-rust-tests/SKILL.md
  </read_first>
  <files>crates/observation-ingest/src/scheduler.rs, crates/observation-ingest/src/scheduler_budget.rs, crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/scheduler_policy.rs</files>
  <behavior>
    - Test 1: Declared work at the step limit admits and consumes exactly that amount; one-over rejects without consuming a real-time permit or queue entry.
    - Test 2: Heavy intents admit only while a permit is available; exact matching completion releases once, duplicate completion is idempotent, and unknown/conflicting completion releases nothing.
    - Test 3: Actual indivisible work above the declared step creates exact overrun debt up to the configured limit; one-over returns collector rejection and never wraps or grants a permit.
    - Test 4: A frozen clock repays no debt or permits; after a coarse real-time jump, one pulse repays at most `step_work`, creates no permit until debt is zero, and cannot loop through missed intervals to catch up.
    - Test 5: Burst, step work, heavy permits, terminal reserve, queue count, and debt pass exact/one-over tables independently, with all observable counters unchanged after rejection.
  </behavior>
  <tdd_cycle>
    - RED: Add the three named budget/debt tests first; run `cargo test --locked -p observation-ingest --test scheduler_policy step_and_heavy_limits_are_exact_and_completion_is_idempotent -- --exact`, `cargo test --locked -p observation-ingest --test scheduler_policy overrun_debt_repayment_is_bounded_under_frozen_and_coarse_clocks -- --exact`, and `cargo test --locked -p observation-ingest --test scheduler_policy policy_dimensions_reject_one_over_without_state_change -- --exact`, and record that all three fail before production edits.
    - GREEN: Implement the exact budget, completion, and debt state transitions; rerun `cargo test --locked -p observation-ingest --test scheduler_policy step_and_heavy_limits_are_exact_and_completion_is_idempotent -- --exact`, `cargo test --locked -p observation-ingest --test scheduler_policy overrun_debt_repayment_is_bounded_under_frozen_and_coarse_clocks -- --exact`, and `cargo test --locked -p observation-ingest --test scheduler_policy policy_dimensions_reject_one_over_without_state_change -- --exact`, and require every test to pass.
    - REFACTOR: Consolidate checked arithmetic in `scheduler_budget.rs` and cleanup scheduler delegation without changing outcomes; rerun `cargo test --locked -p observation-ingest --test scheduler_policy step_and_heavy_limits_are_exact_and_completion_is_idempotent -- --exact`, `cargo test --locked -p observation-ingest --test scheduler_policy overrun_debt_repayment_is_bounded_under_frozen_and_coarse_clocks -- --exact`, and `cargo test --locked -p observation-ingest --test scheduler_policy policy_dimensions_reject_one_over_without_state_change -- --exact`, then run `cargo test --locked -p observation-ingest --test scheduler_policy` as the post-refactor target regression.
  </tdd_cycle>
  <action>Write the exact/one-over table and frozen/coarse-clock failures before implementation. Continue following the mapped `05.3-PATTERNS.md` analog `crates/x4-bridge/src/session.rs:28-114`: preserve its finite typed state transitions and explicit session-boundary rejection/reset behavior, while intentionally superseding its generation/sequence-only admission with independent urgency ordering, declared-work accounting, heavy permits, terminal reserve, and bounded overrun debt. Move checked permit, in-flight heavy identity, per-pulse work, and debt arithmetic into `scheduler_budget.rs`; use checked operations and typed outcomes, not booleans. One admitted intent consumes one real-time permit and its declared work from the current pulse allowance; a heavy intent also reserves one heavy permit until `complete` receives the exact admitted identity. Completion records actual indivisible work: add only the positive actual-minus-declared overrun to debt, cap state at the injected maximum, and return `CompletionDisposition::CollectorRejected` when the new total would exceed it. On each later pulse, derive elapsed refill quanta from injected monotonic real time, repay debt before adding permits, and cap repayment to the lesser of available quanta, current debt, and one `step_work` allowance; never iterate missed intervals. Do not advance `last_refill` beyond accounted quanta, double-release heavy capacity, or let rejected work disappear from observability. Keep fixture values explicit and unrelated to retired 129/64/1800/FIFO/buffer values.</action>
  <acceptance_criteria>
    - Every configured dimension changes at least one behavioral result and has exact/one-over coverage.
    - Frozen clocks preserve debt and permit counts; coarse clocks cannot admit proportional catch-up work.
    - Debt is finite, visible, repaid before permit creation, and never wraps.
    - Heavy in-flight state is identity-bound and duplicate-safe.
    - The full scheduler policy target contains no sleeps, wall-clock access, X4 calls, or fabricated runtime measurements.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --locked -p observation-ingest --test scheduler_policy</automated>
  </verify>
  <done>Step work, heavy permits, terminal reserve, burst/refill, queue capacity, and overrun debt are independently finite, observable, and exact under frozen and coarse clocks.</done>
</task>

</tasks>

<risk_gates>
| Gate | Disposition | Evidence boundary |
| --- | --- | --- |
| Layer | `rust-domain` only | Pure scheduling policy over abstract delivered pulses and injected clocks. |
| Code review | Selected | Mandatory; inspect stable ordering, checked accounting, exact/one-over behavior, and non-catch-up semantics. |
| Verification | Selected | Mandatory focused scheduler target here; terminal Plan 05.3-04 owns the single full workspace regression after all revised contracts exist. |
| Security review | Not separately selected | No new external boundary or authority is added; resource-exhaustion invariants remain covered by mandatory review and tests. |
| Rust mutation | Assigned once to terminal Plan 05.3-04 | Do not establish a scheduler-only baseline here; the terminal gate measures the completed scheduler/slot/finalization/application transition surface and invents no threshold. |
| X4 probe / SETA runtime proof | Rejected | Abstract policy cannot prove delivered cue timing, carrier pressure, game-thread load, or SETA behavior; 05.4 owns those gates. |
| UI / AI specification / eval | Rejected | No frontend or model-visible behavior changes. |
| Nyquist | Not selected | The new exact boundary target directly closes the verifier's named coverage gap. |
</risk_gates>

<source_audit>
| Source | Item | Coverage | Status | Notes |
| --- | --- | --- | --- | --- |
| GOAL | Independently bounded collection/transport policy | Tasks 1-2 | COVERED | Observable scheduler behavior replaces inert fields. |
| REQ | OBS-02 | Existing 01-03 plus Task 1 | COVERED | Stable typed intent identity and ordering do not alter observation identity semantics. |
| REQ | OBS-03 | Tasks 1-2 | COVERED | Starvation/debt remain observable and cannot be hidden by urgency-driven extra work. |
| REQ | OBS-07 | Tasks 1-2 | COVERED | Saturation, overrun, duplicate completion, and capacity failure reject without corrupting scheduler or pending bytes. |
| RESEARCH | Urgency-only ordering, finite step/heavy/reserve, frozen/coarse clocks, bounded debt | Tasks 1-2 | COVERED | Every policy dimension receives exact behavior tests. |
| CONTEXT | Pump first; real time controls admission; no catch-up; reserve terminal capacity | Tasks 1-2 | COVERED | Values remain injected and non-production. |
| CONTEXT | Actual X4 cadence, SETA/load behavior, Carrier B, save boundary | none | EXCLUDED | Explicit Phase 05.4 scope. |
</source_audit>

<verification>
1. `cargo test --locked -p observation-ingest --test scheduler_policy`
2. `cargo test --locked -p observation-ingest scheduler`
3. `cargo fmt --all --check`
4. `cargo run --locked -p source-size-lint -- crates`
5. `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings`
6. `git diff --check`

Plan 05.3-04 owns the one terminal full workspace regression and mutation availability/baseline gate after all scheduler, slot, finalization, and application state machines exist. Do not repeat either gate here.
</verification>

<success_criteria>
- Game-time urgency affects only deterministic selection order and never increases a real-time resource allowance.
- Every previously inert policy field constrains and reports behavior.
- Frozen and coarse clocks prove bounded refill/debt repayment without proportional catch-up.
- Pending bytes remain immutable and terminal reserve remains unavailable to bulk work at exact boundaries.
- All Rust files remain at or below 200 physical lines and the focused format, size, Clippy, and diff checks pass; terminal Plan 05.3-04 owns the final full regression.
- No production carrier, X4 runtime, SETA, save/load, UI, model, or mutation-command scope is introduced or claimed.
</success_criteria>

<output>
Create `.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-05-SUMMARY.md` with the exact urgency order, admission/debt state machine, boundary matrix, focused verification, terminal-gate assignment to Plan 05.3-04, and explicit statement that results are deterministic local Rust evidence only.
</output>
