---
phase: 05.3-generic-observation-contracts-and-durable-publication
plan: 03
subsystem: observation-persistence
tags: [rust, sqlite, atomic-publication, crash-recovery, retention]
requires:
  - phase: 05.3-02
    provides: immutable validated section revisions, frozen dependencies, and canonical decision revision sets
provides:
  - shared fake and SQLite observation publication contract with exact replay and CAS outcomes
  - atomic immutable revision, terminal receipt, and current-pointer publication
  - ambiguous commit reconciliation against durable receipt and current-pointer state
  - durable decision pins and deterministic bounded retention preserving current and pinned reachability
affects: [05.4, production-observation-carrier, decision-recovery, persistence]
actuals:
  tokens: 22461
  tasks: 3
  commits: 6
tech-stack:
  added: [rusqlite 0.40.2 with bundled SQLite]
  patterns: [immediate SQLite transactions, exact durable replay, fail-closed reconciliation, pin-aware retention]
key-files:
  created:
    - crates/observation-persistence/src/port.rs
    - crates/observation-persistence/src/sqlite.rs
    - crates/observation-persistence/src/recovery.rs
    - crates/observation-persistence/src/retention.rs
    - crates/observation-persistence/tests/port_contract.rs
    - crates/observation-persistence/tests/crash_recovery.rs
  modified:
    - Cargo.toml
    - Cargo.lock
key-decisions:
  - "Durable publication uses one immediate transaction for immutable content, terminal receipt, and conditional current-pointer advancement."
  - "Unknown commit results and post-commit response loss remain ambiguous until exact durable receipt and pointer reconciliation."
  - "Retention preserves all current, dependency-reachable, and decision-pinned revisions; unpinning only removes reachability."
patterns-established:
  - "Every SQLite open verifies foreign-key enforcement, schema identity, foreign-key integrity, canonical content digests, and receipt reachability before serving repository operations."
  - "Repository retries distinguish exact committed replay, proven not committed, and still ambiguous outcomes without inferring authority from transport failure."
requirements-completed: [OBS-02, OBS-03, OBS-07]
coverage:
  - id: D1
    description: Fake and file-backed SQLite repositories share exact publish, replay, conflict, CAS, and decision-pin behavior.
    requirement: OBS-07
    verification:
      - kind: integration
        ref: cargo test -p observation-persistence --test port_contract --locked
        status: pass
    human_judgment: false
  - id: D2
    description: File-backed crash cuts expose only complete old or new revisions and require durable reconciliation after ambiguous outcomes.
    requirement: OBS-07
    verification:
      - kind: integration
        ref: cargo test -p observation-persistence --test crash_recovery --locked
        status: pass
    human_judgment: false
  - id: D3
    description: Schema, foreign-key, digest, receipt, and bounded-retention invariants fail closed while preserving current and pinned evidence.
    requirement: OBS-03
    verification:
      - kind: integration
        ref: cargo test -p observation-persistence --test schema_contract --locked
        status: pass
      - kind: integration
        ref: cargo test -p observation-persistence --test crash_recovery --locked
        status: pass
    human_judgment: false
duration: 17h 17m wall elapsed (resumed execution)
completed: 2026-09-04
status: complete
---

# Phase 05.3 Plan 03: Durable Observation Publication and Recovery Summary

**Validated observation revisions now publish through one fake/SQLite contract with atomic receipts and pointers, exact ambiguous-outcome reconciliation, durable decision pins, and bounded retention.**

## Performance

- **Duration:** 17h 17m wall elapsed across interrupted and resumed execution
- **Started:** 2026-09-03T22:33:02Z
- **Completed:** 2026-09-04T15:50:27Z
- **Tasks:** 3
- **Files modified:** 19

## Accomplishments

- Added an observation-specific repository port and deterministic fake with exact replay, changed-content conflict, stale pointer/dependency, permanent rejection, and decision pin/load/unpin outcomes.
- Added file-backed SQLite publication that commits immutable normalized records, terminal receipt, and conditional current pointer in one immediate transaction after foreign-key and schema validation.
- Added deterministic crash failpoints, ambiguous commit reconciliation, corruption rejection, and finite pin-aware retention that never deletes current or retained decision evidence.

## Task Commits

1. **Task 1 RED: failing observation port tracer** — `2c0cb73`
2. **Task 1 GREEN: observation repository port and fake** — `e2aee76`
3. **Task 2 RED: failing shared SQLite port contract** — `86d75fc`
4. **Task 2 GREEN: atomic SQLite observation publication** — `1bf8077`
5. **Task 3 RED: failing recovery and retention contracts** — `4178705`
6. **Task 3 GREEN: crash recovery, reconciliation, retention, and mechanical source split** — `c2a8226`

## Interfaces Delivered

- `ObservationRepository` owns publication, current/history reads, durable decision pin creation/loading, and receipt-CAS unpinning.
- `SqliteObservationRepository::publish_with_failpoint` exposes deterministic pre-commit and ambiguous response cuts for file-backed tests without weakening the normal publish path.
- `reconcile_publication` returns only `CommittedReplay`, `ProvenNotCommitted`, or bounded `Ambiguous` after validating stored integrity and exact durable receipt/current-pointer state.
- `RetentionPolicy` requires injected non-zero history and receipt limits; deterministic collection preserves current, dependency-reachable, and decision-pinned revisions.

## Verification

- `cargo test -p observation-persistence --lib port::tests --locked` — 3 passed.
- `cargo test -p observation-persistence --test port_contract --locked` — 1 passed.
- `cargo test -p observation-persistence --test schema_contract --locked` — 4 passed.
- `cargo test -p observation-persistence --test crash_recovery --locked` — 4 passed.
- `cargo test -p observation-persistence --locked` — all tests passed.
- `cargo fmt --all --check` — passed.
- `cargo run -p source-size-lint --locked -- crates` — passed; every Rust source file is at or below 200 physical lines.
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` — passed without command-line suppression.
- `cargo test --workspace --locked` — passed; five pre-existing deferred X4 tests remained ignored and were not changed by this plan.
- `git diff --check` — passed.
- `cargo mutants --version` — unavailable (`cargo: no such command: mutants`), so the conditional reviewed mutation run was not executed and no score was inferred.

## Security Findings

- Stored tampering fails closed through exact schema/protocol identity, canonical digest validation, verified foreign keys, `foreign_key_check`, receipt reachability, and uniqueness constraints.
- Exact replay returns the original durable receipt; changed content, stale pointer, and stale dependencies cannot advance current authority.
- A commit call whose outcome cannot be proven blocks retry until exact durable reconciliation; no transport failure is promoted into publication authority.
- Retention is finite and deterministic and cannot delete current or decision-pinned content, receipts, or dependency reachability.
- Repository diagnostics remain bounded typed codes without raw payloads, private paths, prompts, or hidden reasoning.

No new security-relevant surface was introduced outside the plan threat model.

## Deviations from Plan

### Owner-Approved Mechanical Source Split

`sqlite_write.rs` reached 204 physical lines after Task 3 failpoint work. The owner standing instruction authorized a narrowly named helper module when needed solely for the 200-line rule. Existing row insertion and current-pointer SQL moved mechanically to `sqlite_write_rows.rs`; behavior, dependencies, public contracts, architecture, and product scope did not change.

### Auto-Fixed Clippy Gate

**[Rule 3 - Blocking] Made the new integration-test fixtures satisfy the workspace all-target Clippy contract.**

- **Found during:** Task 3 final verification
- **Issue:** Test-only immediate-failure fixtures from Tasks 1-3 triggered the workspace `expect_used`, `unwrap_used`, `panic`, and fixture-subset `dead_code` policies.
- **Fix:** Applied the existing repository pattern of reasoned test-only lint expectations, made map defaults explicit, and changed the failpoint test helper to borrow its request.
- **Files modified:** `tests/support.rs`, `tests/port_contract.rs`, `tests/schema_contract.rs`, `tests/crash_recovery.rs`, `src/sqlite.rs`
- **Verification:** all-target/all-feature Clippy passed with `-D warnings`; focused and full workspace tests passed.
- **Committed in:** `c2a8226`

**Total deviations:** 2 (one owner-approved mechanical responsibility split, one blocking verification fix). No product scope or architecture changed.

## Ownership Anomaly

`.planning/phases/05.4-owned-carrier-b-and-production-observation-path/05.4-CONTEXT.md` remained unexpectedly modified outside Plan 05.3-03 ownership. It was not edited, staged, committed, reverted, or otherwise changed by this execution. The orchestrator-owned `.planning/config.json` modification was likewise preserved untouched.

## Known Stubs

None.

## Skill Learning

Skill learning: none. Evidence considered: the recovered Task 3 GREEN diff, the mechanical source split, Clippy findings, focused crash/schema/port tests, full workspace tests, and the unavailable mutation runner. The reusable 200-line, reasoned test-only lint, deterministic recovery, and bounded retention rules are already explicit in `live-galaxy-rust-conventions` and `live-galaxy-rust-tests`; this execution did not establish a missing or defective project-skill rule.

## Next Phase Readiness

Phase 05.4 can consume durable publication outcomes without treating local handoff or an ambiguous response as committed authority. It remains responsible for the separately approved Carrier B native/Lua production path and X4 runtime evidence.

The requirements handler left `OBS-02`, `OBS-03`, and `OBS-07` unchecked because the requirement registry classifies their successor acceptance as pending. The summary records this plan's coverage without overstating milestone-level acceptance.

## Self-Check: PASSED

All declared files and all six task commits were verified on disk and in Git history. The unrelated Plan 05.4 and orchestrator-config modifications remain unstaged and untouched.

---

*Phase: 05.3-generic-observation-contracts-and-durable-publication*
*Completed: 2026-09-04*
