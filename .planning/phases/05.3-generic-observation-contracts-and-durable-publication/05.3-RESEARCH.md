# Phase 05.3: Generic Observation Contracts and Durable Publication - Research

**Researched:** 2026-09-04
**Domain:** Rust carrier-neutral observation contracts, bounded assembly, validation, and atomic durable publication
**Confidence:** HIGH

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions

#### Phase boundary

Deliver the generic observation lifecycle from carrier-neutral immutable ingress and receiver-feedback contracts through private section assembly and durable atomic publication to immutable decision inputs. Ships and stations are conformance examples, not architecture owners. Use at least two materially different examples for the generic contracts; the production Carrier B path belongs to 05.4, real heavy faction-ship coverage to 05.5, and station-specific remainder to conditional 05.6.

The visible developer result is a traceable distinction between collection progress, local transport handoff, volatile receipt, durable publication and decision eligibility. A failed replacement leaves the last accepted revision intact within the same valid source scope. Stale required observations visibly block dependent decisions. A source/load uncertainty makes prior revisions history-only until a fresh baseline qualifies.

No native carrier implementation, X4 runtime integration claim, game mutation, faction rollout, new model behavior, Mail/Logbook product feature, custom UI, or public-readiness claim is added. Milestone 0.1 remains the internal ZYA/ARG Shadow Director prototype. Later observation proofs do not expand the Faction Mind roster.

#### Observation and publication

- Independent section revisions carry capture windows, coverage, quality, availability and freshness. A decision freezes exact compatible accepted revisions; it never claims a globally simultaneous X4 snapshot.
- A bounded keyed Rust map holds at most one incomplete revision per section key, with independent per-candidate and aggregate byte/count/work/age limits.
- Freeze dependencies at candidate start. Finish privately and revalidate at commit; stale dependencies discard the candidate and trigger bounded recollection. No baseline cross-revision merge or carry-forward.
- Use a lightweight start envelope and content-bound completion. Validate contiguous section-local ordinals, counts/digests, exact schema/policy/canonicalization/digest versions, dependencies and coverage.
- One transaction binds immutable accepted content, the terminal idempotency receipt and conditional current-pointer update. Emit `committed` only after commit. Incomplete candidates remain volatile and are recollected after Rust loss.
- Keep source, producer-incarnation, transport, section, batch, record and decision identities distinct. Exact identity with exact bytes is idempotent; changed bytes under the same identity are a conflict.

#### Scheduling and bounds

- Rust owns global collection policy. This phase defines the contract for X4-local continuations and final safe-step admission without implementing the production Lua/native path. Logical intent is not remote RPC for individual getters.
- One shared scheduler advances bounded persistent work on abstract delivered pulses. The selected instantiated MD cue and its observed delivery/lifecycle behavior belong to the 05.4 runtime adapter; local tests here do not claim that evidence.
- Collection scheduler and transport pump are separate state machines; pump first, reserve downstream capacity before producing new work, and never repeat getters while retrying immutable bytes.
- Game time controls urgency; real time controls admission. Apply bounded refill, burst, step count, heavy permits and overrun debt even with frozen/coarse clocks. SETA cannot multiply real-time budgets or trigger proportional catch-up.
- Prioritize core identity/ownership/location before deferrable details. Fairness is conditional on capacity; starvation appears as stale data, and missing freshness blocks dependent decisions.
- Bound native allocations/results, retained continuations, semantic records, batches, pending transport, receiver allocation, raw/decoded candidates, accepted history and diagnostics separately. Native calls remain indivisible; unsafe hitches reject the collector.
- Derive configuration values from relevant measurements. Legacy 129-member, 64-frame, 1800-byte, FIFO and borrowed 256 KiB values are not production defaults. Removing arbitrary limits never authorizes unbounded state.

#### Carrier-neutral feedback contracts

- Define the Live Galaxy facade with bounded complete-message, connection, send, control-poll and error semantics. Do not implement or select a production carrier in this phase.
- Begin with one globally unacknowledged immutable application batch. Multiple sections may interleave through this single slot.
- `received` means batch validation and volatile RAM staging. `committed` means durable atomic section publication. A pipe write alone means neither.
- Preserve capacity-unavailable, exact duplicate, permanent rejection, timeout/supersession, stale epoch and ambiguous commit outcomes as separate classes. Only proven non-consumption permits retry with the same identity and bytes.
- Reserve bounded terminal/control capacity. Inbound traffic contains small handshake, demand, disposition, collection intent, health and reset records, never bulk observations, raw model output or complete plans.
- An oversized inbound message must fail closed as a whole; do not reconstruct it by assuming raw error fragments contain all original bytes.
- Reconnect discards volatile candidates. Exact terminal replay uses the durable receipt; ambiguous state is never continued as if receipt were proven.
- The 2026-09-04 Docs MCP evidence demonstrated the Carrier B trigger: Carrier A cannot prove bounded nonblocking writes or typed short-write/error outcomes. Carrier B implementation and X4 lifecycle proof therefore belong to dedicated Phase 05.4. A fixed window, dynamic credit and durable per-batch staging remain separately evidence-triggered extensions.

#### Source uncertainty and lifecycle

- Source membership, producer assembly, transfer and publication completeness are independent. A correct certificate cannot promote `partial_set` or count/fill observations to `complete_set`.
- Unknown source epoch is an accepted baseline: a local producer incarnation fences one uninterrupted unambiguous runtime scope. The generic contracts invalidate unfinished work and previous current decision eligibility when the adapter reports start/load/reload uncertainty; runtime detection belongs to 05.4 and does not invent a campaign UUID.
- Complete-set absence and entity deletion require qualifying source coverage, stable identity and the same authoritative scope. Two consecutive qualifying absences may remove; partial or failed scans have no absence meaning. Optional field failure never deletes the core entity.
- Automatic save pause/resume remains unsupported because the current evidence does not establish a safe save-begin or durability boundary. This phase defines no numeric cooldown; any later runtime behavior must be discussed and proven in 05.4.
- Event/delta mode requires an authoritative source boundary, contiguous ordered events and gap/retention semantics. It is deferred, not an alternative inferred from repeated polling.

### the agent's Discretion

Research owns code reuse assessment, wire encoding, storage implementation, measured budgets and test decomposition within accepted contracts. Inspect existing modules before choosing dependencies; do not rebuild retired research tooling or add a generic admission platform for agent duties.

The 2026-09-04 Docs MCP publication established that Carrier A cannot satisfy the required bounded typed facade, that the selected 50 ms MD cue is a best-effort game-time pump rather than a hard real-time service, and that no safe automatic save-resume boundary is currently proven. Those results close 05.3 research by removing the dependent runtime slice rather than weakening its contracts. No new X4 run is authorized in this phase.

If evidence contradicts an accepted architectural baseline or changes visible behavior, present the concrete conflict to the owner. Routine implementation choices and review fixes preserving these decisions do not reopen discussion.

### Deferred Ideas (OUT OF SCOPE)

Owned Carrier B and the production observation path (05.4), real heavy faction-ship coverage (05.5), station-specific remainder (05.6), Mail/Logbook and product diagnostics (06), full director AFK/SETA proof (07), quality/publication evidence (08), true source epoch, baseline-plus-deltas, dynamic credit and durable partial staging remain outside this phase's accepted baseline.
</user_constraints>

## Summary

Phase 05.3 is fully planable as a Rust-only vertical slice. Replace the present single-candidate, in-memory admission path with carrier-neutral immutable batch and feedback types, bounded keyed private candidates, content-bound completion, monotone coverage/eligibility rules, and a durable repository whose single transaction installs immutable revision content, terminal receipt, and current pointer. This implements the source-agnostic lifecycle without claiming an X4 carrier or lifecycle detector. [DOCUMENTED; VERIFIED: `.planning/ROADMAP.md:270-285`, `docs/ARCHITECTURE.md:1229-1272`]

The repository already has useful typed identity, quality, strict-JSON, receipt-clock, bounded rejection-history, compare-and-set, and recovery-test seams. It lacks the required keyed candidate map and durable observation repository: `GenerationStager` has exactly `candidate: Option<Candidate>` and replaces `AcceptedProjection` in memory at completion. [OBSERVED; VERIFIED: `crates/observation-ingest/src/generation.rs:9-24,127-182`]

The expanded X4 Docs replay no longer blocks this phase. It constrains the facade: Carrier A cannot establish safe bounded retry under pressure, the selected MD cue is best-effort game-time delivery, and no safe automatic save-resume boundary is proven. Runtime implementation and proof belong to 05.4; 05.3 uses abstract delivered pulses and adapter-reported uncertainty only. [DOCUMENTED/OBSERVED: `05.3-CONTEXT.md`; immutable snapshots listed under Sources]

**Primary recommendation:** implement three Rust plans: typed carrier-neutral contracts, bounded keyed assembly/eligibility, then SQLite-backed atomic publication/recovery.

## Project Constraints (from AGENTS.md)

- Rust owns normalized state, validation, persistence, recovery, and deterministic diagnostics; X4 owns authoritative game state. [DOCUMENTED]
- External wire input remains outside the trust boundary until schema, semantic, budget, safety, and current-state validation pass. [DOCUMENTED]
- Bound time, memory, bytes, counts, retries, and retained state; make recovery idempotent; use explicit state machines and typed values. [DOCUMENTED]
- Do not inspect saves or modify installed X4, extensions, or sibling references. [DOCUMENTED]
- Structural discovery uses `ast-index`. This session ran `ast-index stats` (137 files, 3,221 symbols, 9,632 references, 2.07 MiB), then `ast-index update` (8 changed files), and narrow queries for `GenerationStager`, `AcceptedProjection`, `CheckpointPort`, `FakeCheckpointPort`, and deferred-publication tests. [OBSERVED: exact local commands, 2026-09-04]
- Run focused Rust checks, then formatting, Clippy, and full workspace tests; mutation follows a measured baseline. [DOCUMENTED]
- Local fake/persistence evidence must never be reported as X4 runtime proof. [DOCUMENTED]
- `.planning/**` is deliberately excluded from Markdownlint; do not force an alternate config. [DOCUMENTED]
- Preserve concurrent/user changes; this research owns only this file. [DOCUMENTED]

<phase_requirements>
## Phase Requirements

| ID | Exact requirement | Role and research support |
| --- | --- | --- |
| OBS-02 | “Every observed entity and event used by strategy has a stable typed identity, source, observation time, and monotonic state or event version.” | Primary: distinct source/incarnation/section/batch/record identities, capture window, version and digest contracts. [VERIFIED: `.planning/REQUIREMENTS.md:11`] |
| OBS-03 | “Normalized world-state sections preserve freshness, coverage, quality, and explicit unknown or unsupported states instead of fabricating missing facts.” | Primary: independent coverage, quality, availability and freshness plus monotone eligibility. [VERIFIED: `.planning/REQUIREMENTS.md:12`] |
| OBS-07 | “Malformed, oversized, duplicate, stale, and out-of-order observation input is rejected or reconciled without corrupting the last accepted snapshot.” | Primary: strict decode, limits, contiguous ordinals, exact replay/conflict, private rejection, atomic pointer update. [VERIFIED: `.planning/REQUIREMENTS.md:16`] |
| OBS-01 | “The system can ingest bounded, versioned observation envelopes from X4 without blocking the game thread on bridge or model work, and the X4 adapter and Rust bridge negotiate explicit transport/session capabilities before accepting traffic.” | Supporting contract only: 05.3 defines bounded envelopes/capabilities/feedback; 05.4 implements and proves X4 ingress. [VERIFIED: `.planning/REQUIREMENTS.md:10`; `.planning/ROADMAP.md:288-302`] |
| OBS-08 | “Milestone 0.1 X4 integration exposes no fleet, economy, diplomacy, institution, or other game-state mutation command.” | Supporting safety boundary only: the facade exposes no mutation command. OBS-08 does **not** require a production observation carrier here. [VERIFIED: `.planning/REQUIREMENTS.md:17`] |
| VAL-06 | “Exact X4 9.00 observation, identity, scheduling, transport, persistence, and Mail or Logbook semantics used by the implementation are backed by documented or disposable observed evidence.” | Supporting evidence boundary: no X4 runtime semantic is implemented here; prior evidence constrains the contract and runtime proof moves to 05.4. [VERIFIED: `.planning/REQUIREMENTS.md:76`] |
| STATE-02/03/04/06 | Existing transactional persistence, idempotent recovery, fail-closed corruption, and protocol-compatibility requirements. | Supporting precedents: reuse port/recovery patterns, not the mind-specific schema. [VERIFIED: `.planning/REQUIREMENTS.md:55-59`] |
</phase_requirements>

## Architectural Responsibility Map

| Capability | Primary tier | Secondary tier | Rationale |
| --- | --- | --- | --- |
| Identity and section semantics | `observation-domain` | `strategic-state` | Carrier-independent values; strategy consumes immutable revision references. |
| Envelope and feedback contracts | `observation-ingest` | `x4-bridge` compile-time adapter only | Generic validation owns semantics; 05.4 supplies a carrier. |
| Collection/transport policy state machines | `observation-ingest` | — | Rust owns policy; abstract pulses keep it deterministic. |
| Keyed private assembly | `observation-ingest` | — | Volatile validation state is not persistence state. |
| Revision/receipt/pointer transaction | new `observation-persistence` crate | domain types | Storage owns atomicity without coupling ingestion to SQLite. |
| MD/Lua/native delivery | Phase 05.4 | 05.3 contracts | Explicitly out of scope. |

## Standard Stack

| Library/tool | Version | Purpose and recommendation |
| --- | --- | --- |
| Rust | 1.97.1, edition 2024 | Keep pinned workspace toolchain and deny lints. [VERIFIED: `Cargo.toml:1-70`] |
| `serde` | 1.0.229 | Reuse exact pin and `deny_unknown_fields`; no second codec. [VERIFIED: crate manifests; `wire.rs:103-170`] |
| `serde_json` | 1.0.151 | Reuse exact pin for contracts/fixtures and canonical bytes. [VERIFIED: crate manifests] |
| `sha2` | 0.11.0 | Add exact pin for SHA-256 content/manifest identity; do not hand-roll collision-resistant hashing. [VERIFIED: crates.io and package-legitimacy seam, 2026-09-04] |
| `rusqlite` | 0.40.2 | Add exact pin with `default-features = false, features = ["bundled"]`; official feature metadata confirms `bundled`, avoiding a system SQLite dependency. [VERIFIED: `cargo info rusqlite --verbose`, 2026-09-04] |

### Package Legitimacy Audit

| Package | Registry evidence | Source | Verdict | Disposition |
| --- | --- | --- | --- | --- |
| `sha2` 0.11.0 | Lineage since 2016; 18,549,489 weekly downloads at check | RustCrypto/hashes | OK | Approved exact pin |
| `rusqlite` 0.40.2 | Lineage since 2014; 2,496,759 weekly downloads at check | rusqlite/rusqlite | OK | Approved exact pin/minimal features |

No SLOP or SUS package remained. [VERIFIED: `package-legitimacy check --ecosystem crates`, 2026-09-04]

## Existing Reuse Map

| Seam | Verified current shape | Use |
| --- | --- | --- |
| Section evidence | `SectionQuality`: `Fresh`, `KnownEmpty`, `Unknown`, `Partial`, `Stale`, `Unsupported`; `SectionFreshness`: `Fresh`, `Stale`; `SectionCoverage`: `Complete`, `KnownEmpty`, `Unknown`, `Partial`, `Unsupported`. [VERIFIED: `crates/observation-domain/src/section.rs:3-29`] | Preserve/extend independent axes; never collapse them. |
| Source identity | `ObservationSource` exact value is `X4Runtime`. [VERIFIED: `identity.rs:35-44`] | Keep typed; add separate incarnation/transport/section/batch/record types. |
| Strict wire | `WireFrame`: `Hello`, `Heartbeat`, `RuntimeHealth`, `Observation`, `CompleteMarker`; payloads deny unknown fields. [VERIFIED: `wire.rs:6-14,103-170`] | Evolve versioned start/batch/completion/control types with whole-message rejection. |
| Bounds | `prepare` checks sequence plus checked byte/work accumulation and maxima. [VERIFIED: `generation.rs:82-125`] | Extract per-candidate and aggregate byte/count/work/age/inactivity accounting. |
| Candidate | Current candidate clones accepted snapshot/runtime facts. [VERIFIED: `generation.rs:127-136`] | Replace with section-private content and frozen dependency references. |
| Rejections | `MAX_REJECTION_EVIDENCE = 8`; exact current errors/reasons are defined in `model.rs`. [VERIFIED: `model.rs:7-33`] | Keep bounded diagnostics; add typed disposition classes. |
| Snapshot | `ProjectionSnapshot` contains `observations: BTreeMap<...>` and private `completed_scopes`. [VERIFIED: `snapshot.rs:17-22`] | Replace mutable install with immutable revisions/current pointers. |
| CAS precedent | Fake port returns exact retry ack and rejects same-sequence changed content with `ContentCollision`. [VERIFIED: `mind-persistence/src/fake_port.rs:24-38,55-94`] | Mirror in observation-specific port/SQLite adapter. |
| Mind persistence constants | Exact values are `SCHEMA_VERSION = "1"`, `GAME_PROTOCOL_IDENTITY = "live_galaxy.persistence.v1"`, `MAX_ENVELOPE_BYTES = 32_768`. [VERIFIED: `mind-persistence/src/checkpoint.rs:7-33`] | Reuse pattern, never the mind-specific payload/identity. |
| Five ignored contracts | Ignored tests preserve deferred publication expectations. [OBSERVED: `x4-bridge/tests/deferred_publication.rs`] | Activate pure contract/persistence semantics here; leave listener/pressure runtime assertions for 05.4. |

## Architecture Patterns

```text
Conformance source A/B
  -> strict carrier-neutral start + immutable batches + completion
  -> local handoff
  -> bounded keyed private candidates
       malformed/conflict/stale -> reject; retain accepted pointer
       valid batch -> volatile received
  -> completion + frozen-dependency/current-pointer revalidation
       mismatch -> discard private candidate
       valid -> one SQLite transaction
          immutable content + terminal receipt + conditional pointer
  -> committed only after commit
  -> decision freezes exact compatible revisions

Adapter-reported uncertainty
  -> discard volatile candidates
  -> prior revisions become history-only
  -> require a fresh qualifying baseline
```

### Prescriptive patterns

1. **Identity ladder and immutable retry:** separate newtypes for source, producer incarnation, transport epoch, section key/revision, batch, record, and decision snapshot. Exact identity plus exact bytes/digest replays; changed bytes conflict; never rebuild an unacknowledged batch. [DOCUMENTED: ADR-LG-013/017/018]
2. **Keyed private candidates:** use `BTreeMap<SectionKey, Candidate>`, one incomplete revision per key. Store frozen dependencies, expected pointer, next ordinal, raw/decoded bytes, record/batch/work counts, start/last-progress times, capture window, evidence axes, and digest state. Maintain checked aggregate counters on every admit/drop path. [DOCUMENTED: VER-LG-008/010]
3. **Monotone evidence:** completion certifies content only; it cannot strengthen source coverage. Unknown epoch fences one uninterrupted scope. Uncertainty invalidates eligibility and volatile work but retains durable history. Absence/deletion remains disabled without qualifying source/identity evidence. [DOCUMENTED]
4. **Atomic publication:** repository `publish(candidate, expected_pointer)` returns committed replay/new, terminal conflict, stale dependency/pointer, or storage/ambiguous outcome. One transaction checks receipt/state, inserts immutable content and receipt, conditionally updates pointer, then commits before `committed`. [VERIFIED: `docs/ARCHITECTURE.md:1243-1271`]
5. **Abstract pulse state machines:** separate collection and transport pump; pump first and reserve capacity. Game time affects urgency, real time admission. Do not encode 50 ms, SETA, pause, save, or load behavior. [DOCUMENTED]

### Recommended structure

```text
crates/observation-domain/       # identities, metadata, revisions, eligibility
crates/observation-ingest/       # contract, bounds, candidates, completion, scheduler
crates/observation-persistence/  # port, SQLite adapter, schema, recovery, tests
crates/x4-bridge/                # compile-time facade adapter only in 05.3
```

The new crate/module names are recommendations, not current facts. [INFERRED]

## Wire and Persistence Decisions

- Use strict tagged JSON with existing Serde pins. Check complete raw-message size before decode; unknown fields and unsupported exact versions fail closed. [INFERRED choice supported by VERIFIED current pattern]
- Define exact schema, policy, canonicalization, and digest versions once in source. Do not reuse historical protocol literals accidentally. [DOCUMENTED; exact new values UNKNOWN]
- Compute SHA-256 ordered batch-manifest and canonical membership/content digests; version canonicalization and test golden vectors. [DOCUMENTED: VER-LG-008]
- Normalize immutable revision metadata/content, terminal receipts, current pointers, and decision pins in SQLite with foreign keys/unique constraints. Use one immediate write transaction for publication. [INFERRED, HIGH]
- Bound receipts/history separately; garbage collection never deletes current or decision-pinned revisions. [VERIFIED: `docs/ARCHITECTURE.md:1234-1246`]
- Treat unconfirmed commit I/O as ambiguous; reconcile exact receipt and pointer before retry/new publication. [DOCUMENTED: VER-LG-007/009]

## Don't Hand-Roll

| Problem | Do not build | Use |
| --- | --- | --- |
| Collision-resistant identity | Custom checksum | `sha2::Sha256` |
| Durable transaction | Multi-file rename/separate writes | SQLite transaction via `rusqlite` |
| Parsing | Partial scans/custom lexer | Serde strict structs |
| Deterministic time | Wall-clock calls/sleeps | Existing `ReceiptClock` plus injected monotonic clocks |
| Admission infrastructure | Generic cross-domain framework | Observation-specific ports/state machines |
| X4 lifecycle proof | Fake promoted as proof | Abstract events now; disposable runtime in 05.4 |

## Common Pitfalls

- **Handoff equals commit:** prevent with typed `handoff`/`received`/`committed`/rejected/ambiguous outcomes and crash/disconnect cuts. [DOCUMENTED: VER-LG-006/007]
- **Coverage strengthened by certificate:** preserve source evidence independently; test a non-strengthening transition table/property. [DOCUMENTED: VER-LG-008]
- **Single candidate/full projection clone retained:** replace with section-keyed private content and aggregate limits. [OBSERVED: `generation.rs:9-24,127-173`]
- **Receipt/pointer written separately:** one transaction and injected failure around every statement/response. [DOCUMENTED: VER-LG-009]
- **Unknown epoch reused globally:** uncertainty must fence eligibility and volatile work. [DOCUMENTED]
- **Legacy fixture limits promoted:** initial values are finite non-production test/policy defaults; 05.4/05.5 measurements set production budgets. [DOCUMENTED]
- **Local proof called X4 proof:** verification language must say generic contract/persistence evidence. [DOCUMENTED]

## Concrete Validation Architecture

Nyquist workflow is disabled in `.planning/config.json`; these implementation tests remain mandatory. [OBSERVED]

| Slice | Required tests | Focused command |
| --- | --- | --- |
| Domain | identity separation; monotonic version; coverage non-strengthening; unknown/unsupported; two materially different examples | `cargo test -p observation-domain` |
| Contract | strict/versioned decode; whole-message size failure; immutable retry; duplicate/conflict; distinct outcomes; no mutation command | `cargo test -p observation-ingest contract` |
| Candidates/bounds | per/aggregate byte/count/work/age/inactivity; counter release; interleaving; failure isolation; pump-first capacity; no time-scale catch-up | `cargo test -p observation-ingest candidates` |
| Completion/eligibility | ordinal/count/length/digest/version/dependency; evidence preservation; known-empty qualification; uncertainty fence; stale-required block | `cargo test -p observation-ingest completion` |
| Repository | fake and SQLite exact replay, conflict, stale pointer, rollback, bounded receipts, corruption/incompatibility, restart | `cargo test -p observation-persistence` |
| Crash cuts | before/after content, receipt, pointer, commit, response; recover exactly old or new complete revision | `cargo test -p observation-persistence --test crash_recovery` |
| Compile boundary | facade expresses outcomes and no mutation command; no runtime connection claim | `cargo test -p x4-bridge` |
| Regression | format, lint, all tests | `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --all-features`; `cargo test --workspace` |

Use actual test target names created by the plan. Inject clocks/failpoints; use no sleeps. Golden vectors cover order permutations, zero-record qualified known-empty, and same identity/different bytes. Property tests or exhaustive tables cover evidence monotonicity and counter release; add no property-test package without legitimacy verification. [INFERRED]

### Risk gates

| Gate | Decision and reason |
| --- | --- |
| Security review | Selected: untrusted input, hashes, persistence, replay, resource bounds. |
| Rust mutation | Selected after measured baseline: target evidence monotonicity, replay/conflict, bounds, dependency and transaction outcome logic. |
| X4 probe | Rejected: runtime implementation is 05.4. |
| SETA soak | Rejected: abstract policy only; delivery proof is 05.4. |
| Lua/Busted mutation | Rejected: no Lua/XML scope. |
| AI/eval | Rejected: no model-facing change. |
| UI | Rejected: no UI. |
| Nyquist | Rejected unless planning discovers a new coverage gap; config disables it. |

## Security Domain

| Category | Applies | Control |
| --- | --- | --- |
| Session identity | Yes, non-web | Exact typed protocol/incarnation/epoch negotiation; fail closed before admission. |
| Authority | Yes | Observation/control only; no game-state mutation command. |
| Input validation | Yes | Raw byte cap, strict decode, exact versions, identities, bounds, digests, sequence/dependencies. |
| Cryptography | Integrity identity only | SHA-256 via `sha2`; no authenticity claim/custom cryptography. |
| Stored integrity | Yes | Constraints, transaction, digest, compatibility metadata, corruption fail-closed. |
| Diagnostics | Yes | Bounded structured data; no secrets/private paths/unbounded payload echo. |

Threats are tampering, replay/duplication, exhaustion, stale-scope confusion, and partial installation. [INFERRED, HIGH]

## Docs MCP Evidence Retained for 05.4

The exact expanded snapshot set was `x4-9.00-steam-23660954`, `sn-mod-support-apis-1.95-a96285fe`, `x4-probe-transport-lifecycle-42a8852a-v1`, `carrier-a-message-mode-source-20260903-v2`, `save-boundary-source-20260903-v1`, and `x4-probe-md-pulse-a70af158-v1`. Searches used limit 10/no cursor; selected inspections used the same set/limit 50 through `next_cursor=null`; conflicts were empty. [OBSERVED: registered replay, 2026-09-04]

- `Named_Pipes._Write_Pipe_Raw`: six claims show conditional message/nonblocking behavior; wrapper success/positive bytes do not prove complete consumption or safe retry. Keep capacity/non-consumption/ambiguous outcomes; do not implement Carrier A. [OBSERVED: entity `entity:f8246e26c9f9638e151733350defb0358cae584a234c6a25927930adc3d85cd2`]
- `checkinterval`: three claims from run `a70af15873454651a1c786f1080cd158` establish best-effort game-time delivery, pause stop, SETA frequency change, and Lua/pipe recreation on load/reload. Use abstract pulses; 05.4 owns proof. [OBSERVED: entity `entity:47eea368f480d4b1c105f886f4018eb6fb0d9bf8d6eb035cd5a0c87711c46b08`]
- `IsSavingPossible`: five claims establish no safe save-begin/durability boundary. Define no automatic resume/cooldown; accept adapter-reported uncertainty only. [DOCUMENTED/OBSERVED: entity `entity:3692f0ac30db49e079a6dde41f6c01a08eddb8e437a63ca11ef211f32dbeffe2`]

These are constraints, not blockers: roadmap criteria 1-6 explicitly require local generic contract/persistence proof and assign production carrier implementation to 05.4. [VERIFIED: `.planning/ROADMAP.md:278-285`]

## Recommended Plan Decomposition

1. **05.3-01 — Carrier-neutral lifecycle contracts:** typed identities/revision evidence, version/digest contract, feedback state machine, abstract collection/transport policy, uncertainty/eligibility, and two shared conformance examples. Delivers OBS-02/03 foundations and OBS-08 supporting boundary.
2. **05.3-02 — Bounded keyed assembly:** replace single candidate; add per/aggregate bounds, immutable batch retry, sequence/manifest/content checks, frozen dependencies, monotone coverage, known-empty gating, and failure isolation. Delivers volatile `received` and most OBS-07.
3. **05.3-03 — Durable publication/recovery:** observation repository port plus `rusqlite`; schema compatibility, immutable content, terminal receipt, conditional pointer transaction, bounded retention/pins, replay/conflict, corruption diagnostics, crash cuts, and end-to-end local `committed`/decision-reference proof. Closes OBS-07.

Plan 03 depends on exact contracts from 01 and validated candidate from 02; do not design its schema against unsettled identities. [INFERRED]

## Explicit Out of Scope

- Native Rust/C DLL, Lua, MD XML, named pipes, packaging, or installed-X4 changes.
- Carrier B implementation, connection lifecycle, pressure/short-write measurement, runtime retry admission.
- X4 start/load/reload/save detection, automatic save resume, 50 ms cue behavior, SETA/soak/X4 probe.
- Heavy faction ships (05.5), station remainder (05.6), new minds, or mutation commands.
- Model contracts/prompts/evals, Mail/Logbook, UI, public readiness.
- True source epoch, event/delta mode, dynamic credit, multi-batch window, durable partial candidates, production numeric budgets.
- More AI Economy Ships as a commitment/release gate; KUDA/Add More Sectors targets and Faction Enhancer policy are unchanged.

## Residual Unknowns and Checkpoints

No unknown blocks planning. The planner must choose within accepted discretion:

1. Exact new observation schema/protocol/policy/canonicalization/digest constant names and values. Define each once; do not reuse historical literals. [UNKNOWN, non-blocking]
2. Finite local-test defaults for every bound, explicitly non-production and injectable pending 05.4/05.5 measurement. [UNKNOWN production values, non-blocking]
3. Exact SQLite schema/migration version and decision-pin retention API, preserving atomicity/reachability. [UNKNOWN exact shape, non-blocking]
4. Mutation targets/threshold after a measured implementation baseline. [UNKNOWN until code exists, non-blocking]

Carrier selection, save policy, MD cadence, and SETA proof are deferred scope, not 05.3 questions. [DOCUMENTED]

## Assumptions Log

| # | Claim | Risk/handling |
| --- | --- | --- |
| A1 | SQLite/`rusqlite` is the smallest suitable store. | Planner may substitute only equal atomic/crash evidence with a verified dependency. |
| A2 | Recommended new crate/module names fit. | Preserve ownership, not literal names. |
| A3 | Ship-like and station-like fixtures are materially different. | Plan must specify distinct cardinality, optionality, and known-empty behavior. |

## Environment Availability

| Dependency | Available | Version/fallback |
| --- | --- | --- |
| Rust/Cargo | Yes | Rust 1.97.1 pinned |
| crates.io metadata | Yes via approved network | `sha2` 0.11.0, `rusqlite` 0.40.2 |
| System SQLite | Not required | `rusqlite` `bundled` |
| X4/Lua/Busted | Deliberately unused | Phase 05.4 |

## Sources

- `.planning/ROADMAP.md:270-302`; `.planning/REQUIREMENTS.md:10-17,55-59,71-76`.
- `05.3-CONTEXT.md`; `05.3-READINESS.md`.
- `docs/ARCHITECTURE.md:1229-1272`; `docs/architecture-decisions.md:325-341`; `docs/architecture-verification.md:232-327`.
- Source seams cited under `crates/observation-*`, `crates/mind-persistence`, and `crates/x4-bridge/tests`.
- crates.io/Cargo metadata and package-legitimacy results for `sha2` and `rusqlite`.
- Registered read-only X4 Docs MCP replay and immutable snapshots listed above.

## Metadata

- Standard stack: HIGH — exact pins, registry metadata/features, and legitimacy verified.
- Architecture: HIGH — context, roadmap, ADRs, verification contracts, and source seams align.
- Runtime boundary: HIGH — approved split and Docs replay assign implementation/proof to 05.4.
- Production numeric budgets: LOW/unknown — intentionally deferred; finite injectable local policies remain planable.

**Research date:** 2026-09-04
**Valid until:** accepted scope/ADRs change; recheck package versions after 30 days.

## RESEARCH COMPLETE
