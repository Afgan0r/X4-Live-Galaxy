---
phase: 05.3-generic-observation-contracts-and-durable-publication
verified: 2026-09-04T17:43:26Z
status: gaps_found
score: 8/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
decision_coverage:
  honored: 0
  total: 0
  not_honored: []
gaps:
  - truth: "Two materially different examples traverse the same complete section lifecycle while several private section candidates share exactly one global unacknowledged immutable batch slot."
    status: failed
    reason: "The ship/station test only decodes two different envelope variants, and the stop-and-wait slot, keyed assembler, eligibility index, and repository are not wired into one shared lifecycle or behavioral test."
    artifacts:
      - path: "crates/observation-ingest/tests/contract_lifecycle.rs"
        issue: "ship_and_known_empty_station_use_the_same_strict_envelopes asserts decoded shape only; it does not stage, complete, accept, or publish either example."
      - path: "crates/observation-ingest/src/feedback.rs"
        issue: "StopAndWaitSlot is exported and unit-tested but has no production consumer and no link to GenerationStager."
      - path: "crates/observation-ingest/tests/keyed_candidates.rs"
        issue: "The interleaving test calls GenerationStager directly and does not exercise the global immutable slot."
    missing:
      - "Wire one carrier-neutral application service or equivalent contract harness from strict decode through StopAndWaitSlot, keyed assembly, completion, accepted-publication authority, and repository publication."
      - "Run both materially different ship-like and station-like examples through that same lifecycle, including terminal slot turnover and durable/non-durable dispositions."
  - truth: "Game time changes urgency without changing real-time work allowance, and collection/transport policy enforces finite refill, burst, step work, heavy permits, terminal reserve, and overrun debt."
    status: failed
    reason: "The scheduler ignores game-time urgency, treats step_work/heavy_permits/terminal_reserve only as tautological non-zero checks, and implements no overrun-debt state."
    artifacts:
      - path: "crates/observation-ingest/src/scheduler.rs"
        issue: "DeliveredPulse::_game_time_urgency is never read; step_work, heavy_permits, and terminal_reserve do not constrain or report admitted work; no debt is tracked."
      - path: "crates/observation-ingest/tests/contract_lifecycle.rs"
        issue: "No behavioral test proves urgency ordering with unchanged allowance, finite step/heavy consumption, terminal reserve, or debt repayment."
    missing:
      - "Implement observable urgency ordering that cannot increase permits or cause catch-up."
      - "Enforce and expose finite step-work and heavy-permit admission, reserved terminal capacity, and bounded overrun-debt repayment under frozen/coarse clocks."
      - "Add exact boundary tests for each policy dimension."
---

# Phase 05.3: Generic Observation Contracts and Durable Publication Verification Report

**Phase Goal:** Build the source-agnostic bounded observation lifecycle from immutable ingress contracts through validated durable publication, without selecting or claiming a production X4 carrier.
**Verified:** 2026-09-04T17:43:26Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

The six ROADMAP success criteria and twenty PLAN truths were merged into eleven non-overlapping observable truths. SUMMARY claims were not used as proof.

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Two materially different examples traverse one complete section lifecycle. | ✗ FAILED | `ship_and_known_empty_station_use_the_same_strict_envelopes` only decodes one ship batch and one station completion and asserts shape. |
| 2 | Distinct typed identities and source-scoped monotone entity versions reject regression, replay exact content, and conflict on changed equal-version content. | ✓ VERIFIED | `identity.rs`, `observation.rs`, `candidate_limits.rs`; named monotone-version regression passed. |
| 3 | Collection progress, local handoff, volatile receipt, durable commit, rejection, ambiguity, and a mutation-free carrier facade remain distinct. | ✓ VERIFIED | `feedback.rs`, `facade.rs`; lifecycle and facade named regressions passed. |
| 4 | Game time affects urgency only while finite real-time work policy enforces refill, burst, step/heavy limits, reserve, and debt. | ✗ FAILED | `_game_time_urgency` is ignored; step/heavy/reserve values are only checked as non-zero; no overrun debt exists. |
| 5 | Several keyed candidates advance through exactly one global immutable unacknowledged slot. | ✗ FAILED | Keyed candidates interleave when called directly, but `StopAndWaitSlot` has no production consumer or test link to `GenerationStager`. |
| 6 | Candidate/map byte, count, work, age, and inactivity bounds are checked and failure releases exact charges without damaging another key. | ✓ VERIFIED | `generation.rs`, `candidate_limits.rs`, `batch_budget.rs`; exact charge-release regression passed. |
| 7 | Exact completion, frozen dependencies/current pointer, non-strengthening evidence, uncertainty fencing, stale eligibility, and qualified two-absence deletion hold. | ✓ VERIFIED | Completion, coverage, eligibility, and reconciliation named regressions passed. |
| 8 | Fake and SQLite publication share exact replay/conflict/CAS behavior, one immediate content/receipt/pointer transaction, ambiguous reconciliation, and revocable accepted-publication authority. | ✓ VERIFIED | Shared port contract, ambiguous recovery, and adversarial `bb0da96` stale-authority regression passed; `bb0da96` is an ancestor of verified HEAD. |
| 9 | Crash cuts expose only complete old/new state; every connection verifies foreign keys; pins and bounded retention protect current and decision-reachable evidence; corruption fails closed. | ✓ VERIFIED | Named precommit, orphan-FK, digest/partial-row, pin retention, and receipt-bound regressions passed. |
| 10 | Results remain generic local Rust evidence and introduce no production carrier, Lua/XML runtime path, X4 claim, or mutation vocabulary. | ✓ VERIFIED | Phase source diff is Rust contract/persistence only; facade contract is observation/control-only; 05.4 owns production Carrier B. |
| 11 | Required research and checked plans exist, and all PLAN requirement IDs are mapped. | ✓ VERIFIED | `05.3-RESEARCH.md`, three PLANs, roadmap execution record, and requirement registry cover OBS-02/03/07. |

**Score:** 8/11 truths verified (0 present, behavior-unverified)

## Required Artifacts

`gsd-tools query verify.artifacts` passed all 19 declared artifacts across the three plans. Manual substance and wiring checks qualify that result as follows.

| Artifact group | Exists | Substantive | Wired | Status | Details |
| --- | --- | --- | --- | --- | --- |
| Identity, strict wire, feedback, scheduler, and facade | Yes | Mostly | Partial | ✗ GAP | The types and state machines are substantive, but the feedback slot is not connected to assembly and scheduler policy fields are partly inert. |
| Keyed assembly, completion, eligibility, and reconciliation | Yes | Yes | Yes internally | ✓ VERIFIED | Canonical batches, checked charges, exact completion, uncertainty, and qualified absence are exercised behaviorally. |
| Repository port, fake, SQLite, schema, recovery, pins, and retention | Yes | Yes | Yes internally | ✓ VERIFIED | Shared contract and file-backed crash/corruption tests exercise the durable boundary. |

## Key Link Verification

| From | To | Status | Details |
| --- | --- | --- | --- |
| Typed identities | Strict envelopes | ✓ WIRED | Declared identity/version types are carried by strict envelope fields. |
| Feedback dispositions | Scheduler | ⚠ PARTIAL | Shared disposition vocabulary exists, but no application path connects the slot to scheduler/assembler lifecycle. |
| X4 facade | Shared feedback/control types | ✓ WIRED | Type-only trait consumes complete-message and bounded-control outcomes. |
| Keyed candidates | Checked accounting | ✓ WIRED | Admit/drop/expiry/completion paths charge and release aggregate state. |
| Candidate completion | Validated revision | ✓ WIRED | Exact candidate yields one immutable unpublished revision. |
| Eligibility | Reconciliation evidence | ✓ WIRED | Source evidence and absence policy remain non-strengthening. |
| Validated revision | Repository request | ✓ WIRED | `PublishRequest` can only be constructed from `AcceptedPublication`. |
| Repository port | Fake/SQLite | ✓ WIRED | Shared behavioral contract passes for both backends. |
| SQLite publish | Recovery/reconciliation | ✓ WIRED | Failpoints, ambiguity barrier, durable receipt, and pointer classification are connected. |

## Data-Flow Trace

| Flow | Status | Evidence |
| --- | --- | --- |
| Strict bytes → feedback slot → keyed candidate | ✗ DISCONNECTED | No production use of `StopAndWaitSlot`; keyed tests bypass it. |
| Keyed candidate → validated revision → accepted authority → repository | ✓ FLOWING (local contract) | Completion, eligibility authority, and persistence helpers/tests exercise the typed path. |
| SQLite transaction → receipt/current pointer → reconciliation/retention | ✓ FLOWING | Immediate transaction and reopen tests prove durable local behavior. |

No rendered dynamic UI data exists; Level 4 UI/DB rendering analysis is not applicable.

## Behavioral Spot-Checks

All commands below were run independently during verification and passed after correcting the initial Cargo `--exact` argument placement.

| Behavior | Command/result | Status |
| --- | --- | --- |
| Lifecycle stage distinction | Named `contract_lifecycle` test, 1 passed | ✓ PASS |
| Source-scoped monotone versions | Named `identity_section_contract` test, 1 passed | ✓ PASS |
| Ship/station strict-envelope shape | Named `contract_lifecycle` test, 1 passed | ✓ PASS, but insufficient for full-lifecycle truth |
| Real-time refill independent of urgency | Named scheduler test, 1 passed | ✓ PASS, but insufficient for declared work/debt policy |
| Keyed interleaving and exact charge release | Two named `keyed_candidates` tests, 2 passed | ✓ PASS |
| Completion, coverage, and uncertainty eligibility | Three named `completion_contract` tests, 3 passed | ✓ PASS |
| Two qualifying absences | Named reconciliation test, 1 passed | ✓ PASS |
| Accepted-publication authority revocation | Named `port_contract` reconnect regression, 1 passed | ✓ PASS |
| Shared fake/SQLite contract | Named `port_contract` test, 1 passed | ✓ PASS |
| Crash ambiguity and old/new atomicity | Two named `crash_recovery` tests, 2 passed | ✓ PASS |
| Foreign-key, digest, and partial-row corruption | Two named schema tests, 2 passed | ✓ PASS |
| Current/pin retention and receipt cap | Two named retention tests, 2 passed | ✓ PASS |
| Mutation-free compile facade | Named `protocol_contract` test, 1 passed | ✓ PASS |

The orchestrator-provided regression gate `cargo test --workspace --locked` passed before this verification. It was not redundantly rerun.

## Probe Execution

No phase-declared or conventional `probe-*.sh` applies. Production X4 carrier and runtime probes are explicitly Phase 05.4+ work.

## Requirements Coverage

Every requirement ID in all three PLAN frontmatters is accounted for; each plan declares the same three IDs.

| Requirement | Source Plans | Status | Evidence |
| --- | --- | --- | --- |
| OBS-02 | 05.3-01/02/03 | ✓ SATISFIED | Typed source/session/section/batch/record/revision identities, capture window, exact versions, durable records, and monotone acceptance tests exist. |
| OBS-03 | 05.3-01/02/03 | ✓ SATISFIED | Freshness, coverage, quality, availability, unknown/unsupported, uncertainty, and history-only eligibility remain explicit and non-strengthening. |
| OBS-07 | 05.3-01/02/03 | ✗ BLOCKED at phase integration | Individual strict-decode, bounds, replay, completion, and persistence tests pass, but no shared ingress-to-publication lifecycle proves malformed/duplicate/stale input cannot bypass or strand the disconnected seams. |

No additional requirement is mapped to Phase 05.3 in `REQUIREMENTS.md`; OBS-01, OBS-08, VAL-06, and STATE-02/03/04/06 are documented supporting constraints, not PLAN-frontmatter ownership.

## Decision Coverage

The configured decision-coverage query returned `skipped: true`, `reason: no trackable decisions`. This non-blocking heuristic result does not override the explicit accepted decisions manually checked above.

## Test Quality Audit

| Test surface | Linked requirement/truth | Active | Skipped | Circular | Assertion level | Verdict |
| --- | --- | ---: | ---: | --- | --- | --- |
| Domain identity/reconciliation | OBS-02/03 | Yes | 0 | No | Behavioral/value | PASS |
| Contract lifecycle/scheduler | OBS-03/07, roadmap SC1/2/3 | Yes | 0 | No | Shape plus narrow state transitions | BLOCKER for SC1/2 breadth |
| Keyed candidates/completion | OBS-02/03/07 | Yes | 0 | No | Behavioral/value | PASS |
| Port/schema/crash/retention | OBS-03/07 | Yes | 0 | No | Behavioral, file-backed reopen | PASS |

Disabled requirement tests: 0. Circular fixture generation: 0. The two insufficient assertions are the same root gaps already reported, not extra findings.

## Anti-Patterns and Quality Gates

| Check | Result |
| --- | --- |
| Unreferenced `TBD` / `FIXME` / `XXX` | None in phase implementation/test surface |
| Placeholder/empty implementation scan | None material |
| Rust source size | PASS, all repository Rust source files at or below 200 lines |
| `cargo fmt --all -- --check` | PASS |
| `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` | PASS |
| `git diff --check` | PASS |

The inert scheduler policy fields are classified as a blocker because they contradict an explicit must-have, not merely as style debt.

## Human Verification

N/A — infrastructure/foundation phase with no user-facing elements. The failures are observable code/test gaps, not manual-UAT items. Production X4 proof remains intentionally deferred to 05.4+ and is not requested here.

## Deferred-Item Filter

Phase 05.4 clearly owns the native Rust/C/Lua carrier, packaging, reconnect/load/save/cadence behavior, and disposable X4 proof. Phase 05.5/05.6 own heavy ship and station runtime/source remainder. Neither later phase owns the missing generic lifecycle wiring or the generic scheduler policy explicitly promised by 05.3, so neither gap is deferred.

## Gaps Summary

The durable publication half is strong and the accepted-publication authority defect from CR-10 is closed by `bb0da96`. The phase goal is nevertheless not achieved: its generic ingress/feedback/assembly/publication pieces remain separate contract islands, and the scheduler exposes policy knobs that do not implement the accepted urgency/work/debt semantics. These must be closed before Phase 05.4 can safely implement the production carrier against the 05.3 facade.

## Skill Learning

Skill learning: none. Evidence considered: the disconnected lifecycle seams, inert scheduler policy fields, CR-10 authority fix, focused behavioral tests, full quality gates, prior review findings, and the project skill changelog. Existing `live-galaxy-rust-conventions`, `live-galaxy-rust-tests`, and `live-galaxy-code-review` rules already require substantive wiring, bounded work, explicit state transitions, and behavior-level tests; the findings demonstrate missed application of existing rules rather than a missing reusable rule.

---

_Verified: 2026-09-04T17:43:26Z_
_Verifier: the agent (gsd-verifier)_
