---
phase: 05.3-generic-observation-contracts-and-durable-publication
fixed_at: 2026-09-04T17:29:48.9455338Z
review_path: .planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-REVIEW.md
iteration: 3
findings_in_scope: 1
fixed: 1
skipped: 0
status: all_fixed
---

# Phase 05.3: Code Review Fix Report

**Fixed at:** 2026-09-04T17:29:48.9455338Z
**Source review:** `.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-REVIEW.md`
**Iteration:** 3

**Summary:**

- Findings in scope: 1
- Fixed: 1
- Skipped: 0

## Fixed Issues

### CR-10: Publication accepts a stale session when the caller repeats its identity

**Files modified:** `crates/observation-ingest/src/eligibility.rs`, `crates/observation-ingest/src/eligibility/authority.rs`, `crates/observation-ingest/src/lib.rs`, `crates/observation-ingest/tests/completion_contract/eligibility.rs`, `crates/observation-persistence/src/fake.rs`, `crates/observation-persistence/src/lib.rs`, `crates/observation-persistence/src/port.rs`, `crates/observation-persistence/src/record.rs`, `crates/observation-persistence/src/sqlite.rs`, `crates/observation-persistence/src/sqlite_publish.rs`, `crates/observation-persistence/src/sqlite_reconcile.rs`, `crates/observation-persistence/src/sqlite_write.rs`, `crates/observation-persistence/src/types.rs`, `crates/observation-persistence/tests/port_contract.rs`, `crates/observation-persistence/tests/support.rs`
**Commit:** bb0da96
**Applied fix:** Replaced caller-supplied session authority with an opaque accepted-publication capability issued by `DecisionRevisionIndex::accept`. Authority transitions revoke every previously issued capability for the scope, and publication validates the capability immediately before normalization and the SQLite transaction. The adversarial regression accepts session A, makes session B authoritative, then publishes the retained session-A request and proves that no revision, receipt, or current pointer is written. Fixed: requires human verification because this changes current-authority state logic.

## Verification

All verification ran in the isolated review-fix worktree before cleanup.

- `cargo test -p observation-persistence --test port_contract reconnect_keeps_a_delayed_validated_revision_history_only --locked` — passed.
- `cargo test -p observation-ingest --locked` — passed.
- `cargo test -p observation-persistence --locked` — passed.
- `cargo fmt --all -- --check` — passed.
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` — passed.
- `cargo test --workspace --all-targets --all-features --locked` — passed; five pre-existing deferred named-pipe tests remained ignored.
- `cargo run -p source-size-lint --locked -- crates` — passed; all Rust source files remain at or below 200 physical lines.
- `git diff --check` — passed.
- `cargo mutants --version` — unavailable because `cargo-mutants` is not installed; no mutation result is claimed.

## Skipped Issues

None.

## Skill Learning

Skill learning: none. Evidence considered: CR-10's stale-session authority trace, the revocable accepted-capability implementation, the adversarial durable-row regression, focused and workspace tests, format, Clippy, source-size, diff checks, unavailable mutation runner, and the project skill changelog. The fix enforces existing typed-authority and current-state validation rules; it does not establish a missing reusable project-skill rule.

---

_Fixed: 2026-09-04T17:29:48.9455338Z_
_Fixer: the agent (gsd-code-fixer)_
_Iteration: 3_
