---
phase: 05.3
fixed_at: 2026-09-05T14:30:10+07:00
review_path: .planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-REVIEW.md
iteration: 3
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 05.3: Code Review Fix Report

**Fixed at:** 2026-09-05T14:30:10+07:00
**Source review:** `.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-REVIEW.md`
**Iteration:** 3

**Summary:**

- Findings in scope: 5
- Fixed: 5
- Skipped: 0

## Fixed Issues

### P1-01: The fallible hydration API still lets ordinary consumers mint validated authority

**Files modified:** `crates/observation-ingest/src/validated_revision.rs`, `crates/observation-ingest/src/lib.rs`, `crates/observation-ingest/src/eligibility/restore.rs`, `crates/observation-ingest/src/accepted_versions.rs`, `crates/observation-persistence/src/hydration.rs`, `crates/observation-application/src/lifecycle_restore.rs`

**Commit:** `63ca7c4ec87cc10953e0b7f3f4adcee84536127c`

**Status:** Fixed; requires human verification.

**Applied fix:** Durable hydration now returns a distinct `HydratedSectionRevision`. It can restore durable indexes and fences but cannot enter `DecisionRevisionIndex::prepare_publication`, which continues to accept only live `ValidatedSectionRevision` authority. A compile-fail doctest proves the type boundary.

### P1-02: Restart accepts caller-forged freshness and can leave partial authority after failure

**Files modified:** `crates/observation-application/src/lifecycle_restore.rs`, `crates/observation-application/tests/restart_hydration.rs`, `crates/observation-application/tests/support/hydration.rs`, `crates/observation-ingest/src/completion_types.rs`, `crates/observation-ingest/src/eligibility.rs`, `crates/observation-ingest/src/generation.rs`, `crates/observation-ingest/src/wire.rs`, `crates/observation-persistence/src/hydration.rs`, `crates/observation-persistence/src/port.rs`, `crates/observation-persistence/src/types.rs`, and related focused persistence tests.

**Commits:** `adaa87282f576eb36bd92e4b5f06b76b2195a585`, `21613d36780d831dfb9bb06500cc84954e354d61`

**Status:** Fixed; requires human verification.

**Applied fix:** Restore owns its canonical snapshot through the repository, binds each durable receipt to the exact persisted revision, builds cloned provisional index and stager state, and installs both only after the complete snapshot validates. The regression corrupts a receipt, observes no partial mutation, repairs it, and retries successfully.

### P1-03: Multi-section restore can regress the accepted entity-version fence

**Files modified:** `crates/observation-ingest/src/accepted_versions.rs`, `crates/observation-ingest/src/accepted_versions_tests.rs`, `crates/observation-application/src/lifecycle_restore.rs`

**Commit:** `e2dce003c506891cb6ce0bf69d07d9b0857da563`

**Status:** Fixed; requires human verification.

**Applied fix:** Restore constructs one provisional cross-section entity fence, retains the highest observation version independent of section order, accepts identical equal-version content, and rejects equal-version divergent content without installing partial state.

### P1-04: Durable decision pins are not integrity-checked on open or load

**Files modified:** `crates/observation-ingest/src/eligibility.rs`, `crates/observation-persistence/src/sqlite.rs`, `crates/observation-persistence/src/sqlite_pins.rs`, `crates/observation-persistence/tests/schema_contract.rs`

**Commit:** `17d03462a61c9fa9916b5a37181cc942d89626ba`

**Status:** Fixed; requires human verification.

**Applied fix:** Repository open and pin load reject empty, duplicate, incomplete, replaced, or decision-identity-mismatched link sets. Corruption fixtures cover a missing link, a zero-link pin, and replacement with another valid revision.

### P2-01: Publication replay discards the repository's authoritative receipt timestamp

**Files modified:** `crates/observation-application/src/lifecycle_publication.rs`, `crates/observation-application/src/publication.rs`, `crates/observation-application/tests/replay_receipt_time.rs`

**Commit:** `87a3067598c460e63618ce286d8d75ef440dda05`

**Status:** Fixed; requires human verification.

**Applied fix:** New commits, exact replays, and reconciled replays finalize with the repository receipt's durable `accepted_at`. The regression uses a later volatile attempt time and proves the restored revision remains stale according to its original receipt time.

## Verification

All verification ran in the isolated review-fix worktree at `63ca7c4ec87cc10953e0b7f3f4adcee84536127c`.

- P1-01 compile-fail hydration/publication boundary: passed.
- P1-02 receipt binding and atomic retry regression: passed.
- P1-03 reverse-order maximum fence and equal-version conflict regressions: passed.
- P1-04 open/load decision-pin corruption regression: passed.
- P2-01 durable replay timestamp regression: passed.
- `cargo fmt --all -- --check`: passed.
- `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings`: passed.
- `cargo test --locked --workspace --all-targets`: passed; five pre-existing deferred X4 publication tests remained ignored by their explicit phase handoff.
- `cargo run --locked -p source-size-lint -- crates`: passed.
- `git diff --check`: passed.
- Production panic scan found no runtime `unwrap`, `expect`, or `panic!`; matches were test-only modules or Rust lint attributes.
- Push: blocked by the execution approval policy for the unverified `origin` destination; the local branch remains six commits ahead for the orchestrator to push.

## Skill Learning Gate

Skill learning: none. Evidence considered: the five review findings, their focused regressions, strict Clippy, the full workspace suite, and the source-size gate. The fixes apply existing Live Galaxy rules for typed authority, atomic restore, monotonic fences, durable integrity, and receipt-owned time; no new reusable project rule was discovered.

---

_Fixed: 2026-09-05T14:30:10+07:00_
_Fixer: the agent (gsd-code-fixer)_
_Iteration: 3_
