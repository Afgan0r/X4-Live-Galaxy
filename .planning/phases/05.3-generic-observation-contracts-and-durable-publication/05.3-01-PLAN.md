---
phase: 05.3-generic-observation-contracts-and-durable-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/observation-domain/src/identity.rs
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/src/section.rs
  - crates/observation-domain/src/observation.rs
  - crates/observation-domain/tests/identity_section_contract.rs
  - crates/observation-ingest/src/feedback.rs
  - crates/observation-ingest/src/scheduler.rs
  - crates/observation-ingest/src/wire.rs
  - crates/observation-ingest/src/lib.rs
  - crates/observation-ingest/tests/contract_lifecycle.rs
  - crates/x4-bridge/src/facade.rs
  - crates/x4-bridge/src/lib.rs
  - crates/x4-bridge/tests/protocol_contract.rs
autonomous: true
requirements:
  - OBS-02
  - OBS-03
  - OBS-07
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Carrier-neutral contracts keep source, producer incarnation, transport epoch, section revision, batch, record, and decision identities distinct."
    - "Within one source scope, an accepted entity version is monotone: lower versions reject unchanged, exact equal-version/equal-content input replays idempotently, and equal-version/changed-content conflicts."
    - "A developer can distinguish collection progress, local handoff, volatile received, durable committed, terminal rejection, and ambiguous publication without selecting a production carrier."
    - "Ship-like and station-like examples traverse the same strict lifecycle while preserving materially different cardinality, optionality, and empty-set evidence."
    - "Game time changes collection urgency only; injected monotonic real-time permits and finite work limits control admission without time-scale catch-up."
    - "The compile-time X4 facade contains observation and bounded control vocabulary only and proves no X4 runtime behavior."
  artifacts:
    - path: "crates/observation-domain/src/identity.rs"
      provides: "Typed observation identity ladder and exact version values"
    - path: "crates/observation-ingest/src/feedback.rs"
      provides: "Immutable stop-and-wait lifecycle and receiver disposition contracts"
    - path: "crates/observation-ingest/src/scheduler.rs"
      provides: "Separate deterministic collection and transport-pump state machines"
    - path: "crates/observation-ingest/src/wire.rs"
      provides: "Strict size-first carrier-neutral envelope decoding"
    - path: "crates/x4-bridge/src/facade.rs"
      provides: "Compile-time complete-message carrier facade with no implementation"
    - path: "crates/observation-ingest/tests/contract_lifecycle.rs"
      provides: "Two shared local conformance examples and lifecycle behavior evidence"
  key_links:
    - from: "crates/observation-domain/src/identity.rs"
      to: "crates/observation-ingest/src/wire.rs"
      via: "strict envelope fields use distinct exported identity and version types"
      pattern: "SectionKey|SectionRevisionId|BatchId|RecordId"
    - from: "crates/observation-ingest/src/feedback.rs"
      to: "crates/observation-ingest/src/scheduler.rs"
      via: "pump capacity and immutable-slot disposition gate collection work"
      pattern: "Received|Committed|CapacityUnavailable|AmbiguousCommit"
    - from: "crates/x4-bridge/src/facade.rs"
      to: "crates/observation-ingest/src/feedback.rs"
      via: "type-only carrier trait consumes shared complete-message and control outcomes"
      pattern: "try_send_complete|poll_control"
---

<objective>
Establish the carrier-neutral observation lifecycle contracts used by all later Phase 05.3 work.

Purpose: Give OBS-02, OBS-03, and OBS-07 a typed, strict, bounded contract surface while preserving OBS-01, OBS-08, and VAL-06 only as future-runtime constraints rather than claiming them complete.
Output: Distinct identity/version values, strict envelopes, feedback and scheduler state machines, a compile-time facade, and two materially different local conformance examples.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-CONTEXT.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-RESEARCH.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-PATTERNS.md
@docs/ARCHITECTURE.md
@docs/architecture-decisions.md
@docs/architecture-verification.md
@crates/observation-domain/src/identity.rs
@crates/observation-domain/src/section.rs
@crates/observation-domain/src/observation.rs
@crates/observation-ingest/src/wire.rs
@crates/observation-ingest/src/lib.rs
@crates/x4-bridge/src/ingress.rs
@crates/x4-bridge/src/session.rs

<interfaces>
Existing contracts to preserve:
- `EntityId::new`, `ObservationVersion::new`, and the canonical `Ord` derivations in `observation-domain::identity`.
- Independent `SectionFreshness`, `SectionCoverage`, `SectionQuality`, and `SectionState` axes in `observation-domain::section`.
- `WireFrame`, `inspect_frame`, size-before-decode validation, strict Serde tagging, and `deny_unknown_fields` in `observation-ingest::wire`.
- Typed `BackpressureOutcome`, `SessionState`, `SessionGeneration`, and `SequenceNumber` ordering from `x4-bridge`; these are analogs, not runtime evidence.

New exports fixed by this plan:
- `SourceScopeId`, `ProducerIncarnationId`, `TransportEpoch`, `SectionKey`, `SectionRevisionId`, `BatchId`, `RecordId`, and `DecisionSnapshotId`.
- `ObservationSchemaVersion`, `ObservationPolicyVersion`, `CanonicalizationVersion`, and `DigestAlgorithmVersion`.
- `SectionStartEnvelope`, `ImmutableBatchEnvelope`, `SectionCompletionEnvelope`, `ControlEnvelope`, `DeliveryStage`, and `ReceiverDisposition`.
- `ObservationScheduler`, `CollectionPolicyLimits`, `TransportPolicyLimits`, `DeliveredPulse`, and `MonotonicClock`.
- `ObservationCarrierFacade`, `ConnectionState`, `CompleteMessageSendOutcome`, and `ControlPollOutcome` with no production implementation.
</interfaces>
</context>

<file_ownership>
This plan exclusively owns the listed Phase 05.3 contract files during Wave 1. Preserve unrelated concurrent changes. `facade.rs`, `feedback.rs`, `scheduler.rs`, and `contract_lifecycle.rs` are created; every other listed path is modified. Later plans may extend exported types only through their declared dependencies.
</file_ownership>

<scope_rationale>
The 13-file frontmatter is one cohesive carrier-neutral contract slice, not three subsystems: Task 1 owns the identity/disposition tracer, Task 2 owns strict decode plus shared evidence examples, and Task 3 owns deterministic scheduling plus the type-only facade consumer. Each task changes at most five files, has one focused test boundary, and can be reviewed atomically. Cross-task overlap is limited to the ingest export/test composition files; no source file is concurrently owned by another wave because 05.3-02 depends on this plan.
</scope_rationale>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one immutable batch through typed carrier-neutral lifecycle states</name>
  <reversibility rating="costly">These shared identities and dispositions become the compile contract for assembly, persistence, and the future carrier adapter.</reversibility>
  <files>crates/observation-domain/src/identity.rs, crates/observation-domain/src/lib.rs, crates/observation-ingest/src/feedback.rs, crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/contract_lifecycle.rs</files>
  <behavior>
    - Test 1: A valid ship-like section start and immutable application batch move from collection progress to local handoff and volatile `Received`; neither state is `Committed`.
    - Test 2: An exact batch identity with exact immutable bytes returns the prior disposition, while the same identity with changed bytes is a typed terminal conflict.
    - Test 3: `CapacityUnavailable` proves non-consumption and preserves retry eligibility; timeout/supersession, stale epoch, permanent rejection, and ambiguous commit remain distinct and never advance the sender.
    - Test 4: Producer incarnation, transport epoch, section revision, batch, record, and decision identifiers cannot be substituted through one primitive type.
  </behavior>
  <action>First add the failing `contract_lifecycle` tracer for one complete-message happy path and the listed rejection/replay paths. Extend `crates/observation-domain/src/identity.rs` using its existing `EntityId::new` non-empty-string and `ObservationVersion::new` `NonZeroU64` patterns, canonical ordering, and checked accessors; export the exact identity and version symbols listed in the interfaces block. Create `feedback.rs` with one globally unacknowledged immutable application slot and separate `DeliveryStage` versus `ReceiverDisposition` enums. Reuse the validation order from `crates/x4-bridge/src/ingress.rs::BoundedIngress::submit`—compatibility/epoch, identity/sequence, kind/size, then capacity—and the generation/sequence fencing behavior from `crates/x4-bridge/src/session.rs::SessionState`, but do not copy their historical transport policy or claim runtime evidence. The slot must retain the original bytes and digest-bearing identity, permit retry only after proven non-consumption, and never infer durable publication from local handoff or volatile receipt. Keep diagnostics bounded to typed metadata and reason codes; retain no raw rejected payload. Implement only enough production code to make the tracer pass, then refactor without changing the public contract.</action>
  <verify>
    <automated>cargo test -p observation-ingest --test contract_lifecycle --locked</automated>
  </verify>
  <done>The typed identity ladder compiles, the single immutable slot distinguishes every accepted/rejected/ambiguous state, exact replay is idempotent, conflict cannot advance state, and the local tracer passes without a carrier or persistence implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce strict envelopes and prove two shared conformance examples</name>
  <reversibility rating="costly">The strict wire shape and evidence axes are consumed by every later producer and repository adapter.</reversibility>
  <files>crates/observation-domain/src/section.rs, crates/observation-domain/src/observation.rs, crates/observation-domain/tests/identity_section_contract.rs, crates/observation-ingest/src/wire.rs, crates/observation-ingest/tests/contract_lifecycle.rs</files>
  <behavior>
    - Test 1: Whole-message bytes are bounded before strict decode; malformed, oversized, unknown-field, empty-identity, zero-version, and unsupported-version inputs fail without allocating or staging a candidate.
    - Test 2: A non-empty ship-like example with several core records and optional detail uses the same start/batch/completion types as a station-like zero-record example with qualifying known-empty evidence.
    - Test 3: Capture window, freshness, quality, availability, and coverage remain independent; a content certificate never promotes partial, unknown, or unsupported source coverage.
    - Test 4: Canonical record ordering is deterministic across input permutations and an exact record replay differs from a same-identity changed-content conflict.
    - Test 5: Within the same source scope and entity identity, a lower `ObservationVersion` rejects without changing accepted state, equal-version/equal-content is idempotent replay, equal-version/changed-content conflicts, and only a higher valid version may advance.
  </behavior>
  <action>Write the focused domain and lifecycle failures first. Modify `crates/observation-ingest/src/wire.rs` by extending its existing `WireFrame`/`inspect_frame` size-before-Serde pattern with strict tagged `SectionStartEnvelope`, `ImmutableBatchEnvelope`, `SectionCompletionEnvelope`, and bounded small `ControlEnvelope` variants for handshake, demand, disposition, collection intent, health, and reset. Check complete raw-message length before decode, reject unknown fields and exact contract-version mismatches, and fail an oversized inbound message as one unit without treating fragments as recoverable content. Extend `SectionState`/`SectionDescriptor` in `crates/observation-domain/src/section.rs` without collapsing evidence axes. Extend `ObservationRecord` and `classify_duplicate` in `crates/observation-domain/src/observation.rs` into a source-scope-aware monotonic version classifier: lower version rejects unchanged, equal version compares canonical content for replay/conflict, and higher version remains eligible for later validation. Build two fixture constructors inside the tests: ship-like uses non-empty core membership plus failed optional detail without deleting the core; station-like uses zero records and becomes known-empty only when source coverage and identity qualify. Both examples must traverse the shared types and must be described as local contract evidence only.</action>
  <verify>
    <automated>cargo test -p observation-domain --test identity_section_contract --locked</automated>
    <automated>cargo test -p observation-ingest --test contract_lifecycle --locked</automated>
  </verify>
  <done>Strict whole-message decoding and both materially different examples pass; typed evidence is monotone, deterministic, and never fabricates completeness or runtime proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Separate collection from pump policy and expose the compile-time facade</name>
  <reversibility rating="costly">The future Carrier B adapter must implement this facade and cannot compensate for scheduler semantics hidden in a transport.</reversibility>
  <files>crates/observation-ingest/src/scheduler.rs, crates/observation-ingest/src/lib.rs, crates/x4-bridge/src/facade.rs, crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/protocol_contract.rs</files>
  <behavior>
    - Test 1: Each abstract delivered pulse pumps pending immutable bytes first and starts collection only after downstream capacity is reserved.
    - Test 2: Increasing game-time urgency does not increase injected real-time refill, burst, step, heavy-permit, or overrun-debt allowances and causes no proportional catch-up.
    - Test 3: Frozen or coarse clocks remain bounded; capacity loss prevents new getters/work while retaining the exact pending bytes.
    - Test 4: The facade exposes bounded connection, complete-message send, control-poll, and typed-error semantics and exposes no fleet, economy, diplomacy, institution, report, or generic mutation command.
  </behavior>
  <action>Begin with failing scheduler unit cases and `protocol_contract` compile/behavior checks. Create `ObservationScheduler` as two explicit state machines: collection policy and transport pump. Inject `MonotonicClock`, `CollectionPolicyLimits`, and `TransportPolicyLimits`; require finite non-zero constructor values and checked refill/debt arithmetic. Reuse only the typed outcome/fencing precedent in `crates/x4-bridge/src/ingress.rs` and `crates/x4-bridge/src/session.rs`: pump/capacity rejection must not consume sequence or immutable bytes, and epoch change must fence late work. Production code must expose no numerical `Default`; focused tests construct finite fixture-only limits through a test helper and must not reuse the historical generation, frame, byte, or borrowed buffer values identified in CONTEXT.md. Game time may rank urgency only. Create `crates/x4-bridge/src/facade.rs` as a trait/type boundary that consumes shared lifecycle values and supports complete-message sends plus bounded control polling; provide no native, pipe, Lua, Mission Director, callback, packaging, reconnect, save, cadence, or runtime implementation. Keep X4-facing assertions compile-time/local and explicitly non-runtime.</action>
  <verify>
    <automated>cargo test -p observation-ingest scheduler --locked</automated>
    <automated>cargo test -p x4-bridge --test protocol_contract --locked</automated>
  </verify>
  <done>Pump-first and real-time-admission behaviors pass under injected clocks and finite limits, the type-only facade expresses every required outcome, and no production carrier or mutation vocabulary exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Untrusted complete message -> strict envelope | Raw carrier bytes cross into typed observation contracts. |
| Adapter-reported lifecycle -> scheduler | Untrusted or stale epoch/connection signals may influence work eligibility. |
| Compile-time facade -> future carrier | The shared vocabulary constrains what Phase 05.4 may implement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.3-01-01 | Spoofing | Identity ladder / epoch fence | high | mitigate | Distinct non-empty typed identities, exact version matching, and stale-epoch rejection before staging. |
| T-05.3-01-02 | Tampering | Strict envelope decode | high | mitigate | Size-first whole-message rejection, strict Serde fields, exact immutable bytes, and conflict on changed content under one identity. |
| T-05.3-01-03 | Denial of Service | Scheduler and control plane | high | mitigate | Finite injected bounds, checked arithmetic, pump-first reservation, one data slot, and separately reserved bounded terminal/control capacity. |
| T-05.3-01-04 | Information Disclosure | Rejection evidence | medium | mitigate | Bounded typed reason codes only; no raw rejected payload, private path, prompt, or hidden reasoning retention. |
| T-05.3-01-05 | Elevation of Privilege | `ObservationCarrierFacade` | high | mitigate | Closed observation/control vocabulary and contract tests demonstrating no game-state mutation command surface. |
</threat_model>

<risk_gates>
| Gate | Disposition | Evidence boundary |
| --- | --- | --- |
| Research, plan-check, code review, verification | Selected | Required interactive GSD gates. |
| Security review | Selected | Untrusted input, authority vocabulary, replay, and resource bounds change. |
| Rust mutation | Selected after a measured baseline | Target `feedback.rs` disposition transitions and pure `scheduler.rs` permit/debt logic; record runner absence or all survivor dispositions and invent no threshold. |
| X4 probe | Rejected for this phase | Production carrier and runtime lifecycle proof belong to 05.4. |
| SETA soak | Rejected for this phase | Deterministic time-scale policy is local evidence only; actual delivery/load proof belongs to 05.4. |
| AI/eval | Rejected | No model-visible contract, prompt, model selection, or evaluation behavior changes. |
| UI | Rejected | No frontend or custom UI scope. |
| Lua/Busted | Rejected | No Lua, Mission Director XML, or X4 adapter implementation. |
| Nyquist | Deferred unless a real coverage gap appears | Project configuration disables the gate and this plan supplies explicit focused coverage. |
</risk_gates>

<requirement_coverage>
- OBS-02: Task 1 establishes stable typed identities and exact versions; Task 2 carries them through source/time/versioned records and defines per-entity monotonic acceptance/replay/conflict behavior within one source scope.
- OBS-03: Task 2 preserves independent freshness, coverage, quality, availability, unknown, and unsupported states; Task 3 turns staleness into bounded scheduling evidence.
- OBS-07: Tasks 1 and 2 reject malformed, oversized, conflicting, stale-epoch, duplicate, and out-of-order contract input without advancing accepted state.
- OBS-01, OBS-08, VAL-06: supporting constraints only. The facade is bounded and mutation-free, and local evidence is labeled local; completion remains owned by 05.4 and later runtime phases.
</requirement_coverage>

<source_audit>
| Source | Item | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- |
| GOAL | Source-agnostic bounded lifecycle through durable publication | 01-03 | COVERED | Contracts, assembly, and repository form one sequential slice. |
| REQ | OBS-02 | 01-02 | COVERED | Typed identity/version contracts and validated revisions. |
| REQ | OBS-03 | 01-03 | COVERED | Evidence axes, eligibility, immutable revisions, and retained history. |
| REQ | OBS-07 | 01-03 | COVERED | Strict decode, bounded assembly, exact replay/conflict, atomic publication. |
| REQ | OBS-01 / OBS-08 / VAL-06 | 01-03 | COVERED | Supporting contract/evidence boundaries only; no completion claim. |
| RESEARCH | Carrier-neutral strict contracts and two conformance examples | 01 | COVERED | Shared ship-like and station-like local fixtures. |
| RESEARCH | Bounded keyed candidates, completion, dependency and eligibility policy | 02 | COVERED | One candidate per section key plus checked aggregate budgets. |
| RESEARCH | Observation repository, SQLite transaction, retention and crash recovery | 03 | COVERED | Fake and SQLite share one port contract. |
| CONTEXT | Observation/publication decisions | 01-03 | COVERED | Identity, immutable bytes, private validation, one durable transaction. |
| CONTEXT | Scheduling/bounds decisions | 01-02 | COVERED | Pump-first policy, injected clocks, independent finite budgets. |
| CONTEXT | Carrier-neutral feedback decisions | 01-03 | COVERED | Distinct handoff/received/committed/rejected/ambiguous outcomes. |
| CONTEXT | Source uncertainty/lifecycle decisions | 02-03 | COVERED | Uncertainty fence, history-only retention, fresh baseline eligibility. |
| CONTEXT | Deferred production/runtime work | none | EXCLUDED | Carrier B, X4/Lua/MD, heavy ships, station remainder, reports, UI, and model work remain out of scope. |
</source_audit>

<verification>
Run focused checks before the workspace gate:

1. `cargo test -p observation-domain --locked`
2. `cargo test -p observation-ingest --test contract_lifecycle --locked`
3. `cargo test -p observation-ingest scheduler --locked`
4. `cargo test -p x4-bridge --test protocol_contract --locked`
5. `cargo fmt --all --check`
6. `cargo run -p source-size-lint --locked -- crates`
7. `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
8. `cargo test --workspace --locked`
9. `git diff --check`

If the already reviewed `cargo-mutants` runner is available, measure the scoped pure-policy baseline named in the risk gate after ordinary tests pass. If it remains unavailable, record the exact tool failure without installing, substituting, scoring, or claiming mutation evidence.
</verification>

<success_criteria>
- All three tasks follow RED -> GREEN -> REFACTOR and their focused targets pass.
- Every new/modified Rust source file remains at or below 200 physical lines with workspace lints unchanged.
- The two examples share one lifecycle and differ materially in cardinality, optionality, and known-empty evidence.
- Per-entity `ObservationVersion` cannot regress inside one source scope: lower versions preserve accepted state, equal versions are replay only for equal content, equal-version content changes conflict, and only higher valid versions advance.
- No production carrier, runtime connection, save/load policy, cadence, SETA behavior, X4 observation proof, or mutation command is introduced or claimed.
- Security controls cover spoofing, tampering, exhaustion, disclosure, and authority expansion at the actual contract boundaries.
- Final format, size, Clippy, workspace test, and diff checks pass.
</success_criteria>

<output>
Create `.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-01-SUMMARY.md` with exact changed symbols, focused/full verification, evidence classification, security controls, mutation-runner status, and a handoff naming the exports consumed by 05.3-02.
</output>
