---
phase: 05.3-generic-observation-contracts-and-durable-publication
plan: 02
type: execute
wave: 2
depends_on:
  - 05.3-01
files_modified:
  - Cargo.lock
  - crates/observation-ingest/Cargo.toml
  - crates/observation-ingest/src/generation.rs
  - crates/observation-ingest/src/batch_budget.rs
  - crates/observation-ingest/src/batch.rs
  - crates/observation-ingest/src/completion.rs
  - crates/observation-ingest/src/model.rs
  - crates/observation-ingest/src/snapshot.rs
  - crates/observation-ingest/src/lib.rs
  - crates/observation-ingest/tests/keyed_candidates.rs
  - crates/observation-ingest/tests/completion_contract.rs
  - crates/observation-domain/src/reconciliation.rs
  - crates/observation-domain/tests/reconciliation_policy.rs
autonomous: true
requirements:
  - OBS-02
  - OBS-03
  - OBS-07
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Several section revisions can assemble privately and deterministically while exactly one immutable application batch occupies the global unacknowledged slot."
    - "Candidate admission enforces monotone per-entity `ObservationVersion` within one source scope before completion or publication can advance state."
    - "Every candidate and the candidate map obey independent checked byte, count, work, age, and inactivity limits; one failure releases counters and leaves unrelated candidates unchanged."
    - "Only contiguous section-local ordinals plus exact counts, lengths, digests, versions, frozen dependencies, expected pointer, and monotone source evidence produce a validated revision."
    - "Stale required sections and uncertain source boundaries block decision eligibility; prior durable revisions remain history-only until a fresh qualifying baseline exists."
    - "Complete-set absence and deletion remain disabled unless stable identity and same-scope qualifying coverage support two consecutive absences."
  artifacts:
    - path: "crates/observation-ingest/src/generation.rs"
      provides: "Bounded `BTreeMap<SectionKey, Candidate>` assembly with isolated failure"
    - path: "crates/observation-ingest/src/batch_budget.rs"
      provides: "Checked per-candidate and aggregate accounting"
    - path: "crates/observation-ingest/src/completion.rs"
      provides: "Content-bound SHA-256 completion and frozen-dependency validation"
    - path: "crates/observation-ingest/src/snapshot.rs"
      provides: "Exact revision-set freezing and decision eligibility"
    - path: "crates/observation-domain/src/reconciliation.rs"
      provides: "Monotone complete-set absence and deletion policy"
    - path: "crates/observation-ingest/tests/keyed_candidates.rs"
      provides: "Interleaving, limits, replay, expiry, and counter-release evidence"
    - path: "crates/observation-ingest/tests/completion_contract.rs"
      provides: "Completion, dependency, coverage, uncertainty, and eligibility evidence"
  key_links:
    - from: "crates/observation-ingest/src/generation.rs"
      to: "crates/observation-ingest/src/batch_budget.rs"
      via: "every admit, reject, expire, uncertainty reset, and completion path updates checked aggregate charges"
      pattern: "checked_add|checked_sub"
    - from: "crates/observation-ingest/src/completion.rs"
      to: "crates/observation-ingest/src/generation.rs"
      via: "content-bound certificate consumes only one private keyed candidate and yields one immutable validated revision"
      pattern: "ValidatedSectionRevision"
    - from: "crates/observation-ingest/src/snapshot.rs"
      to: "crates/observation-domain/src/reconciliation.rs"
      via: "decision eligibility and absence reconciliation preserve source evidence without strengthening it"
      pattern: "DecisionEligibility|ReconciliationDecision"
---

<objective>
Build bounded keyed private assembly and turn only exact, dependency-current, evidence-monotone candidates into decision-eligible immutable revisions.

Purpose: Complete the volatile validation half of OBS-02, OBS-03, and OBS-07 before any durable repository transaction exists.
Output: Interleaved keyed candidates, immutable batch replay/conflict handling, SHA-256 content-bound completion, coverage-safe reconciliation, uncertainty fencing, and exact revision-set eligibility.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-CONTEXT.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-RESEARCH.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-PATTERNS.md
@.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-01-SUMMARY.md
@docs/architecture-verification.md
@crates/observation-ingest/src/generation.rs
@crates/observation-ingest/src/batch_budget.rs
@crates/observation-ingest/src/batch.rs
@crates/observation-ingest/src/completion.rs
@crates/observation-ingest/src/snapshot.rs
@crates/observation-domain/src/reconciliation.rs

<interfaces>
Consume the exact Plan 01 exports recorded in `05.3-01-SUMMARY.md`, especially `SectionKey`, `SectionRevisionId`, `BatchId`, `RecordId`, `TransportEpoch`, the four exact version types, `SectionStartEnvelope`, `ImmutableBatchEnvelope`, `SectionCompletionEnvelope`, `ReceiverDisposition`, and the injected clock/limit patterns.

Existing implementation to supersede without reviving obsolete policy:
- `GenerationStager` currently owns one `candidate: Option<Candidate>` and clones the accepted snapshot/runtime facts at start.
- `GenerationLimits` currently exposes only staged-byte and work-unit bounds.
- `BatchBudget` already uses checked arithmetic but its historical constants are not production defaults.
- `CompletedScope::new` already sorts/deduplicates canonical keys.
- `ProjectionSnapshot` currently owns mutable observation content and `completed_scopes`.
- `reconcile_membership` already preserves incomplete scopes and produces tombstones only under matching complete evidence.

New stable outputs for Plan 03:
- `ValidatedSectionRevision` with immutable canonical content, capture window, source evidence, exact versions, frozen dependencies, expected current pointer, and manifest/content digests.
- `DecisionRevisionSet`, `DecisionEligibility`, and bounded `EligibilityBlocker` values.
</interfaces>
</context>

<file_ownership>
This plan owns the listed assembly/eligibility files in Wave 2 and may extend Plan 01 types only through their documented contract. `keyed_candidates.rs` and `completion_contract.rs` are created; manifests, lockfile, and remaining listed paths are modified. Preserve unrelated concurrent work and do not alter X4 runtime, Lua, XML, packaging, or persistence adapters.
</file_ownership>

<scope_rationale>
The 13-file scope is one volatile assembly/eligibility boundary: Task 1 replaces the single candidate and owns checked accounting, Task 2 converts one exact private candidate into one validated revision, and Task 3 decides whether accepted revisions may feed a decision. Each task touches at most five files and has an independent focused target; shared files are modified sequentially inside this plan. Plan 03 cannot begin until these exact outputs exist, so the dependency chain removes cross-plan ownership conflicts despite the lockfile/export-file overlap.
</scope_rationale>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace interleaved immutable batches into isolated keyed candidates</name>
  <reversibility rating="costly">The keyed-candidate and accounting shape is shared by completion, diagnostics, and later runtime adapters.</reversibility>
  <files>Cargo.lock, crates/observation-ingest/Cargo.toml, crates/observation-ingest/src/generation.rs, crates/observation-ingest/src/batch_budget.rs, crates/observation-ingest/tests/keyed_candidates.rs</files>
  <behavior>
    - Test 1: Batches for two section keys alternate through the single global immutable transport slot and advance two private candidates independently.
    - Test 2: At most one incomplete revision exists per section key; a new conflicting revision or same batch identity with changed bytes terminates only that key.
    - Test 3: Exact duplicate bytes return the prior `Received` disposition without double charging bytes, records, work, or batch counts.
    - Test 4: Per-candidate and aggregate raw bytes, decoded estimate, records, batches, work, age, and inactivity fail closed at exact/one-over boundaries using checked arithmetic.
    - Test 5: Reject, expiry, supersession, and uncertainty drop release every aggregate charge and leave unrelated candidates byte-for-byte unchanged.
    - Test 6: Against the accepted entity state in the same source scope, lower `ObservationVersion` rejects without any candidate/accepted mutation, equal-version/equal-content replays idempotently, equal-version/changed-content conflicts, and a higher version may stage only after all other checks pass.
  </behavior>
  <action>Write the failing `keyed_candidates` tracer and boundary tables first. In `crates/observation-ingest/src/generation.rs`, replace the current `candidate: Option<Candidate>` and accepted snapshot/runtime-fact clones with a `BTreeMap<SectionKey, Candidate>` that stores only private section content, frozen dependency/current-pointer references, next section-local ordinal, raw/decoded charges, record/batch/work counts, start/last-progress times, capture window, source evidence, exact versions, and incremental digest state. Reuse the atomic private-replacement behavior from `crates/observation-ingest/src/batch.rs::validate_runtime_batch` but never clone or carry forward a baseline revision. Call the Plan 01 source-scope-aware monotonic record classifier before charging/staging: lower version rejects unchanged, equal version is replay only for equal canonical content, equal-version changed content conflicts, and higher version continues through the remaining checks. Refactor `crates/observation-ingest/src/batch_budget.rs` checked-add pattern into injected per-candidate and aggregate non-zero limits with no production `Default`; tests use explicit finite fixture limits that are not exported as runtime policy and do not reuse superseded historical values. Every charge and release is atomic with candidate mutation. Add audited `sha2 = "=0.11.0"` to `crates/observation-ingest/Cargo.toml`, update `Cargo.lock`, and use `Sha256` for immutable canonical batch identity; do not add a custom checksum or another hashing dependency.</action>
  <verify>
    <automated>cargo test -p observation-ingest --test keyed_candidates --locked</automated>
  </verify>
  <done>Two sections interleave through one slot, exact replay is charge-free, conflicts/failures are key-local, every independent limit rejects atomically, and all counter-release tests pass with the audited exact SHA dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate content-bound completion and frozen dependencies</name>
  <reversibility rating="costly">The completion certificate becomes the sole bridge between volatile assembly and durable publication.</reversibility>
  <files>crates/observation-ingest/src/batch.rs, crates/observation-ingest/src/completion.rs, crates/observation-ingest/src/model.rs, crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/completion_contract.rs</files>
  <behavior>
    - Test 1: Only contiguous section-local ordinals with exact batch/record counts, byte lengths, ordered batch-manifest digest, and canonical membership/content digest validate.
    - Test 2: Missing, duplicate, reordered, conflicting, or extra batches and any schema, policy, canonicalization, or digest-version mismatch discard the private candidate and preserve prior accepted state.
    - Test 3: Dependencies and expected current pointer are frozen at start, may change without interrupting collection, and are revalidated exactly at completion; mismatch discards the candidate and emits one bounded recollection disposition whose injected cooldown prevents an immediate churn loop.
    - Test 4: Completion handles its own terminal message, preserves capture window/coverage/quality/availability without strengthening, and yields `ValidatedSectionRevision` but performs no durable publication.
  </behavior>
  <action>Add the failing completion matrix before implementation. Extend `CompletedScope` in `crates/observation-ingest/src/completion.rs`, preserving its existing canonical sort/deduplicate responsibility but rejecting conflicting duplicates, into a content-bound validator over the Plan 01 completion envelope and exact private candidate. Use `crates/observation-domain/src/reconciliation.rs::reconcile_membership` as the no-strengthening precedent: completion may certify exact content but cannot upgrade source coverage. Compute the ordered batch manifest plus canonical membership/content SHA-256 digests using the exact algorithm/version contract. Validate contiguous section-local ordinals independently of transport sequence, exact counts/lengths, all four version types, capture window, source evidence, frozen dependency revisions, and expected current pointer. Return an immutable `ValidatedSectionRevision` on success; on any failure, remove only the private candidate, preserve prior accepted/history state, release counters, append a bounded typed reason through the existing `MAX_REJECTION_EVIDENCE` policy, and arm an injected finite recollection cooldown for dependency churn. No completion result may update a durable pointer or emit `Committed`.</action>
  <verify>
    <automated>cargo test -p observation-ingest --test completion_contract --locked</automated>
  </verify>
  <done>All completion dimensions are exact, stale dependencies/pointers cannot publish, evidence is not strengthened, invalid candidates disappear privately, and the only success output is one immutable `ValidatedSectionRevision` ready for Plan 03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fence source uncertainty and freeze eligible decision revisions</name>
  <reversibility rating="costly">Eligibility and absence semantics determine which durable observations later strategy may treat as current.</reversibility>
  <files>crates/observation-ingest/src/generation.rs, crates/observation-ingest/src/snapshot.rs, crates/observation-ingest/tests/completion_contract.rs, crates/observation-domain/src/reconciliation.rs, crates/observation-domain/tests/reconciliation_policy.rs</files>
  <behavior>
    - Test 1: Adapter-reported start/load/reload or transport-reconnect/epoch uncertainty discards every volatile candidate in the affected source scope, releases counters, and makes prior current revisions history-only without deleting them.
    - Test 2: A decision freezes exact compatible accepted revision identities in canonical section-key order; any stale required section, dependency mismatch, uncertain scope, unknown, partial, or unsupported required evidence yields bounded blockers instead.
    - Test 3: Zero-record known-empty requires qualifying same-scope source coverage and stable identity; partial, failed, or count/fill-only evidence has no absence meaning.
    - Test 4: Deletion requires two consecutive qualifying absences in the same authoritative scope; optional-field failure never deletes a core entity and a scope change resets absence evidence.
    - Test 5: Capacity-conditioned fairness may intentionally starve work under overload, but starvation becomes stale evidence and blocks dependent decisions rather than being hidden.
  </behavior>
  <action>Write the uncertainty, eligibility, and absence-policy failures first. In `crates/observation-ingest/src/generation.rs`, add a source-scope uncertainty transition for start/load/reload and transport reconnect/epoch changes; invalidate only matching volatile work and record no invented campaign UUID or authoritative epoch. In `crates/observation-ingest/src/snapshot.rs`, replace mutable projection eligibility with `DecisionRevisionSet`/`DecisionEligibility`: freeze exact accepted revision IDs and dependencies in deterministic `BTreeMap` order only when every required section is fresh enough, evidence-compatible, monotone by entity version, and inside one unambiguous producer scope. Extend `crates/observation-domain/src/reconciliation.rs::reconcile_membership` from its current matching-complete-marker/tombstone behavior so source coverage and stable identity must already qualify, two consecutive same-scope absences may tombstone, and any partial/failed/optional-field observation preserves the core. Bound and canonically order eligibility blockers/recollection requests, use injected clocks without sleeps, and retain previous revisions as history-only until a fresh qualifying baseline is installed.</action>
  <verify>
    <automated>cargo test -p observation-domain --test reconciliation_policy --locked</automated>
    <automated>cargo test -p observation-ingest --test completion_contract --locked</automated>
  </verify>
  <done>Uncertain boundaries cannot leak old revisions into current decisions, exact eligible revision sets are deterministic, stale or weak evidence blocks decisions visibly, and absence/deletion never exceeds source proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Strict envelope -> private candidate map | Untrusted batches become bounded volatile decoded state. |
| Private candidate -> validated revision | Completion evidence attempts to authorize durable publication. |
| Source/lifecycle evidence -> decision eligibility | Uncertain or stale state attempts to become current strategy input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05.3-02-01 | Tampering | Batch/completion digests | high | mitigate | Ordered SHA-256 manifest/content digests, exact versions/counts/lengths, and same-identity changed-byte conflict. |
| T-05.3-02-02 | Replay | `GenerationStager` | high | mitigate | Exact prior disposition for identical bytes, distinct section-local ordering, stale epoch rejection, and no double charge. |
| T-05.3-02-03 | Denial of Service | Candidate map/accounting | high | mitigate | Independent finite per-candidate/aggregate byte/count/work/age/inactivity limits, checked arithmetic, and complete charge release. |
| T-05.3-02-04 | Spoofing | Frozen dependencies/current pointer | high | mitigate | Typed exact references frozen at start and revalidated at completion before producing `ValidatedSectionRevision`. |
| T-05.3-02-05 | Information Disclosure | Rejection/eligibility diagnostics | medium | mitigate | Bounded canonical reason codes and identities only; never echo raw candidate bytes. |
| T-05.3-02-SC | Tampering | `sha2` dependency | high | mitigate | Use only audited registry package `sha2 = "=0.11.0"`; commit lockfile resolution and admit no alternative hash package. |
</threat_model>

<risk_gates>
| Gate | Disposition | Evidence boundary |
| --- | --- | --- |
| Research, plan-check, code review, verification | Selected | Required interactive GSD gates. |
| Security review | Selected | Untrusted decoded state, replay, resource exhaustion, and eligibility authority change. |
| Rust mutation | Selected after a measured baseline | Target pure coverage monotonicity, counter release, replay/conflict, completion dimensions, dependency checks, and eligibility blockers; record all survivors or runner absence without a threshold. |
| X4 probe | Rejected for this phase | No carrier or runtime adapter is implemented. |
| SETA soak | Rejected for this phase | Time-scale/capacity behavior is deterministic local policy only. |
| AI/eval | Rejected | No model-facing behavior changes. |
| UI | Rejected | No interface/frontend work. |
| Lua/Busted | Rejected | No Lua or Mission Director source changes. |
| Nyquist | Deferred unless a real gap appears | Focused contract tests cover the mapped requirements; the configured gate is disabled. |
</risk_gates>

<requirement_coverage>
- OBS-02: Exact section, batch, record, source, time, and monotonic revision identities survive private assembly and content-bound completion; lower/equal/higher per-entity versions enforce reject/replay/conflict/advance semantics within one source scope.
- OBS-03: Capture window, freshness, quality, availability, coverage, unknown/unsupported states, and source uncertainty remain independent and control eligibility without fabrication.
- OBS-07: Per/aggregate bounds, strict ordinals/digests/versions, exact replay/conflict, dependency/current-pointer revalidation, and key-local failure preserve accepted history.
- OBS-01, OBS-08, VAL-06: supporting constraints only. Assembly accepts the shared contract and introduces no carrier or mutation surface; local tests remain non-runtime evidence.
</requirement_coverage>

<verification>
Run focused checks before the workspace gate:

1. `cargo test -p observation-ingest --test keyed_candidates --locked`
2. `cargo test -p observation-ingest --test completion_contract --locked`
3. `cargo test -p observation-domain --test reconciliation_policy --locked`
4. `cargo test -p observation-ingest --locked`
5. `cargo fmt --all --check`
6. `cargo run -p source-size-lint --locked -- crates`
7. `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
8. `cargo test --workspace --locked`
9. `git diff --check`

After ordinary tests pass, run the already reviewed mutation runner only if available, scoped to `reconciliation.rs`, checked accounting, completion validation, and eligibility policy. Record version, exact command, duration, survivor categories/dispositions, or the exact unavailable-tool result; infer no score.
</verification>

<success_criteria>
- The three focused test targets demonstrate interleaving, exact/one-over bounds, immutable replay/conflict, every completion dimension, uncertainty, eligibility, and absence policy.
- Every counter path is checked and releases exact charges after rejection, expiry, completion, supersession, or uncertainty.
- `ValidatedSectionRevision` is immutable, canonically ordered, content-bound, dependency-current, and does not itself publish.
- Accepted per-entity versions never regress: lower input changes nothing, equal-version replay requires equal content, equal-version changed content conflicts, and only a higher fully valid observation may advance.
- No baseline merge/carry-forward, source-evidence promotion, runtime carrier, Lua/XML, X4 lifecycle claim, production numeric budget, or game-state mutation command is added.
- Security review can trace tampering, replay, exhaustion, stale-scope confusion, and diagnostic disclosure controls to exact files and tests.
- Final format, size, Clippy, workspace test, and diff checks pass with the audited lockfile.
</success_criteria>

<output>
Create `.planning/phases/05.3-generic-observation-contracts-and-durable-publication/05.3-02-SUMMARY.md` with exact `ValidatedSectionRevision`/eligibility interfaces, injected-limit policy, SHA pin and lockfile evidence, focused/full checks, security findings, mutation-runner status, and the persistence handoff for 05.3-03.
</output>
