---
phase: 05-bounded-shadow-deliberation
plan: 05
type: tdd
wave: 5
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - tools/shadow-harness/Cargo.toml
  - tools/shadow-harness/Cargo.lock
  - tools/shadow-harness/src/main.rs
  - tools/shadow-harness/src/subscription_adapter.rs
  - tools/shadow-harness/src/evidence.rs
  - tools/shadow-harness/tests/manual_contract.rs
  - crates/mind-domain/tests/shadow_deliberation_evals.rs
  - shadow-deliberation-evals/v1/manifest.json
  - .planning/phases/05-bounded-shadow-deliberation/05-VALIDATION.md
  - .planning/phases/05-bounded-shadow-deliberation/05-SECURITY-REVIEW.md
autonomous: true
requirements: [MIND-06, MODEL-01, MODEL-03, MODEL-04, MODEL-06, MODEL-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Only an explicit developer benchmark command may invoke local `codex exec --json --output-schema`; it uses the shared typed port, has timeout/byte limits, and is absent from bridge/runtime/normal CI."
    - "The manual harness records redacted, versioned benchmark evidence with provider/model/generation and corpus/configuration identity, while unavailable local auth/model is a typed unavailable result."
    - "All SD-001 through SD-013, guardrails, corpus evidence classes, and Phase 5 monitoring fields have executable deterministic coverage or an explicitly manual benchmark instruction; no Phase 8 quality threshold is introduced."
  artifacts:
    - path: tools/shadow-harness/src/subscription_adapter.rs
      provides: Manual-only local Codex CLI adapter implementing the Phase 5 provider port.
    - path: tools/shadow-harness/src/evidence.rs
      provides: Redacted benchmark evidence record and deterministic manifest validation.
    - path: tools/shadow-harness/tests/manual_contract.rs
      provides: Manual-only invocation and runtime/CI exclusion contract.
  key_links:
    - from: tools/shadow-harness/src/subscription_adapter.rs
      to: crates/mind-orchestration/src/provider_port.rs
      via: Manual adapter implements the same typed provider trait as the fake.
      pattern: ShadowProvider
    - from: tools/shadow-harness/src/evidence.rs
      to: shadow-deliberation-evals/v1/manifest.json
      via: Manual evidence pins corpus hashes, versions, evidence class, and configuration fingerprint.
      pattern: manifest
  prohibitions:
    - requirement_id: MODEL-01
      category: safety
      status: unresolved
      verification: null
      statement: "The subscription harness MUST NOT become a bridge, normal-runtime, normal-CI, public-API, credential, or API-entitlement dependency."
      resolution: "Flagged-unverified phase safety prohibition; explicit manual-only contract and evidence separation are planned."
    - requirement_id: MODEL-07
      category: transparency
      status: unresolved
      verification: null
      statement: "Phase 5 MUST NOT claim real-model strategic quality, observed X4 behavior, playability, or public readiness from fake or local contract evidence."
      resolution: "Flagged-unverified product-transparency prohibition; evidence classes and manual instructions are planned."
---

<objective>
Finish the interchangeable provider boundary with the manual subscription benchmark adapter
and complete deterministic/benchmark evidence separation for the entire Phase 5 corpus.

Purpose: enable controlled real-model evidence without silently adding an API runtime or
letting manual/quality claims leak into CI or X4 scope.
Output: manual-only harness, redacted benchmark evidence, complete corpus gate, and aligned
validation map.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-bounded-shadow-deliberation/05-CONTEXT.md
@.planning/phases/05-bounded-shadow-deliberation/05-AI-SPEC.md
@.planning/phases/05-bounded-shadow-deliberation/05-RESEARCH.md
@.planning/phases/05-bounded-shadow-deliberation/05-PATTERNS.md
@.planning/phases/05-bounded-shadow-deliberation/05-VALIDATION.md
@.planning/phases/05-bounded-shadow-deliberation/05-04-SUMMARY.md
</context>

## Artifacts this phase produces

- Manual `shadow-harness` adapter, evidence record, and manual-only contract test.
- Complete SD-001…SD-013 corpus wiring and benchmark evidence metadata.
- Phase 5 validation map aligned to all five executable plans.
- A Phase 5 ASVS L1 security-review artifact that blocks completion on unresolved high findings.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED manual-only harness isolation and evidence cases</name>
  <files>tools/shadow-harness/tests/manual_contract.rs, crates/mind-domain/tests/shadow_deliberation_evals.rs, shadow-deliberation-evals/v1/manifest.json</files>
  <read_first>Read `05-RESEARCH.md` manual harness contract/pitfalls and environment availability; read AI-SPEC Evaluation Strategy, Tooling and Gates, and Production Monitoring; read `05-PATTERNS.md` manual harness and corpus assignments; read Plan 04 port tests first.</read_first>
  <behavior>
    - Test 1: the manual adapter has the same typed port contract as the fake and returns bounded bytes or typed unavailable/timeout/failure without API credentials.
    - Test 2: normal workspace test/CI commands do not invoke `codex`, and missing local authentication/model availability becomes an unavailable benchmark result rather than an API fallback.
    - Test 3: manual evidence pins corpus/harness/provider/model/generation/prompt/schema/policy/cache identity and redacts raw prompts, candidates, secrets, hidden reasoning, and local paths.
  </behavior>
  <action>Write failing manual-contract and corpus tests before the adapter. The harness-owned contract test must import the Plan 04 public provider-port API and prove the manual adapter has the same typed boundary as the fake, while remaining an explicit developer command outside the normal Cargo workspace/bridge dependency graph. It must also prove deterministic fakes cannot produce a strategic-quality pass. Complete missing SD case assertions, including SD-002 stale/unknown, SD-007 recovery, SD-008 two-cycle cap, SD-009 replay, SD-010 benchmark-only divergence label, and SD-011 continuity. Do not execute the subscription harness in automated tests.</action>
  <verify><automated>cargo test -p mind-orchestration --test provider_contract --locked; cargo test -p mind-domain --test shadow_deliberation_evals --locked</automated></verify>
  <acceptance_criteria>Tests are red before harness files exist; they define no live-model dependency, enforce evidence class, and enumerate all SD-001 through SD-013 cases.</acceptance_criteria>
  <done>The final corpus and isolation requirements are testable without a subscription or external service.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN the explicit local Codex CLI adapter and redacted evidence writer</name>
  <files>tools/shadow-harness/Cargo.toml, tools/shadow-harness/Cargo.lock, tools/shadow-harness/src/main.rs, tools/shadow-harness/src/subscription_adapter.rs, tools/shadow-harness/src/evidence.rs</files>
  <read_first>Read AI-SPEC section 2/3 framework decision and manual benchmark rules; read `05-RESEARCH.md` Standard Stack and manual invocation contract; read `crates/mind-orchestration/src/provider_port.rs` and its public test API once.</read_first>
  <behavior>
    - Test 1: an explicit developer command transforms only canonical request data into local `codex exec --json --output-schema` process input, with hard timeout and output-byte limits.
    - Test 2: the adapter has no X4, checkpoint, report, network API-client, credential, or hidden-prompt capability and returns typed local-process failures.
    - Test 3: a completed manual run records benchmark evidence only, never alters deterministic fixtures, CI expectations, or quality thresholds.
  </behavior>
  <action>Implement a standalone manual tool, excluded from normal workspace members and bridge/runtime dependencies, that implements the Plan 04 typed provider port. Give `tools/shadow-harness/Cargo.toml` its own workspace boundary, declare every local crate as an explicit `path` dependency, run `cargo generate-lockfile --manifest-path tools/shadow-harness/Cargo.toml` deterministically after dependency declaration, and commit the resulting `tools/shadow-harness/Cargo.lock`; verify the manifest-path test resolves those path dependencies with `--locked` before treating the tool as usable. Per D-01, its only real-model path is an explicit developer invocation of the authenticated local `codex exec --json --output-schema` process with canonical request, strict timeout and byte caps, no tool exposure, and typed unavailable/timeout/process/schema failure mapping. Add the resolved manual preflight: unavailable authentication, model configuration, structured output, or required metadata returns a typed unavailable result with redacted availability evidence and never an API fallback. Per D-02, do not read API keys, add an API SDK, imply API entitlement, or use a network client. Write non-authoritative trace/evidence only beneath an explicit developer-supplied `--evidence-dir` sidecar, outside the X4-owned checkpoint and normal runtime/CI; key it by exact request/cache identity and retain only the AI-SPEC monitoring tuple plus corpus/configuration fingerprints. The sidecar must be deletable without changing admitted state. Do not persist raw prompts/candidates, secrets, hidden reasoning, paths, authoritative state, or quality thresholds. Keep every Rust file <=200 lines and use existing pinned dependencies only.</action>
  <verify><automated>cargo generate-lockfile --manifest-path tools/shadow-harness/Cargo.toml; cargo test --manifest-path tools/shadow-harness/Cargo.toml --test manual_contract --locked; cargo test -p mind-orchestration --test provider_contract --locked; cargo test -p mind-domain --test shadow_deliberation_evals --locked</automated></verify>
  <acceptance_criteria>The manual harness compiles and its committed lockfile resolves all declared path dependencies with the manifest-path `--locked` test; contract tests are green without executing a subscription request; local-auth/model absence is bounded/unavailable; automated workspace CI has no executable path to the tool.</acceptance_criteria>
  <done>The developer can deliberately collect redacted benchmark evidence behind the same typed port as the fake, with no public API/runtime/CI dependency.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR corpus completeness, monitoring fields, and validation mapping</name>
  <files>tools/shadow-harness/src/evidence.rs, tools/shadow-harness/tests/manual_contract.rs, crates/mind-domain/tests/shadow_deliberation_evals.rs, shadow-deliberation-evals/v1/manifest.json, .planning/phases/05-bounded-shadow-deliberation/05-VALIDATION.md, .planning/phases/05-bounded-shadow-deliberation/05-SECURITY-REVIEW.md</files>
  <read_first>Read the completed corpus/contract tests; read the AI-SPEC guardrails, offline flywheel, and production-monitoring tables; read `05-RESEARCH.md` Security Domain and Resolved Execution Decisions; read `05-VALIDATION.md` once and retain its focused/full commands.</read_first>
  <behavior>
    - Test 1: manifest validation fails if any SD-001 through SD-013 case, hash/version, deterministic disposition, or evidence class is absent or altered.
    - Test 2: every failure/degradation/cache/replay evidence record has bounded correlation, identity, validator, usage/latency, and recovery fields without sensitive content.
  </behavior>
  <action>Refactor for explicit corpus/evidence types and manifest integrity. Update `05-VALIDATION.md` only to map its task IDs/waves to Plans 01–05, record the manual benchmark command as non-CI evidence, and add the Phase 5 ASVS L1 completion gate. Create `05-SECURITY-REVIEW.md` as the security-review output: record executable evidence for V2 local-client authentication/no-secret handling, V3 non-authoritative session metadata, V4 frozen faction access and admission authority, and V5 bounded strict validation; list finding ID, severity, disposition, evidence command, and remediation. Any unresolved high-severity V2–V5 finding makes the review fail and Phase 5 incomplete; retain the artifact for the normal `$gsd-secure-phase 5` follow-up rather than replacing it. Retain no invented latency/cost/reliability number. Confirm the full guardrail corpus covers exact cache revalidation, all bounds, failure reconciliation, finite dialogue, causal continuity, information/safety rejection, fake separation, and manual-harness isolation. Do not extend into Phase 6 durable diagnostics/report delivery, Phase 7 X4 observation, or Phase 8 baseline-derived quality gates.</action>
  <verify><automated>cargo test --manifest-path tools/shadow-harness/Cargo.toml --test manual_contract --locked; cargo test -p mind-domain --test shadow_deliberation_evals --locked; cargo test --workspace --locked; cargo fmt --check; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo run -p source-size-lint --locked -- crates tools; gsd-secure-phase 5</automated></verify>
  <acceptance_criteria>All 13 SD cases resolve deterministically where marked CI, benchmark-only cases remain non-gating, required redacted monitoring fields exist, the validation map names every final task/wave, `05-SECURITY-REVIEW.md` has V2–V5 evidence and dispositions, and no high-severity security finding remains unresolved.</acceptance_criteria>
  <done>Phase 5 has complete executable deterministic coverage, a blocking ASVS L1 security-review gate, and an explicitly isolated manual benchmark route, without quality, X4, API, or public-readiness claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local Codex CLI → manual adapter | Developer-controlled subscription process returns bounded untrusted content or failure. |
| benchmark evidence → evaluation | Manual evidence is descriptive and versioned, never authority or a CI threshold. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-13 | Information disclosure | harness evidence | high | mitigate | Hash/classify prompt/candidate content and exclude credentials, paths, and hidden reasoning. |
| T-05-14 | Elevation | CLI harness | high | mitigate | Standalone manual tool has only canonical request/process capability and no X4/persistence/report ports. |
| T-05-15 | Repudiation | benchmark record | medium | mitigate | Pin corpus/harness/provider/model/generation/configuration identities and evidence class. |
| T-05-16 | Denial of service | local process | medium | mitigate | Enforce explicit timeout/output-byte limits and typed unavailable/process failure. |
| T-05-SC | Tampering | package installs | high | accept | No package installation is planned; the harness uses existing pinned dependencies only. |
</threat_model>

<verification>
Run the harness contract test by explicit manifest path, focused corpus, workspace suite, formatting, strict Clippy, and source-size lint. Run the subscription command only as a developer-controlled manual benchmark; retain it as benchmark evidence, not a phase quality gate.
</verification>

<success_criteria>
All Phase 5 deterministic contracts and SD corpus cases are green, and any subscription use remains explicit, typed, redacted, bounded, manual-only, and non-authoritative.
</success_criteria>

<output>
Create `.planning/phases/05-bounded-shadow-deliberation/05-05-SUMMARY.md` when done.
</output>
