# Memory Recall (MemPalace)

_Wing: `wing_x4_live_galaxy` · Mode: `augment` · Transport: MCP_

## Prior decisions

- All pre-alpha real-model prototype and benchmark work uses a
  developer-controlled subscription harness. The typed provider boundary stays
  neutral; deterministic fakes cover contracts and replay but cannot satisfy
  strategic-quality acceptance. Public runtime API integration starts on the
  alpha path. — `drawer_wing_x4_live_galaxy_decisions_83e034eeae0266e22c764810`
- The Executive may originate and assign a valid initiative, but the same
  explicit admission path applies as for an institution proposal. —
  `drawer_wing_x4_live_galaxy_decisions_0867da464250f5531970300c`
- An institution cannot silently preempt its own active initiative. It must
  preserve the trigger, prior state, proposed replacement, Executive decision,
  and reason. — `drawer_wing_x4_live_galaxy_decisions_7a4ecd82c439a81958e6a17e`
- Strong caching and reduced token consumption are first-class architecture
  priorities. — `drawer_wing_x4_live_galaxy_decisions_9506977cf12da5507e5a5e8e`

## Patterns

- Use provider-supported stable-prefix caching, deterministic rendered-context
  caching, and exact strategic-decision memoization together. Exact reuse must
  bind faction, snapshot, policy, prompt package, schema, provider, model,
  generation settings, available primitives, and faction profile; approximate
  semantic reuse is forbidden. — MemPalace semantic result, `decisions`, filed
  2026-08-27T22:20:33.761017
- Treat one benchmark or campaign run as a multi-turn trajectory. Provider
  conversation identifiers are grouped beneath a deterministic run identity,
  while bridge-owned typed state and the decision ledger remain authoritative.
  — MemPalace semantic result, `decisions`, filed
  2026-08-27T22:45:45.968531
- Trigger compaction from provider/model token budgets with reserved headroom
  for the next state packet, schema/tool overhead, output, and retry handling.
  Event count is diagnostic only. — MemPalace semantic result, `decisions`,
  filed 2026-08-27T22:59:09.867042

## Surprises / gotchas

- Current subscription model availability is a dated environment observation,
  not a product contract; concrete model choice and role routing must remain an
  empirical Phase 5 result.
- Typed deliberation records are useful observability, but majority voting must
  not weaken Executive authority or deterministic kernel admission.
- The personal temporal knowledge graph contained no entity facts for
  `Live Galaxy`; this recall therefore supplements native GSD artifacts with
  drawer evidence only.
