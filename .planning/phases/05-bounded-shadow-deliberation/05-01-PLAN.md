---
phase: 05-bounded-shadow-deliberation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/mind-domain/src/lib.rs
  - crates/mind-domain/src/deliberation.rs
  - crates/mind-domain/src/admission.rs
  - crates/mind-domain/tests/shadow_deliberation_evals.rs
  - crates/mind-persistence/src/lib.rs
  - crates/mind-persistence/src/deliberation_checkpoint.rs
  - crates/mind-persistence/tests/deliberation_checkpoint.rs
  - shadow-deliberation-evals/v1/manifest.json
autonomous: true
requirements: [MIND-06, MODEL-02, MODEL-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A valid typed Shadow candidate becomes one pending commit and one checkpoint compare-and-set only after the ordered schema, semantic, information, safety, budget, and current-state gates accept it."
    - "Malformed, hidden, stale, unsafe, unsupported, empty, null, oversized, or over-budget candidates leave the aggregate, checkpoint cursor, report intent, and X4 state unchanged."
    - "Accepted plan/posture values contain typed goal or disposition, priority, horizon, supporting fact IDs, expected trade-offs, and a bounded safe explanation, per D-05 and D-09."
  artifacts:
    - path: crates/mind-domain/src/deliberation.rs
      provides: Strict Serde proposal and frozen request domain types.
    - path: crates/mind-domain/src/admission.rs
      provides: Ordered pure admission decision and pending-commit projection.
    - path: crates/mind-persistence/src/deliberation_checkpoint.rs
      provides: Atomic accepted/degraded checkpoint projection.
    - path: shadow-deliberation-evals/v1/manifest.json
      provides: Pinned SD-001 through SD-013 corpus metadata and evidence class.
  key_links:
    - from: crates/mind-domain/src/admission.rs
      to: crates/mind-domain/src/mind.rs
      via: Accepted AdmissionDecision creates the existing PendingMindCommit.
      pattern: AdmissionDecision.*PendingMindCommit
    - from: crates/mind-persistence/src/deliberation_checkpoint.rs
      to: crates/mind-persistence/src/port.rs
      via: One validated envelope uses CheckpointPort::compare_and_set.
      pattern: compare_and_set
  prohibitions:
    - requirement_id: MODEL-02
      category: safety
      status: unresolved
      verification: null
      statement: "Provider candidates MUST NOT create an X4 command, report intent, or authoritative transition before the complete deterministic admission chain succeeds."
      resolution: "Flagged-unverified phase safety prohibition; deterministic no-side-effect cases are planned."
---

<objective>
Deliver the production-quality tracer: one frozen ZYA or ARG candidate crosses strict
typed admission and the existing checkpoint CAS seam without trusting provider bytes.

Purpose: establish the sole Shadow acceptance boundary before any scheduling, dialogue,
cache, or provider adapter expansion.
Output: strict proposal/admission/checkpoint types, executable initial corpus, and one
end-to-end accepted path.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bounded-shadow-deliberation/05-CONTEXT.md
@.planning/phases/05-bounded-shadow-deliberation/05-AI-SPEC.md
@.planning/phases/05-bounded-shadow-deliberation/05-PATTERNS.md
@.planning/phases/04-persistent-full-faction-minds/04-04-SUMMARY.md
</context>

## Artifacts this phase produces

- `DeliberationRequest`, strict `ShadowProposal`, typed trade-offs/explanation, and
  `AdmissionDecision` in `mind-domain`.
- `DeliberationCheckpointRecord` and its checked checkpoint projection.
- `shadow_deliberation_evals` plus `manifest.json` with SD-001 through SD-013 IDs.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED tracer for one valid candidate through admission and checkpoint CAS</name>
  <files>crates/mind-domain/tests/shadow_deliberation_evals.rs, crates/mind-persistence/tests/deliberation_checkpoint.rs, shadow-deliberation-evals/v1/manifest.json</files>
  <read_first>Read `05-AI-SPEC.md` sections 4–6 and the SD-001…SD-013 table; read `05-PATTERNS.md` entries for `mind_checkpoint.rs`, `checkpoint_tracer.rs`, `FakeCheckpointPort`, and `CheckpointEnvelope`; read `crates/mind-domain/tests/mind_checkpoint.rs` and `crates/mind-persistence/tests/fake_port_contract.rs` once as the test analogs.</read_first>
  <behavior>
    - Test 1: a canonical frozen ZYA request plus one valid strict JSON proposal yields one accepted pending commit and one successful checkpoint CAS.
    - Test 2: SD-003 malformed, missing-field, and unknown-field candidates produce classified rejection with no aggregate or checkpoint change.
    - Test 3: SD-001 and SD-013 hidden fact and forbidden primitive candidates have no persistence, report-intent, or X4-effect projection.
  </behavior>
  <action>Create the focused integration-test target and a versioned JSON manifest that pins all SD-001 through SD-013 case IDs, fixture hashes, schema/policy/prompt versions, expected deterministic disposition, and CI-versus-benchmark evidence class. Start RED: compile tests against the planned public API and prove the happy path crosses the existing `MindAggregate` pending-commit and `CheckpointPort::compare_and_set` seams. Keep benchmark labels declarative; per D-03 they cannot become a fake-based quality pass or Phase 8 threshold.</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked</automated></verify>
  <acceptance_criteria>The initial tests fail before production types exist; the manifest contains each SD identifier exactly once with a deterministic expected disposition and no raw prompt, candidate payload, credential, or machine-local path.</acceptance_criteria>
  <done>The tracer contract is red for missing admission/checkpoint behavior and the complete corpus inventory exists.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN the strict ordered admission boundary</name>
  <files>crates/mind-domain/src/lib.rs, crates/mind-domain/src/deliberation.rs, crates/mind-domain/src/admission.rs, crates/mind-persistence/src/lib.rs, crates/mind-persistence/src/deliberation_checkpoint.rs</files>
  <read_first>Read `05-PATTERNS.md` assignments for `deliberation.rs`, `admission.rs`, and `deliberation_checkpoint.rs`; read `crates/mind-domain/src/mind.rs`, `initiative.rs`, and `ledger.rs` once for typed IDs, pending commits, and idempotency; read `crates/mind-persistence/src/checkpoint.rs` and `port.rs` once for one-CAS envelope construction.</read_first>
  <behavior>
    - Test 1: the validator order is strict byte-size/decode, schema, semantic, information, safety, budget, then current-state.
    - Test 2: an accepted plan and Executive diplomatic posture expose typed goal/disposition, priority, horizon, evidence, trade-offs, and bounded explanation.
    - Test 3: empty/null/single-element candidates and equal ordered elements have explicit deterministic outcomes; invalid inputs fail closed.
  </behavior>
  <action>Implement the smallest cohesive, <=200-line modules that make Task 1 pass: direct Serde decoding with `deny_unknown_fields`; named domain enums/identifiers; pure ordered admission that returns immutable accepted or classified rejection decisions; and one checkpoint projection that validates before calling the existing CAS seam once. Use the `ledger::apply` pending-commit and idempotency pattern, and `CheckpointEnvelope` validation/integrity pattern. Per D-05, never coerce unknown/missing fields or persist intermediate validator state. Per D-09/D-10, only model the typed Shadow posture vocabulary; do not create diplomacy institutions, cross-faction exchange, relationship updates, X4 commands, or report intents.</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked; cargo test -p mind-persistence --test deliberation_checkpoint --locked</automated></verify>
  <acceptance_criteria>The valid tracer is green; every failed gate is classified and leaves durable authority untouched; every new Rust source file is at most 200 physical lines and production code has no unwrap, expect, or panic path.</acceptance_criteria>
  <done>One valid Shadow-only candidate reaches exactly one atomic checkpoint path, while malformed, inaccessible, unsafe, stale, and over-budget candidates leave accepted state unchanged.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR and prove the atomic no-side-effect boundary</name>
  <files>crates/mind-domain/tests/shadow_deliberation_evals.rs, crates/mind-persistence/tests/deliberation_checkpoint.rs, crates/mind-domain/src/deliberation.rs, crates/mind-domain/src/admission.rs, crates/mind-persistence/src/deliberation_checkpoint.rs</files>
  <read_first>Read the tests created in Tasks 1–2 and `05-AI-SPEC.md` guardrails/production-monitoring sections; use `05-PATTERNS.md` shared idempotency and diagnostics guidance.</read_first>
  <behavior>
    - Test 1: one failing validator does not allocate an initiative, advance a cursor, mark a cache result admitted, or request a report/X4 effect.
    - Test 2: exact repeated accepted input follows the existing durable idempotency behavior rather than duplicating initiative history.
  </behavior>
  <action>Refactor only after green tests, preserving public behavior. Add explicit assertions for a single CAS, zero partial projection, and bounded diagnostic fields (correlation ID, hashes/versions, class, byte count) without raw secret or reasoning material. Keep authority in typed aggregate/checkpoint data, not candidate bytes or provider conversation content. Do not widen into caching, scheduling, or provider invocation; those are later slices.</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked; cargo test --workspace --locked; cargo fmt --check; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo run -p source-size-lint --locked -- crates</automated></verify>
  <acceptance_criteria>All SD-001/003/004/013 admission assertions are green; a second acceptance cannot duplicate authoritative history; diagnostics remain bounded and redacted.</acceptance_criteria>
  <done>The tracer is production-quality, replay-safe, and proven not to create any partial or X4-facing effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| provider bytes → strict proposal | Untrusted candidate content enters the deterministic kernel. |
| admission → checkpoint port | Only an accepted immutable pending commit may request durable state change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-01 | Tampering | proposal decoding/admission | high | mitigate | Strict Serde/schema and ordered validators reject unknown, malformed, hidden, unsafe, and stale candidates before pending commit. |
| T-05-02 | Elevation | accepted proposal projection | high | mitigate | One pending commit plus one existing checkpoint CAS; no candidate can project X4/report capability. |
| T-05-03 | Information disclosure | rejection evidence | medium | mitigate | Persist only hashes, typed codes, versions, and bounded metadata. |
| T-05-SC | Tampering | package installs | high | accept | No package installation is planned; existing pinned workspace dependencies only. |
</threat_model>

<verification>
Run the focused corpus, persistence contract test, workspace suite, formatter, strict Clippy, and source-size lint. Inspect the manifest for complete SD-001…SD-013 coverage and redacted artifacts.
</verification>

<success_criteria>
The thin valid path works end to end through deterministic admission and one checkpoint CAS; every invalid path has no authoritative or X4-facing side effect.
</success_criteria>

<output>
Create `.planning/phases/05-bounded-shadow-deliberation/05-01-SUMMARY.md` when done.
</output>
