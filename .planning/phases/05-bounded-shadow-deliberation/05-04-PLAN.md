---
phase: 05-bounded-shadow-deliberation
plan: 04
type: tdd
wave: 4
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - Cargo.toml
  - crates/mind-orchestration/Cargo.toml
  - crates/mind-orchestration/src/lib.rs
  - crates/mind-orchestration/src/provider_port.rs
  - crates/mind-orchestration/src/runner.rs
  - crates/mind-orchestration/src/degraded.rs
  - crates/mind-orchestration/tests/provider_contract.rs
  - crates/mind-domain/tests/shadow_deliberation_evals.rs
autonomous: true
requirements: [MODEL-01, MODEL-03, MODEL-06, MODEL-07]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A provider-neutral effect port accepts canonical request data and returns bounded bytes or typed failure, while deterministic domain state contains no I/O, async, or provider response values."
    - "The deterministic fake and later manual harness implement the same port; fake trajectories can prove contracts/replay but cannot be quality evidence, per D-03 and D-04."
    - "Timeout/outage records one bounded degradation, preserves accepted state, pauses new decisions, and requires observation reconciliation before replanning, per D-11."
  artifacts:
    - path: crates/mind-orchestration/src/provider_port.rs
      provides: Narrow interchangeable provider port and bounded response/failure types at the effect edge.
    - path: crates/mind-orchestration/src/runner.rs
      provides: Canonical request execution, exact cache/admission orchestration, and no-side-effect failure mapping.
    - path: crates/mind-orchestration/src/degraded.rs
      provides: Bounded pause/reconcile/replan degradation state.
  key_links:
    - from: crates/mind-orchestration/src/runner.rs
      to: crates/mind-domain/src/admission.rs
      via: Provider/cache bytes call the one shared admission function.
      pattern: admit
    - from: crates/mind-orchestration/src/degraded.rs
      to: crates/mind-domain/src/scheduler.rs
      via: Failure pause and reconciled observation unlock request eligibility.
      pattern: reconcile
  prohibitions:
    - requirement_id: MODEL-07
      category: transparency
      status: unresolved
      verification: null
      statement: "Deterministic fake output MUST NOT be recorded or presented as subscription-backed strategic-quality acceptance."
      resolution: "Flagged-unverified evidence-separation prohibition; corpus evidence class and manual-only contract are planned."
---

<objective>
Put interchangeable provider effects outside the pure kernel, then prove failure/degradation
cannot corrupt accepted Shadow state.

Purpose: make the established tracer callable by a fake without creating an API runtime,
provider vendor coupling, or unbounded retry loop.
Output: `mind-orchestration` effect seam, fake port, runner, and degraded-state contract.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-bounded-shadow-deliberation/05-CONTEXT.md
@.planning/phases/05-bounded-shadow-deliberation/05-AI-SPEC.md
@.planning/phases/05-bounded-shadow-deliberation/05-RESEARCH.md
@.planning/phases/05-bounded-shadow-deliberation/05-PATTERNS.md
@.planning/phases/05-bounded-shadow-deliberation/05-03-SUMMARY.md
</context>

## Artifacts this phase produces

- `ShadowProvider` provider-neutral effect port, bounded provider metadata, and deterministic fake adapter.
- `DeliberationRunner` and `DegradedDeliberation` state for pause/reconcile/replan.
- Provider contract tests and SD-007/009 evidence connected to the focused corpus.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED shared-port fake and degradation contracts</name>
  <files>crates/mind-orchestration/tests/provider_contract.rs, crates/mind-domain/tests/shadow_deliberation_evals.rs</files>
  <read_first>Read `05-PATTERNS.md` provider/harness and fake-fixture assignments; read `crates/mind-persistence/src/fake_port.rs` and its contract test once as the small adapter analog; read AI-SPEC SD-007/009, guardrails, and evidence-track separation.</read_first>
  <behavior>
    - Test 1: a deterministic fake implementing the shared port yields bounded candidate bytes to the runner and has the same identity/admission result as replay.
    - Test 2: timeout/outage records one bounded degraded class, preserves last accepted state, pauses scheduling, and needs a newer reconciled observation before a replacement request.
    - Test 3: concurrent fake/manual-port attempts cannot duplicate a request identity, initiative, or checkpoint CAS.
  </behavior>
  <action>Create RED contract tests for a new effect crate and compile only against its public port/runner API. Use the `FakeCheckpointPort` narrow-boundary pattern, but keep fake provider fixtures deterministic and offline. The tests must demonstrate D-03 evidence separation: fake output can satisfy contract/replay assertions but never a quality label. Include SD-007 and SD-009 fixtures without live network, account, time sleep, or CLI execution.</action>
  <verify><automated>cargo test -p mind-orchestration --test provider_contract --locked; cargo test -p mind-domain --test shadow_deliberation_evals --locked</automated></verify>
  <acceptance_criteria>Contract tests fail before the port/runner exists and express timeout, replay, concurrent identity, and fake-evidence behavior in public observable terms.</acceptance_criteria>
  <done>RED tests define the external effect seam without any public API, API credential, or runtime harness dependency.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN the bounded provider port and degradation runner</name>
  <files>Cargo.toml, crates/mind-orchestration/Cargo.toml, crates/mind-orchestration/src/lib.rs, crates/mind-orchestration/src/provider_port.rs, crates/mind-orchestration/src/runner.rs, crates/mind-orchestration/src/degraded.rs</files>
  <read_first>Read AI-SPEC framework decision and boundary table; read `05-RESEARCH.md` Patterns 1–5 and responsibility map; read the Phase 1 `Cargo.toml` workspace lint policy before adding the minimal member crate.</read_first>
  <behavior>
    - Test 1: the port accepts only a canonical request and returns bounded bytes/metadata or typed failure; it has no persistence, X4, report, tool, credential, or prompt-log capability.
    - Test 2: runner retries only the same canonical identity up to the explicit budget; a terminal failure cannot admit or mutate authoritative state.
    - Test 3: recovery requires newer reconciled packet identity rather than replaying stale work.
  </behavior>
  <action>Add the minimal workspace member `mind-orchestration`, using only existing pinned workspace crates/dependencies. Per D-04, keep the direct Rust provider trait at the effect edge so fake and manual adapters share it; the pure `mind-domain` request, cache identity, validators, and state transitions remain synchronous and provider-type-free. Implement runner wiring so a fake/cache result invokes the Phase 5 admission function, and implement a typed bounded degradation state that carries correlation/request identity, class, attempt/cycle counts, and pause/reconcile state only. Per D-11, preserve accepted state, admit nothing, and prohibit unbounded retry/queue/history. Do not add an async runtime or external SDK; the manual adapter is intentionally deferred to Plan 05.</action>
  <verify><automated>cargo test -p mind-orchestration --test provider_contract --locked; cargo test -p mind-domain --test shadow_deliberation_evals --locked</automated></verify>
  <acceptance_criteria>Provider effects are dependency-inverted, bounded, and isolated; all provider failure tests leave accepted aggregate/checkpoint state untouched and require reconciliation before replan.</acceptance_criteria>
  <done>Fake-driven runner behavior is green while the deterministic domain has no I/O/async/provider-response state and no public API integration.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR evidence separation and bounded metadata</name>
  <files>crates/mind-orchestration/src/provider_port.rs, crates/mind-orchestration/src/runner.rs, crates/mind-orchestration/src/degraded.rs, crates/mind-orchestration/tests/provider_contract.rs, crates/mind-domain/tests/shadow_deliberation_evals.rs</files>
  <read_first>Read completed port modules and AI-SPEC production-monitoring requirements; apply `05-PATTERNS.md` diagnostics/secrecy guidance and the Rust conventions skill's <=200-line/lint requirements.</read_first>
  <behavior>
    - Test 1: each fake trajectory carries deterministic fixture evidence, never a benchmark-success marker.
    - Test 2: degradation records redact raw provider bytes, prompts, hidden reasoning, credentials, and local paths while retaining correlation/identity/classification fields.
  </behavior>
  <action>Refactor only to clarify effect/domain ownership, bounded metadata, and explicit evidence class. Add regressions proving the fake, cache, and a future manual adapter cannot bypass request identity or create a second CAS. Retain no persistent diagnostics backend, X4 report channel, or benchmark threshold: Phase 6 and Phase 8 own those scopes.</action>
  <verify><automated>cargo test -p mind-orchestration --test provider_contract --locked; cargo test --workspace --locked; cargo fmt --check; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo run -p source-size-lint --locked -- crates</automated></verify>
  <acceptance_criteria>Port contracts prove no provider result changes authoritative state until admission, fake evidence stays distinct, and failure evidence is bounded/redacted.</acceptance_criteria>
  <done>The provider-neutral runner is replayable, bounded, and safe under timeout, outage, retry, and recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| provider adapter → runner | External or fake effect produces bounded untrusted bytes/failure only. |
| runner → domain/persistence | Runner may request shared admission but cannot mutate aggregate/checkpoint independently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-10 | Spoofing | provider metadata | medium | mitigate | Record bounded provider/model identity and treat it as non-authoritative metadata. |
| T-05-11 | Denial of service | retry/failure runner | high | mitigate | Enforce same-identity retry/call/timeout limits and pause/reconcile state. |
| T-05-12 | Repudiation | fake benchmark evidence | medium | mitigate | Versioned fixture evidence class distinguishes deterministic contracts from manual benchmarks. |
| T-05-SC | Tampering | package installs | high | accept | No package installation is planned; minimal crate uses existing pinned dependencies only. |
</threat_model>

<verification>
Run provider-contract and focused corpus tests, then workspace formatter, strict Clippy, full suite, and source-size lint. Confirm there is no network/API credential or live-model call in automated tests.
</verification>

<success_criteria>
Interchangeable fake provider effects are bounded and admission-gated; all failure paths retain accepted state and evidence separation is explicit.
</success_criteria>

<output>
Create `.planning/phases/05-bounded-shadow-deliberation/05-04-SUMMARY.md` when done.
</output>
