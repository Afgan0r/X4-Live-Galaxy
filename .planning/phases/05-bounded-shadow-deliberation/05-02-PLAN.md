---
phase: 05-bounded-shadow-deliberation
plan: 02
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - crates/mind-domain/src/cache_identity.rs
  - crates/mind-domain/src/admission.rs
  - crates/mind-domain/src/deliberation.rs
  - crates/mind-domain/tests/shadow_deliberation_evals.rs
  - shadow-deliberation-evals/v1/manifest.json
autonomous: true
requirements: [MIND-06, MODEL-02, MODEL-04, MODEL-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An exact cache key canonically binds faction, snapshot, policy, prompt package, schema, provider, model, generation settings, primitive vocabulary, and compaction identity, per D-12."
    - "Identical tuples are revalidated cache hits; a changed tuple component is a miss, and a current-state or validator failure cannot be bypassed."
    - "All request, context, output, queue, call, retry, history, and dialogue resource limits are explicit configuration fields enforced before admission."
  artifacts:
    - path: crates/mind-domain/src/cache_identity.rs
      provides: Versioned length-framed canonical exact cache identity.
    - path: crates/mind-domain/src/admission.rs
      provides: Cache-hit revalidation and bounded validator inputs.
  key_links:
    - from: crates/mind-domain/src/cache_identity.rs
      to: crates/strategic-state/src/fingerprint.rs
      via: Stable canonical ordering and framed identity construction reuse established fingerprint semantics.
      pattern: ExactCacheKey
    - from: crates/mind-domain/src/admission.rs
      to: crates/mind-domain/src/cache_identity.rs
      via: Cache bytes re-enter the complete admission chain using the current frozen request.
      pattern: revalidate
  prohibitions:
    - requirement_id: MODEL-04
      category: safety
      status: unresolved
      verification: null
      statement: "Exact cache reuse MUST NOT become approximate semantic reuse or bypass a current frozen-request validation gate."
      resolution: "Flagged-unverified phase safety prohibition; deterministic changed-component and revalidation cases are planned."
---

<objective>
Make replay/cache identity exact and all admission resources bounded without permitting
old candidate bytes to become current authority.

Purpose: close the most likely visibility, replay, and exhaustion bypasses after the tracer.
Output: canonical cache identity, revalidation path, and exhaustive bounds tests.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-bounded-shadow-deliberation/05-CONTEXT.md
@.planning/phases/05-bounded-shadow-deliberation/05-AI-SPEC.md
@.planning/phases/05-bounded-shadow-deliberation/05-PATTERNS.md
@.planning/phases/05-bounded-shadow-deliberation/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `ExactCacheKey`, canonical generation settings, and bounded request-budget types.
- Cache-hit revalidation behavior and SD-005/SD-006 corpus assertions.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED exact-key component, ordering, and bound cases</name>
  <files>crates/mind-domain/tests/shadow_deliberation_evals.rs, shadow-deliberation-evals/v1/manifest.json</files>
  <read_first>Read `05-PATTERNS.md` cache identity assignment and the `StrategicPacket::canonical_facts`, `admission_inputs`, and `capsule_identity::build` analogs; read AI-SPEC SD-005/SD-006 and Guardrails before changing test fixtures.</read_first>
  <behavior>
    - Test 1: changing exactly one D-12 identity component creates a miss; the unchanged tuple creates a hit that re-enters all validators.
    - Test 2: equal elements and alternate input orders produce stable canonical keys and outcomes.
    - Test 3: absent/empty/unbounded request, context, output, queue, call, retry, history, and dialogue settings are rejected before side effects.
  </behavior>
  <action>Extend the corpus with RED SD-005/SD-006 tests that mutate one identity component at a time, including faction, snapshot, policy, prompt package, schema, provider, model, generation settings, primitive vocabulary, and compaction identity. Cover empty/null/single-element candidates and stable equal-element ordering. Express bounds as required typed configuration, not guessed measurements; per D-12 cache identity never uses wall-clock or provider-session values.</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked</automated></verify>
  <acceptance_criteria>Tests fail before canonical identity and enforcement exist; manifest records SD-005/006 without conflating deterministic CI outcomes with benchmark quality evidence.</acceptance_criteria>
  <done>RED coverage proves every exact-key component, ordering edge, and missing bound requires implementation.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN canonical identity and complete revalidation</name>
  <files>crates/mind-domain/src/cache_identity.rs, crates/mind-domain/src/admission.rs, crates/mind-domain/src/deliberation.rs</files>
  <read_first>Read `crates/strategic-state/src/fingerprint.rs` and `crates/mind-persistence/src/capsule_identity.rs` once; apply the length-framing and sort-before-hash patterns called out in `05-PATTERNS.md`; read Task 1 tests first.</read_first>
  <behavior>
    - Test 1: a cache hit supplies bytes only; it cannot mark a candidate accepted until strict decode, schema, semantic, information, safety, budget, and current-state checks pass again.
    - Test 2: same tuple preserves deterministic identity; any changed component produces a distinct identity.
    - Test 3: every required bound has an explicit checked value and a classified failure path.
  </behavior>
  <action>Implement a versioned, length-framed `ExactCacheKey` with canonical collection ordering, no wall-clock/session IDs, and the complete D-12 tuple plus compaction and allowed-vocabulary identities. Add checked `RequestBounds` values for queue depth, request/context/output bytes, provider calls, retries, timeout, retained history, and dialogue cycles; every field is required, non-zero, and capped by the versioned configuration contract, with no production default. CI fixtures supply declared bounded values solely to prove enforcement; an absent/invalid profile is a fail-closed disposition. Route cached candidate bytes through the same pure admission function as a miss; do not create an alternate fast path. Retain the <=200-line cohesive-module rule and avoid adding dependencies or a cache storage backend, which Phase 6 owns.</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked; cargo test -p mind-domain --locked</automated></verify>
  <acceptance_criteria>All one-component key mutations miss, identical canonical tuples hit then revalidate, and each configured resource category is checked rather than assumed.</acceptance_criteria>
  <done>Exact replay identity and bound validation are green with no cache bypass or storage-side authority.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR identity diagnostics and replay assertions</name>
  <files>crates/mind-domain/src/cache_identity.rs, crates/mind-domain/src/admission.rs, crates/mind-domain/tests/shadow_deliberation_evals.rs</files>
  <read_first>Read the completed cache/admission modules once and the AI-SPEC production-monitoring table; follow `05-PATTERNS.md` diagnostics guidance to keep identity evidence hash-only.</read_first>
  <behavior>
    - Test 1: a stale cached proposal fails current-state or information validation without checkpoint change.
    - Test 2: diagnostics identify the cache hit/miss, changed identity class, validator outcome, and bound name without raw prompt/candidate data.
  </behavior>
  <action>Refactor for one canonical serialization path and compact typed diagnostics. Add property-style permutations around order stability and a stale-hit regression. Preserve the tracer's one-CAS/no-partial-write invariant; do not introduce semantic matching, persistence schema changes, provider calls, or benchmark thresholds.</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked; cargo test --workspace --locked; cargo fmt --check; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo run -p source-size-lint --locked -- crates</automated></verify>
  <acceptance_criteria>SD-005/006 are green, every cache decision is deterministic and redacted, and source-size/lint gates remain clean.</acceptance_criteria>
  <done>Cache identity cannot merge adjacent candidates or admit stale bytes, and all resource bounds fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| frozen request → cache key | Authority-bearing request components become replay identity. |
| cache bytes → admission | Non-authoritative cached bytes are treated exactly as untrusted provider bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-04 | Tampering | exact cache key | high | mitigate | Canonical length-framed identity includes every D-12 component and has changed-component tests. |
| T-05-05 | Elevation | cache hit path | high | mitigate | Cache hits re-enter all validators using the current frozen request before pending commit. |
| T-05-06 | Denial of service | request budgets | high | mitigate | Explicit checked bounds cover bytes, context, calls, retries, queue, history, and dialogue. |
| T-05-SC | Tampering | package installs | high | accept | No package installation is planned; existing pinned workspace dependencies only. |
</threat_model>

<verification>
Run the focused corpus plus full workspace, formatting, strict Clippy, and source-size checks. Confirm no identity diagnostic contains raw provider content.
</verification>

<success_criteria>
Exact-key cache behavior is reproducible and revalidated; every admission resource category is bounded and observable through typed redacted evidence.
</success_criteria>

<output>
Create `.planning/phases/05-bounded-shadow-deliberation/05-02-SUMMARY.md` when done.
</output>
