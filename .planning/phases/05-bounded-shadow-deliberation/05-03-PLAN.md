---
phase: 05-bounded-shadow-deliberation
plan: 03
type: tdd
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - crates/mind-domain/src/scheduler.rs
  - crates/mind-domain/src/deliberation.rs
  - crates/mind-domain/src/admission.rs
  - crates/mind-persistence/src/deliberation_checkpoint.rs
  - crates/mind-persistence/tests/deliberation_checkpoint.rs
  - crates/mind-domain/tests/shadow_deliberation_evals.rs
  - shadow-deliberation-evals/v1/manifest.json
autonomous: true
requirements: [MIND-07, INST-04, INST-05, INST-06, INST-07, MODEL-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Strategic ticks, relevant events, and cooldowns form one bounded, deduplicated outstanding request per faction without blocking or waiting, per D-06."
    - "Executive originate, assign, approve, revise, preempt, reject, and terminate dispositions travel through deterministic admission and retain single-owner causal evidence, per D-07."
    - "Every INST-05 replacement is a typed preemption request carrying trigger, prior initiative state, suspend-or-cancel disposition, replacement, Executive decision, and preserved reason before it can cross admission."
    - "Direct agreement has zero dialogue cycles; only objection, mandate, revision, or preemption reaches at most two full kernel-owned cycles and one final disposition, per D-08."
  artifacts:
    - path: crates/mind-domain/src/scheduler.rs
      provides: Pure per-faction trigger coalescing, pause/reconcile eligibility, and resource-bound dispositions.
    - path: crates/mind-domain/src/deliberation.rs
      provides: Executive/institution dialogue state and typed final disposition.
    - path: crates/mind-domain/src/admission.rs
      provides: Admission-gated preemption request projection that cannot produce a ledger command on an incomplete causal record.
    - path: crates/mind-persistence/src/deliberation_checkpoint.rs
      provides: Versioned checkpoint projection that preserves the admitted preemption causal record for recovery and replay.
  key_links:
    - from: crates/mind-domain/src/scheduler.rs
      to: crates/mind-domain/src/admission.rs
      via: Scheduler emits a frozen request eligibility disposition rather than provider I/O.
      pattern: RequestEligibility
    - from: crates/mind-domain/src/deliberation.rs
      to: crates/mind-domain/src/ledger.rs
      via: Final admitted Executive disposition maps to existing typed initiative command/preemption causal record.
      pattern: InitiativeCommand
  prohibitions:
    - requirement_id: INST-07
      category: product-safety
      status: unresolved
      verification: null
      statement: "Institutions MUST NOT gain political refusal, sabotage, unbounded conversation, or cross-faction negotiation power in the Shadow Director slice."
      resolution: "Flagged-unverified product-boundary prohibition; finite state-machine cases are planned."
---

<objective>
Add bounded per-faction request eligibility and Executive–institution arbitration without
turning provider conversation into authority or bypassing admission.

Purpose: make the tracer usable for ticks, events, cooldowns, initiative replacement, and
the narrow typed ZYA–ARG posture contract.
Output: pure scheduler and dialogue state machine with causal lifecycle assertions.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-bounded-shadow-deliberation/05-CONTEXT.md
@.planning/phases/05-bounded-shadow-deliberation/05-AI-SPEC.md
@.planning/phases/05-bounded-shadow-deliberation/05-PATTERNS.md
@.planning/phases/05-bounded-shadow-deliberation/05-01-SUMMARY.md
@.planning/phases/05-bounded-shadow-deliberation/05-02-SUMMARY.md
</context>

## Artifacts this phase produces

- Pure `DeliberationScheduler` eligibility/paused/reconciled disposition types.
- Kernel-owned dialogue-cycle state and final Executive disposition projection.
- SD-008/009/011/012 scheduler, continuity, preemption, and dialogue cases.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED scheduler coalescing, interruption, and cooldown cases</name>
  <files>crates/mind-domain/tests/shadow_deliberation_evals.rs, shadow-deliberation-evals/v1/manifest.json</files>
  <read_first>Read the `scheduler.rs` and test assignments in `05-PATTERNS.md`; read `extensions/live_galaxy/lua/live_galaxy_scheduler.lua` once as the bounded-disposition analog; read AI-SPEC SD-005/007/009/011/012 requirements.</read_first>
  <behavior>
    - Test 1: duplicate tick/event triggers coalesce to one outstanding faction request; a concurrent or interrupted trigger cannot create a second owner or call.
    - Test 2: cooldown ineligibility returns an explicit disposition without waiting; queue/call/retry bounds stop new work deterministically.
    - Test 3: timeout pauses the faction, requires newer reconciled observation before replan, and preserves accepted state.
  </behavior>
  <action>Add RED corpus cases for per-faction coalescing, event relevance, cooldown eligibility, parallel/interrupted trigger ordering, and D-11 recovery. Treat the Lua `MAX_SLICES_PER_TICK` explicit-disposition behavior as the data-flow analog only: the Rust scheduler remains pure and returns typed outcomes, performs no provider I/O, and never waits. Use declared bounded CI fixture values only to prove queue/call/retry/timeout/history/dialogue enforcement; those values are not benchmark claims. Missing, zero, malformed, or over-cap versioned configuration is a fail-closed disposition, while the real-model path remains unavailable until its matching manual measured profile exists.</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked</automated></verify>
  <acceptance_criteria>Tests fail before scheduler behavior exists and prove one per-faction outstanding request survives duplicate, parallel, interrupted, cooldown, and timeout paths.</acceptance_criteria>
  <done>RED coverage locks scheduler safety, pause/reconcile behavior, and no-duplicate admission expectations.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN finite arbitration and causal replacement state</name>
  <files>crates/mind-domain/src/scheduler.rs, crates/mind-domain/src/deliberation.rs, crates/mind-domain/src/admission.rs, crates/mind-persistence/src/deliberation_checkpoint.rs, crates/mind-persistence/tests/deliberation_checkpoint.rs, crates/mind-domain/tests/shadow_deliberation_evals.rs, shadow-deliberation-evals/v1/manifest.json</files>
  <read_first>Read `05-PATTERNS.md` `deliberation.rs`, scheduler, and deliberation-checkpoint assignments; read `crates/mind-domain/src/initiative.rs` and `ledger.rs` once for `InitiativeCommand::Preempt`, active-slot, and same-command patterns; read `crates/mind-persistence/src/checkpoint.rs` once for versioned payload projection; read Task 1 tests and the SD-010 posture corpus entries in `shadow_deliberation_evals.rs` and `shadow-deliberation-evals/v1/manifest.json` first.</read_first>
  <behavior>
    - Test 1: an Executive-originated or institution-originated action can only produce the existing initiative command after admission accepts it.
    - Test 2: preemption retains trigger, previous state, suspend-or-cancel disposition, replacement, Executive decision, and reason with exactly one active owner.
    - Test 3: direct agreement terminates at zero cycles; exceptional paths stop after two complete cycles with one final admission-tested disposition.
    - Test 4: SD-010 has one deterministic frozen faction-visible packet fixture for each D-09 posture variant — `maintain`, `de-escalate`, `intensify`, and `limited threat-driven coordination` — and accepts each only when every cited fact ID belongs to that packet; the coordination fixture includes a visible threat fact.
    - Test 5: SD-010 rejects an unsupported posture value, any cited hidden/stale/non-packet fact, and any candidate carrying a negotiation, X4 command, report intent, or relationship-change request; each rejection leaves admitted posture, initiative state, checkpoint state, and external effects unchanged.
  </behavior>
  <action>Implement cohesive <=200-line pure scheduler and dialogue modules. Model strategic tick/event/cooldown eligibility, one outstanding request per faction, bounded pause/reconcile state, and a kernel-owned cycle counter. Define a typed `PreemptionRequest` whose constructor requires the trigger, complete prior `Initiative` state, explicit suspend-or-cancel disposition, typed replacement proposal, Executive final decision, and bounded preserved reason; reject incomplete or inconsistent records before the admission projection. Route that request through `admission.rs`, then map only the accepted projection to existing `InitiativeCommand::Preempt` and ledger behavior, and include the full record in the pending checkpoint payload so restart/replay retains it. Per D-07 the Executive cannot directly execute. Per D-08, direct agreement bypasses dialogue, while only material objection, mandate, revision, or preemption can advance a cycle; third-cycle construction is impossible. Per D-09, define the closed posture enum `maintain`, `de-escalate`, `intensify`, and `limited threat-driven coordination`; add one deterministic SD-010 corpus fixture per variant, canonicalize its frozen faction-visible fact IDs, and admit a posture only after information validation proves every citation is in that frozen packet. Require the coordination variant to cite a packet-visible threat fact. Reject unsupported enum values and candidates that request negotiation, a diplomacy institution, cross-faction model exchange, X4 command, report intent, or relationship mutation before checkpoint projection. Per D-10, posture is Shadow-only typed evidence and cannot produce an external command, report, relationship change, or dialogue with another faction.</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked; cargo test -p mind-domain --locked; cargo test -p mind-persistence --test deliberation_checkpoint --locked</automated></verify>
  <acceptance_criteria>SD-008/011/012 and all four SD-010 posture fixtures are green; each accepted D-09 variant cites only its canonically ordered frozen faction-visible fact IDs, and limited threat-driven coordination cites a visible threat fact. SD-010 rejects unsupported posture values, hidden/stale/non-packet citations, and negotiation, X4-command, report-intent, or relationship-change candidates with no admitted posture, initiative transition, checkpoint transition, report intent, or X4 effect. An INST-05 preemption fails without any one of trigger, prior state, suspend-or-cancel disposition, replacement, Executive decision, or reason; a valid preemption survives checkpoint replay with exactly one active owner; no path admits direct Executive execution or a duplicate active owner.</acceptance_criteria>
  <done>Scheduling and arbitration add real bounded capability while every final initiative state and D-09 posture, including a fully typed preemption record, crosses the Phase 5 admission boundary; only the four frozen-packet-grounded Shadow posture variants can be admitted, and D-10 external mutation and negotiation paths remain rejected.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR causal/replay properties and resource diagnostics</name>
  <files>crates/mind-domain/src/scheduler.rs, crates/mind-domain/src/deliberation.rs, crates/mind-persistence/src/deliberation_checkpoint.rs, crates/mind-persistence/tests/deliberation_checkpoint.rs, crates/mind-domain/tests/shadow_deliberation_evals.rs</files>
  <read_first>Read AI-SPEC evaluation rubric for causal continuity/non-repetition and monitoring guardrails; use the `initiative_lifecycle.rs` and `mind_checkpoint.rs` analogs listed in `05-PATTERNS.md`.</read_first>
  <behavior>
    - Test 1: replayed frozen tuple and command identity preserves one owner, predecessor evidence, and final disposition.
    - Test 2: bounded scheduler/degradation diagnostics identify trigger/cycle/bound state but not provider prose or hidden faction data.
  </behavior>
  <action>Refactor the finite state transitions around explicit enums and typed diagnostic codes. Add deterministic permutations for preemption, repeated trigger, and interruption ordering; preserve stable canonical outputs. Keep no async, lock, provider-response type, queue worker, or persistence writer in domain state. Do not add reporting/diagnostics storage (Phase 6), live X4 behavior (Phase 7), or quality scoring/thresholds (Phase 8).</action>
  <verify><automated>cargo test -p mind-domain --test shadow_deliberation_evals --locked; cargo test -p mind-persistence --test deliberation_checkpoint --locked; cargo test --workspace --locked; cargo fmt --check; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo run -p source-size-lint --locked -- crates</automated></verify>
  <acceptance_criteria>Scheduler and dialogue property cases remain green after refactor; all diagnostics are bounded/redacted; every new source file remains <=200 lines.</acceptance_criteria>
  <done>One owner, one causal predecessor, zero-or-two-cycle boundary, and all resource limits are deterministic under replay and interruption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| trigger inputs → scheduler | Tick/event/cooldown signals request eligibility but do not gain provider or persistence authority. |
| dialogue candidate → final disposition | Provider discussion cannot set cycle count, owner, or lifecycle state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-07 | Denial of service | scheduler | high | mitigate | One outstanding faction request plus bounded queue/call/retry/cooldown/cycle dispositions. |
| T-05-08 | Elevation | Executive arbitration | high | mitigate | Final Executive action uses prior admission then existing initiative ledger transition only. |
| T-05-09 | Tampering | preemption/dialogue | high | mitigate | Kernel-owned finite state and causal predecessor fields prevent silent replacement or third cycle. |
| T-05-SC | Tampering | package installs | high | accept | No package installation is planned; existing pinned workspace dependencies only. |
</threat_model>

<verification>
Run the focused corpus, workspace suite, formatting, strict Clippy, and source-size lint. Inspect all dialogue cases for no cross-faction negotiation or X4-facing effect.
</verification>

<success_criteria>
Every trigger remains bounded/deduplicated, and Executive–institution arbitration is finite, causal, and still admission-gated.
</success_criteria>

<output>
Create `.planning/phases/05-bounded-shadow-deliberation/05-03-SUMMARY.md` when done.
</output>
