---
phase: 05.5-heavy-faction-ship-conformance
plan: 02
status: superseded
superseded_by: [05.5-09, 05.5-10, 05.5-11]
type: execute
wave: 2
depends_on: [05.5-01]
files_modified:
  - docs/development/heavy-ship-probe.md
  - tests/fixtures/heavy-ship-source-contract.json
  - tests/heavy-ship-evidence.ps1
  - .planning/phases/05.5-heavy-faction-ship-conformance/05.5-SOURCE-EVIDENCE.md
autonomous: false
requirements: [OBS-06, VAL-01, VAL-02, VAL-06, VER-LG-003, VER-LG-011, VER-LG-012]
user_setup:
  - service: Disposable local X4
    why: Only the owner may perform game actions or supply runtime observations.
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Every ship field has an explicit source, units, applicability rule and evidence strength before detail implementation relies on it.
    - Runtime observations and immutable documentation evidence remain distinct.
    - Unresolved applicable fields and unknown faction dispositions block acceptance rather than becoming unsupported success.
  artifacts:
    - path: docs/development/heavy-ship-probe.md
      provides: Written owner-operated disposable-X4 probe and stop conditions
    - path: tests/fixtures/heavy-ship-source-contract.json
      provides: Sanitized source shape, applicability and numerical representation fixtures
    - path: .planning/phases/05.5-heavy-faction-ship-conformance/05.5-SOURCE-EVIDENCE.md
      provides: Evidence-backed source decisions and residual blockers
  key_links:
    - from: docs/development/heavy-ship-probe.md
      to: tests/fixtures/heavy-ship-source-contract.json
      via: Explicit source/query/snapshot identity for every observed field
---

<objective>
Resolve the precise runtime source contract needed for D-01 through D-04 and D-06 through D-12 without claiming either heavy-workload gate. Preserve D-14 and D-16: vanilla plus installed DLC plus Live Galaxy only, disposable campaigns, owner-operated game, no save access. This source probe is evidence gathering, not full-set collection or Gate A acceptance.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-RESEARCH.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-PATTERNS.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-01-SUMMARY.md
@.agent-instructions/x4/AGENTS.md
@.agent-instructions/x4/MEMORY.md
</context>

## Artifacts this phase produces

All four files in `files_modified` are new artifacts. The JSON contract introduces `source_snapshot`, `runtime_snapshot`, `claim_kind`, `getter`, `input_shape`, `output_shape`, `units`, `numeric_representation`, `applicability_evidence`, `observation_outcome`, `capture_interval`, `source_strength`, `faction_origin_evidence`, `stop_reason`, and evidence IDs. These are test/evidence fields, not new wire authority or gameplay flags. The evidence checker introduces `-SelfTest` and `-EvidenceFile`; it has no X4-control operation. The Markdown record introduces source-resolution and blocked-claim tables. No installed path or corpus-bearing locator enters Git.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the source probe and executable evidence-shape checker</name>
  <files>docs/development/heavy-ship-probe.md, tests/fixtures/heavy-ship-source-contract.json, tests/heavy-ship-evidence.ps1</files>
  <read_first>Read the phase RESEARCH source matrix and its two immutable snapshot identities, D-01 through D-16 in CONTEXT, the new tracer SUMMARY, the shared X4 Docs lookup/handoff contract, and the common test plus X4 test skills. The paths in files are new; do not populate observations from memory.</read_first>
  <behavior>RED: an absent provenance ID, merged outcome or fabricated successful run is rejected by explicit failing self-test assertions before checker implementation. GREEN implements those checks; REFACTOR reruns the same assertions. Valid sanitized evidence is accepted without executing any game operation.</behavior>
  <action>Create a written disposable Creative Custom campaign procedure with purpose, exact mod stack, build identities, source calls, owner actions, capture method, finite per-call/allocation/attempt/wall-time stop conditions, abort/unload procedure and an empty results template. First use the registered Docs evidence already identified by RESEARCH; any genuinely unresolved claim gets a narrow Docs query and a shared knowledge-gap handoff if still unresolved. Preserve separately game snapshot x4-9.00-steam-23660954 / blake3:8d30413b740369e8572d6a7647366be338421698596eee84d62fec0415685c79 and probe snapshot x4-probe-faction-ship-runtime-686318dd-v1 / blake3:01ea3b0d39afee3541a1c7e87ce2c1c8fc011ce688584966952c0d20e7a90c82; the second is observed documentation/probe evidence, not a stronger engine guarantee. Specify canonical faction and ship count/fill, core owner/location/type shape, cargo quantities/capacity/occupied volume units, aggregate people role/capacity/qualification scale, installed engine/shield/weapon/turret/software/virtual-thruster representation, missiles and every drone/unit class with false selection as researched. Check installed-slot APIs, not loadout templates. Include ownership/location changes, disappearance versus terminal absence, docked/nonlocal cases, empty versus zero and churn mismatch. Discover faction IDs once for taxonomy evidence, not full workload acceptance: ordinary, small/pirate, XEN/KHK, service/temporary/player/mod-added origin and explicit unknown. Require approved finite probe limits before any source call; derive proposals from prior measured limits plus local bounded allocation tests, label all unmeasured callback costs unknown, and stop for owner choice if a safe finite probe envelope cannot be justified. Never silently promote prior clock timings into ship timings. Write the PowerShell checker with self-tests rejecting absent provenance, fabricated observed status, missing required source fields, leaked machine paths and merged zero/empty/N/A/unknown states. It parses evidence only and never launches or operates X4.</action>
  <reversibility rating="reversible">A written procedure and sanitized fixtures can be revised without game-state or protocol migration.</reversibility>
  <acceptance_criteria>Every required field has a query and an initially unresolved evidence slot; runtime results start pending; the procedure contains an owner action boundary before game work and finite abort limits; checker self-tests reject a fabricated successful run.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -SelfTest</automated><fails_when>Exit is nonzero, an invalid fixture is accepted, or the self-test invokes X4 or touches saves.</fails_when></verify>
  <done>The owner can execute the written probe without interpreting missing source or safety requirements.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Owner performs the approved disposable source probe</name>
  <instructions>Approve or replace the finite probe envelope in docs/development/heavy-ship-probe.md, perform its disposable-X4 source steps yourself, and return the recorded run identity and observations. Stop at any listed abort condition.</instructions>
  <verification>The agent compares the returned observations with the written field/source matrix and the exact approved probe envelope; unresolved shapes remain blockers.</verification>
  <resume-signal>Reply in ordinary chat with the explicit envelope approval and the completed source-probe evidence, or identify the failed/pending rows. Silence is not approval.</resume-signal>
  <files>docs/development/heavy-ship-probe.md</files>
  <read_first>Read the completed probe, all pending source rows and the exact proposed finite probe limits. Inspect tracer local-test results before asking for game work.</read_first>
  <action>STOP. Present the owner the written procedure, exact proposed limits, unmeasured risks, requested evidence and abort conditions. Ask the owner to approve or replace those limits and perform the listed in-game actions. The agent must not launch X4, configure its campaign, use UI automation, manipulate ships, load/reload, engage SETA or inspect saves. The owner supplies observations and build/probe identities. Ask for one deliberately heavy ordinary eligible faction candidate selected from measured ship count/detail volume; record selection rationale, not a convenient small fixture. This checkpoint may return partial evidence: unresolved source shape, faction classification or unsafe indivisible native cost remains blocked, and no success is inferred from silence or a log file's mere existence.</action>
  <reversibility rating="reversible">Only owner-authorized disposable runtime observation is requested; no production state migration is admitted.</reversibility>
  <acceptance_criteria>The owner explicitly acknowledges the procedure and provides traceable results or a stated blocker; no game action is attributed to the agent; Gate A and Gate B remain pending.</acceptance_criteria>
  <verify><human-check>Compare owner-supplied run identity, source records and abort conditions with the written procedure. Missing or ambiguous evidence blocks this checkpoint. There is deliberately no fake automated X4 command.</human-check></verify>
  <done>Owner evidence is available for normalization, or execution is paused with the exact missing claim.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize source evidence and lock implementation inputs</name>
  <files>tests/fixtures/heavy-ship-source-contract.json, .planning/phases/05.5-heavy-faction-ship-conformance/05.5-SOURCE-EVIDENCE.md</files>
  <read_first>Read owner-supplied evidence, the fixture schema, all phase RESEARCH residual unknowns and D-02 through D-13. Read the private-artifact retention rule if any raw evidence cannot be committed.</read_first>
  <action>Record documented, observed, inferred and unknown separately for every field and faction-classification claim. Preserve independent core and detail capture intervals; record exact integer/float limits, units and qualification scale without rounding identity through Lua number. Source-dependent N/A needs evidence; inaccessible/unknown/unsupported cannot erase applicability. Record count/fill-only strength and count/fill mismatch observations without inventing snapshot atomicity. Produce sanitized executable fixtures for all shapes needed by Plan 03, including zero-capacity, zero crew, empty cargo/ammunition/drone lists, absent components and terminal unavailability. Exhaust supported source alternatives through the documented lookup/handoff gate for applicable missing data; leave unresolved closure-blocking rows explicit. Private raw evidence must use the shared durable machine-local locator/digest contract, never a temporary-only retention claim or machine path in Git. Mark the next plan ready only when its required shapes/units/applicability decisions have actual evidence. Record provisional calibration samples separately from approved acceptance thresholds; numerical acceptance is not finalized here.</action>
  <reversibility rating="reversible">Evidence and fixtures are versioned and can be corrected while preserving provenance.</reversibility>
  <acceptance_criteria>All Plan 03 source shapes are evidence-backed or execution is explicitly blocked; no unresolved applicable field becomes an unsupported waiver; runtime evidence is never relabelled as complete-set engine semantics.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -EvidenceFile tests/fixtures/heavy-ship-source-contract.json</automated><fails_when>Exit is nonzero, required implementation claims remain unknown, provenance is missing, or state distinctions were collapsed.</fails_when></verify>
  <done>A source contract is ready for implementation; workload conformance still awaits measured Gate A and Gate B.</done>
</task>

<verification>
The evidence checker proves artifact shape and consistency only. Only the owner checkpoint can supply runtime observations. Verify each of the two immutable snapshot boundaries and each residual unknown independently; do not reopen settled research broadly or claim this probe proves all-faction scalability.
</verification>

<success_criteria>
The source-dependent implementation has exact, reviewable inputs, a selected heavy ordinary-faction candidate, and no silent unsupported exceptions. Any unresolved applicable field or unbounded source operation pauses execution with the claim and lookup/probe evidence needed to resume.
</success_criteria>

<output>Create `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-02-SUMMARY.md` with owner checkpoint evidence and unresolved blockers, never invented run results.</output>
