---
phase: 05.5-heavy-faction-ship-conformance
plan: 11
type: execute
status: planned
wave: 4
depends_on: [05.5-10]
files_modified:
  - .planning/phases/05.5-heavy-faction-ship-conformance/05.5-REVIEW.md
  - .planning/phases/05.5-heavy-faction-ship-conformance/05.5-SECURITY.md
  - .planning/phases/05.5-heavy-faction-ship-conformance/05.5-MUTATION.md
  - .planning/phases/05.5-heavy-faction-ship-conformance/05.5-VALIDATION.md
  - .planning/phases/05.5-heavy-faction-ship-conformance/05.5-VERIFICATION.md
  - tests/x4-disposable/05.5-heavy-ship-procedure.md
  - tests/x4-disposable/05.5-heavy-ship-evidence.md
  - docs/development/heavy-ship-probe.md
  - docs/architecture-verification.md
autonomous: false
requirements: [OBS-06, VAL-01, VAL-02, VAL-06, VER-LG-003, VER-LG-011, VER-LG-012]
user_setup:
  - service: owner-operated X4 disposable stand
    why: Only the owner performs normal-speed and approximately 6.2x SETA full-set runtime actions.
    dashboard_config:
      - task: Execute the reviewed finite Gate-B procedure on its exact package/profile and return evidence.
        location: Owner-controlled disposable vanilla plus DLC plus Live Galaxy stand.
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The reviewed production collector obtains every applicable core/cargo/crew/loadout field across the full required dynamic faction set, including small/pirate factions and XEN/KHK.
    - Ordered Gate A then Gate B have normal and sustained SETA evidence, independently durable readback and approved latency/freshness/recovery/headroom.
    - Code and security review converge, targeted safety mutation has a measured baseline and no unexplained non-equivalent survivor, and final full regression passes for the tested identity.
    - statement: E01 OBS-06 boundary min/max and one-step-outside cases have evidence.
      verification: backstop
    - statement: E02 OBS-06 precision, overflow and rounding/ties preserve source meaning.
      verification: backstop
    - statement: E03 VAL-01 unclassified edge assumption is covered by layered executable and manual evidence.
      verification: backstop
    - statement: E04 VAL-02 unclassified edge assumption has normal/SETA responsiveness and stability evidence.
      verification: backstop
    - statement: E05 VAL-06 unclassified edge assumption has exact used-X4-source evidence.
      verification: backstop
    - statement: E06 VER-LG-003 boundary count/fill/allocation/call ceilings are independently enforced.
      verification: backstop
    - statement: E07 VER-LG-003 precision keeps IDs and clock arithmetic lossless and bounded.
      verification: backstop
    - statement: E08 VER-LG-011 unclassified edge assumption covers every profile consumer and sustained workload stability.
      verification: backstop
    - statement: E09 VER-LG-012 adjacency distinguishes exact-equal identity from conflict and cross-faction substitution.
      verification: backstop
    - statement: E10 VER-LG-012 empty/null/singleton/zero/NA outcomes remain distinct.
      verification: backstop
    - statement: E11 VER-LG-012 ordering is deterministic including equal-priority ties.
      verification: backstop
  prohibitions:
    - statement: Ship collection must not grant foreign information to recipients or create a Faction Mind, including for XEN/KHK.
      status: unverified
      verification: backstop
      flagged: true
    - statement: Conformance work must not mutate X4, operate the owner's game or access saves.
      status: unverified
      verification: backstop
      flagged: true
    - statement: Missing applicable data or an unperformed run must not be presented as successful conformance.
      status: unverified
      verification: backstop
      flagged: true
  artifacts:
    - path: .planning/phases/05.5-heavy-faction-ship-conformance/05.5-VERIFICATION.md
      provides: Goal-backward verdict with explicit remaining blockers and station-only handoff
    - path: tests/x4-disposable/05.5-heavy-ship-evidence.md
      provides: Actual ordered Gate-A/Gate-B evidence and source-strength non-claims
  key_links:
    - from: .planning/phases/05.5-heavy-faction-ship-conformance/05.5-VALIDATION.md
      to: .planning/phases/05.5-heavy-faction-ship-conformance/05.5-VERIFICATION.md
      via: All requirements, decisions, eleven assumptions and three prohibitions receive explicit evidence dispositions
---

<objective>
Close the real heavy-faction observation outcome, not merely plan/task completion.
Enforce D-01–D-16 through independent review, full dynamic runtime evidence and
honest source/field/performance verdicts; preserve the station-specific 05.6 boundary.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-EXECUTION-REVISION.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-VALIDATION.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-10-SUMMARY.md
@docs/ARCHITECTURE.md
@docs/architecture-decisions.md
@docs/architecture-verification.md
</context>

## Artifacts this phase produces

New phase REVIEW, SECURITY, MUTATION and VERIFICATION files at the frontmatter
paths. VALIDATION was introduced by this replan and is updated with actual evidence.
New evidence sections: exact reviewed commit/package/profile, finding dispositions,
mutation baseline/caught/surviving/unviable results, complete dynamic roster/field
matrix, stage performance/headroom, eleven assumption verdicts and three prohibition
backstops. No new runtime API, package or feature flag is planned. In-scope review
fixes use the earlier plan-owned files and record exact deviations and reruns.

## Selected risk gates

Code review and independent goal verification are mandatory. Security review is
selected despite the global default because native input, resource bounds,
authority and persistence change. Targeted mutation is selected for the changed
deterministic bounds, classification, dependencies and recovery invariants.
Requirement-to-test/Nyquist audit is selected for this exposed coverage gap.
Owner X4 runtime evidence and sustained SETA are mandatory. AI specification/evals
are not applicable: no model/prompts/model-facing strategic contract changes.
Supported-mod compatibility remains later; this stand is vanilla+DLC+Live Galaxy.
Carrier A, station implementation, minds, recipient visibility and game mutation
are outside scope, not shortcuts to closure.

<tasks>

<task type="auto">
  <name>Task 1: Converge review and mutation evidence, then run the final regression</name>
  <files>.planning/phases/05.5-heavy-faction-ship-conformance/05.5-REVIEW.md, .planning/phases/05.5-heavy-faction-ship-conformance/05.5-SECURITY.md, .planning/phases/05.5-heavy-faction-ship-conformance/05.5-MUTATION.md, .planning/phases/05.5-heavy-faction-ship-conformance/05.5-VALIDATION.md, tests/x4-disposable/05.5-heavy-ship-procedure.md, docs/development/heavy-ship-probe.md</files>
  <read_first>Read current files in files, the exact complete phase implementation diff including preserved 01, 09/10 summaries, accepted Gate-A evidence and every limit consumer. Read .agents/skills/live-galaxy-code-review/SKILL.md and references/fan-out.md; code-conventions/SKILL.md plus references/logging.md and tooling.md; tests/SKILL.md plus references/test-standards.md; rust-conventions/SKILL.md; rust-tests/SKILL.md; x4-integration/SKILL.md plus references/lua-md-conventions.md; x4-tests/SKILL.md; lua/SKILL.md, using their full live-galaxy-prefixed paths. Name every exact applicable path in delegated prompts.</read_first>
  <action>Run the mandatory GSD code-review and selected security-review gates using configured project routing and the review skill's bounded specialist/adjudication process. Audit actual production wiring, every configured-limit consumer, scheduler placement, retained-memory bounds, strict ABI input, source honesty, durable outcome/restart semantics, developer diagnostics, source provenance and Rust file size. Verify assertion-level RED/GREEN/REFACTOR records for behavior changes; compilation errors alone are not RED evidence. Resolve in-scope findings in their owning implementation, with focused tests; stop for owner direction only if a serious finding contradicts accepted scope/architecture.

  Run targeted cargo-mutants using the installed tool in its default copied workspace, not in-place. Select changed deterministic safety functions in observation-domain, observation-ingest and x4-bridge/native input as supported by the tool, record the measured passing baseline and exact invocation, and classify caught, surviving, equivalent and unviable mutants. Strengthen tests and rerun for every non-equivalent safety survivor; do not invent a percentage threshold or relabel unexplained survivors. Preserve the original source checkout and user changes.

  Complete the requirement-to-test audit in VALIDATION, including all eleven assumptions and three descriptor-less prohibition backstops. Once fixes/reviews/mutations converge, run the full regression below for the exact new identity, plus actual local detail/recovery/full-set scenarios and existing clock scenario. Rebuild the exact package and finalize Gate-B instructions only after these checks. If a fix changes behavior covered by Gate A, revalidate affected approved-profile local checks and obtain affected owner Gate-A reruns before Gate B; an old pass cannot certify changed behavior. Later code fixes likewise reconverge and repeat final regression.</action>
  <acceptance_criteria>Review findings have verified dispositions; selected security mitigations have evidence; no unexplained non-equivalent safety mutant survives; the final complete regression passes after fixes for the exact Gate-B package. All assumptions have test or explicit human-backstop ownership.</acceptance_criteria>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <fails_when>Non-zero exit or formatting differences remain.</fails_when>
    <automated>cargo clippy --locked --workspace --all-targets -- -D warnings</automated>
    <fails_when>Non-zero exit or a lint warning remains.</fails_when>
    <automated>cargo test --locked --workspace -- --test-threads=1</automated>
    <fails_when>Non-zero exit or required tests are skipped/ignored to obtain green.</fails_when>
    <automated>cargo run --locked -p source-size-lint -- crates</automated>
    <fails_when>Non-zero exit or a Rust file exceeds the allowed size.</fails_when>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite all</automated>
    <fails_when>Non-zero exit or a required Lua/MD contract suite is omitted.</fails_when>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/x4-package-conformance.ps1</automated>
    <fails_when>Non-zero exit or package/runtime dependencies fail conformance.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario actual-chain -LimitsFile config/carrier-b-limits.json</automated>
    <fails_when>Non-zero exit or the pre-existing clock producer/publication contract regresses.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-full-set -LimitsFile config/heavy-ship-limits.json</automated>
    <fails_when>Non-zero exit or the reviewed full-set path lacks independent earlier/current durable readback.</fails_when>
  </verify>
  <done>Reviewed and regression-tested production identity is ready for owner Gate B; runtime success is still pending.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Run ordered full dynamic-set Gate B at normal speed and sustained SETA</name>
  <files>tests/x4-disposable/05.5-heavy-ship-evidence.md</files>
  <read_first>Read the current evidence file, exact final procedure/package/profile identity, accepted numerical thresholds and heavy Gate-A evidence; review/security/mutation dispositions; dynamic roster and source-strength contracts. No save access.</read_first>
  <precondition>Task 1 converged; Gate A remains valid for the relevant production behavior; the owner-approved finite Gate-B procedure and package are ready.</precondition>
  <action>Per D-01–D-16, give the owner the prepared procedure and stop for their actual game execution. The owner runs the full dynamically discovered eligible vanilla/DLC independent faction set, explicitly including small/pirate factions and XEN/KHK, with sustained normal and approximately 6.2x SETA windows. Require every discovered ID to appear in the included/excluded/unknown matrix; source-backed exclusions remain documented and unknown dispositions block closure. Zero-ship factions remain represented honestly without fabricated completeness/deletion.

  The agent analyzes returned evidence and independently reopens durable data. Match exact run/package/profile identity, core and detail parent/member revisions, independent capture intervals, every applicable cargo/crew/loadout field and multiple changing revisions. Validate the full per-stage timing/rate/allocation/backlog, latency/freshness, vanilla responsiveness, recovery and approved positive headroom criteria from 10, under aggregate full-set load rather than multiplying single-faction averages. Include finite approved bridge absence/reconnect, late feedback, producer/bridge restart and owner load/reload cases; incomplete work cannot become current after recovery. Compare baseline and accelerated windows without claiming engine atomicity or hiding unobserved windows. Each unknown/inaccessible/unsupported/stale applicable field, starvation, growing backlog, headroom shortfall or absent sustained window is a failed/pending gate, even if the carrier delivered bytes.

  If a defect is within accepted scope, agent repairs through its owning plan, reconverges reviews and local regression, repackages and requests only the affected owner rerun. A product/architecture conflict returns to the owner instead of changing scope. Keep required raw capture outside Git in durable private storage with exact verified digests/locator; commit only sanitized metrics, field/disposition matrix, evidence IDs and conclusions.</action>
  <instructions>Owner executes the exact finite Gate-B procedure and returns identified evidence. Agent performs provenance checks, quantitative comparison and independent readback. Local harness success is not substituted for any game window.</instructions>
  <verification>Verify full discovered-faction/required-field coverage, exact dependencies, actual normal/SETA windows and every approved performance/recovery/headroom threshold against owner evidence and independent readback.</verification>
  <resume-signal>Owner-supplied normal and sustained SETA Gate-B evidence for the exact reviewed package/profile, or an explicit stop report.</resume-signal>
  <acceptance_criteria>Full required dynamic set and every applicable field pass normal and sustained SETA limits, fairness/freshness/recovery and measured headroom. Every discovered ID has an evidenced disposition, independent durable history/current readback matches, and Gate-A-before-Gate-B ordering is documented.</acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -SelfTest</automated>
    <fails_when>Non-zero exit or negative evidence tests accept missing required rows, fabricated approval or pending runtime as a pass.</fails_when>
    <human-check>Owner X4 execution and actual returned evidence are mandatory. Agent invokes the prepared RuntimeAcceptance checker with the supplied evidence path and inspects independent durable readback; the checker validates consistency, not runtime authenticity by itself.</human-check>
  </verify>
  <done>Gate B has actual passing evidence or remains explicitly pending/failed; a missing run never completes this task.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the goal and close only the proven heavy-ship scope</name>
  <files>.planning/phases/05.5-heavy-faction-ship-conformance/05.5-VALIDATION.md, .planning/phases/05.5-heavy-faction-ship-conformance/05.5-VERIFICATION.md, docs/development/heavy-ship-probe.md, docs/architecture-verification.md</files>
  <read_first>Read current files in files, ROADMAP success criteria, REQUIREMENTS scope ownership, CONTEXT D-01–D-16, REPLAN source audit, all active summaries, source evidence, actual Gate-A/Gate-B records and final review/security/mutation evidence. Read no old superseded summary as new completion.</read_first>
  <action>Use the independent GSD verifier for goal-backward verification of the delivered production capability, not just completed task boxes. Map every GOAL/REQ/RESEARCH/CONTEXT item from REPLAN to actual code/tests/runtime evidence in VALIDATION. Resolve all eleven edge assumptions explicitly: executable proof where available, otherwise the declared human backstop. An unverified backstop yields human_needed, not silent pass. Independently adjudicate all three descriptor-less prohibitions against code scope, execution evidence and artifact claims; retain flagged-unverified when evidence is insufficient. Separate source-documented, source-observed, runtime-observed, inferred and unknown facts throughout.

  Verify actual high-volume collection, receiver staging/commit capacity, bounded allocation/backlog/retention, tail latency and freshness, recovery and numerical headroom across both gates. Require all applicable field rows and dynamic faction dispositions, not a source availability checklist. Report residual count-fill consistency as such; no transport receipt creates atomicity, complete-set authority or deletion. Confirm runtime source acceptance has not leaked into recipient visibility or Mind admission.

  Write the final goal verdict with exact identities and concise non-claims. Update docs/architecture-verification.md only for earned VER-LG-003/011/012 evidence, preserving history and broader open gates. OBS-06 closure is the heavy shared ship path only; list any genuinely station-specific remainder for conditional 05.6 without implementing it or broadening this phase. No supported-mod, public-alpha, report, action, mutation, SEED-003 foreign-visibility or new-Mind claim follows. If any required runtime/field/performance evidence is missing, leave the phase incomplete and name the concrete next owner/agent action.</action>
  <acceptance_criteria>Every source-audit row has actual evidence or an explicit blocker; eleven assumptions and three prohibitions have dispositions; the verdict cannot be passed with missing applicable data, unperformed windows or unexplained performance/headroom gaps. Station-only remainder and non-claims are explicit.</acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -SelfTest</automated>
    <fails_when>Non-zero exit or fixture/provenance/outcome rejection contracts regress.</fails_when>
    <human-check>Independent goal verification checks actual evidence and all backstop assumptions/prohibitions. Machine-readable consistency alone cannot discharge a manual runtime or authority claim.</human-check>
  </verify>
  <done>05.5-VERIFICATION.md truthfully reports the achieved heavy-ship goal or exact gaps; no runtime gap is hidden by task completion.</done>
</task>
</tasks>

<threat_model>
| Threat | STRIDE | Severity | Disposition | Required evidence |
| --- | --- | --- | --- | --- |
| Forged fields, faction or parent authority | Tampering/elevation | high | mitigate | Receiver-owned strict input/dependency/disposition negative tests and security review |
| Native stall, allocation flood, unbounded nested groups/backlog | Denial of service | high | mitigate | Independent enforced bounds, finite risk envelope, stage tails and both owner gates |
| Replay, lost responses or stale incarnation becomes current | Spoofing/repudiation | high | mitigate | Exact durable receipts and independent multi-revision/restart readback |
| Private captures or foreign observations escape their scope | Information disclosure | high | mitigate | Sanitized diagnostics/artifacts and recipient/Mind prohibition backstop |
| Source-only or local-only checks presented as runtime success | Tampering | medium | mitigate | Distinct evidence types, exact identities, owner windows and independent verifier |
| Dependency supply-chain change | Tampering | low | accept | No new dependency planned; legitimacy gate required before any added package |
</threat_model>

<verification>Code/security review, targeted mutation, final full regression, owner normal/SETA Gate B and independent goal verification are all mandatory. An unresolved manual/backstop item remains human_needed. No new generic validation service or research wave is required.</verification>

<success_criteria>Actual heavy collection across the complete required dynamic faction set meets all accepted field, authority, boundedness, recovery and measured performance/headroom requirements, with honest evidence and no scope expansion.</success_criteria>

<output>Create .planning/phases/05.5-heavy-faction-ship-conformance/05.5-11-SUMMARY.md only with the actual gate/verifier verdicts, exact evidence identities and any still-pending work.</output>
