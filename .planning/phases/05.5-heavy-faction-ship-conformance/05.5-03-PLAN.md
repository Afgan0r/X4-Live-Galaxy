---
phase: 05.5-heavy-faction-ship-conformance
plan: 03
status: superseded
superseded_by: [05.5-09, 05.5-10, 05.5-11]
type: tdd
wave: 3
depends_on: [05.5-02]
files_modified:
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/src/ship_detail.rs
  - crates/observation-domain/src/ship_group.rs
  - crates/observation-domain/tests/ship_contract.rs
  - tests/fixtures/heavy-ship-source-contract.json
  - crates/x4-carrier-native/src/lua_input.rs
  - crates/x4-carrier-native/src/producer_types.rs
  - crates/x4-carrier-native/src/producer_collection.rs
  - crates/x4-carrier-native/src/producer_message.rs
  - crates/x4-carrier-native/src/ship_detail_input.rs
  - extensions/live_galaxy/lua/live_galaxy_ship_details.lua
  - extensions/live_galaxy/lua/live_galaxy_observation.lua
  - extensions/live_galaxy/tests/carrier_b_contract.lua
  - crates/x4-bridge/src/receiver_context.rs
  - crates/x4-bridge/tests/ship_contract.rs
autonomous: true
requirements: [OBS-06, VAL-01, VAL-06, VER-LG-003, VER-LG-012]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Cargo, aggregate crew and installed-loadout detail groups traverse the production carrier and durable receiver independently of core.
    - Each detail group binds the exact core/member revision and keeps its own capture interval, source evidence and freshness.
    - Missing applicable detail cannot masquerade as empty, zero, not applicable or accepted completeness.
    - A rejected detail group preserves an already valid core without satisfying heavy-workload closure.
  artifacts:
    - path: crates/observation-domain/src/ship_detail.rs
      provides: Typed cargo, crew and installed-loadout observations
    - path: extensions/live_galaxy/lua/live_galaxy_ship_details.lua
      provides: Resumable source-backed detail capture
    - path: crates/x4-carrier-native/src/ship_detail_input.rs
      provides: Strict bounded typed detail input parsing
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_ship_details.lua
      to: crates/x4-carrier-native/src/ship_detail_input.rs
      via: Owned structured values, applicability and outcome states
    - from: crates/observation-domain/src/ship_group.rs
      to: crates/x4-bridge/src/receiver_context.rs
      via: Receiver-validated exact core/member dependency and capture interval
---

<objective>
Expand the proven one-faction tracer with every required detail group per D-02, D-03, D-04, D-05, D-11, D-12 and D-13. The result remains observation-only and does not create recipient visibility, minds, reports or strategic behavior.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-RESEARCH.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-PATTERNS.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-02-SUMMARY.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-SOURCE-EVIDENCE.md
@tests/fixtures/heavy-ship-source-contract.json
</context>

## Artifacts this phase produces

New paths: `crates/observation-domain/src/ship_detail.rs`, `crates/x4-carrier-native/src/ship_detail_input.rs`, `extensions/live_galaxy/lua/live_galaxy_ship_details.lua`. Plan 01 created `ship_group.rs` and both `ship_contract.rs` test targets; Plan 02 created the fixture. New symbols: `CargoObservation`, `CrewObservation`, `InstalledLoadoutObservation`, `CargoEntry`, `CrewAggregate`, `InstalledComponent`, `ConsumableEntry`, `DetailDependency`, and `parse_ship_detail`. New fields: ware identity/quantity, capacity/occupied volume with source units; aggregate role count/capacity/qualification distribution; installed slot/component/macro identity, category and software/virtual-component distinction; ammunition/drone counts; exact parent core revision/member identity; capture start/end, applicability/outcome and source proof references. No individual crew identities or generic JSON authority object is introduced. Narrow helpers needed for the 200-physical-line production limit are allowed within these crates, with the deviation recorded.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify typed detail states and dependency validation</name>
  <files>crates/observation-domain/src/lib.rs, crates/observation-domain/src/ship_detail.rs, crates/observation-domain/src/ship_group.rs, crates/observation-domain/tests/ship_contract.rs, tests/fixtures/heavy-ship-source-contract.json</files>
  <read_first>Read Plan 01 domain types, Plan 02 source evidence and fixtures, the common test standards and Rust skills. Verify the exact measured units/scales before writing conversions.</read_first>
  <precondition>The source evidence records every required detail shape, units, applicability and strength; any unresolved required shape blocks this task.</precondition>
  <behavior>Present zero differs from empty collection, not applicable, inaccessible, unknown and unsupported. Cargo arithmetic rejects overflow, non-finite values and invalid source-unit conversion. Crew aggregates preserve roles, capacity and qualification distributions without personal records. A detail group with a different core revision, ship identity, owner or location dependency is rejected. Valid detail never strengthens weak enumeration evidence.</behavior>
  <action>RED: add fixture-driven tests for every required detail category and outcome, exact integer boundaries, below/at/above capacity and size limits, duplicate slot/ware identities, stable ordering ties, empty lists, one member and malformed/null input. GREEN: implement dependency-free typed values and validation per D-02 through D-05 and D-11 through D-13. Do not force a qualification scale or source quantity into an invented range; use the source contract and retain provenance. Keep actual installed engines/shields/weapons/turrets/software and virtual thrusters distinguishable, with missiles and drones separate from cargo ownership assumptions. Applicable-but-unsupported remains a closure error. Source-proven N/A is allowed; zero capacity or zero count is not automatically N/A. REFACTOR: split narrow responsibilities to keep every Rust production file at most 200 physical lines, then rerun tests. Source fixtures added here must be derived test cases labelled synthetic, not fabricated observations.</action>
  <reversibility rating="reversible">New domain values extend the existing envelope without database migration or protocol reinterpretation.</reversibility>
  <acceptance_criteria>Tests cover all required field families and distinct outcome states; source-based bounds and checked arithmetic reject invalid inputs; exact dependency binding is deterministic.</acceptance_criteria>
  <verify><automated>cargo test --locked -p observation-domain --test ship_contract</automated><fails_when>Exit is nonzero, weakened applicability/dependency validation passes a negative test, or a required detail category lacks a case.</fails_when></verify>
  <done>Detail values have executable RED/GREEN evidence and cannot confer stronger source authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry bounded detail groups through native framing</name>
  <files>crates/x4-carrier-native/src/lua_input.rs, crates/x4-carrier-native/src/producer_types.rs, crates/x4-carrier-native/src/producer_collection.rs, crates/x4-carrier-native/src/producer_message.rs, crates/x4-carrier-native/src/ship_detail_input.rs</files>
  <read_first>Read the generalized producer from Plan 01, Task 1 detail contract, existing native parser/producer tests and limits. Reuse current envelope, digest and completion machinery.</read_first>
  <behavior>Typed detail input is copied out of Lua ownership, unknown keys are rejected, one oversize item fails before aggregate growth, byte/count/group/candidate bounds are independent, and replay serializes the identical accepted bytes. Finish-then-validate retains separate capture intervals and never carries an old detail into a new core revision.</behavior>
  <action>RED: add focused tests in owned parser/producer modules for cargo, crew and loadout input, every field outcome, wrong dependencies, sparse Lua tables, integer precision edges, malformed nested records, batch rollover, final item exactly on/off limits and immutable pending bytes. GREEN: add strict detail parsing and typed section dispatch using the established section keys and source schema. Copy bounded input only; perform canonicalization/encoding/digest in Rust, not Lua. Encode and yield incrementally within the existing continuation budget; enforce maximum record, group, message and aggregate candidate bytes before allocation/append. Retain the existing clock contract and one pending immutable transport message. Per inherited Phase 05.4 D-06 and Plan 01, only known-retryable transport-local outcomes within the same qualified connection/source boundary use retained bytes without invoking getters. Response loss requires exact durable-outcome reconciliation; reconnect/load/reload, source uncertainty or stale connection generation discards incomplete source work and requires fresh baseline and collection without republishing a committed receipt. REFACTOR only inside owned responsibilities, adding narrow helpers when the source-size rule requires it.</action>
  <reversibility rating="reversible">Typed optional section profiles preserve existing clock readers and current carrier ABI/envelope semantics.</reversibility>
  <acceptance_criteria>Detail groups use the production framing path; malformed/oversize input cannot partially publish; all emitted source/application states round-trip exactly; native production files meet the 200-line rule.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-carrier-native --lib</automated><fails_when>Exit is nonzero, any detail parser/producer negative case is accepted, or retry regenerates bytes.</fails_when><automated>cargo test --locked -p x4-carrier-native --test abi_contract --test producer_contract --test lifecycle_contract</automated><fails_when>Exit is nonzero or any existing carrier ABI, producer or lifecycle contract regresses.</fails_when></verify>
  <done>Native detail transport is bounded, typed and replay-stable before source adapters use it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collect, publish and read back dependent detail groups</name>
  <files>extensions/live_galaxy/lua/live_galaxy_ship_details.lua, extensions/live_galaxy/lua/live_galaxy_observation.lua, extensions/live_galaxy/tests/carrier_b_contract.lua, crates/x4-bridge/src/receiver_context.rs, crates/x4-bridge/tests/ship_contract.rs</files>
  <read_first>Read Plan 02 exact source calls and observed applicability, Lua/MD conventions, current source cursor and receiver-owned admission context. Read existing certificate and lifecycle tests for publication boundaries.</read_first>
  <behavior>Source fakes cover cargo, people aggregates, installed slots/software, ammunition and every drone family. Each callback obeys its permit and resumes a cursor; count/fill mismatch discards that attempt, retries/yields finitely and publishes no truncated group. Rejected/stale details leave core readable, but do not satisfy the all-fields acceptance ledger.</behavior>
  <action>RED: write behavior tests for the real Lua collector with only external C APIs replaced and for the real Rust receiver with deterministic transported detail records. Include mismatch under churn, ownership/location change between core and details, inaccessible or removed entity, repeated retry without extra getters, stale core parent and separate capture times. GREEN: implement source adapters exactly as the approved contract: GetComponentData cargo, GetPeople2/GetPeopleCapacity aggregates, current installed-slot queries and separate software/virtual representation, missile count/fill, and GetNumAllUnits/GetAllUnits with the researched selector including defence drones. Never substitute template loadout or containment enumeration for installed/canonical data. Bind deterministic groups to the immutable core member set/revision and receiver-owned authority; preserve source strength and distinct freshness. No generic reflection loop over arbitrary getters. The existing scheduler grants each bounded step; no additional callback/loop is registered. Extend bridge tests to commit/read back two core revisions and their distinct detail groups, retaining earlier revision readback. REFACTOR with narrow Lua modules only when responsibility demands it.</action>
  <reversibility rating="reversible">Adapters are read-only and detail sections can be disabled without reinterpreting stored core revisions.</reversibility>
  <acceptance_criteria>Every required detail family reaches durable readback; dependency mismatch rejects atomically; count/fill churn cannot create complete/truncated data; tests demonstrate separate freshness and retained earlier revision content.</acceptance_criteria>
  <verify><automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/carrier_b_contract.lua</automated><fails_when>Exit is nonzero, an applicable field is silently skipped, or a fake source is called during transport replay.</fails_when><automated>cargo test --locked -p x4-bridge --test ship_contract</automated><fails_when>Exit is nonzero, details attach to the wrong core/member revision, or independent earlier/current readback disagrees with admitted content.</fails_when></verify>
  <done>The one-faction tracer now carries all applicable required fields with honest degraded outcomes.</done>
</task>

<verification>
Require RED/GREEN/REFACTOR evidence for all three tasks. Run the existing observation-ingest wire/completion/lifecycle/keyed/session suite and observation-application certificate/lifecycle suite when their seam is touched, using the commands from Plan 08. No X4 heavy-load acceptance is claimed by local fixtures.
</verification>

<success_criteria>
The ordinary-faction path carries core plus cargo, aggregate crew, installed engines/shields/weapons/turrets/software, ammunition and drones, subject to evidence-backed applicability, through real native transport and Rust durable publication. Unknown/inaccessible applicable fields are observable closure blockers, not erased data.
</success_criteria>

<output>Create `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-03-SUMMARY.md` with RED/GREEN results, exact new helpers and remaining runtime-only claims.</output>
