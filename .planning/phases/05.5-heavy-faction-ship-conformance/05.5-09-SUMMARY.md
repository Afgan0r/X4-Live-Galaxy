---
phase: 05.5-heavy-faction-ship-conformance
plan: 09
subsystem: observation
tags: [rust, lua, cargo, crew, carrier-b, sqlite, source-evidence]
status: halted
requires:
  - phase: 05.5-01
    provides: Existing ordinary-faction core carrier and durable tracer
provides:
  - Tested cargo groups through the actual owned DLL, pipe, receiver and SQLite
  - Preparation versus RuntimeAcceptance evidence separation
  - Tested aggregate crew domain contract, not production crew integration
affects: [05.5-09-continuation, 05.5-10, 05.5-11]
tech-stack:
  added: []
  patterns: [strict copied ABI arrays, receiver-owned exact core dependencies, independent durable history]
key-files:
  created:
    - crates/observation-domain/src/ship_cargo.rs
    - crates/observation-domain/src/ship_cargo_decode.rs
    - crates/observation-domain/src/ship_detail.rs
    - crates/observation-domain/src/ship_crew.rs
    - crates/observation-domain/src/ship_crew_decode.rs
    - crates/observation-domain/src/ship_detail_codec.rs
    - crates/x4-bridge/src/receiver_ship_detail.rs
    - extensions/live_galaxy/lua/live_galaxy_ship_details.lua
    - docs/development/heavy-ship-probe.md
  modified:
    - crates/observation-domain/src/ship_group.rs
    - crates/observation-ingest/src/completion.rs
    - crates/x4-carrier-native/src/producer_collection.rs
    - tests/heavy-ship-evidence.ps1
key-decisions:
  - Preserve all 21 runtime outcome rows as pending; local success is not runtime acceptance.
  - Use the existing carrier and stager; no second collector clock or scheduler is introduced.
  - Stop only physical UpgradeGroup copying on the source-knowledge gap; do not invent its fields.
requirements-completed: []
duration: 34min from first RED commit; bootstrap excluded
completed: 2026-09-17
plan_head_before: d02747e46517e7e842c29d15403abe1c88ae7aa9
actuals:
  tokens: 51386
  tasks: 1
  commits: 4
---

# Phase 05.5 Plan 09: Heavy ship production checkpoint

Cargo traverses the real Lua/owned-DLL/pipe/receiver/SQLite path; aggregate crew has a tested domain contract, while required installed-group source evidence and remaining production integration are unfinished.

## Task status and commits

| Task | Status | Commits |
| --- | --- | --- |
| 1: source preparation and cargo | Completed locally; X4 runtime evidence pending | RED `10b1674`, GREEN `31d47ac` |
| 2: crew and actual installed loadout | Incomplete: intentional actual-chain RED plus verified crew domain-only slice | RED `d2f3561`, partial implementation `6c8fca2` |
| 3: shared limits, scheduler, recovery and finite package | Not started | None |

The measured actuals cover the realized Git diff before this metadata commit, using characters/4 rather than session tokens. No task/plan closure or requirement completion is claimed for this halted checkpoint.

## Implemented and verified

- Selectively recovered four preparation artifacts using `git show 64b68b8:<path>`; the retained source work originated in `cab157d`. No old SUMMARY, STATE or halt metadata was merged. Obsolete E0 advice was removed from the retained probe document.
- `-Preparation` accepts a valid source contract with pending runtime rows; `-RuntimeAcceptance` and the default strict checker reject it. Existing provenance, units, applicability and outcome negatives remain active.
- Cargo canonical records preserve lossless decimal IDs, ware item amounts, transport capacity/occupied volume in cubic metres, separate capture intervals, exact parent/member revision, policy and source identity. Reservation data is explicitly excluded rather than mixed with current cargo.
- Raw native dense-array decoding rejects holes, extra keys, metatables, string coercion, unsafe/nonfinite/negative unsigned quantities and inner-count excess; copied values do not depend on retained Lua table pointers.
- Receiver admission derives bounded group membership and ordinal from its current accepted core, verifies owner/scope/incarnation/epoch, freezes the exact core dependency at start and rechecks it at completion. Invalid detail does not replace valid core.
- The actual local chain publishes cargo groups `g0` and `g1` at revisions 2/3 and 4/5 on one connection, crossing 5-second demand intervals. Independent SQLite access checks earlier/current records, receipts, dependencies and capture windows; latest completion replay is exact. Only the supplied X4 source is a local fixture.
- Aggregate crew domain records preserve all role IDs/counts, reported tier counts, canhire, raw signed i32 qualification thresholds and unsigned people counts. Include-arriving/include-pilot selectors and count-fill coverage are explicit. Missing pilot/roles are not fabricated as zero. This domain-only slice is not wired into ABI, Lua, receiver or durable readback yet.

## Exact checks and results

| Check | Result |
| --- | --- |
| `cargo test --locked -p observation-domain --test ship_contract` | 7/7 passed |
| `cargo test --locked -p observation-domain --test ship_crew_contract` | 3/3 passed |
| `cargo test --locked -p x4-bridge --test ship_cargo_contract` | Passed; 9 negative dependency/numeric/source variants |
| `cargo check --locked -p x4-carrier-native` and `-p x4-bridge` | Passed during cargo integration |
| `cargo clippy --locked -p observation-domain -p x4-carrier-native -p x4-bridge --all-targets -- -D warnings` | Passed after the partial crew slice |
| `cargo fmt --all -- --check` | Passed before checkpoint metadata; final format applied afterward |
| `cargo run --locked -p source-size-lint` | Passed during Task 1; also ran its configured workspace Clippy checks |
| `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite ship_detail` | Cargo Lua contract 3/3 passed |
| `pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -SelfTest` | 16/16 passed |
| `pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-detail` before Task 2 RED | Passed, real DLL/pipe/SQLite; 2 groups, 4 cargo revisions, 8 strict malformed ABI cases |
| Same actual-chain command after Task 2 RED | Intended assertion failure: independently reopened crew/loadout revision missing |
| GSD `check tdd-red-evidence` for both Task 1 records and Task 2 record | `RED_EVIDENCE_OK`, target assertion failures |
| `markdownlint-cli2 --fix docs/development/heavy-ship-probe.md` | Passed; nearest config deliberately ignores `.planning/**` |
| Git diff whitespace, commit message/trailer, tracked deletion checks | Passed; no tracked deletions |

The focused checks are not a final full regression. Plan 09 has not converged, so the full workspace tests, complete production recovery scenario and finite package verification have not been run. No X4 launch/control, save access or new runtime capture occurred.

## TDD gate compliance

Task 1 records `05.5-09-task1-preparation-red.json` and `05.5-09-task1-cargo-red.json` establish intended failures before production changes. Preparation was initially rejected as `pending_implementation`; the actual cargo assertion failed after independent core readback. GREEN restores the intended checker distinction and real cargo persistence. Narrow refactoring extracted bounded helpers to meet existing lint/file constraints; no extra REFACTOR commit was needed.

Task 2 record `05.5-09-task2-details-red.json` establishes the intended missing crew/loadout durable-readback assertion and passes the GSD evidence validator. The partial crew domain commit does **not** satisfy that actual-chain GREEN gate. Task 3 has no RED or GREEN yet. No partial failing RED was pushed.

## Source knowledge boundary

The retained declaration/detail snapshots and 24 detail claims remain source evidence, not game outcomes. Narrow follow-up inspection of the existing installed-slot entity used the same detail snapshot, limit 20, all four pages to terminal cursor, partial entity coverage and no conflicts. It confirmed exact software count/fill and installed slot signatures; empty exact software searches alone were not treated as API absence.

The missing declared/source traversal shape of `UpgradeGroup`, returned by `GetUpgradeSlotGroup`, blocks only physical installed-group copying. The exact registered source lookup and chat-only knowledge-gap handoff were delivered to the coordinating lead. No handoff proposal, source corpus or queue was written. The known unpacked vanilla menu locator was absent; repository tooling exposes no source extractor for that declaration. Do not guess struct fields or stringify its pointer/struct as group identity. Retry the original Docs request only after owner-coordinated enrichment is reported.

## Resume point and remaining work

1. Resume Task 2, preserve RED `d2f3561` and partial domain `6c8fca2`. Crew needs source-backed resumable getters, strict actual ABI, receiver admission and independent durable chain evidence. Software, signed missiles, all raw units and physical/virtual actual slots are not implemented. Physical group copying waits for source enrichment; no required field may be silently excluded.
2. Bind the current local finite grouping of 4 and receiver inner-count bound of 64 to one validated shared HeavyShipLimits profile in Task 3. They are implementation/fixture bounds, not runtime-accepted calibration numbers. Lua/DLL/bridge consumer consistency, cumulative call/allocation/work/age accounting and pump-first scheduler integration are unfinished.
3. Complete real process restart/recovery, earlier/current durable detail history, invalidated-parent and response-loss evidence. The cargo-only scenario currently ends before a distinct producer restart; Plan 01's core restart proof does not establish new detail recovery.
4. Materialize a nonzero finite experiment profile/package and complete executable instructions/digests; obtain owner approval in Plan 10. All 21 runtime rows stay pending until actual game evidence. Native call latency, pointer lifetime guarantees, actual applicability, signed missile semantics and virtual-thruster interpretation remain measurement/knowledge targets.
5. Run one relevant full regression after production integration converges, then finish the summary and parent-owned STATE/ROADMAP/requirements updates. This checkpoint does not advance those files.

## Deviations and engineering scope

- Added cohesive helpers `ship_cargo_decode.rs`, `ship_detail_codec.rs`, `ship_crew_decode.rs`, `lua_detail_array.rs`, `producer_ship_detail.rs`, example `detail_peer.rs`, `cargo_readback.rs`, `crew_readback.rs` and focused tests `ship_cargo_contract.rs`/`ship_crew_contract.rs`. These preserve the accepted layers/dependencies and satisfy 200-line Rust files and 60-line functions without artificial execution waves.
- Extended the existing stager capture-close rule and exact ship ordering to explicitly recognized cargo group keys; generic unknown keys do not acquire that exception. Receiver context freezes/rechecks existing dependency maps rather than introducing a second authority mechanism.
- Git index writes required approved escalation; normal hooks were used, no forced staging/commit/push or protected branch rewrite occurred. Work remained on the actual milestone branch, not a guessed init branch.
- The coordinating lead owns memory, STATE/ROADMAP and cross-phase registrars. No independent memory call or outbox was created.

## Skills read

- `.agents/skills/live-galaxy-code-conventions/SKILL.md`
- `.agents/skills/live-galaxy-code-conventions/references/logging.md`
- `.agents/skills/live-galaxy-code-conventions/references/tooling.md`
- `.agents/skills/live-galaxy-tests/SKILL.md`
- `.agents/skills/live-galaxy-tests/references/test-standards.md`
- `.agents/skills/live-galaxy-rust-conventions/SKILL.md`
- `.agents/skills/live-galaxy-rust-tests/SKILL.md`
- `.agents/skills/live-galaxy-x4-integration/SKILL.md`
- `.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md`
- `.agents/skills/live-galaxy-x4-tests/SKILL.md`
- `.agents/skills/lua/SKILL.md`
- Global `game-repo-standard/SKILL.md`, `references/profiles-and-registry.md`, `references/memory-lifecycle.md`
- Global `git-commit/SKILL.md` before every commit; machine-local configured model read, explicit session GPT-5.6 Sol override used truthfully.

## Self-Check: PASSED

All listed production/test artifacts and three RED evidence records exist. All four implementation/test commits exist on the intended milestone branch. Focused checks distinguish the completed cargo slice from the intentional unfinished Task 2 assertion. No completion or runtime-success claim is made for unfinished work.
