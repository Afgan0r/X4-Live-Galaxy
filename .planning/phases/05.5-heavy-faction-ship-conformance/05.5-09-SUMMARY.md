---
phase: 05.5-heavy-faction-ship-conformance
plan: 09
subsystem: observation
tags: [rust, lua, crew, loadout, carrier-b, sqlite, bounded-experiment]
status: complete
requires:
  - phase: 05.5-01
    provides: Ordinary-faction core carrier and durable tracer
provides:
  - All required ordinary-faction detail families through the actual owned DLL and SQLite
  - One validated finite profile across actual Lua, native, startup and receiver consumers
  - Independently reopened earlier/current history and interrupted-family recovery
  - Prepared exact-revision experiment package; game acceptance remains pending
affects: [05.5-10, 05.5-11]
tech-stack:
  added: []
  patterns: [strict copied ABI arrays, exact receiver-owned dependencies, pump-first bounded composition, protected history retention]
key-files:
  created:
    - crates/observation-domain/src/ship_crew.rs
    - crates/observation-domain/src/ship_loadout.rs
    - crates/x4-bridge/src/production_ship_limits.rs
    - crates/x4-bridge/src/production_ship_schedule.rs
    - crates/x4-carrier-native/src/producer_policy.rs
    - extensions/live_galaxy/lua/live_galaxy_ship_selection.lua
    - extensions/live_galaxy/lua/live_galaxy_ship_budget.lua
    - config/heavy-ship-experiment.json
    - tests/carrier-b-heavy-configured.ps1
  modified:
    - crates/x4-bridge/src/production_runtime.rs
    - crates/x4-bridge/src/receiver_ship_detail.rs
    - tools/package-live-galaxy.ps1
    - docs/development/heavy-ship-probe.md
key-decisions:
  - All 21 runtime outcome rows remain pending; no game acceptance or requirement closure.
  - Use existing scheduler, owned monotonic clock, collectors and SQLite retention without a second framework.
  - Copy both source-backed UpgradeGroup strings before yielding, preserving empty and parent-path values.
  - The 2ms callback target measures after synchronous return and cannot preempt X4.
requirements-completed: []
duration: 174min from first RED commit through implementation completion; bootstrap included only in elapsed wall time
completed: 2026-09-17
plan_head_before: d02747e46517e7e842c29d15403abe1c88ae7aa9
actuals:
  tokens: 104764
  tasks: 3
  commits: 8
---

# Phase 05.5 Plan 09: Heavy ship production summary

Aggregate crew, actual installed loadout, cargo and core observations traverse Lua → owned DLL → named pipe → Rust → durable SQLite, with a shared finite experimental profile and independently reopened recovery/history evidence. All tasks are complete locally; owner-operated X4 measurement and numerical acceptance remain pending.

## Tasks and implementation identity

| Task | Local result | Commits |
| --- | --- | --- |
| 1: source preparation and cargo | Complete; preserved previous execution | RED `10b1674`, GREEN `31d47ac` |
| 2: crew and installed loadout | Complete through actual ABI, receiver and independent history | RED `d2f3561`, domain slice `6c8fca2`, GREEN `fc6ca82` |
| 3: shared bounds, recovery and finite package | Complete locally; proposed limits are not runtime accepted | RED `ddda9d8`, GREEN `5e3f45a` |

The historical halted metadata commit `d918040` is superseded by this summary. Actuals measure characters/4 of the realized Git diff and eight commits from the persisted plan ledger before this metadata commit, not model-session tokens. The coordinating lead owns STATE, ROADMAP, requirements and family memory.

## Implemented behavior

- Required aggregate crew preserves all reported role IDs, arriving/pilot selectors, role counts, canhire, raw signed i32 qualification thresholds and unsigned counts. No individual personnel inventory is collected.
- Installed loadout preserves physical and virtual slots, both UpgradeGroup strings, macro-only/component-zero slots, current software, raw signed missile quantities/capacity and every returned unit category with the exact all-units selector. UI-specific pseudo-group filtering is not promoted into an absence contract.
- Strict copied native records reject sparse/extra/metatable-bearing arrays, coercion, noncanonical IDs, unsafe/nonfinite numbers and count/range excess. No Lua/X4 pointer survives accepted ABI input.
- Receiver-owned scope, membership, ordinal, owner, incarnation, epoch and exact current core dependency are independently validated. Parent freshness and configured inner limits reject before publication; changed core identity/owner/type/class/location suppresses detail completion.
- Deterministic core/detail rotation uses the existing Rust ObservationScheduler. Lua composes existing collectors and keeps only bounded committed core membership, not an all-ships detail cache. Each callback pumps carrier control/feedback before source admission.
- Cumulative calls, allocation bytes, collection steps, canonical raw bytes, attempts, age/inactivity, candidate/aggregate/message/pending/publication limits and finite run window reach actual consumers. Native count × target ffi.sizeof is checked before allocation; nested role/tier budgets accumulate across one capture.
- The bridge run's 60-second window does not reset on reconnect. Frozen/backward clock, overrun, source failure, changed parent and transport loss suppress new source work. The loaded DLL's owned monotonic clock is sampled after synchronous return; it does not interrupt the call.
- Existing durable transactions/receipts remain authoritative. Fresh producer processes use the durable global revision floor; incomplete candidates are discarded. Existing retention protects current revisions, referenced parents, decision pins and ambiguous replay evidence while bounding unprotected history.
- Sanitized profile/run/section identity and capture metrics are bounded. Diagnostic write failure is separately observable without forging successful diagnostics or changing valid collection; recovery reports the bounded gap.

## Verification results

| Required check | Result |
| --- | --- |
| Final workspace formatting and strict all-target Clippy | Passed |
| Final `cargo test --locked --workspace -- --test-threads=1` | Passed, including startup/profile, scheduler, receiver, retention, recovery and actual DLL ABI |
| `cargo run --locked -p source-size-lint -- --crates` | Passed; required Rust file/function bounds preserved |
| Full `run_contracts.ps1 -Suite all` | Passed: 78 Lua contracts, 19 syntax checks, XML/schema and seven actual clock-carrier scenarios |
| Final actual `heavy-ship-detail` scenario | Passed: two groups, core plus 12 cargo/crew/loadout revisions, earlier/current independent reopen and current exact replay |
| Configured actual `heavy-ship-recovery` scenario | Passed: first producer core/detail revisions 1–8, distinct producer revisions 9–12; independent earlier/current reopen |
| Interrupted core, cargo, crew and loadout matrix | All four passed with separate producer processes and fresh qualification |
| Native cumulative raw-byte, configured age/backward-clock and intent-mismatch negatives | Passed |
| Lua count/allocation/call/step/window/clock/overrun and mutable-core negatives | Passed |
| Independent receiver configured inner-count and stale/backward-parent negatives | Passed; valid core retained and invalid detail unpublished |
| Heavy package self-test | Passed: release PE, sole export, XML/layout, manifest tuple, exact limits and atomic preservation |
| Heavy evidence self-test and Preparation | 16/16 and shape passed; RuntimeAcceptance rejects all pending rows |
| Markdown and Git whitespace/message/trailer/deletion checks | Passed; no tracked deletion; nearest config intentionally ignores .planning |

Only X4 getters/state are supplied by local fixtures in these chains; the Lua host, owned release DLL, IPC, real production bridge and SQLite are actual local components. The configured scenario does not rely on a manual peer. Legacy detail exercises multi-member grouping; the shared profile exercises actual single-member composition. Existing native/clock recovery covers local acceptance, Received, response loss, bridge absence/restart, pending cancellation and stale generation. Lua/receiver negatives cover load/reload-equivalent incarnation invalidation and changed parent. None substitutes for game lifecycle behavior.

## TDD Gate Compliance

Task 1 Preparation/cargo records and Task 2 detail record retain the previous intentional assertion failures and `RED_EVIDENCE_OK`. Task 2 GREEN resolves independently reopened missing crew/loadout records rather than duplicating RED. Task 3 `05.5-09-task3-profile-red.json` records rejection of the proposed profile by the real preimplementation validator; RED `ddda9d8` precedes consumer implementation and passes the evidence gate. GREEN accepts the supported profile while independently rejecting zero, overflow, duplicate, unknown, missing, unsupported and cross-limit inconsistencies. No lint or assertion was weakened to obtain GREEN.

## Source evidence and limits of knowledge

The exact UpgradeGroup enrichment request and all three terminal pages were verified by the coordinating lead. SOURCE-EVIDENCE records the declared path/group layout, by-value GetUpgradeSlotGroup call, observed count/traversal/buffer copies and inferred immediate slot-string copies. The producer commit `4e0069c` is a reported enrichment identity, not independently verified sibling Git. Declared layout is ABI-dependent, not packed. Pointer lifetime/nullability outside observed callers, cross-call atomicity and cross-session identity remain unknown.

The exact installed/software/missile/all-unit selectors, ranges and source claim identities are persisted in SOURCE-EVIDENCE. Count/fill observation remains Partial/Unknown; successful transfer cannot certify complete source membership, known-empty, deletion or globally simultaneous state. No save, installation or sibling repository was read or written during implementation.

## Exact prepared package

- Package: `dist/live-galaxy-heavy-experiment`, labelled `prepared-heavy-experiment`.
- Implementation/source revision: `5e3f45a88aef9fac7b7177a449cbf1acf57b3fa9`.
- Manifest SHA-256: `0a72f75c9373842b88e3658b2ec7fb4f1f176e8e44fa0b218e496f295acf9d7e`.
- Native SHA-256: `58d8b3719330e7f1a526689a3c51b427fc17b2689e7bc4a476fb8012a302ade2`.
- Profile SHA-256: `caadd18aadcadd68d715da04e93bf96b8912f46e8b62fe5a545ebd4ef1a1aa1b`.
- Bridge SHA-256: `7e65a0220defb1eeeb1fc78363b6919f7ed1e171e34da7580df4995fe48bf2fc`.
- Generated Lua configuration SHA-256: `08e86bd537e970ea991eda29c8f817a7cc801c13712115554d2946b26e9143b5`.
- Exact tuple: native ABI2, control3, envelope2; schema1, policy2, canonicalization3, digest1.

The complete flat 38-field profile is `config/heavy-ship-experiment.json`, copied byte-exact into the bundle and materialized into its Lua module. Proposed nonzero values: ordinary faction argon; window60000ms, core128, group1, inner64, per-allocation8192 bytes, cumulative65536 bytes, calls384, steps512, callback2ms, heavy permit1, interval50ms, debt512, freshness30000ms, current plus2 unprotected prior receipts. Message8192/control512; candidate raw/work131072, records/batches128; candidate count1; aggregate bytes262144, records/batches128, work131072; publication records128/content131072; pending8192/total524288; lifecycle work262144; capture age30000/inactivity10000ms; delivery/reconnect attempts1; reconnect25ms; availability5000ms. Referenced or pinned history is protected separately.

Arithmetic and fixture-versus-target ABI distinctions are in the probe document: 128 UniverseIDs ×8=1024 bytes; inner64 uses actual target ffi.sizeof and independent allocation ceilings. A faction over128 is refused, not read as a supposedly complete prefix. A nested tier population can be refused by cumulative bytes despite legal individual counts. Proposed ceilings permit measurements but are not proven X4 timing safety.

## Runnable local checks and owner checkpoint

```powershell
pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-recovery -LimitsFile config/heavy-ship-experiment.json
pwsh -NoProfile -File tools/package-live-galaxy.ps1 -SelfTest -LimitsFile config/heavy-ship-experiment.json
pwsh -NoProfile -File tools/package-live-galaxy.ps1 -OutputDirectory dist/live-galaxy-heavy-experiment -LimitsFile config/heavy-ship-experiment.json
```

Plan 10 must obtain explicit owner approval before installation or game actions. The owner alone installs this exact bundle, uses a disposable Creative Custom campaign, follows bundled STARTUP.txt with `--ship-faction argon`, and records the finite normal-time cohort. No player saves. SETA is a separate approved sample. An already-entered synchronous call may freeze a frame indefinitely; the timer cannot preempt it. Stop on refused count/allocation/work/bytes, source/identity change, frozen/backward clock, feedback stall or postreturn overrun. If the call does not return, the owner exits the disposable game session. Stop the bridge and disable the experimental extension between runs; restart/load/reload requires fresh qualification.

All 21 game outcomes, applicability/units/semantic interpretation, actual cadence/native latency, normal/SETA load and numerical acceptance remain pending. Retain private run evidence only in the contract-defined durable machine-local artifact store with verified digests and an owner-only locator; public handoffs use logical run identity, not raw payloads.

## Deviations and resolved failures

- Cohesive detail decoders/capture/readback and profile/schedule/runtime-control/retention/native-policy/identity helpers preserve architecture and dependencies while meeting 200-line files and 60-line functions. No arbitrary execution waves, new collector, global clock, framework or SQLite schema were added.
- Cumulative raw-byte enforcement exposed an old large-record test fixture whose budget covered only one batch. The fixture now declares its whole-candidate allowance; production limits were not widened.
- Actual composition exposed Lua colon-method self shadowing captured selection state; closures now explicitly own that state.
- Final actual ABI regression exposed stale begin_section reading replacement state before handle validation. Begin/push/finish/fail now reject stale handles before replacement access or decoding; the existing actual ABI stale-generation assertions pass.
- One premature full regression overlapped the fixed named-pipe endpoint with the recovery matrix, causing endpoint creation failure. Final regression and harnesses were run serially; no production stall timeout was changed.
- The earlier core fixture advertised512 bytes while opening a2048-byte DLL. Its declared fixture intent now agrees with the DLL; no publication watchdog was widened.
- Approved Git escalation was needed for index/network access. Normal hooks and the intended milestone branch were retained; no force staging, reset, stash, new worktree or branch rewrite.
- No new trust surface beyond planned typed input/IPC/durable boundaries, no dependencies installed, no game actions, no independent memory writes. No implementation-blocking stubs remain.

## Skills read

- `.agents/skills/live-galaxy-code-conventions/SKILL.md`, `references/logging.md`, `references/tooling.md`.
- `.agents/skills/live-galaxy-tests/SKILL.md`, `references/test-standards.md`.
- `.agents/skills/live-galaxy-rust-conventions/SKILL.md`.
- `.agents/skills/live-galaxy-rust-tests/SKILL.md`.
- `.agents/skills/live-galaxy-x4-integration/SKILL.md`, `references/lua-md-conventions.md`.
- `.agents/skills/live-galaxy-x4-tests/SKILL.md`.
- `.agents/skills/lua/SKILL.md`.
- `C:/Users/pavlo/.agents/skills/game-repo-standard/SKILL.md`, `references/profiles-and-registry.md`, `references/memory-lifecycle.md`.
- `C:/Users/pavlo/.agents/skills/git-commit/SKILL.md` before commits; configured full model read and explicit GPT-5.6 Sol session identity used.

Root/shared instructions, memory routing/version/handoffs, GSD execution/TDD/checkpoint/template references and supplied phase context were read. Bounded affected accepted architecture contracts were completed without expanding research.

## Self-Check: PASSED

All named production/test/profile/probe artifacts and RED records exist; all listed task commits exist on the intended milestone. The generated bundle was independently checked against manifest entries and its recorded source revision/digests. Final local checks pass, while the unchanged21 runtime outcomes are independently counted as pending. Parent-owned planning/memory files were not modified.
