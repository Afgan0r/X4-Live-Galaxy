---
phase: 05.5-heavy-faction-ship-conformance
plan: 05
type: tdd
wave: 5
depends_on: [05.5-04]
files_modified:
  - crates/x4-bridge/src/production_limits.rs
  - crates/x4-bridge/src/production_ship_limits.rs
  - crates/x4-bridge/src/production_startup.rs
  - crates/x4-bridge/tests/production_startup.rs
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-carrier-native/src/producer_types.rs
  - crates/x4-carrier-native/src/lua_input.rs
  - crates/x4-carrier-native/src/lua_producer_operations.rs
  - extensions/live_galaxy/lua/live_galaxy_carrier.lua
  - extensions/live_galaxy/tests/carrier_b_contract.lua
  - tools/package-live-galaxy.ps1
  - crates/x4-bridge/src/bin/validate_limits.rs
  - tests/carrier-b-local.ps1
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/tests/run_contracts.ps1
autonomous: true
requirements: [OBS-06, VAL-01, VAL-02, VAL-06, VER-LG-003, VER-LG-012]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - One validated limits profile reaches Rust startup, the packaged Lua runtime and native producer without independent defaults.
    - Incomplete, inconsistent, oversized or unapproved heavy profiles fail closed before source collection.
    - Existing clock-profile packaging and readback remain compatible.
  artifacts:
    - path: crates/x4-bridge/src/production_ship_limits.rs
      provides: Typed heavy limits and cross-limit validation
    - path: tools/package-live-galaxy.ps1
      provides: Single-source validated profile materialization
  key_links:
    - from: config/carrier-b-limits.json
      to: crates/x4-bridge/src/production_limits.rs
      via: Exact production parser and profile identity
    - from: tools/package-live-galaxy.ps1
      to: extensions/live_galaxy/lua/live_galaxy_carrier.lua
      via: Generated bounded Lua profile from the same validated input
---

<objective>
Make calibration enforceable end to end per D-05, D-10, D-15 and D-16. This plan wires typed limits but does not choose numerical ship limits. Plan 06 owns measured values and owner approval before heavy acceptance or full-set expansion.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-RESEARCH.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-PATTERNS.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-04-SUMMARY.md
@config/carrier-b-limits.json
@crates/x4-bridge/src/production_limits.rs
@tools/package-live-galaxy.ps1
</context>

## Artifacts this phase produces

New path: `crates/x4-bridge/src/production_ship_limits.rs`; wire it as a narrow module of the existing production limits implementation. New symbols: `ShipCollectionLimits`, `validate_ship_limit_relationships`, and an explicit collection profile discriminator/identity. New profile fields: `collection_profile`, `profile_revision`, `refill_millis`, `burst`, `heavy_permits`, `max_overrun_debt`, `step_work`, `heavy_work_threshold`, `max_native_results`, `max_native_call_micros`, `max_record_bytes`, `max_group_records`, `max_group_bytes`, `batch_target_bytes`, `max_continuation_work`, `max_source_attempts`, `source_cooldown_millis`, `max_capture_age_millis`, `max_capture_skew_millis`, `core_freshness_millis`, `detail_freshness_millis`, `stale_block_millis`, `retained_revisions`, `max_pins`, `max_receipts`, `max_diagnostic_entries`. Existing complete-message/candidate/aggregate/publication/transport/reconnect fields remain authoritative. Use explicit integral units and checked conversion; distinguish policy duration from game-time timestamps. These are schema names, not invented values. Generated package profile metadata is build output, never committed or read from an installation mirror. The current `-LimitsFile` / `--limits-file` entrypoints are reused; no duplicate configuration file or scheduler is introduced.

The numerical contract also includes an explicit `post_boundary_cooldown_millis` and revalidation policy for documented/observed save/load lifecycle signals. This is callback state only, never save-file inspection. Plan 02 must establish the signal semantics; if no qualifying save signal exists, record that limit and use the proven load/reload incarnation boundary without claiming save-specific proof. Include Lua normalization/copy/allocation/GC and native serialization cost, pipe quotas/whole-message overhead, and raw-to-decoded candidate expansion in the Plan 06 measurement worksheet. These costs count against existing independent budgets; they do not create new timers or loops.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate the complete heavy profile at production startup</name>
  <files>crates/x4-bridge/src/production_limits.rs, crates/x4-bridge/src/production_ship_limits.rs, crates/x4-bridge/src/production_startup.rs, crates/x4-bridge/tests/production_startup.rs, crates/x4-bridge/src/production_runtime.rs</files>
  <read_first>Read ProductionLimits' current exact 23-field parser, startup arguments and generation/publication/lifecycle limits, Plan 04 scheduling inputs, and source-size conventions.</read_first>
  <behavior>The existing clock profile still parses unchanged. Heavy profiles require every heavy field and an explicit revision; partial, duplicate, unknown, zero-invalid, overflowed or inconsistent fields reject before opening collection. Record/group/message/candidate/aggregate bounds and freshness/retry/retention relationships are checked with no lossy casts.</behavior>
  <action>RED: expand production_startup tests with synthetic valid profiles and below/at/above each bound, invalid cross-limit combinations, excessive config length, partial heavy fields, duplicate keys, unknown keys, zero and usize/u64 precision edges. GREEN: retain strict parsing and add a typed heavy profile with integral units, explicit identity and all required policy fields. Validate record and batch targets against the hard complete-message limit; group/candidate/aggregate records and bytes against publication/storage limits; retry/cooldown/capture age against freshness/stale deadlines; bounded retention/pins/receipts/diagnostics. Keep the existing clock input backward-compatible through an explicit clock profile, never silently interpreting its one-record limits as heavy acceptance. Thread the typed profile from startup into runtime admission and recovery; reject missing heavy policy before getters or candidate allocation. Do not populate production ship values yet: local tests use clearly synthetic boundary values, and Plan 06 calibrates the committed profile. REFACTOR parsing helpers as needed to keep each Rust production file at most 200 lines. If a measured bound requires a changed policy rather than a new value, return to owner only for an actual product/architecture conflict.</action>
  <reversibility rating="reversible">An additive explicit profile keeps existing clock parsing and stored envelopes intact.</reversibility>
  <acceptance_criteria>One parser validates both profiles without ambiguous fallback; tests cover every new field and relationship; heavy mode cannot start on the old one-record profile or a partial profile.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-bridge --test production_startup</automated><fails_when>Exit is nonzero, a malformed heavy profile starts collection, or the existing clock startup/readback contract fails.</fails_when></verify>
  <done>The receiver has strict, tested numerical policy inputs with no guessed production values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume the identical profile in Lua and native admission</name>
  <files>crates/x4-carrier-native/src/producer_types.rs, crates/x4-carrier-native/src/lua_input.rs, crates/x4-carrier-native/src/lua_producer_operations.rs, extensions/live_galaxy/lua/live_galaxy_carrier.lua, extensions/live_galaxy/tests/carrier_b_contract.lua</files>
  <read_first>Read Plan 04 ShipSchedule inputs, the existing Lua LIMITS and options injection, native initialization/begin strict-key validation and the Task 1 profile schema.</read_first>
  <behavior>A profile mismatch rejects before source work. Measured time/work/size limits reach their actual source/producer consumers. No fallback to clock constants occurs in heavy mode; typed input, serialization and transport share the same hard message bound.</behavior>
  <action>RED: add Lua/native behavioral tests proving profile propagation, exact identity mismatch rejection, each consumer's boundary and immutable replay under profile change. GREEN: copy the validated numeric profile into bounded native initialization and Lua source policy with explicit units and identity. Reuse the existing owned module operations and ABI contract; update the structured input/status contract consistently with its callers. Admit new collection only under the matching policy identity; retained immutable messages finish reconciliation under their original identity instead of being regenerated under new limits. Replace hard-coded producer retry/cooldown/one-record assumptions with the profile's validated values. All source attempts, continuation work, native result allocation, group growth and candidate sizes must have a reachable enforcement point. Existing clock tests retain their supported profile. No source call or arbitrary Lua code is accepted from configuration, and no Lua-side JSON/digest encoder is added. REFACTOR focused helpers without widening native unsafe scope.</action>
  <reversibility rating="reversible">Configuration propagation changes bounded input data, not the ten-operation ABI or permission model.</reversibility>
  <acceptance_criteria>Tests identify the consumer for every bound; missing/mismatched heavy profile stops before a getter; replay keeps its original bytes and policy identity; no additional native operation is registered.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-carrier-native --test abi_contract --test producer_contract --test lifecycle_contract</automated><fails_when>Exit is nonzero, ABI/lifecycle regresses, or a mismatched profile is accepted.</fails_when><automated>cargo test --locked -p x4-carrier-native --lib</automated><fails_when>Exit is nonzero or a configured native source/continuation/size limit is not enforced.</fails_when><automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/carrier_b_contract.lua</automated><fails_when>Exit is nonzero, heavy collection uses an independent fallback constant, or profile rejection occurs after a source call.</fails_when></verify>
  <done>The numerical contract has real enforcement at both source and transport boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Package and test the profile as one reproducible input</name>
  <files>tools/package-live-galaxy.ps1, crates/x4-bridge/src/bin/validate_limits.rs, tests/carrier-b-local.ps1, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <read_first>Read existing package SelfTest, production validator binary, local harness and runtime loader. Read the newly added faction Lua test path and current all-suite dispatch.</read_first>
  <action>Use the production parser in packaging, not a second hand-maintained policy validator. Materialize the validated values/profile identity into the packaged runtime input and native initialization; wire the loader to that input. Preserve the existing package structure, dependencies and single MD/UI event cue. Add package/local self-test fixtures for missing/altered profile, stale generated metadata, exact byte budget propagation, current clock compatibility and heavy synthetic profiles. Ensure the new faction Lua suite is included by run_contracts -Suite all. Extend the local harness to accept an explicit validated profile and emit a bounded calibration metric summary for source-step work/duration, production/staging counts, pending/aggregate bytes, batches, commit age and getter calls. Source timings from fakes must be labelled synthetic; actual X4 timings come only from owner evidence. No package install or game launch is part of SelfTest. Keep generated Lua/profile artifacts and private raw metrics outside tracked source, and use stable sanitized evidence IDs in reports.</action>
  <reversibility rating="reversible">Package generation is reproducible from versioned source and does not deploy to X4.</reversibility>
  <acceptance_criteria>Package and bridge consume the same input and identity; self-tests fail on drift; all new Lua tests are reachable through the existing runner; metrics cannot be mistaken for in-game observations.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File tools/package-live-galaxy.ps1 -SelfTest</automated><fails_when>Exit is nonzero, package/receiver limits drift, an invalid profile is packaged, or SelfTest changes an installation.</fails_when><automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest</automated><fails_when>Exit is nonzero, profile propagation or bounded metrics fail, or synthetic metrics are labelled runtime observations.</fails_when><automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite all</automated><fails_when>Exit is nonzero or the faction/ship behavioral suites are omitted from the all-suite run.</fails_when></verify>
  <done>One profile is ready to receive measured owner-approved values and is packaged reproducibly.</done>
</task>

<verification>
Audit every new field from profile parser to actual consumer and negative test. An unused limit is a failing result. Configuration wiring does not prove the values are safe: Plan 06 remains a blocking calibration/owner boundary.
</verification>

<success_criteria>
There is no configuration drift between Lua, native producer and Rust receiver, and no uncalibrated heavy default. The current clock path remains supported while heavy startup requires the complete explicit profile.
</success_criteria>

<output>Create `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-05-SUMMARY.md` with the field-to-consumer map and synthetic versus observed measurement boundary.</output>
