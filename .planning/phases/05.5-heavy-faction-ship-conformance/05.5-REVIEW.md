---
phase: 05.5-heavy-faction-ship-conformance
reviewed: 2026-09-18T04:43:36Z
depth: standard
diff_base: d5cc9e3d270acbcbcd36bfc4972a96db7d778b59
reviewed_head: d5cc9e3 plus scoped uncommitted CI prerequisite repair
production_reviewed_head: d5cc9e3 unchanged
scope: Plan 10 owner-approved CI ABI prerequisite preparation only
files_reviewed: 4
files_reviewed_list:
  - .github/workflows/phase-05.yml
  - tests/carrier-b-local.ps1
  - .agents/skills/live-galaxy-code-conventions/references/tooling.md
  - .agents/skills/live-galaxy-code-conventions/CHANGELOG.md
rejection_reviewed_head: 4afa4f4 plus scoped uncommitted rejection-context repair
rejection_scope: Plan 10 owner-approved rejection diagnostics only
rejection_files_reviewed: 13
rejection_files_reviewed_list:
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_budget.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_collection.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_crew_capture.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_details.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_loadout_capture.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_selection.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_source.lua
  - extensions/live_galaxy/tests/carrier_b_contract.lua
  - extensions/live_galaxy/tests/heavy_profile_contract.lua
  - extensions/live_galaxy/tests/ship_detail_contract.lua
  - .agents/skills/live-galaxy-code-conventions/references/logging.md
  - .agents/skills/live-galaxy-code-conventions/CHANGELOG.md
synchronous_reviewed_head: e2ffa42 plus frozen uncommitted repair
synchronous_scope: Plan 10 synchronous-first experimental repair only
synchronous_files_reviewed: 20
synchronous_files_reviewed_list:
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/production_runtime_control.rs
  - crates/x4-bridge/src/production_runtime_idle.rs
  - crates/x4-bridge/src/production_runtime_idle_tests.rs
  - crates/x4-bridge/src/production_runtime_message.rs
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_budget.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_collection.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_crew_capture.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_details.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_order.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_selection.lua
  - extensions/live_galaxy/tests/carrier_b_contract.lua
  - extensions/live_galaxy/tests/carrier_b_heavy_configured.lua
  - extensions/live_galaxy/tests/heavy_profile_contract.lua
  - extensions/live_galaxy/tests/run_contracts.ps1
  - extensions/live_galaxy/tests/ship_collection_contract.lua
  - extensions/live_galaxy/tests/ship_detail_contract.lua
  - extensions/live_galaxy/tests/ship_synchronous_contract.lua
  - tests/carrier-b-heavy-configured.ps1
historical_reviewed_head: e661f36
historical_production_reviewed_head: 2b41ec4
historical_files_reviewed: 42
historical_files_reviewed_list:
  - config/heavy-ship-experiment.json
  - crates/x4-bridge/src/lib.rs
  - crates/x4-bridge/src/production.rs
  - crates/x4-bridge/src/production_admission.rs
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/production_runtime_control.rs
  - crates/x4-bridge/src/production_selection.rs
  - crates/x4-bridge/src/production_ship_cursor.rs
  - crates/x4-bridge/src/production_ship_timing.rs
  - crates/x4-bridge/src/production_ship_timing_tests.rs
  - crates/x4-bridge/src/production_ship_limits.rs
  - crates/x4-bridge/src/receiver_ship_detail_record.rs
  - crates/x4-bridge/src/receiver_ship_detail.rs
  - crates/x4-bridge/tests/heavy_profile_contract.rs
  - crates/x4-bridge/tests/ship_support/heavy_receiver.rs
  - crates/x4-carrier-native/src/lib.rs
  - crates/x4-carrier-native/src/lua_detail_array.rs
  - crates/x4-carrier-native/src/lua_detail_budget.rs
  - crates/x4-carrier-native/src/lua_input.rs
  - crates/x4-carrier-native/src/lua_open_limits.rs
  - crates/x4-carrier-native/src/lua_producer_operations.rs
  - crates/x4-carrier-native/src/producer.rs
  - crates/x4-carrier-native/src/producer_feedback.rs
  - crates/x4-carrier-native/src/producer_policy.rs
  - crates/x4-carrier-native/tests/producer_contract.rs
  - crates/x4-carrier-native/tests/producer_contract/ship_refresh.rs
  - extensions/live_galaxy/lua/live_galaxy_scheduler.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_collection.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_crew_capture.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_details.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_order.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_profile.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_selection.lua
  - extensions/live_galaxy/tests/ship_collection_contract.lua
  - extensions/live_galaxy/tests/carrier_b_ship_local.lua
  - extensions/live_galaxy/tests/ship_detail_contract.lua
  - docs/development/heavy-ship-probe.md
  - extensions/live_galaxy/tests/carrier_b_heavy_configured.lua
  - tests/carrier-b-heavy-configured.ps1
  - tests/carrier-b-local.ps1
  - tests/carrier-b-heavy-oracles.ps1
  - tests/carrier-b-heavy-oracles-contract.ps1
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 05.5: Scoped Preparation Code Review

## CI ABI prerequisite scoped review at d5cc9e3 plus uncommitted repair

**Reviewed:** 2026-09-18T04:43:36Z.
**Depth:** standard. **Open findings:** 0 BLOCKER, 0 WARNING.

### Narrative Findings (CI prerequisite repair)

No grounded defect was found in the four-file scoped change. This is not a
whole-phase, whole-harness or remote CI-success verdict. The task lead established
fresh origin refs and a clean base before editing; reviewer status confirmed
the intended milestone branch and exact `d5cc9e3` base with only the authorized
repair and execution-record changes.

Source tracing covered the full changed harness and workflow, existing
provisioner/lock, actual Windows ABI host lookup and native crate build target.
`PrepareOnly` selects the standalone Lua5.1.5 source/executable, retains version
and required-source checks, and omits the unrelated Lua DLL prerequisite because
the existing host embeds the Lua sources. The same `Build-Host` compiles the same
C source and exports; no replacement ABI host or alternate fixture is introduced.
Compiler failure throws before the prepared marker. The preparation branch runs
before release builds, product copies, limits processing, host scenarios and
bridge processes, and the earlier selected Cargo tests are explicitly guarded.
Normal non-preparation execution retains its Busted runtime/DLL requirements.

The workflow provisions the checksum-verified locked Lua source, prepares the
host, and explicitly builds `x4-carrier-native` in debug mode before the unchanged
workspace tests. The native crate declares `cdylib` plus `rlib`; the ABI fixture
copies `target/debug/x4_carrier_native.dll` relative to the test executable and
locates prepared hosts under the checkout's common-Git-root ignored cache.
The new preparation writes that expected cache in the clean-clone CI shape.
No test exclusion, ignored-test promotion, dependency/version change or product
semantic change was introduced. The TOOL-04 clarification records this concrete
fresh-runner prerequisite rather than adding an unrelated verification framework.

### Verification and limitations

- Reviewer PowerShell parser check passed for `tests/carrier-b-local.ps1`.
- Reviewer executed `pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest
  -PrepareOnly`: exit0, one `PREPARED_LUA_HOST` marker, no test/build-cycle/scenario
  output. The generated host remains in the intended ignored local host cache.
  This local check used already provisioned source and does not prove clean-runner
  download availability.
- Filtered lead evidence: same-user clean clone at the base with only the two
  changed executable-source files transferred, initially without Lua/DLL/host
  caches; locked official-archive checksum verification, provisioning,
  `PrepareOnly`, debug native build and all9 ABI contracts passed in1.84s for the
  ABI test stage. The reviewer traced these producer/consumer paths but did not
  repeat provisioning or the clean-clone suite.
- The first local clone's Git ownership/sandbox discovery failure is excluded
  from product evidence. No global Git configuration was changed.
- Reviewer `git diff --check` passed. Markdownlint was invoked for this artifact
  and respected the repository's `.planning/**` exclusion.
- No new Actions execution was inspected or asserted successful. The final
  relevant local regression and remote workflow result remain lead-owned gates.
  No X4, SETA, runtime-capacity or Plan10 closure claim follows from ABI fixture
  preparation. No secrets, private captures, game operations or memory were used.
- Review stayed inline: this is one coupled preparation dependency chain with
  no materially independent specialist slice.

Read/reused the already complete chain: root `AGENTS.md`; all five shared X4
bundle files; code-conventions `SKILL.md`, `references/tooling.md` (re-read current
text) and `references/logging.md`; tests `SKILL.md` and
`references/test-standards.md`; code-review `SKILL.md` and
`references/fan-out.md`; Rust conventions/tests `SKILL.md`; X4 integration
`SKILL.md` and `references/lua-md-conventions.md`; X4 tests `SKILL.md`; Lua
`SKILL.md`; global game-repo-standard `SKILL.md`; and configured skill bootstrap.
No game research, memory or skill-authoring references were applicable to this
read-only review. Named-file/Git reads required no ast-index structural discovery.

Readiness: scoped repair is ready for the lead's required final regression and
Git/CI completion workflow; no open code-review finding remains in this change.

## Rejection-context scoped review at 4afa4f4 plus uncommitted repair

### Final focused re-review disposition

**Reviewed:** 2026-09-18T04:20:12Z.
**Open findings:** 0 BLOCKER, 0 WARNING in this scoped repair.

**WR-02: fixed.** Selection-owned refusal metadata now carries the requested
section/revision/run and `stage=selection`, without stale capture metrics or the
obsolete collector ordinal. Cleanup and accepted-parent invalidation still run
through the existing owner. Active-capture failures retain collector context and
metrics. The initial-detail regression also closes the related pre-existing
nil-metrics rendering failure described below.

Reviewer ordinary-runtime stdin probe independently passed initial g1 revision4
refusal, duplicate suppression, distinct g1 revision5 refusal, successful core
revision6 and cargo:g0 revision7, then nonexistent g1 revision8 refusal. The final
line identified `stage=selection section=ship_cargo:g1 revision=8 run=producer:1`
with neither old ordinal nor capture counters. No source capture was fabricated.

**WR-03: fixed.** Renderer suppression identity now includes bounded rejection
metadata and capture section/revision/run. An unchanged non-sampled terminal
outcome is suppressed; changed attempt, condition, field, operation or safe
correlation remains distinct. Successful captures with metrics retain emission.
The ordinary-runtime regression reaches halted `retry_exhausted`, records its
first transition, and proves that three unchanged callbacks add no records.
The independent stdin probe additionally proves that unchanged selection refusal
is suppressed while a distinct requested revision still emits.

Reviewer reran `pwsh -NoProfile -File
extensions/live_galaxy/tests/run_contracts.ps1 -Suite lua`:95 contracts and20
syntax checks passed. `git diff --check` passed. The lead reported an intentional
temporary removal of runtime rejection propagation causing all six context
cases to fail, followed by restoration. That is a bounded fault-injection oracle
check, not an exhaustive mutation score or an original-version RED rerun; the
reviewer did not repeat the temporary edit.

No grounded remaining correlation, disclosure, suppression, validation or retry
regression was found in the examined fix. The diagnostic repair is ready for the
lead's final relevant regression and scoped completion workflow. This does not
identify the original run055 rejected field or close pending X4/SETA/phase gates.
No new instruction/reference chain was required: the complete coverage listed
below was reused, and no nested specialist, memory or game operation ran.

The following narrative preserves the initial findings and their evidence.

Reviewed the thirteen listed changed files at standard depth and traced the
ordinary runtime, observation and scheduler owners. The task lead established
fresh origin refs and the clean base before implementation; reviewer status
confirmed the intended tracked milestone branch and exact base. No reviewer
fetch, source edit, game operation, private-log inspection or memory access ran.

### Narrative Findings (AI reviewer)

#### WR-02 — historical WARNING / P2, fixed: selection refusal inherits the preceding collector

**File:** `F:/Agent Projects/X4 Modding/live-galaxy/extensions/live_galaxy/lua/live_galaxy_ship_selection.lua:18-28`

**Related:** `live_galaxy_ship_selection.lua:60-85` and
`live_galaxy_runtime.lua:73-76`.

**Rule:** LOG-02 condition/state/correlation ownership; TEST-06 semantic
diagnostic assertions.

**Issue:** A new selection can be refused before `start` replaces the collector
and publishes the new correlation identity. The newly propagated rejection is
then constructed by the previous collector's `fail`/`discard`, attributing the
selection refusal to that collector's old stage and ordinal. The associated
metrics also retain the previous section/revision. An executable actual-runtime
probe collected core revision2 and cargo:g0 revision3, then requested nonexistent
cargo:g1 revision4. It logged `selection_unavailable stage=done ordinal=2
section=ship_cargo:g0 revision=3`, instead of a selection-stage rejection for g1
revision4. The existing missing-group test checks only disposition/read count,
so it passes this false attribution. This directly undermines the diagnostic
repair's purpose.

**Fix:** Distinguish failure to start the requested operation from failure of an
active collector. Return selection-owned rejection metadata with the requested
validated identity; do not derive its stage/ordinal from an obsolete collector.
Keep cleanup and accepted-parent invalidation behavior unchanged. Associate the
new budget with its identity before rejection metrics are published, or omit
capture metrics when no capture exists and carry safe correlation separately.
Add an ordinary-runtime missing-group regression asserting the requested section,
revision and `stage=selection`.

**Scope distinction:** The metric miscorrelation predates this diff, while the
obsolete rejection stage/ordinal is newly exposed by context propagation. A
separate initial-detail-without-parent probe throws at runtime.lua:74 because
metrics contain nil incarnation. Base inspection confirms that initial-detail
crash also predates this repair; it is not counted as a new BLOCKER. Avoid
reproducing that known incomplete-metrics shape when repairing this boundary.

#### WR-03 — historical WARNING / P2, fixed: halted collection is logged again on every callback

**File:** `F:/Agent Projects/X4 Modding/live-galaxy/extensions/live_galaxy/lua/live_galaxy_ship_selection.lua:117-118`

**Related:** `live_galaxy_runtime.lua:64-66` and
`live_galaxy_ship_collection.lua:54`.

**Rule:** LOG-04 bounded emission and distinct significant operations.

**Issue:** All non-collecting/non-busy results now receive capture metrics,
including the unchanged halted collector's repeated `retry_exhausted` outcome.
Runtime deduplication deliberately does not suppress results carrying metrics.
Consequently the same completed rejection is re-emitted on every callback,
although no attempt, source call or collector work advances. An actual-runtime
probe with one invalid core read emitted `invalid_fact` once and identical
`retry_exhausted` operation identities on ticks2,3,4; calls/steps stayed5/17 and
total source reads stayed1. Only idle elapsed duration changed. Before this diff,
these repeated terminal results had no capture metrics and were deduplicated.
The retained real run already encountered this terminal state, making this a
concrete affected path rather than speculative tracing advice.

**Fix:** Preserve the first terminal transition's full context, but suppress or
aggregate repeated unchanged terminal outcomes for the same section/revision/run
and attempt. Do not deduplicate different field rejections or different attempts.
The smallest correction can leave subsequent halted results without metrics,
allowing the existing runtime suppression to work. Add a runtime regression
asserting the first terminal record, no repeated transition record without new
work, and distinct subsequent operation identity still being recorded.

### Evidence, dispositions and readiness

- Reviewer reran `pwsh -NoProfile -File
  extensions/live_galaxy/tests/run_contracts.ps1 -Suite lua`:94 contracts and20
  syntax checks passed. This green suite does not detect WR-02 or WR-03.
- Two temporary stdin-only Lua5.1.5 probes loaded real product runtime,
  observation, selection and source-policy modules with only external carrier/X4
  seams faked. They established the exact logged outcomes described above.
  No probe file or source/test modification was retained.
- Split field predicates preserve the original order, limits and dispositions;
  rejection labels/types exclude raw source values. Source sector refusal reaches
  the budget wrapper. Cargo failure records its pre-cleanup condition and forbids
  delivery/completion. No grounded semantic/cadence/limit regression was found.
- Unknown original run055 rejected field remains unknown. These diagnostic
  regressions do not establish its cause, a freeze, X4 compatibility or runtime
  acceptance. No whole-phase, Rust-boundary, SETA or new-game conclusion is made.
- Bugs, test sufficiency and logging lenses were covered inline because this is
  one closely coupled diagnostic repair; no nested specialists ran.
- Initial-pass readiness, superseded by the final disposition above: fix both
  bounded diagnostic findings and re-review the affected
  ordinary renderer paths before the final relevant regression. Existing runtime
  proof gates and owner checkpoints remain separate and pending.

### Instruction/reference coverage for this pass

Read completely: root `AGENTS.md`; shared `.agent-instructions/x4/AGENTS.md`,
`MEMORY.md`, `CONTRACT_VERSION`, `handoffs/knowledge-gap.md` and
`handoffs/validated-example.md`; project code-conventions `SKILL.md` plus
`references/logging.md` and `references/tooling.md`; tests `SKILL.md` plus
`references/test-standards.md`; X4 integration `SKILL.md` plus
`references/lua-md-conventions.md`; X4 tests `SKILL.md`; Lua `SKILL.md`;
code-review `SKILL.md` plus `references/fan-out.md`; configured Rust conventions
and Rust tests `SKILL.md`; global
`C:/Users/pavlo/.agents/skills/game-repo-standard/SKILL.md`; and
`C:/Users/pavlo/.codex/gsd-core/references/agent-skills-bootstrap.md`.
Game-repo task routing selected no memory/research/skill-authoring references
because this subtask neither accesses memory nor authors engineering rules.
Accepted architecture/ADR context was read for authority, bounds and dependencies.
Only named files/callers, literal text and Git diffs were used; no structural
discovery required ast-index. Global Git ignore access was denied; repository
ignore rules were read and no ignored source/build/log/secret target was reviewed.

## Synchronous-first scoped re-review at e2ffa42 plus frozen repair

**Reviewed:** 2026-09-17T06:03:25Z
**Depth:** standard
**Open findings:** 0 BLOCKER, 0 WARNING.

Reviewed only the owner-approved Plan 10 synchronous-first experimental repair.
The executor confirmed final freeze after the completed-detail guard and its
regression; the reviewer re-read that final production/test delta.

The lead covered correctness, tests and diagnostics inline without specialist
fan-out. Source tracing covered complete capture before admission, independent
core ownership, copied cargo values, immediate native string copying, cumulative
represented-value/allocation accounting, finite work/call/age refusal,
lossless busy delivery without repeating getters, parent binding, ordinary
duration/overrun diagnostics, and demand-origin preparation timing.

Initial and subsequent heavy demands establish a finite 30-second first-data
window. Polling cannot renew its origin. Received data switches to the existing
10-second inactivity policy. The clock profile retains its 10-second policy.
Moved runtime helpers preserve selection completion, retention, durable
revision floors and existing parent-refresh ownership.

No new grounded defect was found. This is a scoped conclusion, not a
whole-phase or whole-repository approval.

### Candidate dispositions

- Mandatory 32-operation slicing, one-heavy-call rejection and 2 ms overrun
  rejection are not findings: the owner explicitly approved their scoped
  experimental replacement.
- Simulated 1,900 ms capture duration is contract-clock evidence, not measured
  CPU cost or proof of a game freeze/thread topology.
- A zero ship count retains the `empty_unproven` refusal rather than falsely
  establishing authoritative emptiness.
- The completed-detail internal-loop candidate was corrected before final
  freeze. Its final guard returns immediately; the executable regression
  verifies no further getter access.
- Retained storage/crew/loadout tables originate from source-owned construction;
  borrowed strings are copied immediately. Cargo values and core fields
  are copied into independent records before delivery can pause.

### Reviewer verification

- Bridge `production_runtime_idle` tests: 5 passed, covering preparation expiry,
  no polling renewal, subsequent demand origin, post-data inactivity and clock
  behavior.
- `run_contracts.ps1 -Suite ship_detail`: 18 passed.
- Prepared Busted runner against `ship_collection_contract.lua` and
  `carrier_b_contract.lua`: 40 passed.
- `git diff --check`: passed. Changed Rust files remain below 200 physical lines.
- Structural navigation used `ast-index stats`, `ast-index update`, and a narrow
  outline of `production_runtime_control.rs`.

Executor-reported evidence, not reviewer reruns: intentional bridge RED 3/5
followed by GREEN 5/5; preserved selection timing 3/3; syntax 20/20;
scoped all-target Clippy and formatting passed.

### Readiness and remaining gates

The frozen repair has no open scoped review finding and may proceed to final
relevant local regression and package regeneration/hash checks. No reviewer-run
full regression, actual pipe/DLL/SQLite chain, packaging or X4 experiment is
claimed here. X4 timing, native-call latency, responsiveness, SETA, runtime
headroom, numerical acceptance, Gate A/B and Plan 10 completion remain pending.
Another deployment/run requires separate owner approval.

### Final interleave fixture follow-up

The faster synchronous path completed 19 revisions before natural parent aging,
so the previous fixture no longer exercised its required replacement-parent
scenario. The strict oracle was retained rather than weakened.

The reviewer inspected the final single-file change in
`extensions/live_galaxy/tests/carrier_b_heavy_configured.lua`: interleave-only
ACKs 2 and 3 each schedule one 16-second synthetic wait, consumed after
`scheduler.tick` and callback-duration recording. No production deadline,
profile, source behavior or readback/cursor oracle changed. The fixture reports
two waits totaling 32,000 ms separately from callback measurements.

Executor-reported serial GREEN: 57,004.1472 ms within the unchanged 60-second
window; core revisions 1 and 4; cargo revision 2 and crew revision 3 precede
replacement, which resumes unfinished loadout:g0 at revision 5 and subsequently
progresses through later members g1–g5. Exact 19 independent readbacks,
current-parent bindings, continuity/later-member oracles and final backlog zero
passed. The source retains one carrier/producer through the scenario. The
reviewer inspected the fixture and unchanged recovery path but did not
independently rerun the actual-chain scenario or inspect private runtime logs.

Reported callback p95 0.0751 ms and maximum 1.5243 ms exclude the deliberate
waits. This is an aging/recovery regression, not an undelayed throughput
benchmark or evidence of production headroom. Its elapsed time must not be
represented as continuous CPU cost or an X4 freeze.

No additional scoped finding remains. Final relevant regression and
packaging/hash checks remain lead-owned gates; game deployment/run,
runtime acceptance and Plan 10 closure remain pending.

### WR-02 — historical WARNING / P2, fixed: premature disconnected diagnostic

`production_runtime_idle.rs:68` records `disconnected/peer-inactive` before
`recover_idle` decides the outcome. Successful recovery in `receive_loop`
continues with the same `&mut BridgePeer`. The actual history between crew
revision 3 and replacement core revision 4 contains the inactive event and
subsequent received records, but no peer-disconnected, peer-absent, compatible
session or bootstrap event. This establishes misleading classification under
LOG-02/03, not a recovery/reconnect defect. Unchanged session/epoch alone would
not have proved transport continuity.

The reviewed bounded correction changes only the inactive event state to
`waiting`, retaining reason `peer-inactive` and the actual terminal exit path.
The reviewer inspected the exact state-only fix and the complete updated
`tests/carrier-b-heavy-configured.ps1`. The oracle reads current/rotated history,
requires first/last commits 1 and 19, rejects terminal/reconnect events and
requires exactly one nonterminal inactive wait with one session and epoch.

Executor-reported RED: the original trace fails with
`THROUGHPUT_SAME_PEER_CONTINUITY_LOSS`. Serial GREEN: 57,071.2118 ms within
the unchanged 60-second window, with two synthetic waits totaling 32,000 ms;
cores 1 and 4; resumed loadout:g0 revision 5 and later member g5. Same-peer
history, 19 independent readbacks and cursor/later-member assertions passed.
Callback p95 0.0985 ms and maximum 1.7134 ms exclude the synthetic waits.
These actual-chain runs were not independently repeated by the reviewer.
This aging/recovery scenario is not throughput headroom or X4 callback safety.

Final scoped review has no open finding. Post-fix formatting, Clippy,
source-size checks and 330 Rust tests passed as executor-reported evidence;
five existing ignored tests remain ignored. Final Lua/native regression and
packaging were subsequently completed as lead-reported evidence: 86 Lua,
20 syntax, XML/schema and serial native clock/recovery passed; package
self-test/build and independent 29/29 digest checks passed. Prepared source
is `b4d6b65`, manifest digest
`832e3de04eba392215abf9295b6b8254b57c85123385c812636608106b344f3d`.
No new game operation or phase acceptance is implied.

## Historical preparation reviews through e661f36

The following sections retain prior reviewed evidence and fixed findings. Their
revision-specific production and package statements do not describe the current
synchronous-first repair.

## Narrative Findings (AI reviewer)

This review covers preparation changes `03e2793..e661f36`, including targeted
production re-review of `67b9b09..7051ff3` and harness/documentation follow-up
`7051ff3..e3468de`, oracle re-review `e3468de..2488caa`, and clock repair
`2488caa..2b41ec4` and final probe documentation `2b41ec4..e661f36`, not the entire phase
or subsequent executor changes. The lead
established fresh origin by fetch before re-review; the checkout was ahead by six
commits with separately owned workflow edits. No fetch, checkout, production edit,
game operation, save access, or memory operation was performed.

The historical production findings, harness oracle gap, and lead-discovered
clock-domain defect are fixed.
No grounded open findings remain in the examined frozen scope. Historical
classifications are retained for traceability, not counted as current blockers.

### CR-03 — historical BLOCKER / P1, fixed: game capture duration used as wall cost

**Original file:** `F:/Agent Projects/X4 Modding/live-galaxy/crates/x4-bridge/src/production_selection.rs:139`

**Issue:** At `2488caa`, proactive refresh derived family cost from canonical
`capture_start`/`capture_end`. These originate in X4 game time, while receiver
parent freshness uses receiver wall time. SETA could turn five wall seconds into
thirty estimated seconds and force unnecessary repeated core refreshes, starving
details. This violates C-04 clock/unit meaning and C-08 distinct urgency/work
clocks. The task lead discovered this after the earlier clean scoped review;
that verdict did not establish correctness of the game-to-wall estimate.

**Fixed at 2b41ec4:** The selector reads observed intent-to-commit wall cost,
not canonical evidence timestamps. Runtime timestamps immediately before sending
a detail intent and stores one pending identity only after successful send.
Admission completes timing after durable `Committed`, before disposition send.
`production_ship_timing.rs:47-64` requires SectionCompletion with matching scope,
incarnation, epoch, key and revision; checked subtraction refuses backward-clock
samples. Nonterminal/wrong receipts, storage failure and ambiguity cannot publish
cost; replay from an older identity cannot match the new pending intent. Core
intent and source invalidation clear pending timing. Three finite family estimates
remain reusable; response loss follows the unchanged terminal/ambiguity contract.

Source SectionStart is assembled after capture work begins and records become
available. Measuring from its receipt would miss deferred collection work.
Intent-to-durable-commit includes that work, but remains an observed wall estimate,
not a native worst-case latency bound or proof that arbitrary demand fits.

Lead-reported intentional RED is `ee6c20d`, frozen GREEN `2b41ec4`. Reviewer rerun
`cargo test --locked -p x4-bridge --lib timing_tests` passed three tests: accelerated
game interval, wrong revision/backward wall clock, and scope/incarnation/epoch/
reset identity. No endpoint/full regression or X4 run was performed.

### Related Lua clock-consumer check

No same-class mixing was found in unchanged related Lua source at `2488caa`:

- `live_galaxy_observation.lua:24,53-59,84-87`: GetCurrentGameTime supplies only
  explicitly labeled game-time capture metadata.
- `live_galaxy_ship_details.lua:47,121`: endpoints populate semantic evidence,
  not callback, candidate or parent-freshness admission.
- `live_galaxy_ship_selection.lua:28-33,46-49,59-61,81-83`: producer status
  `monotonic_millis` supplies parent feedback time, freshness and run admission.
- `live_galaxy_ship_collection.lua:59-64`, `live_galaxy_ship_budget.lua:36-43,56-59`:
  collection/candidate age and callback duration use producer monotonic status;
  metrics use the same start/last values.
- `live_galaxy_scheduler.lua`: pumps/forwards status and control without game-time
  admission arithmetic. Native status comes from transport QPC monotonic time.

These are frozen-source facts, not observed-in-X4 timing evidence. Producer
monotonic and receiver wall timestamps retain separate epochs and are never
subtracted from each other here. Receiver-local inactivity is unchanged.

### WR-01 re-review disposition at 2488caa

**Fixed.** `tests/carrier-b-heavy-oracles.ps1:2-3` rejects any capture/readback
revision sequence that differs from the expected committed sequence, including
missing or empty manifests. Lines22–33 compare the first post-refresh detail
with the previous surviving identity and its next unfinished family, or its
successor when that member disappeared or completed loadout. Lines34–38 require
subsequent progress to another member and reject an outstanding unfulfilled
resume. `carrier-b-heavy-configured.ps1:46` invokes this oracle against independently
reopened readback after every throughput run, using revisions1–19 or1–12.

The offline contract first accepts a valid six-revision trace, then rejects
otherwise equivalent reset-family, missing-capture and later-member-starvation
traces. Reviewer rerun `pwsh -NoProfile -File
tests/carrier-b-heavy-oracles-contract.ps1` passed. The lead reported extraction
`e25bbb3`, intentional RED `0e4776e` accepting the reset trace, and GREEN
`2488caa`; those historical executions were not repeated by the reviewer.

No production transition, architecture, deadline or game policy changed in this
oracle repair. The current TEST-01 clarification was re-read and reinforces the
same concrete continuity/readback contract; no new runtime requirement is inferred.

### WR-01 — historical WARNING / P2, fixed: interleave/readback false green

**File:** `F:/Agent Projects/X4 Modding/live-galaxy/tests/carrier-b-heavy-configured.ps1:22-29,37-42`

**Rule:** TEST-01 independent observable contract and TEST-07 capable regression;
TOOL-05 empty discovery cannot satisfy mandatory checks.

**Issue:** Interleave mode requires only two core captures, then verifies each
listed detail against that parent and its own ordinal. It does not assert that
the replacement core resumes the previous member's unfinished family or that
later members subsequently progress. A selector regression that ignores the
durable cursor and restarts at `g0` after every refresh still satisfies nineteen
commits, two cores, exact current dependencies, ordinal identity binding, all
nested counts and every existing readback assertion. Pure cursor tests cannot
detect a caller ignoring their result. The reported good `g4` to `g5` run is
observed local evidence, but the retained actual-path oracle cannot detect this
original starvation class.

Capture lines also form the entire readback manifest without comparison to the
expected committed revision sequence. In non-interleave throughput mode, missing
capture metrics can leave an empty manifest and skip every independent readback
while printing PASS. In interleave mode, core-only capture lines can satisfy the
two-core check while omitting detail verification.

**Fix:** Assert the first post-replacement-core detail's surviving identity and
next unfinished family against the preceding committed detail, and assert later
identity progress beyond it. Require exact capture/readback revision coverage
against the committed expected sequence, rejecting missing, duplicate or empty
entries. Use small executable negative traces for reset-to-`g0` and missing
capture lines, then the focused actual-chain GREEN run. No scheduler, protocol,
deadline or game acceptance change is needed.

### Re-review disposition at 7051ff3

**CR-01: fixed.** `production_selection.rs:117-140` derives the next family and
member from the latest durable detail identity, predicts required capture time
from observed family durations, and refreshes the current core when its remaining
freshness cannot admit that work. An unobserved family has zero observed duration
and is admitted against a fresh parent, avoiding a bootstrap refresh cycle.
`production_ship_cursor.rs:16-33` preserves unfinished family progress when the
member survives reorder; after removal it advances to the next surviving identity,
wrapping fairly rather than restarting every fresh parent at `g0`. Receiver-owned
current-core/group checks remain intact.

`production_admission.rs:74-78` maps stale parent to supersession and invalidates
incomplete source work. Runtime advances on that recoverable disposition and its
idle stale-parent path can request refresh without immediately dropping the peer.
Lua polls replacement controls even when producer progress is paused. Native
`Readiness::RefreshCore` accepts only a replacement core intent, while direct
permanent and ambiguous dispositions remain terminal. Explicit Lua stale-parent
failure enables the same narrowly scoped refresh path. This establishes the
examined transitions; it does not establish that every arbitrary workload can
fit unchanged deadlines.

**CR-02: fixed.** `lua_input.rs:114-126` runs shared admission before owned detail
decoding. `lua_detail_budget.rs:7-65` traverses the original Lua table without
owned payload copies, using one checked frame-derived budget across all nested
arrays, key/value entries and strings. It rejects excessive depth, metatables,
unsupported values and budget overflow, restoring the Lua stack on every outcome.
The typed vector/string and canonical formatting owners are consequently bounded
by that preflight; later semantic/raw/frame checks remain explicit. The earlier
four-million-tier example cannot pass this traversal. The conservative accounting
may refuse a large record; refusal is resource-derived, not population truncation.

The actual-ABI fixture uses 16 roles with 16 tiers, within its 16-row per-array
limit, but collectively above the 4 KiB frame-derived admission budget. Without
preflight that valid shape reaches later serialization refusal (`-12`), not the
expected preflight/decode refusal (`-20`), making the oracle capable of detecting
removal of the new boundary. Later successful same-section records/commits and
independent readback are lead-reported no-partial-state evidence. This regression
is GREEN-only; no RED execution is claimed.

### CR-01 — historical BLOCKER / P1, fixed: expired-parent rotation

**File:** `F:/Agent Projects/X4 Modding/live-galaxy/crates/x4-bridge/src/production_selection.rs:89-96`

**Related:** `extensions/live_galaxy/lua/live_galaxy_ship_selection.lua:59-61`,
`crates/x4-bridge/src/receiver_ship_detail.rs:103-107`, and
`crates/x4-bridge/src/production_runtime.rs:102-104`.

**Issue:** The selector returns to core only after all three detail families
for every member commit. The larger admitted workload and resumable crew stages
can exceed the unchanged 30-second parent freshness window before that rotation
finishes. Lua then refuses the next detail as `stale_parent`; the receiver also
checks parent freshness independently. Without a commit the runtime never calls
`next`, so it cannot select the refresh that would unblock progress. Disconnect
and reconnect restart at core followed by `g0`, starving later members under a
repeatable workload. This violates D-05 eventual coverage and the preparation
repair's complete-workload intent. It is a correctness failure, not a request
to optimize throughput or relax freshness.

**Evidence:** The source proves the transition deadlock. The task lead additionally
reported measured synthetic crew capture of approximately 4.475 seconds per
80-role member; a 129-member rotation cannot fit the existing parent window.
Those measurements are filtered lead evidence, not a reviewer-run X4 experiment.
A finite first-groups benchmark does not disprove starvation.

**Fix:** Use the existing selector to refresh core before a stale detail can
stall the rotation, preserving fair member/family progress across replacement
parents and binding every detail to the exact current parent. Preserve identity
progress through membership reorder/churn rather than resetting to `g0`. Do not
extend deadlines or introduce a second scheduler. Add a deterministic regression
that crosses parent refreshes and proves later-member progress plus exact
dependency binding, then exercise the affected actual-chain interleaving.

### CR-02 — historical BLOCKER / P1, fixed: native aggregate admission

**File:** `F:/Agent Projects/X4 Modding/live-galaxy/crates/x4-carrier-native/src/lua_detail_array.rs:56-60`

**Related:** `crates/x4-carrier-native/src/ship_crew_input.rs:43-44,82-84`,
`crates/x4-carrier-native/src/lua_input.rs:119-123`, and
`crates/x4-carrier-native/src/producer_ship_detail.rs:43-48`.

**Issue:** Checked multiplication only rejects an address-space-overflowing
individual vector. It does not reserve a configured aggregate byte/work budget.
The new 1,048,576 inner limit is independently reused for every crew role and
every role's tiers. Native decoding copies every vector and string before producer
serialization checks `max_raw_bytes`; complete-message refusal is later still.
Lua's allocation wrapper accounts for FFI buffers, not these native copies.
Consequently per-array admission no longer establishes the aggregate safeguard
required by ADR-LG-006/C-08.

**Evidence:** A dense record with four sorted valid roles, each reporting
1,048,576 tiers and 128-byte valid tier names, passes the decoder's individual
count/string checks. Copied name bytes alone exceed 512 MiB, before vector storage
and canonical serialization, despite a 256 MiB configured total envelope and a
1 MiB packet limit. `CrewObservation::validate` bounds each role separately;
it has no cumulative tier/byte bound. Such input must be refused before owned
allocation, not after constructing its inevitably oversized canonical record.
No giant-allocation experiment was run.

**Fix:** Pass a shared resource-derived decode admission budget across nested
arrays and copied strings; charge checked vector allocation, retained text and
work before copying. Bound canonical output construction against the admitted
semantic-record/message budget rather than materializing unlimited content and
checking afterward. Keep honest refusal without truncation. Add a small-budget
native regression in which individually acceptable nested arrays collectively
exceed the shared budget and assert early refusal without partial producer state.

## Verification and limitations

- Final oracle follow-up at `2488caa`: offline valid and three defective trace
  scenarios passed their intended accept/refuse outcomes. Source/readback
  integration reviewed; no endpoint rerun. Filtered lead evidence reports a fresh
  actual 19-commit interleave GREEN in 59.396s, exact capture/readback coverage,
  core15 resuming crew/loadout:g4 then memberg5, and exact current dependencies.
  Its callback maximum1.077ms does not erase the earlier2.705ms risk. Neither run
  establishes material60s headroom or prepared2ms game safety.
- Harness/documentation follow-up at `e3468de` was source-only while the executor
  owned serial endpoint runs. Source timing uses QPC, actual method/callback
  samples and nearest-rank empirical percentiles; the document correctly labels
  small samples and combined transfer/validation/commit timing. Its synthetic
  callback20ms/rate25ms copy is separate from prepared callback2ms/rate50ms.
- Filtered lead evidence: one producer/peer committed 19 revisions in 59.362s,
  with core15 following cargo:g4 revision14, crew/loadout:g4 bound to core15,
  then progression to g5. Reopened earlier/current readback passed. Maximum
  callback2.7049ms exceeds the prepared 2ms guard; the document makes no 2ms-safe
  or meaningful 60s admission-headroom claim. A later restarted-producer run
  passed revisions1–12: core9 followed by crew:g2 revision10, loadout:g2 revision11
  and cargo:g3 revision12, all rebound to core9. Those runs were not independently
  repeated by the reviewer. At that earlier revision they did not eliminate
  WR-01's retained-oracle gap; the later `2488caa` oracle repair closes it as
  described above.
- Reviewer reruns at frozen head: `cargo test --locked -p x4-bridge --lib
  production_ship_cursor` — 2 passed; `cargo test --locked -p x4-carrier-native
  --lib lua_detail_budget` — 1 passed; `cargo test --locked -p x4-carrier-native
  --test producer_contract
  superseded_heavy_capture_accepts_only_fresh_core_and_keeps_refusals_terminal`
  — 1 passed; Busted `ship_detail_contract.lua` — 9 passed. These are focused
  offline tests, not a full regression or X4 evidence.
- Filtered lead/executor evidence: actual ABI cargo/crew/loadout revisions 2–13,
  independent reopened history/current/replay, native timeout versus permanent/
  ambiguous refusal, bridge cargo contract, Lua and syntax checks, strict native/
  bridge all-target Clippy and source-size checks passed. The reviewer inspected
  relevant fixtures and source but did not independently rerun the fixed endpoint.
- Focused executable check: `tools/.cache/luarocks-tree/bin/busted.bat
  --lpath=extensions/?.lua;extensions/live_galaxy/tests/?.lua
  extensions/live_galaxy/tests/ship_collection_contract.lua` — 13 successes,
  zero failures/errors. This verifies the committed collection contract, not
  scheduling over successive parent revisions or native aggregate admission.
- Structural readiness: `ast-index stats`, `ast-index update`, and narrow
  `outline` for the native array reader. Source/call-chain review included
  source adapters, collection budget, shared scheduler, native detail decoders,
  producer serialization, receiver dependencies, and domain group/crew contracts.
- No full regression or fixed-endpoint actual-chain rerun was performed while
  the executor owned benchmark work. Its synthetic callback profile and finite
  commit/readback slice cannot establish game-safe callback timing or eventual
  coverage of every admitted ship. The prepared 2 ms/50 ms values are unchanged;
  their previously known native-chain guard failure is not a new finding here.
- `u16` group ordinals remain a producer/domain/receiver compatibility ceiling.
  The wider allocation envelope alone does not remove that representation
  limit. No protocol widening is prescribed without a concrete required workload
  and compatibility decision; it is not silently treated as unlimited support.
- Gate A, Gate B, SETA/runtime evidence, numerical acceptance, and whole-phase
  closure remain pending. No open code-review blocker remains in this frozen
  production/harness repair. Final relevant local regression and candidate
  packaging have subsequently passed as filtered lead evidence below; full
  129-member detail coverage/headroom, the prepared 2 ms game
  callback, and all 21 runtime acceptance rows remain unproven. A finite synthetic
  slice must not be represented as full-faction capacity or game readiness.
- Git's global ignore file was inaccessible; repository ignore rules were read
  and explicit scoped source targets were checked. No ignored build/runtime or
  secret files were reviewed.

## Post-review local gate completion

The reviewer read the final probe-document delta through `e661f36`; it preserves
receiver-wall versus game-capture clock meaning, synthetic versus prepared
settings, sample-count limitations, and all outstanding runtime obligations.
Production remains frozen at the separately recorded `2b41ec4`.

The following are filtered lead/executor facts, not reviewer-rerun suites:

- Final formatting, workspace all-target Clippy, 200-line source check and 325
  executed Rust tests passed. Five existing ignored station-publication contracts
  remain explicitly deferred for Phase05.3 reconciliation, not counted as passes.
- Final 80 Lua contracts, 20 syntax checks, XML4+7/schema and serial native
  clock/recovery checks passed; reported combined elapsed time was71.708s.
- Actual heavy ABI core/detail, exact prepared2ms/rate50ms first/restart and four
  interrupted-family recovery checks passed. Synthetic20ms/rate25ms restart12
  and same-peer interleave19 passed complete capture/readback and cursor oracles.
- Latest synthetic restart callback maximum was10.764ms, above the prepared2ms
  guard. Final interleave took59.473s of60s, leaving0.527s, approximately0.9%;
  it does not demonstrate required headroom. Its1.411ms callback maximum does
  not erase the larger restart or earlier samples. Zero final backlog is not
  a steady-state, whole-faction or independently timed per-stage capacity proof.
- Earlier pre-clock full-pass evidence was discarded as final evidence. A
  premature overlapping synthetic launch's pipe-open refusal was excluded and
  the affected run repeated serially after predecessor/process cleanup.
- Prepared candidate source is `e661f3640af3a05b0f2f5336face8b239d1c33b5`;
  package manifest digest is
  `293ca4462909052e05a4a84772c3eb34ee1856d6313f7343c5c909e1d1dbc6f4`.
  The lead independently rehashed29/29 bundle files. Evidence self-test16 and
  preparation checks passed; runtime acceptance intentionally refused with
  `pending_implementation`.

No grounded new documentation/status defect was found. The scoped local repair
and candidate are ready for lead-owned commit/push and the still-pending owner
checkpoint, not whole-phase closure, game acceptance or a public-readiness claim.

## Instruction and skill files read

- `AGENTS.md`
- `.agent-instructions/x4/AGENTS.md`
- `.agents/skills/live-galaxy-code-review/SKILL.md`
- `.agents/skills/live-galaxy-code-review/references/fan-out.md`
- `.agents/skills/live-galaxy-code-conventions/SKILL.md`
- `.agents/skills/live-galaxy-code-conventions/references/logging.md`
- `.agents/skills/live-galaxy-code-conventions/references/tooling.md`
- `.agents/skills/live-galaxy-tests/SKILL.md`
- `.agents/skills/live-galaxy-tests/references/test-standards.md`
- `.agents/skills/live-galaxy-rust-conventions/SKILL.md`
- `.agents/skills/live-galaxy-rust-tests/SKILL.md`
- `.agents/skills/live-galaxy-x4-integration/SKILL.md`
- `.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md`
- `.agents/skills/live-galaxy-x4-tests/SKILL.md`
- `.agents/skills/lua/SKILL.md`

The bounded bugs/tests/diagnostic lenses were covered inline; no nested
specialists were dispatched. Accepted context was the Plan 10 local preparation
section, current Phase 05.5 context, and ADR-LG-006. No source files were modified.

## Plan 10 runtime-repair re-review

Reviewed base: `f43b595b9f4783c6d960c907d4b41ed683db8dfc`.
Reviewed committed head: `bbe286169baa5a0cb2daec8ec613a10f80819ee5`,
plus the declared uncommitted Plan 10 repair. The unrelated review-policy commit
at that head was excluded from the Plan 10 task boundary.

The review followed the updated full-task re-review contract. Every Plan 10 file
was reread in full on each round, including files unchanged since earlier review.
Bounded independent correctness and test lenses supplemented the lead pass.

Resolved findings:

- **P1, fixed:** diagnostic pending-message capture reset the producer before the
  bytes were durably retained. The flow now writes and synchronizes first; failed
  persistence does not reset and malformed but durable bytes remain diagnosable.
- **P2, fixed:** `.planning/STATE.md` had duplicate obsolete `state_head` keys.
  It now contains one truthful installed revision.
- **P2, fixed:** the FFI oracle did not protect the deliberate LuaID-only
  `GetComponentData(..., "cargo")` exception. It now invokes and asserts that
  exact boundary.
- **P2, fixed:** opening the final diagnostic output before demand/receive left an
  empty artifact on send, receive or timeout failures. Output creation now occurs
  only after successful receipt; partial persistence is cleaned up, with executable
  negative coverage.
- **P2, fixed:** the capture repair exceeded RUST-04's 200-line ceiling. A cohesive
  output-lifecycle sibling module restored the limit without changing behavior.

The proposed XEN/KHK rejection finding was rejected: the behavior is unchanged
from the Plan 10 base and explicitly remains Task 2 work after ordinary-faction
Gate A. It is an open execution obligation, not a regression in this repair.

Final verdict: **CLEAN**, with no remaining or newly introduced P0-P2 findings.
Reviewer verification passed capture 3/3, workspace all-target Clippy with warnings
denied, formatting, source-size enforcement and diff validation. The remaining
evidence gap is owner-operated X4 runtime confirmation of the corrected cargo,
crew and loadout FFI calls; local fake-FFI evidence cannot establish that claim.

## Plan 10 full-snapshot and per-record churn review

The scoped review covered the follow-up repair that captures core, cargo, crew,
and loadout in one game callback, drains the captured detail through the existing
durable section envelopes without bridge rate sleeps, and marks only an affected
detail record as possibly stale when its ship changes during capture.

One blocker was found and fixed: a cached detail collector retained
`capture_only=true` after entering delivery. A later native push or finish failure
could therefore skip `fail_section` and leave the reserved producer section active.
`details:deliver()` now clears capture-only mode before reserving native delivery,
and an executable regression verifies that a failed cached push aborts exactly one
reserved section.

Final verdict: **CLEAN**. The focused detail suite passed 25/25. Workspace format,
all-target Clippy with warnings denied, all Rust tests, source-size enforcement,
Lua 100/100 plus 20 syntax checks, XML/schema checks, configured recovery, and
throughput recovery all passed. The throughput run completed 12 independent
readbacks with zero final backlog and zero synthetic waits. Runtime acceptance in
X4 remains the next owner-operated gate; these local results do not claim in-game
frame-time behavior.

## Plan 10 core-churn runtime repair review

Runtime run `055-gate-a-normal-sdk-20260921-214108` exposed one remaining
whole-snapshot rejection path. At core ordinal 16, revision 95, the ship owner
changed during final revalidation. The collector discarded the approximately
750-record core snapshot and retried the same stale capture until exhaustion.
No revision committed, so the otherwise clean frame trace is not Gate-A
evidence.

The repair keeps the census owner as the source-scope owner, carries strict
`consistency` and `consistency_reason` fields through Lua, the native ABI,
canonical wire content, and receiver validation, and marks only the affected
record `possibly_stale`. Missing, owner, location, and remaining core changes
have distinct reasons. Invalid initial identity or invalid field shapes still
fail closed. The receiver rejects malformed consistency pairs and does not
weaken atomic section admission.

Review also found two verification/tooling defects and resolved them. Exact
byte-budget fixtures were updated for the two new mandatory fields without
changing production limits. The runtime runner now supports the working
FrameView SDK public CLI in repeated ten-second segments, filters rows to the
X4 PID, and retains the legacy PresentMon path. Fixed-pipe recovery suites are
run serially; their earlier parallel `open_failure` was test isolation, not a
product failure.

Final verdict: **CLEAN**. Focused domain, native, bridge, Lua, ABI, and
configured recovery tests pass. Workspace formatting, strict all-target
Clippy, all Rust tests, source-size enforcement, Lua 103/103 plus 20 syntax
checks, XML/schema checks, and throughput recovery pass. Replacement in-game
runtime evidence remains required after packaging and installation.

## Plan 10 FrameView deduplication review

Normal-time run `055-gate-a-normal-20260922-122441` exposed a FrameView SDK
aggregation defect. The SDK emitted each exact X4 frame event 8--10 times with
the same process, swap chain, QPC timestamp, and metric values. The runner had
counted all 59,755 raw rows as frames.

The repair preserves raw counts and deduplicates only SDK metrics by
`ProcessId + SwapChainAddress + QpcTime`. Its self-test proves exact duplicate
collapse without merging adjacent timestamps. Recalculation retained 7,050
unique frames and 52,705 duplicate rows. The transport verdict is unchanged:
34 commits, zero rejected or failed outcomes, zero runtime diagnostic failures,
and a 0.999 normal-time factor.

The corrected frame verdict is a Gate-A failure. With bridge collection active,
the run measured 121.984 average FPS, 25.400 ms p99, 552.879 ms maximum, and 25
frames at least 50 ms. A 30-second same-session control with no bridge measured
264.387 average FPS, 6.084 ms p99, 43.845 ms maximum, and no frame at least 50
ms. X4 diagnostics independently reported a 277 ms source callback. The
accepted fallback to chunked work is therefore required before SETA evidence.

Final verdict for the runner repair: **CLEAN**. The runner self-test and diff
validation pass. Runtime acceptance remains **FAILED** pending the chunked-work
repair and replacement normal-time evidence.
