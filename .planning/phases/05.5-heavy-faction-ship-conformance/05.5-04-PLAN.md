---
phase: 05.5-heavy-faction-ship-conformance
plan: 04
status: superseded
superseded_by: [05.5-09, 05.5-10, 05.5-11]
type: tdd
wave: 4
depends_on: [05.5-03]
files_modified:
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/src/faction_observation.rs
  - crates/observation-domain/tests/faction_observation_contract.rs
  - extensions/live_galaxy/lua/live_galaxy_factions.lua
  - extensions/live_galaxy/tests/faction_observation_contract.lua
  - crates/x4-carrier-native/src/producer.rs
  - crates/x4-carrier-native/src/producer_feedback.rs
  - crates/x4-carrier-native/src/producer_types.rs
  - crates/x4-carrier-native/src/lua_progress.rs
  - crates/x4-carrier-native/src/ship_schedule.rs
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/receiver_context.rs
  - crates/x4-bridge/tests/ship_recovery.rs
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/tests/carrier_b_contract.lua
autonomous: true
requirements: [OBS-06, VAL-01, VAL-06, VER-LG-003, VER-LG-011, VER-LG-012]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Every discovered faction has an evidence-backed included, excluded or unknown observation disposition independent of mind participation.
    - The existing scheduler enforces heavy permits, real-time token debt and bounded fair progress for core and detail work.
    - Restart, reconnect, load/reload, source uncertainty and stale connection generations reconcile durable receipts without duplicate publication, discard incomplete source work and require fresh qualification and collection; stale dependencies cannot acquire current authority.
  artifacts:
    - path: crates/observation-domain/src/faction_observation.rs
      provides: Evidence-backed observation eligibility and closure blockers
    - path: crates/x4-carrier-native/src/ship_schedule.rs
      provides: Production adapter to the existing ObservationScheduler
    - path: crates/x4-bridge/tests/ship_recovery.rs
      provides: Multi-group lifecycle, ownership and earlier/current revision matrix
  key_links:
    - from: crates/x4-carrier-native/src/ship_schedule.rs
      to: crates/observation-ingest/src/scheduler.rs
      via: CollectionIntent admission and actual-work completion
    - from: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      to: crates/x4-bridge/src/receiver_context.rs
      via: Load/incarnation fences and receiver-owned exact dependency authority
---

<objective>
Make the heavy ordinary-faction slice schedulable and recoverable before measured Gate A, and prepare the evidence-backed dynamic taxonomy needed for Gate B. Implement D-05 through D-13 and D-15 without activating the full faction workload before Gate A.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-RESEARCH.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-PATTERNS.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-03-SUMMARY.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-SOURCE-EVIDENCE.md
@crates/observation-ingest/src/scheduler.rs
@crates/observation-ingest/src/scheduler_budget.rs
@crates/observation-ingest/src/scheduler_queue.rs
</context>

## Artifacts this phase produces

New paths: `crates/observation-domain/src/faction_observation.rs`, `crates/observation-domain/tests/faction_observation_contract.rs`, `extensions/live_galaxy/lua/live_galaxy_factions.lua`, `extensions/live_galaxy/tests/faction_observation_contract.lua`, `crates/x4-carrier-native/src/ship_schedule.rs`, `crates/x4-bridge/tests/ship_recovery.rs`. New symbols: `FactionObservationDisposition`, `FactionOriginEvidence`, `FactionCensusEntry`, `classify_observation_faction`, `ShipSchedule`, `complete_source_step`. New fields: raw faction ID, origin evidence/reference, disposition/reason, unknown blocker, independent observation eligibility and mind-participation fields, census revision/capture interval, exact collection intent ID, declared/actual work, native-call duration, queue age and bounded diagnostic counters. Existing `ObservationScheduler`, `CollectionClass`, `WorkKind`, heavy permits and overrun debt remain the authority, not a duplicated scheduling implementation. No new mind/visibility flags are created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify every dynamically discovered faction without feature admission</name>
  <files>crates/observation-domain/src/lib.rs, crates/observation-domain/src/faction_observation.rs, crates/observation-domain/tests/faction_observation_contract.rs, extensions/live_galaxy/lua/live_galaxy_factions.lua, extensions/live_galaxy/tests/faction_observation_contract.lua</files>
  <read_first>Read D-06 through D-09, source-evidence faction taxonomy and uncertainty rows, existing domain authority types, Lua conventions and Rust/X4 test skills.</read_first>
  <behavior>Independent vanilla/DLC ordinary, small and pirate factions are included without territory/shipyard/size prerequisites. XEN and KHK are included for observation only. Player/mod-added/service/temporary exclusions need source evidence and preserve the raw ID; unresolved origin is unknown and blocks closure. Zero observed ships does not imply excluded or authoritative known-empty.</behavior>
  <action>RED: write deterministic classifier and pure-Lua discovery tests for every disposition, all faction categories, duplicate raw IDs, empty/one-member census, stable equal-priority order, unseen DLC IDs and conflicting origin evidence. GREEN: capture C.GetAllFactions dynamically with bounded count/fill and preserve every returned ID. Classify from source-backed origin/role facts approved in Plan 02, never a fixed mandatory-faction allowlist or observed count such as 25. A versioned evidence mapping may explain source metadata, but cannot silently discard unseen IDs: unclassified entries become unknown. Keep observation eligibility separate from Faction Mind participation and recipient access; inclusion of XEN/KHK must not enable hostile minds. Retain service/temporary exclusions with reason and evidence. On count/fill mismatch discard the census attempt atomically and retry/yield under supplied limits. REFACTOR without creating a general faction framework. This task prepares classification and tests; full-set source scheduling remains disabled until Plan 07.</action>
  <reversibility rating="reversible">Classification is observation policy backed by versioned evidence, not persisted mind admission.</reversibility>
  <acceptance_criteria>Every returned ID survives into an explicit disposition; all required independent vanilla/DLC and XEN/KHK categories are represented in tests; unknown is a closure blocker; no gameplay participation is changed.</acceptance_criteria>
  <verify><automated>cargo test --locked -p observation-domain --test faction_observation_contract</automated><fails_when>Exit is nonzero, an unseen ID disappears, unknown is accepted as closure, or observation inclusion changes mind participation.</fails_when><automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/faction_observation_contract.lua</automated><fails_when>Exit is nonzero, churn emits a truncated census, or a raw faction ID is silently filtered.</fails_when></verify>
  <done>Dynamic discovery has an explicit, testable and scope-safe disposition contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect heavy source steps to the existing permit/debt scheduler</name>
  <files>crates/x4-carrier-native/src/producer.rs, crates/x4-carrier-native/src/producer_feedback.rs, crates/x4-carrier-native/src/producer_types.rs, crates/x4-carrier-native/src/lua_progress.rs, crates/x4-carrier-native/src/ship_schedule.rs</files>
  <read_first>Read ObservationScheduler, SchedulerBudget, IntentQueue and scheduler_policy tests; read the Plan 01 producer pump/control and Plan 03 group cursor. Inspect callback status consumers before changing an internal status field.</read_first>
  <behavior>Pending I/O wins before source admission. Only a matching intent consumes one permit; heavy permits are returned once; actual-work overrun becomes bounded debt; repayment does not create burst credit. Game-time urgency cannot mint real-time permits. Equal priorities have deterministic progress and no unbounded detail/faction starvation within a finite admitted round.</behavior>
  <action>RED: add focused production-adapter tests with an injected monotonic clock for exact refill boundaries, backwards time, large jumps, overflow, zero credit, pending bytes, malformed completion identity, debt saturation, heavy permit exhaustion and fair round progress. GREEN: adapt the existing ObservationScheduler to production ship intents using CollectionClass::Core/Detail and WorkKind::Heavy where measured cost/indivisibility requires it. Use a bounded round frontier: enqueue one eligible continuation per active scope; do not refill completed core intents indefinitely ahead of waiting details; begin a new round only after bounded completion/degradation of the current frontier. Retain core-before-detail ordering within a round and terminal transport reserve. Expose a bounded source-step authorization through the already generalized progress/control contract; report actual work/duration by the same intent identity exactly once. A native call cannot be interrupted: oversized count/allocation or measured indivisible cost rejects/cools down the source before starting another call; record the overrun instead of pretending a preempted slice. The Lua callback remains the sole pulse source; transport pumping happens before collection, and retries reuse immutable bytes. Accept limit inputs from the single profile wired by Plan 05; do not invent production rates or a second scheduler. REFACTOR into ship_schedule helpers to meet the 200-line limit.</action>
  <reversibility rating="reversible">The adapter reuses existing scheduling policy and typed limits; no persistent schema or scheduler replacement is introduced.</reversibility>
  <acceptance_criteria>Production steps use existing heavy/debt paths; tests demonstrate bounded fair progress and transport priority; normal versus accelerated game time changes urgency only; no new callback loop exists.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-carrier-native --lib</automated><fails_when>Exit is nonzero, the adapter admits source work while pending transport wins, duplicate completion restores credit twice, or any budget boundary is bypassed.</fails_when><automated>cargo test --locked -p observation-ingest --test scheduler_policy</automated><fails_when>Exit is nonzero or existing scheduler ordering, debt, heavy permit or pending-transport behavior regresses.</fails_when></verify>
  <done>The real producer shares the established scheduler and exposes measured bounded work accounting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve multi-group recovery and invalidate changed dependencies</name>
  <files>crates/x4-bridge/src/production_runtime.rs, crates/x4-bridge/src/receiver_context.rs, crates/x4-bridge/tests/ship_recovery.rs, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/tests/carrier_b_contract.lua</files>
  <read_first>Read current carrier_b_recovery, receiver_context_contract, persistence replay and load-latch patterns; read D-05, D-11, D-13 and D-15. Use Plan 03 exact dependency identities rather than sender assertions.</read_first>
  <behavior>A lost completion response reconciles to the same durable receipt without publication. Changed content at the same identity is rejected. Known-retryable transport-local failures replay immutable bytes only within the same qualified connection/source boundary. Restart/reconnect, load/reload, source uncertainty and stale connection generations discard incomplete capture/pending source work; neither an absent nor an unresolved receipt permits resuming it. Fresh baseline and collection use a new attempt identity. Load/reload fences the old incarnation; stale callbacks cannot become current. Owner/location changes invalidate dependent detail authority; disappearance alone is not deletion. Earlier/current revision readback remains available under retention policy.</behavior>
  <action>RED: create a matrix across core and each detail group for same-boundary known-retryable failures, disconnect before/after acceptance, lost ACK, lost completion response, durable-receipt reconciliation after restart, absent/unresolved receipt after reconnect, digest/content conflict, old/new producer incarnation, stale connection generation, source-boundary uncertainty, load/reload during pending I/O, ownership/location change, missing entity, source mismatch and stale details. Assert unchanged getter counts for transport-only retry, discard of incomplete source work at every boundary, and fresh qualification before new collection. GREEN: remove any remaining clock-only revision floors/demand assumptions in the owned production path and validate group dependency state from durable receiver context. Per inherited Phase 05.4 D-06, hydrate accepted revisions and bounded reconciliation identities from existing persistence; reconcile durable receipts/committed outcomes by exact identity and digest without publishing them again. Retain immutable sender bytes for retry only while the same connection/source boundary remains qualified and the outcome is transport-local and proven retryable. At reconnect, game load/reload, source uncertainty or stale connection generation, discard incomplete capture and pending source work, fence old callbacks and require compatibility confirmation, a fresh qualifying baseline and fresh collection under the new boundary with a new attempt identity. An absent or unresolved receipt cannot authorize old-work replay. On game load/reload latch the boundary until safely sampled and fence the old incarnation; never bind old facts to a new save/session automatically. Preserve historical published revisions as historical, without granting current eligibility to invalidated details or interpreting transient UI disappearance as authoritative deletion. Keep failed detail isolated from valid core. Bound reconnect/retry/age paths using profile limits; on exhaustion emit explicit degraded/rejected state and retain honest recoverability evidence. REFACTOR narrowly; if existing persistence cannot represent the required identity without an incompatible migration, stop for a checkpoint:decision before that one-way door.</action>
  <reversibility rating="costly">Recovery logic affects durable replay semantics; compatibility tests and retained history are required, but no destructive migration is authorized.</reversibility>
  <acceptance_criteria>Every lifecycle matrix row has behavioral assertions; historical/current authority stays distinct; response loss/restart reconciles a committed receipt without another publication. Each reconnect/load/reload/uncertainty/stale-generation case discards incomplete source work and requires fresh baseline and collection, including absent/unresolved-receipt cases; unknown or stale source state never promotes current completeness.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-bridge --test ship_recovery</automated><fails_when>Exit is nonzero, a scenario duplicates publication, changed content reuses a receipt, old work resumes across a boundary, fresh qualification is bypassed, or stale detail gains current authority.</fails_when><automated>cargo test --locked -p x4-bridge --test receiver_context_contract --test carrier_b_publication --test carrier_b_recovery</automated><fails_when>Exit is nonzero or existing receiver authority, publication or recovery semantics regress.</fails_when><automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/carrier_b_contract.lua</automated><fails_when>Exit is nonzero, a boundary loses its fence or retains incomplete source work, fresh qualification is bypassed, or transport-only retry re-enters a source getter.</fails_when></verify>
  <done>Heavy group recovery is proven locally, with runtime lifecycle rows explicitly reserved for the owner gates.</done>
</task>

<verification>
Run focused tests first. Mutation testing of new deterministic classification, dependency and scheduling safety logic is selected and performed during final review convergence in Plan 08; surviving non-equivalent mutants block closure. No full-set workload is activated by this plan.
</verification>

<success_criteria>
The complete one-faction path can be throttled, invalidated and recovered under bounded deterministic behavior. Taxonomy is ready but its full dynamic workload remains gated by measured ordinary-faction acceptance.
</success_criteria>

<output>Create `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-04-SUMMARY.md` with lifecycle matrix, RED/GREEN evidence and costly-decision compatibility notes.</output>
