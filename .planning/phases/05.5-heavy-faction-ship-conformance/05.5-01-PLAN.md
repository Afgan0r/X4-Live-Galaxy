---
phase: 05.5-heavy-faction-ship-conformance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/src/ship_core.rs
  - crates/observation-domain/src/ship_field.rs
  - crates/observation-domain/src/ship_group.rs
  - crates/observation-domain/tests/ship_contract.rs
  - crates/x4-carrier-native/src/producer.rs
  - crates/x4-carrier-native/src/producer_collection.rs
  - crates/x4-carrier-native/src/producer_message.rs
  - crates/x4-carrier-native/src/producer_feedback.rs
  - crates/x4-carrier-native/src/producer_types.rs
  - crates/x4-carrier-native/src/lua_input.rs
  - crates/x4-carrier-native/src/lua_producer_operations.rs
  - crates/x4-carrier-native/src/lua_progress.rs
  - crates/x4-carrier-native/src/lua_producer_operations_tests.rs
  - extensions/live_galaxy/lua/live_galaxy_carrier.lua
  - extensions/live_galaxy/lua/live_galaxy_observation.lua
  - extensions/live_galaxy/lua/live_galaxy_scheduler.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/tests/carrier_b_contract.lua
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/receiver_context.rs
  - crates/x4-bridge/tests/ship_contract.rs
  - tests/carrier-b-local.ps1
  - extensions/live_galaxy/tests/carrier_b_local.lua
autonomous: true
requirements: [OBS-06, VAL-01, VAL-06, VER-LG-003, VER-LG-012]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 5
  confidence: low
must_haves:
  truths:
    - A single ordinary-faction ship-core capture traverses the real Lua loader, owned DLL, named pipe, receiver validation, SQLite publication and independent readback.
    - Source count/fill evidence remains weaker than authoritative complete-set evidence throughout that path.
    - Known-retryable transport-local failures within the same qualified connection/source boundary retry exact immutable bytes without another X4 getter call.
    - Reconnect, load/reload, source-boundary uncertainty or stale connection generation discards incomplete capture and pending source work, reconciles durable outcomes by exact identity without duplicate publication, and requires a fresh qualifying baseline and collection under the new boundary.
    - Every admitted core record has validated identity, owner, type/class and location.
  artifacts:
    - path: crates/observation-domain/src/ship_core.rs
      provides: Typed ship core and lossless source identity
    - path: extensions/live_galaxy/lua/live_galaxy_observation.lua
      provides: Resumable canonical faction-ship source path
    - path: crates/x4-bridge/tests/ship_contract.rs
      provides: Independent durable readback of ship revisions
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_scheduler.lua
      to: crates/x4-carrier-native/src/lua_producer_operations.rs
      via: Pump/control before bounded typed fact admission
    - from: crates/x4-carrier-native/src/producer_message.rs
      to: crates/x4-bridge/src/receiver_context.rs
      via: Exact section identity, certificate and receiver-owned admission context
  prohibitions:
    - statement: Ship collection must not grant foreign information to a recipient or create a Faction Mind, including XEN or KHK minds.
      status: unverified
      verification: backstop
      flagged: true
    - statement: Conformance work must not mutate X4, operate the game on the user's behalf, or access save files.
      status: unverified
      verification: backstop
      flagged: true
    - statement: Missing applicable data or an unperformed X4 run must not be presented as successful phase acceptance.
      status: unverified
      verification: backstop
      flagged: true
---

<objective>
Build the production-quality ship-core tracer per D-01, D-05, D-10, D-11 and D-13 through the existing Carrier B path. This is ONE plan-level vertical slice: its five ordered tasks have testable intermediate contracts, but only its final integration check earns the tracer claim. Heavy detail completeness and observed-in-X4 acceptance belong to the dependent plans.

Sizing exception: the orchestrator explicitly selected five bounded tasks for this first tracer after inspection found independent clock-only gates in Lua, native parsing, producer assembly, feedback and bridge demand. Each task names at most five files. Do not call any intermediate task the completed tracer. No phase scope is removed.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-RESEARCH.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-PATTERNS.md
@docs/ARCHITECTURE.md
@docs/architecture-decisions.md
@docs/architecture-verification.md
</context>

## Execution contract for the plan set

Layers are `rust-domain` and `x4-runtime`. Discovery is satisfied by current research and the tracked code seams; do not add dependencies. Keep Rust production files at or below 200 physical lines. A cohesive helper split inside the owned crate is allowed by AGENTS.md; record exact added files and reason in the SUMMARY, and preserve task responsibility.

Each behavior task follows RED, GREEN, REFACTOR. RED must be an assertion failure for the intended behavior, never a compilation/import failure or zero tests. Retain command, exit, failing assertion, expected and actual results in the normal GSD RED evidence record; validate with `check tdd-red-evidence` before GREEN. Existing compatibility tests remain tests of the old clock profile, not a mandate to encode ship fields as a clock number. Run focused checks while iterating; the final full regression occurs only after review convergence in Plan 08.

Keep the immutable evidence boundary: game declarations are from `x4-9.00-steam-23660954`, root `blake3:8d30413b740369e8572d6a7647366be338421698596eee84d62fec0415685c79`; observed semantics are from `x4-probe-faction-ship-runtime-686318dd-v1`, root `blake3:01ea3b0d39afee3541a1c7e87ce2c1c8fc011ce688584966952c0d20e7a90c82`. Preserve source kind, entity and cursor-complete provenance from RESEARCH. For an exact declaration absent from its summary, retrieve the named registered entity, exhaust inspection pages, and stop only the dependent work with the shared knowledge-gap handoff if unresolved. Do not guess signatures, inspect saves, or substitute an installed runtime mirror.

The existing native crate is the already-approved isolated unsafe boundary; add no unsafe code outside it and preserve synchronous copies before background work. Keep envelope/control/ABI identities exact. The ship payload is a new internal profile carried by the existing envelope, not a rewrite of persisted envelopes; retain old-profile readback and explicitly reject incompatible current profiles. If implementation instead requires a persisted-format migration, public contract break or a new native ABI, stop at a decision checkpoint with the actual migration/consumer impact before that change.

## Artifacts this phase produces

New: `crates/observation-domain/src/ship_core.rs` (`ShipCoreRecord`, `ShipOwner`, `ShipLocation`, validated lossless identifier construction); `crates/observation-domain/src/ship_field.rs` (`FieldApplicability`, `FieldOutcome`, source evidence reference); `crates/observation-domain/src/ship_group.rs` (`ShipGroupDescriptor`, exact core revision/member identities, grouping policy version); `crates/observation-domain/tests/ship_contract.rs` and `crates/x4-bridge/tests/ship_contract.rs`.

Planning-input status: `05.5-PATTERNS.md` and this phase's PLAN files are newly authored artifacts in the current planning session, not pre-existing tracked implementation. All SUMMARY and execution-evidence references are new outputs of their named producer plans. Existing implementation/configuration/test paths were checked with `git ls-files -- <path>`; no installation mirror is an implementation target.

Extended existing interfaces: typed ship fact admission beside the clock fixture, `Producer` section identity/ordinal/counters and bounded batch state, begin/record/finish Lua input validation, bounded producer status with the current collection selection, resumable observation transitions, and receiver ship-profile validation. New local harness scenario: `heavy-ship-core`. No game command, mind selector or recipient-policy flag is added. New paths above are artifacts to create; all antecedent implementation/configuration/test paths are tracked, with current-session planning inputs distinguished above.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Define the validated core and dependent-field contracts used by the tracer</name>
  <reversibility rating="costly">Shared typed source values affect native, Lua and receiver consumers; the existing persisted envelope remains readable.</reversibility>
  <precondition>The Phase 05.4 production path and prepared Rust/Lua tools exist; the owner has approved this checked phase plan before execution.</precondition>
  <files>crates/observation-domain/src/lib.rs, crates/observation-domain/src/ship_core.rs, crates/observation-domain/src/ship_field.rs, crates/observation-domain/src/ship_group.rs, crates/observation-domain/tests/ship_contract.rs</files>
  <read_first>Read section_state.rs, observation.rs, section.rs in crates/observation-domain/src; the three phase sources above; the common code/tests skills and all their applicable Rust and X4 references.</read_first>
  <behavior>Core constructors reject missing identity, owner, class/type or location. Identity round trips above Lua's exact-number range without numeric coercion. Not-applicable requires source evidence; zero, empty, absent, unknown, inaccessible, unsupported and stale stay distinguishable. Equal keys produce deterministic group membership and duplicate identity is rejected.</behavior>
  <action>Per D-01, D-05, D-11 and D-13, add dependency-free typed records and explicit field applicability/result axes beside existing SectionState axes. Use source-scoped lossless string identifiers with checked canonical conversion; never route UniverseID through floating point. A core has identity, canonical owner, class/type and location with source identity and capture evidence. A group freezes its member identities, exact accepted core revision and policy version; sort by canonical identity with a documented ordinal comparison and reject duplicate IDs. Preserve separate field freshness and section coverage. Define only source-backed core semantics now; raw units still unknown for detail fields are resolved in Plan 02 before Plan 03 supplies their types. Export only required constructors/accessors. Tests start RED against the missing/rejected behavior and finish GREEN; keep test fixture values explicitly local, not measured production limits.</action>
  <acceptance_criteria>Constructors expose every D-11 distinction without boolean combinations; large identifiers remain byte-exact; group order is independent of input order; no schema default turns missing data into a value.</acceptance_criteria>
  <verify><automated>cargo test --locked -p observation-domain --test ship_contract</automated><fails_when>Nonzero exit, zero tests run, or any core/field/group assertion fails.</fails_when></verify>
  <done>Typed core and group contracts are executable and ready for the native and receiver consumers; no end-to-end success is claimed yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stream ship-core records through the owned producer state machine</name>
  <reversibility rating="costly">Replacing clock-only producer branches affects cross-language collection and feedback, while retaining the existing transport contract.</reversibility>
  <files>crates/x4-carrier-native/src/producer.rs, crates/x4-carrier-native/src/producer_collection.rs, crates/x4-carrier-native/src/producer_message.rs, crates/x4-carrier-native/src/producer_feedback.rs, crates/x4-carrier-native/src/producer_types.rs</files>
  <read_first>Read all five files, crates/x4-carrier-native/src/producer_recovery.rs and crates/x4-carrier-native/tests/producer_contract.rs; use the typed artifacts from Task 1 and the native safety contract in crates/x4-carrier-native/Cargo.toml.</read_first>
  <behavior>A ship-core section emits ordered complete-record batches and one content-bound completion. A second record continues the same bounded section. Known-retryable capacity refusal within the same qualified connection/source boundary retains exact bytes without getters. Reconnect, load/reload, source uncertainty or stale connection generation discards incomplete capture and pending source work; old bytes and callbacks cannot resume collection. Durable committed outcomes reconcile by exact identity without another publication; absent or unresolved outcomes cannot authorize old-work replay. Fresh qualifying baseline and fresh collection are required under the new boundary. Conflicting feedback cannot release a slot. Unsupported profile and oversize record reject before publication.</behavior>
  <action>Per D-01, D-05 and D-13, generalize the existing clock-only section selection, fact slot and three preassembled-message assumptions into one bounded builder plus one immutable pending message. Keep the clock API as an explicit existing profile; add typed ship-core admission using Task 1 domain records. Bind section key, expected count, source evidence, exact revision and ordinal to the active attempt instead of constant clock-key strings. Build canonical content and certificates only in Rust; account for each complete record, envelope UTF-8 bytes, work and capture age before accepting it. Incrementally maintain manifest/content evidence without retaining all prepared messages. Pump a sealed batch while collection waits, then reopen only bounded builder capacity after its exact received disposition. Completion releases no authority before committed. Per inherited Phase 05.4 D-06, preserve exact immutable retry only for a transport-local outcome proven retryable within the same qualified connection/source boundary. At reconnect, game load/reload, source-boundary uncertainty or stale connection generation, fence old callbacks, discard incomplete capture and pending source work, and reconcile any durable receipt/committed outcome by exact identity and digest through the existing recovery path. Retain only bounded outcome-reconciliation identity; do not retain old capture bytes for resumption under a new boundary. An absent or unresolved receipt does not authorize replay of discarded work. Require compatibility confirmation, a fresh qualifying baseline and fresh collection with new attempt identity before resuming; prior accepted revisions stay history-only across source uncertainty. Never repeat publication of a committed receipt. Add focused state-machine assertions within owned modules and preserve the existing producer contract. Refactor cohesive helpers only as needed for the 200-line rule, recording the mechanical split.</action>
  <acceptance_criteria>Zero records, one record and multiple batches have explicit behavior; a source-unproven empty result cannot certify known-empty; no collection or certificate contains split semantic records; memory owners and all counters are finite and checked. Tests distinguish same-boundary known-retryable byte retention from reconnect/load/reload/uncertainty/stale-generation discard, require fresh baseline and collection, and prove committed-receipt reconciliation never republishes.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-carrier-native --lib</automated><fails_when>Nonzero exit, zero tests, or a producer stream/retry-scope/discard/fresh-baseline assertion fails.</fails_when><automated>cargo test --locked -p x4-carrier-native --test producer_contract</automated><fails_when>Nonzero exit or any immutable producer/revision contract regresses.</fails_when></verify>
  <done>The native producer accepts typed core records and emits bounded certified sections through its existing feedback lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Carry structured core facts and bounded collection selection across the Lua ABI</name>
  <reversibility rating="costly">The exact typed table profile and bounded status shape must be updated together on both sides of the existing ABI.</reversibility>
  <files>crates/x4-carrier-native/src/lua_input.rs, crates/x4-carrier-native/src/lua_producer_operations.rs, crates/x4-carrier-native/src/lua_progress.rs, crates/x4-carrier-native/src/lua_producer_operations_tests.rs, extensions/live_galaxy/lua/live_galaxy_carrier.lua</files>
  <read_first>Read these files and lua_table.rs; preserve the ten-operation registration, same-process generation fencing and copied-memory ownership.</read_first>
  <behavior>Exact core table fields are copied before returning to Lua; mutation afterward cannot change pending bytes. Extra/missing keys, nonfinite numbers and precision-losing IDs reject. Progress reports bounded active selection and capacity; stale controls never authorize a getter.</behavior>
  <action>Per D-01, D-11 and D-13, parse a discriminated ship-core table into the typed domain record, with exact allowlisted fields and configured string/record bounds. Keep envelope assembly and digests outside Lua. Expose only bounded collection-intent metadata needed to select this one source/faction/profile and advance one continuation; do not transmit entity inventories or per-getter RPCs. Update the existing carrier wrapper's exact result-shape validation alongside native status production. Copy all accepted strings/values synchronously. Preserve existing clock-profile rejection tests and add ship success/malformed/copy-isolation cases to the actual ABI harness. Make a schema/profile incompatibility restart-required before collection rather than permissively accepting mismatched representations.</action>
  <acceptance_criteria>The normal registered DLL operations accept validated ship fields; ABI tests prove strict shapes, synchronous ownership and fail-closed incompatible profiles. No background call reaches Lua or X4.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-carrier-native --test abi_contract --test producer_contract --test lifecycle_contract</automated><fails_when>Nonzero exit or any ABI, producer or lifecycle case fails.</fails_when><automated>cargo test --locked -p x4-carrier-native --lib</automated><fails_when>Nonzero exit or the added structured-core/copy-isolation cases fail or are not discovered.</fails_when></verify>
  <done>Typed ship-core input and the existing bounded feedback path cross the actual native ABI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Collect one ordinary faction's core through the existing callback</name>
  <reversibility rating="reversible">The collector is an internal source adapter behind the current scheduler and loader.</reversibility>
  <files>extensions/live_galaxy/lua/live_galaxy_observation.lua, extensions/live_galaxy/lua/live_galaxy_scheduler.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/tests/carrier_b_contract.lua</files>
  <read_first>Read the four files and live_galaxy_component_discovery.lua as a partial count/fill analog; use the exact registered source entities and their signatures, not station constants.</read_first>
  <behavior>Fake X4 records exact census/count/allocation/fill/core call order. Churn discards the entire attempt; duplicate, invalid and transferred identities never publish a truncated core. Pump/control precede every native step. Reentry, frozen clock, occupied producer and retry perform no fresh getter.</behavior>
  <action>Per D-01, D-05, D-10 and D-13, replace one-shot capture with explicit persistent count, checked allocation, fill, identity validation, core-read, record-ready and completion transitions. Use C.GetAllFactions plus C.GetNumAllFactionShips/C.GetAllFactionShips, then the documented GetComponentData fields. GetContainedObjectsByOwner is reconciliation evidence only. Require explicit single-faction selection for this proof; reject missing selection rather than choosing a lore-based faction. The real adapter and fake adapter execute the same collector. Use finite supplied native-byte/work/attempt/time ceilings; no production number is inferred from old clock limits. At most one heavy native step per callback, with no clock-spin loop. A count/fill mismatch, duplicate, changed owner or work-limit breach abandons the attempt atomically and yields/recounts only within the same finite retry budget. Preserve evidence as observed_count_fill_only unless stronger source evidence exists. Latch load boundaries through backpressure and clear only on an accepted fresh source transition. Retain exactly one callback registration and pump before source work; capture/normalize only with downstream reservation. Store source fields, never Lua-generated wire JSON, digests or certificates.</action>
  <acceptance_criteria>The fake native call trace proves resumability, deterministic identity order, pre-allocation bounds and zero source calls during transport-only retry. Changed ownership/location cannot silently enter an already-frozen core. A full heavy fixture completes across callbacks without exceeding supplied limits.</acceptance_criteria>
  <verify><automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/carrier_b_contract.lua</automated><fails_when>Nonzero exit, zero discovered tests, or the ship count/fill/pump-order cases fail.</fails_when></verify>
  <done>The production Lua entrypoint can collect one explicitly selected faction with bounded resumable work; real X4 execution remains pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Complete the tracer with strict receiver admission and independent readback</name>
  <reversibility rating="costly">The bridge consumes a new internal source profile while preserving historical envelope and receipt readers.</reversibility>
  <files>crates/x4-bridge/src/production_runtime.rs, crates/x4-bridge/src/receiver_context.rs, crates/x4-bridge/tests/ship_contract.rs, tests/carrier-b-local.ps1, extensions/live_galaxy/tests/carrier_b_local.lua</files>
  <read_first>Read production.rs, production_admission.rs, production_runtime_idle.rs, carrier_b_publication.rs and carrier_b_recovery.rs; read the existing local harness before adding its scenario.</read_first>
  <behavior>Real Lua-to-DLL-to-pipe input publishes validated core only after completion. Dropping all in-memory state and reopening SQLite yields exact ordered core records, digest and receipt. Replaying completion does not add a revision; a malformed core preserves the prior current pointer.</behavior>
  <action>Per D-01, D-10 and D-13, replace clock-specific demand/revision-floor selection with the active approved single ship-core profile. Derive receiver authority from current accepted state and exact supported profile, not sender claims. Validate all core fields before publication through ProductionObservationSession; preserve weak coverage, atomic transaction and current/history distinction. Add heavy-ship-core to the existing local harness using production require paths, the real owned DLL and pipe, and isolated real SQLite. The fake X4 adapter supplies a nontrivial multi-batch ordinary-faction census; it is not labelled observed-in-X4. Require at least two commits on one connection across the demand interval, a new producer process after restart, exact ascending revision history, independent earlier/current readback, exact replay and a rejected malformed replacement. Retain bounded getter and stage counters. No recipient visibility or mind roster change is authorized.</action>
  <acceptance_criteria>The complete tracer passes through actual producer/consumer code and independently reopens its durable state. It does not use a fake DLL or hand-built receiver envelope to claim cross-language success.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-bridge --test ship_contract</automated><fails_when>Nonzero exit, zero tests, or core publication/readback/replay checks fail.</fails_when><automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-core</automated><fails_when>Nonzero exit, missing heavy-ship-core result, missing independent readback, or any revision/getter/bound assertion fails.</fails_when></verify>
  <done>Plan-level core tracer is verified locally across Lua, DLL, transport, Rust and durable recovery; heavy field/runtime acceptance is still pending.</done>
</task>
</tasks>

<verification>
The final task is the plan-level end-to-end gate. Preserve prior proven commands for overlapping seams. No install is needed. Numeric fixture bounds protect local tests only and carry no game-admission authority. Plan 02 owns the first written human-operated source/calibration checkpoint; no agent launches X4.
</verification>

<success_criteria>
All five intermediate contracts pass and heavy-ship-core proves the real local data path. The source/transfer distinction survives durable readback. Local-only evidence is explicitly labelled. All three descriptor-less prohibition backstops remain flagged for Plan 08 judgment; automated tests alone do not silently resolve them.
</success_criteria>

<output>Create `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-01-SUMMARY.md` with RED/GREEN evidence, exact new helper paths, tracer result and pending X4 scope.</output>
