---
phase: 05.5-heavy-faction-ship-conformance
plan: 06
type: execute
wave: 6
depends_on: [05.5-05]
files_modified:
  - docs/development/heavy-ship-probe.md
  - tests/carrier-b-local.ps1
  - tests/heavy-ship-evidence.ps1
  - tests/fixtures/heavy-ship-calibration.json
  - tests/x4-disposable/05.5-heavy-ship-evidence.md
  - config/carrier-b-limits.json
autonomous: false
requirements: [OBS-06, VAL-01, VAL-02, VAL-06, VER-LG-003, VER-LG-011, VER-LG-012]
user_setup:
  - service: Disposable X4 normal-speed and SETA runs
    why: The owner performs every game action and approves measured numeric acceptance limits.
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Numerical limits and normal-speed/approximately 6.2x SETA acceptance thresholds come from measurements and explicit owner approval.
    - Gate A demonstrates all applicable required fields for one deliberately heavy ordinary eligible faction before all-faction expansion.
    - Source, scheduler, transport, receiver, publication, recovery and freshness each have independent bounded evidence.
  artifacts:
    - path: tests/fixtures/heavy-ship-calibration.json
      provides: Sanitized measurements, selected limits, units and approval identity
    - path: tests/x4-disposable/05.5-heavy-ship-evidence.md
      provides: Owner-operated Gate A evidence and later Gate B ledger
    - path: config/carrier-b-limits.json
      provides: Approved measured production profile
  key_links:
    - from: tests/fixtures/heavy-ship-calibration.json
      to: config/carrier-b-limits.json
      via: Explicit owner-approved measured values, never guessed defaults
    - from: tests/x4-disposable/05.5-heavy-ship-evidence.md
      to: .planning/phases/05.5-heavy-faction-ship-conformance/05.5-07-PLAN.md
      via: Blocking Gate A acceptance prerequisite
---

<objective>
Perform D-10 Gate A and D-15/D-16 numerical calibration with the owner. The single deliberately heavy ordinary faction must pass before the workload expands. This plan contains real pauses; runtime evidence is pending until the owner supplies it.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-RESEARCH.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-05-SUMMARY.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-SOURCE-EVIDENCE.md
@docs/development/heavy-ship-probe.md
</context>

## Artifacts this phase produces

New paths: `tests/fixtures/heavy-ship-calibration.json`, `tests/x4-disposable/05.5-heavy-ship-evidence.md`. Plan 02 created the probe and evidence checker. New evidence fields: `measurement_kind`, `run_id`, `package_digest`, `profile_identity`, `heavy_faction_id`, selection rationale, callback latency distribution, indivisible native-call maximum, real-time token rate/burst/debt, collection/serialization/transport/staging/commit timings, produced/staged work rate, total backlog high-water mark, group ages/capture skew, rejection/degradation/retry counts, retention/pin/receipt/diagnostic high-water marks, normal/SETA duration and acceptance thresholds, `owner_approval`, `gate_a`, `gate_b`, and blocking reasons. The harness adds a `heavy-ship-calibration` scenario. Existing profile keys from Plan 05 receive measured values only after approval. Gate B remains pending.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a bounded calibration matrix and written Gate A procedure</name>
  <files>docs/development/heavy-ship-probe.md, tests/carrier-b-local.ps1, tests/heavy-ship-evidence.ps1</files>
  <read_first>Read Plan 05 field-to-consumer map, Plan 02 source probe findings, all RESEARCH numerical unknowns and its stability inequalities, and the X4 test standards.</read_first>
  <behavior>RED: self-tests demonstrate rejected unapproved/missing numeric values, unstable backlog and fabricated runtime claims; GREEN adds the checker rules; REFACTOR preserves all negative assertions. Calibration metrics distinguish Lua normalization/copy/allocation/GC, native encoding, pipe quota/whole-message overhead and raw-to-decoded candidate expansion. Lifecycle cooldown/revalidation is measured only for source-proven signals. Runtime responsiveness and an approved no-Live-Galaxy baseline comparison are required for Gate A as well as Gate B.</behavior>
  <action>Extend the existing probe rather than creating a second runbook. Add local synthetic throughput/size sweeps and a separately labelled owner-run native-source calibration. Add heavy-ship-calibration to the local harness, exercising the real Lua/native/pipe/receiver/persistence path with many ships and detail groups, delayed receiver, full pending buffers, count/fill churn, retry exhaustion, reconnect and history readback. Record measurable production versus staging rates, bounded peak allocation/backlog, p99 end-to-end age and per-stage work; local fake source timing never becomes X4 timing. Specify how owner runtime samples supply callback/native-call timings at normal speed and approximately 6.2x SETA. Include proposed measurement duration/sample sufficiency criteria for owner approval, not invented universal acceptance values. The numeric worksheet must cover token rate/refill, burst, debt, heavy threshold/permits, max step/native allocation/call duration, continuation/record/group/batch/message/candidate/aggregate size/work, source attempts/cooldown/age, transport retry/reconnect/timeouts, core/detail freshness/skew/stale blocking, retention/pins/receipts/diagnostics, normal and SETA acceptance durations, latency and recovery thresholds. Existing safe clock or synthetic values may bound local tests only. Before each owner probe require an explicitly approved finite runtime envelope and abort path. Add evidence checker rules for missing units, unapproved limits, rate/backlog instability, missing required fields and success without a recorded runtime run. Do not weaken applicability or source strength to fit throughput.</action>
  <reversibility rating="reversible">Calibration scripts and the written procedure are local and do not deploy or control the game.</reversibility>
  <acceptance_criteria>The worksheet covers every listed numeric decision; the harness emits bounded synthetic metrics; the procedure specifies owner-only actions and clear abort/degradation outcomes; absent evidence fails validation.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-calibration</automated><fails_when>Exit is nonzero, queues exceed configured test bounds, publication/readback differs, or synthetic timings are labelled in-game.</fails_when><automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -SelfTest</automated><fails_when>Exit is nonzero or the checker accepts fabricated runtime success, missing limits, unknown disposition or incomplete required fields.</fails_when></verify>
  <done>The owner receives an executable bounded measurement procedure and a complete decision worksheet.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Owner supplies calibration runs and approves the numerical contract</name>
  <decision>After performing the approved bounded calibration steps, accept the complete measured numerical contract for normal-speed and approximately 6.2x SETA acceptance, or request changed limits/further measurements.</decision>
  <options><option id="approve-measured-contract"><name>Approve the measured contract</name><pros>Unlocks Gate A with explicit finite limits and traceable acceptance thresholds.</pros><cons>Acceptance remains tied to the measured build, workload and profile; changed conditions require revalidation.</cons></option><option id="revise-or-measure"><name>Revise limits or collect more evidence</name><pros>Resolves uncertain call costs, stability or headroom before acceptance.</pros><cons>Gate A and full-set expansion remain blocked until a complete numerical contract is explicitly approved.</cons></option></options>
  <resume-signal>Return the calibration evidence first; after reviewing the resulting exact limit table, reply in ordinary chat approving its stated profile identity or naming requested changes. These are two explicit pauses, not an automatic continuation.</resume-signal>
  <files>tests/fixtures/heavy-ship-calibration.json, tests/x4-disposable/05.5-heavy-ship-evidence.md, docs/development/heavy-ship-probe.md</files>
  <read_first>Read the written calibration procedure, local results, source evidence and measured heavy-faction candidate rationale. Both evidence files are new and initially pending.</read_first>
  <action>STOP for the owner. Present exact finite proposed probe limits, known versus unknown costs and the required game actions. The owner approves/replaces the probe envelope, performs calibration on the disposable vanilla+DLC+Live Galaxy stack at normal speed and approximately 6.2x SETA, and returns observations. The agent may analyze supplied data but cannot perform the actions. Then present a complete measured limit/threshold table with units, sample distribution, derivation, headroom and failure behavior, and STOP again for explicit approval or changes; returning data alone is not approval. Include the chosen heavy ordinary faction's measured count and detail/byte workload and explain why it is deliberately heavy. Evaluate sup_t(produced minus staged) against bounded total backlog, staging rate at least production rate or bounded upstream throttling before overflow, and p99(collection + batching + transport + staging + commit) below approved freshness/stale deadlines. Treat an indivisible call exceeding the safe callback budget as a blocking finding; reducing faction/field coverage or relabelling required data unsupported is not an option. Record approval identity and exact numbers only after the owner accepts them. If evidence is inadequate, remain paused with specific missing measurements; never infer assent from silence.</action>
  <reversibility rating="costly">Measured limits govern runtime load and freshness; changes are reversible configuration edits but invalidate affected prior acceptance evidence.</reversibility>
  <acceptance_criteria>Every numerical worksheet row has observed/local provenance, units, a finite selected value and explicit owner approval; both speed regimes have calibration evidence; unstable or unbounded work blocks progress.</acceptance_criteria>
  <verify><human-check>Owner confirms the exact numerical contract after seeing measurements. Validate the supplied runs against the written procedure. Missing measurements or approval keep this checkpoint unresolved.</human-check></verify>
  <done>The numeric profile and Gate A normal/SETA success thresholds are explicitly approved, or execution remains stopped.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Apply approved values and obtain heavy ordinary-faction Gate A evidence</name>
  <instructions>After local packaging of the approved profile, perform the written Gate A procedure yourself on the selected heavy ordinary faction at normal speed and sustained approximately 6.2x SETA. Return run identities and every requested field, timing, recovery and responsiveness observation.</instructions>
  <verification>The agent checks supplied results against the exact approved profile and thresholds, independently reads retained earlier/current revisions, and accepts Gate A only with complete evidence.</verification>
  <resume-signal>Reply in ordinary chat with the completed Gate A evidence or the exact failed/pending rows. An acknowledgment without evidence does not unlock full-set expansion.</resume-signal>
  <files>config/carrier-b-limits.json, tests/fixtures/heavy-ship-calibration.json, tests/x4-disposable/05.5-heavy-ship-evidence.md</files>
  <read_first>Read the owner's exact approved profile and written Gate A steps. Read local source/transport/recovery results; do not use preliminary calibration as Gate A success.</read_first>
  <precondition>The owner has explicitly approved every numerical limit and normal/SETA threshold in Task 2.</precondition>
  <action>Materialize only the approved values in the canonical limits file and validate/repackage locally. Present the resulting package digest, profile identity and written Gate A procedure, then STOP for owner-run X4 evidence. The owner exercises the selected heavy ordinary eligible faction with all applicable core/cargo/crew/installed-loadout/ammunition/drone fields, sustained normal speed and sustained approximately 6.2x SETA, delayed/reconnected transport, bridge restart, lost completion response/reconciliation where the harness supports safe fault injection, load/reload and observed ownership/location changes in the disposable workflow. Distinguish runtime-observed rows from locally fault-injected rows; never fabricate an unobservable live case. Agent reads supplied evidence and independently reads retained earlier/current Rust revisions using existing readback facilities; no save access. Record selection membership/source strength, group intervals/applicability/outcomes, callback cost, allocation/work bounds, transport/receiver rates, recovery identity and freshness separately. Set gate_a accepted only if all source-applicable fields, calibrated bounds and required layered scenarios pass. Otherwise record precise blockers and rerun affected tests after in-scope fixes; a source/architecture conflict returns to the owner. Leave gate_b pending. Full-set expansion is prohibited until gate_a is accepted.</action>
  <reversibility rating="costly">Changing the active profile and accepting runtime evidence is recoverable, but changed source/policy/package identity requires affected evidence to be repeated.</reversibility>
  <acceptance_criteria>The committed profile exactly matches approval; Gate A has a real heavy ordinary-faction run in both speed regimes with bounded independent-stage evidence and earlier/current readback; unknown applicable data or missing runtime results block acceptance.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File tools/package-live-galaxy.ps1 -SelfTest -LimitsFile config/carrier-b-limits.json</automated><fails_when>Exit is nonzero or the approved profile differs between package and receiver.</fails_when><automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -EvidenceFile tests/fixtures/heavy-ship-calibration.json</automated><fails_when>Exit is nonzero, approval/measurement identity is missing, or any required bound is unapproved or violated.</fails_when><human-check>The owner supplies the Gate A runs; the agent checks the written evidence ledger and independent readback. Automation validates artifacts, not the occurrence of gameplay. Gate A stays pending until the runtime evidence actually passes.</human-check></verify>
  <done>Gate A is accepted with evidence, or execution is paused before full-set expansion with the exact blocker.</done>
</task>

<verification>
No numerical constant is selected because it is convenient. Audit all worksheet rows against actual enforcement, local measurement, runtime samples and approval. Mark runtime gaps as gaps; the source probe, local harness and calibration alone do not constitute Gate A.
</verification>

<success_criteria>
One deliberately heavy ordinary faction satisfies the whole field contract and measured bounds at normal speed and approximately 6.2x SETA, including recovery and honest freshness. This is the sole prerequisite that unlocks full mandatory-faction workload expansion.
</success_criteria>

<output>Create `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-06-SUMMARY.md` only after reporting the real checkpoint outcome. If pending, retain the durable evidence locator and exact resume condition instead of claiming Gate A passed.</output>
