---
phase: 05.5-heavy-faction-ship-conformance
plan: 10
type: execute
status: planned
wave: 3
depends_on: [05.5-09]
files_modified:
  - config/heavy-ship-experiment.json
  - config/heavy-ship-limits.json
  - config/faction-source-inventory.json
  - docs/development/heavy-ship-probe.md
  - tests/fixtures/heavy-ship-source-contract.json
  - tests/heavy-ship-evidence.ps1
  - tests/x4-disposable/05.5-heavy-ship-procedure.md
  - tests/x4-disposable/05.5-heavy-ship-evidence.md
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/src/faction_observation.rs
  - crates/observation-domain/tests/faction_observation_contract.rs
  - crates/x4-bridge/src/production_selection.rs
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/receiver_ship.rs
  - crates/x4-bridge/src/receiver_ship_detail.rs
  - crates/x4-bridge/tests/ship_contract.rs
  - crates/x4-bridge/tests/ship_recovery.rs
  - crates/x4-bridge/examples/carrier_b_ship_local/main.rs
  - crates/x4-bridge/examples/carrier_b_ship_local/peer.rs
  - crates/x4-bridge/examples/carrier_b_ship_local/readback.rs
  - extensions/live_galaxy/lua/live_galaxy_factions.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_collection.lua
  - extensions/live_galaxy/tests/faction_observation_contract.lua
  - extensions/live_galaxy/tests/carrier_b_ship_local.lua
  - extensions/live_galaxy/tests/run_contracts.ps1
  - tests/carrier-b-local.ps1
autonomous: false
requirements: [OBS-06, VAL-01, VAL-02, VAL-06, VER-LG-003, VER-LG-011, VER-LG-012]
user_setup:
  - service: owner-operated X4 disposable stand
    why: Only the owner may launch or operate X4 and explicitly approve experiment risk and measured acceptance limits.
    dashboard_config:
      - task: Use the prepared package in a disposable vanilla plus DLC plus Live Galaxy stand; return bounded evidence at each explicit pause.
        location: Owner-controlled X4 installation; no saves are read by agents.
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - 'D-07: XEN and KHK are mandatory observation subjects and receive no Faction Mind through this phase.'
    - 'D-08: Player and mod-added factions are outside mandatory coverage; service and temporary exclusions need evidence, unknown eligibility blocks closure, and zero ships is not exclusion or authoritative empty.'
    - Gate A proves the measured deliberately heavy ordinary faction before any full-set runtime gate is activated.
    - Owner-approved numerical acceptance follows a finite production calibration and includes throughput, latency, freshness, memory, backlog, recovery and demonstrable headroom.
    - Every dynamically discovered faction has a provenance-bearing included, excluded or unknown disposition, independent of Mind participation.
    - Full-set local production tests include small and pirate factions, XEN/KHK and zero-ship cases without granting recipients any foreign information.
  artifacts:
    - path: config/heavy-ship-limits.json
      provides: Evidence-derived owner-approved acceptance profile
    - path: crates/observation-domain/src/faction_observation.rs
      provides: Dynamic observation eligibility distinct from strategic participation
    - path: tests/x4-disposable/05.5-heavy-ship-evidence.md
      provides: Sanitized calibration and ordered Gate A evidence
  key_links:
    - from: config/heavy-ship-limits.json
      to: docs/development/heavy-ship-probe.md
      via: Measured proposal, explicit owner approval and exact runtime consumers
    - from: extensions/live_galaxy/lua/live_galaxy_factions.lua
      to: crates/x4-bridge/src/production_selection.rs
      via: Dynamic census identity and evidence-backed disposition, never a Mind allowlist
---

<objective>
Measure the production ordinary-faction slice, prove Gate A, then expand that same
path to the complete dynamic observation set locally. Honor D-01–D-16 without
running Gate B early or making source uncertainty a hidden scope reduction.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-EXECUTION-REVISION.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-VALIDATION.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-09-SUMMARY.md
@docs/development/heavy-ship-probe.md
@docs/ARCHITECTURE.md
</context>

## Artifacts this phase produces

New `config/heavy-ship-limits.json`, `config/faction-source-inventory.json`,
`crates/observation-domain/src/faction_observation.rs`, its named Rust contract
test, `live_galaxy_factions.lua`, its named Lua contract test and the two disposable
procedure/evidence files are listed in frontmatter. New contracts:
`FactionObservationDisposition` (Included/Excluded/Unknown with reason and source
evidence), `FactionObservationRoster`, dynamic discovery revision and per-ID
origin/independence/disposition fields. New existing-harness scenario:
`heavy-ship-full-set`. Evidence gains calibration measurements, owner approval,
normal/SETA window, baseline, headroom and Gate-A verdict fields. These are planned
additions, not already available APIs. No new dependency or scheduling loop.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Calibrate the production path and prove heavy ordinary-faction Gate A</name>
  <files>config/heavy-ship-experiment.json, config/heavy-ship-limits.json, docs/development/heavy-ship-probe.md, tests/fixtures/heavy-ship-source-contract.json, tests/heavy-ship-evidence.ps1, tests/x4-disposable/05.5-heavy-ship-procedure.md, tests/x4-disposable/05.5-heavy-ship-evidence.md</files>
  <read_first>Read current files in files where present, 09 SUMMARY/package identity/local checks, integrated SOURCE-EVIDENCE, REPLAN runtime risk contract, RESEARCH stage metrics and previous 05.4 disposable procedure/evidence. Use the full project test and X4 skill chain. Read no private captures or saves without the task's explicit evidence authorization.</read_first>
  <precondition>09 is locally complete: actual details use the real owned DLL/pipe/receiver; all profile consumers are tested; the finite nonzero experimental package, exact invocation and stop procedure exist.</precondition>
  <action>Per D-10 and D-14–D-16, this checkpoint is an explicit owner/agent measurement loop, not permission for the agent to operate X4. The agent first presents the exact package/profile identity, finite count/allocation/call/attempt/admission-window limits, small initial cohort, observable stop conditions and the remaining non-preemptible native-call risk. Pause for an explicit owner decision before any game action. No calibration result or safe native duration is required before this first consent.

  After approval the owner performs only the written game actions in a disposable vanilla+DLC+Live Galaxy stand and supplies the identified evidence. The agent runs the external bridge/capture tooling when authorized by those instructions, checks digests and independently reads durable output, then analyzes the result. Measure faction/ship count calls, native allocation/fill, Lua normalization/core/detail getters, native copy/encode, message/pipe delivery, receiver validation/staging, durable commit and independently readable publication. For each stage record call/sample count, elapsed real time, p50/p95/p99/max latency where sample size supports it, records/bytes per second and peak/current allocation or retained bytes; mark insufficient samples, do not invent percentiles. Record delivered callbacks, native duration, debt, queue age/depth, produced versus committed rate, core/detail freshness, UI responsiveness/stalls and unobserved intervals. Keep normal and approximately 6.2x SETA measurements separate; game-time acceleration is not a wall-clock scheduler budget.

  Enlarge the finite cohort only by a revised explicit envelope, preserving stop conditions and profile identity; obtain enough candidates to select a deliberately heavy ordinary faction by observed ship count and detail/record volume, not a convenient name. Count/fill over-cap, changed counts, missing fields and Unknown results are evidence to analyze, not silently truncated success. Confirm each required getter's actual semantics/applicability and every one of the 21 evidence rows against the production run. For uncertain missile/crew/virtual fields preserve raw numeric representation and inference status until measured/source evidence resolves it. An applicable unsupported or missing field blocks acceptance; the agent diagnoses a source-backed alternative in the owning implementation, reruns local tests and repackages before requesting another bounded run. Do not ask the owner to research APIs.

  From those measurements, the agent proposes numerical limits for each actual profile consumer, normal/SETA window lengths, native-call and callback acceptance, maximum memory/backlog/queue age, sustained source/receiver/commit rates, core/detail freshness, recovery time and an explicit positive headroom margin. Show the bottleneck and margin arithmetic against observed maxima/tail latencies and the intended workload; explain sample limitations. Demonstrate headroom with a bounded above-target local load and owner-approved production operating margin, not an arbitrary percentage or average-only throughput. The stability check is bounded produced-minus-consumed work, no growing steady backlog, receiver service at least producer demand, and tail end-to-end latency below the approved freshness/stale deadline. Pause again for the owner's numerical approval; do not derive consent from silence or from the first experimental consent.

  Build the approved profile/package and provide the finite Gate-A procedure. The owner then runs sustained normal and approximately 6.2x SETA windows on the measured heavy ordinary faction, including planned bridge absence/reconnect, response loss/restart and load/reload actions that the package can observe without accessing saves. The agent checks all required field groups, exact dependencies, earlier/current independent durable readback, workload stability, responsiveness, recovery and approved headroom. Record pass only if every criterion is actually met; otherwise repair within accepted scope, reverify locally and repeat the affected window. No full-set runtime acceptance starts until the recorded Gate-A pass. Store only sanitized provenance/metrics/verdicts in Git; retain required raw artifacts durably and verify locator/digests before pause.</action>
  <instructions>Three explicit pauses: owner approves the finite initial experiment; owner returns calibration evidence and approves the agent's measured numerical proposal; owner executes the exact normal/SETA Gate-A package and returns evidence. The agent performs all build, analysis, checker and independent readback work. Missing evidence leaves the checkpoint pending.</instructions>
  <verification>Compare actual supplied run evidence and independently reopened durable data against every approved threshold and required field. Require explicit owner decisions at all three pauses; consistency self-tests alone never verify the runtime run.</verification>
  <resume-signal>Explicit experiment approval, then supplied calibration evidence plus explicit numerical approval, then Gate-A evidence. These are separate decisions, not blanket execution authorization.</resume-signal>
  <acceptance_criteria>Gate A has normal and sustained SETA evidence from the locally verified production collector, measured heavy-faction selection, every applicable field, independent durable readback, bounded backlog/memory, latency/freshness/recovery within explicitly approved limits and demonstrated headroom. The package/profile/run identities and owner decisions match. Unknown or unperformed runtime checks cannot pass.</acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -SelfTest</automated>
    <fails_when>Non-zero exit or negative tests permit fabricated approval, missing measurements or unresolved required outcomes.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-recovery -LimitsFile config/heavy-ship-limits.json</automated>
    <fails_when>Non-zero exit or the approved profile is not enforced by actual consumers and durable recovery assertions.</fails_when>
    <human-check>Owner-only X4 actions and explicit risk/numerical approvals, followed by agent comparison of actual evidence against the accepted procedure. Passing checker self-tests do not certify the owner's runtime run.</human-check>
  </verify>
  <done>Measured owner-approved Gate A passes; full-set production expansion may proceed. A pending or failed owner run cannot produce a completed 10 summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the production collector to the complete dynamic faction set</name>
  <files>config/faction-source-inventory.json, crates/observation-domain/src/lib.rs, crates/observation-domain/src/faction_observation.rs, crates/observation-domain/tests/faction_observation_contract.rs, crates/x4-bridge/src/production_selection.rs, crates/x4-bridge/src/production_runtime.rs, crates/x4-bridge/src/receiver_ship.rs, crates/x4-bridge/src/receiver_ship_detail.rs, crates/x4-bridge/tests/ship_contract.rs, crates/x4-bridge/tests/ship_recovery.rs, crates/x4-bridge/examples/carrier_b_ship_local/main.rs, crates/x4-bridge/examples/carrier_b_ship_local/peer.rs, crates/x4-bridge/examples/carrier_b_ship_local/readback.rs, extensions/live_galaxy/lua/live_galaxy_factions.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/lua/live_galaxy_ship_collection.lua, extensions/live_galaxy/tests/faction_observation_contract.lua, extensions/live_galaxy/tests/carrier_b_ship_local.lua, extensions/live_galaxy/tests/run_contracts.ps1, tests/carrier-b-local.ps1, tests/heavy-ship-evidence.ps1, tests/x4-disposable/05.5-heavy-ship-procedure.md</files>
  <read_first>Read current files in files, the accepted Gate-A evidence/profile and integrated faction declaration/origin source evidence. Read current production_selection.rs/receiver_ship.rs special-faction guards and the domain FieldOutcome/ShipGroupDescriptor contracts. Read D-06–D-09 and the research responsibility map; new faction modules use these existing contracts as analogs.</read_first>
  <precondition>Task 1 Gate A passed on the actual production path with owner-approved numerical limits; the evidence names the exact approved baseline.</precondition>
  <behavior>Every discovered ID retains a disposition; eligible small/pirate/zero-ship/XEN/KHK factions are not filtered by economic or strategic criteria; unknown origin/service/lifetime cannot silently disappear; duplicate/changed census results remain partial; forged cross-faction parents and stale disposition revisions reject; one blocked faction cannot starve healthy groups or hide an incomplete full-set verdict.</behavior>
  <action>Per D-06–D-09 and D-12–D-15, RED-test a mixed dynamic roster through the existing local production chain before implementing it. Use GetNumAllFactions/GetAllFactions(false) with exact count/fill declarations, bounds and immediate pointer copying. Build a provenance-bearing origin inventory from the already reviewed base plus installed Egosoft DLC definition evidence; preserve additions versus patches of existing vanilla IDs. This inventory explains origin, not a fixed discovery allowlist. Intersect it with the actual discovered census and preserve every ID as Included, Excluded or Unknown with reason/source evidence and discovery revision. Include all independent vanilla/DLC factions, small/pirate factions and XEN/KHK. Player and mod-added factions are outside mandatory acceptance but remain explicitly disposed. No territory, station, shipyard, faction size, economic activity or Mind membership prerequisite is permitted. Zero ships is not exclusion or authoritative known-empty. Service/temporary exclusions require the specific first-party evidence and honest inference label; universal lifetime is not established by the source snapshot. Unknown independence/lifetime/origin remains a visible closure blocker until evidence resolves it, not an invented classification.

  Replace the current ordinary-only receiver selection restriction with receiver-owned validated observation dispositions and exact scope binding; do not merely delete the XEN/KHK checks and trust the sender. Extend the existing bridge scheduler's deterministic rotation across faction/core/detail intents with shared aggregate limits, per-faction age/freshness, bounded retry and debt; keep only bounded active working sets. No second scheduler, native background getters or per-faction unbounded collectors. Pin detail membership to the exact faction/core/discovery policy context. Detect membership/ownership/location changes and invalidate affected dependent work without cross-faction carry-forward. Census refresh retains disposition history and source uncertainty without inventing deletion authority.

  Add heavy-ship-full-set to the existing real-DLL/pipe local harness. Exercise at least ordinary, small pirate, XEN, KHK, explicit excluded and unknown IDs; varied ordering, zero/single/large membership, per-faction blocked detail, churn, simultaneous backlog pressure, reconnect and producer restart. Assert more than one core and detail revision per included nonempty faction, independent earlier/current readback, fair progress, exact cross-faction rejection and bounded shared state. Keep unknown cases as negative closure tests. Package the same collector for Gate B, with the approved profile and an exact dynamic faction/field matrix procedure; any required envelope change is proposed to the owner before runtime use. Do not run Gate B here.</action>
  <acceptance_criteria>Dynamic discovered identity is primary; an inventory/allowlist cannot omit an unknown discovered faction. All required faction classes use the same production path and receiver-owned authority. The local actual-chain scenario proves fairness, bounded aggregate state, exact dependency rejection, multiple revisions and restart readback. Gate-B instructions are executable and preserve Gate-A ordering.</acceptance_criteria>
  <verify>
    <automated>cargo test --locked -p observation-domain --test faction_observation_contract</automated>
    <fails_when>Non-zero exit or an unknown/excluded/eligible disposition is silently merged or dropped.</fails_when>
    <automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/faction_observation_contract.lua</automated>
    <fails_when>Non-zero exit or discovered small/pirate/XEN/KHK/zero/unknown cases disappear without evidence.</fails_when>
    <automated>cargo build --locked -p x4-carrier-native</automated>
    <fails_when>Non-zero exit or the owned producer DLL is unavailable.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-full-set -LimitsFile config/heavy-ship-limits.json</automated>
    <fails_when>Non-zero exit, lost/reused cross-faction dependencies, starvation, bound breach or missing independent revision readback.</fails_when>
  </verify>
  <done>Full dynamic-set production collection is locally verified and ready for reviewed owner Gate B, without claiming Gate B has run.</done>
</task>
</tasks>

<threat_model>
High-severity spoofing/elevation at faction-disposition and exact-scope boundaries
is mitigated by receiver-owned classification/dependency validation and forged
cross-faction tests. High-severity denial of service from a large dynamic roster is
mitigated by shared aggregate bounds, fair scheduling, admission debt and the
measured Gate-A profile. Medium-severity evidence tampering is mitigated by exact
source/run/profile identities, explicit approvals, pending-state rejection and
independent readback. No runtime control authority or new package is granted.
</threat_model>

<verification>
Gate A is manual runtime evidence plus independent analysis, not checker self-test
success. Full-set local tests remain fixture-source evidence. Any code fix repeats
affected local tests and repackages before another owner run. Final reviews,
mutation, regression and Gate B remain mandatory in 11.
</verification>

<success_criteria>Gate A passes with measured headroom; full dynamic-set production expansion passes local actual-chain tests and preserves every discovered disposition. No unmeasured full-set runtime success is claimed.</success_criteria>

<output>Create .planning/phases/05.5-heavy-faction-ship-conformance/05.5-10-SUMMARY.md with Gate-A evidence/approvals, exact profile/package identity, full-set local results and pending Gate-B owner action.</output>
