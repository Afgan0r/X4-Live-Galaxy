# Phase 05.5: Heavy Faction Ship Conformance - Research

**Researched:** 2026-09-16
**Domain:** X4 9.00 faction/ship observation, bounded Lua collection, Carrier B transport, Rust publication, and conformance evidence
**Confidence:** MEDIUM — the registered X4 game snapshot and a separate registered runtime-probe snapshot now establish the load-bearing API and lifecycle behavior; faction disposition evidence, exact normalized field schemas, race behavior under churn, and numerical workload limits remain execution-time closure gates

<user_constraints>

## User Constraints (from CONTEXT.md)

The following text is copied verbatim from `05.5-CONTEXT.md`; it is the planning authority for this phase.

### Locked Decisions

#### Required observation content

- **D-01:** Core records contain ship identity, owner, type/class and location.
- **D-02:** Cargo observations contain ware types, quantities per ware, capacity
  and occupied volume.
- **D-03:** Crew observations contain counts by role, capacity and qualification
  distribution. Individual crew records are not required.
- **D-04:** Equipment observations contain actually installed engines, shields,
  weapons and turrets, plus ammunition and drones. Deeper per-component state
  is not required by this phase.
- **D-05:** These are required research targets, not claims that a suitable API
  exists or that any specific source returns authoritative complete results.
  Cargo, crew and loadout retain the accepted deterministic bounded-group
  contract, exact core revision and member dependencies, independent capture
  windows, availability and freshness, and eventual group coverage. No global
  simultaneous snapshot or cross-core carry-forward is selected.

#### Observation eligibility and Faction Mind participation

- **D-06:** Include all dynamically discovered independent vanilla and DLC
  factions, including small and pirate factions. Sector ownership, a shipyard
  and fleet size are not eligibility prerequisites.
- **D-07:** XEN and KHK are mandatory ship-observation subjects. This supersedes
  both the former ADR-LG-022 exclusions and the intermediate discussion answer
  that still excluded KHK. Neither receives a Faction Mind through this phase.
- **D-08:** The player and mod-added factions remain outside mandatory coverage.
  Service and temporary narrative records require an evidenced exclusion;
  unresolved eligibility blocks closure. Finding no ships does not itself
  establish exclusion or an authoritative empty set.
- **D-09:** Record observation-proof eligibility independently of Faction Mind
  participation. Do not force XEN/KHK into `mind_candidate` solely to collect
  them. The exact representation belongs to research and checked planning.
  Classification must preserve each discovered faction and its disposition.
- **D-10:** Keep the ordered proof: one deliberately heavy ordinary eligible
  faction first, then the full admitted observation set including XEN and KHK.
  Research selects the first faction from measured representative workload.

#### Unavailable data and closure

- **D-11:** Source-proven `not_applicable` is acceptable when a characteristic
  does not belong to the object. Zero, empty, absent, stale, inaccessible,
  unknown and unsupported remain distinct states; none may be coerced into
  non-applicability.
- **D-12:** Every applicable field in D-01 through D-04 must be obtained.
  An applicable field that cannot be obtained is an unresolved problem that
  blocks phase closure, including a confirmed limitation of the selected
  source. `unsupported` is diagnostic information, not an acceptance waiver.
  Search for a suitable alternative source; failure to find an API does not
  prove impossibility. Bring concrete residual blockers to the owner through
  the required research/handoff flow rather than silently reducing coverage.
- **D-13:** Preserve the actual strength of source evidence. Transfer completion
  cannot create membership completeness, known-empty semantics or deletion
  authority. Source completeness/identity gaps remain explicit blockers for
  claims that require them. Optional-detail failure never deletes the core
  entity; safe runtime degradation does not mean successful phase completion.

#### Disposable acceptance environment

- **D-14:** Use vanilla X4 plus DLC and Live Galaxy. KUDA AI Tweaks and Add More
  Sectors remain later compatibility obligations, not this phase's closure
  gates. Do not reintroduce Carrier A as a runtime dependency or fallback.
- **D-15:** Require normal-time and sustained SETA evidence for independent
  bounds on collection, feedback, assembly, publication, recovery and freshness.
  The one-getter Phase 05.4 result does not establish heavy-workload safety.
- **D-16:** The user starts and operates X4 under a written disposable
  verification plan. Agents may prepare the run and inspect retained evidence;
  they never control X4 or read/modify saves. Exact durations, workload limits,
  lifecycle scenarios and numerical acceptance thresholds are research-owned
  choices based on measurements, followed by owner approval of checked plans.

#### Inherited architecture

Reuse the Phase 05.3 contracts and Phase 05.4 production path. Lua owns X4
getters, resumable collection and final callback admission. The owned DLL
copies accepted facts and assembles protocol messages; the external bridge
owns global collection policy, receiver validation, publication and recovery.
Background native work never calls X4/Lua or retains their pointers.

Pump before collection, reserve downstream capacity, bound independent and
aggregate resources, retry immutable bytes without repeating getters, and
preserve distinct local handoff, volatile receipt and durable commit outcomes.
Keep finish-then-validate dependencies, fresh qualification across uncertain
source boundaries and history-only prior revisions after such uncertainty.
Required stale facts block dependent decisions. Automatic save resume remains
unsupported. No new transport algorithm or general research platform is chosen.

### the agent's Discretion

Exact APIs, identity/ownership semantics, faction classification evidence,
capability applicability, source consistency, numeric budgets, collection
scheduling, data representation and test decomposition belong to mandatory
research and checked planning. The owner has approved the decisions above,
not a source selection, plan or execution. Return material contradictions to
the owner; ordinary implementation choices preserve these decisions.

### Deferred Ideas (OUT OF SCOPE)

SEED-003 owns the accepted public-alpha passive-observation direction: existing
faction objects supply foreign contacts; detection does not automatically
reveal cargo, crew or full equipment; contact loss retains the last observation
and its age without refreshing hidden positions. Exact contact sources,
disclosure policy and aging remain later design/research. Active scout
deployment and intelligence-network construction remain later work.

This phase does not implement that policy, grant omniscience, add hostile minds,
or widen the active ZYA/ARG mind roster. Supported-mod compatibility, station
remainder, reports and full director operational proof retain their owners.

</user_constraints>

<phase_requirements>

## Phase Requirements

| ID | Description | Research Support |
| --- | --- | --- |
| OBS-06 | “Runtime sectors, assets, capacity, and ownership are discovered from observed state rather than assumed from a fixed vanilla map or job count.” | Use the observed `C.GetAllFactions` inventory and canonical faction-ship count/fill path; retain source strength and unknown classification rather than a fixed faction or ship allow-list. [VERIFIED: `.planning/REQUIREMENTS.md:15`; x4-docs runtime snapshot `x4-probe-faction-ship-runtime-686318dd-v1`] |
| VAL-01 | “X4 integration has layered static, pure Lua where applicable, fake-adapter contract, Rust, and disposable in-game tests with evidence levels reported separately.” | Use the five-level evidence matrix below; never collapse local/fake evidence into observed-in-X4. [VERIFIED: `.planning/REQUIREMENTS.md:71`] |
| VAL-02 | “Disposable normal-speed and SETA runs demonstrate bounded game-side work, bounded bridge backlog, reconnect behavior, and no observable vanilla simulation stall caused by Live Galaxy.” | Use the two ordered workload gates and measure every stage, backlog slope, recovery, and freshness. [VERIFIED: `.planning/REQUIREMENTS.md:72`] |
| VAL-06 | “Exact X4 9.00 observation, identity, scheduling, transport, persistence, and Mail or Logbook semantics used by the implementation are backed by documented or disposable observed evidence.” | Preserve the two-snapshot evidence boundary: declarations/call shapes come from the immutable game snapshot and runtime semantics from the registered probe snapshot; add disposable evidence only for the remaining unknowns. [VERIFIED: `.planning/REQUIREMENTS.md:76`; x4-docs snapshots listed below] |
| VER-LG-003 | Native enumeration cost, identity, and source coverage. | Count/fill enumeration is now observed for player and TEL, but the plan must still measure cost and must not turn successful fill into stronger membership/deletion authority than the source proves. [VERIFIED: `docs/architecture-verification.md:155-201`; x4-docs runtime entity `C.GetNumAllFactionShips`] |
| VER-LG-011 | Aggregate bounds and sustained stream stability. | Derive limits from measured production/staging/commit rates, independent retained-state bounds, and end-to-end p99 freshness. [VERIFIED: `docs/architecture-verification.md:371-431`] |
| VER-LG-012 | Heavy faction ship conformance proof. | Preserve the ordered one-faction then full-faction gates and all required field/disposition coverage. [VERIFIED: `docs/architecture-verification.md:433-478`] |

</phase_requirements>

## Summary

The previous source-selection blocker is closed. The immutable game snapshot now exposes the relevant declarations and call shapes, while the separately registered runtime-probe snapshot demonstrates faction discovery, canonical faction-ship count/fill enumeration, core/cargo/crew/equipment/unit reads, owner transfer, UI disappearance, and terminal absence behavior. The snapshots have different source kinds and digests and must remain separate in every citation. [OBSERVED; VERIFIED: x4-docs game snapshot `x4-9.00-steam-23660954`, root `blake3:8d30413b740369e8572d6a7647366be338421698596eee84d62fec0415685c79`; runtime snapshot `x4-probe-faction-ship-runtime-686318dd-v1`, root `blake3:01ea3b0d39afee3541a1c7e87ce2c1c8fc011ce688584966952c0d20e7a90c82`]

The shared architecture is ready for this workload: core identity and deterministic bounded detail groups are separate; transport is pumped before new collection; Rust owns permits, validation, durable publication, and recovery; collection remains separate from recipient visibility. [DOCUMENTED; VERIFIED: `docs/ARCHITECTURE.md:449-497`; `crates/observation-ingest/src/scheduler.rs:116-183`; `crates/observation-ingest/src/scheduler_budget.rs:54-135`; `crates/x4-bridge/src/production.rs`]

Planning can now select the demonstrated X4 paths. It must still make four uncertainties explicit closure gates rather than inventing defaults: source-backed faction disposition, exact normalized cargo/crew/equipment field schema and units, count/fill race behavior under live churn, and numerical normal/SETA budgets. [INFERRED; ASSUMED]

**Primary recommendation:** plan a measured, resumable Lua collector around `C.GetAllFactions` and `C.GetNumAllFactionShips`/`C.GetAllFactionShips`; publish core first and cargo/crew/equipment as deterministic dependent groups through the existing Carrier B and `ProductionObservationSession`; calibrate limits before the heavy-faction gate; then prove the full admitted set with unknown classification blocking closure.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
| --- | --- | --- | --- |
| Faction census and ship/detail capture | X4 runtime / Lua | — | Lua owns getters and resumable collection; no background native work may call X4/Lua. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:99-105`] |
| Admission and protocol assembly | Owned native DLL | X4 runtime / Lua | The DLL copies admitted facts and assembles messages; Lua retains final callback admission. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:101-105`] |
| Global scheduling, validation, feedback, persistence, recovery | Rust bridge | Database / storage | The bridge owns global policy and durable publication/recovery. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:101-113`] |
| Compatible decision snapshots | Rust strategic-state | Rust observation application | Collection does not imply recipient visibility; the current filter quotes exact accepted family value checks `ThreatFamily::Xen` and `ThreatFamily::Khk`, but this phase must not widen policy. [VERIFIED: `crates/strategic-state/src/derive.rs:56-76,101-114`] |
| Human-operated acceptance | Disposable X4 campaign | Retained evidence reader | The user starts and operates X4; agents prepare and inspect evidence and never read or modify saves. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:85-97`] |

## Project Constraints (from AGENTS.md)

- Treat X4 and installed extensions as read-only evidence; never inspect saves or copy unlicensed third-party code. [VERIFIED: `.agent-instructions/x4/AGENTS.md`, “Evidence and External Sources”]
- Keep model output outside the trust boundary and preserve deterministic replay, atomic rejection, bounded resources, idempotent recovery, typed state, and safe diagnostics. [VERIFIED: `.agent-instructions/x4/AGENTS.md`, “Engineering Invariants”]
- This phase affects `rust-domain` and `x4-runtime`, not `llm`; research, plan-check, code review, verification, security review, layered Rust/Lua verification, X4 runtime evidence, and sustained SETA are required. Mutation testing is conditional on changed deterministic safety logic. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:126-139`]
- `.planning/config.json` quotes `"nyquist_validation": false` and `"security_enforcement": false`; therefore the generic RESEARCH template’s Validation Architecture and Security Domain sections are omitted. The phase-specific owner decision still selects a security review. [VERIFIED: `.planning/config.json`]
- `.planning/**` is deliberately excluded from forced Markdownlint in this repository. [VERIFIED: `.agent-instructions/x4/AGENTS.md`, “Verification”]
- No package, tool, or implementation dependency should be added merely to compensate for missing source evidence. [ASSUMED]

## Structural Discovery Record

`ast-index stats` found an existing index with `323` files, `6213` symbols, `26213` references, and a `5.71 MB` database; `ast-index update` reported `0` new, changed, or deleted files. [OBSERVED; VERIFIED: local `ast-index` output, 2026-09-16]

Exact structural queries used:

1. `ast-index explore "production observation admission publication recovery"`
2. `ast-index explore "bounded scheduler heavy permit fairness"`
3. `ast-index explore "Lua runtime carrier collection callback"`
4. `ast-index explore "strategic visible threat family facts"`

The queries identified `ProductionObservationSession`, the observation scheduler/budget, the Lua runtime/carrier path, and the strategic visibility filter as the narrow implementation seams subsequently read. [OBSERVED; VERIFIED: local `ast-index` output, 2026-09-16]

## X4 Documentation Research Ledger

### Snapshot identity and request contract

Every request used exactly one snapshot and the same bounded page size:

```text
snapshot_ids: ["x4-9.00-steam-23660954"]
limit: 20
snapshot game_build: "9.00 / Steam build 23660954"
snapshot source_kind: "game"
snapshot source_namespace: "x4:vanilla"
snapshot root_digest: "blake3:8d30413b740369e8572d6a7647366be338421698596eee84d62fec0415685c79"
```

These values are copied verbatim from the tool responses. [OBSERVED; VERIFIED: x4-docs search responses, 2026-09-16]

| Exact query | Items | Coverage | Conflicts | Pagination | Planning consequence |
| --- | ---: | --- | --- | --- | --- |
| `faction` | 2 | `partial` | none | `next_cursor: null` | `GetFactionData` and owner-contained-object evidence; not a disposition registry. |
| `GetContainedObjectsByOwner` | 1 | `partial` | none | `next_cursor: null` | Declared owner query exists, but runtime proof shows mixed objects; use only for reconciliation. |
| `cargo` | 6 | `partial` | none | `next_cursor: null` | Component cargo plus missile/unit cargo declarations are inspectable. |
| `GetPeople2` | 1 | `partial` | none | `next_cursor: null` | Inspectable count/fill crew declaration. |
| `GetLoadout2` | 1 | `complete` | none | `next_cursor: null` | Inspectable template API; runtime evidence rejects it for installed equipment. |
| `GetComponentData owner class macro sector` | 1 | `partial` | none | `next_cursor: null` | Inspectable component core/cargo field calls. |
| `ammunition drones` | 2 | `partial` | none | `next_cursor: null` | Inspectable all-unit count/fill declarations. |
| `GetNumAllFactionShips` | 0 | `complete` | none | `next_cursor: null` | No count/fill ship enumeration contract. |

All searches terminated on the first search page with `next_cursor: null` and no conflicts. Provenance counts were respectively `57`, `8`, `424`, `45`, `3`, `382`, `12`, and `0`. Every selected entity was inspected through `next_cursor: null`. [OBSERVED; VERIFIED: x4-docs game-snapshot search and inspect responses, 2026-09-16]

The exact `GetNumAllFactionShips` query still returns no game-snapshot entity. This is only a corpus boundary. Its runtime behavior is supplied by the separate registered probe snapshot below, never silently attributed to the game snapshot. [OBSERVED; VERIFIED: x4-docs game-snapshot response; DOCUMENTED; VERIFIED: `docs/architecture-verification.md:170-181`]

The generic research seam classifies provider id `x4-docs` as `LOW`, even with `--verified`; the project contract independently designates registered x4-docs evidence as the authoritative lookup gate. This research therefore cites exact snapshot/entity identity and distinguishes documented declarations from observed probe behavior instead of treating the seam tier as API evidence. [OBSERVED; VERIFIED: `gsd-tools query classify-confidence --provider x4-docs --verified`, 2026-09-16; DOCUMENTED; VERIFIED: `.agent-instructions/x4/AGENTS.md`, “Reference Map”]

### Registered runtime snapshot and immutable boundary

The runtime snapshot is quoted verbatim as:

```text
snapshot_id: "x4-probe-faction-ship-runtime-686318dd-v1"
source_kind: "documentation"
source_namespace: "probe:x4-docs"
game_build: "9.00 / Steam build 23660954"
mod_version: "x4-faction-ship-runtime-semantics-v5"
root_digest: "blake3:01ea3b0d39afee3541a1c7e87ce2c1c8fc011ce688584966952c0d20e7a90c82"
```

[OBSERVED; VERIFIED: x4-docs runtime-snapshot responses, 2026-09-16]

The same eight exact queries were repeated explicitly against this snapshot. Every search and every selected-entity inspection was exhausted through `next_cursor: null`; no conflicts were returned. [OBSERVED; VERIFIED: x4-docs runtime-snapshot search/inspect responses, 2026-09-16]

| Exact query | Search coverage | Selected runtime entity | Inspect pages | Load-bearing result |
| --- | --- | --- | ---: | --- |
| `faction` | `unsupported` | `C.GetAllFactions`, `X4.faction_ship.runtime`, `C.GetNumAllFactionShips` | 2 + 10 + 14 | Faction census and lifecycle/count-fill observations. |
| `GetContainedObjectsByOwner` | `complete` | `X4.faction_ship.runtime` | 12 | Mixed-object reconciliation source, not canonical ship membership. |
| `cargo` | `unknown` | runtime `GetComponentData` | 34 | Core/cargo result across baseline and transfer states. |
| `GetPeople2` | `unknown` | runtime `C.GetPeople2` | 42 | Crew count/fill and role observations. |
| `GetLoadout2` | `unknown` | `C.GetUpgradeSlotCurrentComponent` | 30 | Current installed-component route; template route contradicted. |
| `GetComponentData owner class macro sector` | `unknown` | runtime `GetComponentData` | 34 | Identity, owner, type/class, location, and cargo observation. |
| `ammunition drones` | `unknown` | runtime `C.GetAllUnits` | 13 | Full unit inventory requires `onlydrones=false`. |
| `GetNumAllFactionShips` | `unsupported` | `C.GetNumAllFactionShips` | 14 | Canonical faction-ship count/fill behavior for player and TEL. |

[OBSERVED; VERIFIED: x4-docs runtime-snapshot search/inspect responses, 2026-09-16]

### Established X4 semantics

- `C.GetAllFactions` returned `reported_count = filled_count = 25` in five observations. This proves the observed runtime inventory, not a built-in vanilla/DLC/service/mod disposition taxonomy. [OBSERVED; VERIFIED: runtime entity `C.GetAllFactions`, snapshot `x4-probe-faction-ship-runtime-686318dd-v1`]
- `C.GetNumAllFactionShips`/`C.GetAllFactionShips` worked for player and TEL and is the canonical ship-membership path. `GetContainedObjectsByOwner` returned mixed object kinds and is reconciliation-only. [OBSERVED; VERIFIED: runtime entities `C.GetNumAllFactionShips`, `X4.faction_ship.runtime`, snapshot `x4-probe-faction-ship-runtime-686318dd-v1`]
- The runtime identity survived player-to-TEL ownership transfer. Player count changed from `3` to `2`; the transferred identity remained addressable after the UI showed “Unknown Ship”. Terminal absence made detail calls unavailable; UI disappearance alone did not prove deletion. [OBSERVED; VERIFIED: runtime entities `X4.faction_ship.runtime`, `C.GetNumAllFactionShips`, runtime `GetComponentData`, runtime `C.GetPeople2`]
- `GetComponentData` supplied core and cargo observations; `GetPeople2` supplied crew observations. The game snapshot declares `uint32_t GetPeople2(PeopleInfo* result, uint32_t resultlen, UniverseID controllableid, bool includearriving);` and `GetPeopleCapacity` with `UniverseID controllableid`, `const char* macroname`, and `bool includepilot`. [DOCUMENTED; VERIFIED: game-snapshot entities `C.GetPeople2`, `C.GetPeopleCapacity`]
- Installed physical equipment is read from current upgrade slots, with virtual thruster and software slots handled separately. `GetLoadout2`/`GetLoadoutCounts2` are template/default-loadout APIs, not installed-equipment queries. [OBSERVED; VERIFIED: runtime entity `C.GetUpgradeSlotCurrentComponent`; DOCUMENTED; VERIFIED: game-snapshot entities `C.GetLoadout2`, `C.GetLoadoutCounts2`]
- Ammunition uses missile-cargo count/fill APIs. Full drone/unit inventory uses `GetNumAllUnits`/`GetAllUnits(..., false)`; `true` omitted a defence drone in the probe. The game snapshot declares `uint32_t GetAllUnits(UnitData* result, uint32_t resultlen, UniverseID defensibleid, bool onlydrones);`. [OBSERVED; VERIFIED: runtime entity `C.GetAllUnits`; DOCUMENTED; VERIFIED: game-snapshot entities `C.GetAllUnits`, `C.GetNumAllUnits`, `C.GetMissileCargo`, `C.GetNumMissileCargo`]

### Residual semantic limits

- Every observed count/fill attempt matched its allocated count. Concurrent growth/shrink was not observed, so race handling is still an explicit fake-adapter and disposable-runtime requirement. [UNKNOWN; VERIFIED: runtime entities `C.GetAllFactions`, `C.GetNumAllFactionShips`, `C.GetPeople2`, `C.GetAllUnits`]
- Faction origin/lifecycle classification is consumer-owned. `GetFactionData` exposes metadata such as `name`, `shortname`, and `primaryrace`, but no authoritative vanilla/DLC/mod/service/temporary disposition. Every discovered ID must therefore survive with `included`, `excluded`, or closure-blocking `unknown` evidence, separate from mind participation. [DOCUMENTED; VERIFIED: game-snapshot entity `GetFactionData`; INFERRED; ASSUMED for the recommended classifier representation]
- The inspected claims establish the routes and lifecycle behavior but do not expose a complete normalized schema for cargo volume units, crew qualification scale, or every equipment applicability case. Those schemas must be captured as source-bound evidence before the corresponding field can pass D-12. [UNKNOWN; VERIFIED: x4-docs inspected entities above]

## Standard Stack

### Core

| Component | Version / identity | Purpose | Prescription |
| --- | --- | --- | --- |
| Existing Rust workspace | `rust-version = "1.97.1"`, `edition = "2024"` | Typed observation, scheduling, validation, persistence, recovery | Reuse; add no parallel runtime or framework. [VERIFIED: `Cargo.toml`] |
| Existing Lua runtime | `live_galaxy_runtime.lua` + `live_galaxy_scheduler.lua` | X4 callback, pump/control, final collection admission | Extend the existing single callback/scheduler path. [VERIFIED: `extensions/live_galaxy/lua/live_galaxy_runtime.lua:1-100`; `extensions/live_galaxy/lua/live_galaxy_scheduler.lua:15-65`] |
| Owned Carrier B | ABI version literal `2` | Copy accepted facts, assemble bounded protocol messages, carry feedback | Reuse; do not restore Carrier A. [VERIFIED: `extensions/live_galaxy/lua/live_galaxy_carrier.lua:29-42,107-126`] |
| Existing persistence/application path | repository ports + SQLite implementation | Atomic durable publication and restart recovery | Extend through typed sections and independent durable readback; do not create side storage. [VERIFIED: `crates/x4-bridge/src/production.rs:31-198`] |

### Supporting

| Component | Purpose | When to use |
| --- | --- | --- |
| Existing Lua contract harness | Pure-Lua deterministic and adapter-shape evidence | Every collector, continuation, retry, and classification change. [VERIFIED: repository paths `extensions/live_galaxy/tests/run_contracts.ps1`, `scheduler_contract.lua`, `carrier_b_contract.lua`] |
| Existing Rust test suites | Domain, scheduler, transport, publication, and recovery evidence | Focused test per task; full workspace suite at wave/phase gates. [VERIFIED: repository test paths under `crates/observation-*`, `crates/x4-bridge`, and `crates/x4-carrier-native`] |
| Disposable-X4 runbook pattern | Human-operated normal/SETA/runtime evidence | Only after local gates pass and exact X4 source probes are designed. [VERIFIED: `tests/x4-disposable/05.4-carrier-b-procedure.md`] |

### Alternatives Considered

| Instead of | Could Use | Tradeoff |
| --- | --- | --- |
| Existing Carrier B | Carrier A | Rejected by D-14; Carrier A may not be a dependency or fallback. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:85-97`] |
| Bounded core/detail groups | One faction-wide snapshot | Rejected because it is too large and easy to invalidate. [VERIFIED: `docs/architecture-decisions.md:397-413`] |
| Bounded core/detail groups | One section per ship | Rejected because it creates excessive metadata and scheduling overhead. [VERIFIED: `docs/architecture-decisions.md:397-413`] |
| Dynamic faction census | Fixed vanilla allowlist | Rejected by D-06/D-08 and ADR-LG-022. [VERIFIED: `docs/architecture-decisions.md:415-442`] |

**Installation:** no external packages are recommended. Package legitimacy auditing is not applicable.

## Architecture Patterns

### System Architecture Diagram

```text
X4 pulse/event
  -> pump pending Carrier B bytes and control feedback
  -> capacity available?
       no  -> visible backpressure; retain continuation
       yes -> admit one bounded Lua collection step
                -> faction census / core ship index
                -> deterministic cargo | crew | loadout group
                -> source/applicability/coverage disposition
  -> owned DLL copies facts and assembles immutable messages
  -> named-pipe transport and receiver validation
  -> keyed Rust candidate assembly
       -> exact core revision still current?
            no  -> reject dependent group; recollect later
            yes -> atomic durable publication
  -> independent durable readback
  -> compatible decision snapshot with separate recipient policy
```

The ordering and dependency behavior follow the accepted architecture. [VERIFIED: `docs/ARCHITECTURE.md:449-497`; `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:99-113`]

### Recommended Project Structure

```text
extensions/live_galaxy/lua/              # X4 getters, resumable collectors, admission
extensions/live_galaxy/tests/            # pure-Lua and fake-adapter contracts
crates/observation-domain/               # typed core/detail values and evidence axes
crates/observation-ingest/               # limits, scheduler, keyed candidate assembly
crates/observation-application/          # dependency validation and publication
crates/observation-persistence/          # atomic durable current/history/receipt state
crates/x4-carrier-native/                 # owned producer and transport assembly
crates/x4-bridge/                         # production integration and recovery
tests/x4-disposable/                      # human-run X4 procedure and retained evidence index
```

This is a mapping of existing repository responsibility, not a mandate to create every listed file. [VERIFIED: repository tree and `docs/ARCHITECTURE.md`]

### Pattern 1: Core First, Deterministic Dependent Groups

Core contains exactly the phase-required identity, owner, type/class, and location. Cargo, crew, and loadout are separate deterministic, versioned, bounded groups carrying exact member identities, exact source-core revision, capture window, coverage, and freshness. A core change may finish the running group but invalidates it before publication if its frozen dependency is no longer current. [VERIFIED: `docs/ARCHITECTURE.md:463-497`; `docs/architecture-decisions.md:397-413`]

### Pattern 2: Pump Before Collection

The current Rust scheduler stages at most one pending byte vector, reserves terminal capacity, pumps pending bulk first, and only then selects a new admission. [VERIFIED: `crates/observation-ingest/src/scheduler.rs:116-166`]

The current Lua scheduler likewise calls `progress` and `poll_control` before `begin_evidence` and capture. [VERIFIED: `extensions/live_galaxy/lua/live_galaxy_scheduler.lua:27-52`]

### Pattern 3: Measured Heavy-Work Admission

The Rust budget admits a heavy intent only when ordinary permits, zero debt, in-flight capacity, step bounds, and a heavy permit all allow it. Completion restores the heavy permit and converts work above declaration into bounded debt or collector rejection. [VERIFIED: `crates/observation-ingest/src/scheduler_budget.rs:54-135`]

The Phase 05.4 Lua constants quote `max_records = 1`, `max_work = 1`, `max_batches = 1`, and `max_age_millis = 5000`; they are one-getter proof values, not valid heavy-workload defaults. [VERIFIED: `extensions/live_galaxy/lua/live_galaxy_carrier.lua:9-22`; VERIFIED: `docs/architecture-verification.md:359-369`]

### Pattern 4: Atomic Count/Fill Attempts

For each count/fill source, one Lua continuation owns the observed count, checked allocation, fill result, and attempt budget. A fill that exceeds allocation, disagrees with the accepted count, duplicates identities, or crosses its work/time limit is discarded as a whole; the collector takes a fresh count only if the bounded retry policy permits it, otherwise it resumes or reports partial/unknown without publication as complete. Successful transport retry reuses frozen bytes and never repeats getters. [INFERRED; ASSUMED; supported by DOCUMENTED `docs/architecture-verification.md:183-201`]

Observed count/fill equality is evidence for the tested captures, not an ABA-safe global completeness certificate. Membership absence may affect the core index only after the selected source has produced an accepted complete core revision. [DOCUMENTED; VERIFIED: `docs/ARCHITECTURE.md:487-497`; OBSERVED; VERIFIED: runtime entities `C.GetAllFactions`, `C.GetNumAllFactionShips`]

### Existing Touchpoints and Likely Ownership

| Responsibility | Existing touchpoint | Planning consequence |
| --- | --- | --- |
| Pump-first callback and bounded Lua work | `live_galaxy_runtime.lua`, `live_galaxy_scheduler.lua` | Extend the one shared callback with resumable faction/core/detail states; do not add a second timer. [VERIFIED: source files opened this session] |
| X4 count/fill precedent | `live_galaxy_component_discovery.lua` | Reuse its adapter-injection and checked-allocation ideas, but do not reuse station-specific constants or treat it as ship semantics. [VERIFIED: source file opened this session] |
| Producer limits and copied facts | `live_galaxy_carrier.lua`, `crates/x4-carrier-native/` | Calibrate and version limits; preserve synchronous copy before background work. [VERIFIED: `extensions/live_galaxy/lua/live_galaxy_carrier.lua:3-27`] |
| Scheduling and aggregate bounds | `crates/observation-ingest/src/scheduler.rs`, `scheduler_budget.rs` | Add intents/costs only through the existing permit, heavy-permit, debt, and pending-byte model. [VERIFIED: source files opened this session] |
| Evidence axes and typed records | `crates/observation-domain/src/section_state.rs`, adjacent observation types | Extend typed field/applicability representation without collapsing section coverage, freshness, or availability. [VERIFIED: source file opened this session] |
| Publication and recovery | `crates/x4-bridge/src/production.rs` and existing application/persistence ports | Reuse `ProductionObservationSession`; do not create side persistence or a second publication path. [VERIFIED: source file opened this session] |
| Human runtime proof | `tests/x4-disposable/` | Add a Phase 05.5 procedure/evidence index only after fake/local gates and owner approval. [DOCUMENTED; VERIFIED: `.agent-instructions/x4/AGENTS.md`, “Verification”] |

Exact new filenames and section-key spellings are planner/implementation choices and must follow the existing module-size and responsibility rules; this research does not invent them. [INFERRED; ASSUMED]

### Anti-Patterns to Avoid

- **API by precedent:** a working player-scoped call site does not prove arbitrary-faction support, complete membership, stable identity, or field applicability.
- **Empty means complete:** zero ships, empty cargo, or absent equipment cannot become `known_empty` without source completeness evidence.
- **Transport completion upgrades source quality:** successful delivery says nothing about source membership or deletion authority.
- **Global snapshot fiction:** never attach all detail groups to an invented simultaneous faction-wide capture.
- **Detail failure deletes core:** preserve core and publish explicit unknown/stale/unsupported detail state.
- **One-getter constants become production limits:** heavy limits must be derived from Phase 05.5 measurements.
- **Collection implies visibility:** do not expose foreign detail to ZYA/ARG or change `mind_candidate` to make collection easier.

Each anti-pattern is prohibited by the accepted context or verification contract. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:39-83,99-122`; `docs/architecture-verification.md:195-201,433-478`]

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
| --- | --- | --- | --- |
| Scheduling/backpressure | A second Lua timer or collector-local busy loop | Existing single callback, Carrier B feedback, and Rust scheduler | Maintains shared bounds, pump-first behavior, fairness, and visible degradation. [VERIFIED: `docs/ARCHITECTURE.md:499-510`; `crates/observation-ingest/src/scheduler.rs:76-183`] |
| Retry | Re-run X4 getters after ambiguous delivery | Retry identical immutable bytes through Carrier B | Prevents observations from changing identity/content across transport retry. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:107-113`] |
| Publication | Side files or ad hoc SQLite writes | Existing repository/application transaction | Preserves atomic publication, receipts, history, and recovery. [VERIFIED: `crates/x4-bridge/src/production.rs:31-198`] |
| Faction classification | Hard-coded faction allowlist | Source-backed dynamic census with included/excluded/unknown disposition | DLC and small/pirate membership must be discovered; unknown blocks closure. [VERIFIED: `docs/architecture-decisions.md:421-434`] |
| Detail batching | One giant faction payload or one section per ship | Deterministic bounded groups | Both rejected extremes have known invalidation or metadata costs. [VERIFIED: `docs/architecture-decisions.md:397-413`] |
| Qualification aggregation | Stringly typed, unordered Lua maps | Typed/versioned deterministic role-tier distribution | Replay and group digests require canonical representation. [ASSUMED]

**Key insight:** the hard problem is not serializing more fields; it is preserving source-strength, identity, applicability, bounded work, dependency validity, and durable outcome across a changing X4 world.

## Evidence Architecture and Workload Guidance

### Evidence levels must remain separate

| Level | What it proves | Required Phase 05.5 evidence |
| --- | --- | --- |
| Static | Package shape, forbidden mutation vocabulary, schemas/types, bounded constants | Package conformance, type/contract review, no new mutation or mind surface. |
| Pure Lua | Deterministic grouping, continuations, sorting/canonicalization, error/disposition mapping | Fixtures for growth/shrink, empty/unknown/N/A, transfer, stale IDs, reentry, exact retry without getters. |
| Fake adapter | Exact call order, count/fill churn, unavailable functions, partial detail, backpressure/reconnect | Scripted API results and failure injection per source adapter. |
| Local integration | Actual DLL load, ABI, named pipe, receiver, candidate assembly, SQLite commit/readback/restart | Full heavy synthetic payload through the owned producer and Rust path. |
| Pending game | Procedure prepared but not run | Must be labelled pending, never observed. |
| Observed in X4 | Runtime getter behavior, cost, population, normal/SETA timing, save/load boundary | Human-operated disposable vanilla+DLC+Live Galaxy gates with retained, independently read evidence. |

The repository requires these classes to be reported separately. [VERIFIED: `.planning/REQUIREMENTS.md:71-76`; `.agent-instructions/x4/AGENTS.md`, “Verification”]

### Ordered workload gates

1. **Calibration and faction census:** record every dynamically discovered faction with raw source identity, provenance, `included`/`excluded`/`unknown`, exclusion evidence, observation eligibility, and separate Faction Mind participation. Unknown service/temporary/mod provenance blocks the full gate. [VERIFIED: `docs/architecture-decisions.md:421-434`]
2. **Select the heavy ordinary faction from measurements:** choose the ordinary eligible faction with the largest representative combination of ship count, cargo cardinality/bytes, crew tiers, and loadout/unit cardinality in the disposable campaign. Do not select by reputation, lore, territory, or assumed vanilla size. [ASSUMED]
3. **Gate A — one heavy faction:** exercise core plus cargo/crew/loadout through X4, Lua, DLL, transport, Rust, durable publication, readback, recovery, and compatible decision snapshot. Diagnose per-stage cost before aggregate proof. [VERIFIED: `docs/architecture-verification.md:442-464`]
4. **Gate B — full admitted set:** run every included vanilla/DLC faction, including small/pirate plus XEN/KHK, under normal time and sustained approximately `6.2x` SETA. [VERIFIED: `docs/architecture-verification.md:442-451`]

### Measurements required before selecting limits

Capture complete-record UTF-8 sizes; section cardinality and canonical bytes; Lua normalization/serialization/allocation/GC; latency distribution for each indivisible native stage; delivered callback cadence; control round trip; actual pipe quota/whole-message behavior; Rust receive/decode/validation/staging/commit cost; candidate expansion; retry amplification; backlog slope/drain time; capture-to-commit freshness; and retained history/receipt/diagnostic cost. [VERIFIED: `docs/architecture-verification.md:371-389`]

Require all three existing stability conditions verbatim:

```text
supremum over t of (P(t) - S(t)) <= B_total
Rust staged rate >= Lua produced rate
p99(collection + batching + transport + staging + commit)
  < section freshness or stale-block deadline
```

[VERIFIED: `docs/architecture-verification.md:391-415`]

Do not preselect a duration or threshold from the one-record run. The checked plan must use a calibration pass to derive numeric callback token rate, burst, debt, step cap, heavy threshold, group size, message ceiling, candidate bounds, retry/cooldown/age, post-load revalidation, freshness, and retention. The selected value is the minimum independently safe boundary. [VERIFIED: `docs/architecture-verification.md:417-431`]

### Required recovery matrix

The checked plan should inject failure at: before core completion; mid-detail continuation; after DLL copy but before pipe handoff; after volatile receipt; after durable commit with lost response; during Rust absence/restart; during reconnect with incomplete transport identity; after game load/reload; and after ownership/location change between core and detail. The expected result must distinguish retryable immutable bytes, ambiguous durable outcome reconciliation, invalidated dependency, history-only prior revision, and fresh recollection. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:99-113`; VERIFIED: `crates/x4-bridge/src/production_admission.rs:53-116`; ASSUMED for the exact phase-specific injection enumeration]

### Field and coverage ledger

For every ship and every required field, retain: source adapter/version; exact core revision; capture start/end; raw applicability evidence; normalized state (`available`, `not_applicable`, `unsupported`, `unknown`, `stale`, `inaccessible` as applicable); coverage strength; member identity; error; and eventual group-coverage result. Values named here must match the final source/type definitions; where those discrete enums do not yet exist, they remain planning placeholders rather than implementation values. [ASSUMED]

The current source-of-truth enums quote `SectionQuality { Fresh, KnownEmpty, Unknown, Partial, Stale, Unsupported }`, `SectionFreshness { Fresh, Stale }`, `SectionCoverage { Complete, KnownEmpty, PointMeasurement, Unknown, Partial, Unsupported }`, and `SectionAvailability { Available, Unavailable }`. They do not encode the locked `not_applicable` versus `inaccessible` distinction. Plan a separate typed field applicability/result axis rather than overloading the existing section axes; preserve the exact D-11 vocabulary in tests. [VERIFIED: `crates/observation-domain/src/section_state.rs:3-31`; INFERRED; ASSUMED for the new type shape]

### Selected and Rejected Risk Gates

| Gate | Decision | Reason |
| --- | --- | --- |
| Code review and phase verification | Selected | Mandatory for every phase. [DOCUMENTED; VERIFIED: `.agent-instructions/x4/AGENTS.md`, “GSD Workflow”] |
| Security review | Selected | External X4 inputs, native boundary, persistence, and publication authority are exercised. [DOCUMENTED; VERIFIED: `05.5-CONTEXT.md`, “Layer classification and verification gates”] |
| X4 probe | Selected | Source/static/fake evidence cannot establish runtime cost, live churn, applicability, or normal/SETA behavior. [DOCUMENTED; VERIFIED: `.agent-instructions/x4/AGENTS.md`, “Phase Layer and Risk Matrix”] |
| SETA soak | Selected | Scheduling, freshness, recovery, and aggregate load are phase acceptance. [DOCUMENTED; VERIFIED: D-15 in `05.5-CONTEXT.md`] |
| Nyquist validation | Selected only as a requirement-to-test audit inside the phase | Project-wide `workflow.nyquist_validation` is quoted as `false`, but this phase explicitly owns VAL-01/02/06 and VER-LG-003/011/012 mapping. [VERIFIED: `.planning/config.json`; `.planning/REQUIREMENTS.md`] |
| Mutation testing | Conditional | Run only if implementation changes deterministic validation/safety invariants that ordinary tests could miss. [DOCUMENTED; VERIFIED: `.agent-instructions/x4/AGENTS.md`, risk matrix] |
| AI specification/eval review | Rejected | No model-visible contract, prompt, model choice, or evaluation behavior changes. [DOCUMENTED; VERIFIED: phase goal and CONTEXT deferred scope] |

## Common Pitfalls

### Pitfall 1: Mistaking corpus absence for API absence

**What goes wrong:** the plan declares an API unavailable because Docs MCP returned zero entities.

**Why it happens:** `coverage: complete` applies to the selected indexed corpus/query, not the game’s total API surface.

**How to avoid:** enrich the snapshot or obtain authoritative source, then inspect selected entities through all cursors; probe only remaining semantic gaps.

**Warning signs:** implementation tasks cite memory or a player-only example instead of exact snapshot/entity provenance. [VERIFIED: `docs/architecture-verification.md:170-181`]

### Pitfall 2: Count/fill races become silent truncation

**What goes wrong:** population grows or shrinks between count and fill, producing a partial set, duplicate, or out-of-bounds assumption.

**How to avoid:** source-specific tests must define allocation/fill behavior, bounded retry, ordering/canonicalization, and explicit partial/unknown coverage.

**Warning signs:** equal pre/post counts or digests are used as an ABA-proof completeness certificate. [VERIFIED: `docs/architecture-verification.md:183-201`]

### Pitfall 3: Optional detail weakens mandatory acceptance

**What goes wrong:** a failed cargo/crew/loadout source is safely represented as unknown and then treated as phase success.

**How to avoid:** preserve core at runtime, but separately block closure whenever an agreed applicable field is unavailable.

**Warning signs:** `unsupported` appears in acceptance totals as passed. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:66-83`]

### Pitfall 4: SETA accelerates demand but not real-time service

**What goes wrong:** game-time freshness demand rises while real-time permit refill, transport, and Rust commit capacity do not.

**How to avoid:** use game time for urgency, real time for admission, reserve core service, degrade detail first, and expose stale-block decisions.

**Warning signs:** queue/backlog slope stays positive or detail retries starve core work. [VERIFIED: `docs/architecture-verification.md:149-153,391-415`]

### Pitfall 5: Reconnect appears idempotent but repeats getters

**What goes wrong:** transport retry triggers a fresh X4 read, changing content under the same logical revision.

**How to avoid:** freeze the accepted record/message and retry immutable bytes; after uncertain source boundaries require fresh qualification.

**Warning signs:** getter-call counters increase during transport-only retry. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:107-113`]

### Pitfall 6: Collection leaks into recipient policy

**What goes wrong:** XEN/KHK inclusion or foreign ship collection becomes a `mind_candidate` or universal visibility change.

**How to avoid:** keep observation eligibility and mind participation separate; verify current decision snapshots remain compatible without adding reconnaissance.

**Warning signs:** changes to mind roster, contact aging, disclosure, reports, or active scouting appear in Phase 05.5. [VERIFIED: `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:46-64,224-237`]

## Code Examples

Verified existing patterns follow; they are examples of seams to extend, not a complete Phase 05.5 implementation.

### Pump Before Admission

```rust
if let Some(pumped_bytes) = self.pump_pending(bulk_capacity) {
    return self.outcome(pumped_bytes, reserve, None);
}
let admission = self.select_admission(bulk_capacity);
```

[VERIFIED: `crates/observation-ingest/src/scheduler.rs:127-142`]

### Heavy Permit and Debt

```rust
self.permits > 0
    && self.debt.0 == 0
    && self.in_flight.len() < self.limits.burst.get()
    && self.accepts_declared(intent.declared_work())
    && (intent.work_kind() == WorkKind::Light || self.heavy_permits > 0)
```

[VERIFIED: `crates/observation-ingest/src/scheduler_budget.rs:58-64`]

### Lua Pump/Control Before Capture

```lua
local progress, progress_status = call(carrier, "progress", 1)
if progress == nil then return finish(progress_status, nil) end
if terminal[progress] then return finish(terminal[progress], progress) end
local control, control_error = call(carrier, "poll_control")
```

[VERIFIED: `extensions/live_galaxy/lua/live_galaxy_scheduler.lua:27-32`]

### Bounded Count/Fill State Machine

```text
count -> checked allocation -> fill -> validate cardinality and identities
  mismatch -> discard the whole attempt -> bounded fresh-count retry or yield
  match    -> canonicalize -> freeze evidence -> hand off once
transport retry -> reuse frozen bytes; do not call X4 again
```

The function pair `C.GetNumAllFactionShips`/`C.GetAllFactionShips` and matching tested counts are observed in the separate runtime snapshot. The declaration gap was closed by the coordinated source enrichment below. Matching counts still do not prove race-free or atomic membership. [OBSERVED; VERIFIED: runtime entity `C.GetNumAllFactionShips`; DOCUMENTED: enriched game snapshot below]

### Plan 01 coordinated declaration closure

The orchestrator repeated all four exact searches (`GetNumAllFactions`, `GetAllFactions`, `GetNumAllFactionShips`, `GetAllFactionShips`) and exhausted the four selected entities to `next_cursor: null`, with complete declaration coverage and no conflicts. This supersedes the declaration-only blocker from Plan 01's initial halt; historical zero-result queries above describe the older corpus only. The executor did not repeat these exhausted searches.

- Snapshot: `x4-9.00-steam-23660954-faction-ship-observation-v1`.
- Source: `game`, `x4:vanilla`, `9.00 / Steam build 23660954`.
- Root: `blake3:5cc981dc8a15914c993062aaefc37ebfc45401339d93526dd3df295ba51d996d`.
- Documented declarations: `uint32_t GetNumAllFactions(bool includehidden);`, `uint32_t GetAllFactions(const char** result, uint32_t resultlen, bool includehidden);`, `uint32_t GetNumAllFactionShips(const char* factionid);`, `uint32_t GetAllFactionShips(UniverseID* result, uint32_t resultlen, const char* factionid);`; `UniverseID` is `uint64_t`.
- Observed vanilla calls allocate `const char*[?]` for factions and `UniverseID[?]` for ships, with fill order buffer, capacity, selector. The faction selector is `false`; the ship examples select `"player"`. Arbitrary-faction behavior remains evidence from the separate runtime probe, not these examples.
- Pointer-backed faction strings are copied immediately using `ffi.string`; ship identity remains a decimal string with no Lua-number conversion. No ordering, atomicity, race, pointer lifetime or deletion guarantee is inferred.

For the newly unresolved real-adapter core call shape, the executor searched `GetComponentData owner class macro sector` in this enriched snapshot (`limit: 20`, initial cursor null), then exhausted entity `entity:18e8907ad2e7892bfe8070105e97b3d68b583321c743a82aa557a4367b776a40` in 49 inspect pages to null. Coverage is partial, with 116 claims, 148 calls and 148 occurrences. Observed source claims include `claim:368cbfd2d9408f28453220bb81cf7282b76ae04a9fc40002f99d0830087e6f1c` (`ConvertStringToLuaID` before a component lookup), `claim:2b07318717e618adea2ed116fa7731f1b2438396c3c8b36c4da2cf1a30bd61fd` (`owner`), `claim:3048a47ae08684f744a8c1630d18e617e674c02cfb614c8737a8581b99eb90d1` (`macro`, `classid`), and `claim:39e96605c24c4b222c2d52eaa576f61edf3d650fb37a1a96a8532bbe5869d4cb` (`sectorid`). These establish call shapes only; the collector rejects unavailable or invalid fields and does not claim an atomic core snapshot.

## State of the Art

| Old / insufficient approach | Current required approach | Impact |
| --- | --- | --- |
| Station-only architectural proof | Real heavy faction ship proof across X4 and Rust | Phase 05.5 owns the successor ship evidence. [VERIFIED: `docs/architecture-decisions.md:444-459`] |
| One getter / one record | Measured heavy core plus cargo/crew/loadout workload | Phase 05.4 proves transport readiness only. [VERIFIED: `docs/architecture-verification.md:435-440`] |
| Implicit vanilla allowlist | Dynamic independent vanilla/DLC census plus XEN/KHK | Unknown disposition now blocks closure. [VERIFIED: `docs/architecture-decisions.md:421-442`] |
| One all-or-nothing revision | Deterministic bounded dependent groups | Enables incremental progress without invented simultaneity. [VERIFIED: `docs/architecture-decisions.md:397-413`] |
| Foreign observation equated with a mind | Observation eligibility separate from participation/visibility | No hostile mind or reconnaissance scope is admitted. [VERIFIED: `docs/architecture-decisions.md:428-440`] |

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
| --- | --- | --- | --- |
| A1 | Choose the first heavy ordinary faction from combined measured ship/detail cardinality rather than a lore/name heuristic. | Evidence Architecture | Selection metric may need adjustment after authoritative APIs expose different indivisible costs. |
| A2 | Canonical typed role-tier distribution is the appropriate normalized crew representation. | Don't Hand-Roll | A source may expose a different stable aggregation or qualification scale. |
| A3 | The proposed recovery injection list is sufficient as a planning baseline. | Evidence Architecture | Authoritative lifecycle semantics may add necessary cases. |
| A4 | The proposed per-field evidence ledger fields map cleanly to existing or planned types. | Evidence Architecture | Exact enum/schema values remain unverified and must be read before implementation. |
| A5 | A versioned data-driven classifier with `included`, `excluded`, and `unknown` is the best representation. | X4 Semantics / Open Questions | The evidence source may require a different typed policy shape. |
| A6 | Discard-and-recount is the safe mismatch policy for all selected count/fill APIs. | Architecture Patterns | A documented API may define a different safe fill result or retry boundary. |
| A7 | A separate field applicability/result axis should carry D-11 states. | Field Ledger | Existing types may support another non-lossy composition after design review. |
| A8 | Existing modules should own the listed responsibilities without exact new filenames. | Existing Touchpoints | File-size or dependency rules may require a narrow helper module. |
| A9 | No new package/tool is needed for implementation. | Project Constraints | A verified build or evidence gap may justify an approved dependency. |
| A10 | Carrier B may not require rebuilding if its accepted fact boundary already supports the new records. | Environment Availability | Producer schema/version changes may make a native rebuild mandatory. |

## Open Questions and Closure Gates

### Planning status

The API-selection blocker is closed: planning may select the registered faction census, faction-ship enumeration, component, people, upgrade-slot, missile-cargo, and unit paths. Phase completion remains gated by the four evidence questions below. [OBSERVED; VERIFIED: x4-docs snapshots and entities in the research ledger]

### Remaining questions

1. **Faction disposition evidence:** which reviewed source maps each observed faction ID to independent vanilla/DLC, player, mod-added, service, or temporary? Recommendation: make the classifier versioned and data-driven, retain every raw ID, and fail the full gate on `unknown`. [UNKNOWN]
2. **Normalized field contracts:** what exact raw fields and units represent cargo quantity/capacity/occupied volume, crew qualification distribution, and equipment applicability? Recommendation: capture them in fake-adapter fixtures and a narrow disposable probe before implementing each Rust type. [UNKNOWN]
3. **Count/fill churn:** how do the faction, faction-ship, people, missile, and unit sources behave when membership changes between count and fill? Recommendation: discard mismatched attempts atomically and retry from a fresh count under a finite attempt/work/time budget; never publish truncation as complete. [INFERRED; ASSUMED]
4. **Measured limits:** what callback cost, group size, message/candidate cap, freshness deadline, retry age, and normal/SETA duration are safe? Recommendation: derive them from calibration and return the numerical acceptance thresholds for owner approval. [UNKNOWN]

### Exact requests completed

The eight mandated exact searches were completed separately against both immutable snapshots with `limit: 20`, cursor omitted/null, no conflicts, and terminal `next_cursor: null`. Every selected entity was inspected through its terminal cursor; page counts are recorded above. [OBSERVED; VERIFIED: x4-docs search/inspect responses, 2026-09-16]

### Closure rule

Each residual unknown must be resolved by registered documentation or a minimal disposable observation, then recorded as documented, observed, inferred, or unknown. A still-applicable unknown field blocks Phase 05.5 closure; it does not block writing the plan that owns the probe and checkpoint. [DOCUMENTED; VERIFIED: `.agent-instructions/x4/handoffs/knowledge-gap.md`; `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md:66-83`]

## Environment Availability

| Dependency | Required By | Available | Version / state | Fallback |
| --- | --- | --- | --- | --- |
| Rust | Rust contracts/tests | yes | `rustc 1.97.1`, `cargo 1.97.1` | — |
| PowerShell | repository scripts | yes | `7.6.5` | — |
| Node/npm | GSD and Markdown tooling | yes | Node `v24.16.0`, npm `11.13.0` | — |
| X4 9.00 installation | disposable runtime evidence | yes | Steam build `23660954` represented by both selected snapshots; game not controlled during research | Human starts the disposable run after plan approval. |
| x4-docs MCP | authoritative lookup gate | yes | game and runtime snapshots queried exhaustively | — |
| Clang | native rebuild if required | no command found | — | Rust MSVC toolchain may be sufficient; exact native build prerequisites must be checked before an implementation wave. [ASSUMED] |

Tool versions are retained from the prior local probes; x4-docs availability and snapshot state were reverified on 2026-09-16. [OBSERVED; VERIFIED: local command output and x4-docs responses]

**Missing dependencies with no fallback:** none for planning.

**Execution-time checks:** verify the native build toolchain only if the accepted plan changes Carrier B; the existing runtime artifact may avoid a rebuild. [INFERRED; ASSUMED]

## Sources

### Primary (HIGH confidence for repository contracts)

- `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md` — owner-approved scope and gates.
- `docs/ARCHITECTURE.md` — accepted core/detail, scheduling, bounds, and lifecycle architecture.
- `docs/architecture-decisions.md` — ADR-LG-021/022/023.
- `docs/architecture-verification.md` — VER-LG-003/011/012 evidence contracts.
- Current repository source files cited inline — exact implemented seams and discrete values.

### Primary X4 evidence

- x4-docs game snapshot `x4-9.00-steam-23660954`, root `blake3:8d30413b740369e8572d6a7647366be338421698596eee84d62fec0415685c79` — declarations and call shapes.
- x4-docs runtime snapshot `x4-probe-faction-ship-runtime-686318dd-v1`, root `blake3:01ea3b0d39afee3541a1c7e87ce2c1c8fc011ce688584966952c0d20e7a90c82` — observed runtime semantics and transfer lifecycle.

### Tertiary (LOW confidence)

- Claims listed in the Assumptions Log — planning recommendations that require fake-adapter, calibration, or disposable-X4 confirmation.

## Metadata

**Confidence breakdown:**

- Standard stack: HIGH — current repository source and accepted architecture were opened directly.
- Architecture: HIGH — Phase 05.3/05.4 contracts and ADR-LG-021/022/023 are explicit.
- X4 API/source selection: MEDIUM — exact registered declarations and runtime observations now exist, while several normalized field details remain unknown.
- Workload design: MEDIUM — required measurements and stability equations are explicit; numeric limits intentionally await calibration.
- Pitfalls: HIGH for repository contracts; MEDIUM for X4-specific behavior observed in one controlled runtime archive.

**Research date:** 2026-09-16
**Valid until:** 2026-09-23, or immediately invalidated by an X4 build change, a conflicting registered snapshot, or a changed Phase 05.5 decision.
