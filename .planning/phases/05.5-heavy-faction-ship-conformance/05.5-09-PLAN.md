---
phase: 05.5-heavy-faction-ship-conformance
plan: 09
type: execute
status: planned
wave: 2
depends_on: [05.5-01]
files_modified:
  - .planning/phases/05.5-heavy-faction-ship-conformance/05.5-SOURCE-EVIDENCE.md
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/src/ship_detail.rs
  - crates/observation-domain/src/ship_cargo.rs
  - crates/observation-domain/src/ship_crew.rs
  - crates/observation-domain/src/ship_loadout.rs
  - crates/observation-domain/src/ship_group.rs
  - crates/observation-domain/tests/ship_contract.rs
  - crates/x4-carrier-native/src/lua_ship_input.rs
  - crates/x4-carrier-native/src/ship_detail_input.rs
  - crates/x4-carrier-native/src/producer_types.rs
  - crates/x4-carrier-native/src/producer_collection.rs
  - crates/x4-carrier-native/src/producer_message.rs
  - crates/x4-carrier-native/src/producer_validation.rs
  - crates/x4-carrier-native/src/lua_input.rs
  - crates/x4-carrier-native/tests/abi_contract/ship_abi_contract.rs
  - crates/x4-carrier-native/tests/producer_contract/ship_stream.rs
  - crates/observation-ingest/src/scheduler.rs
  - crates/observation-ingest/src/scheduler_budget.rs
  - crates/observation-ingest/tests/scheduler_policy.rs
  - crates/x4-bridge/src/receiver_ship.rs
  - crates/x4-bridge/src/receiver_ship_detail.rs
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/production_limits.rs
  - crates/x4-bridge/src/production_ship_limits.rs
  - crates/x4-bridge/src/production_startup.rs
  - crates/x4-bridge/src/bin/validate_limits.rs
  - crates/x4-bridge/tests/ship_contract.rs
  - crates/x4-bridge/tests/ship_recovery.rs
  - crates/x4-bridge/tests/production_startup.rs
  - crates/x4-bridge/examples/carrier_b_ship_local/main.rs
  - crates/x4-bridge/examples/carrier_b_ship_local/peer.rs
  - crates/x4-bridge/examples/carrier_b_ship_local/readback.rs
  - extensions/live_galaxy/lua/live_galaxy_ship_source.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_collection.lua
  - extensions/live_galaxy/lua/live_galaxy_ship_details.lua
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_carrier.lua
  - extensions/live_galaxy/tests/ship_collection_contract.lua
  - extensions/live_galaxy/tests/ship_detail_contract.lua
  - extensions/live_galaxy/tests/carrier_b_ship_local.lua
  - extensions/live_galaxy/tests/run_contracts.ps1
  - tools/package-live-galaxy.ps1
  - tests/carrier-b-local.ps1
  - tests/heavy-ship-evidence.ps1
  - tests/fixtures/heavy-ship-source-contract.json
  - config/heavy-ship-experiment.json
  - docs/development/heavy-ship-probe.md
autonomous: true
requirements: [OBS-06, VAL-01, VAL-02, VAL-06, VER-LG-003, VER-LG-011, VER-LG-012]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - An ordinary faction's core and every required cargo, crew and actual-loadout detail use the existing production Lua to owned DLL to pipe to Rust persistence path.
    - Independent durable readback preserves distinct revisions, exact core and member dependencies, independent detail capture intervals and truthful outcome states.
    - The same validated finite profile bounds all actual consumers and supports a first owner calibration without claiming an upper bound on an uninterruptible native call.
    - Source-only preparation permits implementation but cannot pass runtime acceptance; all unperformed runtime checks remain pending.
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_ship_details.lua
      provides: Resumable production detail capture using source-backed getters
    - path: crates/observation-domain/src/ship_detail.rs
      provides: Exact dependency and typed detail contract
    - path: crates/x4-bridge/src/production_ship_limits.rs
      provides: Shared validated heavy profile and cross-limit invariants
    - path: docs/development/heavy-ship-probe.md
      provides: Executable production package instructions and finite experimental proposal
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_ship_details.lua
      to: crates/x4-carrier-native/src/ship_detail_input.rs
      via: Strict copied typed records through actual exported ABI
    - from: crates/x4-bridge/src/receiver_ship_detail.rs
      to: crates/x4-bridge/examples/carrier_b_ship_local/readback.rs
      via: Receiver-owned dependency validation and independently reopened durable revisions
    - from: config/heavy-ship-experiment.json
      to: tools/package-live-galaxy.ps1
      via: Exact validated profile materialized for Lua, DLL and bridge consumers
---

<objective>
Deliver the actual ordinary-faction heavy collector before runtime calibration.
Expand completed 01, not a second tracer or disposable collector. Implement D-01,
D-02, D-03, D-04, D-05, D-10, D-11, D-12, D-13, D-14, D-15 and D-16 locally;
actual runtime semantics and numerical acceptance stay pending until 10/11.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-EXECUTION-REVISION.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-RESEARCH.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-PATTERNS.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-01-SUMMARY.md
@docs/ARCHITECTURE.md
@docs/architecture-decisions.md
</context>

<interfaces>
Existing `ShipGroupDescriptor::new(source_scope, core_revision, policy_version,
members)` sorts lossless decimal ship IDs bytewise and rejects duplicates.
`FieldApplicability::{Applicable,NotApplicable(SourceEvidenceRef)}` and
`FieldOutcome::{Value,Zero,Empty,Absent,Unknown,Inaccessible,Unsupported,Stale}`
must stay distinct. `receiver_ship::validate` currently admits only selected
`ship_core`, one record per batch, and count-fill Partial/Unknown evidence.
Extend that owned seam explicitly; accepting an unknown detail key by falling
through is not validation. `ObservationScheduler` already owns enqueue,
deliver_pulse, stage_pending and complete with real-time permits/debt.
`ProductionLimits` is currently a strict 23-field clock profile: extend through
an explicit heavy profile without changing clock semantics or silently ignoring
new fields. Source structs use target `ffi.sizeof`/alignment, not packed guesses.
</interfaces>

## Artifacts this phase produces

New files are `ship_detail.rs`, `ship_cargo.rs`, `ship_crew.rs`, `ship_loadout.rs`,
`ship_detail_input.rs`, `receiver_ship_detail.rs`, `production_ship_limits.rs`,
`ship_recovery.rs`, `live_galaxy_ship_details.lua`, `ship_detail_contract.lua` and
`config/heavy-ship-experiment.json` at the full paths in frontmatter. New contracts:
`ShipDetailGroup`, `CargoObservation`, `CrewObservation`, `LoadoutObservation` and
`HeavyShipLimits`; group kind, exact parent/member revision, source evidence,
capture start/end and independent freshness fields; explicit experimental versus
accepted profile status. Add `heavy-ship-detail` and `heavy-ship-recovery` scenarios
to the existing local harness, and `-Preparation` versus `-RuntimeAcceptance` to
the existing evidence checker. These are planned names, not existing APIs.
Use narrow helper modules where necessary for the 200-line Rust limit; record
their exact paths as normal file-scope deviations. No package dependency is added.

## Execution contract

Before edits read root/shared AGENTS and the complete skill chain:
`.agents/skills/live-galaxy-code-conventions/SKILL.md`, its
`references/logging.md` and `references/tooling.md`;
`.agents/skills/live-galaxy-tests/SKILL.md` and its `references/test-standards.md`;
`.agents/skills/live-galaxy-rust-conventions/SKILL.md`;
`.agents/skills/live-galaxy-rust-tests/SKILL.md`;
`.agents/skills/live-galaxy-x4-integration/SKILL.md` and its
`references/lua-md-conventions.md`;
`.agents/skills/live-galaxy-x4-tests/SKILL.md`; `.agents/skills/lua/SKILL.md`.
Read the existing version of every file listed in the task before
modifying it; a new file is read through its named analog. Record assertion-level
RED, GREEN and REFACTOR for every behavior addition. The original 01 is the phase
tracer. This plan contains vertical expansions, not a replacement architecture.
No X4 run is permitted in this autonomous plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry source-faithful cargo groups through the real durable path</name>
<files>.planning/phases/05.5-heavy-faction-ship-conformance/05.5-SOURCE-EVIDENCE.md, docs/development/heavy-ship-probe.md, crates/observation-domain/src/lib.rs, crates/observation-domain/src/ship_detail.rs, crates/observation-domain/src/ship_cargo.rs, crates/observation-domain/src/ship_group.rs, crates/observation-domain/tests/ship_contract.rs, crates/x4-carrier-native/src/lua_ship_input.rs, crates/x4-carrier-native/src/ship_detail_input.rs, crates/x4-carrier-native/src/producer_types.rs, crates/x4-carrier-native/src/producer_collection.rs, crates/x4-carrier-native/src/producer_message.rs, crates/x4-carrier-native/src/producer_validation.rs, crates/x4-carrier-native/tests/abi_contract/ship_abi_contract.rs, crates/x4-carrier-native/tests/producer_contract/ship_stream.rs, crates/x4-bridge/src/receiver_ship.rs, crates/x4-bridge/src/receiver_ship_detail.rs, crates/x4-bridge/tests/ship_contract.rs, crates/x4-bridge/examples/carrier_b_ship_local/main.rs, crates/x4-bridge/examples/carrier_b_ship_local/peer.rs, crates/x4-bridge/examples/carrier_b_ship_local/readback.rs, extensions/live_galaxy/lua/live_galaxy_ship_source.lua, extensions/live_galaxy/lua/live_galaxy_ship_collection.lua, extensions/live_galaxy/lua/live_galaxy_ship_details.lua, extensions/live_galaxy/tests/ship_detail_contract.lua, extensions/live_galaxy/tests/carrier_b_ship_local.lua, tests/carrier-b-local.ps1, tests/heavy-ship-evidence.ps1, tests/fixtures/heavy-ship-source-contract.json</files>
  <read_first>Read existing production files in files, using ship_core.rs/ship_field.rs and receiver_ship.rs as analogs for new modules; completed 01 summary; 05.5-EXECUTION-REVISION.md integration instructions; 05.5-VALIDATION.md; docs/ARCHITECTURE.md accepted evidence and authority contracts. New modules and the four preparation artifacts are read after creation or integration during the action.</read_first>
  <precondition>Plan 01 is complete. Source-preparation integration is owned by this task, not an external prerequisite; runtime rows may remain pending.</precondition>
  <behavior>Cargo ware quantities, capacity and occupied volume round-trip without merging units/outcomes; mismatched exact parent, member, faction, policy or interval is rejected atomically; malformed/over-limit detail cannot replace valid core; two successive valid cargo groups remain independently readable after DB reopen; source preparation with pending runtime is accepted only by Preparation, never RuntimeAcceptance.</behavior>
  <action>First the execution lead owns the bounded selective integration described in 05.5-EXECUTION-REVISION.md: import the four useful preparation artifacts from retained commits cab157d/64b68b8, excluding old SUMMARY, STATE and halt metadata; record source revision and run checker self-tests. Read the integrated artifacts after this step, not before they exist. Preserve all 21 runtime rows as pending. Add RED proof separating implementation preparation from runtime acceptance before changing checker behavior. This is reuse of completed evidence, not new research or a second collector.

Per D-01, D-02, D-05 and D-11–D-13, start with a failing actual-chain cargo readback assertion and focused negative dependency tests. Add typed bounded cargo records to the existing structured Lua/native ABI and producer stream, using strict exact keys, immediate pointer copying, lossless IDs, finite numeric validation and no metatable/coercion acceptance. Use GetComponentData(component,cargo) keyed ware item quantities and source-backed storage transport count/fill for capacity/spaceused in cubic metres; validate count and target ABI allocation before fill, use returned count only, preserve transport dimensions and reservation distinctions. Store exact observed numeric values within verified representation limits; overflow/nonfinite/unsafe Lua precision is Unknown/rejection, not rounded success. Do not use per-ware item capacity as volume. Cargo-volume computation, if used, retains the explicit amount times unit-volume formula and evidence, never an unexplained number.

  Form deterministic bounded groups from the accepted core member set with exact core revision, member identities, policy identity and separate capture interval. The receiver independently checks all dependencies against its accepted core and validates each outcome/applicability/source-strength field before staging or publishing; stale/absent parent groups reject, detail failure preserves core. Keep count-fill source evidence Partial/Unknown unless stronger evidence actually exists; durable transfer completion does not imply source completeness, known-empty or deletion. Keep one immutable pending message and incremental record/digest construction, not whole-section encoded payloads or generic byte chunks.

  Extend the existing local Lua host scenario to invoke the actual built owned DLL, named pipe, Rust receiver and independently reopened SQLite. The only fake is the supplied X4 source, labelled as local fixture evidence. Add heavy-ship-detail with at least two revisions on the same connection across the existing availability interval, more than one bounded group, reverse/unordered IDs beyond Lua exact-number range, oversized nested collections and exact replay. Read earlier and current records, receipts and dependencies by independent persistence access, not producer counters. Separate checker source-preparation validity from runtime acceptance; preserve pending outcomes in the fixture and all negative provenance tests. This makes implementation possible without falsely approving a game run.</action>
  <reversibility rating="reversible">Versioned internal observation records extend the existing carrier and can be revised before public alpha; no destructive data migration.</reversibility>
  <acceptance_criteria>Actual cargo detail readback passes through the owned ABI/DLL/pipe/receiver. Invalid dependencies, malformed numeric input and exceeded bounds publish no partial replacement. Every cargo field/outcome/source distinction has an executable positive and negative assertion. Preparation accepts pending runtime but RuntimeAcceptance rejects it.</acceptance_criteria>
  <verify>
    <automated>cargo test --locked -p observation-domain --test ship_contract</automated>
    <fails_when>Non-zero exit or a cargo/dependency assertion is ignored.</fails_when>
    <automated>cargo build --locked -p x4-carrier-native</automated>
    <fails_when>Non-zero exit or the owned DLL is not produced.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-detail</automated>
    <fails_when>Non-zero exit, missing independently reopened cargo revisions, or a fake transport is used.</fails_when>
    <automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -SelfTest</automated>
    <fails_when>Non-zero exit or pending evidence passes the RuntimeAcceptance negative test.</fails_when>
  </verify>
  <done>Cargo is a tested production vertical expansion, with pending X4 runtime evidence clearly separate from local success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the same path to aggregate crew and actual installed loadout</name>
  <files>crates/observation-domain/src/lib.rs, crates/observation-domain/src/ship_detail.rs, crates/observation-domain/src/ship_crew.rs, crates/observation-domain/src/ship_loadout.rs, crates/observation-domain/tests/ship_contract.rs, crates/x4-carrier-native/src/ship_detail_input.rs, crates/x4-carrier-native/tests/abi_contract/ship_abi_contract.rs, crates/x4-bridge/src/receiver_ship_detail.rs, crates/x4-bridge/tests/ship_contract.rs, crates/x4-bridge/examples/carrier_b_ship_local/readback.rs, extensions/live_galaxy/lua/live_galaxy_ship_details.lua, extensions/live_galaxy/tests/ship_detail_contract.lua, extensions/live_galaxy/tests/carrier_b_ship_local.lua, extensions/live_galaxy/tests/run_contracts.ps1, tests/fixtures/heavy-ship-source-contract.json</files>
  <read_first>Read current files in files and Task 1's cargo pattern; integrated PeopleInfo/RoleTierData, installed slots, UIWareInfo/AmmoData and UnitData source claims; D-03/D-04/D-11/D-12; live-galaxy-x4-integration Lua/MD conventions and live-galaxy-x4-tests.</read_first>
  <behavior>All role counts and qualification tiers round-trip with raw units; absent pilot and empty crew remain distinguishable from unknown; physical, virtual, software, missile and every raw unit category are preserved; macro-only installed entries survive component zero; signed missile values are not cast to unsigned or silently discarded; unknown applicability cannot pass completeness.</behavior>
  <action>Per D-03–D-05 and D-11–D-13, add RED tests before extending the same production group path. Implement aggregate crew using GetNumAllRoles/GetPeople2 with the observed arriving selector explicitly recorded, preserving all role IDs rather than only UI-hireable roles. Capture GetRoleTiers from the returned per-role numtiers with raw signed threshold and unsigned count; keep normalization/display scales separate and do not assert unobserved runtime bounds. Capture GetPeopleCapacity with includepilot=true and encode that selector; label role-count coverage separately so a missing role/pilot is not fabricated as zero. Do not collect individual personnel/seed records.

  Enumerate actual installed engines, shields, weapons and turrets through current physical slot component/macro/group getters, with source-backed index conventions and exact size_t validation. Preserve macro-only installed pseudo-groups instead of treating component zero as absent. Preserve software current/max values and virtual slots with their distinct evidence; retain thruster interpretation as inferred where not proven. Never substitute a macro loadout template for installed state. Capture missile UIWareInfo quantities as signed values with provenance and unresolved semantics explicit; validate negative/unexpected values honestly, and retain raw evidence for runtime comparison with source-backed AmmoData semantics. Capture GetAllUnits(...,false) and preserve all raw macro/category counts, not only UI-filtered drone classes. Bound every inner count/allocation and resumable step, copy pointer-backed strings immediately and keep nested detail independently age-bounded.

  Extend heavy-ship-detail to assert every required field, changing data over multiple revisions, zero/empty/absent/NA/stale/inaccessible/unknown/unsupported distinctions, wrong parent/member rejection, and independent earlier/current readback. Use source-proven NA only; an unsupported applicable field is a runtime-closure blocker, not a passing reduction. Source-resolvable implementation discrepancies go to a narrow exact Docs MCP lookup with terminal pagination, not an owner research assignment. Leave runtime-only semantic assertions pending for the production calibration in 10.</action>
  <acceptance_criteria>Every D-03/D-04 field family traverses the same actual local chain and is independently checked after persistence reopen. No individual crew collection, template substitution, hidden drone filtering or signed-to-unsigned coercion exists in the changed paths. Source/runtime uncertainty remains typed and visible.</acceptance_criteria>
  <verify>
    <automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/ship_detail_contract.lua</automated>
    <fails_when>Non-zero exit or any required getter/outcome assertion is pending or ignored.</fails_when>
    <automated>cargo test --locked -p x4-bridge --test ship_contract</automated>
    <fails_when>Non-zero exit or malformed crew/loadout groups gain durable authority.</fails_when>
    <automated>cargo build --locked -p x4-carrier-native</automated>
    <fails_when>Non-zero exit or the DLL is stale relative to changed native input code.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-detail</automated>
    <fails_when>Non-zero exit or reopened crew/loadout values differ from the supplied local source and exact dependencies.</fails_when>
  </verify>
  <done>All required detail families work locally through production; the owner is not asked to run an unimplemented detail collector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make sustained collection bounded, recoverable and ready for a finite owner experiment</name>
  <files>crates/observation-ingest/src/scheduler.rs, crates/observation-ingest/src/scheduler_budget.rs, crates/observation-ingest/tests/scheduler_policy.rs, crates/x4-bridge/src/production_runtime.rs, crates/x4-bridge/src/production_limits.rs, crates/x4-bridge/src/production_ship_limits.rs, crates/x4-bridge/src/production_startup.rs, crates/x4-bridge/src/bin/validate_limits.rs, crates/x4-bridge/tests/production_startup.rs, crates/x4-bridge/tests/ship_recovery.rs, crates/x4-carrier-native/src/producer_types.rs, crates/x4-carrier-native/src/lua_input.rs, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/lua/live_galaxy_carrier.lua, extensions/live_galaxy/lua/live_galaxy_ship_collection.lua, extensions/live_galaxy/tests/ship_collection_contract.lua, extensions/live_galaxy/tests/carrier_b_ship_local.lua, tests/carrier-b-local.ps1, tools/package-live-galaxy.ps1, config/heavy-ship-experiment.json, docs/development/heavy-ship-probe.md</files>
  <read_first>Read current files in files; existing ObservationScheduler, scheduler_budget and clock profile startup/package tests; Plan-01 producer recovery and actual-chain example; 05.5-RESEARCH performance/recovery sections; 05.5-VALIDATION edge matrix; docs/ARCHITECTURE.md responsibility and durability contracts.</read_first>
  <behavior>Slow consumers cannot create an unbounded backlog; control pumping precedes source calls; callbacks and heavy native work consume real-time budget with debt; limit disagreement rejects before collection; response loss/restart never duplicates publication; a changed core invalidates dependent detail; an uninterruptible call is measured after return without a false timeout claim.</behavior>
  <action>Per D-05, D-10 and D-13–D-16, integrate detail intents into the existing external Rust ObservationScheduler and bridge production loop. Lua owns frame-safe resumable slicing and final admission; the DLL owns copied data, encoding and transport, not a second global scheduler or background game getter. Preserve pump-first order, bounded heavy permits, token rate/burst/overrun debt and deterministic fair rotation across core and detail groups. Charge actual work, freeze/backward-clock handling, cumulative call/allocation/record/byte/work limits and age; stalled or absent feedback suppresses new source work. Do not retain an unbounded all-ships detail table or pre-encode a section.

  Add one strict HeavyShipLimits profile consumed by bridge startup, receiver staging/publication, generated packaged Lua and native open/admission. Include native count/allocation/call ceilings, callback budget, heavy permits, group/record/message bounds, candidate and aggregate raw/decoded budgets, pending bytes, attempts, age/inactivity, refresh/freshness and durable retention bounds. Reject zero/overflow/cross-limit inconsistency, unknown fields and unsupported policies before source work. Expand existing native policies that heavy collection genuinely needs; do not pass accepted configuration which the DLL ignores. Keep clock profile compatibility. Log bounded stage duration/rate/allocation/backlog counters and structured stop/recovery reasons with package/profile/run identity; no raw private payloads. Test every profile consumer independently against the same fixture and materialize the exact validated identity in the package.

  Add heavy-ship-recovery to the actual-chain harness: failure before core completion, midway through each detail family, after DLL acceptance before pipe delivery, after Received, after durable commit before response, bridge absence, reconnect, fresh producer process, load/reload, owner/location change and stale callback/parent. Reconcile exact durable outcomes only, discard incomplete captures, require fresh qualification/incarnation and never promote prior history into current data. Independently reopen earlier/current revisions after each relevant restart; bound retention without deleting referenced parents or replay evidence.

  Prepare the first nonzero experimental profile and executable run procedure, not a runtime success claim. Start with one selected ordinary faction, one sampled ship per detail group, one heavy native call admitted per delivered callback, one attempt and a finite admission window. Set concrete count/allocation/byte/window values from target ABI sizes and local boundary tests before handoff; include both per-call and aggregate limits, and show the arithmetic. The initial membership count/fill may need the whole faction array; explicitly cap it and refuse an over-cap source rather than call a prefix complete. The profile must permit a useful nonzero measurement and must not require prior X4 timings. The document must state that an already-entered synchronous call has unknown duration and cannot be interrupted by this timer; owner abort/recovery steps and residual freeze risk are explicit. Agent builds and locally verifies the package, records exact digests and commands, and performs focused code/security inspection of changed native/input/bounds paths before presenting it. Include calibration cohort expansion, normal/SETA measurement fields, fixed owner-operated actions and evidence retention instructions. No game launch or owner approval is performed here.</action>
  <acceptance_criteria>All configured limits reach and constrain the actual consumer; no silent default/profile drift. Recovery assertions pass on the actual DLL/pipe path and independent readback. The package/run document contains finite nonzero proposed values, concrete invocation, stop conditions, profile/digest identity and honest non-preemption warning. Runtime evidence remains pending.</acceptance_criteria>
  <verify>
    <automated>cargo test --locked -p observation-ingest --test scheduler_policy</automated>
    <fails_when>Non-zero exit or starvation, debt, frozen-clock or budget-bound assertions fail.</fails_when>
    <automated>cargo test --locked -p x4-bridge --test production_startup --test ship_recovery</automated>
    <fails_when>Non-zero exit, an invalid profile starts collection, or restart changes committed identity/content.</fails_when>
    <automated>cargo build --locked -p x4-carrier-native</automated>
    <fails_when>Non-zero exit or the real DLL cannot be rebuilt.</fails_when>
    <automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-recovery -LimitsFile config/heavy-ship-experiment.json</automated>
    <fails_when>Non-zero exit or independent history/current readback, bounded backlog or exact replay assertions fail.</fails_when>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/x4-package-conformance.ps1</automated>
    <fails_when>Non-zero exit or package dependencies/runtime entrypoints are invalid.</fails_when>
    <automated>cargo run --locked -p source-size-lint -- crates</automated>
    <fails_when>Non-zero exit or any Rust file exceeds the project size limit.</fails_when>
  </verify>
  <done>A locally verified production package can collect all required ordinary-faction detail with bounded state and recoverable durable publication; only owner-run measurement and numerical acceptance remain.</done>
</task>
</tasks>

<threat_model>
Boundary: untrusted getter values → Lua/owned native ABI → pipe → receiver → durable
history. High-severity tampering/spoofing is mitigated by strict typed keys,
lossless identity and exact receiver-owned dependencies (tasks 1–2). High-severity
denial of service is mitigated by independent count/byte/work/age/callback bounds,
one pending message and measured non-preemptible call risk (task 3). High-severity
replay/repudiation is mitigated by durable receipts and independently reopened
restart tests (task 3). Information disclosure is mitigated by sanitized bounded
diagnostics and no recipient/Mind grant. Supply-chain change is not planned; if a
dependency becomes necessary, stop for the package-legitimacy gate before install.
</threat_model>

<verification>
Run focused RED/GREEN tests while implementing, then cargo fmt --all -- --check,
cargo clippy --locked --workspace --all-targets -- -D warnings, cargo test --locked
--workspace -- --test-threads=1, Lua contract suites and existing actual-chain clock
regression. Record local fake-source versus real-DLL evidence separately. Final
phase-wide review, mutation and regression are owned by 11, not silently waived.
</verification>

<success_criteria>
Actual local chain and independent durable readback cover all D-01–D-05 field
families, bounded/recoverable operation and the executable calibration package.
Neither this plan nor its summary claims X4 runtime conformance or approved limits.
</success_criteria>

<output>Create .planning/phases/05.5-heavy-faction-ship-conformance/05.5-09-SUMMARY.md with implementation identity, RED/GREEN evidence, package/profile digest, exact runnable commands and pending owner actions.</output>
