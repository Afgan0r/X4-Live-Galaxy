---
phase: 05.5-heavy-faction-ship-conformance
plan: 01
subsystem: observation
tags: [rust, lua, carrier-b, ship-core, tdd]
requires:
  - phase: 05.4-owned-carrier-b-and-production-observation-path
    provides: Owned native producer, Lua ABI, named-pipe transport and publication path
provides:
  - Validated lossless ship-core and deterministic group domain contracts
  - Partial bounded native ship stream and structured Lua ABI seams
  - Committed RED evidence for the resumable faction collector
affects: [05.5-heavy-faction-ship-conformance]
actuals:
  tokens: 21135
  tasks: 1
  commits: 7
plan_head_before: f9396ac42f32e2445dbdecc471a42ec49e23e7d0
tech-stack:
  added: []
  patterns: [lossless decimal ship identity, typed field outcomes, deterministic groups]
key-files:
  created:
    - crates/observation-domain/src/ship_core.rs
    - crates/observation-domain/src/ship_field.rs
    - crates/observation-domain/src/ship_group.rs
    - crates/observation-domain/tests/ship_contract.rs
    - crates/x4-carrier-native/src/producer_feedback_codec.rs
    - crates/x4-carrier-native/src/producer_validation.rs
    - crates/x4-carrier-native/src/lua_producer_context.rs
    - crates/x4-carrier-native/src/lua_producer_test_support.rs
    - crates/x4-carrier-native/src/lua_ship_input.rs
    - crates/x4-carrier-native/tests/producer_contract/ship_stream.rs
    - crates/x4-carrier-native/tests/producer_contract/ship_support.rs
  modified:
    - crates/x4-carrier-native/src/producer_collection.rs
    - crates/x4-carrier-native/src/producer_message.rs
    - crates/x4-carrier-native/src/producer_feedback.rs
    - crates/x4-carrier-native/src/producer_recovery.rs
    - crates/x4-carrier-native/src/lua_input.rs
    - crates/x4-carrier-native/src/lua_producer_operations.rs
    - crates/x4-carrier-native/src/lua_progress.rs
    - extensions/live_galaxy/lua/live_galaxy_carrier.lua
    - extensions/live_galaxy/tests/carrier_b_contract.lua
key-decisions:
  - Do not infer faction API declarations or argument order from station APIs.
  - Halt without claiming the partial native and ABI seams are a complete tracer.
patterns-established:
  - Ship identifiers remain canonical decimal strings above Lua's exact numeric range.
requirements-completed: []
coverage:
  - id: D1
    description: Validated lossless ship core, distinct field outcomes and deterministic bounded groups
    verification:
      - kind: unit
        ref: cargo test --locked -p observation-domain --test ship_contract
        status: pass
    human_judgment: false
duration: 30 min
completed: 2026-09-16
status: halted
---

# Phase 05.5 Plan 01: Ship Core Tracer Halted Summary

Lossless typed ship-core contracts are verified; native stream and ABI seams are partially implemented, and the faction collector remains at RED.

## Performance

- Started: 2026-09-16T06:41:11Z (first task commit timestamp)
- Completed: 2026-09-16T07:11:20Z
- Duration: 30 min
- Fully accepted tasks: 1 of 5
- Files changed before metadata: 31
- Production/TDD commits measured from the plan ledger: 7

## Accomplishments

- Task 1 provides validated identity, owner, type/class and location values, distinct field outcomes and deterministic groups with duplicate rejection.
- Partial native ship streaming and copied ABI input/status code pass the focused Rust checks, but the acceptance gaps below prevent counting Tasks 2 and 3 as complete.

## Task Status and Commits

| Task | Status | RED | GREEN |
| --- | --- | --- | --- |
| 1: Domain contract | Complete | `3cdaa80` | `d6f8529` |
| 2: Native stream | Partial; acceptance gaps remain | `ff69c6f` | `dbdbc97` |
| 3: Structured Lua ABI | Partial; operation-level proof remains | `7885637` | `15ed6d4` |
| 4: Resumable faction collector | RED only; source prerequisite missing | `ac7a875` | Not implemented |
| 5: Receiver and real local tracer | Not started | None | None |

All commits ran normal hooks. No force-add, hook bypass, stash, reset or protected-ref rewrite was used. STATE.md and ROADMAP.md were not modified. Requirements were not marked complete; the required frontmatter array is copied from the plan and does not override `status: halted`.

## TDD Gate Compliance

Each committed RED target failed on its planned assertion and its persisted evidence returned `RED_EVIDENCE_OK`:

- `05.5-01-task1-red.json`: deterministic group ordering.
- `05.5-01-task2-red.json`: admission of multiple complete ship records.
- `05.5-01-task3-red.json`: progress must return profile and remaining capacity, not six values.
- `05.5-01-task4-red.json`: selected-faction count/fill/core trace was empty in the clock-only collector.

Tasks 1 through 3 have GREEN commits. Task 4 intentionally remains failing; no GREEN or end-to-end success is claimed. No refactor-only commit was needed.

## Verification Evidence

Latest available checks passed:

- `cargo fmt --all -- --check`.
- `cargo test --locked --workspace` (existing ignored tests remain ignored; none were added or skipped by this plan).
- `cargo clippy --locked --workspace --all-targets -- -D warnings`.
- `cargo test --locked -p observation-domain --test ship_contract`: 3 passed.
- `cargo test --locked -p x4-carrier-native --lib`: 6 passed.
- Native `abi_contract`, `producer_contract`, `lifecycle_contract`: 7, 11 and 5 passed.
- Native `producer_recovery_contract` and `producer_receipt_contract`: 8 and 1 passed.
- All native Rust source/test files are at most 200 lines after mechanical helper splits.
- Busted carrier contract before Task 4 RED: 24 passed.
- Task 4 focused Busted RED: 0 successes, 1 planned assertion failure, 0 errors.
- `git diff --check` passed.

The worktree has no ignored Lua cache. The existing locked Busted launcher in the main checkout was executed with the worktree as cwd and worktree-relative module paths; no package was installed or updated. The literal worktree-relative launcher command is therefore unavailable until that cache is materialized, but the same provisioned runner executed the worktree tests.

Task 5 commands were not run: `cargo test --locked -p x4-bridge --test ship_contract` and the `heavy-ship-core` local scenario do not yet exist. The unrun end-to-end gate is recorded as open entry 13 in `.planning/WINDOWS.md`.

## Blocking Source Prerequisite

The plan requires exact registered faction API declarations and argument order before implementing the real adapter. Research explicitly says not to infer these from station APIs.

The x4-docs lookup gate was executed against the two approved snapshots. The game snapshot returned no entities for `GetAllFactions`, `GetAllFactionShips` or `GetNumAllFactions`. Complete pagination of the runtime entities yielded:

| Runtime entity | Pages | Claims | Observed calls | Source excerpts |
| --- | --- | --- | --- | --- |
| `C.GetAllFactions` | 1 | 5 | 0 | 0 |
| `C.GetNumAllFactionShips` | 6 | 45 | 0 | 0 |
| `X4.faction_ship.runtime` | 4 | 31 | 0 | 0 |

Runtime snapshot: `x4-probe-faction-ship-runtime-686318dd-v1`, root `blake3:01ea3b0d39afee3541a1c7e87ce2c1c8fc011ce688584966952c0d20e7a90c82`. It proves recorded outcomes, not an exposed FFI declaration. A narrow read-only search of installed `x4_live_mcp` also found no call shape.

Continuation needs the registered probe adapter/declaration source, or an updated Docs snapshot exposing exact `C.GetAllFactions`, `C.GetNumAllFactionShips` and `C.GetAllFactionShips` signatures and arguments. No X4 instance was launched or controlled, and no save was read.

## Acceptance Gaps Requiring Continuation

1. Task 2 currently retains all bounded records and preassembled batch messages in `SectionMessages`. It does not yet implement the required single bounded builder plus one immutable pending message with collection capacity reopened after each exact receipt. Focused tests passing does not close this architectural requirement.
2. Task 2 tests need explicit load/reload/source-uncertainty/stale-generation coverage and fresh-baseline assertions for the ship profile, beyond the existing reconnect contracts.
3. Task 3 needs operation-level ship-table ABI tests for extra/missing keys, numeric/nonfinite IDs, incompatible profiles and mutation after return. Current tests prove owned field construction and progress shape, not all actual table operations.
4. `lua_open.rs` still restricts opening to the clock bring-up limits and source scope. Approved configured heavy-profile opening must be implemented before the real Lua-to-DLL ship tracer can run.
5. Ship begin parsing must validate and preserve the supplied epoch/boundary values, rather than merely allowlisting their keys.
6. Task 4 production collector and its churn, ownership/location, allocation/work/age and pump-order cases remain unimplemented.
7. Task 5 strict receiver admission, multi-revision durable readback/replay and actual local data path remain unimplemented.

## Deviations from Plan

### Mechanical Helper Splits

The 200-line constraint required `producer_feedback_codec.rs`, `producer_validation.rs`, `lua_producer_context.rs`, `lua_producer_test_support.rs`, `ship_stream.rs` and `ship_support.rs`. These preserve existing responsibilities and dependencies.

### Halt

Execution stopped after Task 4 RED because exact required source call shapes were not exposed by available registered evidence. Tasks 2 and 3 also retain the acceptance gaps above; they are not silently waived. No product scope, architecture decision or source-authority guarantee was changed.

## Issues Encountered

- Worktree-local Lua cache was absent; the already provisioned locked runner was reused without installation.
- The first Task 3 RED attempt had an incomplete test fixture initializer and was invalid. It was repaired before the intentional assertion failure was verified and committed.
- AST index rebuild reported access denied; narrow known-file reads were used. No compiler-semantic claim was made from the index.

## User Setup Required

No credentials or service setup is needed. The orchestrator must supply the exact registered adapter/declaration evidence before source implementation resumes.

## Next Phase Readiness

Not ready for Plan 02 or phase completion. Resume Plan 01 with the source prerequisite and all acceptance gaps above. Heavy field/runtime evidence remains pending, and all three descriptor-less prohibition backstops remain reserved for Plan 08 judgment. Local tests do not establish observed-in-X4 behavior, membership completeness, deletion authority or runtime calibration.

## Convention and Reference Files Read

- `.agents/skills/live-galaxy-code-conventions/SKILL.md`
- `.agents/skills/live-galaxy-code-conventions/references/logging.md`
- `.agents/skills/live-galaxy-code-conventions/references/tooling.md`
- `.agents/skills/live-galaxy-tests/SKILL.md`
- `.agents/skills/live-galaxy-tests/references/test-standards.md`
- `.agents/skills/live-galaxy-rust-conventions/SKILL.md`
- `.agents/skills/live-galaxy-rust-tests/SKILL.md`
- `.agents/skills/live-galaxy-x4-integration/SKILL.md`
- `.agents/skills/live-galaxy-x4-integration/references/lua-md-conventions.md`
- `.agents/skills/live-galaxy-x4-tests/SKILL.md`
- `.agents/skills/lua/SKILL.md`
- `C:/Users/pavlo/.agents/skills/game-repo-standard/SKILL.md`
- `C:/Users/pavlo/.agents/skills/game-repo-standard/references/profiles-and-registry.md`
- `C:/Users/pavlo/.agents/skills/game-repo-standard/references/memory-lifecycle.md`
- `C:/Users/pavlo/.agents/skills/git-commit/SKILL.md` before every commit.

## Self-Check: PASSED

All named created code/evidence files and seven listed commits exist. The initial base and commit count match the persisted plan ledger. The working-tree halt handoff is explicit; this self-check does not claim plan success. STATE.md and ROADMAP.md match the initial tree. No unplanned endpoint, authentication path or effect-bearing authority was introduced.
