---
phase: 05.5-heavy-faction-ship-conformance
plan: 07
type: tdd
wave: 7
depends_on: [05.5-06]
files_modified:
  - crates/x4-carrier-native/src/producer_types.rs
  - crates/x4-carrier-native/src/lua_input.rs
  - crates/x4-carrier-native/src/producer_message.rs
  - crates/x4-bridge/src/production_runtime.rs
  - crates/x4-bridge/src/receiver_context.rs
  - extensions/live_galaxy/lua/live_galaxy_runtime.lua
  - extensions/live_galaxy/lua/live_galaxy_observation.lua
  - extensions/live_galaxy/lua/live_galaxy_factions.lua
  - extensions/live_galaxy/tests/carrier_b_contract.lua
  - extensions/live_galaxy/tests/faction_observation_contract.lua
  - tests/carrier-b-local.ps1
  - extensions/live_galaxy/tests/carrier_b_local.lua
  - crates/x4-bridge/tests/ship_contract.rs
  - crates/x4-bridge/tests/ship_recovery.rs
  - tests/heavy-ship-evidence.ps1
autonomous: true
requirements: [OBS-06, VAL-01, VAL-06, VER-LG-003, VER-LG-011, VER-LG-012]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - After accepted Gate A, all dynamically discovered independent vanilla/DLC factions including small/pirate factions and XEN/KHK use the same bounded observation path.
    - Every observed faction survives into a durable evidence-backed disposition; unknown blocks closure.
    - Aggregate scheduling, staging, recovery and retained revision readback remain bounded across factions.
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_factions.lua
      provides: Dynamic census driving bounded admitted collection rounds
    - path: crates/x4-bridge/tests/ship_recovery.rs
      provides: Cross-faction isolation and durable recovery matrix
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_factions.lua
      to: crates/x4-bridge/src/receiver_context.rs
      via: Typed census evidence and independently validated observation dispositions
    - from: extensions/live_galaxy/lua/live_galaxy_observation.lua
      to: crates/x4-carrier-native/src/ship_schedule.rs
      via: Existing bounded round frontier and per-faction core/detail intents
---

<objective>
Expand the accepted heavy ordinary-faction path to the full admitted observation set per D-06, D-07, D-08, D-09 and D-10. This is expansion of the shared architecture, not a new Faction Mind feature or a visibility policy change. Local completion here does not substitute for Gate B.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-CONTEXT.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-RESEARCH.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-PATTERNS.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-04-SUMMARY.md
@.planning/phases/05.5-heavy-faction-ship-conformance/05.5-06-SUMMARY.md
@tests/x4-disposable/05.5-heavy-ship-evidence.md
@config/carrier-b-limits.json
</context>

## Artifacts this phase produces

No new source paths are prescribed. Extend the existing typed observation profile with `FactionCensusObservation`, census revision and source-evidence fields, an internal `full_admitted_set` collection scope, and per-faction disposition/required-field coverage rows. Extend local scenarios with `heavy-ship-full-set`. This scope is selected explicitly only after the accepted Gate A artifact; it does not become a gameplay feature or a second scheduler. Every raw ID, origin proof, disposition, reason and source strength is durably inspectable. No hard-coded faction count, faction-name allowlist, recipient grant or mind-admission flag is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit durable census evidence and isolated per-faction demand</name>
  <files>crates/x4-carrier-native/src/producer_types.rs, crates/x4-carrier-native/src/lua_input.rs, crates/x4-carrier-native/src/producer_message.rs, crates/x4-bridge/src/production_runtime.rs, crates/x4-bridge/src/receiver_context.rs</files>
  <read_first>Read accepted Gate A evidence, Plan 04 classifier, typed section input and current receiver-owned demand/revision authority. Read observation visibility and Faction Mind boundaries in ARCHITECTURE.</read_first>
  <precondition>Gate A is explicitly accepted for the current source and calibrated policy; pending, failed or identity-mismatched evidence blocks this task.</precondition>
  <behavior>Every raw faction ID has exactly one disposition per census revision. Unknown origin/conflicting evidence blocks closure, not all valid core publication. Cross-faction section or dependency substitution rejects. Census count/fill-only evidence never establishes authoritative absence or deletion.</behavior>
  <action>RED: add focused native/receiver tests for canonical census transport, duplicate/conflicting IDs, unseen DLC IDs, XEN/KHK, small/pirate, excluded player/mod/service entries, unknown origin, scope substitution and old/current census revision replay. GREEN: carry typed census evidence through the same bounded native framing and durable receiver publication. Recompute/validate dispositions with the domain classifier and approved source evidence rather than trusting a sender's included boolean. Maintain per-faction core/detail keys, demand and revision floors; bind group dependencies to their exact faction/member/core identity. Persist all raw IDs and dispositions for closure readback while treating collection eligibility as separate from mind participation and recipient visibility. Require census evidence before full-admitted-set demand; unknown entries remain visible blockers. Keep valid per-faction publications isolated when another faction fails. Reject aggregate limit exhaustion honestly; never truncate the mandatory set or relabel overflow as excluded. REFACTOR narrow helpers as required by the 200-line limit.</action>
  <reversibility rating="reversible">Census is another typed observation section within existing identity and publication semantics.</reversibility>
  <acceptance_criteria>Raw-ID/disposition coverage is durable; receiver tests reject cross-faction authority substitution and sender-only classification; XEN/KHK inclusion does not create minds; aggregate failures cannot look complete.</acceptance_criteria>
  <verify><automated>cargo test --locked -p x4-carrier-native --lib</automated><fails_when>Exit is nonzero, census input loses IDs, or byte/work limits are bypassed.</fails_when><automated>cargo test --locked -p x4-bridge --lib</automated><fails_when>Exit is nonzero, sender classification becomes receiver authority, or per-faction scope/revision isolation fails.</fails_when></verify>
  <done>The receiver can preserve and validate a full census before collecting its admitted scopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive full admitted rounds through the single real source scheduler</name>
  <files>extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/lua/live_galaxy_observation.lua, extensions/live_galaxy/lua/live_galaxy_factions.lua, extensions/live_galaxy/tests/carrier_b_contract.lua, extensions/live_galaxy/tests/faction_observation_contract.lua</files>
  <read_first>Read the dynamic faction module, Plan 04 bounded round scheduler, approved profile and current source/detail cursors. Preserve existing runtime event registration.</read_first>
  <behavior>An unlisted independent DLC faction is discovered and collected. XEN/KHK and small/pirate factions traverse the identical core/detail applicability pipeline. Unknown remains in the evidence ledger. A busy faction cannot starve finite-round peers or cause unbounded queue growth. Pending transport always precedes the next source step.</behavior>
  <action>RED: extend real-module fake-C tests with changing raw faction count, full mandatory categories, zero-ship faction, oversized faction, count/fill churn, incomplete details, load mid-round, source error and stable equal-priority progression. GREEN: wire full_admitted_set into the existing runtime and observation cursors. Enumerate C.GetAllFactions dynamically and C.GetNumAllFactionShips/C.GetAllFactionShips per admitted faction. GetContainedObjectsByOwner remains reconciliation-only. Use the existing bounded frontier, heavy permits/debt and measured profile; advance each faction's deterministic core then dependent detail groups without a new callback or game-time token source. Preserve exclusion reasons and unknown blockers. A zero result is observed empty at count/fill-only strength, not exclusion or authoritative known-empty. On count/fill mismatch discard only the attempt, retry/yield under finite limits and mark exhausted scope honestly. Full-set mode is not the default before Gate A; tests assert explicit mode selection and accepted-gate prerequisite. Do not create reconnaissance, scouting, reports, player controls or faction actions. REFACTOR only current source responsibilities.</action>
  <reversibility rating="reversible">Workload scope selection reuses existing read-only collectors and can return to single-faction mode without data loss.</reversibility>
  <acceptance_criteria>Every admitted dynamic ID gets bounded progress; unknown is preserved; no hard-coded faction count or strategic feature is needed; accelerated game-time input cannot increase real-time collection credit.</acceptance_criteria>
  <verify><automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/carrier_b_contract.lua extensions/live_galaxy/tests/faction_observation_contract.lua</automated><fails_when>Exit is nonzero, any mandatory category is skipped, a faction starves within the bounded round, or a new source call precedes pending transport.</fails_when></verify>
  <done>Full admitted workload is wired to the same real source path with no new authority or scheduler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove full-set local transport, isolation and recovery before runtime Gate B</name>
  <files>tests/carrier-b-local.ps1, extensions/live_galaxy/tests/carrier_b_local.lua, crates/x4-bridge/tests/ship_contract.rs, crates/x4-bridge/tests/ship_recovery.rs, tests/heavy-ship-evidence.ps1</files>
  <read_first>Read the existing heavy local scenarios, Gate A limits and source distinctions, and the full-set census/closure contract. Reuse real DLL/pipe/production receiver/readback rather than a fake producer.</read_first>
  <behavior>Multiple factions and dependent groups commit over one connection, reconcile durable outcomes across a new process/connection and remain independently readable after reopening storage. Known-retryable transport-local outcomes within the same qualified connection/source boundary replay exact bytes without getters. Response loss reconciles the exact durable receipt without duplicate publication; reconnect/load/reload, source uncertainty or stale connection generation discards incomplete source work and requires fresh baseline and collection per inherited Phase 05.4 D-06 and Plan 04. Full-set closure rejects one missing faction, unknown classification, one applicable missing field or an expired current dependency.</behavior>
  <action>RED: add heavy-ship-full-set with synthetic external source fixtures covering all categories and at least two revisions; introduce negative fixtures with one missing admitted ID, cross-faction identity, unsupported applicable loadout, stale detail, reordered equal keys and duplicate/malformed evidence. GREEN: drive the real Lua loader, native module, named pipe, production receiver and SQLite; assert candidate/message/aggregate/retention bounds using the approved profile, independent per-stage counters, no unbounded catch-up, and exact earlier/current readback. Inject delayed staging, lost completion response, reconnect, bridge restart and pending-I/O unload with deterministic controls. Preserve the existing pending-io-unload scenario. Extend evidence validation to compare the complete discovered-ID set with included/excluded/unknown rows and every included faction's field matrix; unknown blocks closure, and source strength remains separate from downstream publication success. Tests can prove the checker rejects a false Gate B, but cannot set Gate B accepted. REFACTOR shared harness mechanics instead of duplicating transport implementations.</action>
  <reversibility rating="reversible">Local deterministic scenarios do not modify a game installation or save.</reversibility>
  <acceptance_criteria>The complete dynamic-set local path and all negative closure cases pass; same-boundary transport-only retry leaves getter count unchanged; reconnect and source-boundary scenarios discard incomplete work, reconcile receipts without duplicate publication and require fresh baseline and collection; earlier/current revisions survive restart; final game evidence remains explicitly pending.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario heavy-ship-full-set</automated><fails_when>Exit is nonzero, the real carrier path is bypassed, a required ID/field disappears, or configured bounds are exceeded.</fails_when><automated>pwsh -NoProfile -File tests/carrier-b-local.ps1 -SelfTest -Scenario pending-io-unload</automated><fails_when>Exit is nonzero or pending I/O unload/reinitialization regresses.</fails_when><automated>cargo test --locked -p x4-bridge --test ship_contract --test ship_recovery</automated><fails_when>Exit is nonzero, durable cross-faction isolation fails, or earlier/current readback/replay assertions fail.</fails_when><automated>pwsh -NoProfile -File tests/heavy-ship-evidence.ps1 -SelfTest</automated><fails_when>Exit is nonzero or a missing/unknown faction, missing applicable field or unperformed runtime run is accepted.</fails_when></verify>
  <done>The full-set candidate is ready for review and the owner-operated final runtime gate.</done>
</task>

<verification>
The accepted Gate A artifact is a real precondition, not optional context. Check it before any full-set source expansion. Local full-set success establishes bounded architecture, not runtime coverage of the installed vanilla/DLC population.
</verification>

<success_criteria>
The production path covers the full dynamically admitted observation set locally, preserves every disposition and all source/applicability distinctions, and remains bounded/recoverable without changing strategic participation or recipient authority.
</success_criteria>

<output>Create `.planning/phases/05.5-heavy-faction-ship-conformance/05.5-07-SUMMARY.md` with full-set local evidence and the explicitly pending Gate B runtime rows.</output>
