# Phase 05.5 execution replan

## Authority and reason

Owner-requested replan on 2026-09-16, against main `954b5f1`.
Plan 01 is completed and unchanged. Its real Lua/owned-DLL/pipe/bridge tracer,
independent SQLite readback and recovery are the production starting point.
Old 02 demanded detail runtime observations before old 03 implemented the detail
collector. This circular dependency is removed, not converted into another probe.
Research informs implementation; owner runtime measurements follow locally verified
production implementation. There is one collector and one existing scheduling loop.

Remaining execution is three cohesive vertical plans, each with at most three tasks.
Cross-layer tasks deliberately exceed the generic five-file heuristic: splitting
types, limits or packaging into separate execution plans would recreate horizontal
dependencies, not deliver another usable outcome. Preserve the Rust 200-line rule
through narrowly named helpers within the owning crate, not new architecture.

| Active ID | Wave | Delivered result | Owner involvement |
| --- | --- | --- | --- |
| 05.5-01 | 1, completed | Production core tracer | No repeated work |
| 05.5-09 | 2 | All ordinary-faction detail groups, bounded/recoverable production path and executable calibration package | None until package is ready |
| 05.5-10 | 3 | Finite calibration, approved limits and Gate A; full dynamic-faction production expansion verified locally | Approve experimental risk, operate calibration, approve measured thresholds, operate Gate A |
| 05.5-11 | 4 | Converged reviews/regression, Gate B and honest goal verification | Operate normal/SETA Gate B; resolve only genuine product conflicts |

02–08 have `status: superseded`; their text remains historical. Their summaries,
partial work or wave numbers are not completion evidence for 09–11. The execution
lead must select active IDs explicitly if its plan-discovery implementation does
not filter this status. Do not run the superseded plans or infer completion from
`05.5-02-SUMMARY.md`. STATE and requirements are deliberately not rewritten by this
planning task; the lead reconciles execution routing at the approved handoff.

## Plan 09 Task 1 integration ownership

Preparation from commits `cab157d` and `64b68b8` is unmerged at this replan. The
isolated old-02 checkout is evidence, not an execution dependency. At the beginning
of Plan 09 Task 1, the execution lead integrates useful source material into owned paths:

- `05.5-SOURCE-EVIDENCE.md` in this phase: retain source identities, exact query and
  paginated-inspection provenance, per-claim status and remaining unknowns. Replace
  stale execution-halt/readiness conclusions with the current unmeasured status.
- `tests/fixtures/heavy-ship-source-contract.json`: retain the 21 pending runtime
  rows and source enrichment, not the old zero-call envelope or old checkout status.
- `tests/heavy-ship-evidence.ps1`: retain shape/provenance/outcome self-tests; change
  the old "pending runtime means implementation blocked" rule into separate source
  preparation and runtime acceptance checks. Pending runtime permits implementation
  but can never pass a runtime gate. Plan 09 owns the behavior change and RED proof.
- `docs/development/heavy-ship-probe.md`: retain useful field/provenance definitions;
  replace the obsolete source-only stop with the production collector run procedure
  prepared in 09. Do not merge old SUMMARY, STATE, halt metadata or runtime claims.

The lead records the integrated revision and runs the retained checker self-test.
No plan reads an isolated worktree or assumes these files already exist on main.
Task 1 owns this bounded integration and its checks; it is not an unowned entry
gate, another research wave or an owner game run. No raw source corpus is imported.

The source enrichment is snapshot
`x4-9.00-steam-23660954-ship-detail-source-v1`, root
`blake3:4f04a991bd595ff9493dc025a2f636fa64a6c59ed213c0dc1b19d3598b97b518`,
`game` / `x4:vanilla`, Steam build `23660954`. The lead verified six exact queries
at limit 5, one selected entity each, 3+5+4+3+1+12 inspection pages, all terminal
cursors null: 24 claims, no conflicts, partial coverage. Existing faction/ship
declarations also have source evidence. Preserve the distinctions: source-observed
is not runtime-observed; missile signed quantities/completeness and thruster
interpretation remain inferred/unknown; crew runtime bounds, universal temporary
faction lifetime and campaign availability are unknown; service classification is
inferred from specific first-party usage. All 21 new runtime rows remain pending.

## Migration and complete source audit

| Prior scope / authority | Active owner | Coverage |
| --- | --- | --- |
| Old 02 source preparation | Lead integration above; 09 tasks 1–2 consume it | Source work is planning input; runtime observations use completed production code |
| Old 03 typed cargo/crew/loadout and dependencies | 09 tasks 1–2 | D-01–D-05, D-11–D-13; no field family removed |
| Old 04 scheduling and recovery | 09 task 3 | Existing scheduler, external global policy, local admission, complete failure matrix |
| Old 04 faction eligibility and old 07 full set | 10 task 2 | D-06–D-09, D-12–D-13; every discovered ID has a disposition |
| Old 05 effective configuration/package plumbing | 09 tasks 1/3; 10 tasks 1/2 | Same validated profile reaches every actual consumer; no standalone plumbing wave |
| Old 06 calibration and heavy Gate A | 10 task 1 | D-10, D-14–D-16; native latency measured after implementation |
| Old 08 reviews/Gate B/closure | 11 tasks 1–3 | Full selected risk gates and all assumptions/prohibitions retained |
| GOAL: real heavy data through the shared architecture | 09 actual-chain tests; 10 Gate A; 11 Gate B/readback | Not mocked transport, synthetic-only success or an architecture-only deliverable |
| ROADMAP SC1 dynamic full eligibility | 10 task 2; 11 task 2 | Small/pirate, XEN/KHK and vanilla/DLC independent factions, including zero-ship dispositions |
| ROADMAP SC2 complete required field contract | 09 tasks 1–2; 11 task 2 | Core, cargo, aggregate crew, actual installed loadout; exact groups/windows |
| ROADMAP SC3 independent bounds and performance | 09 task 3; 10 task 1; 11 task 2 | Every stage, backlog, freshness, recovery and measured headroom, normal and SETA |
| ROADMAP SC4 honest strength/all-applicable delivery | 09–11 | Transport completion never upgrades source certainty or excuses unavailable applicable data |
| REQ OBS-06 | 09–11 | Heavy ships only; station-specific remainder explicitly passed to 05.6 |
| REQ VAL-01 / VAL-06 | 09–11 | Static, pure Lua, native/receiver, actual local chain and owner runtime source evidence |
| REQ VAL-02 / VER-LG-003 / VER-LG-011 | 09 task 3; 10 task 1; 11 | Independent limits, all consumers, baseline comparison and sustained SETA |
| REQ VER-LG-012 | 09–11 | Exact dependencies, outcome distinctions, multi-revision and restart readback |
| RESEARCH architecture, source semantics, trust boundaries | 09–11 and integrated source evidence | Existing carrier/ABI, strict input, no duplicate loop, bounded durable retention and diagnostics |
| CONTEXT D-14–D-16 | 09 package; 10/11 owner gates | Vanilla+DLC+Live Galaxy only, no Carrier A or supported-mod acceptance claim |

The old research's source-before-detail-implementation/calibration-first ordering
is superseded. Existing patterns are reused with the actual Plan-01 helper splits.
The plan:pre contributions were read: assumption-delta scan returned
`detected:false`; schema-gate found no listed ORM scope, so no schema push task;
TDD is active. Original 01 remains the leading tracer; behavior-adding tasks start
with assertion-level RED and use task-level RED/GREEN/REFACTOR across the vertical
slice. No new skeleton or interface-only wave is added. Graph context is absent.
Estimate calibration returned factor 1, sample_count 0, confidence `low`.

## Runtime risk and evidence contract

A synchronous X4 native call cannot be interrupted by the collector's timer. The
initial profile therefore bounds admitted calls, allocations, selected objects,
attempts, retained state and admission duration; it does not certify a maximum
duration for a call already executing. The agent supplies a finite, locally tested
nonzero proposal and explains this residual risk before owner approval. Unknown
latency is a measurement target, not an impossible precondition. A freeze or limit
breach stops later admissions and closes neither gate. Changes to the package or
limits after measurement require relevant reruns, not reuse of mismatched evidence.

The owner performs only game actions and explicit risk/threshold approvals. Agents
build packages, run local tests, analyze supplied evidence and independent durable
readback. Raw captures stay in durable private storage with verified digest/locator;
Git receives sanitized run identities, provenance, metrics and outcomes only.
No game control, saves, station feature, minds, recipient visibility or mutation is
authorized by this replan. No source-resolvable question is assigned as owner research.

## Plan 10 local preparation repair completed

Owner clarification on 2026-09-17 replaces standalone ship and nested-record
population quotas with resource-derived admission, bounded incremental work and
transport-first pause/resume. ADR-LG-006, CONTEXT and Plan 10 were reconciled in
`03e2793`. This is repair of the existing production path before the first owner
checkpoint, not a new research wave or early full-set expansion.

The repair timeline is RED `25925ea`, initial GREEN `67b9b09`, cursor extraction
`932da78`, cursor RED `2c8faac`, and production repair GREEN `7051ff3`. Independent
review of the initial GREEN found CR-01 stale-parent cursor starvation and CR-02
missing shared native nested-allocation admission. Targeted re-review of
`7051ff3` closed both findings with a clean scoped verdict and no grounded new
findings. The reviewer reran cursor (2), native budget (1), native recovery (1)
and Lua detail (9) checks; all passed. Whole-phase review remains a later gate.

Focused executor checks passed for identity/family progress across replacement
parents, shared native preflight admission, stale-capture versus permanent or
ambiguous refusal, Lua contracts/syntax, bridge/native strict Clippy and source
size checks. The actual ABI cargo/crew/loadout harness passed revisions 2–13,
independent history/current readback and exact replay. Individually acceptable
16-element role/tier arrays collectively exceeded the tiny injected budget and
were refused before owned decoding; later valid revisions committed without
partial replacement. The native aggregate regression is GREEN evidence, not a
claimed baseline RED reproduction.

Prior synthetic local measurements admitted 129 core members and 80 nested
records, crossing the old 128/64 quotas and 8 KiB semantic-record threshold.
That finite first-groups slice does not prove eventual detail coverage of every
member or whole-faction throughput headroom. The updated actual-chain interleave
passed 19 committed revisions on one producer in 59.362 seconds: two 129-member
cores and 17 detail records, 80 nested entries, exact replacement-parent bindings
and independently reopened earlier/current revisions. Core revision 15 followed
cargo:g4; crew:g4 and loadout:g4 resumed against core 15, followed by g5. Final
backlog was zero. Callback max was 2.7049 ms; seal-to-commit p95/max was 48.5984 ms.
This proves inter-parent progress, not full 129-member detail coverage; a run
this close to 60 seconds does not demonstrate positive throughput headroom.
Final post-convergence regression and exact package rebuild passed as recorded
below; none of these local results complete the owner runtime checkpoint.
The synthetic 20 ms callback/25 ms rate profile is not the prepared game profile.
Earlier prepared 2 ms actual-chain guard refusals remain calibration risks.

Harness follow-up at `e3468de` found WR-01/P2: exact per-record current-parent
binding alone could pass a reset-to-first-member caller regression, and missing
capture rows could skip readback. The executor owns a RED/GREEN oracle repair
for cross-refresh identity/family continuity, later-member progress and exact
committed-versus-readback revision coverage. Extraction `e25bbb3`, intentional
RED `0e4776e` and GREEN `2488caa` established that the old oracle accepted a reset
and the repaired oracle rejects reset, missing capture and later-member
starvation. A fresh 19-revision interleave passed in 59.396 seconds with the
exact capture/readback set and resumed g4/g5 progress. Its callback max 1.077 ms
does not erase the earlier 2.7049 ms observation. Targeted re-review at `2488caa`
closed WR-01 and reran the valid/three-invalid trace contract successfully.
Final relevant regression and packaging may proceed. TEST-01 was clarified
with this concrete case in the owning project test skill and its changelog;
no runtime policy was added.

During the first final regression, the executor identified a clock-domain
defect in proactive refresh: canonical capture intervals are X4 game time,
whereas parent freshness is receiver real time. SETA can inflate the apparent
family duration and recreate repeated-refresh starvation. The owning executor
replaced that estimate with bounded three-family receiver wall-duration
measurement from issued intent to matching committed receipt, with an accelerated
game-time regression. SectionStart arrives after collection stages and would
undercount source work; its arrival is not the duration start. RED `ee6c20d`
reproduced a 30-second game interval inflating a 5-second receiver interval.
This preserves the existing real-time admission contract,
not a new scheduler or deadline. GREEN `2b41ec4` passed targeted re-review,
including three reviewer-run timing regressions. Related Lua admission paths
already use producer monotonic status; game timestamps remain capture evidence.
The sole review is clean for the scoped local repair. The final regression was
restarted after this convergence; the earlier 322-test Rust pass is intermediate,
not the final post-convergence regression.

### Final local verification and prepared package

At `e661f36`, final workspace formatting, all-target Clippy with warnings denied,
325 Rust tests and source-size checks passed. Five existing deferred Phase
05.1-09 RED contracts remain ignored with their unchanged Phase 05.3
reconciliation reason; they are not heavy ABI cases. The full Lua/XML/schema
and serial clock suite passed in 71.708 seconds: 80 Lua contracts, 20 syntax
checks, four XML documents/seven rejection cases, schema and startup/restart/
sustained/pending-I/O recovery checks. The executable oracle passed valid and
three invalid traces. Heavy actual ABI core/detail tests and prepared 2 ms /
50 ms restart plus interrupted core/cargo/crew/loadout cases passed separately.

Final synthetic 20 ms / 25 ms restart passed all 12 independently reopened
revisions, continuing crew:g2/loadout:g2 after core 9 and progressing to g3.
First/restart commits took 24.722/22.526 seconds. The latest callback max was
10.764 ms, not the earlier 2.7049 ms; post-return guards cannot preempt that work.
Final interleave passed the exact 19-revision readback manifest and g4/g5
continuity in 59.473/60 seconds: 275 records, 701 backpressure pauses and zero
final backlog. Its callback max was 1.411 ms. Only 0.527 seconds remained;
required operating headroom, full-member coverage and X4 safety are unproven.

The executor prematurely overlapped one synthetic launch with the prepared
interruption batch. Its pipe-open failure is an orchestration error excluded
from product measurements; the reported reruns began only after predecessor
exit and process cleanup. Final task-owned process inventory was empty.

Package self-test/build and independent verification of all 29 manifest digests
passed. Candidate: `prepared-heavy-experiment`, source
`e661f3640af3a05b0f2f5336face8b239d1c33b5`; manifest SHA-256
`293ca4462909052e05a4a84772c3eb34ee1856d6313f7343c5c909e1d1dbc6f4`.
Profile SHA-256:
`c09a9f35acec0df61470299cc87e7726265974340173c6ff5f6b01eff333dd2e`;
native SHA-256:
`a2086fded06708ff7bafe25c6ea720fadc0c2e09dc0fa5a337b5b24468913e53`;
bridge SHA-256:
`d7b426c917378305c56ebd811d17e1a8743f470ea709536edf8077481987e4ee`.
Evidence self-tests passed 16 cases; Preparation passed, while RuntimeAcceptance
correctly refused `pending_implementation`. No game installation or operation,
approved numerical limits, Gate A/B or completed Plan 10 summary was produced.

Commands: workspace `cargo fmt --all -- --check`, `cargo clippy --locked
--workspace --all-targets -- -D warnings`, `cargo test --locked --workspace
-- --test-threads=1`; existing `run_contracts.ps1 -Suite all`, source-size lint,
oracle contract, serial heavy-core/detail/recovery, synthetic `-Throughput`
and `-Throughput -Interleave`, package self-test/build and evidence
self-test/Preparation/expected RuntimeAcceptance refusal. Exact invocations
remain in the owning harness and probe document.

The verified clock-domain incident is retained in primary memory
`wing_x4_modding/incidents`, drawer
`drawer_wing_x4_modding_incidents_cdbf564c1410105fe177b54a`.

### Initial experiment consent and installation

On 2026-09-17 the owner explicitly approved the first bounded normal-time
experiment, then closed X4 before deployment. The agent installed the exact
`e661f36` bundle into the owned Live Galaxy extension directory: 26/26 installed
file digests match the approved manifest, with no old-file leftovers. The
previous 15 files were retained in a verified machine-local backup outside the
game's extensions directory. Installed Steam build is `23660954`.

The agent prepared an owner-only machine-local run directory but did not start
the bridge: its 60-second window begins at bridge startup, so menu preparation
must not consume it. Logical initial run ID:
`055-normal-e661f36-20260917-051641`. The initial run is recorded below.
The owner still performs X4 launch, extension-menu changes and all game actions;
technical installation/capture preparation for this approved run belongs to the
agent. This consent does not approve measured numerical acceptance, SETA,
Gate A/B or another phase. All 21 outcomes remain pending.

Candidate/inactivity/parent-freshness deadlines remain 30/10/30 seconds; the
initial game experiment remains 60 seconds, callback 2 ms and rate 50 ms. No
additional game consent, measured numerical acceptance, Gate A/B or completed Plan 10
is implied. All 21 required runtime outcomes remain pending. The Plan 09 package
identity is historical and must not be presented as the repaired package.

### Initial normal-time run: collection refusal

After the owner confirmed the disposable campaign loaded and paused, the agent
asked the owner to unpause without SETA and started the exact prepared bridge
at 2026-09-17 05:30:26 UTC. The fixed 60-second admission window expired; the
agent then stopped only its bridge. Owner unpause timing and subjective game
responsiveness have not yet been independently confirmed.

Observed X4 diagnostics: compatible producer initialization with the exact
profile digest, followed by `ship_core` revision 1 `collection_overflow`:
four native calls, 7,736 allocation bytes, 570 collection steps, 29,978 ms
reported duration and backlog one. Bridge history contains compatible-session
connections and peer-inactivity disconnects, but no successful publication.
Independent exact revision-1 readback returned `Diagnostic(MissingRevision)`.
This is a failed initial collection, not runtime acceptance. The cause is
undetermined: the aggregate overflow reason alone does not identify which
guard fired or whether owner pause timing affected collection progress.

Six private artifacts (database, bridge status/history/stdout/stderr and an X4
debug-log snapshot) are retained outside Git with owner-only Windows ACLs.
Logical artifact ID: `055-normal-e661f36-20260917-051641`; stable locator:
`%LOCALAPPDATA%/LiveGalaxy/runs/055-normal-e661f36-20260917-051641/locator.json`.
All six SHA-256 digests were independently re-read and verified after retention.
Raw paths, payloads and diagnostics remain private. No retry, SETA run,
deadline increase, game control, save inspection or implementation change was
performed. Plan 10, Gate A/B and all 21 outcomes remain incomplete.

### Owner-approved synchronous-first follow-up

The owner confirmed immediate unpause and no visible freezes in the initial
run; X4 is now paused. Source and retained metadata exclude memory/work-budget
exhaustion as the reported refusal: calls/allocation/steps were far below
configured maxima. The approximately 30-second duration is the last successful
step timestamp, so it is not the duration of a continuous CPU sort. The generic
reason omits precise guard/stage fields. Local actual ordering at 950 synthetic
IDs required 594 invocations (29.7 seconds at a simulated 50 ms cadence);
the actual core collector at 950 IDs refused at 30,050 ms in `order`, before any
core getter. This establishes an artificial-slicing bottleneck locally, not the
exact game population or X4 callback thread topology.

The owner explicitly selected full synchronous collection of one requested
selection first, with resumable slicing only if measured problems occur, then
approved the 2 ms callback threshold as measurement-only for the experiment and
the bridge's initial-data wait of 30 seconds followed by 10-second inactivity.
The owner approved proceeding with the scoped Plan 10 repair. The agent updates
the native/bridge/Lua consumers and RED/GREEN tests, preserving existing
transport, ordering/dependencies, resource refusal and lossless backpressure.
No Lua waiting for ACKs, background X4 calls, guessed thread/freeze guarantee,
heartbeat, new research wave, larger numerical budgets or new game run follows
from these approvals. The next package needs local review/regression and
separate owner-approved deployment/run. Plan 10 remains incomplete.

The accepted scoped experimental contract is retained and re-fetched in primary
memory `wing_x4_modding/decisions`, drawer
`drawer_wing_x4_modding_decisions_e830424eb95c967eb61cb0af`.

Standalone Lua 5.1.5 ordinary ordering was measured with `os.clock` over three
fresh reverse-decimal ID arrays per size: 950 IDs 1/0/0 ms, 10,000 IDs 4/4/4 ms,
100,000 IDs 57/57/50 ms. Every output was independently checked bytewise sorted.
The coarse clock cannot resolve sub-millisecond samples, and three samples do
not establish percentiles. These are local sort-only CPU-clock observations,
not full capture, LuaJIT/X4 callback duration, production capacity or game-impact
evidence. They explain why the earlier simulated-cadence arithmetic must not be
reported as continuous sorting cost.

The scoped repair now captures core or one detail selection fully in one Lua
callback, then retains the owned snapshot through downstream busy delivery.
Core revalidation and borrowed-string copying precede native admission. Existing
aggregate represented-value/allocation, call, work and age guards remain finite;
represented source bytes are not an exact Lua-heap measurement. The 2 ms callback
threshold is diagnostic-only. Fresh monotonic age/clock failure still refuses
before admission. Every heavy demand starts its own finite 30-second first-data
wait; polls do not renew it and subsequent DATA retains 10-second inactivity.

Intentional bridge timing RED (three failures) and core/detail synchronous RED
were followed by focused GREEN: five preparation timing tests, 18 detail/profile/
synchronous regressions, 13 core regressions, 20 syntax checks, three preserved
selection timing tests and one ordinary diagnostic-renderer regression. The
controlled 1,900 ms callback/1,898 ms overrun is contract evidence, not measured
performance. Independent standard-depth re-review of the frozen repair found
zero open scoped blockers/warnings. Final local regression is in progress;
package regeneration, deployment and another game experiment have not occurred.

Final post-fix regression passed: workspace fmt/Clippy/source-size, 330 Rust
tests (five unchanged ignored), 86 Lua, 20 syntax, XML/schema and serial clock
chains. Actual core/detail, configured recovery with four interrupted families,
12-revision first/restart throughput and 19-revision interleave passed exact
independent readback. Undelayed first/restart elapsed 5,286.9603/9,570.4167 ms;
callback maxima 1.4653/1.8998 ms. Finite fixtures are not game capacity evidence.

The initially untriggered interleave run is excluded. Its faster completion
could not naturally age the parent, so test-only ACK2/3 waits of 16 seconds
were added outside callback timing, retaining all prior oracles. Final interleave
took 57,071.2118 ms including 32,000 ms deliberate aging; core4 resumed
loadout:g0 revision5 after cargo2/crew3 and progressed through g5. All 19 exact
readbacks and same-peer history/cursor assertions passed. The history oracle
exposed a misleading pre-recovery `disconnected` event; the state-only correction
to `waiting` passed retained-trace RED and serial GREEN. Recovery behavior did
not change. Independent re-review closed WR-02; no scoped finding remains.

Evidence self-tests 16/16 and Preparation passed. RuntimeAcceptance deliberately
refused `pending_implementation`. Final owned process inventory is empty.
Installation, X4 responsiveness/headroom, SETA,
all 21 runtime outcomes, Gate A/B and Plan 10 completion remain pending.

### Revised prepared package

Local repair source: `b4d6b65d9323bc6c527aef784362deff38e95838`.
Package self-test, build and independent 29/29 file digest checks passed.
Candidate remains `prepared-heavy-experiment`; manifest SHA-256:
`832e3de04eba392215abf9295b6b8254b57c85123385c812636608106b344f3d`.
The numeric profile digest is unchanged; source/manifest identity distinguishes
the revised callback/preparation semantics from the old installed package.
The installed game extension is still `e661f36`. No revised deployment or game
run occurred. Next checkpoint: obtain separate owner consent, require X4 closed
before installation, then prepare one owner-operated bounded normal-time run.

### Revised deployment consent and installation

The owner explicitly approved installation when X4 is closed and the revised
60-second normal-time experiment. On 2026-09-17 X4 process absence was checked
before backup and immediately before replacement. The exact `b4d6b65` package
was installed into the owned Live Galaxy extension directory; 26/26 installed
digests match the verified manifest. The previous 26 files were copied to durable
machine-local rollback storage outside the game directory and independently
hash-checked before replacement. No other extension or save was touched.

The bridge has not started, so the admission window has not been consumed.
Next: the owner launches X4 and loads the approved disposable campaign paused;
the agent prepares capture and starts only the owned bridge at the agreed
unpause checkpoint. No SETA, numerical acceptance, Gate A/B or completed Plan 10
is implied by installation consent.

### Revised normal-time experiment: source refusal

Logical run ID: `055-normal-b4d6b65-20260918-005155`. Exact installed/bundled
identity was rechecked before starting the owned bridge. Start was
2026-09-17T17:52:12.5025965Z; automatic stop was
2026-09-17T17:53:12.6155667Z (approximately 60.113 wall seconds including
process launch/stop overhead). No X4 process or game action was controlled.

Observed game diagnostics: compatible Carrier B initialization, then
`invalid_fact` at game time 13.91 followed by `retry_exhausted` at 13.97.
Bridge history contains compatible sessions and a first-data wait expiry,
but no committed publication. Independent exact ship_core revision-1 readback
returned `Diagnostic(MissingRevision)`. The specific rejected source field/stage
is unknown; this does not establish a throughput/resource failure or game freeze.
The owner confirmed unpause and no noticeable freezes, then paused X4 again.
Exact unpause timing and SETA state were not recorded.

Six artifacts are retained outside Git in an owner-only durable directory;
their digests and generated locator were independently re-read and checked.
Stable locator:
`%LOCALAPPDATA%/LiveGalaxy/runs/055-normal-b4d6b65-20260918-005155/locator.json`.
Read-only inspection proved existing owner-only directory/file ACLs.
No retry, reinstallation, new numerical budget or game acceptance is implied.
Plan 10, Gate A/B and all 21 runtime outcomes remain pending.

### Rejection-context repair (owner authorized)

The owner requested repair of the existing LOG-02/03 violation. Collector
cleanup erased rejection stage/field context, and selection attached metrics
only to successful captures. The repair preserves the existing dispositions,
validation, retry policy, resource budgets and synchronous capture semantics.
Owned rejection metadata now survives cleanup, including source sector identity
normalization refusal and recursive source-value cycle detection. Runtime logs
carry safe labels/types/ordinals plus section/revision/incarnation and metrics;
raw source values, native identities and exception text remain excluded.

Focused Lua checks: 95 contracts and 20 syntax cases passed. Six new ordinary
runtime cases cover owner/type/class/location refusals, absent sector identity
and cyclic source values, including correlation and disclosure assertions.
Actual source-adapter and cargo-cleanup regressions passed. Independent review
closed WR-02 (obsolete selection context) and WR-03 (terminal per-tick flooding).
Initial missing-parent requests now log requested correlation without stale
capture counters; unchanged terminal refusals are suppressed while distinct
attempts and revisions remain visible. Removing rejection propagation at the
runtime boundary temporarily caused all six context regressions to fail; the
fault was restored immediately. This is an oracle check, not an exhaustive
mutation score or original-version full rerun. Final relevant regression passed:
`run_contracts.ps1 -Suite all` in 61.051 seconds, including 95 Lua contracts,
20 syntax cases, XML 4 files plus 7 negative checks, persistence schema, 16
targeted Rust wire/bridge tests and seven serial actual-native lifecycle
scenarios (startup orders, repeated/sustained collections, producer/bridge
restart and pending-I/O unload). No Rust production code or wire format changed.
The actual rejected source
field in the retained X4 run remains unknown. No installation or new X4 run
occurred, and Plan 10 is not complete.

### Diagnostic repair prepared package

Repair source: `b156a5257af12f1fa21e73df56e3defc1218c25a`, pushed to the active
milestone branch. Package self-test and build passed; all 29 bundle file digests
were independently rehashed against the manifest. Ignored local output:
`dist/live-galaxy-rejection-diagnostics`. Manifest SHA-256:
`62e33d2b8471cf34d4bd4733861ff712d391db3ea79c2e52c0d25495bc87dd1b`.
Candidate remains `prepared-heavy-experiment`. The installed extension was not
replaced and no new game run occurred. Original source cause, runtime capacity,
Gate A/B and Plan 10 completion remain pending.

### CI ABI prerequisite repair (owner authorized)

Actions run `35306856167` at `d5cc9e3` failed in two ship ABI contracts while
copying the absent debug native DLL (`ship_abi_host.rs:65`, `NotFound`). Run
`35255726574` at `4afa4f4` had the same error before the rejection logging repair.
The workflow also omitted compatible Lua-host preparation. Local ignored build
artifacts had masked both prerequisites.

The existing local carrier harness now has an explicit `-SelfTest -PrepareOnly`
mode, building the same statically linked ABI host from the locked standalone
Lua source route without running bridge scenarios or requiring Busted/LuaRocks.
CI provisions checksum-verified Lua, prepares that host and explicitly builds
the debug native DLL before unchanged workspace tests. No test is skipped,
production behavior changed, or dependency version upgraded. Clean-clone
provisioning/host preparation and all 9 actual ABI contracts passed, followed
after clean independent review by the full locked workspace: 330 passed and
5 existing deferred station contracts ignored (not counted as passes).
No pre-existing native/Lua/host artifacts were used in that clone. The unchanged
normal harness path also passed actual native-first capture and independent
durable readback. Remote Actions run `35308219502` at `5feb9f6` passed all
steps, including unchanged workspace tests. TOOL-04 received
the concrete clean-runner ABI prerequisite example; no new policy was added.

### Diagnostic deployment and owner readiness checkpoint

On 2026-09-18, X4 was confirmed closed before backup and again immediately
before replacement. The previously verified `b156a5257af12f1fa21e73df56e3defc1218c25a`
package was installed into the owned `live_galaxy` extension only. CI source
`5feb9f6` changes no production code relative to that diagnostic package.
All 29 bundle digests and all 26 installed extension digests matched the manifest
SHA-256 `62e33d2b8471cf34d4bd4733861ff712d391db3ea79c2e52c0d25495bc87dd1b`.
The installed path set remained exactly 26 files; no other extension, profile,
vanilla source or save was changed.

Rollback artifact ID: `05.5-b156a52-20260918-230830-e223a7ef`.
Stable machine-local locator:
`%LOCALAPPDATA%/LiveGalaxy/install-backups/05.5-b156a52-20260918-230830-e223a7ef/locator.json`.
The backup directory has owner-only permissions. All 26 previous extension
digests were checked before installation and re-read against the retained
locator afterward. This preserves the previous installation, not game saves.

No bridge window or game run started during deployment. Next action is owner
launch of the approved disposable Creative Custom campaign, initially paused,
then explicit readiness for the same bounded 60-second normal-time diagnostic
run with SETA off. No numerical limit, scenario or scope was expanded. The
original specific source refusal remains unknown; Gate A/B, capacity evidence
and Plan 10 completion remain pending.

### Sector identity runtime diagnosis and repair

Owner-approved normal-time run `055-normal-b156a52-20260919-224241` used the
installed diagnostic package with SETA off. The bridge established compatible
sessions but received no DATA; both 30-second first-data waits ended as
`peer-inactive`, and independent `ship_core` revision 1 readback returned
`MissingRevision`. X4's detailed developer log supplied the missing game-side
cause before collector cleanup:

- stage `core`, condition `identity_invalid`, field `sectorid`;
- observed Lua type `userdata`, attempt 1, ordinal 1;
- six native calls, 7,384/7,440 allocation bytes, 934/941 steps;
- callback maxima 1/2 ms and no measured callback overrun;
- one retry exhausted per connection, followed by the unchanged admission
  window stopping further source work.

This establishes the source refusal, not throughput or X4 acceptance. The run
is retained as private artifact `055-normal-b156a52-20260919-224241`; stable
locator:
`%LOCALAPPDATA%/LiveGalaxy/runs/055-normal-b156a52-20260919-224241/locator.json`.
Eight retained files and their SHA-256 values were re-read, and the directory
has owner-only permissions. Raw game payloads and private paths remain outside
Git.

The root cause was treating `GetComponentData(component, "sectorid")` as a
stable UniverseID. X4 returned a Lua userdata instead. The repaired adapter
converts the already validated component to its 64-bit identity and calls
`C.GetContextByClass(component64, "sector", false)`, then applies the existing
strict unsigned-decimal normalization. Exact X4 Docs searches across both
phase snapshots returned no matching entity. As narrow secondary X4 9.00
evidence, the current local X4 Live MCP checkout uses the same call shape and
records a successful runtime sector-ID smoke; that checkout has no configured
remote, so this is local observed precedent rather than refreshed remote source.

The focused ship-detail suite passed 21/21. The complete Lua suite passed 96/96
plus 20/20 syntax checks. An attempted full regression while X4 was still open
was discarded because the game owned the fixed native pipe and the local host
correctly failed with `open_failure`; it is an isolation conflict, not product
evidence. After X4 closed, the clean 61.142-second full regression passed all
seven actual native pipe/lifecycle scenarios, 96 Lua contracts, 20 syntax
checks, XML package validation and persistence schema validation. Independent
review found no grounded issues and retained runtime reattempt as mandatory.
A rebuilt package, closed-game installation and bounded runtime reattempt remain
pending. Gate A/B, capacity conclusions and Plan 10 completion remain pending.

The reviewed repair was committed and pushed as `715365c`. Its package contains
29 independently rehashed files and records exact source revision
`715365c18b830c84a390e452862b855c35069d3d`; manifest SHA-256:
`cfc7dff80ab493265877dbdc1496cd19936ae38a8c42da7847abd748b6e1de4a`.
With X4 closed, all 26 owned extension files were replaced and rehashed against
that manifest. No other installed extension or game file changed.

Rollback artifact ID: `05.5-715365c-20260919-225304-26313c73`.
Stable machine-local locator:
`%LOCALAPPDATA%/LiveGalaxy/install-backups/05.5-715365c-20260919-225304-26313c73/locator.json`.
The owner-only backup retains the preceding diagnostic installation; all 26
digests were verified before and after replacement. No bridge window or game
run started during installation. The next bounded normal-time reattempt requires
owner launch/readiness; SETA remains off and the same 60-second window applies.

### Numeric class identity diagnosis

An initial owner launch remained paused for the whole bridge window and is not
runtime source evidence. After the owner explicitly unpaused, bounded run
`055-normal-715365c-20260919-230117` progressed beyond the repaired sector
lookup and exposed the next specific source shape: stage `core`, condition
`token_invalid`, field `class`, observed Lua type `number`, attempt 1, ordinal 1.
The two connection attempts used six native calls, 7,384/7,400 allocation bytes
and 939/941 steps. Callback maxima were 2/1 ms with no measured overrun.
The bridge received no DATA, both first-data waits expired, and independent
`ship_core` revision 1 readback returned `MissingRevision`.

The retained private artifact ID is
`055-normal-715365c-20260919-230117`; stable locator:
`%LOCALAPPDATA%/LiveGalaxy/runs/055-normal-715365c-20260919-230117/locator.json`.
All eight retained file digests were re-read under owner-only permissions.

The adapter now canonicalizes an observed numeric `classid` only when it is a
finite, nonnegative safe integer, emitting `classid:<decimal>`. Existing string
class values remain unchanged. Fractional, negative, out-of-safe-range and
non-number/non-string values remain rejected before sector lookup. Exact Docs
MCP inspection of all 1,000 GetComponentData evidence records across both phase
snapshots confirmed extensive `classid` call-shape coverage but no return-type
claim; the X4 runtime observation therefore owns the numeric shape. Focused
ship-detail contracts passed 22/22, followed by 97/97 Lua contracts and 20/20
syntax checks. Independent review, clean native regression, package/install and
runtime reattempt remain pending; no Gate A/B or capacity conclusion follows.

Review found one nonblocking but concrete canonicalization defect: Lua 5.1
formats signed negative zero as `-0`, so equal numeric zero values could have
produced different stable strings. The adapter now maps every numeric zero to
`classid:0`. Executable cases cover positive/signed zero plus fractional,
negative, 2^53 and nonnumeric values, including whether sector lookup is
entered. Re-review confirmed the finding fixed with no remaining issues.

After X4 closed, the final 57.204-second full regression passed all seven actual
native pipe/lifecycle scenarios, 97 Lua contracts, 20 syntax checks, XML package
validation and persistence schema validation. Package build, closed-game
installation and the next runtime reattempt remain pending.

The repair was committed and pushed as `9845f37`. Its package records exact
source revision `9845f3754888c4cd627610e383d0854eff559378`; manifest SHA-256:
`4ac568dbb1f23761ad3ed3ebcec63822dbf47696ef3a05bcb9819eb41dc62400`.
All 29 bundle files were independently rehashed. With X4 closed, the 26 owned
extension files were backed up, replaced and rehashed against the manifest.
No other game file or extension changed.

Rollback artifact ID: `05.5-9845f37-20260919-231113-a7434381`.
Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/install-backups/05.5-9845f37-20260919-231113-a7434381/locator.json`.
All 26 previous installation digests were re-read after replacement. The next
bounded 60-second normal-time run remains an owner readiness checkpoint; SETA
stays off and no runtime acceptance is claimed by installation.

### Sectorless core and pending-message diagnosis

Owner-unpaused normal-time run `055-normal-9845f37-20260919-231540`
produced a complete game-side `ship_core` revision 1 in 9 ms with 1,794 native
calls, 7,352 allocation bytes, 12,554 steps and 203,418 source-value bytes.
The bridge rejected its first DATA message as `message-decode`; independent
revision-1 readback returned `MissingRevision`. A later revision-2 capture
reached ordinal 470 before `GetContextByClass(..., "sector", false)` returned
a zero cdata context, which the adapter rejected as an invalid sector identity.
No freeze was reported. The 9 ms callback maximum exceeded the 2 ms
measurement target by 7 ms; this is observed cost, not accepted headroom.

The retained private artifact ID is
`055-normal-9845f37-20260919-231540`; stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/runs/055-normal-9845f37-20260919-231540/locator.json`.
All ten retained file digests were re-read successfully. A second unpaused
bridge window reproduced the same first-message decode rejection before its
finite admission window closed.

The adapter now preserves a zero sector context as explicit location
`context:none`; nonzero malformed identities remain rejected. Bridge
operational history now distinguishes all five complete-message decode error
classes. A bounded read-only capture example can retain exactly one pending
message under a caller-selected private path for structural diagnosis; it has
one 30-second receive, a 1 MiB message cap, finite approved resource intent and
create-new output semantics. The runtime procedure now keeps X4 at normal
speed between bridge-only attempts; pausing is reserved for an explicit pause
scenario and extension replacement still requires X4 to be closed.

Independent review found only a temporary Rust 200-line violation caused by
inline tests. Moving those tests to a 72-line sibling restored the limit;
re-review reported no remaining P0-P2 findings. The converged regression passed
workspace formatting, clippy, Rust tests, source-size lint, all seven actual
native pipe/lifecycle scenarios, 98 Lua contracts, 20 Lua syntax checks, XML
package checks and persistence schema validation. The all-suite duration was
59.081 seconds.

The repair was committed and pushed as `a96d357`. Its package records exact
source revision `a96d357055b9e44edadda2247159155054d21a9d`; manifest SHA-256:
`2d8d208ff742cb77883ebb4970f8fbcd36420302e0a8e468d3407ffd87b423fd`.
All 29 bundle files were independently rehashed. With X4 closed, all 26 owned
extension files were atomically replaced and rehashed; no other game file or
extension changed.

Rollback artifact ID: `05.5-a96d357-20260919-234731-ce5d9187`.
Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/install-backups/05.5-a96d357-20260919-234731-ce5d9187/locator.json`.
All 26 previous installation digests were re-read after replacement. The next
normal-time run begins with the bounded pending-message capture so the remaining
decode failure is diagnosed from exact bytes. Gate A/B, capacity acceptance and
Plan 10 completion remain pending.

### Runtime transport bottleneck and batching repair

The bounded diagnostic capture retained a 674-byte `section_start` under logical
artifact ID `055-pending-a96d357-20260919-235144`. The exact production decoder
accepted it, disproving the first-message JSON/declaration hypothesis. Because the
initial capture helper disconnected without a terminal reset, that diagnostic
session was discarded rather than treated as recovery evidence. Its owner-only
locator is `%LOCALAPPDATA%/LiveGalaxy/runs/055-pending-a96d357-20260919-235144/locator.json`;
the raw message SHA-256 is
`2951fbc36359085f29b3d632f26262ff961c853d91c948438118c5a3b869bea0`.

A fresh unpaused normal-time run, logical artifact ID
`055-direct-a96d357-20260920-0002`, then accepted 366 observed receive events
without a decode error. It reached at least batch 278 before the candidate expired
at the unchanged 30-second age bound; the following message was permanently
rejected. The game-side collector had already sampled 893 ships in 9 ms. Inspection
of the exact producer path established the bottleneck: the native producer emitted
one ship record per immutable IPC batch, while X4 advanced the callback at roughly
100 ms cadence. The approximately 90-second transfer requirement, not native
collection cost, JSON validity or the bridge decoder, caused expiry. The owner
reported no freeze. Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/runs/055-direct-a96d357-20260920-0002/locator.json`.

The repair keeps the approved 30-second candidate bound and existing byte/resource
limits. The producer now packs the largest ordered record prefix that fits the
existing complete-message byte limit, normally emitting the full 893-record core
as one batch instead of one message per record. Batch and record ordinals remain
distinct; the receiver requires global record ordinals exactly `1..N`. Durable
context now retains the accepted completion batch count and raw byte count, so an
exact replay remains independent of a later message-limit change. Legacy context
payloads retain byte-for-byte canonical serialization and the historical
one-record reconstruction path.

The diagnostic helper now limits offline reads to 1 MiB, sends terminal reset on
success, decode failure and timeout, and preserves the primary error when cleanup
also fails. Detail negative fixtures were corrected to use valid one-record detail
batches, with a positive staging oracle before targeted mutations. Mandatory code
review converged to CLEAN after five initial findings and two follow-up findings.
Focused tests, full all-target Clippy and source-size enforcement are green. The
final regression also passed workspace formatting and tests, all seven native
pipe/lifecycle scenarios, 98 Lua contracts, 20 Lua syntax checks, XML package checks
and persistence schema validation; the aggregate suite completed in 83.812 seconds.
Package self-test then passed with the heavy profile. The exact package records source
revision `e7a843a30a7dd132aa25ffca5a79ce47d96f26be`; manifest SHA-256:
`78eac8c684a564efe7bb0c0e4514bed63783ac26fc944e0fb8db67b94dbd372d`.
All 29 bundle files were independently rehashed. With X4 closed, all 26 owned
extension files were atomically replaced and rehashed; no other game file or
extension changed.

Rollback artifact ID: `05.5-e7a843a-20260920-005754-4a2e2b47`.
Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/install-backups/05.5-e7a843a-20260920-005754-4a2e2b47/locator.json`.
All 26 prior installed-file digests were re-read before replacement. The next step is
an owner-operated normal-time Gate-A reattempt. No timeout was increased, no
numerical threshold was accepted and no Gate A/B or Plan 10 completion is claimed.

### Gate A batching reattempt and order repair

The normal-time owner-operated run used exact package source `e7a843a` and logical
artifact ID `055-gate-a-e7a843a-20260920-121406`. Four successive core revisions
each reached start, one immutable batch and completion in 204–212 ms. This closes
the prior approximately 90-second transport-path defect: the producer transferred
the full real core in one bounded message without candidate timeout or backlog.

All four completions were permanently rejected and production readback for revision
1 returned `MissingRevision`. The failure was therefore fail-closed and created no
durable authority. Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/runs/055-gate-a-e7a843a-20260920-121406/locator.json`.
The retained history, status, database and summary digests were re-read successfully.

Source and receiver inspection established one exact contract mismatch. The Lua
collector sorts canonical decimal `UniverseID` strings lexicographically, so `10`
precedes `2`; completion instead parsed them as integers and required numeric order.
The receiver now validates the same lexicographic order emitted by the source while
still requiring exact global record ordinals `1..N`. A runtime-shaped regression
accepts `10, 2`, rejects `2, 10` and independently rejects shifted or missing
ordinals. Mandatory focused re-review reported CLEAN with no P0–P2 findings.

The repair passed workspace formatting, all-target Clippy, the full Rust workspace,
source-size enforcement and the full native/Lua/XML/persistence suite. All seven
actual Carrier B lifecycle scenarios passed; Busted reported 98 successes, Lua
syntax 20 successes, and the aggregate suite completed in 70.175 seconds. Package
rebuild and installed-X4 Gate-A reattempt remain pending. No numerical acceptance,
SETA, Gate A/B or Plan 10 completion is claimed.

The exact repaired package records source revision
`d93e6f936cd9f8107cbd5d3bd8ca69c3ca984867`; manifest SHA-256:
`d5184f2f485751e989ce75aa77f739ad429f23dbba311f567b00d21703caa574`.
All 29 bundle files were independently rehashed. With X4 closed, all 26 owned
extension files were atomically replaced and rehashed.

Rollback artifact ID: `05.5-d93e6f9-20260920-122933-7ff4447e`.
Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/install-backups/05.5-d93e6f9-20260920-122933-7ff4447e/locator.json`.
All 26 prior installed-file digests were re-read before replacement. The next step is
the owner-operated normal-time Gate-A reattempt; Plan 10 remains open.

### Core Gate A success and detail FFI repair

The owner-operated normal-time run used exact installed package source `d93e6f9`
and logical artifact ID `055-gate-a-d93e6f9-20260920-123239`. The source emitted
906 core records. One bounded batch reached start, completion, durable commit and
production readback in 239 ms. This is the first successful production core
Gate-A path for the repaired batching and lexical-order contract.

The bridge immediately requested `ship_cargo:g0`. X4 failed closed before
allocation at `stage=count`, `operation=cargo_storage_count`, ordinal 1, revision
2, with three native calls, zero allocation bytes, six steps, zero reported
milliseconds and backlog one. No cargo DATA or durable detail revision followed.
The later admission-window exhaustion was caused by a late reconnect and was not
the source failure. The owner was not asked for a freeze report for this exact
run, so freeze status remains unreported rather than inferred.

Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/runs/055-gate-a-d93e6f9-20260920-123239/locator.json`.
The retained database, history, status, run summary and filtered developer-log
excerpt digests were re-read successfully. The exact developer-log operation and
the successful production readback are the retained runtime evidence; private
payloads and machine-local paths are not committed.

Docs MCP snapshot `x4-9.00-steam-23660954-ship-detail-source-v1` and the matching
published evidence established the defect class. Native crew, cargo-storage and
loadout getters require `UniverseID`, but the adapters supplied LuaID.
`GetPeopleCapacity` additionally requires `(UniverseID, const char*, bool)`, while
the adapter used two arguments. The repair converts every affected native detail
getter through `ConvertIDTo64Bit(ConvertStringToLuaID(identity))`, calls
`GetPeopleCapacity(component, "", false)`, and deliberately keeps
`GetComponentData(LuaID, "cargo")` on its Lua boundary. A fake-FFI executable
oracle verifies the exact argument order, selectors, arity and conversion for
crew, cargo storage, physical and virtual upgrades, software, missiles and units.

Fresh full-task code review found and closed five additional review-gate defects:
the diagnostic capture reset before durable retention; GSD state carried duplicate
obsolete `state_head` keys; the new oracle initially omitted the LuaID-only cargo
exception; failed pre-receipt captures could leave empty final artifacts; and the
repair temporarily exceeded the Rust 200-line limit. The final capture flow now
creates the output only after successful receipt, writes and synchronizes before
decode/reset, removes an exclusively-created partial on persistence failure, and
keeps output lifecycle plus tests in a cohesive sibling module. Final fresh
re-review reported CLEAN with no remaining P0-P2 findings. Focused capture tests
passed 3/3. The final regression then passed workspace formatting, all-target
Clippy, all Rust tests, source-size enforcement, all seven actual native
pipe/lifecycle scenarios, 99 Lua contracts, 20 Lua syntax checks, XML validation
and persistence schema validation. The aggregate harness completed in 57.047
seconds. Exact package build/install and a fresh owner-operated detail runtime
attempt remain pending; full Gate A/B, SETA and Plan 10 completion are not claimed.

The repair was committed and pushed as `658def3`. Its package records exact source
revision `658def3406ee33da0c237f6f2975d98cf2554290`; manifest SHA-256:
`949a7abcf15174709853bad17a5a0462f0675b38341c0fd98f7e4c29820b513c`.
All 29 manifest entries were independently rehashed with zero mismatches. With X4
closed, all 26 owned extension files were backed up, atomically replaced and
rehashed against the manifest.

Rollback artifact ID: `05.5-658def3-20260920-061912-a8e21075`.
Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/install-backups/05.5-658def3-20260920-061912-a8e21075/locator.json`.
Locator SHA-256:
`79f94c380d3e0e362280192239029b3b37098c7ce3dc08a1a41ffa50725eeaf3`.
All 26 previous-installation digests and the locator digest were re-read after the
swap. The next step is an owner-operated normal-time Gate-A reattempt with the game
left unpaused. Full detail coverage, SETA, numerical acceptance and Plan 10
completion remain pending.

### Focused frame evidence and repeatable-run repair

The focused normal-time loaded run for installed source `658def3` produced 35
committed revisions and no rejected or failed outcome. PresentMon capture
`055-fps-loaded-focused-20260920-200209` compared with clean baseline
`055-fps-baseline-clean-20260920-193814`: average FPS -0.627%, 1% low -1.173%,
0.1% low -6.525%, p99 frame time +4.205%, and no frame at least 33.333 ms.
The owner observed no freeze or stutter. Unfocused control
`055-fps-unfocused-control-20260920-200500` captured 4,878 frames in 14.989
seconds, so frame collection itself requires no X4 focus or owner participation.

Two later attempts are invalid SETA evidence. The first ran without SETA; the
second stopped after 0.887 seconds and four commits. Neither is counted toward
Gate A or stability acceptance.

The repeated-run blocker was a Lua lifecycle defect, not an exhausted Rust
resource envelope. Rust owns one finite admission window per bridge process,
while the long-lived Lua module retained `run_started` after that process ended.
The repair observes the transition into fresh compatibility qualification,
discards stale selection state and permits the next ready selection to start a
new finite Lua window. Ordinary polls, retry and reconnect do not renew the
window, and the unchanged Rust schedule still limits each bridge run to 60
seconds.

The retained PowerShell runner now waits for observed 4x--8x game-time advance
before starting a SETA capture, owns the entire timed interval, ignores chat
messages and terminates only the PresentMon and bridge processes that it
created. Its result is fail-closed on missing frames or commits, rejected or
failed history, PresentMon loss or insufficient SETA duration. Focused RED/GREEN
Lua coverage proves no poll renewal and fresh qualification without X4 reload;
the runner self-test exercises exact subprocess arguments containing spaces.
Package rebuild, installation and one valid sustained SETA measurement remain
pending. No numerical acceptance, Gate A/B or Plan 10 completion is claimed.

The repair was committed and pushed as `0869e1a`. Its rebuilt package records
source revision `0869e1aae3afd1cc97cdb8c0b4749b0af01fb01d`; manifest SHA-256:
`bbd9d051a472d57991e7d2f1b33c6d7e2a6cc582d8bfb1d19803cc1fb5e30faa`.
All 29 package entries were independently rehashed with zero mismatches. With
X4 closed, all 26 owned extension files were backed up, individually replaced
through same-directory temporary files and rehashed against the manifest.

Rollback artifact ID: `05.5-0869e1a-20260920-204804-35070262`.
Stable owner-only locator:
`%LOCALAPPDATA%/LiveGalaxy/install-backups/05.5-0869e1a-20260920-204804-35070262/locator.json`.
Locator SHA-256:
`ffe7eb9f8fa1771ea6d6a225cacc3af201265670bc58e85d21ec1d7291cc49d9`.
All prior-installation backup digests and the locator digest were re-read after
the swap. The next valid SETA runner no longer requires an X4 restart between
bridge-run windows. SETA evidence, numerical acceptance, Gate A/B and Plan 10
completion remain pending.

### SETA runtime rejection and recovery correction

The autonomous runner captured logical run
`055-gate-a-seta-20260920-230703` for 65 seconds at an observed 5.991x game-time
factor. PresentMon recorded 7,362 frames, 113.290 average FPS, 54.678 1% low,
41.755 0.1% low, 15.959 ms p99, 37.065 ms maximum frame time, one frame at
least 33.333 ms, no frame at least 50 ms and no lost events. These SETA values
are workload evidence, not a direct comparison with the normal-time baseline.

The bridge committed 25 revisions in 4.282 seconds. Independent durable
reopening verified one 971-record core and cargo, crew and loadout revisions
for groups g0 through g7: 995 total semantic records, zero read failures. X4
then correctly rejected `ship_cargo:g8` revision 26 with `core_changed` during
revalidation. No invalid detail was committed. The native producer nevertheless
remained paused, and the runner's bridge-history-only verdict missed the Lua
rejection; the run is therefore invalid as Gate-A evidence.

A RED ABI regression reproduced refusal of the next `ship_core` intent after
`core_changed`. The native failure path now permits only fresh-core recovery,
matching the existing `stale_parent` path. The runtime runner now records safe
`event:detail` pairs and fails on any diagnostic other than sampled transition
or initialization. Focused native recovery, runner self-test, 99 Lua contracts,
20 syntax checks, formatting, strict workspace Clippy and source-size checks
pass. No timeout, retry, protocol or collection envelope changed. Full actual
ABI regression is deferred until X4 closes because the running installed DLL
owns the same fixed test pipe.

Runtime locator SHA-256:
`d6faa60c5ce5098b9ec9cc5fbcb60d3192a08bc973eea00849c6069aefb6a468`.
Frame locator SHA-256:
`48055139405a852e58b5608fb02309d4f76e0a7de22a56867aae5a7ee8c0a98b`.
All retained files and both owner-only locators were re-read successfully.
Package rebuild/install, full regression and a replacement SETA run remain
pending. Gate A/B and Plan 10 are not complete.
