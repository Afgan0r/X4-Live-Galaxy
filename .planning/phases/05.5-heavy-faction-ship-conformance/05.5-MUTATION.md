---
phase: 05.5-heavy-faction-ship-conformance
status: targeted-local-pass
reviewed_head: 69de7da
runtime_gate: pending
---

# Targeted safety mutation evidence

All successful runs used cargo-mutants 27.1.0 in its default copied workspace,
never `--in-place`. Each reported unmutated baseline passed before mutants ran.
This is a selected deterministic safety slice, not a mutation score for the
entire phase or evidence of X4 runtime performance.

| Target | Baseline | Caught | Surviving | Unviable |
| --- | --- | ---: | ---: | ---: |
| Faction classification | Passed | 7 | 0 | 2 |
| Candidate/frame budget | Passed | 12 | 0 | 0 |
| Native zero-member authority | Passed | 9 | 0 | 0 |
| Bridge inventory source map | Passed | 2 | 0 | 0 |

Exact final invocations from the repository root:

```text
cargo mutants -f crates/observation-domain/src/faction_classification.rs --cap-lints true -j 2 --timeout 120 -o tools/.cache/mutants-classification-cap
cargo mutants -f crates/observation-ingest/src/batch_budget.rs -F 'CandidateUsage::within|BatchBudget::record_frame' --cap-lints true -j 2 --timeout 120 -o tools/.cache/mutants-budget-final
cargo mutants -f crates/x4-carrier-native/src/producer_validation.rs -F observable_zero_members --cap-lints true -j 2 --timeout 120 -o tools/.cache/mutants-native-final -- --test producer_contract
cargo mutants -f crates/x4-bridge/src/production_faction_inventory_schema.rs -F 'insert_unique|visit_map' --cap-lints true -j 1 --timeout 120 -o tools/.cache/mutants-inventory-cap -- --lib
```

The `-o` directories are ignored local scratch, not durable retained artifacts.
The two domain mutants were function replacements with
`Default::default()` that cannot compile: neither `FactionObservationRoster`
nor `FactionCensusEntry` implements `Default`. They are unviable, not caught or
equivalent. No viable non-equivalent mutant remains in these selected slices.

The first classifier run found three missed mutants (4 caught, 2 unviable).
`429842a` added independent player/modded, service and Mind-admission cases;
the final run caught all seven viable mutants. The first candidate-budget run
found three missed conjunction mutants (6 caught, 3 lint-unviable). `97e5f1a`
added one-over checks for each candidate resource. Rerunning with
`--cap-lints true` made the lint-only mutants viable and revealed one further
missed cumulative-frame mutant. `69de7da` added the two-frame aggregate
boundary case; the final run caught all twelve. The first native validation
run found three missed authority-condition mutants (4 caught, 2 lint-unviable).
`218dbad` added distinct negative evidence combinations; with cap-lints the
final run caught all nine. The strict inventory source-map visitor's two
lint-unviable mutants likewise became viable with cap-lints and were caught.

An attempted native ABI/input slice selected `lua_ship_input.rs` and
`lua_detail_budget.rs`, but its *unmutated* copied-workspace baseline failed:
the `abi_contract` host executable was not found in the copy. No mutant was
tested in that run; it is not counted as a pass or a survivor. The normal
workspace ABI/strict-input tests and actual DLL/pipe local scenarios are
separate evidence. Mutation coverage of that ABI entry seam remains an
explicit limitation, not an unexplained viable survivor.

All final runs used the repository's current strict code. The phase's full
regression, code/security review and owner-operated normal/SETA gates remain
separate requirements; a later source change affecting these functions requires
a new targeted mutation run.
