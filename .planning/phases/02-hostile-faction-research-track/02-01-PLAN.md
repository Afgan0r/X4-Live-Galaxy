---
phase: 02-hostile-faction-research-track
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md
  - .planning/phases/02-hostile-faction-research-track/02-VALIDATION.md
  - tools/verify_xen_khk_evidence.ps1
  - tools/verify_xen_khk_evidence.Tests.ps1
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A verifier can parse one versioned claim register that covers XEN and KHK state, events, identity, visibility, scheduling, economy or spawning ownership, and control limits."
    - "Every material claim has a unique identifier, exactly one permitted classification, material provenance, source boundary, and a constrained conclusion."
    - "Structured scope fields prove that the record is non-gating and observation-only while runtime unknowns retain a future owner and evidence need."
  artifacts:
    - path: .planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md
      provides: "Versioned Markdown evidence record containing a machine-readable hostile claim register."
      min_lines: 140
    - path: tools/verify_xen_khk_evidence.ps1
      provides: "Read-only deterministic parser and invariant checker for the evidence register."
      min_lines: 100
    - path: tools/verify_xen_khk_evidence.Tests.ps1
      provides: "Deterministic valid and invalid source-registry fixtures for the parser."
      min_lines: 60
    - path: .planning/phases/02-hostile-faction-research-track/02-VALIDATION.md
      provides: "Requirement, decision, evidence-level, deferral, and sampling matrix."
      min_lines: 55
  key_links:
    - from: .planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md
      to: tools/verify_xen_khk_evidence.ps1
      via: "The script extracts the fenced `json hostile-claim-register` payload and validates schema, source, category, requirement, decision, scope, and deferred-claim invariants."
      pattern: "hostile-claim-register"
    - from: .planning/phases/02-hostile-faction-research-track/02-VALIDATION.md
      to: tools/verify_xen_khk_evidence.ps1
      via: "The validation matrix invokes the read-only script for RES-01 through RES-03 and D-01 through D-08."
      pattern: "verify_xen_khk_evidence"
---

<objective>
Deliver a versioned, deterministic XEN/KHK evidence record and verifier for future hostile-faction design, retaining unresolved runtime facts as explicitly non-gating deferrals.

Purpose: Make static X4 9.00 evidence, its provenance, and its scope limits independently inspectable without selecting hostile autonomy or delaying the ZYA/ARG path.
Output: `02-XEN-KHK-EVIDENCE.md`, its read-only PowerShell verifier, the Phase 2 validation matrix, and the executor's `02-01-SUMMARY.md` handoff.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@.planning/phases/02-hostile-faction-research-track/02-CONTEXT.md
@.planning/phases/02-hostile-faction-research-track/02-RESEARCH.md
@.planning/phases/02-hostile-faction-research-track/02-VALIDATION.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Establish the structured claim register and read-only parser</name>
  <files>.planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md, tools/verify_xen_khk_evidence.ps1, tools/verify_xen_khk_evidence.Tests.ps1</files>
  <behavior>
    - A payload with duplicate claim-register blocks or malformed JSON fails before any scope decision is accepted.
    - A skeleton with XEN and KHK claims, complete metadata, valid booleans, and one permitted classification per claim passes without X4 runtime access.
    - A duplicate source ID, unknown claim source ID, unallowlisted source kind/path/boundary, or a claim conclusion outside that source's scope fails deterministically.
  </behavior>
  <action>Create the English Markdown evidence artifact with a single fenced `json hostile-claim-register` payload. Its top-level schema must contain `schema_version`, `source_boundary`, `scope`, `coverage`, `sources`, and `claims`. Define `scope` booleans for `xen_primary_pressure`, `khk_observed_when_present`, `autonomous_hostile_minds`, `government_institutions`, `hostile_motives`, `hostile_diplomacy`, `hostile_architecture_selected`, `hostile_write_primitives`, `hostile_control_channels`, `critical_path_dependency`, and `phase8_inventory_only`. Define `coverage.requirements` for RES-01 through RES-03 and `coverage.decisions` for D-01 through D-08. Create a source registry whose entries have `id`, `kind`, `path`, `boundary`, and `allowed_conclusions`. Allow only these documented sources from `02-RESEARCH.md`: `x4-version-dat` (`installed_x4_file`, `version.dat`, installed X4 9.00 boundary); `x4-jobs` (`installed_x4_file`, `08.cat::libraries/jobs.xml`, installed X4 9.00 catalog boundary); `x4-khaak-activity` (`installed_x4_file`, `08.cat::md/khaak_activity.xml`, installed X4 9.00 catalog boundary); `khaakfinder-precedent` (`installed_extension_precedent`, `extensions/z_ram_khaakfinder`, installed extension v101 boundary); and `project-contract` (`repository_contract`, `AGENTS.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `02-CONTEXT.md`, repository planning boundary). Each claim object must contain `id`, `faction`, `area`, `classification`, `source_id`, `permitted_conclusion`, `non_gating`, `future_owner`, and `evidence_needed`; its conclusion must be one of the referenced source's declared allowed conclusions. Include at least one XEN and one KHK claim with actual installed-source provenance so the skeleton is source-connected. Create the PowerShell script as a read-only parser: extract only the named fenced JSON payload, parse it with `ConvertFrom-Json`, reject malformed or duplicate payload blocks, and support `-Stage skeleton` and `-Stage full`. At skeleton stage, verify metadata, scope booleans, coverage arrays, unique claim/source IDs, classification enum, source-registry allowlist, source-reference integrity, and nonempty material provenance/conclusion fields without requiring every area. Create Pester tests with in-memory fixtures covering the valid skeleton and duplicate source ID, unknown source ID, unallowlisted kind/path/boundary, and out-of-scope conclusion failures. Per D-01 through D-08, use no runtime, save, game, package, or network operation and do not treat static configuration as observed-in-X4 behavior.</action>
  <verify>
    <automated>Invoke-Pester -Path tools/verify_xen_khk_evidence.Tests.ps1 -CI; powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify_xen_khk_evidence.ps1 -EvidencePath .planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md -Stage skeleton</automated>
  </verify>
  <done>The artifact has a parseable, versioned claim-register skeleton with separate XEN and KHK source-connected claims; invalid source fixtures fail and the valid skeleton passes without X4 runtime access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate complete XEN/KHK static evidence and deferred runtime claims</name>
  <files>.planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md, tools/verify_xen_khk_evidence.ps1</files>
  <behavior>
    - Full validation rejects a missing faction-area pair, duplicate ID, missing or unallowlisted source registry entry, invalid classification, absent RES/D coverage, or incomplete deferred-claim fields.
    - Full validation rejects only forbidden positive scope booleans and does not reject explanatory narrative wording.
  </behavior>
  <action>Populate the register from the existing current static research. For both `XEN` and `KHK`, create at least one uniquely identified claim for each exact `area`: `state`, `events`, `identity`, `visibility`, `scheduling`, `economy_or_spawning_ownership`, and `control_limits`. Use `observed` only for installed static sources or inspected read-only precedents, `documented` for project contracts, `inferred` for bounded planning conclusions, and `unknown` for runtime facts not established by the source; each row must hold exactly one enum value. Every claim must reference a source registry `source_id` and select only a declared `allowed_conclusions` value. The register must use `unknown` claims for runtime event/export semantics, faction visibility semantics, KHK activity/quota observability, and extension interaction; each such claim must have `non_gating: true`, a concrete future owner, and evidence needed. Implement full-stage checks in the script for both-faction/all-area coverage, exact RES/D coverage, per-row fields, unique claim/source IDs, registry kinds/paths/boundaries, source-reference integrity, conclusion scope, and deferred-claim fields. The script must check only structured positive implementation-scope flags: it must fail when any forbidden `scope` boolean is true or when `phase8_inventory_only` is false, but must not inspect narrative prose for prohibited or deferred phrases.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify_xen_khk_evidence.ps1 -EvidencePath .planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md -Stage full</automated>
  </verify>
  <done>The full verifier proves both factions cover every required category, each claim has structured provenance and one classification, every locked decision and requirement is represented, and runtime gaps remain non-gating structured claims.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the validation matrix and audit the evidence boundary</name>
  <files>.planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md, .planning/phases/02-hostile-faction-research-track/02-VALIDATION.md, tools/verify_xen_khk_evidence.ps1</files>
  <action>Complete `02-VALIDATION.md` as the authoritative Phase 2 matrix. Map RES-01 through RES-03 and D-01 through D-08 to exact register/script checks, commands, evidence level, runtime or manual deferral, and sampling cadence. Preserve the distinction between verified static installation evidence and pending disposable-runtime evidence. In the Markdown narrative around the register, summarize only the permitted conclusions represented by structured rows and make its read-only/non-gating status clear; prose is explanatory rather than a validator input. Audit that each referenced installed source is material, each runtime unknown has a future owner/evidence need/non-gating disposition, and the scope booleans retain observation-only boundaries. Run the full validator once more after this audit; do not add a human checkpoint, runtime probe, dependency, hostile architecture, or control behavior.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify_xen_khk_evidence.ps1 -EvidencePath .planning/phases/02-hostile-faction-research-track/02-XEN-KHK-EVIDENCE.md -Stage full; powershell -NoProfile -Command "$p='.planning/phases/02-hostile-faction-research-track/02-VALIDATION.md'; if(-not (Test-Path -LiteralPath $p)){ throw 'Validation matrix missing' }; foreach($id in @('RES-01','RES-02','RES-03','D-01','D-02','D-03','D-04','D-05','D-06','D-07','D-08')){ if(-not (Select-String -LiteralPath $p -SimpleMatch $id -Quiet)){ throw \"Validation mapping missing: $id\" } }"</automated>
  </verify>
  <done>The evidence artifact, parser, and validation matrix prove the Phase 2 contract from static sources; runtime evidence remains pending, owned, and non-gating, with Phase 8 limited to inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Installed X4 / extension data → claim register | Static sources establish configured behavior, not live campaign facts. |
| Claim register → validator | The parser must validate structured values rather than interpreting narrative prose. |
| Evidence record → downstream planning | Future work could elevate an unknown or static finding into an unapproved hostile implementation contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02-01 | Tampering | Claim provenance and source boundary | medium | mitigate | Tasks 1–2 require a structured source kind, path, version boundary, and permitted conclusion for every claim. |
| T-02-02 | Spoofing | XEN/KHK identity and visibility | medium | mitigate | Task 2 separates static observations and read-only precedents from structured unknown runtime claims. |
| T-02-03 | Elevation of privilege | Hostile implementation scope | high | mitigate | The parser rejects positive hostile-autonomy, architecture, write, control, and critical-path scope flags; Task 3 audits the boundary. |
| T-02-04 | Information disclosure | Research output | low | mitigate | Tasks use repository-relative or installed-source-relative provenance and exclude saves, credentials, raw prompts, and local diagnostic data. |
</threat_model>

<verification>
Run the skeleton verifier after Task 1 and the full verifier after Tasks 2–3. Review only source-backed structured fields; classify any runtime observation need as pending disposable-runtime evidence rather than a Phase 2 blocker.
</verification>

<success_criteria>
1. One versioned Markdown artifact exposes a deterministic structured register covering both hostile factions and every RES-01 category.
2. The read-only parser rejects malformed registers, duplicate IDs, invalid classifications, missing material provenance, incomplete faction/area coverage, missing RES/D coverage, and positive implementation-scope flags.
3. `02-VALIDATION.md` maps every Phase 2 requirement and locked decision to an exact check, evidence level, deferral, and sampling cadence.
4. No runtime X4 action is required to complete this phase; runtime claims remain explicit, owned, and non-gating.
</success_criteria>

<output>
Create `.planning/phases/02-hostile-faction-research-track/02-01-SUMMARY.md` when done.
</output>
