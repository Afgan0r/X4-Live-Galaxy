---
phase: 03-faction-scoped-strategic-state
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - crates/strategic-state/src/faction.rs
  - crates/strategic-state/src/policy.rs
  - crates/strategic-state/src/packet.rs
  - crates/strategic-state/src/derive.rs
  - crates/strategic-state/tests/capability_contract.rs
  - crates/strategic-state/tests/doctrine_priority.rs
autonomous: true
requirements: [MIND-03, INST-01, INST-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every ZYA and ARG packet exposes exactly the same three engine capability contracts and one shared faction-visible fact set to each institution."
    - "The six locked descriptive internal labels are retained as Live Galaxy labels, not represented as official X4 institution names."
    - "The shared scenario fixture produces ZYA defense -> territorial -> economy and ARG economy -> defense -> territorial according to versioned product policy."
  artifacts:
    - "packet module gives institution views references or IDs into one faction-visible snapshot."
    - "capability and doctrine tests fix the capability count, labels, and ordered priorities."
  key_links:
    - "FactionProfile -> three shared Capability contracts -> InstitutionView -> same FactionVisibleSnapshot"
---

<objective>
Expand the tracer into complete ZYA and ARG institution views with shared capability contracts and source-grounded, versioned doctrine priorities.

Purpose: make faction divergence observable without separate engines or private institutional knowledge.
Output: exact three-capability contract, six locked internal labels, two versioned profiles, and deterministic priority fixtures.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-faction-scoped-strategic-state/03-CONTEXT.md
@.planning/phases/03-faction-scoped-strategic-state/03-RESEARCH.md
@.planning/phases/03-faction-scoped-strategic-state/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the exhaustive shared capability and shared-snapshot contract</name>
  <files>crates/strategic-state/src/faction.rs, crates/strategic-state/src/packet.rs, crates/strategic-state/src/derive.rs, crates/strategic-state/tests/capability_contract.rs</files>
  <behavior>
    - Both factions expose precisely defense and military strategy, economy and logistics, and territorial development and infrastructure in fixed capability order.
    - Each faction's three institution views reference the same visible-snapshot identity and cannot obtain raw projection access or institution-private facts.
    - Adding a fourth capability or a diplomacy institution is rejected by the exhaustive type/contract test.
  </behavior>
  <action>Begin with failing capability-contract tests, then add one exhaustive shared `Capability` enum and institution-view type. Per D-04 and D-06 and INST-01/02, create exactly three views for each faction and make all views point to the one immutable faction-visible snapshot already compiled in Plan 03-01. Keep capability contracts engine-level and shared; no per-faction strategy engine, private knowledge store, false-belief model, raw projection handle, or fourth diplomacy view may be introduced. Use an explicit fixed ordering and bounded collection construction so malformed profile input fails closed.</action>
  <verify>
    <automated>cargo test -p strategic-state --test capability_contract &amp;&amp; cargo run -p source-size-lint</automated>
  </verify>
  <done>The public packet contains exactly three shared capability views per faction, all tied to one faction-visible snapshot, with an automated guard against capability-count and data-boundary drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add evidence-labeled ZYA and ARG doctrine profiles with fixed priorities</name>
  <files>crates/strategic-state/src/faction.rs, crates/strategic-state/src/policy.rs, crates/strategic-state/src/packet.rs, crates/strategic-state/tests/doctrine_priority.rs</files>
  <behavior>
    - ZYA labels are `ZYA Defense &amp; Military Strategy`, `ZYA Economy &amp; Logistics`, and `ZYA Territorial Development &amp; Infrastructure`; ARG carries the corresponding ARG labels.
    - The same shared scenario orders ZYA defense, territorial, economy and ARG economy, defense, territorial.
    - A profile version change changes replay input identity; the tests identify priorities as Live Galaxy product policy/inference rather than X4 numeric canon.
  </behavior>
  <action>Write fixture tests before profile code, then add two immutable versioned `FactionProfile` values and doctrine evidence metadata. Per D-05, use exactly these six descriptive Live Galaxy internal labels: ZYA Defense &amp; Military Strategy; ZYA Economy &amp; Logistics; ZYA Territorial Development &amp; Infrastructure; ARG Defense &amp; Military Strategy; ARG Economy &amp; Logistics; ARG Territorial Development &amp; Infrastructure. Ground only the ZYA/Patriarchy and ARG/Federation prefixes in the research-cited faction sources; keep the labels explicitly non-official. Implement the recorded deterministic product-policy inference: in a common fixture, ZYA ranks defense then territorial then economy; ARG ranks economy then defense then territorial. Store priorities and doctrine provenance in the versioned profile and ensure neither is presented as an official X4 numerical statistic.</action>
  <verify>
    <automated>cargo test -p strategic-state --test doctrine_priority &amp;&amp; cargo test -p strategic-state --test capability_contract &amp;&amp; cargo run -p source-size-lint</automated>
  </verify>
  <done>ZYA and ARG use identical capability contracts yet produce their locked differentiated priority orders under the same scenario, with profile version and policy provenance preserved for replay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| faction profile -> institution packet | Product-policy doctrine data controls visible priority ordering. |
| shared snapshot -> institution view | Any fork could create private or divergent institution knowledge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-03 | Tampering | `FactionProfile` priority order | medium | mitigate | Versioned immutable profiles and shared-scenario fixtures assert exact order and replay identity. |
| T-03-04 | Information disclosure | institution view constructor | high | mitigate | Views hold only the compiled faction-visible snapshot identity and capability projection; contract tests deny private/raw inputs. |
| T-03-SC | Tampering | Cargo dependencies | high | mitigate | No package-manager installation: use only existing workspace-local crates. |
</threat_model>

<verification>
Run focused capability and doctrine tests after their respective commits, followed by `cargo run -p source-size-lint` and `cargo test --workspace` at the wave boundary.
</verification>

<success_criteria>
INST-01/02 and the institution component of MIND-03 are met by three shared capabilities, one shared fact set per faction, and deterministic faction-divergent profiles.
</success_criteria>

<output>
Create `.planning/phases/03-faction-scoped-strategic-state/03-02-SUMMARY.md` when done.
</output>
