---
phase: 03-faction-scoped-strategic-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/strategic-state/Cargo.toml
  - crates/strategic-state/src/lib.rs
  - crates/strategic-state/src/faction.rs
  - crates/strategic-state/src/fact.rs
  - crates/strategic-state/src/policy.rs
  - crates/strategic-state/src/derive.rs
  - crates/strategic-state/tests/tracer_packet.rs
  - crates/strategic-state/tests/visibility_contract.rs
autonomous: true
requirements: [OBS-04, OBS-05, MIND-02, MIND-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One frozen accepted projection can produce bounded ZYA and ARG strategic packets for the same four supported fact families without raw transport, model, storage, report, or X4 mutation access."
    - "Every source fact stays Available, Unknown, Stale, Inaccessible, or Unsupported; unavailable input never becomes a numeric or empty default."
    - "A recorded versioned visibility policy filters facts before every derived score or primitive."
  artifacts:
    - "crates/strategic-state is a pure workspace crate depending only on observation-domain and observation-ingest."
    - "tracer_packet and visibility_contract prove paired faction derivation and the public compiler contract."
  key_links:
    - "observation_ingest::ProjectionSnapshot -> strategic_state::derive_packet for ZYA and ARG -> immutable faction-visible snapshots -> paired packets"
    - "SectionQuality and visibility policy -> explicit FactAvailability -> score/primitive eligibility"
---

<objective>
Create the pure, fail-closed strategic-state boundary and prove paired end-to-end ZYA and ARG packet paths from one accepted frozen projection.

Purpose: establish the deterministic trust boundary Phase 4 persists and Phase 5 consumes, while preserving the Phase 1 quality contract.
Output: a dependency-free-from-I/O `strategic-state` crate, availability-aware visible facts, versioned visibility policy, and focused red-green tests.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-faction-scoped-strategic-state/03-CONTEXT.md
@.planning/phases/03-faction-scoped-strategic-state/03-RESEARCH.md
@.planning/phases/01-read-only-observation-spine/01-08-SUMMARY.md
@crates/observation-domain/src/section.rs
@crates/observation-ingest/src/snapshot.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compile one accepted projection into paired bounded faction packets</name>
  <files>Cargo.toml, crates/strategic-state/Cargo.toml, crates/strategic-state/src/lib.rs, crates/strategic-state/src/derive.rs, crates/strategic-state/tests/tracer_packet.rs</files>
  <behavior>
    - RED: one accepted fixture projection carrying economic, military, territorial, and threat observations compiles through the public `ProjectionSnapshot` boundary into one ZYA and one ARG immutable packet, each naming the policy version and having no I/O boundary.
    - RED: each faction packet includes visible, stale, inaccessible, and unsupported fixture entries for every supported family—economic, military, territorial, and threat—and records them explicitly rather than substituting values.
    - RED: the threat family retains XEN as shared primary pressure and KHK only when observed, with availability preserved for both faction packets.
    - GREEN: a fixed maximum packet fact and primitive count rejects excess data with a typed error before packet creation.
  </behavior>
  <action>Create the workspace member and a pure `strategic-state` crate with a focused crate entry and one cohesive compiler module, keeping every Rust source file at or below 200 physical lines. Add only workspace-local `observation-domain` and `observation-ingest` dependencies. Start with a failing integration test that builds one `observation_ingest::ProjectionSnapshot` through its public contract and invokes the compiler for both ZYA and ARG. The shared fixture must carry economic, military, territorial, and threat facts, and each faction result must cover visible, stale, inaccessible, and unsupported entries in every family. Implement a production-quality vertical path through quality interpretation, policy filtering, shared threat inclusion, bounded fact/primitive construction, and immutable paired packet output. Per D-01, D-02, and D-03, preserve own-state and policy-authorized operational facts, allow static resource-map potential only through a separately typed static class, and retain unavailable reasons as data. Per OBS-04, OBS-05, and MIND-03, retain XEN as shared primary pressure and observed KHK as a typed threat subject; introduce neither hostile minds nor an action/mutation vocabulary. Use checked counters, typed errors, ordered collections, and no recoverable-boundary panic paths.</action>
  <verify>
    <automated>cargo test -p strategic-state --test tracer_packet &amp;&amp; cargo run -p source-size-lint</automated>
  </verify>
  <done>One accepted four-family fixture passes from `ProjectionSnapshot` to paired bounded immutable ZYA and ARG packets; quality states, capacity refusal, and XEN/KHK threat availability are externally asserted; the crate contains no model, persistence, reporting, transport, or X4 mutation dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce pre-derivation faction visibility and Phase 1 compatibility</name>
  <files>crates/strategic-state/src/fact.rs, crates/strategic-state/src/policy.rs, crates/strategic-state/src/derive.rs, crates/strategic-state/tests/visibility_contract.rs</files>
  <behavior>
    - ZYA and ARG compile from the same authoritative base projection under recorded policy versions, retain equal policy-authorized base facts, and expose distinct permitted views only where their policies differ.
    - A foreign changing production, fleet, movement, or station fact absent from either faction's policy is represented as Inaccessible and cannot appear in a score or primitive input.
    - A policy-authorized static resource-map fact remains available independently of changing foreign operational facts.
    - Equivalent Phase 1 quality descriptors preserve the compiler's availability classification; the test documents current runtime-scope semantics as an assumption and fails if a future public contract cannot distinguish it.
  </behavior>
  <action>Write failing paired-faction contract tests, then implement a versioned, allowlist visibility policy and availability-bearing visible-fact model. Derive both faction views from the same authoritative base projection, retain equal policy-authorized base facts, and record any permitted-view difference with a typed policy reason. Filter projection records before score or primitive derivation and retain a typed exclusion/audit reason without exposing foreign content. Per D-01 through D-03 and MIND-02, require policy version, owner/subject classification, freshness/quality, and visibility decision for every candidate fact. Add a compatibility fixture that uses Phase 1's public `SectionQuality` values and accepted snapshot structure; label its runtime-scope mapping as a pending Phase 1 runtime-semantics assumption, not observed X4 behavior. Keep the compiler fail-closed when a scope, subject category, quality mapping, or policy version is unsupported.</action>
  <verify>
    <automated>cargo test -p strategic-state --test visibility_contract &amp;&amp; cargo test -p strategic-state --test tracer_packet &amp;&amp; cargo run -p source-size-lint</automated>
  </verify>
  <done>ZYA and ARG receive views derived from one authoritative base under recorded policies, with both equal authorized base facts and distinct permitted-view cases proven; static potential and changing operational data follow distinct policy paths; all unavailable states and unsupported mappings remain explicit and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| accepted projection -> packet compiler | Accepted but incomplete telemetry-derived state crosses into faction-visible strategic input. |
| visibility policy -> derived packet | A policy error could disclose changing foreign operational facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Information disclosure | `policy.rs` visibility decision | high | mitigate | Versioned allowlist filtering precedes all derivation; negative authorization contract tests prove excluded foreign facts cannot influence output. |
| T-03-02 | Tampering | `fact.rs` availability conversion | high | mitigate | Preserve explicit quality/availability states and refuse unsupported mappings or bound excesses before packet creation. |
| T-03-SC | Tampering | Cargo dependencies | high | mitigate | No package-manager installation: use only existing workspace-local crates. |
</threat_model>

<verification>
Run the focused tracer and visibility contracts after each task, then `cargo run -p source-size-lint`, strict workspace Clippy through that tool, and `cargo test --workspace` at the wave boundary.
</verification>

<success_criteria>
OBS-04/05 and MIND-02/03 are satisfied by pure, bounded, deterministic paired ZYA and ARG packets compiled from the same frozen Phase 1 projection, covering economic, military, territorial, and threat fact families with shared XEN pressure and observed KHK semantics, without leaking inaccessible foreign operational facts.
</success_criteria>

<output>
Create `.planning/phases/03-faction-scoped-strategic-state/03-01-SUMMARY.md` when done.
</output>
