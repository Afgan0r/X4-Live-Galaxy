---
phase: 03-faction-scoped-strategic-state
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - crates/strategic-state/src/fact.rs
  - crates/strategic-state/src/packet.rs
  - crates/strategic-state/src/derive.rs
  - crates/strategic-state/src/primitive.rs
  - crates/strategic-state/src/fingerprint.rs
  - crates/strategic-state/tests/shadow_primitive_contract.rs
  - crates/strategic-state/tests/packet_determinism.rs
  - crates/strategic-state/tests/mutation_baseline.rs
autonomous: true
requirements: [OBS-04, OBS-05, MIND-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A finite typed Shadow-primitive allowlist carries bounded, evidence-referenced planning candidates; Executive-only output owns bilateral posture dispositions without institution authority, negotiation, or X4 relation mutation."
    - "Equivalent frozen snapshots and policy/profile versions yield canonically ordered facts, priorities, primitives, admission inputs, and a stable replay fingerprint."
    - "Order permutation, stale/unavailable input, and capacity boundary cases are covered by property and contract tests; mutation results are measured and reviewed rather than thresholded by assumption."
  artifacts:
    - "ExecutiveDiplomacyInputs, ShadowPrimitive, and fingerprint modules remain pure strategic-state domain artifacts."
    - "Focused Shadow-primitive, determinism, and mutation-baseline tests exist."
  key_links:
    - "shared visible facts -> typed ShadowPrimitive candidates and ExecutiveDiplomacyInputs -> finite planning-only allowlist"
    - "canonical packet fields, primitive keys, evidence references, and policy/profile version -> replay fingerprint"
---

<objective>
Complete the finite Shadow-primitive allowlist, Executive diplomatic input, and canonical replay contract, then measure the pure compiler's mutation baseline.

Purpose: give later Shadow deliberation a bounded, reproducible input packet without creating diplomacy execution or persistence.
Output: bounded planning-only primitive contract, Executive-only bilateral input, total ordering/fingerprint, property tests, and a recorded cargo-mutants baseline procedure.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-faction-scoped-strategic-state/03-CONTEXT.md
@.planning/phases/03-faction-scoped-strategic-state/03-RESEARCH.md
@.planning/phases/03-faction-scoped-strategic-state/03-01-SUMMARY.md
@.planning/phases/03-faction-scoped-strategic-state/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the finite planning-only Shadow primitive allowlist</name>
  <files>crates/strategic-state/src/fact.rs, crates/strategic-state/src/packet.rs, crates/strategic-state/src/derive.rs, crates/strategic-state/src/primitive.rs, crates/strategic-state/tests/shadow_primitive_contract.rs</files>
  <behavior>
    - The exhaustive `ShadowPrimitive` contract permits only `DefensiveReadiness`, `LogisticsAllocationPriority`, `TerritorialDevelopmentPriority`, and Executive-only `BilateralPostureDisposition`; every variant has a typed owner, canonical sort key, priority 1 through 100, a closed horizon enum, at most eight evidence references, and the packet permits at most four candidates.
    - The Executive posture variant permits only preserve relations, de-escalate, increase pressure, and seek limited coordination; institution views cannot create it.
    - Unsupported primitive kind, owner, payload value, evidence reference, or collection overflow returns a typed rejection before any packet/fingerprint output.
    - The contract tests prove allowlist closure, payload/evidence bounds, Executive ownership, XEN/KHK evidence retention, and absence of an effect-execution or model-output route.
  </behavior>
  <action>Start with failing Shadow-primitive contract tests, then add an exhaustive typed `ShadowPrimitive` allowlist derived exclusively from already filtered faction-visible facts: `DefensiveReadiness` owned by defense; `LogisticsAllocationPriority` owned by economy; `TerritorialDevelopmentPriority` owned by territorial; and `BilateralPostureDisposition` owned only by the Executive. Each candidate must contain priority 1 through 100, a closed typed horizon enum, at most eight canonical evidence references to fact identities, an explicit canonical sort key, and no free-text payload; each packet permits at most four candidates. Per D-07 and D-08, the Executive variant models only preserve relations, de-escalate, increase pressure, and seek limited coordination; it is planning input, not negotiation or execution. Per D-03 and D-09, reject unsupported kinds/owners/payloads/evidence and unavailable required facts before candidate or fingerprint construction. Do not add an effect executor, X4 command, model-output interface, diplomacy institution, agent, message exchange, persistence, provider call, or report.</action>
  <verify>
    <automated>cargo test -p strategic-state --test shadow_primitive_contract &amp;&amp; cargo run -p source-size-lint</automated>
  </verify>
  <done>The only derivable Shadow candidates are the four bounded typed variants, each with owned evidence references and a canonical key; the bilateral posture variant is Executive-only; unsupported values and all execution/model routes are rejected or absent by contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonicalize packet ordering, admission inputs, and replay fingerprint</name>
  <files>crates/strategic-state/src/packet.rs, crates/strategic-state/src/derive.rs, crates/strategic-state/src/fingerprint.rs, crates/strategic-state/tests/packet_determinism.rs</files>
  <behavior>
    - Permuting otherwise equivalent projection insertion order produces equal ordered facts, capability views, priorities, bounded primitives, admission inputs, and fingerprint.
    - Changing policy or faction-profile version changes the replay identity.
    - Duplicate sort-key ambiguity, oversized input, and unavailable required facts return typed failure rather than nondeterministic output.
  </behavior>
  <action>Write property-style permutation and allowlist-boundary tests with deterministic fixtures before implementation. Per D-09 and MIND-04, assign total typed sort keys to facts, priorities, capabilities, the four `ShadowPrimitive` variants, evidence references, and Executive inputs; use ordered storage and canonical serialization or equivalent deterministic byte construction solely for the fingerprint. Include frozen snapshot identity, visibility-policy and faction-profile versions, primitive kind/owner/payload/evidence, and capacity-relevant canonical data in replay inputs. Assert equal input produces an equal fingerprint and that changed primitive evidence, order key, policy/profile version, unsupported value, duplicate key, or saturation cannot produce an accepted ambiguous fingerprint. Keep all behavior pure and do not serialize a persistence record, call a model, or expose a runtime/X4 claim.</action>
  <verify>
    <automated>cargo test -p strategic-state --test packet_determinism &amp;&amp; cargo test -p strategic-state --test shadow_primitive_contract &amp;&amp; cargo run -p source-size-lint</automated>
  </verify>
  <done>Equivalent frozen inputs produce one canonical packet and replay identity irrespective of insertion order, while semantically different policy/profile versions and invalid canonical inputs are distinguished deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record the strategic compiler mutation baseline and survivor disposition</name>
  <files>crates/strategic-state/tests/mutation_baseline.rs, .planning/phases/03-faction-scoped-strategic-state/03-03-SUMMARY.md</files>
  <precondition>`cargo-mutants` is available at the version already selected by the project Rust-test policy; if unavailable, stop with the tool/version evidence rather than installing an unreviewed package.</precondition>
  <behavior>
    - The baseline test identifies visibility, availability, canonical-order, capability-count, and score-bound regressions by externally observable result.
    - Every reported surviving mutant is classified as a new behavioral test, equivalent, unsupported operator, or explicit bounded gap in the summary evidence.
  </behavior>
  <action>Add a focused behavioral regression target for the high-risk compiler branches, run the normal strategic-state tests first, then run a scoped `cargo mutants -p strategic-state` baseline. Per the Rust test skill, record command, runner version, duration, killed/surviving/invalid/equivalent counts, and the disposition of each survivor in the execution summary; do not invent a score threshold or broaden mutation testing to X4 adapters. If the runner is absent or fails before mutation execution, preserve that measured failure and reason in the summary for Phase 8 rather than silently treating mutation coverage as complete.</action>
  <verify>
    <automated>cargo test -p strategic-state --test mutation_baseline &amp;&amp; cargo mutants -p strategic-state -- --test mutation_baseline</automated>
  </verify>
  <done>A measured, reviewable pure-domain mutation baseline exists or an explicit tool-availability failure is recorded; no unmeasured percentage is used as an acceptance threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| faction-visible facts -> Executive slice | Executive-only relationships must not become institution knowledge or an X4 command. |
| canonical fields -> replay fingerprint | Ambiguous ordering or missing version input can break reproducibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-05 | Elevation of privilege | `ShadowPrimitive` and Executive posture | high | mitigate | Exhaustive four-variant typed allowlist assigns institution/Executive ownership, bounds payloads/evidence/capacity, and exposes no execution, X4 command, or model-output interface. |
| T-03-06 | Tampering | canonical fingerprint | high | mitigate | Total sort keys cover primitive kind, owner, payload, and evidence; versioned inputs, permutation/property tests, and typed rejection cover ambiguous, unsupported, or oversized input. |
| T-03-07 | Repudiation | mutation evidence | medium | mitigate | Record runner/version/command/baseline and individual survivor dispositions in the Phase 3 summary. |
| T-03-SC | Tampering | cargo-mutants execution | high | mitigate | Require existing reviewed tool availability; absent tooling halts the mutation task with evidence and does not install a package. |
</threat_model>

<verification>
Run focused Shadow-primitive and determinism targets per task; run source-size/strict Clippy via `cargo run -p source-size-lint`; then `cargo test --workspace` and the measured scoped cargo-mutants baseline before phase verification.
</verification>

<success_criteria>
MIND-04 completes with reproducible canonical packet output; the finite typed Shadow-primitive allowlist and Executive-only bilateral posture remain bounded, planning-only, and non-mutating; mutation evidence is measured rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/03-faction-scoped-strategic-state/03-03-SUMMARY.md` when done.
</output>
