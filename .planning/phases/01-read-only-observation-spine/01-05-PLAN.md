---
phase: 01-read-only-observation-spine
plan: 05
type: tdd
wave: 3
depends_on: [01-02]
files_modified:
  - crates/x4-bridge/src/lib.rs
  - crates/x4-bridge/tests/session_state_machine.rs
  - crates/x4-bridge/tests/backpressure_contract.rs
autonomous: true
requirements: [OBS-01, OBS-07, OBS-08]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Compatible Rust bridge reconnect creates a fresh session without requiring X4 restart (D-04)."
    - "Protocol-major/build/capability incompatibility becomes sticky degraded state with a specific X4-restart-required reason (D-05)."
    - "Unsupported, oversized, or saturated traffic receives bounded backpressure and cannot grow an unbounded queue or block game-side work (D-01, D-06)."
  artifacts:
    - path: crates/x4-bridge/tests/session_state_machine.rs
      provides: "Compatible reconnect and terminal incompatible-session behavior."
    - path: crates/x4-bridge/tests/backpressure_contract.rs
      provides: "Frame/queue limit and bounded backpressure tests."
  key_links:
    - from: crates/x4-bridge/src/lib.rs
      to: crates/observation-ingest/src/lib.rs
      via: "Only compatible ordered sessions may submit frames to batch admission."
      pattern: "CapabilityDecision"
  prohibitions:
    - requirement_id: OBS-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT turn bridge unavailability or saturation into unbounded game-thread work, an implicit compatible session, or a forced X4 restart for a compatible Rust-only reconnect (D-01, D-04)."
---
<objective>
Harden the tracer's telemetry session boundary with explicit compatibility, reconnect, sequencing, and backpressure rules.

Purpose: preserve the locked distinction between a bridge release reconnect and game-facing incompatibility.
Output: a tested session state machine and bounded ingress contract for the bridge seam.
</objective>
<execution_context>@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-read-only-observation-spine/01-CONTEXT.md
@.planning/phases/01-read-only-observation-spine/01-RESEARCH.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md</context>

## Artifacts this phase produces

`crates/x4-bridge/src/lib.rs`, `crates/x4-bridge/tests/session_state_machine.rs`, and `crates/x4-bridge/tests/backpressure_contract.rs`, plus bridge symbols `SessionState`, `SessionHello`, `SessionGeneration`, `SequenceNumber`, `FrameLimits`, `BackpressureOutcome`, and `RestartRequirement`.

## Task acceptance criteria

<acceptance_criteria task="Task 1">The state-machine suite proves compatible reconnect changes only bridge generation, while protocol/build/capability mismatch remains terminal with a specific X4 restart reason.</acceptance_criteria>
<acceptance_criteria task="Task 2">The suite proves exact and one-over frame/queue limits, saturation, unsupported kind, and terminal state each return deterministic nonblocking non-admission.</acceptance_criteria>

<feature><name>Bounded telemetry session state machine</name><files>crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/session_state_machine.rs, crates/x4-bridge/tests/backpressure_contract.rs</files><behavior>A compatible reconnect increments only bridge session generation; incompatible protocol major, mandatory-capability gap, or game-facing build mismatch is terminal until X4 restart; queue/frame ceilings report a nonblocking bounded outcome.</behavior><implementation>Write all state transition tests first. Implement explicit typed state/sequence values, no booleans for compatibility, and a pure transition function. The adapter-facing result must be telemetry health/backpressure only. Do not create return/report mechanics, acknowledgement routing, command dispatch, save operations, or an automatic recovery path from a game-side incompatibility.</implementation></feature>
<tasks>
<task type="tdd" tdd="true"><name>Task 1: Red-green-refactor compatibility and restart ownership transitions</name><files>crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/session_state_machine.rs</files><read_first>.planning/phases/01-read-only-observation-spine/01-CONTEXT.md; .agents/skills/live-galaxy-rust-tests/SKILL.md</read_first><behavior>Test compatible reconnect, protocol-major mismatch, missing read-only capability, game-build mismatch, stale session sequence, and that terminal mismatch cannot return to compatible absent X4 restart.</behavior><action>Begin with failing transition tests, then implement the session state machine and exact restart owner/reason. Keep compatibility metadata distinct from user authentication and accept no frame before a compatible decision, implementing D-04 and D-05.</action><verify><automated>cargo test -p x4-bridge session_state_machine</automated></verify><done>Rust-only reconnect and X4-restart-required states are explicitly distinct, deterministic, and covered by executable tests.</done></task>
<task type="tdd" tdd="true"><name>Task 2: Red-green-refactor bounded frame and queue backpressure</name><files>crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/backpressure_contract.rs</files><read_first>.planning/phases/01-read-only-observation-spine/01-RESEARCH.md; .agents/skills/live-galaxy-rust-tests/SKILL.md</read_first><behavior>Test exact limits, one-step-over limits, saturated queue behavior, unsupported frame kind, and no admission after a terminal session decision.</behavior><action>Start RED, add checked limits and a bounded ingress outcome that never waits for model/strategy work. Pass only accepted compatible frames onward and make all nonaccepted outcomes explicit health/rejection evidence, implementing D-01 and D-06.</action><verify><automated>cargo test -p x4-bridge backpressure_contract</automated></verify><done>All limit boundaries return deterministic nonblocking outcomes and no state allows a generic effect frame.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| adapter session hello → bridge state | Claimed compatibility and build identity are untrusted until matched. |
| producer enqueue → bridge ingress | Game-facing telemetry can attempt to exceed local bounds. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-10 | S/T | session handshake | high | mitigate | Closed capability/build/protocol comparison transitions to explicit terminal states. |
| T-01-11 | D | queue ingress | high | mitigate | Checked frame and queue limits with nonblocking backpressure outcomes. |
| T-01-12 | E | frame family | high | mitigate | Unsupported variants are rejected before intake and protocol remains telemetry-only. |
| T-01-SC | Tampering | crates install | high | mitigate | Pin legitimacy-audited dependencies only. |
</threat_model>
<verification>Run both focused bridge suites, then `cargo test -p x4-bridge` and `cargo test --workspace`.</verification>
<success_criteria>Every session disposition and every one-step-over bound is mechanically testable; compatible reconnection never becomes a hidden X4 restart demand.</success_criteria>
<output>Create `.planning/phases/01-read-only-observation-spine/01-05-SUMMARY.md` when done.</output>
