---
phase: 01-read-only-observation-spine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - crates/observation-domain/Cargo.toml
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/tests/foundation_contract.rs
autonomous: true
requirements: [OBS-02, OBS-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The workspace builds a pure observation-domain crate with typed identity, source, observation time, monotonic version, and explicit section quality (D-07, D-08)."
    - "The foundation test distinguishes known-empty, unknown, partial, stale, and unsupported data without X4, network, save, or model access."
  artifacts:
    - path: Cargo.toml
      provides: "Pinned Cargo workspace for the Phase 1 crates."
    - path: crates/observation-domain/src/lib.rs
      provides: "Pure typed observation contracts used by later bridge and ingest plans."
    - path: crates/observation-domain/tests/foundation_contract.rs
      provides: "Executable foundation contract for identity and section-state semantics."
  key_links:
    - from: crates/observation-domain/tests/foundation_contract.rs
      to: crates/observation-domain/src/lib.rs
      via: "Public typed contracts are exercised without transport or X4 globals."
      pattern: "SectionDescriptor"
---

<objective>
Establish the narrow workspace and pure domain foundation that later telemetry layers consume.

Purpose: make required observation facts explicit before the tracer crosses the X4 and bridge seams.
Output: pinned workspace, domain crate, and executable identity/quality foundation tests.
</objective>
<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-read-only-observation-spine/01-CONTEXT.md
@.planning/phases/01-read-only-observation-spine/01-RESEARCH.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
</context>

## Artifacts this phase produces

`Cargo.toml`, `rust-toolchain.toml`, `crates/observation-domain/Cargo.toml`, `crates/observation-domain/src/lib.rs`, and `crates/observation-domain/tests/foundation_contract.rs`. New public symbols are `EntityId`, `ObservationSource`, `ObservationTime`, `ObservationVersion`, `SectionDescriptor`, and `SectionQuality`.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Start red with the pure typed-observation foundation slice</name>
  <files>Cargo.toml, rust-toolchain.toml, crates/observation-domain/Cargo.toml, crates/observation-domain/src/lib.rs, crates/observation-domain/tests/foundation_contract.rs</files>
  <read_first>.planning/phases/01-read-only-observation-spine/01-CONTEXT.md; .agents/skills/live-galaxy-rust-conventions/SKILL.md; .agents/skills/live-galaxy-rust-tests/SKILL.md</read_first>
  <behavior>Test a typed entity with deterministic source/time/version and each explicit section state.</behavior>
  <action>Write the failing public-contract test first, then create the pinned workspace and pure typed values. Require identity, source, time, and monotonic version; use explicit section-state enums rather than null/empty conventions, per D-07 and D-08.</action>
  <verify><automated>cargo test -p observation-domain foundation_contract</automated></verify>
  <acceptance_criteria>The focused test starts red, then passes; it proves a typed entity retains identity/source/time/version and all five declared section states remain distinguishable.</acceptance_criteria>
  <done>The narrow domain foundation is green and ready for a separate cross-layer tracer.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Pin the foundation dependency and package boundary</name>
  <files>Cargo.toml, crates/observation-domain/Cargo.toml, crates/observation-domain/tests/foundation_contract.rs</files>
  <read_first>.planning/phases/01-read-only-observation-spine/01-RESEARCH.md</read_first>
  <behavior>Test the crate compiles with exact pinned dependency versions and contains no transport/effect API.</behavior>
  <action>Pin only research-legitimacy-cleared dependencies and keep all public contracts domain-only. Do not introduce bridge I/O, X4 integration, persistence, reports, commands, generic actions, or game-effect vocabulary.</action>
  <verify><automated>cargo test -p observation-domain &amp;&amp; cargo metadata --no-deps --format-version 1</automated></verify>
  <acceptance_criteria>The metadata command identifies a reproducible one-crate foundation, and the focused suite passes with no X4, save, network, model, or mutation dependency.</acceptance_criteria>
  <done>The workspace boundary is deterministic and remains below the plan file-surface limit.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| typed construction → domain contract | Invalid observation facts must not be representable as ambiguous primitives. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-01 | T | typed observation contract | high | mitigate | Opaque identity/version/time values and explicit section states prevent ambiguous admission inputs. |
| T-01-SC | Tampering | crates install | high | mitigate | Pin only legitimacy-audited dependencies from the Phase 1 research audit. |
</threat_model>
<verification>Run the focused foundation contract during red-green-refactor, then `cargo test -p observation-domain`.</verification>
<success_criteria>The workspace establishes deterministic typed observation facts without crossing into bridge, adapter, or mutation layers.</success_criteria>
<output>Create `.planning/phases/01-read-only-observation-spine/01-01-SUMMARY.md` when done.</output>
