---
phase: 01-read-only-observation-spine
plan: 03
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - crates/observation-domain/src/lib.rs
  - crates/observation-domain/tests/identity_section_contract.rs
  - crates/observation-domain/tests/reconciliation_policy.rs
autonomous: true
requirements: [OBS-02, OBS-03, OBS-06]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Each accepted entity/event has typed identity, source, capture time, and a monotonic state/event version (D-07)."
    - "A section distinguishes known-empty from unknown, unsupported, partial, fresh, and stale instead of using null/empty as a substitute (D-08)."
    - "A scope-complete marker is required before runtime-discovered removals are reconciled; one item beyond configured collection limits is rejected or leaves the section partial (D-09)."
    - "Equal-version matching content is idempotent, equal-version conflicting content is rejected, and canonical ordering is stable."
  artifacts:
    - path: crates/observation-domain/tests/identity_section_contract.rs
      provides: "Typed identity, version equality, known-empty, and section-state behavioral coverage."
    - path: crates/observation-domain/tests/reconciliation_policy.rs
      provides: "Scope completion, collection-boundary, overflow/precision, and deterministic ordering coverage."
  key_links:
    - from: crates/observation-domain/src/lib.rs
      to: crates/observation-ingest/src/lib.rs
      via: "The ingest service consumes the domain reconciliation decision and canonical sort key."
      pattern: "Reconciliation"
  prohibitions:
    - requirement_id: OBS-03
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT fabricate a known-empty or fresh world-state fact from omitted, unsupported, stale, or incomplete observation data (D-08)."
---

<objective>
Specify and test the typed observation semantics that downstream strategy may safely consume.

Purpose: make invalid state hard to construct and resolve every Phase 1 identity/quality/reconciliation edge before ingest expands.
Output: deterministic domain policy and red-green-refactor tests for identity, section quality, limits, ordering, and complete-scope reconciliation.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-read-only-observation-spine/01-CONTEXT.md
@.planning/phases/01-read-only-observation-spine/01-RESEARCH.md
@.planning/phases/01-read-only-observation-spine/01-PATTERNS.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
</context>

## Artifacts this phase produces

`crates/observation-domain/src/lib.rs`, `crates/observation-domain/tests/identity_section_contract.rs`, and `crates/observation-domain/tests/reconciliation_policy.rs`, plus the domain symbols they require: `ObservationSource`, `ObservationTime`, `EventId`, `SectionFreshness`, `SectionCoverage`, `SectionQuality`, `CompleteMarker`, `ReconciliationDecision`, and `CanonicalObservationKey`.

<feature>
  <name>Typed observation identity, quality, and scope-complete reconciliation</name>
  <files>crates/observation-domain/src/lib.rs, crates/observation-domain/tests/identity_section_contract.rs, crates/observation-domain/tests/reconciliation_policy.rs</files>
  <behavior>
    - Duplicate stable identity plus equal version/content yields idempotent acceptance; equal version with divergent content yields a typed conflict.
    - Empty collection is known-empty only when its same-scope successful completion is present; otherwise it is partial or unknown as declared.
    - Freshness is independent per section; a heartbeat or neighbouring section never refreshes detail.
    - One entry over an explicit collection ceiling is rejected or marks the section partial without numeric truncation, overflow, or precision loss.
    - Scope-complete reconciliation permits only deterministic removals from the enumerated runtime scope and returns a canonically ordered result.
  </behavior>
  <implementation>Write the failing tests first, then introduce opaque identifiers/times/versions, non-lossy quantity representations, explicit section state enums, and a pure reconciliation function. Decide collection-limit rejection versus partiality in the domain policy and apply it consistently. Use stable sort keys, checked conversion/arithmetic, and typed errors; do not infer a fixed X4 map, job count, or entity universe, per D-07 through D-09.</implementation>
</feature>

<tasks>
<task type="tdd" tdd="true">
  <name>Task 1: Red-green-refactor typed identity and independent section quality</name>
  <files>crates/observation-domain/src/lib.rs, crates/observation-domain/tests/identity_section_contract.rs</files>
  <read_first>.planning/phases/01-read-only-observation-spine/01-RESEARCH.md; .agents/skills/live-galaxy-rust-conventions/SKILL.md; .agents/skills/live-galaxy-rust-tests/SKILL.md</read_first>
  <behavior>Test stable identity/source/time/version, duplicate equality, equal-version conflict, and that an empty section becomes known-empty only after a validated successful same-scope completion marker; without that marker it remains partial or unknown, alongside unsupported, stale, and independent freshness.</behavior>
  <action>Start RED, implement the smallest typed domain surface to pass, then refactor without erasing explicit states. Add the executable empty-section assertion: only a validated successful same-scope completion marker may produce known-empty; its absence must preserve partial or unknown. Treat source/time/version as required on every strategic entity/event and retain a deterministic canonical ordering key, implementing D-07 and D-08.</action>
  <verify><automated>cargo test -p observation-domain identity_section_contract</automated></verify>
  <acceptance_criteria>The focused test proves duplicate equal content is idempotent, equal-version divergent content is rejected, and an empty section is known-empty only after a validated successful same-scope completion marker; without that marker it remains partial or unknown.</acceptance_criteria>
  <done>All identity/version/section-state cases are green and no state representation collapses known-empty with missing data.</done>
</task>
<task type="tdd" tdd="true">
  <name>Task 2: Red-green-refactor scope-complete runtime reconciliation policy</name>
  <files>crates/observation-domain/src/lib.rs, crates/observation-domain/tests/reconciliation_policy.rs</files>
  <read_first>.planning/phases/01-read-only-observation-spine/01-PATTERNS.md; .agents/skills/live-galaxy-rust-tests/SKILL.md</read_first>
  <behavior>Test empty batches, exact and one-over collection limits, overflow/precision rejection, incomplete scope preservation, and canonically ordered complete-scope tombstones.</behavior>
  <action>Start RED, implement a pure scope-complete reconciliation decision that applies only to runtime observations and validated markers. Use checked bounds and deterministic ordering; preserve the previous membership for incomplete scans and never encode fixed-map assumptions, per D-09.</action>
  <verify><automated>cargo test -p observation-domain reconciliation_policy</automated></verify>
  <acceptance_criteria>The focused test proves exact and one-over limits, checked numeric boundaries, deterministic ordering, and no removal without a valid same-scope completion marker.</acceptance_criteria>
  <done>Only a valid same-scope complete marker allows removals; all bound, precision, and ordering edges have executable coverage.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| normalized envelope → domain policy | Runtime facts become typed snapshot candidates. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-04 | T | identity/version comparison | high | mitigate | Typed stable identity and conflict-aware monotonic version policy reject ambiguous equality. |
| T-01-05 | I | section quality | medium | mitigate | Explicit known-empty/unknown/partial/stale/unsupported types prevent fabricated facts. |
| T-01-06 | D | collection reconciliation | high | mitigate | Checked caps, non-lossy quantities, and scope-complete-only deletion bound resource use and loss. |
| T-01-SC | Tampering | crates install | high | mitigate | Pin only legitimacy-audited crates in workspace manifests. |
</threat_model>
<verification>Run each focused test during its red-green-refactor cycle, then `cargo test -p observation-domain` and `cargo test --workspace`.</verification>
<success_criteria>All listed semantic edge predicates are executable and deterministic before any multi-frame ingest state is changed.</success_criteria>
<output>Create `.planning/phases/01-read-only-observation-spine/01-03-SUMMARY.md` when done.</output>
