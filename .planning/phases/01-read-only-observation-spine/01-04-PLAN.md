---
phase: 01-read-only-observation-spine
plan: 04
type: tdd
wave: 4
depends_on: [01-03, 01-05]
files_modified:
  - crates/observation-ingest/src/lib.rs
  - crates/observation-ingest/tests/atomic_rejection.rs
  - crates/observation-ingest/tests/batch_reconciliation.rs
  - tests/fixtures/malformed-envelope.json
  - tests/fixtures/oversized-envelope.json
  - tests/fixtures/reordered-duplicate-sequence.json
autonomous: true
requirements: [OBS-06, OBS-07]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Malformed, oversized, duplicate, stale, and out-of-order frames retain the byte-equivalent last accepted snapshot while yielding bounded rejection evidence (D-06)."
    - "A valid multi-frame batch is validated completely before it changes the projection; a malformed marker cannot partially apply a batch."
    - "Runtime assets are reconciled only after the domain's validated complete-scope decision."
  artifacts:
    - path: crates/observation-ingest/tests/atomic_rejection.rs
      provides: "Last-accepted snapshot preservation tests for hostile input."
    - path: crates/observation-ingest/tests/batch_reconciliation.rs
      provides: "Atomic batch and complete-marker integration tests."
    - path: tests/fixtures/reordered-duplicate-sequence.json
      provides: "Deterministic adversarial frame sequence."
  key_links:
    - from: crates/observation-ingest/src/lib.rs
      to: crates/observation-domain/src/lib.rs
      via: "Admission invokes the typed validation and reconciliation policy before commit."
      pattern: "ReconciliationDecision"
  prohibitions:
    - requirement_id: OBS-07
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT replace or partially corrupt the last accepted snapshot after hostile, stale, reordered, oversized, or incomplete traffic (D-06)."
---

<objective>
Make multi-frame observation admission atomic, bounded, replay-safe, and evidence-producing.

Purpose: expand the tracer into the actual acceptance barrier that shields future state consumers from bad telemetry.
Output: adversarial fixtures, atomic admission tests, bounded rejection evidence, and complete-scope batch reconciliation.
</objective>
<execution_context>@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-read-only-observation-spine/01-CONTEXT.md
@.planning/phases/01-read-only-observation-spine/01-RESEARCH.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md</context>

## Artifacts this phase produces

`crates/observation-ingest/src/lib.rs`, `crates/observation-ingest/tests/atomic_rejection.rs`, `crates/observation-ingest/tests/batch_reconciliation.rs`, `tests/fixtures/malformed-envelope.json`, `tests/fixtures/oversized-envelope.json`, and `tests/fixtures/reordered-duplicate-sequence.json`. New symbols: `AdmissionOutcome`, `RejectionEvidence`, `AcceptedProjection`, `validate_batch`, and `admit_batch`.

## Task acceptance criteria

<acceptance_criteria task="Task 1">The fixture suite proves malformed, oversized, duplicate, stale, reordered, and equal-version-conflict input leaves a pre-existing snapshot structurally unchanged with bounded evidence.</acceptance_criteria>
<acceptance_criteria task="Task 2">The integration suite proves one valid batch commits once, an invalid marker commits nothing, replay is idempotent, and only complete scope reconciles removals.</acceptance_criteria>

<feature><name>Atomic hostile-input rejection and complete-scope reconciliation</name><files>crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/atomic_rejection.rs, crates/observation-ingest/tests/batch_reconciliation.rs, tests/fixtures/malformed-envelope.json, tests/fixtures/oversized-envelope.json, tests/fixtures/reordered-duplicate-sequence.json</files><behavior>Malformed schema, oversized payload, stale version, duplicate replay, out-of-order version, invalid marker, and equal-version conflict leave the accepted projection unchanged; a valid ordered batch commits exactly once.</behavior><implementation>Begin red for every adversarial fixture. Decode with hard limits, validate all envelopes and markers before a single commit, and retain only bounded diagnostic metadata without raw rejected payloads. Use the Plan 03 domain policy for runtime discovery/reconciliation and the Plan 05 session identity/order contract; do not add persistence, saves, reporting, or effects.</implementation></feature>
<tasks>
<task type="tdd" tdd="true"><name>Task 1: Prove every rejected frame preserves the accepted projection</name><files>crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/atomic_rejection.rs, tests/fixtures/malformed-envelope.json, tests/fixtures/oversized-envelope.json, tests/fixtures/reordered-duplicate-sequence.json</files><read_first>.planning/phases/01-read-only-observation-spine/01-VALIDATION.md; .agents/skills/live-galaxy-rust-tests/SKILL.md</read_first><behavior>Test malformed, oversize, duplicate, stale, reordered, and equal-version-conflict paths against a pre-existing accepted snapshot.</behavior><action>Write failing fixture-driven tests, then implement bounded decode/validation and rejection evidence. Compare snapshots structurally after every rejection, cap evidence retention, and avoid logging raw payload content, implementing D-06.</action><verify><automated>cargo test -p observation-ingest atomic_rejection</automated></verify><done>Every hostile-input test is green and proves no partial projection change occurs.</done></task>
<task type="tdd" tdd="true"><name>Task 2: Commit validated batches and runtime reconciliation atomically</name><files>crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/batch_reconciliation.rs</files><read_first>crates/observation-domain/tests/reconciliation_policy.rs; .agents/skills/live-galaxy-rust-tests/SKILL.md</read_first><behavior>Test all-valid batch commits once, invalid completion marker rolls back all changes, replay is idempotent, and valid same-scope completion reconciles runtime entities.</behavior><action>Start with failing integration tests, add a transaction-shaped in-memory projection boundary, and call domain reconciliation only after complete validation. Preserve ordering and last accepted state for all failed transactions, implementing D-06 and D-09.</action><verify><automated>cargo test -p observation-ingest batch_reconciliation</automated></verify><done>Batch state changes are all-or-nothing and reconciliation cannot delete from incomplete or invalid runtime observation scopes.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| decoded batch → accepted projection | Hostile transport content attempts to alter normalized state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-07 | T | batch admission | high | mitigate | Validate every frame/marker before one atomic projection commit. |
| T-01-08 | D | decoder/diagnostics | high | mitigate | Enforce payload/evidence ceilings and retain metadata rather than raw bodies. |
| T-01-09 | R | rejection outcome | medium | mitigate | Correlate bounded reason, session, and sequence with no mutation of accepted state. |
| T-01-SC | Tampering | crates install | high | mitigate | Use only pinned, legitimacy-audited dependencies. |
</threat_model>
<verification>Run both focused tests and `cargo test --workspace`; the rejection cases must execute from fixtures, not source-text checks.</verification>
<success_criteria>One malformed element cannot change any part of an accepted batch, while one complete valid runtime scope can reconcile exactly once.</success_criteria>
<output>Create `.planning/phases/01-read-only-observation-spine/01-04-SUMMARY.md` when done.</output>
