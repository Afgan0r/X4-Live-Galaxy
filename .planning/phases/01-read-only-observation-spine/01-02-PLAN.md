---
phase: 01-read-only-observation-spine
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - crates/observation-ingest/Cargo.toml
  - crates/observation-ingest/src/lib.rs
  - crates/observation-ingest/tests/tracer_ingest.rs
  - crates/x4-bridge/Cargo.toml
  - crates/x4-bridge/src/lib.rs
  - crates/x4-bridge/tests/protocol_contract.rs
  - extensions/live_galaxy/content.xml
  - extensions/live_galaxy/lua/live_galaxy_telemetry.lua
  - extensions/live_galaxy/md/live_galaxy_observation.xml
  - tests/fixtures/tracer-observation.json
autonomous: true
requirements: [OBS-01, OBS-08]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A bounded telemetry-only hello and one observation from a fake X4 adapter travel through bridge and ingest into an atomically accepted snapshot (D-01, D-02)."
    - "No protocol operation expresses a game-state effect, report, acknowledgement, generic action, or command (D-03)."
    - "The X4 shell is statically valid and has an executable fake-contract fallback; a standalone Lua runner is used only after the runtime probe confirms compatibility."
  artifacts:
    - path: crates/observation-ingest/src/lib.rs
      provides: "Single-frame tracer admission port."
    - path: crates/x4-bridge/src/lib.rs
      provides: "Telemetry-only frame family and capability decision."
    - path: extensions/live_galaxy/lua/live_galaxy_telemetry.lua
      provides: "Thin bounded producer shell consuming an injected fake adapter."
    - path: extensions/live_galaxy/md/live_galaxy_observation.xml
      provides: "Read-only scheduler registration shell."
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_telemetry.lua
      to: tests/fixtures/tracer-observation.json
      via: "Named fake-adapter contract emits the versioned telemetry fixture."
      pattern: "produce_observation"
    - from: crates/x4-bridge/src/lib.rs
      to: crates/observation-ingest/src/lib.rs
      via: "Compatible telemetry frame is passed to single-frame admission."
      pattern: "admit_tracer_frame"
---

<objective>
Prove one production-quality, read-only observation path across the X4 shell, bridge, and ingest seam.

Purpose: retain tracer-first architecture proof while keeping foundation and adapter/bridge surfaces separately bounded.
Output: narrow extension shell, bridge/ingest crates, one deterministic fixture, and fake-contract verification.
</objective>
<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-read-only-observation-spine/01-CONTEXT.md
@.planning/phases/01-read-only-observation-spine/01-RESEARCH.md
@.planning/phases/01-read-only-observation-spine/01-PATTERNS.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
</context>

## Artifacts this phase produces

`crates/observation-ingest/Cargo.toml`, `crates/observation-ingest/src/lib.rs`, `crates/observation-ingest/tests/tracer_ingest.rs`, `crates/x4-bridge/Cargo.toml`, `crates/x4-bridge/src/lib.rs`, `crates/x4-bridge/tests/protocol_contract.rs`, `extensions/live_galaxy/content.xml`, `extensions/live_galaxy/lua/live_galaxy_telemetry.lua`, `extensions/live_galaxy/md/live_galaxy_observation.xml`, and `tests/fixtures/tracer-observation.json`. New public symbols are `TelemetryFrame`, `CapabilityDecision`, `AcceptedSnapshot`, `admit_tracer_frame`, and `produce_observation`.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Start red and admit one fake-adapter telemetry observation end-to-end</name>
  <files>crates/observation-ingest/Cargo.toml, crates/observation-ingest/src/lib.rs, crates/observation-ingest/tests/tracer_ingest.rs, crates/x4-bridge/Cargo.toml, crates/x4-bridge/src/lib.rs, crates/x4-bridge/tests/protocol_contract.rs, tests/fixtures/tracer-observation.json</files>
  <read_first>01-01-PLAN.md; .agents/skills/live-galaxy-rust-conventions/SKILL.md; .agents/skills/live-galaxy-rust-tests/SKILL.md</read_first>
  <behavior>Test compatible hello plus one bounded typed frame; test that the closed protocol family has no effect-bearing form.</behavior>
  <action>Write the failing cross-crate tracer tests first, then decode the deterministic fixture, require a compatible capability decision, enforce a frame ceiling, and atomically create the first accepted snapshot. Implement D-01 through D-03 without persistence, report routing, model calls, generic dispatch, or game effects.</action>
  <verify><automated>cargo test -p x4-bridge protocol_contract &amp;&amp; cargo test -p observation-ingest tracer_ingest</automated></verify>
  <acceptance_criteria>The focused end-to-end tests pass from the fixture through capability admission and ingest; attempting to construct a non-telemetry protocol form fails at the public contract boundary.</acceptance_criteria>
  <done>One bounded read-only observation crosses every Rust layer and is locally verified.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add the thin X4 producer shell with named fake-contract fallback</name>
  <files>extensions/live_galaxy/content.xml, extensions/live_galaxy/lua/live_galaxy_telemetry.lua, extensions/live_galaxy/md/live_galaxy_observation.xml, crates/x4-bridge/tests/protocol_contract.rs, tests/fixtures/tracer-observation.json</files>
  <read_first>.agents/skills/live-galaxy-x4-integration/SKILL.md; .agents/skills/live-galaxy-x4-tests/SKILL.md</read_first>
  <behavior>Test a named `fake_x4_adapter_contract` produces the fixture, one-section work stops at its ceiling, and unavailable data is explicit.</behavior>
  <action>Create the extension identity, minimal read-only MD hook, and Lua producer that accepts a narrow injected adapter. Add the named executable `fake_x4_adapter_contract` to the Rust protocol suite: it loads the deterministic fake adapter result, independently decodes the emitted fixture, and asserts bounded telemetry-only output. Parse both XML files. Add a later pure-Lua Busted command only as conditional work after the disposable runtime syntax probe selects a compatible runner; do not claim that command runs now. Keep native API selection, pipe calls, and cadence pending runtime evidence, per D-01 through D-03 and D-09.</action>
  <verify><automated>cargo test -p x4-bridge fake_x4_adapter_contract; powershell -NoProfile -Command "[xml](Get-Content -Raw 'extensions/live_galaxy/content.xml') | Out-Null; [xml](Get-Content -Raw 'extensions/live_galaxy/md/live_galaxy_observation.xml') | Out-Null"</automated></verify>
  <acceptance_criteria>The named fake contract and XML parse checks pass without a standalone Lua runner; the plan records `busted extensions/live_galaxy/tests/telemetry_contract.lua` as conditional on a successful runtime syntax probe.</acceptance_criteria>
  <done>The X4 shell is telemetry-only, bounded, fake-contract-verified, and does not overstate X4 runtime evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| X4 adapter → telemetry frame | Runtime-originated observations cross into bridge validation. |
| bridge decoder → accepted snapshot | Bounded serialization becomes a normalized fact only after admission. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-02 | S/T | hello and capability frame | high | mitigate | Closed compatibility decision rejects missing required capabilities. |
| T-01-03 | D | producer/frame boundary | high | mitigate | Frame ceiling and fake-contract evidence bound work before ingest. |
| T-01-04 | E | protocol vocabulary | high | mitigate | Negative public-contract test excludes effect-bearing and generic action forms. |
| T-01-SC | Tampering | crates install | high | mitigate | Use only legitimacy-audited pinned dependencies. |
</threat_model>
<verification>Run the two focused Rust suites and XML parsing after each task. The fake contract is local evidence; X4 runtime evidence remains pending.</verification>
<success_criteria>A real deterministic fixture crosses fake producer, bridge, and ingest, while the only Lua verification claimed before a runtime probe is the executable fake-contract fallback.</success_criteria>
<output>Create `.planning/phases/01-read-only-observation-spine/01-02-SUMMARY.md` when done.</output>
