---
phase: 01-read-only-observation-spine
plan: 06
type: execute
wave: 4
depends_on: [01-02, 01-05]
files_modified:
  - extensions/live_galaxy/lua/live_galaxy_telemetry.lua
  - extensions/live_galaxy/lua/live_galaxy_normalize.lua
  - extensions/live_galaxy/lua/live_galaxy_scheduler.lua
  - extensions/live_galaxy/md/live_galaxy_observation.xml
  - extensions/live_galaxy/tests/telemetry_contract.lua
  - extensions/live_galaxy/tests/scheduler_contract.lua
  - tests/x4-disposable/README.md
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-06, OBS-08]
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The thin adapter discovers supplied runtime sector/asset/capacity/ownership observations in bounded slices and never assumes a fixed map or job count (D-09)."
    - "Lua normalization carries typed identity/source/time/version and explicit section quality into the bridge fixture schema (D-07, D-08)."
    - "Producer scheduling honors bridge backpressure, save suppression, and unavailable/unsupported data without mutation or synchronous bridge/model work (D-01 through D-03)."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_normalize.lua
      provides: "Pure normalization/serialization boundary with explicit observation metadata."
    - path: extensions/live_galaxy/lua/live_galaxy_scheduler.lua
      provides: "Cooperative bounded sampling/backpressure policy."
    - path: extensions/live_galaxy/tests/telemetry_contract.lua
      provides: "Fake-adapter telemetry shape tests."
    - path: extensions/live_galaxy/tests/scheduler_contract.lua
      provides: "Bounded scheduling and unavailable-backend tests."
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_normalize.lua
      to: crates/x4-bridge/src/lib.rs
      via: "The pure serializer emits the Plan 02 telemetry schema and Plan 05 session/backpressure disposition inputs."
      pattern: "protocol_version"
  prohibitions:
    - requirement_id: OBS-08
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT use Lua, Mission Director, native calls, or an adapter escape hatch to mutate X4 state; the adapter is observation production only (D-01, D-03)."
---
<objective>
Expand the X4 side into a thin, testable, bounded observation producer while leaving exact runtime API claims pending evidence.

Purpose: make adapter responsibilities explicit and independently testable without treating a fake as proof of installed X4 behavior.
Output: pure Lua normalization/scheduling modules, fake-adapter tests, MD wiring, and a pending-disposable-evidence guide.
</objective>
<execution_context>@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-read-only-observation-spine/01-CONTEXT.md
@.planning/phases/01-read-only-observation-spine/01-RESEARCH.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md</context>

## Artifacts this phase produces

`extensions/live_galaxy/lua/live_galaxy_telemetry.lua`, `extensions/live_galaxy/lua/live_galaxy_normalize.lua`, `extensions/live_galaxy/lua/live_galaxy_scheduler.lua`, `extensions/live_galaxy/md/live_galaxy_observation.xml`, `extensions/live_galaxy/tests/telemetry_contract.lua`, `extensions/live_galaxy/tests/scheduler_contract.lua`, and `tests/x4-disposable/README.md`. New symbols: `normalize_section`, `serialize_telemetry`, `sample_slice`, `enqueue_or_backpressure`, `observe_runtime_scope`, and `save_suppressed`.

## Task acceptance criteria

<acceptance_criteria task="Task 1">The Rust fake-contract decoder and XML parse prove typed telemetry serialization and explicit unsupported/partial states without requiring a standalone Lua runner.</acceptance_criteria>
<acceptance_criteria task="Task 2">The bridge contract and XML parse prove one bounded scheduler slice, explicit backpressure/unavailable/save-suppressed outcomes, and no effect request; pure-Lua execution stays conditional on the runtime probe.</acceptance_criteria>

<tasks>
<task type="auto" tdd="true"><name>Task 1: Implement pure normalized runtime-observation serialization against a fake adapter</name><files>extensions/live_galaxy/lua/live_galaxy_telemetry.lua, extensions/live_galaxy/lua/live_galaxy_normalize.lua, extensions/live_galaxy/tests/telemetry_contract.lua</files><read_first>.agents/skills/live-galaxy-x4-integration/SKILL.md; .agents/skills/live-galaxy-x4-tests/SKILL.md; crates/x4-bridge/src/lib.rs</read_first><behavior>Test identity/source/time/version propagation, explicit known-empty/partial/unknown/unsupported states, and runtime scope observations with a fake adapter returning no fixed-map assumptions.</behavior><action>Write failing pure Lua contract cases before implementation once the runtime syntax probe selects the compatible runner; until then create the deterministic fake-contract source and run the Rust schema decoder against its fixtures. Keep raw X4 globals behind the injected adapter, serialize only the versioned telemetry vocabulary, and reject/mark unsupported data explicitly, implementing D-07 through D-09.</action><verify><automated>cargo test -p x4-bridge protocol_contract; powershell -NoProfile -Command "[xml](Get-Content -Raw 'extensions/live_galaxy/md/live_galaxy_observation.xml') | Out-Null"</automated></verify><acceptance_criteria>The Rust fake-contract decoder and XML parse prove typed telemetry serialization and explicit unsupported/partial states without requiring a standalone Lua runner.</acceptance_criteria><done>Pure adapter policy is isolated from X4 globals, emits only typed telemetry, and has fake-contract coverage or an explicit runner-probe dependency rather than an invented Lua runner.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement cooperative bounded scheduling and backpressure handling</name><files>extensions/live_galaxy/lua/live_galaxy_scheduler.lua, extensions/live_galaxy/md/live_galaxy_observation.xml, extensions/live_galaxy/tests/scheduler_contract.lua, tests/x4-disposable/README.md</files><read_first>.agents/skills/live-galaxy-x4-tests/SKILL.md; crates/x4-bridge/src/lib.rs; .planning/phases/01-read-only-observation-spine/01-VALIDATION.md</read_first><behavior>Test one bounded slice per scheduler tick, saturation backpressure, unavailable bridge, save suppression, and that no scheduler branch emits an effect request.</behavior><action>Begin with fake scheduler tests where a compatible local Lua runner is available; otherwise keep the same cases as the written fake contract and use Rust-side fixture verification. Wire the MD hook narrowly, bound each slice and queue handoff, suppress observation safely during save-sensitive windows, and emit health/partiality rather than waiting. Record exact runtime APIs and cadence as pending disposable evidence; do not add effect, report, acknowledgement, or generic-action paths, per D-01 through D-03.</action><verify><automated>cargo test -p x4-bridge backpressure_contract; powershell -NoProfile -Command "[xml](Get-Content -Raw 'extensions/live_galaxy/md/live_galaxy_observation.xml') | Out-Null"</automated></verify><acceptance_criteria>The bridge contract and XML parse prove one bounded scheduler slice, explicit backpressure/unavailable/save-suppressed outcomes, and no effect request; pure-Lua execution stays conditional on the runtime probe.</acceptance_criteria><done>The extension has one cooperative telemetry schedule with bounded backpressure behavior and a clear separation between fake/local evidence and pending X4 smoke proof.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| X4 globals/native APIs → injected adapter | Game runtime behavior is isolated from pure policy. |
| adapter scheduler → bridge queue | Producer work must stay bounded even when bridge capacity changes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-13 | D | scheduler | high | mitigate | Cooperative one-slice policy, save suppression, and backpressure avoid blocking waits. |
| T-01-14 | T | runtime observations | medium | mitigate | Pure normalizer makes missing/unsupported/partial metadata explicit before serialization. |
| T-01-15 | E | Lua/MD seam | high | mitigate | Narrow telemetry-only API and contract coverage exclude game-effect paths. |
| T-01-SC | Tampering | crates install | high | mitigate | Use pinned legitimacy-audited Rust dependencies only. |
</threat_model>
<verification>Run static XML and Rust schema checks now; run pure Lua tests only after the recorded syntax/runtime probe selects a compatible runner. Report each layer separately.</verification>
<success_criteria>The X4 adapter design remains thin, nonblocking, and telemetry-only, with no unsupported runtime behavior claimed as proven.</success_criteria>
<output>Create `.planning/phases/01-read-only-observation-spine/01-06-SUMMARY.md` when done.</output>
