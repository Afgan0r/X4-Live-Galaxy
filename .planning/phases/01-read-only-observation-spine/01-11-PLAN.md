---
phase: 01-read-only-observation-spine
plan: 11
type: tdd
wave: 8
depends_on: [01-10]
files_modified: [extensions/live_galaxy/lua/live_galaxy_normalize.lua, extensions/live_galaxy/lua/live_galaxy_telemetry.lua, extensions/live_galaxy/lua/live_galaxy_trace_config.lua, extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, crates/observation-ingest/src/wire.rs, crates/observation-ingest/src/runtime_facts.rs, crates/observation-ingest/src/model.rs, crates/observation-ingest/src/batch.rs, crates/observation-ingest/tests/runtime_facts_contract.rs, crates/x4-bridge/src/listener.rs, crates/x4-bridge/src/protocol.rs, crates/x4-bridge/src/server/session_gate.rs, crates/x4-bridge/tests/protocol_contract.rs, crates/x4-bridge/tests/named_pipe_contract.rs, crates/x4-bridge/tests/reconnect_idempotency.rs, tests/x4-disposable/01-probe-procedure.md, tests/x4-disposable/01-probe-evidence.md]
autonomous: false
gap_closure: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-06, OBS-07, OBS-08, VAL-06]
user_setup:
  - service: X4 Foundations 9.00
    why: "A disposable Creative Custom session is required to establish the runtime fact envelope and the explicit X4-restart deployment gate; Plan 01-10's separate embedded-Lua syntax/version probe and local runner gate must already be complete."
estimate: {tokens: 45000, raw_tokens: 45000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "A complete accepted runtime observation retains bounded normalized sector, asset, capacity, and ownership facts with stable identities, source, bridge-stamped receipt Unix time, optional separately named X4 game time, and version rather than reducing them to an opaque string (D-07, D-09)."
    - "A missing, malformed, oversized, or unrepresentable required fact class yields an explicit unsupported or incomplete disposition and cannot replace the last accepted snapshot (D-06, D-08, D-09)."
    - "When `facts_unsupported` occurs, one opt-in disposable developer trace records only the four-class diagnostic vector—metadata type, owner-ID validity, selected-sector capacity result class, and first-cargo ware-limit result class—without raw values, identifiers, payloads, or a widened scan (D-06, D-08, D-09)."
    - "The fact-bearing envelope is a game-facing protocol v2: a running v1 X4 client fails closed with an explicit restart requirement, while a compatible Rust bridge restart against a running v2 X4 client retains reconnect semantics (D-04, D-05)."
    - "The only X4-facing runtime behavior remains one-way, bounded telemetry; no effect-bearing vocabulary or persistence/model/report work is introduced (D-01, D-02, D-03)."
  artifacts:
    - path: crates/observation-ingest/src/wire.rs
      provides: "Strict versioned v2 observation-envelope schema containing bounded normalized runtime fact records."
    - path: crates/observation-ingest/tests/runtime_facts_contract.rs
      provides: "Rust admission contracts for required facts, strict untrusted clock handling, bridge receipt-time authority, replay inputs, bounds, and last-snapshot preservation."
    - path: extensions/live_galaxy/tests/x4_discovery_contract.lua
      provides: "Pure/fake producer contracts proving every required discovered fact survives Lua normalization and serialization."
    - path: crates/x4-bridge/tests/reconnect_idempotency.rs
      provides: "Protocol-version deployment and compatible v2 reconnect contract coverage."
    - path: crates/x4-bridge/tests/protocol_contract.rs
      provides: "Receipt-clock injection contracts proving the bridge, not Lua, supplies accepted Unix time."
    - path: tests/x4-disposable/01-probe-evidence.md
      provides: "One isolated classified X4 evidence row for the fact-bearing v2 telemetry path and, if needed, one sanitized capability-vector row."
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
      to: extensions/live_galaxy/lua/live_galaxy_normalize.lua
      via: "Bounded raw runtime values become a typed fact record only after identity, value, count, and explicit-state validation."
    - from: extensions/live_galaxy/lua/live_galaxy_telemetry.lua
      to: crates/observation-ingest/src/wire.rs
      via: "The v2 serializer and strict Rust decoder share the bounded fact-envelope contract."
    - from: crates/x4-bridge/src/protocol.rs
      to: crates/x4-bridge/src/server/session_gate.rs
      via: "The v2 capability controls whether admission may begin or an X4 restart is required."
    - from: crates/x4-bridge/src/listener.rs
      to: crates/observation-ingest/src/batch.rs
      via: "After a frame passes strict schema and semantic admission, the bridge-owned receipt clock supplies the accepted Unix observation time; X4 game time remains a distinct untrusted domain value."
    - from: extensions/live_galaxy/lua/live_galaxy_trace_config.lua
      to: extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
      via: "A default-off, attempt-scoped developer switch permits at most one sanitized four-class result vector before the aggregate unsupported disposition is retained."
  prohibitions:
    - requirement_id: OBS-08
      category: product
      status: resolved
      verification: judgment
      statement: "This closure adds only X4-to-Rust observation facts and validation; it adds no commands, reports, acknowledgements, persistence, model activity, or generic game mutation."
---

<objective>
Close CR-02 and correct the confirmed aggregated `facts_unsupported` diagnostic gap: retain the bounded typed v2 envelope and add one opt-in, attempt-scoped capability vector that can identify the failed existing predicate without treating any candidate as a proved X4 API absence.

Purpose: a partial observation is valid only when the required discovered fact classes are representable and retained; the bridge, after strict admission, owns the Unix receipt timestamp, while X4 game time remains separately named and never gains epoch meaning.
Output: protocol-v2 fact-envelope schema, Lua producer serialization without host-wall-clock access, Rust receipt-time admission and reconnect contracts, and one isolated X4 retry-evidence gate with a four-class sanitized diagnostic vector.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-read-only-observation-spine/01-CONTEXT.md
@.planning/phases/01-read-only-observation-spine/01-10-PLAN.md
@.planning/phases/01-read-only-observation-spine/01-REVIEW.md
@.planning/debug/phase-1-d09-discovery-gap.md
@.planning/debug/phase1-lua-runner-gap.md
@.planning/debug/phase1-plan11-clock-unavailable.md
@.planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/lua/SKILL.md
</context>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Admit one complete bounded v2 runtime-fact observation with bridge receipt-time authority</name>
<files>crates/observation-ingest/src/wire.rs, crates/observation-ingest/src/runtime_facts.rs, crates/observation-ingest/src/model.rs, crates/observation-ingest/src/batch.rs, crates/observation-ingest/tests/runtime_facts_contract.rs, crates/x4-bridge/src/listener.rs, crates/x4-bridge/src/protocol.rs, crates/x4-bridge/src/server/session_gate.rs, crates/x4-bridge/tests/protocol_contract.rs, crates/x4-bridge/tests/named_pipe_contract.rs, crates/x4-bridge/tests/reconnect_idempotency.rs</files>
<read_first>crates/observation-ingest/src/wire.rs; crates/observation-ingest/src/runtime_facts.rs; crates/observation-ingest/src/model.rs; crates/observation-ingest/src/batch.rs; crates/observation-domain/src/observation.rs; crates/x4-bridge/src/listener.rs; crates/x4-bridge/src/protocol.rs; crates/x4-bridge/src/server/session_gate.rs; crates/x4-bridge/tests/protocol_contract.rs; crates/x4-bridge/tests/named_pipe_contract.rs; crates/x4-bridge/tests/reconnect_idempotency.rs; .planning/debug/phase1-plan11-clock-unavailable.md</read_first>
<behavior>
- RED: a canonical v2 observation with no client Unix timestamp, one stable sector, one stable asset associated with that sector, bounded capacity values, explicit ownership, and an optional distinct X4 game-time value is accepted through the pipe state machine and remains inspectable from the accepted normalized projection with a positive bridge receipt Unix time.
- RED: a deterministic injected receipt clock yields the same recorded receipt timestamp across replay fixtures; an X4 game-time value is retained exactly as an optional domain value and never changes receipt-time, duplicate, or version ordering.
- RED: an unknown legacy client timestamp field, an invalid X4 game-time value when present, or a receipt-clock failure rejects the frame atomically with bounded evidence and preserves the last accepted projection.
- RED: omission, invalid identity/value, duplicate fact identity, excess collection count, excess string length, or an unrepresentable required class rejects the batch with bounded metadata and leaves the previous accepted projection byte-for-byte unchanged.
- RED: the v1 game capability is terminal with an explicit restart requirement; a bridge restart is accepted without restarting X4 only when the running client advertises the unchanged v2 capability and advances generation.
</behavior>
<action>Start with failing Rust contract tests. Define the strict v2 envelope so the Lua producer sends no Unix-epoch timestamp: it sends each retained sector, asset, capacity, and ownership fact with typed stable identity, source relationship, monotonic observation version, bounded value representation, explicit availability/quality state, and only an optional separately named X4 game-time value. Deny unknown fields and apply size/count limits before domain construction; validate an X4 game-time value, when supplied, as a finite nonnegative bounded game-domain number, retain it for diagnostics/replay, and never convert it to epoch time or use it for ordering. At the bridge ingress boundary, inject a receipt-clock interface and call it only after the frame has passed strict schema and semantic admission; construct `ObservationTime` exclusively from that positive bridge receipt Unix value, retain the exact receipt value in accepted/replay inputs, and make the production implementation use the bridge host clock while tests use a fixed clock. Retain atomic batch admission, rejection-evidence limits, duplicate/out-of-order policy, deterministic ordering, and completed-snapshot reconciliation. Continue to reject ambiguous identities, missing required fact classes, unsupported legacy timestamp fields, invalid supplied game time, and receipt-clock failure without changing the prior accepted projection. Preserve the explicit `live-galaxy-observation-v2` capability/build contract, D-04 compatible bridge reconnect, and D-05 restart requirement for a running v1 X4 client. Implement D-01, D-04 through D-09; do not add return-side protocol discriminants or any non-telemetry authority.</action>
<verify><automated>cargo test -p observation-ingest --test runtime_facts_contract; cargo test -p x4-bridge --test protocol_contract; cargo test -p x4-bridge --test named_pipe_contract; cargo test -p x4-bridge --test reconnect_idempotency; cargo fmt --check; cargo clippy --workspace --all-targets -- -D warnings</automated></verify>
<done>A bounded canonical v2 observation carries all four D-09 fact classes into Rust's accepted projection with bridge-stamped positive Unix receipt time, preserves optional X4 game time as a distinct validated replay input, and makes invalid clock input or receipt authority atomic rejection while compatibility tests still distinguish a v1 restart gate from a v2-compatible bridge reconnect.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Produce exactly the admitted v2 facts from the bounded read-only Lua seam</name>
<files>extensions/live_galaxy/lua/live_galaxy_normalize.lua, extensions/live_galaxy/lua/live_galaxy_telemetry.lua, extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua</files>
<precondition>Plan 01-10 recorded the X4 embedded-Lua syntax/version result, provisioned its checksum- and version-verified project-local runner, and passed the pure discovery suite; Task 1's v2 Rust fact-envelope tests are green.</precondition>
<read_first>extensions/live_galaxy/lua/live_galaxy_normalize.lua; extensions/live_galaxy/lua/live_galaxy_telemetry.lua; extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua; extensions/live_galaxy/lua/live_galaxy_runtime.lua; extensions/live_galaxy/tests/x4_discovery_contract.lua; crates/observation-ingest/tests/runtime_facts_contract.rs; .planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md</read_first>
<behavior>
- RED: a deterministic fake adapter produces the exact canonical v2 fact envelope consumed by Task 1 with no host-clock API, including one sector, one asset, capacity, ownership, and a separately named X4 game-time value with sorted stable identities.
- RED: the fake has no `os` table or host-clock function, yet a complete fact payload still reaches telemetry; protected `C.GetCurrentGameTime` supplies X4 game time only when it returns a valid game-domain value.
- RED: each required class missing, malformed, unbounded, or unsupported results in an explicit incomplete or unsupported disposition and suppresses the observation frame for that cycle; none of those paths emits an accepted-looking metadata-only observation.
- RED: bounded scans stop before excess work or allocation, Lua output remains within the measured frame ceiling, and the existing hello, heartbeat, health, complete-marker, retry, and reconnect sequence remains unchanged except for its v2 capability/build identifiers.
</behavior>
<action>Write the failing pure/fake Lua contracts before changing the producer. Remove the host-standard-library Unix-time dependency from the injected discovery adapter and from the emitted observation shape. Extend the bounded read-only X4 adapter with protected `C.GetCurrentGameTime` access only for a separately named game-time value; it must be converted with `tonumber`, accepted only when finite and nonnegative, and represented as unavailable when absent, malformed, or failing rather than fabricated or reinterpreted as epoch time. Preserve Plan 01-10's observed-call-shape limits, explicit provenance/unknown classifications, and single-cycle work bounds. Make normalization validate and canonically order every nested identity and value before serialization, using the same required-class and size/count constraints as Rust. The serializer must emit the strict v2 envelope without a client Unix timestamp, not a lossy projection or a stringified nested payload. Runtime production must advance its scheduler slot and emit bounded health only when any required fact class cannot be represented; game-time absence is explicit diagnostic state and cannot replace a required fact-class decision. Update the hello capability/build identifier to v2 exactly as Task 1 specifies, while preserving the established one-way frame cadence, pipe retry outcome, and reconnect behavior. Keep all X4 globals behind the injected adapter, perform no installed-file mutation, save access, command, report, acknowledgement, persistence, model call, or game-state mutation. Apply D-01 through D-03 and D-06 through D-09.</action>
<verify><automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; cargo test -p observation-ingest --test runtime_facts_contract; cargo test -p x4-bridge --test protocol_contract; cargo test -p x4-bridge --test named_pipe_contract; cargo test -p x4-bridge --test reconnect_idempotency; cargo test --workspace</automated></verify>
<done>The fake-adapter producer emits a bounded canonical v2 envelope without host-clock access; its optional X4 game time is distinct and validated, every required D-09 fact is admitted with bridge receipt-time authority, and any absent/unrepresentable required class stays explicit and cannot become an accepted observation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
<name>Task 3: Identify one `facts_unsupported` predicate with a bounded disposable capability vector</name>
<files>extensions/live_galaxy/lua/live_galaxy_trace_config.lua, extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, tests/x4-disposable/01-probe-procedure.md, tests/x4-disposable/01-probe-evidence.md</files>
<precondition>The existing strict-v2 retry is recorded as `facts_unsupported`, Plan 01-10's embedded-Lua and local-runner gates plus Tasks 1-2 automated checks are green, and the user can operate a new disposable Creative Custom X4 9.00 session with no save-file access.</precondition>
<read_first>extensions/live_galaxy/lua/live_galaxy_trace_config.lua; extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua; extensions/live_galaxy/tests/x4_discovery_contract.lua; tests/x4-disposable/01-probe-procedure.md; tests/x4-disposable/01-probe-evidence.md; .planning/debug/phase1-plan11-facts-unsupported.md; .planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md; .agents/skills/live-galaxy-x4-integration/SKILL.md; .agents/skills/live-galaxy-x4-tests/SKILL.md</read_first>
<behavior>
- RED: with the capability probe disabled, the discovery adapter emits no capability-vector event and preserves its current bounded `facts_unsupported` behavior.
- RED: with one valid nonempty attempt ID and the capability probe enabled, the selected-sector path emits exactly one vector containing exactly four result classes: `metadata_type`, `owner_id_validity`, `sector_capacity`, and `first_cargo_ware_limit`; the vector contains no candidate-sector ID, owner ID, name, macro, ware name, numeric value, raw payload, or native error text.
- RED: the selected-sector capacity result class distinguishes `ok`, `call_error`, `wrong_type`, and `invalid_value`; the first-cargo ware-limit result class additionally permits `not_applicable` only when the selected sector has no cargo entry. Metadata and owner checks report only their contract-result classes, never source values.
- RED: a missing, empty, oversized, or second attempt ID, a second probe emission, or any unexpected trace-write failure suppresses the vector, retains the aggregate unsupported disposition, and adds no observation, scan, enumeration, retry, or mutation behavior.
</behavior>
<what-built>A developer-only diagnostic seam that exposes exactly the information lost by the combined completeness predicate and no game-state, payload, or identifying content.</what-built>
<how-to-verify>With X4 closed, deploy the default-off configuration and start one bridge only after the package guard and focused contracts pass. In one new Creative Custom session, enable the probe for one nonempty attempt ID and reproduce only the existing selected-sector discovery cycle. Capture exactly one sanitized four-class vector and one correlated aggregate health disposition in the ledger; immediately disable the probe and stop the attempt. Do not collect a second candidate, enumerate components, inspect or modify a save, retain raw values or payloads, start a report/model/command path, or make a normal-speed/SETA claim.</how-to-verify>
<resume-signal>Return one ledger row as `observed-in-X4`, `failed`, or `pending`, containing the four result classes, aggregate disposition, attempt ID, X4 version, mod list, scenario, and stop disposition. Route only from that row: a capacity or ware `call_error`, `wrong_type`, or `invalid_value` warrants a separately planned bounded real-component-enumeration successor; otherwise retain D-09 as explicit unsupported unless a separately planned correction can explain a contradiction between all admissible classes and the aggregate result.</resume-signal>
<action>Replace the opaque aggregate-only diagnostic at this exact boundary with a default-off `facts_unsupported` capability probe. Per D-01 through D-03 and D-06 through D-09, preserve the existing single deterministic sector selection, normal telemetry cadence, aggregate health reason, one-way X4-to-Rust authority, and no-admission-on-incomplete behavior. Add a trace-config switch and nonempty bounded attempt ID; when both are enabled, allow one diagnostic emission for that attempt and no more. The emitted vector is closed to these exact four fields and result classes: `metadata_type` for the combined name/macro type-and-nonempty contract, `owner_id_validity` for the owner identifier contract, `sector_capacity` for the protected selected-sector `get_people_capacity` call, and `first_cargo_ware_limit` for the protected first-cargo `get_ware_production_limit` call. Classify only `ok`, `call_error`, `wrong_type`, `invalid_value`, or `not_applicable` where no cargo entry exists; never serialize source values, identifiers, names, macros, ware labels, numbers, native errors, Lua tables, frames, or payload content. The code must not call any new X4 API, scan a second sector, enumerate assets/components, reclassify unsupported as known-empty, or change the v2 frame schema. Bound the trace to one line/vector with four class labels and the configured attempt ID, then leave the normal aggregate unsupported result in place. Update fake-adapter tests to prove disabled silence, the exact vector shape, no secret/raw content, one-emission limit, and unchanged failed-admission behavior. Update the disposable procedure and append-only evidence ledger with the exact preflight, trace bound, four allowed classes, stop conditions, and conditional successor routing. If the probe cannot uphold any bound or privacy rule, halt it, record `failed`, disable it, and retain `facts_unsupported` without attempting a workaround.</action>
<verify><automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; powershell -NoProfile -File tests/x4-disposable/01-install-guard.ps1 -VerifyPackageOnly; cargo test -p observation-ingest --test runtime_facts_contract; cargo test -p x4-bridge --test protocol_contract; cargo test -p x4-bridge --test named_pipe_contract; cargo test -p x4-bridge --test reconnect_idempotency; cargo test --workspace</automated><human-check>One disposable attempt produces no more than one attempt-scoped vector with exactly the four specified class labels and no raw or identifying content; the ledger records its aggregate disposition and stop state; the probe is disabled immediately afterwards; no component enumeration or game-state effect occurred; and the successor route is selected only from the retained class results.</human-check></verify>
<done>The aggregated diagnostic gap is closed: one opt-in, rate-limited, privacy-safe disposable probe identifies the existing predicate result classes without changing telemetry scope or authority. A disqualifying selected-sector capacity/ware result yields a separately planned real-component-enumeration successor; all other outcomes retain D-09 as explicit unsupported or route an identified contradiction to a bounded corrective plan.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| X4 discovery adapter → Lua normalizer | Untrusted runtime values must be bounded and typed before becoming an observation. |
| Lua v2 serializer → named pipe → Rust decoder | The game-facing payload is untrusted until strict schema and semantic admission succeed. |
| v1/v2 capability handshake → running X4 | A game-side protocol change must not be mistaken for a Rust-only compatible update. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-35 | T | v2 fact envelope | high | mitigate | Strict nested decoding, typed identities, deterministic ordering, and Task 1 rejection contracts protect the accepted snapshot. |
| T-01-36 | D | X4 discovery callback | high | mitigate | Lua and Rust enforce matching count/size bounds; excess or missing data becomes an explicit disposition before admission. |
| T-01-37 | S | capability negotiation | high | mitigate | The v1 client is terminal restart-required; only an advertised v2 client may enter normal admission. |
| T-01-38 | I | runtime evidence | medium | mitigate | Ledger and diagnostics retain bounded identifiers, quality, and health metadata without raw payloads, saves, prompts, or private paths. |
| T-01-39 | E | Phase 1 protocol | high | mitigate | Task actions retain a telemetry-only discriminant set and exercise no return-side authority. |
| T-01-40 | T | Lua-provided clock values | high | mitigate | Task 1 denies client Unix-time fields, validates an optional X4 game-time value only in its own domain, and stamps the accepted record from an injected bridge receipt clock after strict admission. |
| T-01-SC | Tampering | package-manager installs | high | accept | This plan adds no npm, pip, or cargo package installation. |
</threat_model>

<verification>
Complete the red-green-refactor cycles in Tasks 1 and 2 before the game-facing replacement. The Plan 01-10 embedded-Lua syntax/version probe and provisioned local runner are hard preconditions, not substitute evidence. Task 3 is a one-attempt diagnostic only: tests must prove the default-off, four-class, one-vector, no-raw-content contract before a disposable X4 run can classify the retained predicate results. It does not prove component enumeration or close D-09 by itself.
</verification>

<success_criteria>
CR-02 remains unresolved until a bounded v2 observation retains discovered sector, asset, capacity, and ownership facts across Lua and Rust without host-clock access. This correction closes only the diagnostic aggregation gap when it produces one safe four-class result vector, preserves unsupported data as unsupported, and routes any selected-sector API category mismatch to a separately planned bounded component-enumeration decision without expanding Phase 1 authority.
</success_criteria>

## Source coverage audit

| Source | ID | Item | Plan | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 1 | Trustworthy bounded versioned X4 observations without mutation or stalls | 01-11 | COVERED |
| REQ | OBS-01, OBS-02, OBS-03 | Typed bounded versioned admission with explicit quality | 01-11 | COVERED |
| REQ | OBS-06 | Runtime sectors, assets, capacity, and ownership originate from observed state | 01-11 | COVERED |
| REQ | OBS-07, OBS-08, VAL-06 | Fail-closed snapshot safety, telemetry-only authority, and earned X4 evidence | 01-11 | COVERED |
| RESEARCH | 01-RUNTIME-GAP-RESEARCH | Preserve explicit unknowns and isolate actual X4 proof from installed-source precedent | 01-11 | COVERED |
| RESEARCH | phase1-plan11-clock-unavailable | Bridge receipt-time authority; no epoch conversion of X4-native clocks; isolated retry evidence | 01-11 | COVERED |
| RESEARCH | phase1-plan11-facts-unsupported | Aggregated predicate diagnostic gap; observed sector/component call-shape mismatch candidate; one safe capability vector before any enumeration decision | 01-11 | COVERED |
| CONTEXT | D-01 to D-03 | X4 authority and one-way telemetry-only boundary | 01-11 | COVERED |
| CONTEXT | D-04 to D-06 | Compatible bridge reconnect and game-facing restart-required gate | 01-11 | COVERED |
| CONTEXT | D-07 to D-09 | Typed facts, explicit state, and runtime discovery | 01-11 | COVERED |

<output>
Create `.planning/phases/01-read-only-observation-spine/01-11-SUMMARY.md` when done. Include `Skill learning: none` only if the executor's reviewed evidence shows no reusable project-rule gap; otherwise record one candidate under the single owning project skill.
</output>
