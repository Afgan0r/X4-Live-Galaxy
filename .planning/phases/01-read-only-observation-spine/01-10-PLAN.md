---
phase: 01-read-only-observation-spine
plan: 10
type: tdd
status: superseded
superseded_by: 05.1-bounded-real-component-discovery-inserted
wave: 7
depends_on: [01-08]
files_modified: [extensions/live_galaxy/lua/live_galaxy_trace_config.lua, extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/lua/live_galaxy_telemetry.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, extensions/live_galaxy/tests/run_contracts.ps1, tools/provision-lua.ps1, tools/lua-runner.lock.json, tests/x4-disposable/01-install-guard.ps1, tests/x4-disposable/01-probe-procedure.md, tests/x4-disposable/01-probe-evidence.md, .planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md]
autonomous: false
gap_closure: true
requirements: [OBS-06, OBS-07, OBS-08, VAL-06]
user_setup:
  - service: X4 Foundations 9.00
    why: "Installed-X4 API evidence and one fresh isolated Creative Custom OBS-X4-04 attempt are required to close D-09."
estimate: {tokens: 50000, raw_tokens: 50000, tasks: 4, confidence: high}
must_haves:
  truths:
    - "A separately opt-in, bounded development trace records only the embedded runtime's sanitized `_VERSION` value in one disposable loader probe, without a UI, telemetry frame, bridge, effect, or game-state path (D-01, D-03, D-09)."
    - "A bounded read-only X4 adapter supplies real runtime sector, asset, capacity, and ownership information to the existing telemetry producer rather than a fixed probe payload (D-01, D-07, D-09)."
    - "Unavailable, incomplete, or invalid discovery stays explicit and does not become a known-empty section or a fabricated observation (D-06, D-08, D-09)."
    - "The existing telemetry framing, admission boundary, and compatible bridge-restart behavior remain intact; changing the game-facing Lua extension requires X4 to be closed and restarted (D-02, D-04, D-05)."
    - "A fresh isolated OBS-X4-04 ledger attempt earns only the evidence classification supported by the actual Creative Custom run (D-01, D-03, D-09)."
  artifacts:
    - path: extensions/live_galaxy/lua/live_galaxy_trace_config.lua
      provides: "Default-off, single-event configuration for the embedded `_VERSION` diagnostic."
    - path: extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
      provides: "Installed-X4-evidenced, bounded, read-only discovery adapter."
    - path: extensions/live_galaxy/tests/x4_discovery_contract.lua
      provides: "Pure/fake-adapter discovery and producer contract coverage."
    - path: tools/provision-lua.ps1
      provides: "Project-local reproducible Lua runner provisioner selected only after the disposable X4 syntax/version probe."
    - path: tools/lua-runner.lock.json
      provides: "Selected Lua compatibility, source/binary provenance, platform, exact archive checksum, and executable-version lock."
    - path: tests/x4-disposable/01-probe-evidence.md
      provides: "Fresh OBS-X4-04 runtime evidence or a truthful failed/pending result."
  key_links:
    - from: extensions/live_galaxy/lua/live_galaxy_trace_config.lua
      to: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      via: "One explicit developer-only flag permits a single sanitized `_VERSION` trace during Lua initialization; the default path emits no version diagnostic."
    - from: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      to: extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua
      via: "Registered live_galaxy_observation callback creates the read-only adapter before requesting one bounded observation."
    - from: extensions/live_galaxy/lua/live_galaxy_telemetry.lua
      to: crates/x4-bridge/src/server.rs
      via: "Existing bounded observation envelope travels through the unchanged named-pipe transport and existing admission."
    - from: tools/lua-runner.lock.json
      to: extensions/live_galaxy/tests/run_contracts.ps1
      via: "The runner accepts only an override, local executable, or PATH fallback that matches the probe-selected lock."
  prohibitions:
    - requirement_id: OBS-08
      category: product
      status: resolved
      verification: judgment
      statement: "The closure emits only existing telemetry discriminants and does not widen Phase 1 authority beyond read-only observation."
---

<objective>
Close D-09 by recording the embedded Lua runtime's self-reported `_VERSION` through a bounded opt-in development trace before provisioning a checksum-verified project-local pure-Lua runner, replacing the runtime producer's fixed probe observation with a bounded original adapter backed by installed X4 9.00 evidence, then earning one isolated scope-complete runtime result.

Purpose: make the already-proven transport carry observed X4 discovery without changing its protocol, recovery behavior, or Phase 1 authority.
Output: read-only `_VERSION` diagnostic record, reproducible local Lua runner lock/provisioner, discovery adapter, executable pure/fake contracts, static package guard coverage, and a classified OBS-X4-04 ledger entry.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-read-only-observation-spine/01-CONTEXT.md
@.planning/phases/01-read-only-observation-spine/01-08-SUMMARY.md
@.planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md
@.planning/debug/phase-1-d09-discovery-gap.md
@tests/x4-disposable/01-probe-procedure.md
@tests/x4-disposable/01-probe-evidence.md
@extensions/live_galaxy/lua/live_galaxy_trace_config.lua
@extensions/live_galaxy/lua/live_galaxy_runtime.lua
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/lua/SKILL.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
<name>Task 1: Record the embedded `_VERSION` through one bounded opt-in loader diagnostic</name>
<files>extensions/live_galaxy/lua/live_galaxy_trace_config.lua, extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, tests/x4-disposable/01-install-guard.ps1, tests/x4-disposable/01-probe-procedure.md, tests/x4-disposable/01-probe-evidence.md, .planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md</files>
<precondition>The user can operate a new disposable Creative Custom X4 9.00 campaign and can close X4 before any Live Galaxy extension replacement; no save files may be accessed.</precondition>
<read_first>extensions/live_galaxy/lua/live_galaxy_trace_config.lua; extensions/live_galaxy/lua/live_galaxy_runtime.lua; extensions/live_galaxy/tests/x4_discovery_contract.lua; tests/x4-disposable/01-install-guard.ps1; tests/x4-disposable/01-probe-procedure.md; tests/x4-disposable/01-probe-evidence.md; .planning/phases/01-read-only-observation-spine/01-VALIDATION.md; .planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md; .agents/skills/live-galaxy-x4-integration/SKILL.md; .agents/skills/live-galaxy-x4-tests/SKILL.md; .agents/skills/lua/SKILL.md</read_first>
<what-built>The pre-run compatibility gate for all standalone Lua contracts, using the embedded runtime's own `_VERSION` rather than loader acceptance or host tooling.</what-built>
<how-to-verify>With X4 closed before the guarded extension deployment, use the written disposable Creative Custom procedure to enable the one-shot diagnostic for one named attempt. Do not start a bridge or trigger a telemetry event. Record exact X4 version, active extensions, scenario, elapsed real and game time, SETA state, and the one bounded trace line containing the sanitized embedded `_VERSION` value. Do not inspect saves, emit telemetry frames, add a player-facing or general debug interface, or use the result as discovery or transport evidence.</how-to-verify>
<resume-signal>Return the evidence row with the sanitized `_VERSION` result, or a failed/pending classification with the missing diagnostic; runner work remains blocked if the value is absent, invalid, or cannot identify a compatible standalone target.</resume-signal>
<action>Extend the existing development trace configuration with a default-off flag that enables this diagnostic only for the named disposable attempt. During Lua initialization, and only when that flag is true, read `_VERSION` once and send one trace event through the existing `DebugError` path; do not add a new logger, UI registration, Mission Director event, pipe frame, bridge start, persistent state, command, report, acknowledgement, or game-state effect. The trace detail must contain only the runtime's `_VERSION` value after converting non-string or empty values to an explicit unavailable disposition, replacing every non-ASCII-safe character with an underscore, and truncating the final value to 64 bytes; emit no raw configuration, payload, save, prompt, path, or error object. Enforce a fixed one-event cap independent of frame tracing, preserve default-off behavior, and reset no scheduler or transport state. Add a focused contract/static guard that proves the default disables the diagnostic, an enabled valid fixture yields exactly one sanitized bounded value, and disabled or unavailable paths emit no version value. Update the procedure, evidence ledger, and runtime-gap research with the exact isolated scope and the loader-only result classification. Keep D-01, D-03, and D-09's closed-X4 deployment and disposable Creative Custom boundaries intact. If the probe does not record a usable `_VERSION` value, record `not exposed` or `invalid` and halt this plan before adding a runner lock, downloading source or a binary, selecting a Windows build route, or changing the contract runner.</action>
<verify><automated>powershell -NoProfile -File tests/x4-disposable/01-install-guard.ps1 -VerifyPackageOnly</automated><human-check>The evidence row has the exact X4/mod/scenario/timing context and one sanitized, at-most-64-byte `_VERSION` value or an explicit unavailable disposition; the attempt is loader-only, read-only, save-free, bridge-free, and contains no telemetry frame.</human-check></verify>
<acceptance_criteria>The package guard protects the default-off, one-event diagnostic boundary; the ledger distinguishes the embedded `_VERSION` result from loader acceptance and host tooling; and runner selection remains blocked unless that value establishes a compatible target.</acceptance_criteria>
<done>One disposable loader-only probe records the embedded runtime's bounded sanitized `_VERSION` value or an explicit unavailable disposition; it never overclaims X4 API, discovery, or transport semantics, and later runner work remains halted until a compatible target is established.</done>
</task>

<task type="tracer" tdd="true">
<name>Task 2: Provision the probe-selected Lua runner and prove one fake-adapter path</name>
<files>.planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md, extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, extensions/live_galaxy/tests/run_contracts.ps1, tools/provision-lua.ps1, tools/lua-runner.lock.json</files>
<precondition>Task 1 recorded a usable embedded-X4 `_VERSION` result that identifies a compatible standalone target; the installed X4 9.00 files remain available only as a read-only research source; and explicit observed source call sites are sufficient to construct one minimal read-only discovery call. If either condition is not met, halt before selecting a standalone Lua version, materialization route, or production adapter.</precondition>
<read_first>.planning/debug/phase1-lua-runner-gap.md; .planning/debug/phase-1-d09-discovery-gap.md; .planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md; tests/x4-disposable/01-probe-evidence.md; extensions/live_galaxy/lua/live_galaxy_telemetry.lua; extensions/live_galaxy/lua/live_galaxy_normalize.lua; extensions/live_galaxy/tests/telemetry_contract.lua; extensions/live_galaxy/tests/run_contracts.ps1; installed X4 9.00 support-library documentation and explicit `x4_live_mcp` call sites for `GetClusters`, `GetSectors`, `GetComponentData`, `GetWareProductionLimit`, and `C.GetPeopleCapacity`; .agents/skills/live-galaxy-x4-integration/SKILL.md; .agents/skills/live-galaxy-x4-tests/SKILL.md; .agents/skills/lua/SKILL.md</read_first>
<behavior>
- RED: a fake adapter with deterministic sector, asset, capacity, and ownership records produces one sorted, normalized, source=`x4_runtime` bounded observation.
- RED: unavailable API, unsupported class, invalid raw value, or incomplete scope returns an explicit quality/disposition and cannot yield known-empty or a fixed fallback identity.
- RED: a requested limit above the Phase 1 ceiling performs at most one read-only section operation.
</behavior>
<action>First inspect only installed X4 9.00 files. In 01-RUNTIME-GAP-RESEARCH.md, record the explicit observed `x4_live_mcp` call shapes for `GetClusters`, `GetSectors`, `GetComponentData`, `GetWareProductionLimit`, and `C.GetPeopleCapacity`; record their provenance and license-review status, and classify every remaining semantic, identity, bound, and failure-mode gap as unknown. Observed call shape is sufficient to construct one bounded original read-only adapter, but is not a documented contract or compatibility claim; do not copy third-party code or invent API semantics. Before creating or selecting a standalone runner, consume Task 1's observed compatibility target. Then create a project-owned reproducible provisioner and lock record for that target: the lock records exact compatibility version, source or binary URL, archive SHA-256, platform, executable version, and source/binary provenance; the provisioner verifies every archive checksum and executable version, uses one documented Windows materialization route justified by the now-observed target, and keeps downloaded or built tools only in a project-local ignored cache. Do not choose a version, download source or a binary, or assume a Windows C toolchain until Task 1 establishes compatibility. Update `run_contracts.ps1` to resolve the interpreter in this exact order: `-LuaPath`, `LIVE_GALAXY_LUA`, the lock-selected project-local executable, then a verified PATH fallback. Every resolution path must satisfy the lock's executable-version check; no machine-global executable may silently control test semantics. Add a local provisioner verification and execute the fake-adapter contracts through the resolved interpreter. If no safe minimal discovery call can be constructed from observed call sites, halt before production Lua. Otherwise create `live_galaxy_x4_discovery.lua` as a thin adapter conforming to the existing `list_scope` / `read_observation` seam. Keep X4 globals and API calls in that adapter, map only evidenced values to typed stable identities, and represent unavailable, incomplete, or unsupported source material through the existing explicit quality vocabulary. Add fake-adapter cases before implementation, then make them pass. The adapter may read and normalize runtime state but must not retain it, access saves, introduce commands, mutations, persistence, reports, protocol changes, or modify installed files; do not perform installed-file work while X4 is active. Implement D-01, D-07, D-08, and D-09 exactly; Task 4 alone may establish real discovery behavior.</action>
<verify><automated>powershell -NoProfile -File tools/provision-lua.ps1 -Verify; powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery</automated></verify>
<acceptance_criteria>Research distinguishes explicit observed call shapes from documented guarantees, records provenance/license status and unknowns; the runner lock and provisioner make the selected compatibility target reproducible; and pure contracts prove bounded fake enumeration, deterministic ordering, normalization failure, and explicit incomplete/unsupported handling.</acceptance_criteria>
<done>One fake-adapter path carries a discovered section through the existing telemetry seam using a checksum- and version-verified local runner, while unsupported or unverified installed-X4 facts remain explicit and no real discovery compatibility result is claimed before Task 4.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Wire the registered producer to discovery without changing transport authority</name>
<files>extensions/live_galaxy/lua/live_galaxy_runtime.lua, extensions/live_galaxy/lua/live_galaxy_telemetry.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, extensions/live_galaxy/tests/run_contracts.ps1, tests/x4-disposable/01-install-guard.ps1</files>
<precondition>Task 2 recorded exact installed-X4 call evidence, the X4-probe-selected local runner passed its provisioner verification, and its pure/fake-adapter discovery contracts pass.</precondition>
<read_first>extensions/live_galaxy/lua/live_galaxy_runtime.lua; extensions/live_galaxy/lua/live_galaxy_x4_discovery.lua; extensions/live_galaxy/lua/live_galaxy_telemetry.lua; crates/x4-bridge/tests/named_pipe_contract.rs; tests/x4-disposable/01-install-guard.ps1</read_first>
<behavior>
- The callback's observation step asks the discovery adapter for one bounded observation and emits it using the existing observation frame contract.
- Adapter failure produces bounded runtime health/diagnostic information and suppresses the observation frame for that cycle.
- Existing hello, heartbeat, complete-marker sequence, frame limits, and compatible bridge reconnect behavior keep their prior contracts.
</behavior>
<action>Replace only the runtime observation-production seam so the registered event handler calls the new adapter through existing telemetry normalization before `runtime.emit`; remove the fixed fixture payload from production flow. Preserve the established pipe identifier, frame bounds, named-pipe direction, envelope discriminants, bridge admission, and compatible restart behavior per D-02, D-04, and D-06. Extend the pure contracts to prove the event-side producer does not invoke the pipe for discovery failure and cannot substitute a literal probe identity. Extend the package guard to assert registered runtime-to-discovery-to-telemetry module linkage and reject a package containing the retired fixed observation path. Do not modify Rust bridge protocol types, Mission Director cadence, extension dependencies, game installation, or any return-side vocabulary; enforce D-03 and D-05.</action>
<verify><automated>powershell -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery; powershell -NoProfile -File tests/x4-disposable/01-install-guard.ps1 -VerifyPackageOnly; cargo test -p x4-bridge --test named_pipe_contract; cargo test -p x4-bridge --test reconnect_idempotency; cargo fmt --check; cargo clippy --workspace --all-targets -- -D warnings; cargo test --workspace</automated></verify>
<acceptance_criteria>The package guard and contracts establish the sole production path as event → discovery adapter → telemetry normalization → existing pipe transport; local proof makes no claim about native X4 execution.</acceptance_criteria>
<done>Local contracts and package checks are green, the bridge protocol is unchanged, and the replacement extension is ready for a fresh OBS-X4-04 attempt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
<name>Task 4: Install with X4 closed and run one fresh isolated OBS-X4-04 attempt</name>
<files>tests/x4-disposable/01-probe-procedure.md, tests/x4-disposable/01-probe-evidence.md</files>
<precondition>Task 3 automated verification is green, exactly one compatible bridge is ready, and the user can operate a disposable Creative Custom campaign.</precondition>
<read_first>tests/x4-disposable/01-probe-procedure.md; tests/x4-disposable/01-probe-evidence.md; .planning/phases/01-read-only-observation-spine/01-RUNTIME-GAP-RESEARCH.md; .agents/skills/live-galaxy-x4-tests/SKILL.md</read_first>
<what-built>A locally verified original read-only discovery adapter wired into the existing one-way observation producer.</what-built>
<how-to-verify>Close X4 before replacing the Lua extension, run the verification guard, then start one compatible bridge and a new disposable Creative Custom X4 9.00 scenario. Run only OBS-X4-04. Record exact X4 version, active mod list, extension/protocol identity, scenario, real and game elapsed time, SETA state, expected and observed sectors, assets, capacity, ownership, scope completion, quality, and bounded health. Correlate bridge and Lua diagnostics. If the extension is replaced again, close and restart X4 before retrying. Do not access a save file.</how-to-verify>
<resume-signal>Return the ledger row and state whether OBS-X4-04 earned observed-in-X4, failed, or remains pending, with the missing class or diagnostic named.</resume-signal>
<action>Update the procedure only to make the fresh D-09 scope evidence and X4-close/restart boundary unambiguous. The Task 1 syntax/version probe is a separate prerequisite and does not substitute for this real discovery attempt. Perform a single isolated Creative Custom attempt after the game-facing replacement, never combining it with transport, reconnect, cadence, or other probe claims. Record a full evidence row without promoting local, fake-adapter, runner, or syntax-probe proof. If discovery cannot supply every required class, record the actual explicit quality and preserve the evidence as failed or pending; do not adapt the runtime to make the row look complete. Maintain Phase 1's read-only authority per D-01, D-03, D-05, D-08, and D-09.</action>
<verify><automated>powershell -NoProfile -File tests/x4-disposable/01-install-guard.ps1 -VerifyPackageOnly; cargo test --workspace</automated><human-check>A fresh OBS-X4-04 attempt correlates real discovery-derived identifiers, completion state, explicit quality, and bounded health in the same disposable session; X4 was closed before extension replacement and no save or effect-bearing capability was used.</human-check></verify>
<acceptance_criteria>The ledger keeps an earned classification, exact setup/readback/health fields, and restart evidence; it never reclassifies local contracts as in-game observation.</acceptance_criteria>
<done>D-09 closes only if OBS-X4-04 shows scope-complete discovery at the level the procedure requires; otherwise the gap remains accurately recorded.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| installed X4 → discovery adapter | Read-only implementation evidence and runtime values cross into project-owned normalization. |
| discovery adapter → telemetry producer | Incomplete or malformed source values must not become authoritative observations. |
| Lua producer → named pipe → bridge | Existing bounded frames remain untrusted until existing Rust admission. |
| package replacement → running X4 | Game-facing Lua replacement must not race an active X4 process. |
| trace configuration → X4 debug log | An opt-in development diagnostic may disclose only one sanitized embedded-runtime version value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-29 | T | discovery normalization | high | mitigate | Typed normalization, explicit quality, and fake-adapter failure contracts prevent a fabricated or known-empty result. |
| T-01-30 | I | runtime metadata | medium | mitigate | Bounded diagnostics retain only correlation and health metadata; raw runtime payloads stay out of evidence. |
| T-01-31 | D | X4 discovery callback | high | mitigate | One-section ceiling, installed-evidence work bound, and health disposition stop unbounded game-thread work. |
| T-01-32 | E | Phase 1 authority | high | mitigate | Adapter exposes observation-only methods and reuses closed telemetry discriminants under D-01 through D-03. |
| T-01-33 | T | extension deployment | high | mitigate | Closed-X4 precondition and install guard prevent an active-game file replacement. |
| T-01-34 | T | local Lua runner provenance | high | mitigate | Task 1 supplies the observed compatibility target; Task 2 locks archive checksum, source/binary provenance, platform, and executable version before the provisioner or runner accepts it. |
| T-01-40 | I | embedded version diagnostic | medium | mitigate | Task 1 uses a default-off flag, existing development trace path, fixed one-event cap, ASCII-safe sanitization, 64-byte truncation, and a loader-only Creative Custom procedure. |
| T-01-SC | Tampering | package-manager installs | high | accept | This plan adds no npm, pip, or cargo package installation; package-legitimacy gate is not invoked. |
</threat_model>

<verification>
Task 1 supplies the read-only embedded `_VERSION` record before runner selection and cannot establish discovery or transport semantics. Run the Task 2 provisioner verification and pure/fake Lua suite before wiring. Run the Task 3 package guard, focused bridge contracts, formatting, linting, and workspace suite before any game-facing replacement. Task 4 is the only runtime discovery proof and must be a fresh isolated OBS-X4-04 Creative Custom attempt after X4 is closed and restarted.
</verification>

<success_criteria>
The accepted runtime observation has read-only discovery provenance for sectors, assets, capacity, and ownership; incomplete facts remain explicit; transport and bridge recovery contracts do not change; and the evidence ledger records only what the fresh X4 run earns.
</success_criteria>

<output>
Create `.planning/phases/01-read-only-observation-spine/01-10-SUMMARY.md` when done.
</output>

## Source coverage audit

| Source | ID | Item | Plan | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 1 | Bounded, versioned, read-only X4 observation spine | 01-10 | COVERED |
| REQ | OBS-06 | Runtime sectors, assets, capacity, and ownership derive from observed state | 01-10 | COVERED |
| REQ | OBS-07, OBS-08, VAL-06 | Bounded diagnostics, closed authority, and earned X4 evidence | 01-10 | COVERED |
| RESEARCH | Runtime gap research | Installed-X4 evidence before implementation; preserve proven transport and restart boundary | 01-10 | COVERED |
| CONTEXT | D-01 to D-03 | Read-only X4 authority and telemetry-only protocol boundary | 01-10 | COVERED |
| CONTEXT | D-04 to D-06 | Compatible bridge restart, closed-X4 game-facing replacement, and existing admission safety | 01-10 | COVERED |
| CONTEXT | D-07 to D-09 | Typed observations, explicit quality, and runtime discovery | 01-10 | COVERED |
