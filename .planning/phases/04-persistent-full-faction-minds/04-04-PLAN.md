---
phase: 04-persistent-full-faction-minds
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - crates/mind-persistence/src/recovery.rs
  - crates/mind-persistence/src/capsule.rs
  - crates/mind-persistence/src/lib.rs
  - crates/mind-persistence/tests/recovery_contract.rs
  - crates/mind-persistence/tests/capsule_contract.rs
  - crates/mind-persistence/tests/mutation_baseline.rs
autonomous: true
requirements: [MODEL-05, STATE-04, STATE-05]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Recovery replays only a complete acknowledged checkpoint and retains the last valid cursor after corrupt, partial, duplicate, stale, or out-of-order input."
    - "Ordered schema migrations validate a copied envelope before exposing its target projection, and incompatible state fails closed with structured evidence."
    - "Versioned capsules use provider/model-relative budget-profile inputs, preserve typed commitments and source range/hash, and treat narrative as non-authoritative."
  artifacts:
    - "recovery and capsule modules keep failure policy and compaction separate from the port and codec."
    - "Recovery, capsule, and mutation-baseline tests cover adversarial fixtures and measure pure-policy mutants."
  key_links:
    - "acknowledged CheckpointEnvelope -> ordered migration/validation -> recovered aggregate projection and bounded diagnostic"
    - "canonical ledger range/hash + model budget profile -> typed-plus-narrative capsule without authority inversion"
---

<objective>
Finish deterministic recovery, schema migration, and model-relative compaction above the acknowledged checkpoint contract.

Purpose: preserve campaign continuity and authoritative commitments through corruption and version changes without pretending that local tests prove X4 runtime persistence.
Output: fail-closed recovery/migration policy, typed-plus-narrative capsules, adversarial fixtures, and scoped mutation baseline.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-persistent-full-faction-minds/04-CONTEXT.md
@.planning/phases/04-persistent-full-faction-minds/04-RESEARCH.md
@.planning/phases/04-persistent-full-faction-minds/04-03-SUMMARY.md
@crates/mind-persistence/src/checkpoint.rs
@crates/mind-persistence/src/port.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Recover only the last valid acknowledged checkpoint through schema transitions</name>
  <files>crates/mind-persistence/src/recovery.rs, crates/mind-persistence/src/lib.rs, crates/mind-persistence/tests/recovery_contract.rs</files>
  <behavior>
    - RED: recovery accepts a valid acknowledged envelope and rebuilds the same aggregate/replay/strategic-tick/report-reservation identity without a second port write.
    - RED: injected failure before X4 write, after the write but before acknowledgement, and after acknowledgement but before local projection each recovers only the last acknowledged checkpoint and never exposes a speculative aggregate, tick, initiative, or report reservation.
    - RED: corrupt, partial, invalid-integrity, duplicate-content collision, stale predecessor, and out-of-order fixtures retain the supplied last valid acknowledged checkpoint and return bounded typed diagnostic evidence.
    - RED: an ordered schema migration operates on a copy, commits a fully validated target envelope only, and refuses unsupported source/target versions without a partial target projection.
    - RED: recovery never accesses player saves, a local database, or a live X4 process.
  </behavior>
  <action>Start from failing adversarial recovery fixtures. Per D-08 and D-09 plus STATE-04/05, separate pure recovery policy from codec/port code and make it accept only complete acknowledged checkpoint records. Add deterministic injected crash-point fixtures for before X4 write, after X4 write before acknowledgement, and after acknowledgement before local projection. Each fixture must assert that restart loads the last acknowledged checkpoint, does not expose the speculative aggregate, strategic tick, initiative, or report reservation, and never issues a duplicate port write. Define ordered migration identities and migration functions over copied envelopes; validate schema, integrity, cursor chain, typed aggregate constraints, and report reservation before returning a recovered projection. On malformed, partial, incompatible, duplicate collision, stale, or out-of-order input, return structured bounded failure evidence and retain the last valid acknowledged cursor without a port write. Keep transition/recovery ordering deterministic and source files within the strict 200-line limit; never use local projections to repair, outrank, or replace the X4 authority lane.</action>
  <verify><automated>cargo test -p mind-persistence --test recovery_contract &amp;&amp; cargo run -p source-size-lint &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated></verify>
  <done>Adversarial and three crash-point fixtures recover deterministically, non-destructively, and fail-closed from the last acknowledged checkpoint; no speculative aggregate, strategic tick, initiative, or report reservation becomes visible, and supported version transitions create only fully validated target envelopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compact history with typed authority and measure recovery-policy mutants</name>
  <files>crates/mind-persistence/src/capsule.rs, crates/mind-persistence/src/recovery.rs, crates/mind-persistence/tests/capsule_contract.rs, crates/mind-persistence/tests/mutation_baseline.rs</files>
  <precondition>`cargo-mutants` is installed at the reviewed version already selected by the project Rust-test policy; otherwise record its observed absence or failure in the plan summary without installing a tool.</precondition>
  <behavior>
    - A capsule is accepted only when its source ledger range/hash, schema/codec version, provider/model budget profile identity, typed commitment projection, and bounded narrative are self-consistent.
    - Changing provider/model budget inputs changes capsule eligibility or identity; no fixed event-count or elapsed-time threshold determines compaction.
    - Typed commitments reconstruct independently of narrative, and a corrupted narrative cannot change recovered goals, plans, postures, or initiative ownership.
    - Mutation baseline tests kill representative errors in cursor comparison, integrity checks, migration ordering, and typed-authority enforcement; every survivor receives a disposition.
  </behavior>
  <action>Write capsule and mutation-baseline tests before implementation. Per D-03 through D-05 and MODEL-05, define a versioned capsule carrying exact source ledger range/hash, codec/schema version, provider/model budget-profile identity and measured headroom inputs, typed commitment projection, and bounded optional narrative. Per D-04, preserve typed facts and commitments as the authority and make the narrative a non-authoritative projection only. Make compaction eligibility depend on the recorded budget policy rather than event counts or elapsed time, and ensure decoding/recovery takes commitments solely from typed data. Then run `cargo mutants -p mind-persistence` against the pure codec, recovery, migration, and capsule policies after normal tests pass. Record runner version, command, duration, killed/surviving/invalid/equivalent counts, and every survivor disposition in the execution summary; do not set an invented score threshold or include MD/XML or fake-port adapters in mutation scoring.</action>
  <verify><automated>cargo test -p mind-persistence --test capsule_contract &amp;&amp; cargo test -p mind-persistence --test mutation_baseline &amp;&amp; cargo mutants -p mind-persistence -- --test mutation_baseline</automated></verify>
  <done>Per D-04, compaction preserves authoritative typed commitments while treating narrative as non-authoritative through provider-relative versioned capsules, and a measured pure-policy mutation baseline with survivor disposition exists or its exact tool failure is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted envelope to recovery projection | Corrupt or old state could be mistaken for accepted campaign truth. |
| typed ledger to narrative capsule | Narrative could attempt to overwrite commitments during compaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-11 | Tampering | recovery and migration | high | mitigate | Validate copied envelopes in deterministic migration order and retain the last acknowledged valid state on all rejected fixtures. |
| T-04-12 | Information disclosure | capsule narrative | medium | mitigate | Bound narrative as non-authoritative projection and exclude raw prompts, private paths, and hidden reasoning from capsule contract tests. |
| T-04-13 | Denial of service | compaction/recovery payload | medium | mitigate | Enforce bounded payload, range, and profile inputs before capsule or recovery projection allocation. |
| T-04-14 | Repudiation | mutation evidence | medium | mitigate | Record command, runner version, result categories, and individual survivor dispositions. |
| T-04-SC | Tampering | cargo-mutants | high | mitigate | Require existing reviewed availability; do not install or silently replace the mutation runner. |
</threat_model>

<verification>
Run focused recovery and capsule contracts, source-size lint, strict workspace Clippy, and `cargo test --workspace`. Report those as verified locally; retain all MD payload, interruption, save/load, and reconnect observations as pending-X4 until Phase 7.
</verification>

<success_criteria>
MODEL-05 and STATE-04/05 are complete locally through fail-closed recovery/migration and typed-authoritative compaction, with measured mutation evidence and no unproven X4 runtime claim.
</success_criteria>

<output>
Create `.planning/phases/04-persistent-full-faction-minds/04-04-SUMMARY.md` when done.
</output>
