---
phase: 04-persistent-full-faction-minds
plan: 01
type: execute
wave: 1
depends_on: [03-03]
files_modified:
  - Cargo.toml
  - crates/mind-domain/Cargo.toml
  - crates/mind-domain/src/lib.rs
  - crates/mind-domain/src/mind.rs
  - crates/mind-domain/src/initiative.rs
  - crates/mind-domain/src/causal.rs
  - crates/mind-domain/tests/mind_tracer.rs
  - crates/mind-domain/tests/initiative_lifecycle.rs
autonomous: true
requirements: [MIND-01, MIND-05, INST-03, INST-08]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ZYA and ARG retain independent typed doctrine, motives, priorities, goals, short- and long-term plans, and Executive-owned Shadow diplomatic postures."
    - "Each faction has exactly three institution slots, and each of the six paired-faction aggregate instances permits at most one active initiative while a replacement retains its predecessor and disposition as immutable causal evidence."
    - "The same frozen packet and versioned doctrine profile yield canonically ordered replayable mind state while the shared-scenario fixtures prove meaningful faction divergence."
  artifacts:
    - "crates/mind-domain is a pure typed aggregate and causal-transition crate."
    - "mind_tracer and initiative_lifecycle provide behavioral coverage for independence, one-active-slot, preemption, replay ordering, and no-X4-mutation constraints."
  key_links:
    - "strategic_state::StrategicPacket -> mind_domain::MindCommand -> PendingMindCommit containing canonical causal events and an updated aggregate"
---

<objective>
Build the pure, typed Faction Mind aggregate and causal initiative lifecycle that later persistence commits as one checkpoint.

Purpose: establish durable independent minds and one-owner initiative history before an adapter can persist or recover them.
Output: `mind-domain`, bounded aggregate transitions, immutable causal events, and red-green behavioral contracts.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-persistent-full-faction-minds/04-CONTEXT.md
@.planning/phases/04-persistent-full-faction-minds/04-RESEARCH.md
@.planning/phases/03-faction-scoped-strategic-state/03-01-SUMMARY.md
@crates/strategic-state/src/lib.rs
@crates/strategic-state/src/packet.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Transition independent ZYA and ARG mind aggregates through one replayable path</name>
  <files>Cargo.toml, crates/mind-domain/Cargo.toml, crates/mind-domain/src/lib.rs, crates/mind-domain/src/mind.rs, crates/mind-domain/src/causal.rs, crates/mind-domain/tests/mind_tracer.rs</files>
  <behavior>
    - RED: one frozen paired strategic-packet fixture produces separate ZYA and ARG aggregates containing doctrine version, motives, priorities, goals, short-term plans, long-term plans, and Executive-owned bilateral Shadow posture.
    - RED: the common fixture yields different typed ZYA and ARG priorities, goals, or posture inputs that cannot be explained solely by the faction tag.
    - RED: replaying identical faction packet, profile version, command ID, and prior aggregate returns byte-identical canonical pending-commit events and aggregate projection.
    - RED: model calls, report dispatch, transport, and X4 game mutation are absent from the public transition path.
  </behavior>
  <action>Start with a failing integration test, then add the workspace member and a pure `mind-domain` crate. Define compact domain types for faction-keyed identity, versioned doctrine and motives, ordered priorities/goals/plans, typed bilateral Executive posture, command identity, and canonical causal event metadata. Per D-01, keep the complete independent mind state in the aggregate; per MIND-01 and MIND-05, construct a shared-scenario pair whose divergent result is caused by recorded doctrine/profile values. Accept only `strategic_state::StrategicPacket` inputs and return a typed `PendingMindCommit`; use total sort keys, bounded collections, checked construction, typed errors, and no wall clock, random IDs, I/O, provider boundary, or X4 command. Keep each Rust source at most 200 physical lines and make unsupported packet/profile combinations fail closed.</action>
  <verify><automated>cargo test -p mind-domain --test mind_tracer &amp;&amp; cargo run -p source-size-lint &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated></verify>
  <done>One complete ZYA and ARG transition is reproducible from frozen Phase 3 packets, contains all D-01 mind fields, proves doctrine-driven divergence, and emits no provider, report, transport, persistence, or X4 mutation side effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve one-owner initiative and preemption causality in the aggregate</name>
  <files>crates/mind-domain/src/initiative.rs, crates/mind-domain/src/causal.rs, crates/mind-domain/src/mind.rs, crates/mind-domain/tests/initiative_lifecycle.rs</files>
  <behavior>
    - An active initiative records stable identity, owner, objective, evidence, priority, lifecycle state, and the validating causal event.
    - A second active initiative for the same faction and capability is refused unless a command supplies the predecessor, trigger, and explicit preemption disposition.
    - Proposal, objection, Executive disposition, validation, ownership assignment, and preemption each append a typed causal event that links to the affected initiative and command.
    - A successful preemption preserves the former initiative plus its disposition and links the replacement event to it; repeated matching command IDs return the original result and same-ID/different-content is rejected.
    - Completion, cancellation, rejection, and failure each leave replayable terminal causal records without X4 state mutation.
  </behavior>
  <action>Write lifecycle contract tests before transition code. Per D-02 and INST-03/08, give each of the exactly three `(Faction, Capability)` slots per faction an explicit optional active identity and a typed initiative state machine; across the paired factions this yields six aggregate instances, never six slots in one faction. Make proposal, objection, Executive disposition, validation, ownership assignment, preemption, and each terminal outcome append immutable typed causal events. Make replacement an atomic aggregate transition containing predecessor identity, trigger, requested disposition, Executive decision metadata, evidence references, and its complete causal event set. Per D-03, retain typed ledger records as authoritative and allow no conversation prose to decide state. Implement stable transition/content hashes and idempotency behavior for exact repeats; refuse a stale predecessor, ambiguous ordering, duplicate slot, illegal terminal transition, or content collision with structured typed errors. Do not implement dialogue cycles, model proposals, report transmission, or game-state mutation; those boundaries remain owned by Phases 5 and 6.</action>
  <verify><automated>cargo test -p mind-domain --test initiative_lifecycle &amp;&amp; cargo test -p mind-domain --test mind_tracer &amp;&amp; cargo run -p source-size-lint &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated></verify>
  <done>Exactly one active initiative can exist in each of the three slots per faction; proposal, objection, disposition, validation, ownership, preemption, and every terminal outcome are causally replayable, idempotent retries cannot add a second initiative, and invalid lifecycle input leaves the aggregate unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `StrategicPacket` to mind transition | Bounded faction-visible input becomes durable strategic intent. |
| initiative command to aggregate | An untrusted future candidate can attempt duplicate, stale, or ownership-breaking lifecycle changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | mind command and causal transition | high | mitigate | Stable command/content identities, predecessor checks, canonical ordering, and idempotency tests reject duplicate or altered transition input. |
| T-04-02 | Repudiation | initiative lifecycle | high | mitigate | Append immutable causal events containing owner, predecessor, disposition, trigger, and Executive decision metadata. |
| T-04-03 | Elevation of privilege | public mind-domain API | high | mitigate | Expose only planning-state transitions; no provider, report, transport, persistence, or X4 mutation operation belongs to the crate. |
| T-04-SC | Tampering | Cargo dependencies | high | mitigate | Add only workspace-local crate paths and reuse the pinned serialization stack; no package-manager install occurs. |
</threat_model>

<verification>
Run RED then GREEN for both task contracts; after each task run the focused target, source-size lint, and strict workspace Clippy. At the wave boundary run `cargo test --workspace` and retain mutation measurement for the final pure-domain plan.
</verification>

<success_criteria>
MIND-01, MIND-05, INST-03, and INST-08 are met by independent, doctrine-divergent, deterministic Faction Mind aggregates with a single-active initiative invariant and immutable causal evidence.
</success_criteria>

<output>
Create `.planning/phases/04-persistent-full-faction-minds/04-01-SUMMARY.md` when done.
</output>
