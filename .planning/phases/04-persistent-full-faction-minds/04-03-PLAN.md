---
phase: 04-persistent-full-faction-minds
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - Cargo.toml
  - crates/mind-persistence/Cargo.toml
  - crates/mind-persistence/src/lib.rs
  - crates/mind-persistence/src/checkpoint.rs
  - crates/mind-persistence/src/port.rs
  - crates/mind-persistence/src/fake_port.rs
  - crates/mind-persistence/tests/checkpoint_tracer.rs
  - crates/mind-persistence/tests/schema_contract.rs
  - crates/mind-persistence/tests/fake_port_contract.rs
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-06]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A complete checkpoint envelope binds typed mind state, stable strategic-tick identity, replay/admission identity, reserved report identity, schema, protocol, cursor, and integrity hash into one deterministic record."
    - "The local fake checkpoint port accepts only a matching predecessor cursor/hash and returns an explicit reread acknowledgement; it never becomes campaign authority."
    - "A compatible reconnect reloads the acknowledged checkpoint without duplicating accepted state, and an incompatible game-side protocol returns an explicit restart-required disposition."
  artifacts:
    - "crates/mind-persistence exposes the versioned checkpoint codec and narrow checkpoint-port trait."
    - "checkpoint_tracer, schema_contract, and fake_port_contract test deterministic encoding, shared-schema compatibility, acknowledgement, strategic-tick retry refusal, and reconnect compatibility."
  key_links:
    - "mind_domain::PendingMindCommit -> CheckpointEnvelope -> CheckpointPort compare-and-set -> reread acknowledgement -> acknowledged cursor"
---

<objective>
Implement the canonical checkpoint envelope and fake X4 checkpoint-port contract against the stable MD schema.

Purpose: prove the authority firewall and acknowledged commit semantics locally without an external database, report channel, model call, or live X4 claim.
Output: checkpoint codec, narrow fake-port contract, deterministic acknowledgement tests, and compatibility disposition.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-persistent-full-faction-minds/04-CONTEXT.md
@.planning/phases/04-persistent-full-faction-minds/04-RESEARCH.md
@.planning/phases/04-persistent-full-faction-minds/04-01-SUMMARY.md
@.planning/phases/04-persistent-full-faction-minds/04-02-SUMMARY.md
@crates/mind-domain/src/lib.rs
@extensions/live_galaxy/md/live_galaxy_persistence.xml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Encode one committed mind transition as a complete canonical checkpoint</name>
  <files>Cargo.toml, crates/mind-persistence/Cargo.toml, crates/mind-persistence/src/lib.rs, crates/mind-persistence/src/checkpoint.rs, crates/mind-persistence/tests/checkpoint_tracer.rs, crates/mind-persistence/tests/schema_contract.rs</files>
  <behavior>
    - RED: the Plan 04-01 paired-mind fixture converts one `PendingMindCommit` into a canonical versioned envelope containing accepted snapshot identity, stable strategic-tick identity, typed aggregate/initiative causal state, replay inputs, admission result, and a reserved report identity.
    - RED: encoding the same complete input twice returns identical bytes, sequence, and integrity hash; changing any authoritative field changes the record identity.
    - RED: decode rejects unknown schema, malformed or partial payload, invalid hash, mismatched sequence, oversized field, or missing required typed state without emitting a partial projection.
    - RED: a cross-contract test parses `extensions/live_galaxy/checkpoint_schema.json` and proves Rust checkpoint constants expose the identical schema version and required MD field names.
    - RED: narrative capsule/prose is not required to reconstruct typed commitments.
  </behavior>
  <action>Begin red with the full traced mind transition from Plan 04-01, then create `mind-persistence` using only local `mind-domain`, `strategic-state`, and the already pinned serde stack. Per D-06 and D-08, define a bounded `CheckpointEnvelope` with extension schema version, game-side protocol identity, monotonic cursor, predecessor cursor/hash, canonical payload, and integrity hash; its typed payload includes accepted snapshot identity, stable strategic-tick identity, independent mind aggregates, initiative/causal ledger projection, replay/admission state, and reserved report identity. Define Rust schema constants from the same values named by `extensions/live_galaxy/checkpoint_schema.json`, then add a contract test that parses that manifest and fails when its schema version or required MD field names differ from Rust. Build canonical deterministic encoding/decoding with fixed ordering and explicit size limits, plus errors that retain no partial accepted state. The envelope is the X4 checkpoint payload contract, not a Rust-owned database and not an implementation of Phase 6 report dispatch or acknowledgement.</action>
  <verify><automated>cargo test -p mind-persistence --test checkpoint_tracer &amp;&amp; cargo test -p mind-persistence --test schema_contract &amp;&amp; cargo run -p source-size-lint &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated></verify>
  <done>One complete typed transition produces a deterministic, self-validating checkpoint envelope with all D-08 state categories; the shared MD/Rust schema version and field names remain equal; malformed/incompatible/oversized records fail closed before projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce acknowledged fake-port compare-and-set and compatibility recovery</name>
  <files>crates/mind-persistence/src/port.rs, crates/mind-persistence/src/fake_port.rs, crates/mind-persistence/src/checkpoint.rs, crates/mind-persistence/tests/fake_port_contract.rs</files>
  <behavior>
    - A fake port stores a complete envelope only when expected predecessor sequence/hash match and returns the exact reread sequence/hash acknowledgement.
    - Matching duplicate retries return the prior acknowledgement without a second transition or strategic tick; stale or same-sequence/different-content writes fail closed.
    - A Rust-only compatible reconnect reloads the acknowledged envelope and does not duplicate its accepted strategic tick or reserve a duplicate report identity.
    - An incompatible game-side protocol produces a typed fail-closed status that names X4 restart as the required owner action.
  </behavior>
  <action>Write fake-port contract tests first, then define a narrow synchronous `CheckpointPort` trait with load, compare-and-set store, reread acknowledgement, and compatibility-status operations. Implement its deterministic in-memory fake as a test seam, never as campaign persistence. Per D-09 through D-11 and STATE-03/06, require exact predecessor cursor/hash matching; preserve the former acknowledged record after any malformed, stale, duplicate-content collision, or failed acknowledgement outcome. Assert that an exact retry returns the prior admitted strategic-tick identity and an accepted reconnect reloads it without a second tick. Model a compatible bridge generation as a reload/reconciliation path and an incompatible MD protocol as an explicit fail-closed `X4RestartRequired` result. Keep diagnostics as bounded typed error metadata only; do not touch player saves, external SQLite, named-pipe return framing, Mail/Logbook delivery, or any mutation API.</action>
  <verify><automated>cargo test -p mind-persistence --test fake_port_contract &amp;&amp; cargo test -p mind-persistence --test checkpoint_tracer &amp;&amp; cargo run -p source-size-lint &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated></verify>
  <done>Local fake-port tests prove acknowledged compare-and-set, strategic-tick retry idempotency, last-acknowledged recovery, compatible bridge reload without duplicate ticks, and incompatible X4-restart status without claiming real X4 behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| pending aggregate to checkpoint envelope | Typed authoritative state becomes opaque X4-owned checkpoint bytes. |
| checkpoint envelope to port acknowledgement | A stale/corrupt/replayed write must not overtake acknowledged campaign state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-07 | Tampering | checkpoint codec | high | mitigate | Canonical encoding, schema/protocol checks, integrity binding, explicit size limits, and adversarial decode tests reject altered or partial envelopes. |
| T-04-08 | Tampering | fake checkpoint port | high | mitigate | Compare predecessor cursor/hash and return only matching reread acknowledgement; test stale and content-collision retries. |
| T-04-09 | Denial of service | checkpoint payload | medium | mitigate | Bound serialized fields and reject oversized payload before port invocation. |
| T-04-10 | Repudiation | compatibility recovery | high | mitigate | Persist typed cursor/hash and compatibility disposition; compatible reload and restart-required outcomes are explicit test assertions. |
| T-04-SC | Tampering | Cargo dependencies | high | mitigate | Reuse pinned serde and workspace-local crates; no package-manager install occurs. |
</threat_model>

<verification>
Run RED/GREEN contract cycles, then both focused tests, source-size lint, strict workspace Clippy, and `cargo test --workspace`. Mutation evidence is scoped to the pure codec/recovery policies in Plan 04-04.
</verification>

<success_criteria>
STATE-01/02/03/06 are locally proven at the codec and fake-port boundary: only an X4 acknowledgement can advance durable cursor state, duplicates cannot duplicate it, and incompatible game-side revision fails closed with an X4-restart requirement.
</success_criteria>

<output>
Create `.planning/phases/04-persistent-full-faction-minds/04-03-SUMMARY.md` when done.
</output>
