---
phase: 04-persistent-full-faction-minds
plan: 02
type: execute
wave: 1
depends_on: [03-03]
files_modified:
  - extensions/live_galaxy/md/live_galaxy_persistence.xml
  - extensions/live_galaxy/checkpoint_schema.json
  - extensions/live_galaxy/tests/persistence_schema_contract.ps1
  - .planning/phases/04-persistent-full-faction-minds/04-X4-PERSISTENCE-EVIDENCE.md
autonomous: true
requirements: [STATE-01, STATE-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A stable extension-scoped MD root cue declares the one versioned opaque checkpoint contract without reading or modifying player save files."
    - "Static verification proves stable cue and variable identity, explicit schema/protocol fields, and absence of strategic model, report, or game-mutation actions."
    - "Payload, interruption, save/load, and reconnect claims are recorded as pending-X4 observations for Phase 7 rather than claimed from static evidence."
  artifacts:
    - "extensions/live_galaxy/md/live_galaxy_persistence.xml and extensions/live_galaxy/checkpoint_schema.json declare one shared long-lived checkpoint contract."
    - "extensions/live_galaxy/tests/persistence_schema_contract.ps1 validates the extension-owned static contract."
    - ".planning/phases/04-persistent-full-faction-minds/04-X4-PERSISTENCE-EVIDENCE.md separates documented, locally verified, pending-X4, and observed-in-X4 evidence."
  key_links:
    - "stable MD root cue variable -> opaque checkpoint envelope consumed by the later Rust checkpoint port"
---

<objective>
Define and statically verify the X4-owned Mission Director checkpoint surface without asserting unobserved game runtime behavior.

Purpose: make the campaign-authority endpoint stable before Rust recovery code targets it, while leaving report/ack delivery to Phase 6 and runtime proof to Phase 7.
Output: extension MD checkpoint schema, deterministic static contract test, and bounded Phase 7 evidence protocol.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-persistent-full-faction-minds/04-CONTEXT.md
@.planning/phases/04-persistent-full-faction-minds/04-RESEARCH.md
@extensions/live_galaxy/md/live_galaxy_observation.xml
@extensions/live_galaxy/content.xml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Declare one stable MD root-cue checkpoint record</name>
  <files>extensions/live_galaxy/md/live_galaxy_persistence.xml, extensions/live_galaxy/checkpoint_schema.json, extensions/live_galaxy/tests/persistence_schema_contract.ps1</files>
  <action>Create a separate stable `live_galaxy_persistence_root` Mission Director root cue and a versioned `checkpoint_schema.json` as the canonical shared field/version manifest. The extension-scoped checkpoint variable contains exactly an opaque payload plus explicit envelope schema version, game-side protocol identity, monotonic sequence, and canonical integrity hash. Per D-06 and D-07, the cue owns only campaign-lifetime checkpoint storage and reads neither player save files nor external databases. Per D-10 and D-11, declare compatibility status inputs sufficient for a later port to distinguish compatible Rust reconnect from an incompatible game-side protocol that names an X4 restart requirement. Start with a failing PowerShell XML contract test, then make it parse the XML and the shared manifest and assert identical root-cue/variable identities, field names, and schema version. Do not add model invocation, report or acknowledgement delivery, named-pipe framing, Lua scheduling, or game-state mutation actions; Phase 6 owns the general return channel and Phase 7 owns runtime observation.</action>
  <verify><automated>pwsh -NoProfile -File extensions/live_galaxy/tests/persistence_schema_contract.ps1</automated></verify>
  <done>A parsed MD file and shared manifest provide one stable root-cue variable contract for one opaque versioned checkpoint, and the static test rejects missing or mismatched identity, version, schema, protocol, sequence, or integrity fields.</done>
</task>

<task type="auto">
  <name>Task 2: Record the constrained X4 persistence evidence protocol</name>
  <files>.planning/phases/04-persistent-full-faction-minds/04-X4-PERSISTENCE-EVIDENCE.md, extensions/live_galaxy/tests/persistence_schema_contract.ps1</files>
  <action>Document the Phase 7 disposable Creative Custom procedure that will measure checkpoint payload limits, valid save/load restoration, Rust-only reconnect, interrupted-write last-good retention, and incompatible-protocol fail-closed behavior. Per D-09 through D-11, require each observation to record X4 build, extension/mod set, scenario, elapsed game and real time, checkpoint sequence/hash, expected result, reread result, and bounded diagnostics. Mark the Mission Director saved-state, old-variable, and cue-version-patch facts as documented; mark XML parsing as verified locally; mark all payload/interruption/save-load/reconnect facts pending-X4 until the scheduled Phase 7 gate. Extend the PowerShell test to assert that the evidence document contains those categories and Phase 7 handoff; it must not simulate a game result or claim runtime proof.</action>
  <verify><automated>pwsh -NoProfile -File extensions/live_galaxy/tests/persistence_schema_contract.ps1</automated></verify>
  <done>The repository carries a static checkpoint contract and an executable evidence checklist whose runtime claims are explicitly pending-X4, with no new human checkpoint introduced in Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Rust checkpoint port to MD variable | Rust will submit opaque state to an X4-owned persistence surface. |
| static schema to X4 runtime | XML validity does not prove runtime payload, interruption, or save/load semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-04 | Tampering | checkpoint envelope identity | high | mitigate | Require schema, protocol, sequence, and integrity fields in the stable root-cue schema; later Rust port verifies all before acceptance. |
| T-04-05 | Elevation of privilege | MD cue actions | high | mitigate | Static contract permits storage declaration only and rejects any action path that could introduce model, report, or X4 mutation behavior. |
| T-04-06 | Repudiation | runtime evidence status | medium | mitigate | Evidence document classifies documented, verified locally, pending-X4, and observed-in-X4 separately with Phase 7 procedure metadata. |
| T-04-SC | Tampering | dependencies | high | mitigate | No package installation; PowerShell and repository XML tooling only. |
</threat_model>

<verification>
Run the static PowerShell contract after both tasks. This plan provides local static evidence only; Phase 7 performs the disposable in-game observation without any Phase 4 gate.
</verification>

<success_criteria>
STATE-01 and the X4-side boundary of STATE-06 have a stable documented MD contract with locally parsed invariants, while all runtime persistence claims remain pending-X4 for Phase 7.
</success_criteria>

<output>
Create `.planning/phases/04-persistent-full-faction-minds/04-02-SUMMARY.md` when done.
</output>
