---
phase: quick-260903-32k
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-260903-32k]
files_modified:
  - tools/provision-lua.ps1
  - tools/lua-runner.lock.json
  - tools/live-galaxy-tests-1.0-1.rockspec
  - tools/luarocks.lock
  - extensions/live_galaxy/tests/telemetry_spec.lua
  - extensions/live_galaxy/tests/telemetry_contract.lua
  - extensions/live_galaxy/tests/component_discovery_contract.lua
  - extensions/live_galaxy/tests/x4_discovery_contract.lua
  - extensions/live_galaxy/tests/scheduler_contract.lua
  - extensions/live_galaxy/tests/module_loading_spec.lua
  - extensions/live_galaxy/tests/test_helper.lua
  - extensions/live_galaxy/tests/run_contracts.ps1
  - extensions/live_galaxy/tests/x4-package-conformance.ps1
  - extensions/live_galaxy/tests/persistence_schema_contract.ps1
  - extensions/live_galaxy/tests/package_conformance_contract.ps1
  - extensions/live_galaxy/tests/component_discovery_binding_contract.ps1
  - extensions/live_galaxy/tests/component_discovery_package_guard.ps1
  - extensions/live_galaxy/tests/run_contracts_contract.ps1
  - scripts/component_discovery_package_guard.ps1
  - .agents/skills/live-galaxy-x4-integration/SKILL.md
  - .agents/skills/live-galaxy-x4-integration/CHANGELOG.md
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Full Busted runs through a reproducibly provisioned local Lua 5.1.5 development runtime from the actual Windows checkout, including its spaces.
    - Useful existing Lua behavior survives as focused Busted specs; real product modules execute against explicit external fakes with per-case cleanup.
    - Lua compilation/loading replaces source analysis; small XML checks and genuine runtime bounds remain.
    - Test failures propagate; measured final local evidence makes no X4 compatibility or Rust execution claim.
    - Production Lua/XML/JSON and Rust bytes, paused RED contracts, and unrelated changes remain unchanged.
  artifacts:
    - path: tools/luarocks.lock
      provides: Exact resolved development dependencies, established by successful installation and replay.
    - path: extensions/live_galaxy/tests/module_loading_spec.lua
      provides: Interpreter compilation and real runtime/native/lazy-pipe executable scenarios.
    - path: extensions/live_galaxy/tests/run_contracts.ps1
      provides: Thin focused Busted/XML invocation with exit propagation and component timings.
  key_links:
    - from: tools/provision-lua.ps1
      to: tools/luarocks.lock
      via: Standard LuaRocks make dependency installation and locked replay.
    - from: extensions/live_galaxy/tests/module_loading_spec.lua
      to: extensions/live_galaxy/lua/live_galaxy_runtime.lua
      via: Normal qualified require, captured initialization and observation callbacks, external-only preloads.
    - from: extensions/live_galaxy/tests/run_contracts.ps1
      to: Busted and XML checks
      via: Native exit codes; no test discovery or case execution implemented in PowerShell.
---

<objective>
Execution reconciliation: the owner approved ordinary task-specific Windows TEMP for installation scratch. Observed LuaRocks 3.13.0 virtual-Lua locking failure required standard exact real-package constraints in tools/live-galaxy-tests-1.0-1.rockspec instead of tools/luarocks.lock or --pin. Main accepted this technical adjustment within the reproducible-standard-tooling decision; all ten resolved package versions matched a clean replay. References below to the candidate lock mechanism are superseded by this paragraph, including files_modified, must_haves and Task 1. Native rocks use installed MSVC cl/link without library patches. See SUMMARY for actual commands and evidence. No product or test-coverage scope changed.

Replace custom Lua analysis and execution machinery with Busted-owned product testing, actual Lua compilation/loading, and small XML checks. This is one sequential quick-full migration, not Phase 05.1 execution. QUICK-260903-32k names the approved quick-task acceptance contract, not a new roadmap requirement.
The file count reflects one connected test-infrastructure migration, including obsolete-file deletions and the owning skill/changelog. A generic file-count threshold does not justify another phase or split; the three tasks remain bounded by installation, behavioral migration and cutover.
The owner must approve research plus the checked plan before Task 1. Installation is UNPROVEN: LuaRocks 3.13.0 starts, but installation failed on a space-containing path; the existing Lua EXE has no exports or import library. Do not remove the old runner until Task 1 succeeds.
Use the cited research and primary [LuaRocks Windows setup](https://raw.githubusercontent.com/luarocks/luarocks/main/docs/installation_instructions_for_windows.md), [LuaRocks 3.13.0 make/lock contract](https://raw.githubusercontent.com/luarocks/luarocks/v3.13.0/src/luarocks/cmd/make.lua), [Busted manual](https://lunarmodules.github.io/busted/) and [Lua 5.1 manual](https://www.lua.org/manual/5.1/manual.html) for the specified tooling operations.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/HANDOFF.json
@.planning/phases/05.1-bounded-real-component-discovery-inserted/.continue-here.md
@.planning/quick/260903-32k-replace-custom-lua-test-infrastructure-w/260903-32k-CONTEXT.md
@.planning/quick/260903-32k-replace-custom-lua-test-infrastructure-w/260903-32k-RESEARCH.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-code-review/SKILL.md
@.agents/skills/live-galaxy-skill-evolution/SKILL.md
@.agents/skills/lua/SKILL.md
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Prove full Busted through the pinned Windows Lua development runtime and telemetry behavior</name>
  <files>tools/provision-lua.ps1, tools/lua-runner.lock.json, tools/live-galaxy-tests-1.0-1.rockspec, tools/luarocks.lock, extensions/live_galaxy/tests/telemetry_spec.lua</files>
  <action>
Extend the existing provisioner with an explicit -WithBusted setup mode; normal test runs only verify/use installed tooling. This mode builds only the separate ignored development root tools/.cache/lua-busted/5.1.5. Preserve tools/.cache/lua/5.1.5 and its executable unchanged, including the existing default provisioner/runner lock fields; add a distinct Busted-development materialization entry to tools/lua-runner.lock.json. Capture the original executable hash/version before setup and verify both afterward, including any failed setup attempt. Do not add a swap, promotion or transaction mechanism; the original runtime may remain as fallback.
Preserve the verified official Lua 5.1.5 archive and checksum. Under the new development root, build its core/library C files, excluding lua.c, luac.c and print.c, with clang.exe -O2 -std=c99 -DLUA_USE_WINDOWS -DLUA_BUILD_AS_DLL -shared -fuse-ld=lld; pass the LLVM linker /IMPLIB option for lib/lua51.lib and write bin/lua51.dll. Build lua.c with the same Windows/DLL flags against lua51.lib into the new bin/lua.exe, and copy public headers to include. Keep x64 and the same CRT across Lua and native rocks. Verify version, DLL exports and linked native-module loading; do not claim this recipe is proven before execution. All downloaded sources, DLLs, binaries, trees, generated config and probe files stay under ignored tools/.cache; verify every recursive replacement target belongs to the new Busted tooling directories, never the original Lua root.
Use official LuaRocks 3.13.0, record its archive checksum/provenance in the existing tooling lock, and configure only the current process/local tree. Resolve the installed clang-cl.exe/lld-link.exe and required Windows SDK/CRT environment for standard Windows rock builds; fail clearly if unavailable. Use PowerShell argument arrays and standard LuaRocks settings for the actual space-containing checkout. Try repository-relative --lua-dir and --tree values from the repository root; verify actual interpreter identity. Do not patch LuaRocks, introduce a resolver, change Lua version, fake framework dependencies, strip Busted, or install globally to overcome a failure.
The ordinary development rockspec declares Lua 5.1 and busted == 2.3.0-1 as dependencies with no product installation. Initial command from tools is luarocks --lua-version=5.1 --lua-dir=.cache/lua-busted/5.1.5 --tree=.cache/luarocks-tree make live-galaxy-tests-1.0-1.rockspec --only-deps --pin. Capture the generated tools/luarocks.lock only after success; repeat make --only-deps without --pin against a second empty ignored tree, retaining --lua-dir=.cache/lua-busted/5.1.5, to prove the same resolved versions. All Busted launcher, setup, replay and development-lock paths must select this new runtime. Use the pinned LuaRocks executable via its resolved path, not a global executable. Record any necessary standard compiler/config arguments in the provisioner; leave no ad hoc shell-only setup required for replay. These are candidate commands, not prior success evidence. If ordinary path handling, native build, installation or replay remains blocked, stop before migration/deletion and return the exact failed command/output and missing prerequisite to the owner; do not expand provisioning into a new framework.
After full installation, port the four telemetry scenarios to a separate telemetry_spec.lua using describe/it, real qualified product requires and independent dkjson decoding where JSON semantics are asserted. Keep telemetry_contract.lua unchanged for the old aggregate until Task 3 cutover, then delete it. Use Busted --lpath with the extensions/?.lua root, without the bare product-module fallback. Run through the generated local Busted launcher from the actual repository path; exercise a temporary failing assertion once and remove that probe. This tracer proves the real interpreter, native framework dependencies, standard runner, real product module and useful behavior together.
  </action>
  <verify><automated>pwsh -NoProfile -File tools/provision-lua.ps1 -WithBusted -CompilerPath &lt;resolved installed clang.exe&gt;; then tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua" extensions/live_galaxy/tests/telemetry_spec.lua. Assert the launcher uses tools/.cache/lua-busted/5.1.5/bin/lua.exe, successful native dependency requires, telemetry exit 0, intentional assertion exit nonzero, successful same-version locked replay, and unchanged original Lua hash/version plus telemetry_contract.lua bytes. Report setup separately from test elapsed time.</automated></verify>
  <done>Full pinned Busted and its actual dependencies execute telemetry on Lua 5.1.5 at this checkout; exact dependency replay succeeds. On failure the task is blocked and the current runner remains available.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate useful scenarios and execute real module/native/lifecycle boundaries</name>
  <files>extensions/live_galaxy/tests/component_discovery_contract.lua, extensions/live_galaxy/tests/x4_discovery_contract.lua, extensions/live_galaxy/tests/scheduler_contract.lua, extensions/live_galaxy/tests/module_loading_spec.lua, extensions/live_galaxy/tests/test_helper.lua</files>
  <action>
Migrate the remaining useful scenarios to Busted: component discovery (23 current), runtime/discovery (10), scheduler (3). Together with telemetry these preserve the 40 existing behavioral scenarios, without using a count as substitute for behavior. Retain identity/order, complete-scope validation, allocation-before-limit rejection prevention, sorted frames, payload/work bounds, retry/discard, diagnostics, normalization and save suppression. Repair the owner-mismatch fixture with valid native policy/size and assert that metadata was actually consulted before mismatch rejection.
Keep test_helper a small per-case external-environment fixture, not a runner. Register after_each restoration before installing fakes; restore prior values or absence for every touched global, package.preload/loaded entry and package.path/cpath, including error paths. Use canonical live_galaxy/lua/* names consistently and account for the registered extensions.live_galaxy.lua.live_galaxy_runtime alias when resetting real modules. Never preload/replace product modules. For trace configuration require the real config table, save changed fields and restore them. Each case reloads relevant real modules so captured locals cannot retain another case's fakes.
In module_loading_spec compile every shipped Lua file using loadfile without calling the returned chunk; discover files with Busted's installed filesystem dependency rather than parsing imports or maintaining a fixed source list. Require the actual entrypoint and real transitive modules, capture and execute its initialization callback and registered live_galaxy_observation callback far enough to reach delayed native-adapter and pipe imports. Fake only ffi, required X4 globals and extensions.sn_mod_support_apis.ui.named_pipes.Interface. Exercise real native adapter count/fill and zero-based buffers, UniverseID conversions, owner/sector lookup, and GetPeopleCapacity(component64, "", false); verify arguments, ordering, call counts, results, thrown failures and no allocation after rejected resource counts with luassert spies/mocks. Unexpected external calls must fail the scenario. Exercise _Write_Pipe_Raw and Disconnect_Pipe success/failure and absent external module/callback behavior. Add a targeted missing-product-module failure scenario without a custom resolver; restore standard loaders/cache immediately afterward. Verify isolation through differently seeded cases, including a protected intentional error followed by clean reloading. Local fakes establish only exercised contracts, not X4 loader/native compatibility.
  </action>
  <verify><automated>tools/.cache/luarocks-tree/bin/busted.bat --lpath="extensions/?.lua;extensions/live_galaxy/tests/?.lua" extensions/live_galaxy/tests/component_discovery_contract.lua extensions/live_galaxy/tests/x4_discovery_contract.lua extensions/live_galaxy/tests/scheduler_contract.lua extensions/live_galaxy/tests/telemetry_spec.lua extensions/live_galaxy/tests/module_loading_spec.lua. During edits use the affected file and Busted --filter or --tags only; the legacy telemetry_contract.lua is not a Busted input.</automated></verify>
  <done>Useful behavior survives under Busted; real native and delayed-load paths execute with explicit expectations; intended rejection paths and per-case restoration are demonstrated without product-module substitutes.</done>
</task>

<task type="auto">
  <name>Task 3: Cut over the aggregate, remove obsolete tooling, and complete affected review/verification</name>
  <files>extensions/live_galaxy/tests/run_contracts.ps1, extensions/live_galaxy/tests/x4-package-conformance.ps1, extensions/live_galaxy/tests/persistence_schema_contract.ps1, extensions/live_galaxy/tests/telemetry_contract.lua, extensions/live_galaxy/tests/package_conformance_contract.ps1, extensions/live_galaxy/tests/component_discovery_binding_contract.ps1, extensions/live_galaxy/tests/component_discovery_package_guard.ps1, extensions/live_galaxy/tests/run_contracts_contract.ps1, scripts/component_discovery_package_guard.ps1, .agents/skills/live-galaxy-x4-integration/SKILL.md, .agents/skills/live-galaxy-x4-integration/CHANGELOG.md</files>
  <action>
Replace run_contracts.ps1 with thin local-tool verification and Busted/XML process invocation. Expose -Suite all/lua/component_discovery/x4_discovery/telemetry/scheduler/syntax/xml and a -Filter forwarded to Busted. All runs use already installed dependencies and return useful nonzero status on failure; print raw elapsed seconds and exit result separately for Busted, XML/schema and aggregate. Use Busted case discovery and selection, not case tables or a second discovery engine. Pass product package.path deliberately; retain any required launcher compatibility only where an active consumer exists.
Replace x4-package-conformance.ps1 with small .NET XML checks: parse shipped XML, preserve content identity/dependency, addon identity, ui environment/dependency, exact registered entrypoint filename and existence. Keep malformed XML, required-field and missing-entrypoint negative examples as a few in-process checks of this product validator, without spawning a checker per fixture. Retain persistence_schema_contract.ps1 parsed JSON/XML canonical fields, root cue, static/non-instantiated cue, one checkpoint variable and schema/protocol agreement. Remove its source-vocabulary and prose-evidence phrase assertions; evidence honesty belongs to review.
Delete the old telemetry_contract.lua when the aggregate selects telemetry_spec.lua. Delete package_conformance_contract.ps1, component_discovery_binding_contract.ps1, both component_discovery_package_guard.ps1 files, and run_contracts_contract.ps1. Remove the lexer/expression/import traversal code rather than leaving it orphaned. Remove analyzer-only byte/file/import/depth quotas and source-word/fixed-path guards; retain runtime work/payload assertions from Task 2. Do not preserve the 57 parser fixtures or build a replacement runner metatest suite. Update only the integration skill's Verification section and its changelog: replace static-full-graph requirements with actual compilation, real-module/external-fake checks, honest evidence limits, and new focused/aggregate commands. Testing policy already belongs to live-galaxy-x4-tests; reference it rather than duplicating its rules or introducing a global rule.
Run one disposable failing Busted assertion and one malformed XML product fixture through the actual thin aggregate path, independently confirming each nonzero exit; restore/remove probe artifacts. Avoid production-file edits for probes: give the small XML validator an explicit extension-root parameter and run against an ignored isolated fixture. Conduct required risk-first code review and fix substantiated defects, using focused checks. After fixes converge run one final affected aggregate, record commands/case counts/raw component and total times against the 38.059838-second historical baseline without a new SLA. Inspect the scoped diff and byte hashes to confirm production and Rust preservation, review active references to deleted tooling, and record skill-learning disposition in the existing quick-task output. Markdownlint respects .planning/** ignore; do not install another tool or force an alternate configuration.
  </action>
  <verify><automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite all; git diff --check. Before this final run, demonstrate independent Busted/XML nonzero propagation once and complete review fixes. Use installed markdownlint-cli2 --fix on the edited skill/changelog, respecting repository ignores.</automated></verify>
  <done>Busted owns execution; XML/persistence checks remain small; obsolete analyzer/guards/metatests are removed. Review and one final affected aggregate pass with measured evidence and unchanged production/Rust bytes.</done>
</task>
</tasks>

<threat_model>
Affected layer: x4-runtime test tooling only. No game, model, bridge, save or runtime persistence boundary changes.

| Boundary/threat | Severity | Disposition and mitigation |
| --- | --- | --- |
| Downloaded build/test dependencies execute locally | medium | mitigate: official provenance/checksums, isolated development tree, pinned successful resolution and standard locked replay; no global install or fake framework dependencies. |
| Test fakes/cache state hide product defects | medium | mitigate: real product requires, strict external call expectations, intended-path assertions and per-case cleanup including failure. |
| Wrapper hides a failed product check | medium | mitigate: one deliberate Busted failure and one XML failure through its actual invocation path. |

Selected gates: code review and local verification; scoped dependency/provisioning security review within code review because downloaded native tooling crosses the local execution boundary; requirement-to-test coverage audit because test replacement can lose behavior. No new security or Nyquist framework. Rejected gates: AI specification/eval (no model contract), mutation campaign (no domain/safety logic changes), X4 probe and SETA soak (runtime bytes/timing behavior unchanged). Reassess only if execution would exceed these boundaries, rather than treating disabled config as a risk justification.
</threat_model>

<verification>
One wave, strictly Task 1 → Task 2 → Task 3. Record the existing 40 scenarios' disposition and the added native/loading evidence; all useful behavior must remain covered, while tool-internal fixture counts have no quota. No dependency installation on ordinary test runs. No Rust suite, X4 launch, sibling modification, global skill change or Phase 05.1 resumption. Check required skill-learning output and retain unresolved installation facts if Task 1 stops.
</verification>

<success_criteria>
Approved design fully implemented; installation/replay observed rather than assumed; focused Busted selection and syntax/XML aggregate pass; meaningful negative propagation and review complete; exact timings/case counts reported without compatibility inflation. Parent orchestrator owns owner approval, Git completion, memory and final quick-task state.
</success_criteria>

## Source coverage audit

| Source | Items | Coverage |
| --- | --- | --- |
| GOAL | Approved quick task: ordinary product testing replaces custom infrastructure | Tasks 1–3; COVERED. No roadmap phase is advanced. |
| REQ | QUICK-260903-32k acceptance: runner/selection/failures, retained behavior, small XML, measured evidence, review | Tasks 1–3; COVERED. No assigned roadmap requirement IDs. |
| CONTEXT | Standard test runner and verified Windows pin | Task 1; COVERED. |
| CONTEXT | Actual compilation/requires; external-only fakes, native expectations, lazy paths, isolation | Task 2; COVERED. |
| CONTEXT | Small XML; remove vocabulary/analyzer limits; preserve real bounds and useful behavior | Tasks 2–3; COVERED. |
| CONTEXT | Owning instructions only; in-process fakes; approval before execution; stop constraints | Objective and Tasks 1–3; COVERED. CONTEXT contains no D-NN IDs. |
| RESEARCH | Unproven space-path install, missing Lua development ABI, full dependencies, exact replay | Task 1; COVERED. |
| RESEARCH | Canonical aliases, cleanup, real config, misleading owner mismatch, native/pipe execution | Task 2; COVERED. |
| RESEARCH | Parser/guard/metatest deletion, retained XML/persistence contracts, failure propagation and timing | Task 3; COVERED. |

<output>
Executor reports evidence through the existing quick-full workflow. Planner creates this PLAN only; no completion summary, STATE change, commit, installation or runtime action occurs during planning.
</output>
