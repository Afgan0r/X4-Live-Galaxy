---
phase: quick-260831-3m9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/live_galaxy/tests/run_contracts.ps1
  - tools/x4-verification/tests/evidence_retention_contract.ps1
  - tools/x4-verification/tests/candidate_package_runtime_contract.ps1
  - tools/x4-verification/tests/candidate_package_adversarial.ps1
autonomous: true
requirements: [QUICK-260831-3M9]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Developers have an explicit Phase 05.2 fast iteration gate that completes in no more than 300 seconds on this machine."
    - "The complete closure gate still executes every current contract, PASS marker, and rejection path, and completes in no more than 600 seconds or reports the measured stage that blocks the target."
    - "A single runner invocation never rebuilds identical deterministic candidate fixtures for each consumer."
    - "Reuse is invocation-local and digest-keyed; any identity mismatch fails closed before a reused fixture is consumed."
    - "Authority, evidence, mutation, isolation, verdict, admission, and attestation decisions are recomputed for every contract execution."
    - "The quick-task summary records baseline and optimized total and per-stage timings."
  artifacts:
    - path: "extensions/live_galaxy/tests/run_contracts.ps1"
      provides: "Explicit fast and complete gates, stage timing, prepared-build lifecycle, and cleanup"
      contains: "x4-verification-fast"
    - path: "tools/x4-verification/tests/evidence_retention_contract.ps1"
      provides: "Retention cases that can consume independently validated invocation-local generated builds"
    - path: "tools/x4-verification/tests/candidate_package_runtime_contract.ps1"
      provides: "Runtime contract support for an independently validated prepared-build clone"
    - path: "tools/x4-verification/tests/candidate_package_adversarial.ps1"
      provides: "Adversarial contract support for an isolated prepared-build clone"
  key_links:
    - from: "extensions/live_galaxy/tests/run_contracts.ps1"
      to: "tools/x4-verification/build-candidate-extension.ps1"
      via: "one owner-only prepared build per fast or complete invocation"
      pattern: "build-candidate-extension\\.ps1"
    - from: "extensions/live_galaxy/tests/run_contracts.ps1"
      to: "tools/x4-verification/tests/evidence_retention_contract.ps1"
      via: "prepared root plus exact digest key; every retention case still runs"
      pattern: "PreparedBuild(Root|Key)"
    - from: "prepared deterministic build clone"
      to: "run-candidate-package.ps1 and retain-evidence.ps1"
      via: "existing live conformance, component digest, producer, retention, and authority validation"
      pattern: "PACKAGE_CONFORMANCE|COMPONENT_DIGEST|ATTESTATION"
---

<objective>
Reduce Phase 05.2 test iteration time without weakening closure evidence.

Purpose: Separate a bounded development loop from the canonical closure suite, remove repeated generation of identical candidate build fixtures inside one runner invocation, and make the runtime cost attributable by stage.

Output: Two explicit verification gates, invocation-local digest-keyed generated-build reuse, preserved failure-path execution, and before/after timing evidence in the quick-task summary.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW.md
@.planning/phases/05.2-layered-lua-and-x4-verification-foundation-inserted/05.2-REVIEW-FIX.md
@extensions/live_galaxy/tests/run_contracts.ps1
@tools/x4-verification/tests/evidence_retention_contract.ps1
@tools/x4-verification/tests/candidate_package_runtime_contract.ps1
@tools/x4-verification/tests/candidate_package_adversarial.ps1
@tools/x4-verification/build-candidate-extension.ps1
@tools/x4-verification/retain-evidence.ps1
@tools/x4-verification/run-candidate-package.ps1
</context>

<baseline>

| Measurement | Before |
| --- | ---: |
| Packaged-path conformance | 40.293 s |
| Candidate isolation | 32.973 s |
| Candidate adapters | 54.181 s |
| Evidence retention | 113.856 s |
| Preallocation/open-handle bounds | 4.484 s |
| Package/isolation/adapter/retention/open-handle focused checks | 245.787 s |
| Canonical `x4-verification` closure | 825.845 s |
| Focused plus closure cycle | 1,071.632 s |

These measurements were captured immediately before this plan on the same HEAD and Phase 05.2 fix set. They are the authoritative before column and must not be recreated by rerunning the unoptimized gates. The five retention cases currently build and dispatch the same deterministic candidate fixtures independently. Candidate runtime and adversarial contracts also build equivalent roots. These repeated builds are optimization targets; their authority and behavioral verdicts are not reusable results.
</baseline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire explicit fast and complete gates with per-stage timing</name>
  <files>extensions/live_galaxy/tests/run_contracts.ps1</files>
  <behavior>
    - Test 1: `-Suite x4-verification-fast` runs packaged-path conformance, candidate isolation, adapter behavior, retention, and shared open-handle/preallocation checks, then exits without resolving or running Lua.
    - Test 2: `-Suite x4-verification` continues to run every currently registered admission, authority, build, retention, evidence-chain, isolation, runtime, adversarial, package-conformance, and Lua contract in the same fail-closed order.
    - Test 3: each child stage emits its original stdout and PASS markers unchanged, followed by one sanitized `x4-verification-stage-timing.v1` JSON row containing gate, stable stage ID, elapsed milliseconds, and pass/fail status.
    - Test 4: a failed child stage emits its timing row, preserves the existing failure message and nonzero gate result, and prevents later stages from running.
  </behavior>
  <action>Add `x4-verification-fast` to the runner's validated suites and centralize child execution in a small `Invoke-TimedStage` helper. The fast stage registry must exactly match the measured focused pass: `package_conformance_contract.ps1 -Case packaged-path -SkipAggregateRegistration`, `candidate_isolation_contract.ps1 -Case all`, `candidate_package_runtime_contract.ps1 -Case adapters`, `evidence_retention_contract.ps1 -Case retention`, and `evidence_retention_contract.ps1 -Case preallocation-bounds`. Keep `x4-verification` as the complete closure gate and retain every existing output assertion, special marker assertion, Lua marker assertion, stage order, and fail-fast branch. Emit sanitized timing JSON in a `finally` path without converting a child failure into success. Add one final PASS marker for each top-level gate, without renaming or removing existing contract markers. Use the authoritative measurements in the baseline table as the before column. Do not execute either unoptimized gate merely to recreate those measurements, and do not write timing logs, build roots, or private paths into Git.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$tokens = $null; $errors = $null; [void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'extensions/live_galaxy/tests/run_contracts.ps1'), [ref]$tokens, [ref]$errors); if ($errors.Count -ne 0) { $errors | Out-String | Write-Error; exit 1 }; $command = Get-Command './extensions/live_galaxy/tests/run_contracts.ps1'; $values = @($command.Parameters['Suite'].Attributes | Where-Object { $_ -is [System.Management.Automation.ValidateSetAttribute] } | ForEach-Object ValidValues); if ($values -notcontains 'x4-verification-fast' -or $values -notcontains 'x4-verification') { exit 1 }"</automated>
  </verify>
  <done>The runner parses, exposes explicit fast and complete suite values, preserves the complete stage registry and marker assertions, and contains versioned timing emission. No unoptimized fast or complete gate was run; the plan's authoritative measurements remain the before column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse only digest-bound deterministic generated builds within one invocation</name>
  <files>extensions/live_galaxy/tests/run_contracts.ps1, tools/x4-verification/tests/evidence_retention_contract.ps1, tools/x4-verification/tests/candidate_package_runtime_contract.ps1, tools/x4-verification/tests/candidate_package_adversarial.ps1</files>
  <behavior>
    - Test 1: one fast or complete runner invocation creates one owner-only prepared candidate-build root and supplies independently cloned roots to every eligible consumer instead of rebuilding the same matrix and templates.
    - Test 2: the prepared-build key covers the candidate matrix and every generated manifest/file digest; a key, manifest, generated-file, or current-source mismatch is rejected before dispatch or retention.
    - Test 3: each consumer reruns its existing package conformance, component binding, worker isolation, adapter, producer-attestation, evidence, retention, mutation, and verdict checks against its own clone; no result or decision is shared.
    - Test 4: retention still executes all five closure cases, including unsupported-platform and preallocation/open-handle paths; adversarial mutations remain isolated from later consumers and from the prepared source root.
    - Test 5: the prepared root and every clone are deleted in a runner/consumer `finally` path, while mismatch or cleanup failure remains nonzero and reports only a stable reason/stage code.
    - Test 6: a narrow reuse-contract suite rejects a wrong key, a stale generated-file digest, a changed clone, and a reparse/out-of-temp root without running the complete closure suite or publishing evidence/attestations.
  </behavior>
  <action>Have `run_contracts.ps1` create a fresh owner-only temporary invocation root, call `build-candidate-extension.ps1` once for deterministic generated data, and derive a lowercase SHA-256 key from the sorted candidate matrix digest plus every group manifest and generated-file digest. Rename or place the prepared root under that key, pass both root and key only to eligible test contracts, and remove the invocation root in `finally`. Add optional `PreparedBuildRoot` and `PreparedBuildKey` parameters to the retention, runtime, and adversarial test contracts. Each contract must reject a missing, non-owner-only, reparse, out-of-temp, wrong-key, incomplete, or digest-stale root; copy the validated generated data into its own scratch directory; re-read and validate the clone before use; and preserve its existing standalone behavior by building locally when the parameters are absent. The retention preallocation case must continue invoking the builder with oversized input so that negative bound coverage is not bypassed. The runtime and adversarial contracts may mutate only their private clone and must prove the prepared source root is unchanged. Add a narrow `x4-verification-reuse-contract` runner suite for the new wrong-key, stale-digest, changed-clone, reparse/out-of-temp, unchanged-source, and cleanup failure probes; it must not run unrelated closure stages. Do not reuse JSONL evidence, producer or locator attestations, signatures, authority state, test mutations, worker/isolation outcomes, package/admission verdicts, or PASS results. After the focused reuse probes, run exactly one optimized fast gate and exactly one optimized complete closure gate. Compare their timing rows with the authoritative baseline values: record focused-stage and total before/after comparisons, the optimized complete-closure stage breakdown, and the complete-cycle total in `260831-3m9-SUMMARY.md`; if closure remains above 600 seconds, name the exact measured blocking stages and retained coverage rather than weakening the suite.</action>
  <verify>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-verification-reuse-contract
pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-verification-fast
pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-verification</automated>
  </verify>
  <done>The narrow reuse probes pass; exactly one optimized fast gate completes in at most 300 seconds; exactly one optimized complete closure gate completes in at most 600 seconds or the summary contains a concrete per-stage measured blocker; the closure output retains every established PASS marker and rejection path; one invocation builds deterministic candidate data once; all consumers independently validate digest-keyed clones; no authoritative or behavioral decision is reused; and the summary compares optimized results with the authoritative baseline without an unoptimized rerun.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runner to prepared build root | Mutable filesystem data crosses from the orchestrator into independent child processes. |
| Prepared build clone to contract authority | Generated bytes may be reused, but authority, evidence, isolation, mutation, verdict, and admission decisions must remain fresh. |
| Child stage to timing output | Failure state and timing metadata cross into aggregate output without exposing private paths or changing exit semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-Q3M9-01 | Tampering | Prepared build root and clones | high | mitigate | Bind reuse to a SHA-256 key covering matrix, manifests, and generated files; reject reparse, identity, digest, or source mismatch before consumption. |
| T-Q3M9-02 | Elevation of privilege | Authority/admission consumers | high | mitigate | Reuse generated bytes only; rerun every conformance, authority, attestation, isolation, evidence, retention, mutation, and verdict decision in each contract. |
| T-Q3M9-03 | Repudiation | Stage timing and PASS output | medium | mitigate | Preserve child stdout and stable PASS markers, emit one versioned timing row per stage including failures, and record before/after totals in the quick summary. |
| T-Q3M9-04 | Denial of service | Complete closure runner | medium | mitigate | Build deterministic fixtures once, keep bounded child deadlines and fail-fast ordering, and identify any remaining greater-than-600-second stage from measured output. |
| T-Q3M9-05 | Information disclosure | Timing and cleanup diagnostics | low | accept | Timing rows contain only gate, stable stage ID, duration, and status; no artifact paths, evidence content, authority material, or save data are emitted. |

</threat_model>

<non_goals>

- Do not delete, skip, sample, or weaken any complete-closure contract or failure path.
- Do not persist a fixture cache across invocations or machines.
- Do not cache or reuse authority, signature, evidence, mutation, isolation, verdict, admission, attestation, or retention decisions.
- Do not introduce a new test framework, dependency, service, daemon, or background cache.
- Do not change X4, installed mods, saves, production authority, private retained evidence, or public-package behavior.
- Do not modify Phase 05.2 review/fix artifacts or perform the final clean review, Nyquist, security, or phase-verification audits in this quick task.
- Do not change Phase 05.1/05.2 product scope or promote local-contract evidence to observed-in-X4 evidence.
</non_goals>

<source_audit>

| Source | ID | Requirement | Task | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | QUICK-260831-3M9 | Split fast development and complete closure gates while reducing iteration time. | 1, 2 | COVERED | Explicit suites, measured limits, and unchanged closure coverage. |
| REQ | none | No ROADMAP requirement is assigned to this quick optimization. | — | EXCLUDED | No product capability or phase requirement changes. |
| RESEARCH | baseline | Focused 245.787 s, closure 825.845 s, repeated deterministic builds, retention 113.856 s. | 1, 2 | COVERED | Timing instrumentation and build-only reuse target the measured duplication. |
| CONTEXT | QC-01 | Fast gate at most 300 s. | 1, 2 | COVERED | Both tasks run and time the explicit fast suite. |
| CONTEXT | QC-02 | Complete closure at most 600 s or concrete measured blocker. | 2 | COVERED | Summary records per-stage result without weakening coverage. |
| CONTEXT | QC-03 | Preserve every PASS marker and failure path. | 1, 2 | COVERED | Existing assertions and stages remain; only generated input construction is shared. |
| CONTEXT | QC-04 | Reuse only invocation-local digest-keyed deterministic generated build data. | 2 | COVERED | Prepared root plus independently validated clones. |
| CONTEXT | QC-05 | Never reuse authority or behavioral decisions. | 2 | COVERED | Explicitly recomputed at each consumer boundary. |
| CONTEXT | QC-06 | Record before/after per-stage timings. | 1, 2 | COVERED | Instrument-before, optimize-after sequence feeds the summary table. |

</source_audit>

<verification>

1. Parse all four modified PowerShell files with the PowerShell parser and require zero syntax errors.
2. Run the narrow reuse-contract suite and confirm wrong-key, stale-digest, changed-clone, reparse/out-of-temp, unchanged-source, and cleanup probes fail closed without published evidence or attestations.
3. Run the optimized fast gate exactly once; parse every `x4-verification-stage-timing.v1` row and compare the five focused stages and total with the authoritative before values.
4. Run the optimized complete closure gate exactly once; confirm it reaches every established admission, owner authority, candidate build, five retention, evidence-chain, isolation, runtime, adversarial, package-conformance, and required Lua PASS marker and rejection branch.
5. Record the optimized closure stage breakdown, optimized fast/closure/cycle totals, and authoritative before comparisons in the quick summary; do not rerun an unoptimized gate.
6. Run `git diff --check` over the quick-task implementation range.
</verification>

<success_criteria>

- The explicit fast gate completes in at most 300 seconds on this machine.
- The complete closure gate completes in at most 600 seconds, or the summary identifies a concrete measured blocking stage while retaining all closure coverage.
- One runner invocation creates deterministic candidate build data once and reuses only independently validated clones.
- Every current PASS marker and fail-closed path remains reachable.
- The summary contains before/after totals and per-stage timings, including retention and prepared-build cost.
- No X4 process, save, installed extension, private evidence, authority store, or existing Phase 05.2 artifact is changed.
</success_criteria>

<output>
Create `.planning/quick/260831-3m9-reduce-phase-05-2-test-iteration-time-by/260831-3m9-SUMMARY.md` when done.
</output>
