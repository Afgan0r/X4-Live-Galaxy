---
phase: quick-260903-06l
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/live_galaxy/tests/x4-package-conformance.ps1
  - extensions/live_galaxy/tests/package_conformance_contract.ps1
  - extensions/live_galaxy/tests/run_contracts_contract.ps1
  - extensions/live_galaxy/tests/run_contracts.ps1
  - tools/x4-verification/
  - tests/x4-candidates/
  - extensions/live_galaxy/tests/x4_candidate_runner_contract.lua
  - extensions/live_galaxy/tests/x4_candidate_runner_adversarial.lua
  - .agents/skills/live-galaxy-x4-integration/SKILL.md
  - .agents/skills/live-galaxy-x4-integration/CHANGELOG.md
autonomous: true
requirements:
  - QUICK-260903-06l
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The product test runner validates the actual extension and retained product contracts without the retired candidate research platform.
    - Manifest, registration, full transitive Lua imports, and native acquisition remain executable package checks with meaningful rejection fixtures.
    - Every retained product stage runs once in the final aggregate, and child failure or an empty Lua case table cannot become a passing result.
    - Existing x4_discovery callers and locked Lua resolution remain compatible.
    - Rust, production runtime, historical evidence, and the paused Plan 09 RED contracts retain their existing content and status.
    - Active X4 integration instructions follow the shared research contract and no longer require the deleted local authority framework.
  artifacts:
    - path: extensions/live_galaxy/tests/x4-package-conformance.ps1
      provides: Standalone product package validation using stock reads and the retained lexical import traversal.
    - path: extensions/live_galaxy/tests/package_conformance_contract.ps1
      provides: Actual-package positive and isolated manifest/import/native-binding rejection fixtures.
    - path: extensions/live_galaxy/tests/run_contracts_contract.ps1
      provides: Isolated executable checks of Lua failure, empty cases, and PowerShell stage failure propagation.
    - path: extensions/live_galaxy/tests/run_contracts.ps1
      provides: Focused product suites and one nonrecursive aggregate.
    - path: .agents/skills/live-galaxy-x4-integration/SKILL.md
      provides: Current product verification and shared Docs MCP routing without obsolete local admission mandates.
  key_links:
    - from: extensions/live_galaxy/tests/run_contracts.ps1
      to: extensions/live_galaxy/tests/package_conformance_contract.ps1
      via: The existing x4-package-conformance suite and one aggregate stage.
    - from: extensions/live_galaxy/tests/package_conformance_contract.ps1
      to: extensions/live_galaxy/tests/x4-package-conformance.ps1
      via: Child invocations against the real extension and isolated fixture package roots.
    - from: extensions/live_galaxy/tests/x4-package-conformance.ps1
      to: extensions/live_galaxy/ui.xml
      via: Registration selects the actual Lua entrypoint and its complete import closure.
    - from: extensions/live_galaxy/tests/run_contracts.ps1
      to: tools/lua-runner.lock.json
      via: Existing explicit-path, environment, materialized-path, and PATH resolution with version validation.
    - from: .agents/skills/live-galaxy-x4-integration/SKILL.md
      to: .agent-instructions/x4/AGENTS.md
      via: Shared Docs MCP lookup, knowledge-gap, and validated-example ownership.
---

<objective>
Retire obsolete X4 candidate research verification while preserving checks of the Live Galaxy product.

Purpose: End the repeated research-framework feedback loop and preserve useful executable product coverage, per D-01 through D-08 below.
Output: A lean package checker and fixtures, a product-only runner, removal of the approved research island, and corrected project-local skill instructions.

This is one maintenance quick plan, not a roadmap phase. QUICK-260903-06l is a local traceability label for the quick CONTEXT task boundary, not a new milestone requirement. No roadmap requirement or phase completion status changes.
</objective>

<execution_context>
@C:/Users/pavlo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/pavlo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agent-instructions/x4/AGENTS.md
@.agent-instructions/x4/MEMORY.md
@.planning/quick/260903-06l-remove-obsolete-x4-research-verification/260903-06l-CONTEXT.md
@.planning/quick/260903-06l-remove-obsolete-x4-research-verification/260903-06l-RESEARCH.md
@.planning/STATE.md
@.planning/HANDOFF.json
@.planning/phases/05.1-bounded-real-component-discovery-inserted/.continue-here.md
@.agents/skills/live-galaxy-rust-conventions/SKILL.md
@.agents/skills/live-galaxy-rust-tests/SKILL.md
@.agents/skills/live-galaxy-x4-integration/SKILL.md
@.agents/skills/live-galaxy-x4-tests/SKILL.md
@.agents/skills/live-galaxy-skill-evolution/SKILL.md
@.agents/skills/lua/SKILL.md
@docs/architecture-verification.md
</context>

## Execution boundary

Owner approval of the research and checked plan is required before the orchestrator dispatches execution. `autonomous: true` applies after that approval; it does not authorize bypassing it. Research and plan checking precede execution; code review and verification follow it. The quick workflow owns commits and its completion row.

The main agent established fresh origin refs at `d570ea45afec335344843facdaf37ccbb7199db2`, branch `codex/0.1-shadow-director`, matching upstream. The checkout has unrelated changes. Preserve the shared-contract bundle, `AGENTS.md`, `.planning/config.json`, deleted researcher agent, `.gsd/`, and all other pre-existing work. Do not stage by directory or clean unrelated artifacts. Recheck scoped ownership before edits and deletion.

Discovery level: existing repository implementation and completed scoped research; no external dependency or unresolved X4 API claim is introduced. No installs, external services, saves, installed extensions, sibling changes, or game operations are needed. Do not migrate the removed framework to Docs MCP. Existing machine-local retained evidence, caches, and CNG state remain outside deletion scope.

No production Lua/XML/JSON, Cargo manifest, lockfile, Rust source, Rust test, hook, routing marker, or shared contract is owned here. If an actual dependency requires changing one, stop that dependent slice and return the concrete conflict to the owner. Do not implement Phases 05.3-05.5 or resume/reconcile Phase 05.1.

## Decision aliases

The CONTEXT has eight unnumbered locked owner bullets. These aliases preserve their order for traceability; they add no decisions.

| Alias | Locked decision |
| --- | --- |
| D-01 | Product feedback must not repeatedly run the 15-20 minute research platform. |
| D-02 | Remove the obsolete infrastructure rather than retain it as an optional slow suite. |
| D-03 | Docs MCP owns API knowledge research; no cross-repository migration here. |
| D-04 | Preserve product Lua, adapters, native binding, package, persistence, Rust, and paused RED contracts. |
| D-05 | Extract real package and import validation before deleting its old implementation. |
| D-06 | Preserve history, unverified game behavior, and unfinished requirement status. |
| D-07 | Research, checked-plan approval, code review, and final verification are required. |
| D-08 | No X4 launch, save access, installed-extension changes, or sibling edits. |

## Dependency and size

One sequential wave: Task 1 creates an executable package slice, Task 2 consumes it to retire the old island, and Task 3 updates active instructions before review. The executor then returns with review pending. The orchestrator owns external code review, any scoped fix loop, and the sole final product regression after review convergence; these lifecycle gates are not a fourth implementation task. Shared runner ownership requires sequencing. Deletion roots denote the entire already approved trees, including their tests, templates, and fixtures; they are mechanical removals within the same test-tool concern. No executor must inspect every retired file independently.

Estimate calibration returned factor `1`, zero samples, confidence `low`; raw and calibrated projections are both 30,000 tokens.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate the real extension through an independent product package slice</name>
  <files>extensions/live_galaxy/tests/x4-package-conformance.ps1, extensions/live_galaxy/tests/package_conformance_contract.ps1, extensions/live_galaxy/tests/run_contracts_contract.ps1, extensions/live_galaxy/tests/run_contracts.ps1</files>
  <read_first>Read tools/x4-verification/x4-package-conformance.ps1, tools/x4-verification/contracts/package-conformance.v1.json, tools/x4-verification/tests/package_conformance_contract.ps1, extensions/live_galaxy/tests/run_contracts.ps1, extensions/live_galaxy/tests/component_discovery_binding_contract.ps1, extensions/live_galaxy/tests/component_discovery_package_guard.ps1, extensions/live_galaxy/content.xml, extensions/live_galaxy/ui.xml, and tools/lua-runner.lock.json. Extract only the parser and product invariants identified by RESEARCH.</read_first>
  <behavior>
    - The new checker accepts the actual package with its registered entrypoint and complete transitive imports, without research admission inputs.
    - Independent fixtures accept the existing literal, supported static-concatenation, and helper-import forms; comment, long-string, and quoted decoys do not become imports or native acquisitions.
    - Missing/wrong package identity, dependency, environment, registration, or entrypoint fails with an actionable package-relative reason.
    - Missing transitive modules, wrong path case, bare/test-only/dynamic imports, root escape, cycles, and traversal/read-bound violations fail.
    - Missing, duplicate, or alternate-only native acquisition fails; the real direct ffi.C acquisition passes.
    - In an isolated copy of the runner, a failing Lua assertion, empty Lua case table, and failing PowerShell binding stage each produce a nonzero process result; a diagnostic-only output cannot substitute for executed cases.
  </behavior>
  <action>
    Per D-04 and D-05, first add a failing end-to-end contract for the new checker CLI against the real extension, then extract the working checker and wire only the existing package-suite branch to its new fixture harness. Keep the old trees available until this focused path passes. The checker accepts PackageRoot, defaults to the actual extension beside tests, exits zero on success and nonzero on failure, and reports package-relative diagnostics. A package pass is local static evidence only.

    Reuse the existing comment/string-aware Lua tokenizer, static expression/helper handling, traversal, exact-case resolution, and executable native-access analysis. Retain product expectations independently of the retired JSON contract: package live_galaxy, content dependency ws_2042901274, menus registration with sn_mod_support_apis, lua/live_galaxy_runtime.lua entrypoint, live_galaxy/lua/ internal prefix, and the existing two external modules ffi and extensions.sn_mod_support_apis.ui.named_pipes.Interface. Preserve one approved direct native acquisition over the complete graph. Keep these values in one cohesive checker rather than deriving expected values from the package under test. Retain existing finite file-byte, total-byte, file-count, import-count, depth, and diagnostic limits as package-tool safeguards; they are not runtime policy or performance targets.

    Use ordinary stock reads after existence, containment, exact-case, reparse-point, and size checks. Preserve XML parsing with external resolution disabled. Remove native identity interop, concurrent-tamper defense, admission contracts, candidate modes, evidence certification, and digest production from the extracted path. Do not copy the old whole package harness: it recursively runs aggregate suites. The new package fixture harness calls only the new checker and never calls any runner suite.

    Use isolated temporary package copies for negative fixture edits, with a fresh root per case and cleanup in finally. Never alter the real package or put failure fixtures alongside actual test scripts. Validate every resolved cleanup target stays inside that task-created temporary root before recursive removal. Assert the expected failed condition, not merely any nonzero error, so syntax/setup failure cannot pass a rejection test. Use the actual package positive before negatives.

    Add the separate run_contracts_contract.ps1 fixture driver for process-result propagation. Copy the runner under test byte-for-byte to extensions/live_galaxy/tests/run_contracts.ps1 in an isolated temporary root and copy the real tools/lua-runner.lock.json to the same relative location. Resolve the real locked Lua executable from the original repository using the existing resolver precedence, validate its reported version, and pass that absolute path through LuaPath to every focused fixture invocation. Require pwsh, the copied runner, and the copied lock to exist before evaluating cases; a missing tool, missing lock, invalid version, or preparation error fails the driver.

    The fixture root contains only those copies plus extensions/live_galaxy/tests/x4_discovery_contract.lua, extensions/live_galaxy/lua/runner_fixture.lua, and, for the binding case, extensions/live_galaxy/tests/component_discovery_binding_contract.ps1. The pure runner_fixture module returns a fixed fixture token; the Lua contract imports it through live_galaxy/lua/runner_fixture, using the runner's existing extension module path. No production module, guard, research tree, or materialized Lua binary is copied. The current runner's research-path declarations use Join-Path without importing or executing those paths for these focused selections, so no extra extraction is needed for fixture preparation.

    First run a positive x4_discovery fixture that imports the module, validates its token, and completes a case named FIXTURE_READY; require exit zero and that executed case marker. Then use a fresh isolated root per negative case. The failing Lua case raises FIXTURE_LUA_ASSERTION; require nonzero exit, that exact assertion marker, and the runner's file-specific Lua failure. The empty-case fixture prints FIXTURE_EMPTY_REACHED and returns an empty table; enforce EMPTY_LUA_CASE_TABLE in the runner's Lua case validation and require nonzero exit plus both markers, proving diagnostic output cannot count as an executed case. The binding fixture prints FIXTURE_BINDING_REACHED and exits 23; invoke component_discovery and require nonzero exit, the reached marker, and the binding-stage failure. A nonzero result without the case-specific markers fails the driver rather than satisfying the negative case. Capture child stdout and stderr inside the driver and emit concise case results. Do not invoke aggregate suites, recursively call the driver, copy research tooling, or add test-only knobs to production runtime. Keep this driver out of aggregate registration until Task 2. Preserve the existing Lua resolver and product contracts, per D-04.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4-package-conformance</automated>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts_contract.ps1</automated>
  </verify>
  <done>The new package suite passes the actual product and all listed isolated cases; the process-result fixtures demonstrate failure propagation. It requires no local dossier or candidate admission input. The old deletion roots are still available until this slice passes.</done>
</task>

<task type="auto">
  <name>Task 2: Retire research tooling and make aggregate dispatch product-only</name>
  <files>extensions/live_galaxy/tests/run_contracts.ps1, tools/x4-verification/, tests/x4-candidates/, extensions/live_galaxy/tests/x4_candidate_runner_contract.lua, extensions/live_galaxy/tests/x4_candidate_runner_adversarial.lua</files>
  <read_first>Read the retained portions of extensions/live_galaxy/tests/run_contracts.ps1, extensions/live_galaxy/tests/persistence_schema_contract.ps1, scripts/component_discovery_package_guard.ps1, and the current scoped Git diff. Task 1 owns the available isolated propagation contracts.</read_first>
  <action>
    Per D-01 and D-02, remove the entire two approved research trees and the two candidate-only Lua test entrypoints after Task 1 passes. Before each recursive deletion, resolve the repository and target to absolute paths, confirm the target is exactly one of the two approved roots inside this checkout, and use native PowerShell LiteralPath deletion. Do not follow reparse points into another directory or touch sibling/local retained state. If a deletion root has another task's edits, pause only that conflicting removal.

    Reduce the runner selection to all, x4_discovery, component_discovery, component_discovery_guard, and x4-package-conformance. Keep Suite and LuaPath invocation compatibility for supported callers. Unknown or removed suites must fail parameter validation rather than silently execute another selection. Remove prepared candidate builds, reuse keys, research timing schemas, candidate marker registries, and their helper machinery.

    Per D-04, retain the locked Lua resolution order, exact version validation, extension module paths, and executable Lua cases. Keep x4_discovery focused on x4_discovery_contract.lua. Keep component_discovery's binding contract, guard self-test and four existing production paths, followed by component_discovery_contract.lua. Keep component_discovery_guard's current self-test plus production-file checks. Do not alter the guard implementation or its wrapper.

    Make all explicitly execute the four existing Lua files component_discovery_contract.lua, x4_discovery_contract.lua, telemetry_contract.lua, and scheduler_contract.lua; the lean package fixture harness; component_discovery_binding_contract.ps1; the component guard self-test and each existing production path; persistence_schema_contract.ps1; and the isolated runner process-result driver. Each real product check runs once. Use one shared dispatch definition so named selections and all cannot duplicate binding/guard stages. Aggregate stages call leaf scripts, never another aggregate runner. The runner fixture driver only invokes focused suites on isolated fixtures, as defined in Task 1.

    Keep execution output simple: stage name, exit status, elapsed duration, and Lua file/case identity with nonzero executed case count. Reject an empty or malformed Lua case table. Check child process exit immediately and stop with a useful stage failure. Remove candidate-specific naming from ordinary product output. Do not build a new marker authority, timing schema, certificate, generic orchestration framework, or cache. Reuse the focused propagation fixtures; this task is glue and removal, not a domain behavior change.

    Run the focused checks below. Then try each removed selection from the prior ValidateSet once and assert a nonzero parameter-validation result before any stage begins; do not run its historical implementation. Inspect fresh tracked executable references to the two deleted roots and retired entrypoints using a literal search confined to current code/scripts/CI, excluding historical planning and retirement changelog prose. A search hit must be classified as executable dependency or harmless history; no live dependency may remain. Do not delete historical artifacts just to make text search empty, per D-06.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts_contract.ps1</automated>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery</automated>
    <automated>pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery</automated>
    <automated>git diff --check -- extensions/live_galaxy/tests tools/x4-verification tests/x4-candidates</automated>
  </verify>
  <done>The approved trees and candidate-only tests are absent, supported focused callers still work, removed selections reject, all dispatches every retained product check once, and no current executable path depends on deleted files. Rust and product runtime files remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Correct active instructions and hand off for external review</name>
  <files>.agents/skills/live-galaxy-x4-integration/SKILL.md, .agents/skills/live-galaxy-x4-integration/CHANGELOG.md</files>
  <read_first>Read the skill and changelog, .agent-instructions/x4/AGENTS.md, .agents/skills/live-galaxy-skill-evolution/SKILL.md, .agents/skills/live-galaxy-code-review/SKILL.md, and .markdownlint-cli2.jsonc. The verification registry already separates repository tests from X4 runtime evidence and does not require a retirement rewrite.</read_first>
  <action>
    Apply only the owner-approved obsolete-mandate cleanup, per D-03, D-06, and D-07. Rewrite the skill's Integration Admission, Known-Failure Gate, and affected Verification paragraphs to remove mandatory local dossier, registry/matrix, owner-override, candidate-build, and evidence-authority workflows. Route unknown X4 API/loader/lifecycle claims through the existing shared Docs MCP lookup and handoff contract. Retain source provenance and integration context, extension-relative complete imports, product XML/Lua/fake/native-binding checks, and truthful static versus observed-in-game limitations. Keep all safety, compatibility, and action boundaries.

    The skill must not instruct future agents to recreate the removed corpus or run a deleted command. Refer to the actual focused package suite and product aggregate. Preserve game-version/workload limits for future runtime claims and the shared rule that the user owns in-game actions. Do not introduce new generic rules, edit global skills, or duplicate the shared contract. Add a dated changelog entry describing this approved retirement; preserve earlier entries as history.

    Format these two owned Markdown files with the repository configuration. Do not override the intentional planning-directory ignore. Inspect the scoped diff and protected artifact content against the captured starting state, preserving unrelated edits. Keep historical HANDOFF, ROADMAP, STATE phase status, continue-here, and Rust RED files unchanged. Self-check the changed instructions, but do not substitute this check for the external code-review gate.

    Return the implementation handoff to the orchestrator with initial SUMMARY status pending_review and overall quick completion incomplete. Name the external review and final aggregate as outstanding; do not run all in this task, mark verification complete, or add the quick completion row. The orchestrator will perform review, resolve ordinary fixes within accepted scope, and run the sole final product regression as specified below. An architectural or product-scope conflict returns to the owner.

    Record Plan 09 RED as preserved and not exercised; current failure identities are unmeasured. The proposed Cargo baseline/workspace run remains unselected because no Rust, Cargo, production runtime, cross-language fixture, or bridge boundary changes. Record the approved skill cleanup and its owning skill in the initial summary, and report any additional candidate supported by the focused execution evidence. The orchestrator completes the skill-learning gate after external review and final verification. New behavioral proposals require separate owner approval. No game, save, installed mod, sibling, retained-evidence, CNG, hook, or routing action is part of this task, per D-08.
  </action>
  <verify>
    <automated>markdownlint-cli2 --fix .agents/skills/live-galaxy-x4-integration/SKILL.md .agents/skills/live-galaxy-x4-integration/CHANGELOG.md</automated>
    <automated>git diff --check -- extensions/live_galaxy/tests tools/x4-verification tests/x4-candidates .agents/skills/live-galaxy-x4-integration</automated>
  </verify>
  <done>Active instructions match the retained tests, owned Markdown and scoped diff checks pass, history and paused RED status are unchanged, and the executor hands off an incomplete quick task with pending_review status. External code review, the final product aggregate, and final closure remain explicitly owned by the orchestrator.</done>
</task>

</tasks>

## Selected verification gates

Affected layer: `x4-runtime` test tooling and project-local integration instructions. `rust-domain` and `llm` production behavior are unchanged.

| Gate | Selection and reason |
| --- | --- |
| Code review and final verification | Required after executor handoff: the orchestrator resolves external review, then the orchestrator/verifier runs the sole final aggregate. Task 3 does not claim either gate complete. |
| Focused behavioral fixtures | Required: actual package positive, independent invalid packages, and real subprocess failure propagation protect the changed paths. Source-text checks are supplementary. |
| Security review | Not separately selected: configured enforcement is off; this removes a local research authority and adds no production trust/secrets/effect boundary. Review containment, XML resolution, read bounds, and cleanup in the ordinary scoped review. |
| Mutation testing | No broad campaign: no deterministic product domain or action-safety rule changes. Use targeted broken-package and failing-stage fixtures for the changed verification behavior. |
| AI specification/eval | Not applicable: no model-facing change. |
| X4 probe and SETA soak | Not applicable: no runtime, scheduling, lifecycle, transport, or game behavior change. Existing runtime unknowns remain unknown. |
| Nyquist | Formal workflow disabled; map each quick must-have to concrete executable evidence here. |
| Cargo formatting, lint, baseline, workspace | Not selected absent a concrete cross-boundary risk. No Cargo or Rust files change; preserve paused RED and state that it was not exercised. |

## Multi-source coverage audit

GOAL and REQ are rooted in this quick CONTEXT, because no roadmap phase or phase requirement set is assigned. Historical phase goals are explicitly outside this task. All included research constraints are either implemented or have an explicit risk-based verification disposition.

| Source | Item | Task / gate | Status |
| --- | --- | --- | --- |
| GOAL | Live Galaxy tests Live Galaxy; obsolete research authority removed | 1, 2, 3 | COVERED |
| REQ | QUICK-260903-06l maintenance boundary | 1, 2, 3 and orchestrator closure; no milestone requirement/state promotion | COVERED |
| CONTEXT | D-01 fast product feedback and D-02 actual removal | 2; final regression only after review | COVERED |
| CONTEXT | D-03 Docs MCP ownership without migration | 3; execution boundary | COVERED |
| CONTEXT | D-04 preserved Lua/adapters/binding/package/persistence/Rust/RED | 1, 2, 3 | COVERED |
| CONTEXT | D-05 extract before deletion | 1 before 2 | COVERED |
| CONTEXT | D-06 history, pending game evidence, unfinished requirements | 2, 3 | COVERED |
| CONTEXT | D-07 research, checked approval, review, verification | Dispatch precondition; Task 3 pending_review handoff; orchestrator closure | COVERED |
| CONTEXT | D-08 local-only work and no game/save/sibling actions | Execution boundary; 3 | COVERED |
| RESEARCH | Lexical imports, exact-case closure, real manifests, native expectations | 1 | COVERED |
| RESEARCH | Stock bounded reads without native identity/certificates | 1 | COVERED |
| RESEARCH | Actual package and invalid manifest/import/native fixture coverage | 1 | COVERED |
| RESEARCH | Entire research island and two candidate entrypoints retired | 2 | COVERED |
| RESEARCH | Locked Lua and x4_discovery compatibility | 1, 2 | COVERED |
| RESEARCH | Retained product aggregate coverage, failure propagation, no recursive all | 1, 2; final observed stage list at orchestrator closure | COVERED |
| RESEARCH | Stale executable references and active skill mandates | 2, 3; historical references preserved | COVERED |
| RESEARCH | Private evidence, CNG registrations, caches, secrets preserved | Execution boundary; 3 | COVERED |
| RESEARCH | Exact commands, exits, elapsed time; no obsolete baseline | Verification protocol | COVERED |
| RESEARCH | Proposed Rust baseline and full run | Initial disposition in 3; review checks for concrete dependency; RED explicitly unexercised | COVERED |
| RESEARCH | No installs/frameworks; repository Markdown ignore honored | 1, 3 | COVERED |

<verification>
Run commands from the repository root. Capture each command exactly, its process exit, and wall-clock elapsed time using the command tool's elapsed result or an outer PowerShell Stopwatch; do not rely on a predicted duration. Focused checks target under one minute after Lua is available, but no measured threshold has been established and no slow command is silently treated as a pass. Investigate an unexpected delay in the focused path instead of launching the old baseline or repeating aggregate runs. A missing locked Lua executable is a prerequisite failure, not an empty pass; preserve the existing documented provisioning route without changing lockfiles.

The Task 1 fixture drivers establish executable behavior; Task 2 proves existing focused callers after deletion. Task 3 hands off with pending_review status. The orchestrator then dispatches the mandatory external code review, resolves scoped findings, and only after review convergence has the orchestrator/verifier run `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite all` as the sole final real-product aggregate regression. Record its exact command, exit status, elapsed time, and observed stage/case list in the normal quick summary/verification artifact. Its output must show every retained Lua file with executed cases, package fixtures, native binding, guard self-test and each fixed production path, persistence schema, and runner process-result checks once. The runner process-result fixture uses only isolated focused child invocations and must never invoke all. Count only outer aggregate stages when comparing the observed dispatch list. No retired stage may execute.

At orchestrator closure, review fresh scoped diffs for unintended production/Rust/state changes and classify residual literal references. Use file existence checks for the two absent roots and two removed Lua files. Read-only Git evidence and searches supplement executable checks; they do not replace them. Keep exact observed failures and unresolved risks in the quick summary. Preserve existing Plan 09 RED files and pause state without claiming a current Rust result. If review identifies a concrete cross-boundary dependency, document it and add only the affected Rust baseline/regression justified by that risk; do not implement, ignore, or weaken paused RED tests. Complete the skill-learning gate using focused execution, review, and final regression evidence before declaring overall quick completion.
</verification>

<success_criteria>
Overall quick completion is assessed by the orchestrator after external review and final verification, not by the executor at Task 3. It requires: the approved research island is removed; real product package/import/native validation remains executable; supported focused suites work; final product dispatch covers every retained check once with failure propagation; no production or Rust behavior changes; owner-approved skill cleanup is current; code review and local verification are complete with measured evidence; historical and pending work remains truthful.
</success_criteria>

<output>
The executor writes the initial .planning/quick/260903-06l-remove-obsolete-x4-research-verification/260903-06l-SUMMARY.md with status pending_review, records completed implementation and focused checks, and explicitly leaves overall completion, external review, final aggregate, and final skill-learning closure incomplete. The quick orchestrator updates that summary and its normal verification artifact only after external review convergence and the final aggregate, recording exact timing, observed stages, unchanged paused RED status, remaining limitations, and skill-learning disposition. Do not change roadmap or phase status; only the orchestrator's quick completion row is permitted after these gates pass. Planner ownership ends at this PLAN file.
</output>
