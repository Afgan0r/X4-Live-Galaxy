---
phase: quick-260903-06l
plan: "01"
subsystem: testing
status: complete
completed: 2026-09-03
requirements-completed: [QUICK-260903-06l]
completion-gates:
  implementation: passed
  focused-verification: passed
  external-code-review: passed
  final-product-aggregate: passed
  final-verifier: passed
  final-skill-learning: passed
---

# Quick 260903-06l: Product Test Workflow

Live Galaxy now runs product tests without its retired candidate research and
evidence-authority platform. The final aggregate passed in **38.059838 seconds**.
Independent review is closed and goal verification passed all six must-haves.
QUICK-260903-06l is a maintenance identifier, not a milestone requirement.

## Delivered Changes

- Removed 47 tracked files: 40 in `tools/x4-verification/`, five in
  `tests/x4-candidates/`, and two candidate-only Lua tests.
- Extracted manifest, registration, transitive Lua-import, and native-binding
  checks into product test tooling, with ordinary bounded file reads.
  C# file-identity interop, dossiers, certificates, retention and candidate
  preparation are no longer dependencies of product testing.
- Simplified the runner to direct leaf stages with explicit failures, timings,
  nonempty Lua case tables and compatible focused discovery commands.
- Updated only the owning X4 integration skill and changelog to remove obsolete
  mandatory gates and use the shared Docs MCP research boundary.

## Implementation Commits

| Commit | Change |
| --- | --- |
| `552c880` | RED actual-package contract |
| `8584afa` | Independent checker and isolated fixtures |
| `01ddbd8` | Research retirement and product runner |
| `792b125` | Approved integration-skill cleanup |
| `4dc6f0f` | Correct Lua 5.1 escape decoding |
| `2bbccc1` | Case-sensitive import graph maps |
| `57db7ed` | Current telemetry game-time expectations |

## Verification

| Exact command | Exit | Seconds | Evidence |
| --- | --- | --- | --- |
| `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite x4_discovery` | 0 | 0.505 | 10 real Lua cases |
| `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite component_discovery` | 0 | 2.863 | Binding, guard and 23 Lua cases |
| `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts_contract.ps1` | 0 | 2.847 | Positive and three failure-propagation fixtures |
| `pwsh -NoProfile -File extensions/live_galaxy/tests/package_conformance_contract.ps1` | 0 | 32.250 | 57 package checks after review fixes |
| `pwsh -NoProfile -File extensions/live_galaxy/tests/run_contracts.ps1 -Suite all` | 0 | 38.059838 | 13 distinct stages, each once and successful |

The final aggregate ran at `c81e1f5`, which adds another task's instruction-only
commit to reviewed product-test revision `57db7ed`. It executed 57 package checks,
40 Lua cases (23 component, 10 discovery, 4 telemetry, 3 scheduler), binding,
guard self-test and four production paths, persistence, and four runner fixtures.
Package fixtures took 32.114 s; the four Lua stages together took 0.041 s.
The verifier independently counted raw output and traced source wiring.

The five removed suite names reject at parameter validation. Current executable
references to deleted tooling are absent. Markdownlint passed the changed skill
files and respected the repository's planning-directory exclusion. Scoped diff
checks passed after removing one informational blank line at EOF; that final
whitespace-only cleanup does not change tested behavior and required no rerun.

Local raw output and timing metadata are in ignored
`tools/.cache/quick-260903-06l/final-all.log` and `final-all-result.json`.
The committed verification report contains the portable evidence and limitations.
Historical 688.618 s research-gate and 907.150 s focused-plus-closure measurements
are different workloads: this task removes obsolete work rather than claiming
an equivalent-work speedup.

## Review Findings and Final-Run Correction

Review found two concrete false-acceptance defects: Lua escape decoding could
check a different module name, and case-insensitive visited maps could bypass
exact-case validation. Both reproduced before fixing; independent re-review
closed them with no remaining issue. See `260903-06l-REVIEW.md`.

The initial aggregate at `2bbccc1` failed after 34.918 s on the existing telemetry
host-timestamp assertion. Historical Phase 05.1 Plan 07 already listed that exact
failure. Commit `9299f363` removed the field on August 29. The test-only correction
checks the current `runtime_facts.x4_game_time` contract, retains metadata and
scope assertions, and adds invalid game-time cases. Reviewer confirmed no skipped
or weakened test. Production code was unchanged. The initial failed log remains
in `initial-all-failure.log` and `initial-all-failure-result.json` in the same cache.
The repeated aggregate resolved this concrete failure; no old research gate ran.

## Preservation and Limits

- All 134 protected runtime/Rust/handoff files matched their pre-execution hashes.
  Protected production and Rust paths have no diff from `8b488327`.
- Historical phase records and paused Plan 09 Rust RED contracts remain unchanged
  and unexercised. No current Cargo or in-game success is claimed.
- No X4 launch, save access, installed-extension edit, sibling product change,
  retained-evidence deletion, CNG modification or routing-hook edit was performed.
- Other-task instruction commits and pre-existing dirty AGENTS/config/agent
  deletion/`.gsd` state were excluded from this task's commits.
- STATE changes only record quick completion/activity; phase status is preserved.

## Skill Learning

The approved obsolete-mandate cleanup belongs to `live-galaxy-x4-integration`
and its changelog. **Skill learning: none beyond that approved cleanup.**
Parser fidelity, exact-case validation and stale-test corrections enforce existing
correctness rules; review and final regression justify no additional mandate.

Required project contracts, plan/context/research, handoffs and matching Rust,
X4, Lua, review and skill-evolution skills were read. The executor also read
`game-repo-standard` with migration, memory-lifecycle and profiles references,
and the global git-commit skill. Independent review and verification record
their own evidence and required reads in the companion reports.
