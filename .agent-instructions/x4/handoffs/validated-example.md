# Docs MCP Validated-Example Handoff

Read this file only after an adapted X4 API use has executed successfully in
the game and its intended behavior has been checked.

Emit the following structure in chat. Do not mutate Docs MCP directly.

```text
X4 validated example

Scoped call shape: <exact API and bounded argument/result shape>
Consumer context: <repository, feature, and runtime role>
Stable code locator: <revision and repository-relative path or symbol>
Adaptation evidence: <Docs MCP entity, snapshots, provenance, conflicts>
Game context: <X4 build, extension versions, enabled dependency set>
Observed result: <execution and behavioral evidence>
Limitations: <unverified variants, context bounds, known conflicts>
```

The receipt is scoped reusable evidence, not a universal API rule. The user
decides whether and how a separate Docs MCP task publishes it.
